# Soundness Confidence — the cardinal-sin tracker

The **cardinal sin** is the **silent under-report**: candor reporting a function pure / effect-free when an
effect is actually reachable from it, with **no disclosure** (no `Unknown`, no `invisible`/blind/`incomplete`).
Disclosed uncertainty is safe, and over-reporting (a fabricated effect) is the opposite failure direction — a
precision failure, guarded by the per-engine fabrication probes, never to be confused with the cardinal sin;
the silent under-report is the one failure that breaks trust. This document is the **living instrument** for
answering: *how confident are we that candor never commits it, and how do we make that confidence go up over
time?*

## 1. Epistemic frame (what "eradicate" can and can't mean)

Purity over a Turing-complete language is **undecidable** (Rice's theorem) — every static analyzer has blind
spots. We therefore cannot *prove* zero cardinal sins. The achievable, trackable goal is the **conjunction**:

1. **Every known seam** (way an effect can be reached unseen) has a **standing CI gate** — so a closed sin
   can't silently regress.
2. **Dynamic ground truth** (the syscall oracle — the only evidence that catches *shared* blindness across all
   engines) covers a **growing fraction** of real-world effect-reaching code.
3. The **residual register** of known blind spots is **explicit and shrinking** — and every residual is either
   SILENT (a real cardinal-sin risk, must be driven to zero) or DISCLOSED (marked imprecision, acceptable).
4. The **find-rate** of fresh adversarial hunts trends to **zero** across diverse new seams (convergence
   evidence — never proof).

Confidence = all four, watched over time. Not a single number — a dashboard.

## 2. The cardinal-sin surface

A silent under-report lives at one intersection of **EFFECT × SEAM × ENGINE**:

- **Effects (10):** Net · Fs · Db · Exec · Env · Clock · Rand · Ipc · Clipboard · Log
- **Engines (6):** rust-scan · rust-deep · java · ts · swift · agents
- **Seams:** the *ways* an effect reaches a function without the engine attributing it. Confidence = coverage of
  this surface by standing evidence, weighted by evidence strength.

## 3. Evidence ladder (strongest → weakest)

| # | method | catches | limits | where |
|---|--------|---------|--------|-------|
| 1 | **Dynamic syscall oracle** | shared blindness (the unknown-unknown) — observed-but-not-predicted is undeniable | Fs/Net/Exec only (syscall-distinguishable); exercised paths only; Linux | `candor-rust/soundness/realworld/` (`oracle.sh`, `realworld/run.sh`) |
| 2 | **Independent-method differential** | coverage gaps (disclosed-but-unmodeled) | finds disclosed gaps, not silent ones, unless paired with #1 | ad-hoc (the 2026-06-18 coverage round) |
| 3 | **Adversarial seam probes** | a specific structural seam class | only the seams you think to probe | the per-engine regression tests + one-shot hunts |
| 4 | **Cross-engine generative matrix** | per-engine divergence on effect×indirection | WEAK for *shared* blind spots (engines can share a gap) | `candor-spec/conformance/` (`gen_differential.py`, 72 cells as of 2026-07-09) |
| 5 | **Recall corpus** | recall holes (known-semantics APIs) | only listed APIs | `candor-rust/soundness/realworld/recall/` |
| 6 | **§4 disclosure-invariant checker** | swallowed uncertainty (propagation bugs) | NOT blindness (a never-registered call is invisible to it) | `candor-spec/conformance/check_honesty.py` |
| 7 | **Seam fuzzer** | random structural shapes | shallow | `candor-rust/soundness/gen.py` |

**Key insight:** #4 (cross-engine agreement) is the weakest for the *dangerous* case — all engines sharing a
blind spot (the log-macro bug survived 4 engines + the differential for months). Real confidence needs #1
(external ground truth), not internal consensus. Grow #1.

## 4. Status scorecard — SEAM × ENGINE

Legend: 🟢 standing CI gate · 🟡 checked once / per-engine regression test only (no *cross-engine standing*
gate) · 🔴 unchecked · ⚫ known residual (see §5) · — N/A (immune by construction)

| seam class | rust-scan | rust-deep | java | ts | swift | agents |
|---|---|---|---|---|---|---|
| direct / local-call / method-recv / loop-elem / field / callback (6 basic indirections) | 🟢 | 🟢 | 🟢 | 🟢 | 🟢 | 🟢⁴ |
| key-collision (same-named unit clobber → wrong attribution) | 🟡 | — | — | 🟡 | 🟡 | 🟡 |
| **lazy-init (deferred initializer forced elsewhere)** | 🟢 | 🟢 | 🟢 | 🟢 | 🟢 | — |
| **top-level module code (load-time effect → initializer unit)** ⟨0.14⟩ | — | — | 🟢 | 🟢 | 🟢 | — |
| deferred-iterator (lazy seq built≠consumed) ³ | 🟡 | 🟢 | 🟡 | 🟡 | 🟡 | — |
| **fire-and-forget / spawned task** | 🟢 | 🟢 | 🟢 | 🟢 | 🟢 | 🟢⁴ |
| **gate-evasion / literal-masking (policy fail-closed)** | 🟢 | 🟡 | 🟢 | 🟢 | 🟢 | 🟡² |
| **implicit-conversion (effect via format/concat/interpolation)** | 🟢 | 🟢 | 🟢 | 🟢 | 🟢 | — |
| FFI / extern / opaque foreign call | 🟡⁷ | 🟡⁸ | 🟡⁹ | 🟡¹⁰ | 🟡¹¹ | — |
| macro / codegen reach | 🟡 | 🟢¹ | 🔴⁵ | ⚫⁶ | 🟡 | — |

¹ "🟢" here = clean/correct *by construction* (verified) — but still needs a *cross-engine standing* cell to be
truly green; today these are per-engine. ² agents = the declared-vs-observed drift gate (a different shape).
³ deferred-iterator does NOT fit the shared-compilation-unit matrix (java's whole-program CHA over
`Iterator.next()` fans out across all the cells' Iterator impls and unions every effect) — stays per-engine.
⁴ agents cannot join the shared-compilation-unit matrix (its input is a fleet definition, not code); its
analogs are CI-standing per-engine: the delegation-form chains (named / CHA / ambient + curated/uncurated
MCP sinks) are fuzzed on every push (`fuzz.py`, incl. a precision-distractor twin) and regression-pinned
(`test.py` — spawn residual, hooks matcher tiers, cron entry points) — an agent spawn IS the fleet's
fire-and-forget, and the basic-indirection analog is the delegation-form set. Lazy-init has no fleet analog
(nothing defers an initializer): N/A.
⁵ java's "—" was corrected 2026-08-27 to 🔴: `Loader.java`'s directory walk reads whatever `.class` files it's
handed, with no `generated`/annotation-processor exclusion, so a macro that rewrites BYTECODE inline (Lombok's
`@Data`/`@Builder`) is immune by construction — the rust-deep shape, verified by source inspection. But an
annotation processor that emits a SEPARATE `.java` file compiled to its own `.class` (Dagger, Room, AutoValue,
MapStruct) is a different mechanism the bytecode argument doesn't cover for free — plausible (Gradle/Maven land
those `.class` files in the same output dir the README tells users to scan) but never measured against a real
annotation-processor project. See R58.
⁶ ts's "—" was FALSE, found the same sweep as swift's row (below) — **FIXED 2026-08-28, candor-ts `0a5d493`**
(R57). A named decorator factory's effects land on its own unit, correctly (`ts.isDecorator` guards
`enclosing()` in scan.mjs on purpose, tested). An ANONYMOUS decorator expression with no top-level name
minted no unit at all: live-reproduced pre-fix, `deny Fs` over a fixture with an effectful inline decorator
exited 0 with `violations: []` — no `Unknown`, no disclosure. `localName()` now mints a position-keyed
`<decorator>@<offset>` unit for the anonymous shape before the climb reaches the guard; re-verified against
both the pre-fix and post-fix binaries in a throwaway clone, not taken on the fix's own say-so. Three
narrower, related shapes (a raw effect as a decorator argument, a closure in a named factory's argument
data, an external bodyless decorator reference) remain silent, deliberately left unfixed by the same
commit — filed as R64, not R57. See R57 and R64.
⁷ rust-scan's FFI cell was 🟡 (checked-once, no cross-engine gate) until the 2026-08-27 audit found R59
(below) — **FIXED 2026-08-28, candor-rust `905e1cd`**: a new `CALIBRATED_BUT_PARTIAL_CRATES` carves
libc/nix/rustix out of the coverage ledger's blanket `CALIBRATED_CRATES` exemption at its one consumer
site, so an unmatched call into their deliberately-unclassified generic fd verbs now discloses `invisible`
instead of vanishing; classified calls (`open`/`socket`/…) never reached the ledger at all and are
untouched. MEASURED against candor-rust HEAD: `fn drain(fd: i32) { libc::read(fd, buf, 64); }` with no
classified sibling call now reads `invisible: ["libc"]` where it previously read absent from `functions`
entirely. Corpus (1202 cached crates, standalone before/after): 97 differ (8%), every byte a new
disclosure or a previously-silent report gaining entries, zero changes to any effect field. **A same-day
follow-on audit (candor-rust `352bda3`) checked the other ~79 `CALIBRATED_CRATES` members R59's own fix
commit had left unaudited** (its stated limit at the time) and found three REAL, unrelated gaps — none of
them FFI, all three plain missing classify() rules the same silent-vanish mechanism was hiding:
`clap::Arg::env` (a real immediate `Env` read at builder time, independent of `get_matches()`'s own
classified read), `console::Term`'s raw `io::{Read,Write}` impl (`Ipc`, the same primitive `write_line`
already charges), and `arboard::{Get,Set}::file_list`/`Clear::default` (`Clipboard`, missed structurally —
the coverage-gate generator only triggers on an Fs/Net/Db/Exec self-scan signal, so a missing Clipboard
verb was invisible to it regardless of phrasing). All three fixed with concrete rules (the ambiguity that
justifies disclose-over-classify genuinely didn't apply to any of them, once checked against real vendored
source) and fixture-gated. The remaining ~76 crates were checked against real source and found complete
for this class. One filed, not fixed: `wild::ArgsOs`'s Windows-only `Iterator::next` `Fs` leak — low
reachability (invisible in the dominant `get_matches_from(wild::args_os())` idiom, structurally unseen by
the coverage-gate generator for the same reason `walkdir::IntoIter::next` needed a hand-written rule; see
R62). Direct `extern "C" { fn system(..); }` (the bindgen shape) was independently re-verified genuinely
sound throughout, unaffected by either fix — `decls.rs`'s `ForeignMod` handling records every declared name
so a call to it discloses `Unknown`/`native:extern fn`; conformance PART 79 now pins this cell, and its
byte-parity assertion against rust-deep, plus both engines' controls. Cell moves ⚫→🟡 (closed, regression +
conformance-gated, no cross-engine SEAM-matrix cell — matches java/ts's existing convention, §7b).

> **⚠ THE SENTENCE ABOVE IS FALSE FOR ONE SPELLING, AND THE CORRECTION IS WORTH MORE THAN THE FIX
> (measured 2026-09-22, [[R529b]]).** "Records EVERY declared name" and "sound THROUGHOUT" hold only for
> a `extern "C"` block at MODULE level. Written inside a function body it is a `Stmt::Item`, `collect_decls`
> recurses through `Item::Mod` and nothing else, and the caller is **ABSENT from `functions[]`** — a purity
> claim, not a disclosure. Measured on the shipped v0.39.1 binary, one variable, byte-identical programs:
> module-level `wrap` reads `Unknown` + `native:extern fn`; body-local `wrap` is absent. **PART 79 and
> PART 46 pin the module-level spelling only**, which is why re-verification kept agreeing with itself.
> Real code takes the body-local shape — wasi-0.14.2 `src/bindings.rs:184` declares `extern "C" { fn
> wit_import0() }` inside `pub fn get_stdin()`, the wit-bindgen idiom; 409 silently-pure functions across
> 73 of 1,595 registry crates gained a disclosure when it was fixed.
>
> **This is the register asserting its own safety and being wrong**, which the family has recorded 4/4
> engines doing before: nobody verifies the sentence that makes their own diff correct. A re-verification
> that re-runs the fixture the original claim was written against cannot falsify it — it has to try the
> spelling nobody wrote down. Fixed candor-rust `34ea374`; the PART arms are still module-level-only and
> need the body-local spelling four-way.

⁸ rust-deep's "🟢¹" (verified-by-construction) was NEVER MEASURED against the direct-extern seam and the
2026-08-27 audit found it FALSE (R60, below) — **FIXED THE SAME COMMIT as R59, candor-rust `905e1cd`**:
`Unknown`/`native:extern fn` now discloses unconditionally on `is_foreign_item()`, independent of the
crate-name-keyed `invisible` machinery a local (crate-less) foreign declaration has nothing to hang off.
MEASURED against candor-rust HEAD via `cargo dylint --lib-path`: the R60 fixture (`extern "C" { fn
system(..); } fn run_cmd() { unsafe { system(..); } }`) now reads `run_cmd`'s `inferred ==
["Unknown"]`/`unknownWhy == ["native:extern fn"]`, where the callgraph sidecar (`"run_cmd":["system"]`)
had already proven the HIR walk visited the call — the fix closes the disclosure gap, not a resolution
gap. The libc `invisible` disclosure (this cell's own already-sound mechanism, R59's fixture) is unmoved:
`drain` still reads `invisible: ["libc"]`, unaffected by the extern-block fix. **Conformance PART 79 pins
BOTH functions on BOTH engines and asserts the two reports are BYTE-IDENTICAL** (`inferred`/`unknownWhy`/
`invisible`, per function) — the deliberate point of choosing `is_foreign_item()`'s unconditional
disclosure over routing through crate-name machinery: rust-deep's mechanism now genuinely MIRRORS
rust-scan's on this seam, not merely produces an equivalent-looking answer by a different route. Runs
outside this suite's normal build (needs the pinned nightly + `cargo-dylint`; SKIPs loudly, never silently,
on a runner with neither — see PART 79's own comment). Cell moves 🟢¹→🟡 (closed, regression +
conformance-gated where the toolchain is available; not a cross-engine SEAM-matrix cell, §7b).

⁹ java's FFI cell was independently measured 2026-08-27 across the three real mechanisms named in the
audit (native methods, a JNA-style `Library` interface with zero local/CHA-visible implementations, and
Panama's `java.lang.foreign`/`MethodHandle.invoke`) and all three are CLOSED, sound: `ACC_NATIVE` always
discloses `Unknown` (`native:<name>`, `Candor.java`'s `registerMethod`); an interface with NO implementing
class anywhere (the JNA `Native.load(Lib.class, …)` shape — the real implementation is a runtime dynamic
proxy, never a `.class` file) resolves through the ordinary zero-CHA-target dispatch path to `Unknown`
(`dispatch:Lib.deleteAllFiles`); and a Panama downcall (`Linker.downcallHandle(...).invoke(...)`, built
`--enable-preview --release 21`, stable since JDK 22)
is caught by the REFLECTION disclosure path (`reflect:java.lang.foreign.SymbolLookup.find`,
`reflect:java.lang.invoke.MethodHandle.invoke`) rather than a dedicated FFI rule — sound, not by
construction-reasoning but by measurement against `build/libs/candor-java-0.33.1-all.jar` on JDK 21.
Stays 🟡 (measured sound, no cross-engine gate — this seam has no shared-compilation-unit shape, §7b).

¹⁰ ts's FFI cell was independently measured 2026-08-27 across four mechanisms (a literal
`require('./addon.node')` with an untyped member call; the same addon typed via a co-located `.d.ts`
ambient declaration — the realistic N-API shape, e.g. `better-sqlite3`/`sharp`; `WebAssembly.instantiate`
+ a call through `instance.exports`; and legacy `process.binding(...)`) and all four are CLOSED, sound: the
untyped case falls through the generic unresolved-call-on-an-untyped-value path (`callback:addon.wipeDisk`);
the typed ambient declaration is caught by the SAME body-less-declaration disclosure PART 46 (R46, ⟨0.24⟩)
already ships for any ambient/`.d.ts` signature (`native:wipeDisk`) — an N-API addon's `.d.ts` is
structurally identical to axios's, so the fix that closed that vein closes this one too, for free; WASM
export calls and `process.binding` both fall through the same untyped-value callback path. Measured against
`scan.mjs` at candor-ts HEAD (`fbb9ea2`). Stays 🟡 (measured sound, no cross-engine gate).

¹¹ swift's "—" (immune by construction) was FALSE, found by the SAME 2026-08-27 audit (R61, below) and —
unlike R56's same-shaped macro finding, fixed the same day it was found — **FIXED 2026-08-28, candor-swift
`bcb4a4e`**. Three independently-idiomatic C-interop mechanisms had all read silent-pure, zero disclosure:
(1) a raw POSIX/Darwin/Glibc free function reached via `import Darwin` (`system(..)`/`unlink(..)`), (2)
`@_silgen_name("system")` (Swift's compiler-level direct-symbol-linkage declaration, bypassing `import`
entirely), and (3) `dlopen`/`dlsym` + `unsafeBitCast` to a `@convention(c)` function type, then calling it.
The MODELED `Process`/`Foundation` API stayed correctly charged throughout every measurement (control,
unaffected). Fixed with ONE shared vocabulary (`Unknown`/`unknownWhy`, never a fabricated concrete effect)
across two mechanisms: (1) and (2) now disclose `Unknown`/`native:<symbol>`, gated on a new ALLOWLIST of
specific real syscalls (`NATIVE_DISCLOSURE_C_FREE_FNS` — `system`/`unlink`/`mkdir`/`rename`/`fork`/
`dlopen`/`dlsym`/… — the same bare words `kappaFree`'s own comment already excludes as collision-prone)
plus a `C_PLATFORM_MODULES` import check (`Darwin`/`Glibc`/`Musl`/`WinSDK`), never a denylist — the first,
unrestricted cut MEASURED 1519 false hits on swift-nio alone (`#if os(...)`/`canImport(...)` build-config
predicates, `assert`/`fatalError`, bare operators, none of them FFI), the exact false-positive class that
made an allowlist the deliberate exception to this codebase's usual denylist-over-allowlist rule; (3)
routes the `unsafeBitCast`-bound local through the SAME `opaqueFnLocals` machinery a stored opaque closure
property already uses, disclosing `Unknown`/`callback:<name>` — reused infrastructure, not new. MEASURED
against candor-swift HEAD: `import Darwin; func f() { system("rm -rf /tmp/x") }` now reads
`inferred == ["Unknown"]`/`unknownWhy == ["native:system"]` where it previously read absent from
`functions` entirely; the `Process`-based control is byte-unmoved. Full pre/post binary diff over the
13-package corpus and candor-swift's own source: function-count-identical outside 3 genuine new hits in
swift-syntax (a real `dlopen` in its plugin loader, a real `kill` in its CLI reducer, one more
`unsafeBitCast` site doing runtime metadata introspection — all three spot-checked against source). **The
allowlist is DELIBERATELY INCOMPLETE, stated as this fix's own limit, not a general closure claim**: an
unnamed raw Darwin/Glibc call outside `NATIVE_DISCLOSURE_C_FREE_FNS` still reads silent, the same
under-report-the-rare-case trade-off `kappaFree`'s own comment already accepts for its concrete
classification. Conformance PART 79 now pins the raw-import and `@_silgen_name` shapes plus the Process
control. Cell moves —→🟡 (closed for the allowlisted/attributed shapes, regression + conformance-gated; not
a cross-engine SEAM-matrix cell, §7b — matches java/ts's existing convention).

**Where the scorecard stands (2026-07-09; macro/codegen row corrected 2026-08-27; FFI row corrected
2026-08-27, same sweep):** 🟢 is the plurality — the basic-indirection, lazy-init, fire-and-forget,
gate-evasion and implicit-conversion rows are cross-engine-standing (§7b). The remaining 🟡/🔴/⚫ rows —
key-collision, deferred-iterator, FFI, macro — are per-engine by their nature (§7b: deferred-iterator can't
share a compilation unit across engines; FFI has no clean ts idiom; key-collision and macro are
engine-structural seams), each held by standing per-engine regression tests, plus rust-deep's gate-evasion
cell (the policy differential gates the user-facing engines; deep's policy surface stays per-engine). **The
macro/codegen row's four non-rust cells had never been independently reasoned about — the surrounding prose
only ever discussed rust's `macro_rules!`/`cfg_if!`, and "—" was pasted across java/ts/swift/agents
identically since the row's first commit.** swift's was a FALSE immunity claim (a real silent under-report,
FIXED same day the mistake was found — `94d1aef`, R56); ts's was ALSO false, fixed the next day (`0a5d493`,
R57, a narrower residual filed as R64); java's
construction argument holds for one mechanism (Lombok) but is unmeasured for another (R58); agents has no
macro/codegen concept at all (a fleet definition, not source) and its "—" stands. The roadmap (§7) tracks
what's left. **The FFI row had the identical shape — four non-rust cells never independently measured, two
of them (rust-deep's "🟢¹", swift's "—") simply wrong — and the 2026-08-27 audit found: rust-scan's direct
`extern` path is genuinely sound but its `libc`-calibrated generic-fd-verb path was a live silent sin (R59);
rust-deep, the engine billed as "the sound gate" for exactly this kind of miss, silently dropped a local
`extern "C"` declaration its own callgraph proved it visited (R60) — WORSE than rust-scan on the same
mechanism; java and ts are both genuinely CLOSED across every mechanism tested (native methods / JNA-style
zero-impl dispatch / Panama for java; native addons both untyped and `.d.ts`-typed / WASM / legacy
`process.binding` for ts); swift's "—" was as false as its macro row (R56) (R61); and agents genuinely has
no FFI concept (a fleet definition has no foreign-function boundary) so its "—" stands, unlike the other
five cells which all moved.** **All three open sins (R59, R60, R61) were CLOSED the very next day
(2026-08-28)** — R59/R60 together in candor-rust `905e1cd` (one commit fixes both engines, since they were
independently broken in opposite, complementary ways on the identical fixture), R61 in candor-swift
`bcb4a4e`, each verified against the fix commit rather than the author's own summary of it, and a same-day
follow-on audit of the ~79 other `CALIBRATED_CRATES` R59's own fix had left unaudited (candor-rust
`352bda3`) found and closed three further real gaps, none of them FFI (R62) — see footnote ⁷. None of the
three fixes claims general closure, stated as each fix's own limit: rust-deep's `invisible` disclosure is
still crate-name-keyed everywhere OUTSIDE the local-extern-block seam R60 closed; rust-scan's coverage
ledger still exempts the ~76 other `CALIBRATED_CRATES` members the R62 audit spot-checked and found
complete for THIS class, but did not exhaustively re-verify against every future classify() change; and
swift's `NATIVE_DISCLOSURE_C_FREE_FNS` is a deliberately incomplete allowlist — an unnamed raw Darwin/Glibc
call outside it still reads silent. Conformance PART 79 pins the R59/R60 byte-parity shape across rust-scan
and rust-deep plus swift's two allowlisted mechanisms, falsified against the pre-fix commits (candor-rust
`250851b`, candor-swift `21f1240`) before landing.

## 5. Residual register (known blind spots)

Each is **SILENT** (true cardinal-sin risk → drive to zero) or **DISCLOSED** (engine emits Unknown/invisible —
lower priority). Eradication = SILENT count → 0. Closed rows keep a one-line summary here; the full prose for
the essay-sized ones lives in [SOUNDNESS-LOG.md](SOUNDNESS-LOG.md).

| id | engine | residual | kind | severity | plan |
|---|---|---|---|---|---|
| ~~R1~~ | rust-deep | implicit-conversion class — **RESOLVED 2026-06-18**: empirically already covered, not a residual | ~~SILENT~~ CLOSED | — | probe `candor-rust/ui/implicit_conversion.rs` (13-warning regression fixture) confirms all 6 sub-cases (format/Display·`?`→From·`.into()`·auto-deref·operator·Drop-glue) charge the effect + 4 pure controls stay pure. The type-aware HIR walker resolves these natively (fmt via the explicit "HOLE 2"); the scan 0.5.16 fix was the *syntactic* engine's counterpart, never needed in deep. |
| ~~R28~~ | swift | conditional conformance on a stdlib collection (`extension Array: Saveable where Element: Saveable` via `xs.persist()`) read silent-pure — two coupled gaps. **FIXED 2026-07-11** (0.8.10): (a) an array receiver edges to the local `Array.<member>` extension unit via a soft resolveQual edge (std methods drop silently, no spurious Unknown); (b) a bare `forEach { $0.method() }` over self types `$0` as the extension's `where Element: P` bound (FnInfo.selfElementType + a selfElementStack). Pure conditional conformance stays pure. | ~~SILENT low~~ CLOSED | — | SOUNDNESS-LOG.md, 2026-07-11 R28 entry; gate `testConditionalConformanceOnArrayCollectionDispatches` |
| ~~R29~~ | swift | `@resultBuilder` transform read silent-pure — a func `@SomeBuilder` runs `SomeBuilder.buildBlock(…)` etc (implicit, no call site), so an effectful builder was dropped. **FIXED 2026-07-10** (0.8.9): edge the annotated func to the builder type's `build*` units; a pure builder adds nothing. | ~~SILENT v.low~~ CLOSED | — | SOUNDNESS-LOG.md, 2026-07-10 non-accessor-seam sweep |
| ~~R30~~ | rust-scan | trait DEFAULT method via an empty impl read silent-pure (`impl Logger for S {}` + `s.log()` inheriting `Logger::log`). The caller-fallback already existed but was GATED OUT: a type whose only impl is an empty trait impl has no fn unit, so it was absent from `local_types` → its typed call was un-resolvable. **FIXED 2026-07-11** (candor-scan 0.8.8, 2 lines): register every trait-impl type as local. Override wins (no fabrication); pure default stays pure. | ~~SILENT low-med~~ CLOSED | — | SOUNDNESS-LOG.md, 2026-07-11 R30 entry; gate `trait_default_method_via_empty_impl_charges_the_default_body` |
| ~~R31~~ | rust-scan | a **bounded-generic struct field** read silent-pure — `struct Pipe<T: Saver> { item: T }` reaching `self.item.save()`: field types resolved with an empty generic-bounds map, so `T` never resolved to `Saver`. **FIXED 2026-07-10** (candor-scan 0.8.7): seed the struct's own `<T: P>`/`where T: P` bounds into field trait-leaf resolution; unconstrained field read stays pure. The swift R27 analog. | ~~SILENT low-med~~ CLOSED | — | SOUNDNESS-LOG.md, 2026-07-10 cross-engine sweep; gate `generic_struct_field_resolves_to_its_trait_bound_dispatch` |
| R2 | rust-scan | auto-deref *method* calls (`w.method()` via Deref::Target) | SILENT | low | needs target-type method resolution (syntactic limit) |
| R3 | rust-scan | untyped-operand implicit-conversion (format/operator over an unresolved type) | SILENT | low | syntactic limit; accepted residual (no flood vs precision tradeoff) |
| R4 | rust-scan | bare-unit-struct iterate/drop (`for _ in Unit {}`, `let _g = Unit;`) | SILENT | v.low | rare idiom |
| R5 | rust-scan | general unresolvable-bare-call → Unknown REJECTED (floods 80/tokio) | SILENT | low | needs provenance (extern/glob) to disclose without flooding |
| R6 | rust-scan | multi-impl ambiguity, compound-assign operators | SILENT | v.low | deep was probed (round 13, 2026-06-18) — sound, gated; the residual may hold for scan only |
| R7 | swift | untyped-operand implicit-conversion | SILENT | low | syntactic limit |
| ~~R26~~ | swift | generic-constrained dispatch via a **`where T: P` clause** read silent-pure — only the inline `<T: P>` bound was collected into genericBounds, so `func f<T>(_ x: T) where T: P { x.method() }` didn't dispatch. **FIXED 2026-07-10** (0.8.9): collect where-clause conformance requirements too; gated. | ~~SILENT low-med~~ CLOSED | — | SOUNDNESS-LOG.md, 2026-07-10 generic-dispatch entry |
| ~~R27~~ | swift | a **type-level generic bound** on a stored field read silent-pure — `struct Box<T: P> { let x: T }` reaching `x.method()`: the field typed `T` wasn't resolved to its bound `P`, so the (already-working) protocol-typed-field dispatch never fired. **FIXED 2026-07-10** (0.8.9): record type-level generic bounds (struct/class/enum/actor) + resolve a field typed as a bounded param to its bound; gated. | ~~SILENT low-med~~ CLOSED | — | SOUNDNESS-LOG.md, 2026-07-10 generic-dispatch entry |
| ~~R24~~ | swift | property-wrapper `projectedValue` via `$` access (`m.$name`) read silent-pure — the `$`-prefixed access didn't edge to `<Wrapper>.projectedValue`. **FIXED 2026-07-10** (0.8.9): mirror the `wrappedValue` edging for `$name` (CallCollector property-read visitor); gated. | ~~SILENT low~~ CLOSED | — | SOUNDNESS-LOG.md, 2026-07-10 accessor-vein sweep |
| ~~R25~~ | swift | keypath read of an effectful computed property (`h[keyPath: \.data]`) read silent-pure — the implicit-root keypath resolver handled only the element-iterator form (`xs.map(\.p)`), skipping the `[keyPath:]` subscript application (root = receiver's OWN type). **FIXED 2026-07-10** (0.8.9): resolve the subscript-applied keypath to the member's accessor unit; gated. | ~~SILENT low~~ CLOSED | — | SOUNDNESS-LOG.md, 2026-07-10 accessor-vein sweep |
| R8 | java | container-erased `compareTo`/**`equals`/`hashCode`** reentry (element/argument type erased) — a JDK collection method reaches an *effectful override* through a value whose concrete type is erased at the call site. **Confirmed in the wild 2026-07-20** by the confirmatory-corpus oracle: `commons-collections4` `AbstractMapDecorator.equals` (`return decorated().equals(object)`) read PURE, but at runtime the delegated `HashMap.equals` calls `object.get(k)` on a `PassiveExpiringMap` argument → `Clock` (minimal repro pinned, `eval/corpus-confirmatory`). candor is modular (no whole-program points-to) so cannot recover the erased argument type; the sound-but-imprecise alternative (disclose `Unknown` for every `.equals`/`.compareTo` on a non-final receiver) floods, so this stays a documented boundary. | SILENT | low | needs element/argument-type recovery (points-to) — sound fix floods; documented per candor's ethos |
| R9 | java | okio buffered read/write on an ambiguous BufferedSink | DISCLOSED | n/a | by design (Buffer-vs-socket ambiguous; construction boundary modeled) |
| R10 | ts | `@types/uuid` v8 intersection-typed `v4`; googleapis deep service verbs | DISCLOSED | n/a | reads Unknown (disclosed); modern uuid fixed |
| R11 | agents | seam battery run (2026-06-18): named-delegation-narrowing was UNSOUND (narrowed on a prompt mention, not a proof) — FIXED candor-agents 0.4.13 (`71a6cc7`): declared `Agent(x,y)` allowlist narrows soundly; bare `Agent`+mention discloses an Unknown spawn residual; bare `Agent`+no-mention is CHA. Delegation forms / MCP-Unknown / hooks+cron entry points already covered (fuzz.py + test.py). | was UNCHECKED → mostly covered | low | remaining: allowlist naming a non-existent agent (unresolvable spawn → Unknown?); deeper hook-matcher adversarial cases |
| ~~R12~~ | rust-deep | CI self-guard ICE (nightly-2026-04-16) blocked continuous self-gating. **CLOSED 2026-07-09** (verified against the repo): the pin moved to nightly-2026-06-14, ci.yml's Self-guard step runs the deep engine on every push, `ci/self-gate.sh` gates on the STABLE scanner (never nightly-blocked), `realworld-oracle-deep.yml` runs the deep engine against the kernel oracle on every push/PR, and `nightly-bump.yml` automates the weekly nightly migration. | ~~infra~~ CLOSED | — | continuous self-gating is a standing CI property |
| ~~R13~~ | rust-deep | `thread_local!` force via `KEY.with(...)` read PURE (effect orphaned in the macro-gen init fn). **FIXED 2026-06-18** (`6010832`); gated by ui/thread_local_effects.rs. | ~~SILENT med~~ CLOSED | — | prose: SOUNDNESS-LOG.md, 2026-06-18 thread_local entry |
| ~~R14~~ | rust-deep + rust-scan + swift (SYSTEMIC shared blind spot) | the WRITER side of formatting read PURE — an effectful custom sink driven by a non-local format helper was dropped; silent in three engines at once, the exact case cross-engine agreement hides. **ALL FIXED 2026-06-18** (deep `0e4bf50`; scan `dabafd0` 0.5.18; swift `9368311` 0.5.22); java analog = R16; ts N/A. | ~~SILENT~~ CLOSED (3 engines) | — | prose: SOUNDNESS-LOG.md, 2026-06-18 write-fmt entry |
| R15 | — | *(number never assigned — retired to keep later ids stable)* | — | — | — |
| ~~R16~~ | java | writer side of formatting — a custom effectful `Appendable`/`Writer` driven via a JDK `Formatter`/`PrintWriter` read PURE (the R14 class, 4th engine). **FIXED 2026-06-18** (0.5.40 `efc88f5`, constructor-site reentry); the write-fmt writer-side class is closed in all 4 engines. | ~~SILENT~~ CLOSED | — | prose: SOUNDNESS-LOG.md, 2026-06-18 write-fmt entry |
| ~~R17~~ | java | I/O via an ABSTRACT `java.io` stream param at a rooted entry point read PURE, not Unknown (the jsoup streaming-parser pattern). **FIXED 2026-06-21** (provenance-gated, entry-point-scoped; `R17AbstractStreamTest`); the transitive and getter-return shapes were measured/probed sound — surface genuinely near-empty. | ~~SILENT~~ CLOSED | — | prose: SOUNDNESS-LOG.md, 2026-06-21 R17 entry |
| ~~R18~~ | java | the **inherited-into-project silent-pure vein** (κ batches 25–27): a framework method inherited into a PROJECT type (Panache active-record; repo/base-class mixins — Micronaut Data, Ebean, ActiveJDBC, jOOQ; then ANY classify-modeled base) read silent-pure — the call owner is a project class, so neither the κ-floor invisible disclosure (external owners only) nor CHA (no project body) fired. **FIXED 2026-06-21** (`fdd2278`/`93c3116`/`699c1ea`) — the vein CLASS closed for modeled + unmodeled bases; cross-engine check: java-specific, not shared. | ~~SILENT~~ CLOSED | — | full prose: SOUNDNESS-LOG.md, batches 25–27 |
| ~~R19~~ | java | **six classifier regressions shipped in 0.8.3** (the κ batch 28–31 breadth invited them: name/prefix owner gates + bare verb prefixes fabricating on mixed namespaces; a repo-promotion silent-pure; descriptor rules matching the RETURN position) — found by the 2026-07-08 review, NOT by CI. **FIXED 0.8.4** (`18717b5`); the kappa_libs/mutation probes move into scheduled CI so breadth regressions stop relying on review. | ~~mixed~~ CLOSED | — | SOUNDNESS-LOG.md, "0.8.4 review patch" |
| ~~R20~~ | swift | **UserDefaults / Keychain `SecItem*` / Bundle resource lookups read silent-pure under the covered-module floor** (`Foundation`/`Security` in PLATFORM_MODULES vouch for everything, so a member gap is absolute — the Panache shape, Swift edition; ubiquitous iOS persistence invisible to the gate). **FIXED 2026-07-09** (`dd134e2`; all → Fs, verb-precise, twin-gated). | ~~SILENT~~ CLOSED | — | SOUNDNESS-LOG.md, swift κ batch |
| ~~R23~~ | swift | **setter `newValue` read silent-pure**: an effect reached THROUGH a setter's implicit value param (`set { newValue.write(toFile:) }` on a computed property/subscript, or a `willSet`) was dropped — `newValue` was never typed, so a member call on it didn't resolve. Hit computed-property + subscript setters, `willSet`, and renamed params `set(v)`. **FIXED 2026-07-10** (0.8.8): the accessor unit's `newValue`/named param is seeded with the property/subscript element type. `newValue`-as-arg to a resolved call already worked (the receiver case is the hole); pure setter stays pure. `==`/`+`/subscript-getter probed sound; ts/kotlin/rust use explicit typed setter params — swift-specific. | ~~SILENT low-med~~ CLOSED | — | prose: SOUNDNESS-LOG.md, 2026-07-10 setter-newValue entry; gate `DriverResolutionProcessTests.testSetterNewValueIsTypedSoEffectsThroughItResolve` |
| ~~R22~~ | swift | **inherited property accessors read silent-pure**: an effectful computed property / `didSet`-`willSet` observer / subscript whose BODY lives on a superclass was dropped when accessed through a subclass (`d.payload`, `s.name=x`, two-level) — property-edge resolution matched only the OWN type's `Type.member` unit while the method-call path already climbed `supertypesOf` (the R18 vein, property edition). **FIXED 2026-07-10** (0.8.7): property edges now climb `supertypesOf` (transitive; override-wins so no fabrication; pure inherited stays pure). candor-ts + java checked SOUND (not shared). | ~~SILENT med~~ CLOSED | — | prose: SOUNDNESS-LOG.md, 2026-07-10 inherited-property-accessor entry; gate `DriverResolutionProcessTests.testInheritedPropertyAccessorEffectsClimbTheHierarchy` |
| ~~R21~~ | family porcelain | the **2026-07-09 whole-project review's fail-open class**: gate surfaces converting "the gate could not run" into green — cargo-candor `policy` build-failure swallowed by a shell or-true fallback (spelled out in SOUNDNESS-LOG.md's 2026-07-09 entry, not here — a literal pipe here breaks this row's column count) and `guard` with no baseline ever snapshotted; java `--gate-json` unwritable → exit 0, `CANDOR_DEPS` typo silently ignored; ts MCP `candor_whatif` bad policy path + configured-but-empty policy falsy-skipped; agents `gate_reports` fail-open dead code + undisclosed observed-paths truncation. The engine-level fail-closed doctrine had never swept the OUTPUT/auxiliary channels or the shell porcelain. **FIXED 2026-07-09** (same-day wave, per repo; conformance PARTs 14–15 + wrapper CI lanes added as standing gates). | ~~fail-open~~ CLOSED | — | prose: SOUNDNESS-LOG.md, 2026-07-09 whole-project review entry |
| ~~R32–R44~~ | ALL FOUR (cross-engine dispatch era, 2026-07-18) | **~20 trait-object / dynamic-dispatch cardinal sins** found + FIXED across every engine: provided-method→override (R32, four-way), swift deinit-glue / generic-operator / @dynamicCallable / generic-array / super-protocol (R33–R35, R39, R43-swift), rust trait-default / dyn-collection-param/generic/field / method-returns-dyn(-collection) / supertrait (R36, R37/R37b/R40, R42/R44, R43), java unbound-interface-method-ref (R38), container/Option dispatch four-way (R41). Each regression-gated + corpus-A/B'd (genuine jsoup Clock + pgman Log recoveries) + four-way-conformance-clean, on 0.22. Two halves done in PARALLEL via subagents (R41, R43). | ~~SILENT (systematic, shared)~~ CLOSED | — | full prose: SOUNDNESS-LOG.md, 2026-07-18 entries (R32→R44) |
| ~~R45~~ | rust-scan | **blanket impl** `impl<T: Bound> Ext for T` + `x.ext()` — a blanket impl isn't in the concrete-keyed CHA universe, so an effectful blanket body reads silent-pure. Auto-resolving risks fabrication (a blanket method leaf edged onto any unresolved same-name call) + needs a threaded blanket index. | ~~SILENT~~ CLOSED | — | **FIXED 2026-07-19 (`7dea9e8`)**: cache-threaded `blanket_methods` (leaf→self-param); a TYPED unresolved `x.leaf()` edges to the blanket body `T::leaf`. Inherent method wins (regression); A/B zero fabrication across futures-util/syn/hyper/h2/tower. Effectful blanket bodies stay rare — vein closed for when they occur |
| ~~R46~~ | rust-scan | dispatch long-tail: nested `Vec<Option<Box<dyn>>>` / `Option<Vec<Box<dyn>>>`, tuple-destructured dyn factory return, `Default::default()` turbofish. | ~~SILENT~~ CLOSED | — | **FIXED 2026-07-19** (`b7a0433` nesting + `4b3aa03` tuple): composing `elem_trait_leaves` peel + `trait_vars` carry (both nesting orders); the tuple sub-case via `tuple_trait_of` (param) + inline-cast extraction + a `<tupledyn>` return sentinel (factory). Turbofish already worked. FULLY closed, all sub-cases |
| ~~R47~~ | ts | a sub-interface SUPER-method (`s.base()` on `s: Sub`, `interface Sub extends Sup`) read Unknown, not the precise Fs. | ~~DISCLOSED~~ CLOSED | n/a | **FIXED 2026-07-19 (candor-ts `94d1658`)**: register an impl class under its interface's transitive SUPER-interfaces (climb `extends`) so the super-method CHA finds it. The ts sibling of the rust/swift supertrait dispatch; 553 tests + fabrication probe + conformance OK |
| R53 | rust-scan | **trait-qualified UFCS call reads pure** (fix ATTEMPTED then REVERTED on code review — the typed `T::method` edge could resolve to T's INHERENT `method` shadowing a trait default → fabrication; no cheap gate, low value). Original text: — FIXED 2026-07-18 (`2a1ca07`): a precise typed `T::method` edge from the qself (`<T as Trait>::m`) or first-arg type (`Trait::m(&t)`), gated to `&self` trait methods (LocalTrait.methods filtered to receivers) so an associated fn never mis-reads its data arg. A/B zero concrete over-fire ~2600 fns; : `Runner::go(&t)` and `<Task as Runner>::go(&t)` (t: Task, `impl Runner for Task` effectful) drop silent — the INHERENT form `Task::go(&t)` resolves, but the TRAIT-qualified form doesn't (no fn named `Runner::go`; the impl is `Task::go`). FABRICATION SUBTLETY: unlike `dyn Runner` dispatch (runtime type unknown → CHA-over-all-impls is the sound over-approximation), a UFCS call's receiver type is STATICALLY known (the first arg / the `<T as ..>` type), so CHA-over-all-impls would OVER-CHARGE a multi-impl trait (edging B::go/C::go onto a call that only hits Task::go). Safe pre-designed fix: resolve via the first-arg type (`&t`→Task→`Task::go`), OR edge only when the trait has EXACTLY ONE local impl (unambiguous, the R36-default discipline). Deferred: low incidence (4–8 `<T as Trait>::` sites per large crate, nearly all std-trait disambiguation — `Future`/`From`/`Default` — which candor leaves as external; a LOCAL effectful-trait UFCS target is rare). | SILENT (open) | low/niche | real but rare; the fix must stay precise (arg-typed or single-impl) — never CHA-all a statically-known-receiver call, that fabricates |
| ~~R52~~ | rust-scan | **var-clone rebind lost the pointee type**: `let a = Arc::new(Svc); let b = a.clone(); b.run()` read pure — `a.clone()` is (correctly) the pure `Arc::clone`, NOT charged, but `b` got no type (`ctor_type` doesn't consult `vars` for the clone receiver), so `b.run()` dropped. **FIXED 2026-07-18** (`84c3dc8`): at the `let` binding, an untyped `<expr>.clone()` init carries `resolve_recv_type(<expr>)` to the binding (clone is type-preserving `-> Self` → sound; the clone call stays uncharged, so the anti-fabrication guard `..._but_not_clone` is untouched). Turned out HIGHER-value than estimated — the `let self_ = self.clone(); self_.call_async()` / `let mut cmd = self.cmd.clone(); cmd.build()` idiom is pervasive in tower/hyper `Service::call` + command builders. A/B RECOVERED real silent-pure, all honest (0 concrete fabrication, 0 removed): hyper-util `HttpConnector::call`→[Log,Unknown] (confirmed self.clone→call_async→trace!+connect), clap +30 (Command dispatch through `Box<dyn TypedValueParser>`). | ~~SILENT~~ CLOSED | — | regression `smart_pointer_ctor_..._dispatch` (via_clone Fs, clone_pure control) |
| ~~R49~~ | rust-scan | **a local effectful-`Drop` guard held as a struct FIELD** — constructing an owner `struct Session { _g: Guard }` (or `Vec<Guard>`, or a nested owner) runs `Guard::drop` at the owner's scope exit, but reads silent-pure. The DIRECT case (a local OF the drop-type) IS charged; only the field/transitive case is missed (rust analog of swift's R33 deinit-glue, field edition). **[CORRECTED 2026-08-31 — that sentence is MEASURABLY FALSE as a general claim, and this row's CLOSED status is narrower than it reads. See R66.](#r66)** The field cell IS genuinely closed at HEAD (verified by execution: field owner via assoc ctor, `Vec<Guard>` field, nested owner, field-literal-in-a-`let` all charged) — but only along the two routes that existed when it was written. "The DIRECT case IS charged" holds for `Guard::new()` and for `Guard { .. }` bound to a `Pat::Ident`, and is FALSE for a TUPLE-STRUCT (newtype) guard in every position including the bound local, and false for a brace-literal guard in 16 of 21 executed positions. Measured: `struct BGuard{id}; let _g = BGuard{id};` → `['Fs']`; `struct TGuard(String); let _g = TGuard("x");` → ABSENT. `eat(Session { _g: Guard{..} })` and `let (_s,_n) = (Session{..}, 1)` are also silent — the closure inherits the same syntax/binder limits it was built on. **A prototype fix (a transitive drop-owner closure over `drop_types` × `fields`/`field_elem`, no new cache plumbing) was BUILT + regression-tested green, then REVERTED on the A/B gate**: it fabricated **14 false `Unknown`s on flate2** (`Compress::new`/`Decompress::new`/… — constructors that CONSTRUCT AND RETURN the owner, whose owned `Stream`'s FFI Drop runs in the CALLER's scope, not the constructor's). Field-owners are overwhelmingly constructed-to-be-RETURNED (resource wrappers), so the returned-value escape case DOMINATES this vein — unlike direct guards (constructed-to-be-used-locally), where the same over-approximation is rare/accepted. A correct fix needs a returned-value ESCAPE GATE (swift-R33 style), which needs return-type info not cheaply available (FnInfo carries none; the `returns` index is leaf-keyed so `::new` collides and drops as ambiguous) — i.e. a new FnInfo return-type field or collector-side escape analysis. **Zero corpus incidence for direct effects** (dozens of crates; the only real local-Drop crate, flate2, flushes to a generic writer, not a classified effect). | ~~SILENT~~ CLOSED | — | **FIXED 2026-07-18 (`7c719bf`)**: transitive drop-owner closure + a RETURN-ESCAPE gate (conservative membership check on `FnInfo.ret_idents`, sound under flate2's leaf-name collisions; `-> Self` resolved). ADDITIVE-only (direct case untouched → no under-reports); A/B zero concrete fabrication + zero removed across flate2/syn/h2/tokio-util/…; regression owns_field/nested/vec charged, make_* escape-gated |
| ~~R48~~ | rust-scan | a **local `macro_rules!` whose template does DIRECT I/O** (`macro_rules! do_io { () => { fs::write(..) } }`) reads pure — `visit_macro` scans the invocation's ARG tokens (catching effects in a caller-passed `$expr`) and expands `cfg_if!`, but never the macro DEFINITION's template. Metavar templates (`$m`) that interpolate a caller expr ARE already caught (the effectful call comes from the arg). Rust-specific (java/swift/ts lack C-style macros). **Zero real-world incidence**: across dozens of local-corpus crates (flate2, commons-io, dotenv, …) 8 `macro_rules!` total and NONE with a direct-I/O template — the only hits are the synthetic probe. Pre-designed fix (deferred until it appears in real code): collect `macro_rules!` templates into a cache-threaded `local_macros` map → at a bare local-macro invocation `$`-strip the arm template + parse-or-skip as a Block + inline-`visit_block` (parse-or-skip only ever ADDS visibility, never fabricates; repetition `$(..)*` arms skip). | ~~SILENT~~ CLOSED | — | **FIXED 2026-07-18 (`c6f32de`)**: collect macro templates (cache-threaded), $-strip + parse-or-skip + inline-visit. FAR higher-value than 'zero incidence' — recovers the pervasive local-logging-wrapper pattern (`macro_rules! trace { .. tracing::trace!(..) }`): tokio-util +4 Log, h2 +8 Log, zero fabrication. Regression + conformance OK |
| ~~R54~~ | rust, java, ts (all three that ship it) | **`diff` has no completeness reader at all** — it answers `{ baseline_version, engine_version, changes: [] }` at exit 0 with no caveat on either channel, over reports that declare unread source or an unread `excluded` class. `changes: []` reads as *nothing about this codebase's effects changed*, asserted across two reports nobody fully read. Found 2026-08-25 while closing the `callers`/`impact`/`path` half of the ⟨0.32⟩ descriptive-hedge class (candor-rust `dcb2a71` + siblings), on that fixture, and deliberately NOT fixed with them: it is a different rung, not a fourth spelling of the same one. **FIXED 2026-08-26**: ported `gains`' own PREFIXED shape (`incomplete` + `baselineIncomplete`) rather than inventing a fourth spelling for the same dual-locator hedge — candor-java `a73109b`, candor-ts `9790ce3`, candor-rust `173a740`. | ~~SILENT (open)~~ CLOSED | — | commits candor-java `a73109b`, candor-ts `9790ce3`, candor-rust `173a740`; each reuses `gainsCompletenessFields`/`gainsCompleteness` verbatim rather than mint a fourth spelling for `diff`'s identical two-report shape |
| ~~R55~~ | rust | **`receipt` (TSV) has no completeness reader either** — its `fns/effects/unresolved/…` rows are a pinned CONSUMER surface, emitted with no caveat over an incomplete report. Same 2026-08-25 sweep, same fixture, also deliberately not fixed. **FIXED 2026-08-26 (candor-rust `eb67caf`)**: a new `incomplete\ttrue` key-tab-value ROW, appended only when the report is incomplete — tie-broken by reading this format's one real consumer (`candor-run.sh`'s `while IFS=$'\t' read -r k v; do case "$k" in …` loop) rather than guessing between the three candidates below, since an extra COLUMN was measured to corrupt `read -r k v`'s positional split. rust is the only engine that ships a `receipt` verb at all — java and ts have none, and swift has no query verbs — so this closes the vein rather than leaving three siblings to port. | ~~SILENT (open)~~ CLOSED | — | commit candor-rust `eb67caf`; also closes PART 70's ⟨0.33⟩ cross-policy cell |
| ~~R56~~ | swift | **the §4 macro/codegen row's "—" was a FALSE immunity claim** — a Swift compiler-plugin macro (`@Observable`, `#Preview`, Swift Testing, `#urlFetch(...)`) scanned clean under `deny Net`: exit 0, `functions: []`, zero disclosure. No visitor existed for `MacroExpansionExprSyntax` at all, and no attribute path treated a decl's own attributes as a possible attached macro. Found 2026-08-27 while auditing the row's prose (which only ever discussed rust's macros — "—" had been pasted across java/ts/swift/agents identically since the scorecard's first commit, never independently reasoned about per engine). **FIXED same day (`94d1aef`)**: both forms route into the existing `Unknown`/`unknownWhy` vocabulary (`"macro:<name>"` / `"macro:@Attr"`); a trailing-closure macro (`#Preview { ... }`) stays concrete (already caught via ordinary closure-body walking); a type attribute's disclosure propagates onto the type's existing members. Denylists compiler-builtin freestanding literals (`#file`/`#line`/…, MEASURED to need `fileID`/`isolation` after a first-cut miss produced 101 hits of noise on swift-nio/Nimble) and locally-declared `@resultBuilder`/`@globalActor` (the two other explanations Swift admits for a capitalized decl-attribute) — **the over-charge control this fix needed**: a denylist cut that is too narrow drowns real macros in noise, one that is too wide silently re-opens the hole. Residual: a macro-decorated type with zero source-declared members has no function to attach the disclosure to (the same pre-existing gap any compiler-synthesized member has); an EXTERNAL (non-local) `@resultBuilder`/`@globalActor` is conservatively treated as a possible macro — sound (over-discloses, never under-reports), unmeasured precision cost. 13-package corpus byte-identical post-fix; swift test 892/892, fuzz.py 25/25. | ~~SILENT~~ CLOSED | — | commit candor-swift `94d1aef`; conformance PART pending (item 3 of this session) |
| ~~R57~~ | ts | **an ANONYMOUS decorator expression discloses nothing** — `@((_t,_k,_d) => { fs.readFileSync(...) })` (an inline decorator function literal with no top-level name) mints no unit at all for the effect inside it. A NAMED decorator factory is sound (its effects land on the factory's own unit — `ts.isDecorator(p) → null` in `enclosing()`, scan.mjs, deliberately keeps a decorator application's call site from wiring onto the decorated declaration, tested at test.mjs:5102-5126); this is the narrower, unnamed-literal case that visitor never covers. Live-reproduced 2026-08-27: `deny Fs` over a fixture with exactly this shape exits 0, `"violations": []`, nothing in the 16-function report. Same audit sweep that found and fixed swift's R56 (identical shape: a real effect, zero disclosure, hiding behind a "—" pasted without engine-specific reasoning). A separate, already-known limitation (a decorator that WRAPS a method to inject an effect at call time, `descriptor.value = function(){ ... }`) is DISCLOSED, not silent (SOUNDNESS-LOG.md:1478-1481) — mis-attributed but not invisible; don't conflate the two. **FIXED in candor-ts `0a5d493`** (2026-08-28): `localName()` mints a position-keyed `<decorator>@<offset>` unit for the anonymous function/arrow value (direct, or as a decorator-call's direct callee) BEFORE `enclosing()`'s climb reaches the Decorator guard, leaving the already-sound named-factory path untouched. **RE-VERIFIED here independently, not taken on the fix's commit message or test suite**: built candor-ts `fbb9ea2` (immediately before the fix) and `0a5d493` in a throwaway clone, ran the exact filed repro plus a named-factory control and a genuinely-pure anonymous-decorator over-charge control against both. Pre-fix: the repro's `deny Fs` reads `ok:true`/exit 0/`violations: []`/no `Fs` anywhere in `functions`; post-fix: `ok:false`/exit 1/one `AS-EFF-006` violation on the minted `<decorator>@…` unit. The named-factory control is BYTE-IDENTICAL (report and gate-json) between the two binaries; the pure-anonymous-decorator control stays `ok:true`/`violations: []` on both, with `analyzed.count` the only field that moves (a new unit is analyzed, never charged) — proving the fix does not blanket-flag every anonymous decorator. **Cross-engine: ts-only, structurally** — a TS/JS decorator is a real function called at class-definition time by the language runtime; rust's attribute macros and swift's compiler-plugin macros (R56) are compile-time AST rewrites with no function ever called, and java's annotations are metadata read via reflection unless a processor runs at compile time (R58's separate mechanism). None has this construct for an anonymous value to hide inside, so this does not port. **RESIDUAL, found and live-reproduced verifying this row, filed as R64**: the identical `enclosing()` guard still silently drops three narrower, related shapes the fix's own commit deliberately left uncovered (a raw effect evaluated directly as a decorator argument, a closure nested in a named factory's argument data, and a call through an external bodyless decorator reference) — see R64. | ~~SILENT (open)~~ CLOSED | — | commit candor-ts `0a5d493`; PART 81 pins the fixture (defect + named-factory control + over-charge control), falsified against the pre-fix binary |
| R58 | java | **annotation-processor-generated SOURCE codegen is unmeasured, not confirmed clean** — candor-java reads compiled `.class` files (`Loader.java`), never source, so Lombok (`@Data`/`@Builder`, which rewrites bytecode in the SAME `javac` invocation) is immune by construction, the rust-deep shape. But a processor that emits a separate `.java` file compiled to its own `.class` (Dagger, Room, AutoValue, MapStruct) is a different mechanism: those `.class` files land in the same output directory (`build/classes/java/main`) the README tells users to point candor-java at, and nothing in `Loader.java`'s walk special-cases or drops them — so the construction argument is plausible but has never been checked against a real annotation-processor project. Found 2026-08-27 in the same sweep as R56/R57 (the row's "—" cells had never been independently reasoned about per engine). Not marked SILENT — no reproduction exists either way, unlike R57's confirmed live repro — logged as its own category rather than rounded up to immune or down to a confirmed sin. | UNMEASURED | — | compile a small Dagger or Room sample, point candor-java at its output directory, confirm the generated `.class` files are read and their effects (if any) attributed; then reclassify as CLOSED (immune) or SILENT (a real gap) |
| ~~R59~~ | rust-scan | **a `libc`/`nix`/`rustix` call the syscall-name table deliberately leaves unclassified silently vanishes when no CLASSIFIED sibling call establishes the effect in the same function** — `libc`/`nix`/`rustix` are `CALIBRATED_CRATES`, so an unmatched call into them reads as "this calibrated crate answered: nothing" rather than "unresolved external". `candor-classify/src/lib.rs` deliberately skips the generic fd verbs (`read`/`write`/`close`/`lseek`/`dup`/`fcntl`/`ioctl`/`poll`/`select`/`epoll*`/`mmap`) "because a fixed label would mis-categorise as often as it helps… an honest no-classify (under-report) beats emitting the WRONG effect" — a comment that reads as a disclosed precision trade-off but the MEASURED behaviour was total silence, not disclosure. Found auditing the FFI scorecard row (2026-08-27, same sweep as the extern-block audit below). Live-reproduced against `target/release/candor-scan` 0.33.1: `fn drain(fd: i32) -> usize { unsafe { libc::read(fd, buf.as_mut_ptr() as *mut libc::c_void, 64) as usize } }` produced `"functions": []`; neither function appeared, not even as `invisible`/`Unknown`/`incomplete`. `scan.rs`'s emission gate is `if inf.is_empty() && !has_blind { continue; }` — an unclassified-but-calibrated-crate call set neither `inf` nor `has_blind`, so the function was dropped exactly like a genuinely pure one. **FIXED 2026-08-28 (candor-rust `905e1cd`)**: new `CALIBRATED_BUT_PARTIAL_CRATES` carves libc/nix/rustix out of the ledger's blanket exemption at its one consumer site; classified calls (`open`/`socket`/…) never reached the ledger at all and are untouched. `drain` now reads `invisible: ["libc"]`. Corpus (1202 cached crates, standalone before/after): 97 differ (8%), every byte a new disclosure or a previously-silent report gaining entries, zero effect-field changes; spot-checked against `serial-unix`, a real serial-port crate (6 previously-silent libc calls across 5 functions now disclosed). **Same-day follow-on audit of the OTHER ~79 `CALIBRATED_CRATES` members this fix left unaudited (candor-rust `352bda3`) found three further real gaps, none of them FFI — see R62.** | ~~SILENT~~ CLOSED | — | commit candor-rust `905e1cd`; PART 79 pins the fixture byte-identically against rust-deep (R60) |
| ~~R60~~ | rust-deep | **a local `extern "C" { .. }` foreign-function declaration (the bindgen/direct-FFI shape, `#[link(name="c")]` or bare) is silently dropped even though the engine's own callgraph proves it visited the call** — found auditing the FFI scorecard row (2026-08-27); this is the mechanism the row's prior "🟢¹ verified by construction" claim was actually about, and it was never measured until this pass. Live-reproduced via `cargo dylint --lib-path <libcandor@nightly-2026-06-14-….dylib>` against a fixture identical in shape to rust-scan's own `native:extern fn` regression fixture: `extern "C" { fn system(cmd: *const c_char) -> i32; } fn run_cmd() { unsafe { system(c.as_ptr()); } }`. The report was `"functions": []` — but the SAME run's callgraph sidecar read `{"run_cmd":["system"]}`, proving the HIR walk reached the call; the effect layer attached nothing to it. CONTRAST, same audit: the SAME binary correctly disclosed `"invisible": ["libc"]` for an unclassified `libc::read` call (R59's fixture) — rust-deep's `invisible` machinery is crate-name-keyed, and a bare `extern "C"` block declared IN the local crate has no crate name to hang it off. This made rust-deep, the engine documented elsewhere as "the sound gate" for exactly the misses the syntactic scanner accepts, WORSE than rust-scan on this specific seam. **FIXED 2026-08-28, the SAME COMMIT as R59 (candor-rust `905e1cd`)**: `Unknown`/`native:extern fn` now discloses unconditionally on `is_foreign_item()`, independent of the crate-name-keyed `invisible` mechanism — mirroring rust-scan's own `ForeignMod` handling rather than routing through coverage-ledger crate naming, deliberately, so the two engines' disclosures for this seam MATCH BYTE-FOR-BYTE rather than merely agree in spirit. MEASURED: `run_cmd` now reads `inferred == ["Unknown"]`/`unknownWhy == ["native:extern fn"]`, byte-identical to rust-scan's own answer for the same fixture. New test `ui/ffi_extern.rs` (dylint UI test, red pre-fix). | ~~SILENT~~ CLOSED | — | commit candor-rust `905e1cd`; PART 79 asserts byte-parity against rust-scan (R59) as its own control |
| ~~R61~~ | swift | **raw C-interop through `import Darwin`/`import Glibc`, `@_silgen_name`, and `dlopen`/`dlsym` all read silent-pure — the FFI row's "—" (immune by construction) was FALSE**, the identical shape to R56's macro-row finding (found the same 2026-08-27 audit). THREE independently-idiomatic mechanisms, all live-reproduced against `.build/release/candor-swift` 0.33.1, all producing `"functions": []` (zero disclosure) and both `deny Exec`/`deny Fs` exiting 0 ("policy ✓") over code that performs the effect: (1) `import Darwin; func f() { system("rm -rf /tmp/x") }` and the Fs analog `unlink("/tmp/x")` — `kappaFree`'s own comment in `Classifier.swift` documents the omission of `system`/`fork`/`mkdir`/`rename`/`unlink` as deliberate without registering that `Darwin`/`Glibc` are `PLATFORM_MODULES`, which get NO ledger naming and NO `Unknown` fallback for an unmodeled member at all; the MODELED `Process`/Foundation API stayed correctly charged in the same file (control, unaffected). (2) `@_silgen_name("system") func c_system(_: UnsafePointer<CChar>) -> Int32` — Swift's compiler-level direct-symbol-linkage declaration, bypassing `import` entirely — same silent result. (3) `dlopen(path, RTLD_NOW)` + `dlsym(handle, "sym")` + `unsafeBitCast(sym, to: SomeCFn.self)` + calling it — same silent result, arguably the highest-severity of the three. No generic "unresolved call through an opaque/foreign value" fallback existed anywhere in candor-swift's dispatch model. **FIXED 2026-08-28 (candor-swift `bcb4a4e`)**, one shared vocabulary across two mechanisms: (1) and (2) now disclose `Unknown`/`native:<symbol>`, gated on a new ALLOWLIST (`NATIVE_DISCLOSURE_C_FREE_FNS`) plus a `C_PLATFORM_MODULES` import check — an unrestricted first cut measured 1519 false hits on swift-nio alone (build-config predicates, stdlib control flow, none of them FFI), narrowed to the allowlist; 12/13 corpus packages byte-identical, the 13th (swift-syntax) gains exactly 3 genuine hits, all spot-checked against source. (3) routes through the existing `opaqueFnLocals` machinery, disclosing `Unknown`/`callback:<name>`. **Stated limit, not a general closure claim: the allowlist is deliberately incomplete** — an unnamed raw Darwin/Glibc call outside `NATIVE_DISCLOSURE_C_FREE_FNS` still reads silent, the same under-report-the-rare-case trade-off `kappaFree`'s own comment already accepts. | ~~SILENT~~ CLOSED | — | commit candor-swift `bcb4a4e`; PART 79 pins the raw-import and `@_silgen_name` shapes plus the Process control |
| R62 | rust-scan | **the R59 fix's own stated limit — "the other ~79 `CALIBRATED_CRATES` members are unaudited for the same generic-verb-no-rule shape" — closed by a same-day follow-on audit (candor-rust `352bda3`)**, checking every remaining calibrated-crate entry against real vendored source. Three real, unrelated gaps found and fixed (none of them FFI — plain missing classify() rules the same silent-vanish mechanism was hiding, not an ambiguous-effect trade-off): `clap::Arg::env(name)` calls `env::var_os` directly at builder time (clap_builder 4.6.6) — a real immediate `Env` read independent of `get_matches()`'s own classified read, produced `"functions": []` with no accompanying `get_matches` call; `console::Term` implements raw `io::{Read,Write}` (console 0.15.11) — `Term::write`/`flush` call the same `write_through()` primitive `write_line` already charges `Ipc` for, `Term::read` calls `io::stdin().read` directly, both previously silent; `arboard::{Get,Set}::file_list`/`Clear::default` (arboard 3.6.1) — the same builder-then-terminal shape as the already-covered text/image/html, missed structurally because the coverage-gate generator only triggers on an Fs/Net/Db/Exec self-scan signal, so a missing `Clipboard` verb was invisible to it regardless of phrasing. Each fix is fixture-gated (red pre-fix, green post-fix, with a pure-sibling control) plus a `classify()`-level unit assertion. Corpus quantification (1202 cached crates): 0 differ — none of the published library crates in this registry mirror spell these narrow CLI/tty/clipboard shapes (they live in application binaries); the fixtures, not the corpus, prove each fix fires. The remaining ~76 crates were checked (rule read + spot-checked against real source) and found complete for this class. | ~~SILENT~~ CLOSED | — | commit candor-rust `352bda3`; one filed residual from the same audit, not fixed — see R63 |
| R63 | rust-scan | **`wild::ArgsOs`'s Windows-only `Iterator::next` leaks an `Fs` read (reading `argv` via the Windows API), filed rather than fixed** — found in the same R62 audit. Low reachability: invisible in the dominant `get_matches_from(wild::args_os())` idiom (the iterator is consumed inside a call `wild`'s own README shows, not stepped by hand), and structurally unseen by the coverage-gate generator for the same reason `walkdir::IntoIter::next` needed a hand-written rule before R59/R62 existed to name it (PART 74's own seam, a different mechanism — receiver-typing, not calibrated-crate exemption — but the same "the generator can't see a lazy-iterator verb" blind spot). | SILENT (open) | low (Windows-only, non-dominant idiom, unmeasured real-world incidence) | a hand-written coverage-gate rule for `wild::ArgsOs::next`, mirroring the one `walkdir::IntoIter::next` already needed (PART 74) |
| R64 | ts | **three narrower shapes of R57's guard survive R57's own fix — TWO NOW CLOSED, one still open, on a MEASURED rather than assumed rationale** — found live-reproducing R57's pinning row (2026-08-28), not part of R57's original filing. `enclosing()`'s `if (ts.isDecorator(p)) return null` dropped: (1) a raw effect evaluated directly as a decorator's ARGUMENT (`@Decorate(fs.readFileSync(...))` — no function value exists anywhere to mint a unit on, unlike R57's shape); (2) a closure nested in a NAMED factory's argument DATA rather than its call chain (`@Factory({ init: () => { fs.readFileSync(...) } })` — the closure's ancestor chain runs through an object literal and the factory CALL, never through the factory's own body, so it never reaches a named unit before hitting the Decorator guard); (3) a call through an EXTERNAL bodyless decorator reference whose own (unread) implementation performs the effect (`import { ExternalDecorator } from "extlib"; @ExternalDecorator class …`). All three live-reproduced against candor-ts `0a5d493` (R57's fix, immediately before this one): each read `functions: []`, `deny Fs` exit 0, `ok:true` — the cardinal sin, not even `Unknown`. **FIXED for shapes 1 and 2, candor-ts `b4c3a22`** (2026-08-28): `enclosing()` now tracks the child it climbed FROM at each step (`prev`); when the current node is the decorator's own call/new-expression and `prev` is one of its ARGUMENTS (not the callee), a `<decorator-arg>@<pos>` unit is minted instead of falling through to the Decorator guard — every effect anywhere in one decorator's argument list shares that unit; the decorator's own top-level application (shape 3, R57's original case) is untouched. **RE-VERIFIED HERE independently**, not taken on the commit message or its own suite: built candor-ts `9a8a5c7` (immediately pre-fix) and `b4c3a22` in a throwaway clone, ran shapes 1/2/3 plus a literal-decorator-argument control and a pure-local-function-argument control against both. Pre-fix: all five read `functions: []`/`ok:true`/0 violations. Post-fix: shapes 1 and 2 read `inferred:["Fs"]`/`paths:["/etc/hosts"]` on a minted `<decorator-arg>@…` unit and `deny Fs` exits 1 on AS-EFF-006; shape 3 is UNCHANGED (still `functions: []`/`ok:true`); both over-charge controls stay `functions: []`/`ok:true` on both binaries. **Shape 3 (external body-less decorator reference) is LEFT OPEN, now on a MEASURED rather than folklore rationale**: R57's own commit rejected a blanket fix on the argument that it would flood real framework code, but had never measured that against decorator-bearing code. `b4c3a22`'s own experiment measured a blanket variant simulating that rejected fix against THREE real corpora with dependencies genuinely installed: byte-identical to the shipped narrow fix on a real NestJS+TypeORM app (`lujakob/nestjs-realworld-example-app`) and on TypeORM's own 513-file functional test suite, but **+42 report rows on a base of 80 (+52%)** on a real Angular app (`gothinkster/angular-realworld-example-app`) — every bare `@Injectable()`/`@Component()`/`@Pipe()` gained its own row. The trade-off is now true for one framework and false for two others, not an unmeasured assumption. Not conflated with R57 in SOUNDNESS.md; PART 82 pins both fixed shapes, both over-charge controls, and asserts shape 3 as a DOCUMENTED-OPEN case (currently silent, not required to stay so — a conformance row pins wanted behaviour, not a sin). | shapes 1/2 ~~SILENT~~ CLOSED; shape 3 SILENT (open) | shape 3: MEASURED, not folklore — byte-identical to the narrow fix on 2 of 3 real corpora, +52% report-row inflation (42 of a base 80) on the third (a real Angular app) | commit candor-ts `b4c3a22` closes shapes 1/2, falsified against `9a8a5c7`; PART 82 pins the fixed shapes, both over-charge controls, and shape 3's documented-open value; candor-spec doesn't own candor-ts — if shape 3 is ever revisited, a fix needs the same over-charge discipline plus a way to avoid the Angular-shaped flood (e.g. scoping to decorators whose factory is known to perform an effect, not blanket on every external reference) |
| ~~R65~~ | swift | **R33 deinit-glue fired only where the construction ROOTED A LOCAL BINDING — 19 other construction POSITIONS read silent-pure.** `_ = Ctor()` (no `let`), bare expression statement, call ARGUMENT, array/dict/tuple literal element, struct-field argument, capture-list initializer, `if let`/`guard let`/`switch` subject, TUPLE DESTRUCTURING, `defer` body, ternary arm, `Ctor().member` as receiver, `xs.append(Ctor())`, `[Ctor()].first`, `try? Ctor() as T`. **NOT the shape it was filed under.** It was filed from rust's boxed-closure `Drop` sin (a guard moved into a closure, boxed as a trait object, never called), on the hypothesis that swift's `deinit`-captured-by-an-escaping-closure would share it. It does not: swift's closure cells are ALL CORRECT (escaping closure stored and never called, closure that IS called, array of closures, protocol existential), and 4 of the first 7 silent cells contain no closure at all. **The discriminator is POSITION, not capture** — so a clause written from the rust bug would have been the wrong clause, ported. Ground truth EXECUTED, not reasoned (an unbuffered `deinit` interleaved with call/return markers): all 19 run before the constructing function returns. Scoped `deny Fs` reported **4 where the truth is 11**, inside `analyzed.count`, with no `Unknown`, no per-fn `incomplete` and no advisory — an affirmative purity claim. **Bounded, and the bound is the honest part:** an UNSCOPED `deny Fs` still fires, because the `Type.deinit` units are themselves in the report. So this is CALLER-ATTRIBUTION — it defeats scoped/layered policies, `path`, `gains`, `tour` and `fix-gate`, not a blanket deny. **FIXED 2026-08-31 (`7515876`)**: ONE authority at `visit(FunctionCallExprSyntax)` plus a lexical escape gate (return/throw · binding-whose-name-is-returned · assignment into member/subscript/returned local · stored property or global · implicit return); **the three binder call sites were DELETED, not left beside it** — that duplication is how the vein opened, and a fourth site beside them would have reopened it. R49's return-escape lesson applied and extended. **THE CORPUS A/B CAUGHT THREE FABRICATIONS THE 981-TEST SUITE COULD NOT**, all green under it: GRDB (`TraceEvent.statement(s)` is an ENUM CASE, and `returns` is leaf-keyed, so an unrelated `func statement(_:) -> Statement` made `Database.trace_v2` inherit that class's `Db` — `TraceEvent.Statement` is a struct with no deinit at all); Kingfisher/swift-crypto (a stored-property or global initializer — the unit body IS the initializer, so the ancestor walk stops before the binding that says "stored"); Alamofire (implicit `self` in an init read as a write to a local). Final A/B, 13 packages / 9,566 effectful fns: **0 removed, 0 effect-set changes, 2 new rows**, both `Unknown`, both swift-nio `addHandlers` — the stated over-approximation. **STATED LIMITS, measured not assumed:** a callee that retains an argument is over-charged (unchanged from the shipped bound-local posture); a member-access factory bound to a local (`db.makeStatement(…)`) is no longer charged — 0 measured recoveries, 1 measured fabrication. **One control had to be REWRITTEN**: it passed with AND without its own guard, because its binder built a value of its own, so the assertion could not see what answer the assignment got. | ~~SILENT~~ CLOSED | over-charge, both stated and measured: a callee that retains an argument, and swift-nio's 2 new `Unknown` rows | commit candor-swift `7515876`; `DeinitGlueConstructionPositionProcessTests` (16 positions + fabrication control + R49 factory control + 3 fabrication controls); 981/981, smoke 148/0, four-way conformance OK. **CONFORMANCE PART OWED — nothing in SPEC pins deinit/`Drop` scope-exit attribution cross-engine, so this fix has tests and no row**, which is the "write the row before the port" gap this file exists to stop. **Cross-engine question OPEN, dispatched 2026-08-31:** R49 closed rust's FIELD cell but R49's own entry is BINDER-shaped too, so rust and java may carry this same 19-position vein — measuring whether their `Drop`/finalizer glue keys on a binder or on the construction. Swift is exempt from the SEPARATE CHA-completeness vein that rust and java do share (a compiler-synthesised interface impl that CHA cannot enumerate); these are two distinct properties and should not be collapsed into one clause. |
| ~~R66~~ | rust-scan | <a name="r66"></a>**[CLOSED 2026-08-31, candor-rust `02749cf`](#r68)** — one authority at the construction expression (`CallCollector::note_construction`, reached from call / struct-literal / value-path), the binder site and `scan.rs`'s `tail2`-over-every-call route both **DELETED** rather than a third added beside them, plus a lexical escape gate (`lang::escaping_ctor_leaves`) that now covers the DIRECT route and closes the asymmetry noted below. Parameter-owned drop included. Verified independently by the coordinator on a separate fixture: tuple-struct bound local `['Fs']`, brace-literal `['Fs']`, parameter-owned `['Fs']`, while `fn make_escapes() -> TGuard` and a `format!` accessor both stay PURE. Gates 24/24 (the 5 strace gates are SELFSKIP — UNRUN, not green). **A/B over 256 real crates with `impl Drop`, 31,121 effectful fns: +132 / −295, and the REMOVALS are the finding, not a regression** — flate2's own thirteen R49 constructor rows are now refused (they construct-and-RETURN, the exact case that reverted R49's first prototype) while its sixteen genuinely-releasing `finish`/`into_inner` rows GAINED; 163 more removals are other returns-the-drop-type escapes; **109 are `&self` accessors that were charged off a synthetic `Type::method` edge the old rule could not tell from a construction** — an `==` overload, a `{}` format hole, a `?`-`From` conversion. Coordinator A/B on that class, one variable: `format!("{}", g)` and `a == b` went `['Fs']` → pure, a plain accessor stayed pure both, and the true-positive control stayed `['Fs']`. So the bulk of −295 is REMOVING FABRICATED EFFECTS, not opening blind spots. **THREE FABRICATIONS THE WIDENING ITSELF INTRODUCED were caught ONLY by the corpus A/B**, all green under the 628-test suite, and found by reading a `CANDOR_CTOR_DEBUG=1` marker stream rather than the diff: tokio's `use Ordering::*` beside its own `struct Acquire` with a tracing `Drop` (every `is_closed`/`is_idle` in `batch_semaphore` inherited `Log`); `self: Pin<&mut Self>` parsing as a `Receiver` with `reference: None` (every `poll_*` in the ecosystem read as consuming its receiver); and a `match` arm PATTERN, because syn 2 represents `Pat::Path` with the same `ExprPath` node as an expression. Two further catches were regressions of the shipped route (`_ = Guard::new()` read as a store; a nested `if` block's tail read as the function's return). **A PREMISE THE COORDINATOR SUPPLIED WAS WRONG and the agent broke it: rust-deep is NOT a clean oracle here** — measured silent on 17 of 99 genuinely-releasing units (every `async` body, a closure-released guard, a `Box<Self>` receiver, every value moved into a generic callee), so `async_brace` is a case where rust-scan is RIGHT and rust-deep is silent, before and after. rust-deep has 0 over-charges, so it remains the right oracle for ESCAPE and BORROW-VS-OWN only, which is what it was used for. Incremental cache schema bumped rev10→rev11, because a warm entry would replay the binder-keyed marker set. ORIGINAL FINDING follows. **effectful-`Drop` glue was keyed on construction SYNTAX, not on the construction — the rust-scan edition of swift's R65 position vein, and it reaches further.** TWO routes exist and only one is sound: `scan.rs:1900` handles an assoc-fn CALL `T::assoc()` for `T ∈ drop_types` by walking every call in the body, so it is construction-keyed and **position-independent (24 of 24 positions charged)**; `collector.rs:2274` emits the synthetic `T::<construct>` marker for struct-literal/unit-path construction **only inside `else if let syn::Pat::Ident(id) = &node.pat`** — binder-keyed, and keyed on the binder's PATTERN KIND. Measured with ground truth EXECUTED (destructor appends to a log, `main` brackets each call with call/return markers, ordering read from the log): **brace-literal guards silent in 16 of 21 executed positions** (`let _ =`, `_ =`, bare statement, call argument, array/tuple element, `Some(..)`, `&temp`, `match` scrutinee, `if let`, TUPLE DESTRUCTURING, method receiver, `vec.push`, ternary arm, `unwrap()`, `into_iter().next()`); **unit structs 7 of 8 silent**; enum unit-variant under `Pat::Wild` silent. **THE WORST CELL: a TUPLE-STRUCT (newtype) guard has NO ROUTE AT ALL and is silent in EVERY position INCLUDING THE BOUND LOCAL** — `struct G(File); let _g = G(f);` reads pure, because `G("x")` is a single-segment `Expr::Call` that fails the call route's `c.path.contains("::")` test and is not ``Expr::Struct` or `Expr::Path`` for the literal route; it falls between both. Positive control: `TGuard::make()` IS charged, so the type is in `drop_types` and the miss is purely syntactic. This is the newtype-guard idiom, arguably the most common effectful-`Drop` shape in real Rust. **INDEPENDENTLY REPRODUCED by the coordinator**, one variable: `struct BGuard{id}; let _g = BGuard{id};` → `['Fs']` vs `struct TGuard(String); let _g = TGuard("x");` → ABSENT. **SEPARATE VEIN exposed by the same matrix: parameter-owned drop is silent** — `fn take(g: Guard) {}` genuinely runs the destructor inside `take` (proven by log ordering) and rust-scan reports it PURE; construction-keying has no route for a value it did not see constructed. Scoped `deny Fs` over a 65-unit fixture: 42 reported, 53 units truly drop, **36 true positives, 6 over-charges, 17 silently missed**. Calibration EXECUTED, not assumed: seeding a direct `fs::read` into four of the silent units flips each to `['Fs']`, proving those units are analysed and reportable and their silence real. **rust-deep (the dylint lint) is CLEAN on ALL of it** — every construction syntax, every position, tuple/unit/enum, parameter-owned drop, and it gets the escape cases right too. So the vein is rust-scan-only and rust-deep is available as the oracle. **A LIVE ASYMMETRY a fix must not worsen:** the R49 field route HAS a return-escape gate, the DIRECT route does NOT, so `fn make() -> Guard { Guard::new() }` is already charged `Fs` today — a phantom effect on every guard constructor in a real crate. Widening the direct route without an escape gate multiplies that; rust-deep does not have the problem. | ~~SILENT~~ CLOSED | the pre-existing direct-route over-charge is now GONE (the escape gate covers it); residual over-approximation is stated in R68 | **FIXED 2026-08-31, candor-rust `02749cf`.** Original plan follows, and it is what was done. Shape to follow: swift's `7515876` — ONE construction-keyed authority covering call/struct-literal/tuple-struct/unit-path/enum-variant uniformly, with the duplicated routes DELETED rather than a third added beside them, plus a decision (argued, A/B'd) on whether the direct route gains an escape gate. R49's flate2 revert is the standing warning: its first prototype went regression-green and fabricated 14 false `Unknown`s on constructors that construct-and-RETURN the owner. Unexamined by the measurement and owed by the fix: `async`/`.await`, generics (`T::new()` under a bound), `ManuallyDrop`/`mem::forget`, `Box`/`Rc<Guard>`, and the cross-crate `cr::<drop>::key` marker at `collector.rs:1948` |
| R67 | java | **CLEAN NEGATIVE — and it DECIDES the conformance question for R65/R66.** Asked whether candor-java's attribution is position-sensitive the way swift's (R65) and rust-scan's (R66) are: **it is not. 44 distinct syntactic positions, 55 EXECUTED ground-truth writes, zero silent under-reports.** Scoped `deny Fs` reported **50 of 50** (swift's analogue: 4 of 11). The mechanism is stated, not asserted: javac erases every construction position into the same `NEW`/`INVOKESPECIAL`/`INVOKEVIRTUAL` sequence, candor reads that flat instruction list via ASM, and the effect edge is drawn from the INSTRUCTION, never from what the expression was rooted in. Two arms held constant — effect in a called method, and **effect in the constructor itself** (the faithful translation of the swift shape). Covered: receiver · argument · array literal · `List.of` · ternary · try-with-resources · resource-from-a-CALL · instance and static initializer · nested/chained · anonymous inner class · switch subject · `assert` · catch · finally · indy string-concat · `synchronized` subject · array index · for init and update · popped return value · `&&` right operand · **throw operand** · **`super(...)` argument** · return position · cast/`instanceof` operand. Widened past its own trigger: `--release 8` (synthetic `access$NNN` nest bridges charged, and callers charged THROUGH them), `-g:none` (**byte-identical** charged set — receiver typing does not depend on debug info), covariant-return bridges, and descriptor-keyed unit identity (an effectful/pure overload pair does not name-merge). Over-charge control: 11 units correctly pure, the sharpest being a method that CONSTRUCTS in return position and is pure because the ctor is pure — candor tracks the call, not the construction. **THE CONSEQUENCE FOR THE SPEC, which is the valuable part: a conformance row phrased "construction position must not change attribution" is NOT HOSTABLE ON THE REFERENCE ENGINE IN A DISCRIMINATING WAY.** java passes it 44/44 BY CONSTRUCTION, so the row would be green whether or not the engine were correct — exactly the "passes with and without the fix" shape this family kills rows for. **So R65/R66 are a SOUNDNESS VEIN, NOT A SPEC RUNG**, and R65's "write the row before the port" debt is discharged by this measurement rather than by shipping an undiscriminating row. **The row that IS hostable four-way**, surfaced by the same agent's Net arm: *literal-surface visibility must not depend on the operand's DISTANCE FROM THE SINK, or the loss must be disclosed* — every engine has a literal-window analogue, and the arms discriminate on java today (host literal adjacent to the sink → `Llm, Net` with `hosts`; the same compile-time-constant host ONE CALL HOP away → bare `Net`, no host, `incomplete: ["Net"]`). **A HYPOTHESIS THAT DIED, recorded so nobody re-raises it:** the agent tried hard to make that bare-`Net` case a cardinal sin and could not — SPEC §1 ⟨0.13⟩ makes bare `Net` the CONTRACT for an unknown host ("never guessed"), and AS-EFF-008 quantifies over `e ∈ I(f)` as a PRECONDITION, so `Llm ∉ I(f)` means the rule correctly does not fire. It also withdrew its own attack-K claim against `Policy.java:865` after building the case that comment describes and measuring it fail closed correctly. | CLEAN NEGATIVE | a genuine residual needing judgement, not a fix: `--gate-json` carries only `{spec, ok, analyzed, violations}`, so the per-function `incomplete` marker does NOT ride the machine channel (SPEC §2244 says it is explicitly not guaranteed to), and `deny <E>` never consults `surfaceIncomplete` — only `allow <E>` does. So `deny Llm` over a one-hop-constant host is `ok:true` exit 0 with no signal, while `allow Net <hosts>` fails closed | no fix owed. UNEXAMINED and stated rather than implied: Kotlin/Scala/Groovy front-ends (javac only; Kotlin's `inline`/coroutine lowering is a genuinely different position set), ECJ-compiled bytecode, and whether the Net/Llm literal-window result transfers to `allow Exec`/`allow Fs`/`allow Db` — measured for Net/Llm only, and by this file's own boundary rule that is not assumed to transfer |
| R68 | rust-scan | <a name="r68"></a>**five residuals of the R66 fix, each MEASURED and deliberately not fixed in that commit.** (1) **THE SAME VEIN ONE BOUNDARY OVER — the highest-value item here.** Executed against a real two-crate `CANDOR_DEPS` chain: `deplib::UnitGuard` charges, but `deplib::Guard::new(1)`, `deplib::Guard { n: 1 }` and the bare statement are all **SILENT — 1 of 4 spellings**. The cross-crate marker at `collector.rs:1948` was not widened with the in-crate authority. This is the family's standing pattern: close a vein, and the identical hole sits one constructor over. (2) **Drop-on-overwrite**: `zlib.data = Decompress::new(true)` destroys the OLD value in this scope, but the new one escapes into the field, so the escape gate now refuses it (flate2 `reset_decoder_data`). **The pre-fix engine was right about this case BY ACCIDENT** — it charged it for the wrong reason — which is why it is filed rather than treated as a regression. (3) `owned_drops` is leaf-keyed, so `agent::Inner` owning a guard makes every `Inner` drop-relevant; pre-existing, and R49's own comment argues for leaf keying. (4) A closure-consumed guard whose tail derives from it, and `T::mk()` under a generic bound — rust-deep misses the latter too. (5) A conformance PART pinning WHICH SCOPE a released value's `Drop` is charged to does not exist (`grep -rl 'Drop\b' conformance/` is empty). **RULED: it should not be written.** Per R67, java has no destructors at all and is position-blind by construction, so such a row cannot discriminate on the reference engine — it would be green whether or not any engine were correct. R65/R66 stay a soundness vein, and this row records the ruling so the gap is not re-filed as a debt. | filed, measured, open | (1) is a real silent under-report across a dep boundary; (2)–(4) are bounded over/under-approximations with stated mechanisms | (1) widen `collector.rs:1948` to the same construction authority `02749cf` gave the in-crate path, with the same escape gate and a chained-dep A/B; (2) needs a decision on whether assignment-destroys-old is charged to the assigning scope at all; (5) NO ACTION — ruled unhostable |
| ~~R69~~ | rust-scan | **THE R66 FIX'S OWN ESCAPE GATE SUPPRESSED ON *SOME*-PATH ESCAPE, NOT EVERY-PATH — a silent under-report I introduced in `02749cf` and found one hour after pushing it, by running a single fixture.** `lang::escaping_ctor_leaves` was NAME-BASED, not path-based: if a constructed value's name reached ANY return or tail position, the name was marked escaping and the constructing scope was not charged — whether or not that return happens on every path. So a value that escapes on one path and dies on another read PURE. **Seven spellings, each MEASURED BY EXECUTION** (the destructor writes its own file; the driver calls each function on its NON-escaping path and checks the file appears), all regressed `['Fs']` → pure between `5e31ff0` and `02749cf`: `if`/`else`, an early-return guard, `match`, an early `return`, a `for` loop, `Result`, and `?`. **It survived 628 tests, a 256-crate corpus A/B, 24 green gates, CI, and my own five-cell verification of that very commit — because not one of them had a conditional-escape fixture.** This is [[feedback-fabrication-fixes-cause-misses]] landing on the fix that closed R66: killing an over-charge is exactly where silent under-reports get introduced. **FIXED 2026-08-31 (`9f202ca`)**: a leaf is suppressed only when the construction escapes on EVERY terminal exit reachable from it — each exit analysed separately, `if`/`match` requiring the leaf in EVERY arm, an exit with no reachable value (a bare `return;`, `?`'s failure residual) counted as a real exit that CHARGES, and anything unmodelled (loops, `do`/`catch`) defaulting to CHARGE. That is the denylist-over-allowlist contract for a narrowing — carve out PROVEN-safe cases, never merely POSSIBLE ones — and it errs toward over-charge, never under-report. Controls hold: unconditional `-> G` and `-> Option<G>` factories stay PURE (the flate2 shape that reverted R49's first prototype), a purely local guard stays CHARGED, and `02749cf`'s correct removals stay removed (`format!("{}", g)` and an `==` overload are still pure — they were fabricated effects charged off a synthetic `Type::method` edge). Gates 24/24; the 5 strace gates SELFSKIP on macOS, UNRUN not green, covered by CI on Linux. | ~~SILENT~~ CLOSED | errs toward over-charge by construction; unmodelled control flow charges | commit candor-rust `9f202ca`; `crates/candor-scan/src/tests.rs` pins all seven spellings plus the three controls. **The durable lesson is about the INSTRUMENT, not the bug: a corpus A/B measures crates that exist, and no crate in 256 of them exercised the shape a ten-line fixture found immediately. A corpus is a sample, not a proof.** |
| ~~R70~~ | swift | **the SAME every-path-vs-some-path defect, PRE-EXISTING — not introduced by R65's fix but INHERITED by it.** Found by an adversarial review dispatched specifically because rust had just been broken this way; the question translated on the first try. TWO cardinal sins. **(a) Whole-function name membership:** `returnedNames` was a set built by walking every `return` in a function once, so a name mentioned in ANY return marked EVERY binding of that name as escaping EVERYWHERE, regardless of path. `let g = Loud(); if f { return g } else { return nil }` read silent-pure on the `f == false` path where `g` genuinely drops in scope. Also reproduced with a `switch` where only one case returns, a `throw` on the non-returning path, and **two DIFFERENT same-named locals in sibling `if`/`else` branches**. **The old binder-path guard had the identical bug and R65 carried it forward unchanged** — so swift has been under-reporting conditional-escape shapes for as long as that guard has existed. **(b) Implicit return:** `isImplicitReturnPosition` could not distinguish a genuine if-EXPRESSION implicit return from an `if`/`else` whose arms explicitly `return` — both parse to the same `IfExprSyntax` — so a single-statement body containing `if cond { let g = Loud(…); return 1 } else { return 2 }` silenced the construction entirely. An ordinary, common Swift shape. Both confirmed by EXECUTION with unbuffered `deinit` markers, not by reasoning. **FIXED 2026-08-31 (`220a26f`)** with a bounded `guaranteedToEscape` path check that defaults to "not proven → CHARGE" for anything unmodelled (switch, loops, do/catch). **AND THE FIX'S OWN FIRST DRAFT INTRODUCED AN O(2^N) BLOWUP, caught by TIMING rather than by reading:** it threaded its continuation as an `@escaping () -> Bool` closure invoked from both arms of every `if`, so `PrivacyManifestCLI.swift`'s long `if usage == "…"` table took the engine's self-scan from **3 seconds to 9+ minutes** — and all 988 tests stayed GREEN throughout, because nothing times a self-scan. Fixed by computing each continuation eagerly as a plain `Bool`; self-scan back to 0.2–2.8s, now pinned by a 500-`if`/`else` test with an explicit elapsed-time assertion. | ~~SILENT~~ CLOSED | over-charge controls written first and all held: guard-else before construction, else-if chains where every arm returns the same binding, nested closures with returns, tuple-destructuring returns | commit candor-swift `220a26f`; `DeinitGlueConditionalEscapeProcessTests`; 989/989, smoke 148/0, four-way conformance OK **re-run on clean trees** (its first run read candor-rust's uncommitted tree — see CLAUDE.md on conformance reading working trees). A/B over 6 real packages / ~11,300 fns: zero rows removed, zero changes attributable to the fix. **THE HEADLINE FOR THE FAMILY: one question — "does your escape gate suppress on SOME-path escape?" — found a cardinal sin in TWO engines on the same day, in neither case via a test suite, and in swift's case one that had been shipping for months.** |
| R71 | rust-scan | **a closure invoked through an `if let` binding is silently dropped as pure — and a scoped `deny` over the invoking function PASSES while the effect provably runs.** `if let Some(f) = &self.cb { f() }` — the idiomatic optional-callback-field shape — reports the invoking function **ABSENT from `functions[]`**, with no `Unknown`, no `unresolved`, and the top-level `resolves` field BYTE-IDENTICAL to the honest case. The DIRECT-field form `(self.cb.as_ref().unwrap())()` is honest (`Unknown`/`unresolved:true`/`callback:unresolved call`), so the two spellings of the same operation disagree. **Root cause — AMENDED, because the first diagnosis was wrong and the fixing agent attacked it.** The original filing said `resolve_elem_trait_leaves` returns EMPTY for an `Option<Box<dyn Fn()>>` field so the binding falls through untyped. MEASURED by instrumenting the built binary: it returns `["Fn"]` and the binding DOES land in `trait_vars`. The real gap is a two-table split — `trait_vars` feeds only the `.method()`-call CHA/dispatch resolver, while `visit_expr_call`'s call-SYNTAX resolver consults only `fn_typed_vars`. A Fn-family binding therefore stops at `trait_vars`, is live, and is simply never consulted at its actual invocation site. A plausible mechanism that reads correctly and is false is the most expensive kind of filing; it survived because it explained the symptom. The direct-field form skips that path and hits the honest catch-all at `collector.rs:1453`. **THE SCOPED BYPASS, EXECUTED and independently reproduced by the coordinator** on `fx5_scoped_policy` (ground truth: the write fires every run): `deny Fs` → exit 1, 3 violations; `deny Fs construct` → exit 1, 1 violation (both calibrations alive); **`deny Fs dispatch` → exit 0, 0 violations** over the function that actually invokes the closure. `show`/`path`/`impact dispatch` all answer "no function matching", while `callers Holder::go` DOES find `dispatch` via the separate call-graph cache — the unit is known to exist and the effect layer treats it as pure. Caller-attribution, the R65/R66 class. **DISTINCT from the ⟨0.35⟩ CHA-completeness clause** and found while falsifying it: rust CANNOT host that clause (see below), and this is what the investigation turned up instead. **FIXED 2026-09-01 (candor-rust `bffac6c`)**: a `leaves_are_callable` helper hedges every Fn/FnMut/FnOnce-leaved binding into `fn_typed_vars`, applied at EVERY binding-producing position found by grepping all `Bound::Traits`/`trait_vars.insert` sites — if-let, match arm, for-loop, let-else, HOF closure param, tuple destructure (both forms), and `while let`, which had NO handler for any trait shape at all. A/B over 256 crates: **zero regressions, 60 function-effect changes all in the safe direction**, and real unforced instances of this bug in `futures-executor`, `serde-untagged` and **`url`** — one of the most-depended-upon crates in the ecosystem. **MY ACCEPTANCE TEST WAS WRONG AND THE AGENT REFUSED IT:** I demanded `deny Fs dispatch` exit 1 post-fix; it does not, and must not, because that would fabricate a literal `Fs` claim on a function whose real effect a syntactic scan cannot see. The sound signal is `deny Unknown dispatch`, exit 0 → 1, calibrations unchanged — verified independently. One gap left OPEN and documented: a closure carried as a bare enum tuple-variant payload, which needs a new Pass-A index. | ~~SILENT~~ CLOSED | the direct-field spelling was always honest, so the exposure was the binding positions only | extend the fn-typed detection that already exists for plain `let` (`expr_is_fn_typed`, `collector.rs:1028`) to the binding-producing positions — `visit_expr_if`, `visit_expr_match`, and by the same audit-boundary rule closure params and for-loop vars sharing the shape — keyed on `is_callable_type` rather than on CHA-leaf-bearing dispatch traits. An existing SPEC §4 property (unresolved callback → Unknown) already shipped elsewhere in the same file; an implementation gap in one binding position, NOT a new cross-engine clause |
| R72 | rust-scan | <a name="r72"></a>**CLEAN NEGATIVE, and it cancelled a planned fix slot: rust CANNOT host the ⟨0.35⟩ CHA-completeness clause.** The java/ts sin is a TOGGLE — adding one pure, unrelated implementor flips an honest `Unknown` into silence, because a non-empty candidate set is treated as complete. Three executed attempts to instantiate that toggle on rust all failed: named-impl dispatch at 0→1→2 impls (0 → honest `Unknown`; 1 → `Fs`; **2, adding a pure sibling → still `Fs`, with `calls` listing BOTH impls** — a union, not a pick); a trait default method (charges correctly); and a blanket impl over an `Fn` bound (pure at both counts — a separate gap, noted below, not a toggle). **Mechanism, read from source:** `collector.rs:1771-1786` resolves local-trait dispatch by iterating EVERY locally-visible impl and pushing an edge for each while `impls.len() <= 12`; zero or >12 is the only route to `unresolved`. That is structurally the OPPOSITE of resolving to a single candidate, and you cannot `impl Fn` on stable, so the defining A/B cannot be constructed. **Exemption by CONSTRUCTION, not an unported MUST** — PART 87 records it as such so it is not re-filed as a gap. NOT ATTEMPTED and stated: `#[derive]`-generated impls of a user trait (syn does not expand proc-macros). Secondary, unfiled: `impl<F: Fn()> Trait for F { fn go(&self) { self() } }` reads pure unconditionally — `seed_fn_typed_vars` iterates `sig.inputs`, which excludes the `self` receiver. Rare in real code; recorded so it is not rediscovered as new. | CLEAN NEGATIVE | — | no action for ⟨0.35⟩; the real rust gap is R71 |
| R73 | swift | **a method call whose RECEIVER IS A MODULE-SCOPE `let`/`var` resolves its call-graph edge to the GLOBAL'S OWN BARE IDENTIFIER instead of to the method — so the calling function collapses to empty and vanishes from `functions[]`.** Five lines of ordinary Swift, no protocol, no closure, no lambda: `final class Worker { func doWork() { <writes a file> } }` · `let worker = Worker()` · `func invoke() { worker.doWork() }`. Ground truth EXECUTED (the file is written). **`deny Fs invoke` → exit 0, `policy ✓`, "nothing hidden"**, and `path invoke Fs` answers "no function matching". INDEPENDENTLY REPRODUCED by the coordinator from scratch. The call-graph sidecar shows the mechanism plainly: `invoke → ["worker"]`, `worker → []`, and `Worker.doWork` disconnected — the edge points at the global, which has no effects. **BOUNDED, and the bound is the honest part:** an UNSCOPED `deny Fs` still exits 1, because `Worker.doWork` is itself in the report. So this is CALLER ATTRIBUTION — it defeats scoped and layered policies, `path`, `gains`, `tour` and `fix-gate`, i.e. the reachability surface this product sells, while a blanket deny is untouched. Same class as R65/R66/R71. **FOUND WHILE FALSIFYING SOMETHING ELSE.** The agent was sent to verify swift was EXEMPT from the ⟨0.35⟩ CHA-completeness clause. The java/ts toggle indeed does NOT reproduce (adding an unrelated conformer changes nothing), but this defect sat underneath it, masked in most cells by an unrelated construction-site over-approximation. **IT IS BROADER THAN THE CLAUSE:** the isolation fixture `yInvoke() { yGlobal.doWork() }` is a plain concrete method call with zero protocols and zero closures and fails identically, so a fix scoped to protocol/CHA dispatch would close some instances and leave the plain-receiver one open. Over-charge control clean across every cell: the failure mode is pure under-report, never fabrication. | CLOSED — candor-swift `9f524cd`, ships in 0.35.0 (panel downstream lens: the advisory's own worked example `invoke` ABSENT on v0.34.0 → `['Fs']`, `deny Fs invoke` 0→1) | unscoped deny unaffected; failure is confined to caller attribution | fix receiver resolution so a module-scope `let`/`var` receiver resolves to the METHOD, not the global identifier — and enumerate the neighbouring receiver positions (instance properties `self.singleton.method()`, cross-file and cross-package globals, existential-array elements dispatched in a loop) rather than the three that were measured. UNEXAMINED and stated: F5 existential-array-as-parameter, instance-property receivers, `--target`/cross-package variants, and whether `fix-gate`/`unverified` already catch what `path` and scoped `deny` miss |
| R74 | swift | **scanning a directory with MULTIPLE SwiftPM executable targets collapses every target's top-level code into ONE `functions[]` entry keyed by the literal qual `"<main>"` — so a PURE target's `<main>` inherits an unrelated target's effects. A FABRICATION**, the opposite direction from R73, reproduced fresh on a 2-target fixture where one target's `<main>` gained an `Fs` it does not perform. Found as a BYPRODUCT of fixing R73, and it **COMPROMISED THAT FIX'S OWN A/B**: swift-nio (5664 analyzed) reported a zero-diff that cannot be trusted, while swift-collections (3023), swift-algorithms (486) and swift-argument-parser (976) were clean and are trusted. Likely the same mechanism the initializer-edge row already tracks — `<main>` is a name, and same-named units merging across modules is a class this file has recorded before. **A measurement taken through a defective instrument is not evidence, and saying which of four packages is trustworthy is the difference between an A/B and a number.** | CLOSED — candor-swift `e5c7871`, ships in 0.35.0 (panel downstream lens: the `<main>` row's loc moved from the pure target to the dirty one; still ONE `<main>` row — the CHANGELOG entry must say exactly that) — fabrication direction | bounded to multi-executable-target trees; single-target scans unaffected | key top-level code by its TARGET, not by the literal `<main>`; then re-run the swift-nio arm of R73's A/B, which is currently unusable |

## 6. The metric (track these four; each "step forward" moves one)

1. **Cardinal-surface coverage** = % of (seam × engine) cells at 🟢 in §4. *As of 2026-07-09: the
   cross-engine matrix holds **72** vs-ground-truth cells (9 indirections/seams × 8 effects — the growth
   history 30 → 48 → 64 → 72 is §7b's narrative), 4 of 6 seam classes are cross-engine-standing (§7b), and
   strict mode (G1/G2) makes the gate un-foolable. The rust-deep column is 🟢 except its gate-evasion cell
   (the masked-literal differential gates the user-facing scan/java/ts/swift gates; deep's policy surface
   stays per-engine); the remaining 🟡 rows are per-engine by nature (§4 note). Target: every closed seam →
   🟢 via the matrix's SEAM axis where it fits, standing per-engine gates where it doesn't.*
2. **Oracle coverage** = # real crates × effects under dynamic ground truth (§3 #1). *2026-06-18: grew 7→11
   crates — Net ×3 (std/minreq/ureq), Exec ×3 (duct/xshell/std), Fs ×4 (fs-err/std/walkdir/tempfile) — now
   ≥3 per syscall-distinguishable effect (Fs/Net/Exec); incl. the walkdir calibration confirmed vs the kernel
   + tempfile as a disclosure probe. + 13 crates with an UNCALIBRATED disclosure probe per
   effect (minreq/subprocess/fs_extra — a crate candor doesn't model, exercised for real; the strongest test).
   Rand/Clock are syscall-distinguishable but markerless/noisy (getrandom has no string arg; HashMap seeds
   getrandom) — covered by the non-syscall recall complement instead. **NOW CONTINUOUS: `realworld-oracle.yml`
   runs on every push to main + every PR (was workflow_dispatch) → kernel ground truth is a STANDING gate, a
   new silent under-report on a real crate fails CI.** And the NON-SYSCALL RECALL complement now covers the
   last 2 effects (Env, Clock — recall corpus 14→20 cases, all sound), so with the syscall oracle (Fs/Net/
   Exec) candor's effect classification is under ground-truth/known-semantics coverage for ALL 10 EFFECTS.
   Recall is also wired into realworld-oracle.yml → BOTH ground-truth methods are now continuous standing
   gates. 2026-06-18: moved the session's adversarial-sweep finds UP the evidence ladder to ground truth —
   syscall oracle 13→**14 drivers** (added `fs_writefmt`: a custom `fmt::Write` writing a marker file via
   `write!`; CI-verified `ran=1 effect=Fs candor=[Fs] certain` — the write-fmt class now KERNEL-gated, the
   strongest evidence, independent of engine logic); recall 20→**23** (seam_lazy_force/seam_thread_local/
   seam_write_fmt, all →Clock). realworld-oracle.yml run GREEN: 14 sound / 0 under-reports / 0 fabrications,
   recall 23 sound. So the systemic write-fmt shared blind spot is now caught by EXTERNAL ground truth, not
   just engine-internal fixtures. NEXT: more uncalibrated recall probes; deepen each effect's real-crate
   diversity.*
3. **Open SILENT residuals** (§5) = count by severity. *As of 2026-07-11 this read "**zero FIXABLE
   silent residuals remain**" — and the **2026-07-18 cross-engine dispatch era FALSIFIED that** (the §1
   point, live): a fresh systematic hunt of the trait-object/dispatch vein found **~20 fixable cardinal
   sins across all four engines** (R32–R44 in the LOG), every one now FIXED + regression-gated + corpus-
   A/B'd + four-way-conformance-clean. So "zero fixable remain" is never a provable state, only "none
   found by the hunts run so far." After that wave: the open residuals are again the FUNDAMENTAL syntactic
   limits (R2–R8, low/v.low) PLUS a NEW niche dispatch long-tail — blanket impls `impl<T: B> Ext for T`
   (fabrication-risk to auto-resolve; low value), nested `Vec<Option<Box<dyn>>>`, tuple-destructured dyn
   returns, `Default::default()` turbofish, and a ts super-interface PRECISION gap (reads sound Unknown,
   not a cardinal sin) — all characterized in the LOG (2026-07-18 long-tail entry), niche, none med+.
   **2026-08-25 adds two of a DIFFERENT KIND, and the distinction is worth keeping: R54 (`diff`) and R55
   (`receipt`) are not blind spots in the CLASSIFIER but verbs that read a report correctly and then decline
   to pass on what it says about itself.** Every residual above is "the engine could not see it"; these two
   are "the engine saw it and the answer does not carry it" — reached by driving the VERB SURFACE rather
   than the effect surface, which is an axis this register had not been swept along before. **Both CLOSED
   2026-08-26**: R54 by porting `gains`' PREFIXED dual-locator shape (candor-java `a73109b`, candor-ts
   `9790ce3`, candor-rust `173a740`); R55 by a new TSV `incomplete\ttrue` row, tie-broken by reading the
   format's real consumer rather than guessing (candor-rust `eb67caf` — the only engine shipping `receipt`
   at all; java/ts have none, swift has no query verbs).
   Target: 0 med+; lows/niche documented-accepted; keep hunting fresh veins (the count is a floor, not a proof).*
4. **Find-rate** = cardinal sins found per fresh adversarial round. *Lede (as of 2026-07-18): SIX find
   eras — the 6th is the **cross-engine dispatch era** (2026-07-18, autonomous, part in PARALLEL via
   subagents): a systematic sweep of the trait-object / dynamic-dispatch vein across ALL FOUR engines found
   ~20 cardinal sins — R32 (a PROVIDED method driving a required OVERRIDE, four-way + a real jsoup Clock
   recovery), R33–R35 (swift deinit-glue / generic-operator / @dynamicCallable), R36 (rust trait-default →
   requirement, java/ts already sound), R37/R37b/R40 (rust dyn-collection: param / generic bound / field),
   R38 (java unbound interface-method-ref), R39 (swift generic-array), R41 (container/Option dispatch —
   HashMap values, Arc<Mutex> guard chains, Option/Result unwrap in every form; rust + swift IN PARALLEL,
   real pgman Log recovery), R42/R44 (rust method returning a bare/collection trait object), R43 (supertrait
   / super-protocol dispatch, rust + swift in parallel). Every one fixed + regression-gated + corpus-A/B'd +
   four-way-conformance-clean, all on 0.22. This era MASSIVELY re-opened the find-rate — the 5th (swift-
   resolution) era's "dropping toward zero" was premature for the SHARED dispatch vein, which had a deep
   systematic hole in every engine (the exact cross-engine-agreement blind spot §1 warns of: engines agreeing
   on the wrong PURE answer). After it, fresh NON-dispatch probes (closures / threads / channels / iterator
   adapters / std-I/O classification / stdio) came back SOUND — the dispatch vein is now saturated; the era's
   find-rate is dropping. Earlier five eras — seam-class, κ-coverage, porcelain, coverage, swift-resolution —
   every find fixed and standing-gated; convergence NOT reached, and each era shift re-opens the find-rate.
   That is the epistemic frame (§1) working as designed: two parallel subagent branches even had their OWN
   corpus-A/B gates catch over-fires mid-flight (R43-swift's 139-fn Unknown blast, self-corrected). The
   instrument's job is to make each new era's finds cheap and standing-gated, not to declare victory.*
   - *Inheritance-of-accessors probe (2026-07-10): 1 find — swift inherited property accessors (computed
     getter / `didSet` / subscript) read silent-pure through a subclass while inherited METHODS climbed
     (R22, fixed 0.8.7, gated). A "covered seam, uncovered edition" find: the accessor climb existed for
     the own type and the method climb existed for inheritance, but their intersection did not. candor-ts +
     java were probed with the same shape and are sound — swift-specific, not shared. Prose: LOG.*
   - *Operator-overload / setter probe (2026-07-10): 1 find — swift setter `newValue` read silent-pure
     (an effect through the implicit value param, `set { newValue.write(…) }`; computed-property + subscript
     setters + `willSet`), because `newValue` was never typed (R23, fixed 0.8.8, gated). The operator paths
     themselves — `==`, `+`, subscript-getter — were sound; ts/kotlin/rust use explicit typed setter params
     (no implicit `newValue`), so swift-specific. Another "covered seam, uncovered edition": accessor bodies
     were collected, but the setter's implicit param wasn't given the type that lets its member calls
     resolve. Prose: LOG.*
   - *Error-path / cleanup-block probe (2026-07-10): 0 finds — an effect that runs ONLY in a `finally` /
     `catch` / Swift `defer`/`guard-else` / Rust error combinator / implicit try-with-resources `close()`
     is charged by all four engines (they walk the full statement tree; the cleanup path is an ordinary
     call edge). Java's try-with-resources — an effect via a compiler-synthesized `close()` — is the
     sharpest case and is sound (the close is real bytecode). Convergence evidence, not promoted to a
     standing gate. Prose: LOG.*
   - *Seam-class era (2026-06-18 → 06-21, rounds 1–17): ~14 finds, all fixed + gated. Highlights: the
     lazy-init forcing site (rust-deep `8bf9c6b`), the agents named-delegation narrowing (a prompt mention
     is not a spawn-set proof, `71a6cc7`), thread_local (R13), and the write-fmt writer side (R14/R16) — a
     SYSTEMIC blind spot silent in four engines at once, the exact case cross-engine agreement hides; since
     then a find in one engine triggers a sweep of all. Rounds 7, 8, 12 and 13 found 0. Validated on real
     code the same era (the PetClinic end-to-end gate, the gson InetAddress catch). Full round-by-round
     narrative: SOUNDNESS-LOG.md, the seam-class-era entry.*
   - *κ-coverage and porcelain eras (2026-06-21 → 2026-07-09; full prose per entry in SOUNDNESS-LOG.md;
     register entries R18–R21): real-app dogfooding found the inherited-into-project silent-pure vein and
     closed the CLASS (batches 25–27, R18); the field-case legacy round mined batches 28–31 to a zero ledger and
     found one live silent-Net member gap (Jackson, batch 30b); the breadth invited six shipped classifier
     regressions (0.8.4 review patch, R19 — caught by review, now moving under scheduled CI); the same
     covered-module shape then surfaced in swift (UserDefaults/Keychain/Bundle, R20); and the 2026-07-09
     whole-project review opened a THIRD find category — porcelain/output-channel fail-opens (R21), plus one
     normative contradiction (AS-EFF-008's opaque case: spec text vs the conformance-pinned fail-closed
     behavior — the written contract had lagged the machine-checked one since the 0.5.15 hardening). All
     fixed; chaining + stale-baseline became conformance PARTs 14–15 the same day, and PART 14's first run
     caught candor-scan's missing empty-report ledger exemption.*
   - *Coverage era (2026-07-10): the first-ever coverage measurement made "documented surface with zero
     executions" a find category of its own — four real bugs, including a broken user-facing gate
     (candor-java's CANDOR_STRICT checkConformance had never been executed by any harness). See the LOG's
     coverage-wave entry and §8.*

## 7. Roadmap (meaningful, measurable steps)

1. **Standing gates (highest leverage):** extend `conformance/gen_differential.py` from 6 indirections to the
   full seam set (each seam × effect × engine = a CI cell). Converts the 🟡 one-shot hunts to 🟢; a regression
   becomes un-shippable. *DONE for the classes the matrix can hold (2026-06-18, §7b): 4 of 6 seam classes are
   cross-engine-standing; deferred-iterator + FFI stay per-engine by nature.*
2. **Grow the dynamic oracle (strongest evidence):** add real crates per effect to `soundness/realworld/`,
   wire `realworld-oracle.yml` to run in CI on every push. *DONE 2026-06-18 (metric #2): 14 oracle drivers +
   23 recall cases run on every candor-rust push. Growth (more crates/effects) remains open-ended.*
3. **Eradicate SILENT residuals (§5):** R1 done (already covered); drive the remaining R2–R8 (all low/v.low) to
   zero or convert to disclosed-Unknown. *Each = a step on metric #3.*
4. **rust-deep parity + unblock its self-guard (R12):** the deep engine must carry every scan fix and
   be continuously gated. *Self-guard part DONE (R12 closed 2026-07-09): the nightly pin moved past the ICE,
   the Self-guard step + `realworld-oracle-deep.yml` run in CI on every push, and `nightly-bump.yml` automates
   future bumps.*
5. **agents seam battery (R11):** run the six seam classes against the agents drift model. *DONE 2026-06-18
   (round 10, R11): one find (named-delegation narrowing), fixed + CI-gated; the §4 agents column now reflects
   it (footnote ⁴).*
6. **Convergence log:** record each adversarial round's find-rate in [SOUNDNESS-LOG.md](SOUNDNESS-LOG.md), one
   entry per round (§6 metric 4 keeps the compressed view); a sustained zero across *diverse* new seams is the
   strongest convergence signal we can have.

## 7b. Gate integrity — the gate itself was code-reviewed + hardened (2026-06-18)

Before extending the gate we reviewed *the gate*. The core is sound: the generative matrix compares each
engine against a **hardcoded EXPECTED effect** (vs-ground-truth, not mere inter-engine agreement — so it
catches even a *shared* blind spot), absent-fn → PURE → DROP → fails, and the callback accepted-band tolerates
`Unknown` but never silent-pure. Three findings, all addressed:

- **G1 (fixed) — silent engine-skip → false multi-engine green.** A skipped (absent) ts/swift engine left the
  verdict "OK" with fewer engines; a misconfigured CI could read a 2-engine pass as a 4-engine guarantee.
  FIX: `CONFORMANCE_REQUIRE_ALL=1` strict mode — a skipped engine now FAILS (run.sh Parts 6/6c + gen_differential).
  Verified: strict + engine absent → exit ≠ 0; strict + all present → exit 0.
- **G2 (fixed) — `check_honesty.py` silently degraded** when the callgraph sidecar was missing (fell back to
  inline `calls`, which misses pure-fn callers — the dangerous case). FIX: strict mode FAILS on a missing
  callgraph. Verified.
- **G3 (in progress) — coverage was the real limit.** The matrix is vs-ground-truth, so every (effect×seam)
  cell it covers is strongly gated and every one it doesn't is ungated. Extended the EFFECT axis 5→8
  (added **Rand/Db/Log**, the proven-cross-engine vocab; matrix 30→48 cells, all engines agree). **Ipc/Clipboard
  stay out by design** (no JDK std IPC primitive; no node clipboard model — structurally per-engine). Remaining
  G3 work = the SEAM axis (add renderers for lazy-init / deferred-iterator / fire-and-forget / implicit-conversion
  / gate-masking / FFI to the matrix) — the next roadmap increment. **STARTED: implicit-conversion +
  fire-and-forget are now matrix INDIRECTIONS (matrix 48→64 cells, all 4 engines agree, exact {effect}) —
  those two seam rows are now 🟢 cross-engine-standing. THEN lazy-init too (matrix 64→72, all 4 agree) → 3 of 6
  seam classes are now 🟢.** The other 3 don't fit the EFFECT matrix: deferred-iterator (java whole-program CHA
  over a shared `Iterator` interface unions every cell's effect — footnote ³); gate-masking (a POLICY-verdict
  seam — extend the policy differential instead); FFI (expected is {Unknown}/disclosure, no clean ts idiom).
  So the matrix seam-axis is effectively COMPLETE for the classes it can hold; the residual 3 stay per-engine
  (🟡) by their nature, documented here. THEN gate-masking too, via a SEPARATE policy-verdict differential
  (`gen_masking.py`, also wired into run.sh): for each literal-surface effect {Net→host, Exec→cmd, Fs→path,
  Db→table}, a MASKED denied literal beside a benign one must FAIL the `allow <Effect> <benign>` gate
  (fail-closed) in every engine, and the compliant program must PASS — 16 (effect×engine) cells, all green.
  Building it SURFACED a real swift gate-evasion: `shellOut(to: runtimeVar)` (ShellOut, classified Exec) was
  missing from the Exec establishing set, so a masked command evaded `allow Exec` — FIXED (candor-swift 0.5.21,
  `1a60bce`). So **4 of 6 seam classes are now cross-engine-standing** (implicit-conv/fire-forget/lazy-init via
  the effect matrix; gate-masking via the policy differential); only deferred-iterator (java CHA artifact) and
  FFI (expected {Unknown}, no clean ts idiom) remain per-engine by nature.

**CI action item — DONE (2026-07-09):** conformance.yml now carries a macos four-engine job running with
`CONFORMANCE_REQUIRE_ALL=1` (all four toolchains provisioned), alongside the fast ubuntu three-way leg and a
weekly released-artifacts leg. Strict mode is a standing CI property, no longer local-only.

## 8. How to read confidence today (2026-07-10)

- **Floor is solid and continuously gated:** the §4 disclosure invariant (never silent-pure where an effect
  is reached) is a standing conformance part over every engine's own report; the kernel syscall oracle +
  recall corpus run on every candor-rust push; the cross-engine matrix holds 72 vs-ground-truth cells;
  the masked-literal policy verdict, gate-verdict/exit agreement, `.candor/config`, chaining and the
  stale-baseline posture are all standing four-way differentials (conformance PARTs 12–15).
- **The find-rate has NOT converged** — and the finds have changed shape. The seam-class era (rounds
  1–17, ~14 finds, all fixed) gave way to the κ-coverage era: real-app dogfooding keeps surfacing
  covered-namespace member gaps (Panache, the inherited-into-project class, the legacy-enterprise tier,
  Jackson's live silent-Net, swift's UserDefaults/Keychain) — and breadth-work invites its own
  regressions (the 0.8.4 review patch: six classifier regressions shipped in 0.8.3, caught by review,
  not CI). The 2026-07-09 whole-project review added a third era: the *porcelain* — fail-opens in the
  layers users invoke (cargo-candor policy/guard, gate-json write paths, deps resolution) that the
  engine-level fail-closed doctrine never swept. And the 2026-07-10 coverage wave added a fourth:
  *documented surfaces with zero executions* — the first-ever coverage measurement showed several
  documented, load-bearing gate surfaces had never been executed by any harness, and pinning them found
  four real bugs, including a broken user-facing gate (candor-java's CANDOR_STRICT `checkConformance`).
- **So: high and rising on the analysis core; the active frontier is κ-coverage member gaps,
  gate-surface fail-opens, and never-executed surfaces — each now with standing gates.** Treat any new
  framework's inherited-into-project shape, any new output/auxiliary channel, and any documented surface
  with zero executions as guilty until gated.

### 8.1 Round & batch index (prose in [SOUNDNESS-LOG.md](SOUNDNESS-LOG.md))

| entry | date | engine | class | outcome |
|---|---|---|---|---|
| Seam-class era, rounds 1–17 (find-rate narrative) | 2026-06-18 | all engines | 6 seam classes + sweeps | ~14 finds, all fixed + gated |
| thread_local force (R13) | 2026-06-18 | rust-deep `6010832` | **SILENT** (macro-gen init orphaned) | fixed + gated |
| write-fmt writer side (R14 + R16) | 2026-06-18 | deep/scan/swift/java | **SILENT** shared blind spot (writer sink) | fixed in all 4 engines |
| Java adversarial round | 2026-06-20 | java | ~55 synthetic fixtures, 5 mechanism families | 0 silent |
| Cross-language round | 2026-06-21 | java (Kotlin/Groovy) | compiler-generated dispatch | 0 silent, find-rate 0 |
| κ batch 24 — Hibernate-6/Jakarta Data | 2026-06-21 | java `4231ed6` | DISCLOSED invisible → modeled Db | precision |
| κ batch 25 — Quarkus Panache | 2026-06-21 | java `fdd2278` | **SILENT-PURE sin** (inherited-into-project) | fixed + gated |
| κ batch 26 — Micronaut/Ebean/ActiveJDBC/jOOQ | 2026-06-21 | java `93c3116` | **SILENT-PURE** ×4 (same vein, probed) | fixed + gated |
| κ batch 27 — modeled-base subclass, general | 2026-06-21 | java `699c1ea` | **SILENT-PURE** class closed | fixed + gated |
| Cross-engine vein check | 2026-06-21 | scan/ts/swift | inherited-into-project | java-specific, not shared |
| Abstract-stream entry-point params (R17) | 2026-06-21 | java | **SILENT**, narrow surface (measured near-empty) | fixed + gated |
| κ batch 28 — legacy-enterprise tier (JCL/…) | 2026-07-06 | java `dae561b` | DISCLOSED → modeled | precision |
| κ batch 29 — next tier, same discipline | 2026-07-06 | java `d8035d3` | DISCLOSED → modeled | precision |
| κ batch 30/30b — Jackson (+ live silent-Net find) | 2026-07-06 | java `f434361` | **SILENT-NET** member gap | fixed + gated |
| κ batch 31 — long-tail sweep, ledger → zero | 2026-07-07 | java `5210837` | DISCLOSED → modeled | ledger empty |
| 0.8.4 review patch | 2026-07-08 | java `18717b5` | **6 regressions** batches 28–31 shipped | fixed + gated |
| κ batch — UserDefaults/Keychain/Bundle | 2026-07-09 | swift `dd134e2` | **SILENT-PURE** (covered-module) | fixed + gated |
| Whole-project review (porcelain fail-opens et al.) | 2026-07-09 | all repos | fail-open gate surfaces, doc drift | fix wave, conformance PARTs 14–15 added |
| candor-scan κ-ledger §2 rule-3 gap | 2026-07-09 | scan `2d32086` | over-disclosure on chained empty reports | fixed, PART 14-pinned |
| candor-java mutation_probe rot | 2026-07-09 | java `bca8040` | meta-soundness decay (3/14 patch-error) | re-anchored 14/14, weekly CI |
| Coverage wave — never-executed gate surfaces | 2026-07-10 | all engines | **4 bugs in 0-coverage surfaces** (strict-gate 001 over-fire, guard fail-open, positional swallow, watch no-quit) + swift payload-host parity | all fixed red-then-green; TESTING.md standards |
| Corrupt-report false all-clear (read side) | 2026-07-13 | rust `fdb5e63` + java `bc13f53` + ts `d0d0b1f` | **READ-side cardinal sin** — a found-but-untrustworthy report → `tour` "nothing hidden" / `map` empty at exit 0. Syntactic (truncated) hit rust+ts; semantic (bare junk array) hit rust+java. Swept ALL 4. | fixed loud exit 2 (syntactic + semantic); clean-empty still exit 0 (parity); PART 4k-pinned four-way + unit/fuzz gates |
| Loudness left unfinished: verbs × surfaces (max review) | 2026-07-13 | all engines + suite | **the same §4 class one surface over** — rust comparatives, ts MCP (15 sites), swift gains semantic hole, partial-graph origin downgrade ×4, fail-open 4i/4j oracles | all fixed same-day + gated (PART 5b partial, fail-closed oracles, per-engine pins); lesson: sweep engine × verb × surface |
| Alarm mutes via storage/identity, not parsing (max review r2) | 2026-07-14 | gains corpus + java union + swift/rust gains | **the ⚠⚠ oracle itself** — fabricated `{}` cache, report-only hash key, engine-blind sidecar union, --json-only disclosure | all fixed + pinned; lesson: audit the whole decision chain, undecidable oracle must fail TOWARD the alarm |
| New-effect attribution over-matches (Llm/privacy max review r3) | 2026-07-14 | all engines + candor-scan | **FABRICATION** — :11434 any-host + bedrock-substring over-match, ts raw-literal, swift AVAudioEngine playback; + the reqwest-builder under-report mirror | all fixed four-way + PART 4m given negative-case teeth; lesson: pin BOTH directions, fix a shared predicate everywhere it was copied |
| Provided Write/Read method → local required-method impl (R32, direct-method sibling of R14/R16) — **ALL FOUR ENGINES** | 2026-07-18 | rust scan+lint `d478ab2`; java `577c38d`; swift `ef1d1c7`; ts `77cd4ae` | **SILENT-PURE** — a std/library-provided method drives the receiver's required override, invisibly: rust `s.write_all()`/`r.read_to_end()`; java `w.write(String)`/`r.read(char[])`; swift a protocol-extension `provided()` calling `req()`; ts a node `Writable.write()`→`_write`. Only the format-macro/facade edge (R14/R16) was recovered before | fixed in EVERY engine; 0 over-fire A/B (~900 rust + ~12k java + ~1.5k swift + ~770 ts real fns) + a genuine jsoup Clock recovery; four-way conformance OK; regressions + ui pins in each |
| deinit-glue — an effectful `deinit` charges the constructing scope (R33) | 2026-07-18 | swift `3f5b0f4` | **SILENT-PURE** — `let r = Resource()` where `Resource.deinit` does I/O runs at scope exit (ARC, non-escaping local) but read pure; swift-only (rust Drop-glue already sound; java/ts have no deterministic destructor) | fixed via a `propertyEdges` soft edge (drops on struct/pure-class — no external-protocol-fallback fabrication) + a returned-identifier escape gate (no `makeNSView` factory over-charge); 240 suite + regression; 0 over-fire A/B ~1.5k fns; conformance OK |
| swift generic-operator (R34) + @dynamicCallable (R35) dispatch | 2026-07-18 | swift `7f6ba58`, `2667fdc` | **SILENT-PURE** — a generic `a+b` on a `T: P` local-operator-protocol bound, and `c(1,2)` on a `@dynamicCallable` type, both read pure (the desugar was invisible) | R34 emits protoDispatch(P,op) for a protocol-typed operand; R35 a `<t>.dynamicallyCall` soft edge; each a named regression, 0 over-fire A/B ~1.5k fns, conformance OK |
| trait/interface DEFAULT calling a requirement (R36) — closed four-way | 2026-07-18 | rust `a60ca09` (java/ts/swift already) | **SILENT-PURE** (rust only) — a trait default's `self.persist()` reached an effectful impl witness but read pure; java (bytecode CHA) / ts (class-CHA) / swift (R32) already sound | rust CHA over `trait_impls` gated on `trait_decls` declaring the leaf, bounded ≤12; full suite + regression; 0 effect changes A/B ~950 fns; conformance OK |
| post-dispatch probing — gate surface verified; R48/R49 characterized | 2026-07-18 | rust-scan (no ship) | gate SOUND (Unknown non-silent + mistyped-class fail-closed + incomplete exit-2); two THEORETICAL rust veins (local-macro-template R48; drop-guard-as-field R49) | R48 zero-incidence → documented-with-fix; R49 prototype BUILT+regression-green then **A/B-REVERTED** (14 false Unknowns on flate2 — constructor-returns-owner escape); both residual, honesty-first over net-harm |
| inline struct-literal receiver typed (R50) | 2026-07-18 | rust-scan `09099cd` | **SILENT-PURE** (rust only) — a value CONSTRUCTED INLINE and immediately consumed (`for _ in (RowIter{conn})`, `(RowIter{conn}).count()`) typed to nothing, so the iterator-forcing edge + method resolution dropped it pure; `resolve_recv_type` handled `Call` (ctor_type) but not `Struct` | added the `Struct` arm; A/B **ZERO over-fire across ~2600 real fns** (syn 1442/serde_json 348/tokio 148/…); regression extended; four-way conformance OK; ts/swift swept SOUND (rust struct-literal-syntax-specific — they construct via calls) |
| smart-pointer ctor typed as its pointee (R51) | 2026-07-18 | rust-scan `774da5f` | **SILENT-PURE** (rust only) — `let db = Arc::new(Db::new()); db.migrate()` read pure: `ctor_type` typed the ctor as the impl-less wrapper "Arc" and dropped the `<Db>` arg (`type_path` already peels an `Arc<Db>` FIELD/param — this closes the local-binding form + inline `Arc::new(..).m()`) | `ctor_type` types `Box`/`Rc`/`Arc::new(x)` as the pointee (arg's ctor_type); Mutex/RefCell NOT peeled (methods on the wrapper). A/B **ZERO over-fire across ~2400 real fns**; the clone anti-fabrication guard verified intact (a local `Arc::new` receiver's `.clone()` still not charged); regression + four-way conformance OK; rust-specific (no Box/Rc/Arc in java/ts/swift). Clone-rebind sibling → R52 |
| syscall arms — disclosure-recall calibration | 2026-07-25 | rust + swift `soundness/realworld/recall/` | **INSTRUMENT gap, not an engine defect** — the two syscall oracles reported 0 violations with no evidence they *could* report one (attribution-calibrated only); a stalled marker or a loosened verdict would read identically to soundness | battery seeds a false all-clear into the REAL oracle between analyzer and verdict (`silent` / `wrong` / `transitive`): rust program-level **19/19** + fabrication mirror, per-function **4/4 on all three** with the `transitive` pass naming exactly the one falsified frame; found 2 defects in its own first run (unfalsifiable pure control; a missing `python3` impersonating 13 findings) — both fixed; CI-gated weekly + on oracle change |
| Node arm — transitive recall (how far UP a chain) | 2026-07-25 | candor-ts `cf91b95` | **INSTRUMENT bound, measured not asserted** — a false all-clear can sit on a CALLER whose leaf is honest; whether this arm could see that was an adjective ("weaker against a dropped-caller-edge miss") | 7 of 12 on-path caller frames reachable, **5 UNCORROBORATED** (out of reach but disclosed — trusting the analyzer, not confirming it), **0 BLIND**; boundary is exactly the function whose span lexically contains the continuation, confirmed by a probe fixture; a fire-and-forget control stops correct "pure" claims reading as blind spots (the first draft invented 4); gate verified to catch |
| Node falsifier truncated at 10 stack frames | 2026-07-25 | candor-ts `verify-emit.mjs` | **INSTRUMENT under-reach in the SHIPPED oracle** — transitive attribution reads `new Error().stack`, whose default `Error.stackTraceLimit` is 10; outer project frames were silently dropped, so a false all-clear on them could not be caught. 16-deep sync chain charged **5 of 16** | raise the limit during our own capture + restore (app `Error` untouched), `CAPTURE_DEPTH=256` stated not assumed; probe now 16/16; standing depth gate verified to catch; held-out slice re-run on the corrected engine and filed BESIDE the frozen record. Biased the dangerous way — truncation only loses checked frames and misses violations. FIXING IT EXPOSED A SECOND: CJS module-loader reads charged to the program (`require()` alone → Fs at every require site) — guard keys on the CALLER, not the stack. Corrected held-out slice 92 checked vs 85, 4 violations, **2 NEW candidate silent under-reports traced to source** (a module initializer reaching an unanalyzed dependency's top-level effect, e.g. graceful-fs.js:35's `process.env.NODE_DEBUG`) — recorded, not repaired |
| the initializer edge across the scan boundary (module-import / global-read / `<clinit>` / lazy-static) | 2026-07-25 | **ALL FOUR ENGINES, BOTH SIDES OF THE BOUNDARY.** ts `70553c3` (intra-project) + `3643cd9` (chained) + `fab67fd` (`--dep-inits`, scans them for you); swift `acfed07` (intra-package) + **`7a1b077`** (chained dep); **java `0d8cb1e`** (a dep class's `<clinit>`, superclasses per JVMS §5.5); **rust `b2cfd9f`** (a dep's lazy static, marker bounded at consumption after a first prototype added a spurious callgraph node and was reverted). Sweeping every engine — rather than assuming java/rust were clean because they model the INSIDE-scan case correctly — is what found the last two. **Pinned by conformance PART 19 — all FOUR engines MATCH**, verified to CATCH (unchaining the ts dep makes it DIVERGE while java/rust match) | **SILENT-PURE** — a module whose own top level is pure imports a dep whose top level is not; importing runs it, so the initializer transitively performs the effect and candor reports `(∅,∅)`. Found on real code (proper-lockfile → graceful-fs.js:35 `process.env.NODE_DEBUG`; write-file-atomic → Rand) | **INTRA-PROJECT HALF FIXED** — and the vein was NOT about dependencies: the edge was missing even between two analyzed project modules. A/B 6 repos zero losses / zero non-initializer gains; held-out `proper-lockfile` 1 violation → 0. Concept is right in the reference engine — with the dep INSIDE the scanned set candor-java gives `app.App.<clinit> { Env* }` one hop via `dep.Dep.<clinit>`; outside it, dropped silently. Blanket disclosure MEASURED AND REJECTED (60–100% of modules across 6 packages, candor-ts itself 83%). Resolution is DETERMINATION not disclosure: chain the dep report (`deps`) or scan dependency initializers only. **swift NOT N/A — the vein is LIVE there** (globals are lazy, so a READ forces the initializer; `globalReads` exists but is skipped when the global is the base of a member access — the commonest shape). The two-line fix recovers the fixtures but produced **113 gains / 226 fns** on candor-swift's own tree from module-flat name collisions (`pipe`/`run`/`env` locals matching same-named fixture globals) → **A/B-REVERTED**. Two further attempts also reverted, and the ORDER is the finding: module-scoping cut fabrications 113→94; a file-scope guard (a real defect — a `let` inside a top-level `if`/`for` block is a LOCAL, and candor-swift registered it as a global; `<main>` unchanged so nothing lost) removed the rest but RE-SHUFFLED which colliding declaration wins (`DeclCollector.pushType`, a 3-line array append, went pure → `{Env,Fs,Unknown}`). **Root cause: global units are keyed by BARE NAME.** Required order: (a) unique qualified keying, **(b) file-scope guard — SHIPPED `080fb3e`, A/B'd in isolation: 0 gains on candor-swift (9 effectful phantoms dropped) + swift-syntax (6), `<main>` unchanged, type members untouched**, **(c) DONE `acfed07`** — reading a MEMBER of a global (`cfg.count`, `analysis.allFns`, `SYNTAX_NODES.map{}`) forces its initializer; exclusions for the member NAME and for same-named INSTANCE PROPERTIES keep it exact. 26 gains / 0 losses on candor-swift + 7/0 on swift-syntax, and **all sampled gains TRACED to genuine global-to-global reads**, not assumed. Three reverts, each right at the time: 113 fabrications → 34 (unique identity) → 26 (self-property exclusion) → all genuine. **(a) is NOT edge filtering** — a 2-module fixture shows two same-named file-scope globals **MERGE into one unit** carrying the union of their effects, and a reader of one inherits the other's (`utilUses` gains `Fs` from a different module) — a LIVE fabrication today, independent of the vein. Filtering removes it but also removes the reader's genuine `Env` (fabrication → silent under-report, wrong trade) and is a no-op post-(b). (a) = module-qualified global unit IDENTITY so they do not merge; spec-visible. rust SOUND (LazyLock force, `main { Env }`), java SOUND (both `<clinit>` and method forms). SOUNDNESS-VEIN-initializer-edge.md + fixtures |
| effect mechanisms that die at the SCAN BOUNDARY | 2026-07-25 | **ALL FOUR ENGINES (OPEN; rust 3 of 5 fixed — `5c68de3` stringification, `83fb749` drop glue, `c8c8de6` interfaceUnion in `--deps`; java/ts/swift in progress)** | **FALSE ALL-CLEAR ON A GATE.** Code candor analyses soundly in ONE crate loses the effect entirely when split across a crate boundary with the dep report chained: `format!("{}", e)` on a dep type whose `Display::fmt` reads the clock gives `describe -> ['Clock']` combined and **PURE** chained — and `deny Clock` **exits 1 combined, 0 chained** | five root causes ranked: the join fires only on crate-QUALIFIED paths (desugared `Display::fmt`/`Drop::drop` emit none, though the dep report holds the right entry); no return types travel, so a factory-bound receiver is untyped; `interfaceUnion` is shipped-but-OFF in the default `--deps` path; `trait_decls` is local-only; coverage is crate-granular so one resolved call clears the blind flag for every shape. Also: the sweep's first framing ("chaining deletes the disclosure") was CORRECTED — the miss exists with and without chaining. **GATE-LEVEL IN ALL FOUR** (java 13/13 shapes, swift 7/13, ts 5 families, rust 5 — `deny` flips exit 1 → 0 in every one; implicit stringification is silent across the boundary in all four, the vein closed FOUR-WAY that same morning). **Each engine recovers something the others miss** (ts: inheritance + factory receivers + dep's Unknown; swift: dep's Unknown WITH whyReason + `CANDOR_WORKSPACE_CHAIN` on child scans; java: the direct-hash path), so the union of what they already do is close to the full fix — tractable, not fundamental. Originally stated as **GATE-LEVEL IN BOTH** (`deny Env` on the JVM fixture: exit 1 together, exit 0 chained). **JVM: 13 of 13 probed shapes read PURE (none Unknown)** — incl. the implicit-stringification vein CLOSED FOUR-WAY THE SAME MORNING, still live across the boundary (`app.S.show` Env→pure while the dep report holds `lib/Entry.toString…`→Env). JVM causes: CHA is project-only and an empty CHA emits no Unknown; `this.load()` on a dep supertype has a PROJECT owner so the join is never reached; HOF/callback surfaces project-only with the Unknown fallback gated off for exactly those cases; **a dep's own disclosed Unknown is dropped**. Information already present in 12/13. SOUNDNESS-VEIN-crossing-the-scan-boundary.md |
| global/lazy unit identity is not module-qualified | 2026-07-25 | **CLOSED four-way — rust `48b7669`, swift `b616caf`+`7cec437`+`7f18c38`** | **BOTH HALVES OF THE HONESTY-CRITICAL UNION FROM ONE DEFECT.** Two modules declaring a same-named module-scope global collapse into ONE unit with the union of their effects. **rust = SILENT UNDER-REPORT**: two `<lazy>::CFG` entries, same conflated loc, and the readers vanish (`util_uses`/`main` go `['Env']` → ABSENT/pure). **swift = FABRICATION**: `utilUses` is charged `Fs` from a global in another module it never touches | four-way sweep: **java SOUND** (`core.C.<clinit>`, fully qualified) and **ts SOUND** (`core.core.<module>`, path-qualified) — so the fix has in-family precedent, it is two engines not doing what the other two do. rust qualifies ordinary fns correctly (`util_m::util_uses`); the `<lazy>::` synthetic REPLACES the module path. Edge-filtering rejected (loses the genuine effect too). **rust FIXED**: `<lazy>::CFG` → `<lazy>::util_m::CFG`, module path INSIDE the prefix so tail2 stays discriminating; shared `lazy_qual` for declaration + forcing edge; A/B 0 gains / 0 losses on 8 codebases (a NO-REGRESSION result — none has an effectful module-scope lazy static — recovery pinned by the regression test); 178 tests, conformance 26/0. Spec-visible; blocker (a) of the initializer-edge vein. **SWIFT'S SIBLING IS IN FUNCTIONS TOO**: two modules' same-named `func shared()` are grouped as OVERLOADS and every caller edges to BOTH (`coreEntry`/`utilEntry` each get `['Env','Fs']`) — units stay distinct (`shared()`/`shared()#1`), RESOLUTION unions them. **"Prefer the caller's module" is UNSOUND, measured twice + reverted**: all-candidates dropped 19 real `Unknown`s on swift-syntax (a type extended in another module is idiomatic); narrowed to free functions it still dropped one — `SwiftSyntaxMacrosTestSupport` delegates to `SwiftSyntaxMacrosGenericTestSupport.assertMacroExpansion`, cross-module delegation between same-named free fns. **That lead became a SEPARATE CARDINAL SIN, now FIXED (`7cec437`)**: a module-qualified free call (`Core.shared()`) is neither `typed` nor `unqualified`, matched NO resolution branch, and produced NO edge — `delegates()` read sound-complete pure while calling a filesystem reader. The qualifier is NOT lost (the collector keeps it as `extOwner`; only `call.path` is stripped), so the fix is exact: base must be a real target, not also a local type, with exactly one free fn of that name. A/B 0 losses + **1 real recovery** (`assertMacroExpansion#1` gains `Unknown` on swift-syntax). **The UNQUALIFIED collision is ALSO FIXED (`7f18c38`)**: with qualified calls resolving on their own path, the twice-reverted module preference re-measured **clean (0 gains, 0 losses)** — the counterexample that killed it is written module-qualified, so it no longer had to steal that edge. The earlier revert was right about the measurement and wrong about the cause: it was judged on a broken foundation. Free functions only (members/inits legitimately span modules — 19 lost `Unknown`s when applied there). **SWIFT GLOBAL HALF ALSO FIXED (`b616caf`)**: file-scope globals are ACCESSOR units, which the overload disambiguation pass excludes — so two modules' `let cfg` merged. They now get the same `#n` treatment + module-scoped read resolution. A/B 0/0 on both corpora (no-regression; recovery pinned by test). **Vein CLOSED in all four engines**, and it clears blocker (a) of the initializer-edge vein. SOUNDNESS-VEIN-global-unit-identity.md |
| a caller of a BODY-LESS LOCAL DECLARATION read pure | 2026-08-13 | **CLOSED — candor-ts; pinned four-way by conformance PART 46** | **CARDINAL SIN, ts only.** `localName` mints a unit for any declaration it can name without asking whether it has a BODY, the call site edges the caller to that EMPTY unit, and the caller unioned nothing and was certified pure — a purity claim over code the engine never saw. `deny Unknown`, the gate whose entire purpose is "fail if candor cannot see what this reaches", exited **0** where rust/java/swift all exit 1. Shapes: ambient `declare function`, any member of a local `.d.ts`, an `abstract` member no subclass overrides | Found by `candor/bin/corpus.sh`, unreachable from any hand-written fixture: candor-ts's whole report for **axios is 54 `index.d.ts` declarations** while its 61 `.js` files are never read — `analyzed.count: 54` + `functions: []` + no `unanalyzed` = ⟨0.24⟩ **row 2**, believe-and-do-not-hedge — and stderr said "wrote 0 effectful functions", so BOTH channels read as an all-clear. **THE SIBLING ROUTE**: the same shape in a DEPENDENCY has been pinned since the scan-boundary work (PART 21 + candor-ts's own four `boundary:` rows); nobody asked it of the project's own source. **No spec version moves** — §3 already required the disclosure, §4 already defines `native:` as "a boundary to code the engine cannot analyse"; what was missing was a ROW. Engines legitimately differ on WHERE the charge lands (java on the DECLARATION unit, rust/swift at the EDGE), so PART 46 asserts on the CALLER's transitive set — what a gate reads — never the reason string. ts takes java's shape (mint-side = ONE place; edge-side = every call and desugar site, the drift that made `discloseUnanswerableKey` one function after being two). **THE OVER-CHARGE CONTROL IS MOST OF THE WORK, and it is measured**: charge only where NO LOCAL BODY ANSWERS, since a base member with a local bodied override is already resolved by the class-CHA. With that condition **zod +0** and **hono +18 → +9**; without it the crude rule charges hono's `EventProcessor` (6 abstracts, 3 local subclasses) and zod's `ZodType._parse`. All nine hono survivors are true positives (`Deno.mkdir`/`writeFile` Fs; `Deno.upgradeWebSocket`/`FetcherLike.fetch` Net — local `.d.ts` shims). Second trap: an OVERLOAD SET is N body-less signatures preceding the implementation under ONE unit name, so the marker mirrors `fns.set`'s last-write-wins or every overloaded function in every project becomes unanalysable. Both are fixtures; PART 46 carries the locally-bodied control in all four arms. **Calibrated** — re-run with the fix reverted, PART 46 reddens on ts alone. **RESIDUAL (open, filed in `candor/BACKLOG.md`)**: this closes the "candor cannot see" channel, not the blindness — axios `deny Unknown` is exit 1 now, **`deny Net` is still exit 0**, because the effects live in the 61 `.js` files the scan never reads |
| `Exec` charged the LAUNCH, not the subprocess CAPABILITY | 2026-08-24 | **CLOSED — candor-java; pinned four-way by conformance PART 66** | **SILENT UNDER-REPORT, java only.** The engine enumerated the launch verbs (`ProcessBuilder.start`/`startPipeline`, `Runtime.exec`, the `Desktop` openers) and the live-child control surface; everything else on `java.lang.ProcessBuilder` read pure. `ProcessBuilder arm(String[] argv) { return new ProcessBuilder(argv); }` reported `inferred: []` and passed `deny Exec` at **exit 0** — a fully-armed invocation assembled from caller-supplied argv, certified clean, because splitting build from launch across two methods (or two jars — the supply-chain form) left nobody holding the effect. Not a wrong rule, an ABSENT one: **an allowlist under-reports every verb nobody enumerated**. The type was already recognised at CONSTRUCTION for `cmds` extraction and even carried `incomplete: ["Exec"]` — recognised for the LITERAL, not for the EFFECT. rust/swift/ts already charged the capability; java was the lone outlier. | **Fixed as a DENYLIST** (SPEC §1 ⟨0.32⟩ requires that shape and forbids the allowlist): whole type is `Exec`, carve-outs by name — `environment()` stays `Env`, the NO-ARG read-back overloads (`command()`/`directory()`/`redirect*()`) keyed on the DESCRIPTOR because each shares its name with the setter (a blanket "no args ⇒ pure" would have exempted `start()` and `inheritIO()`), plus the §4 Object protocol. **Over-charge controls written FIRST and green before AND after**: read-back-only, a project-local type sharing the name, `environment()` staying `Env`, and an `OpenOptions`-shaped option builder for another effect staying pure (the ruling's boundary). **A/B: 933 JVM jars, 0 verdict flips / 0 effects lost / 0 functions newly flagged**, and the null EXPLAINED by a bytecode census — 21 `ProcessBuilder` call sites in 9 methods, all 9 launching in the same body — with the harness first proved to catch (rc 0→1 on the gap fixture). the field case baseline byte-identical. **Cross-engine residuals found while building PART 66 and FILED, not fixed:** swift charges construction but not configuration and misses the qualified `Foundation.Process()` spelling; rust over-charges `get_program()` and an `OpenOptions` builder while missing `open` on a received one, and fabricates on a local `Command` when the file also imports the std one. SOUNDNESS-LOG.md 2026-08-24 entry |
| macro/codegen row's "—" pasted across 3 engines unreasoned (R56-R58) | 2026-08-27 | swift `94d1aef` CLOSED; ts `0a5d493` CLOSED (R57, 2026-08-28); java R58 UNMEASURED | **swift: SILENT** (no visitor for `MacroExpansionExprSyntax`, zero disclosure) — fixed. **ts: SILENT**, live-reproduced (anonymous decorator, `deny Fs` exit 0) — fixed the next day, re-verified against the pre-fix binary, pinned by conformance PART 81; a narrower residual filed as R64. **java: UNMEASURED** (Lombok immune by construction; Dagger/Room-style separate-file codegen never checked) | swift + ts fixed and gated; java filed, no candor-spec fix owed; R64 filed against ts, no candor-spec fix owed |
| the ADVISORY VERBS certified what the gate had just started refusing | 2026-08-24 | **CLOSED four-way — candor-rust `06edf9e`, candor-swift `7ce37ba`, candor-java `e60a832`, candor-ts `9f22581`; pinned by conformance PART 67. RESIDUAL OPEN: candor-ts's LSP route** | **FALSE ALL-CLEAR, all four engines at once.** ⟨0.32⟩ made an unread exclusion class a verdict cause and it reached `gate --report` only: over the identical bytes `fix-gate --strict` exited **0** printing *"no deny/pure boundary crossings ✓"* and `unverified --strict` exited **0** printing *"every function … PROVABLY clean ✓"*, and `--strict` is how CI consumes both. **The third time a new verdict cause reached the gate and not these two siblings** — ⟨0.24⟩ `unanalyzed`, ⟨0.30⟩ `outOfScope`, ⟨0.32⟩ `excluded[].peeked` — the third happening inside a file whose own comments say the second one means *"the ARM is what a new verdict cause needs, not a note telling the next person to remember"*. And nothing asserted the fix: PART 62 pins the CAUSE and drives the gate route only, so four commits shipped with no gate under them. | **PART 67, named for the RELATION** (§3.1 ⟨0.24⟩, *"an advisory verb must never be LESS SENSITIVE to incompleteness than the gate over the same bytes"*) so a fourth cause needs a fixture, not a rewrite; **two §3.1 statements move `pre-ledger` → part-named** in the MUST ledger, having been correct, prominent and unasked since ⟨0.24⟩. One tree per engine scanned TWICE (with the policy, without), so the only difference between the two reports is whether the peek was asked and the excluded file is CLEAN in both — the refusal is attributable to the DOCUMENT, not to content. **Over-charge control is half the row** (0/0/0 over the peeked report: an engine refusing everything scores a perfect 2/2/2). **Falsified against pre-fix binaries** — rust `9bf3f2f^` reddens at 2/0/0, java `3682835^` at 0/0/0. **THE TRAP, guarded by `ck67`:** `unverified --strict` has its OWN exit 1 on an `Unknown` hole, which looks like the rule firing and is not, so the instrument refuses any fixture carrying one (or an empty `functions`). **Two routes measured CLEAN and pinned rather than assumed:** java `--parallel` (cannot gate; asserted on the document — `excluded` byte-equal to a standalone scan, all three verbs refusing over it) and ts MCP `candor_gate`. **RESIDUAL, MEASURED AND OPEN:** candor-ts's **LSP** route does not share `loadGateReport` and has no `peeked` handling at all — `didOpen` over the unread report under `deny Exec` publishes `diagnostics: []` and no log line, byte-indistinguishable from the certified control, with the instrument proven first (`deny Fs` draws its squiggle from the same report). No row written: the fix belongs to candor-ts. SOUNDNESS-LOG.md 2026-08-24 advisory-verbs entry |
| a peek finding is scope-matched against the WRONG ENTITY (the ⟨0.34⟩ peek-scope-match sin) | 2026-08-29 | **CLOSED four-way — candor-swift `793e796`, candor-rust `84ccbf1`, candor-java `7fe654f`, candor-ts `8584572`; pinned by conformance PART 85 (falsified against each pre-fix commit)** | **SILENT UNDER-REPORT, all four engines, three different root causes.** A peek re-analyses excluded material and attributes a finding to the EXCLUDED declaration's own qualified name — correct — but the `<scope>` test a `deny`/`pure` rule runs was matched ONLY against that name, never against any IN-SCOPE CALLER reaching it via dynamic dispatch. Measured identically four ways: an in-scope dispatcher whose one visible implementer is pure, plus an excluded conformer/override performing `Net` reached only through that dispatcher — `deny Net Runner` answered `policy ✓`, `outOfScope: []`, exit 0, while the unscoped `deny Net` on the same tree already named the excluded declaration directly. swift's cause was CHA resolving over the peek's own in-scope/excluded union (a fix from an earlier cardinal sin, `8852a1f`, had preserved attribution by skipping any finding whose function lived in a context file — false under dynamic dispatch, since the union can grow a context function's effect set past what the primary ever computed); rust and java never re-analyse in-scope code at all and simply never asked the caller's name; ts's dispatch mechanism was irrelevant (structural and nominal conformance failed identically), the tell that the defect was in WHICH ENTITY was asked, not in dispatch resolution. | **Fixed with four different mechanisms, one property** (SPEC §2/§6.2 ⟨0.34⟩): swift diffs the peek child's CHA-union effect set against the primary's own finalized result; rust cross-references dispatch SITES the primary already computes against the peek's excluded-type membership, with no re-analysis; java re-runs its own `runScan` over a scratch union rather than a second dispatch resolver; ts correlates a syntactic `satisfies`/`implements` annotation against interfaces the primary's own CHA already dispatches into. Where the responsible excluded declaration cannot be named with confidence, swift/java/ts disclose under a new `outOfScope[].class` value, `dispatch-widened`, rather than dropping the finding; **rust does not need it and must not be required to emit it**, since its peek attribution is never ambiguous by construction — mandating the class four-way would be dead vocabulary in one engine. PART 85's over-charge control asserts the fallback does NOT fire when attribution is unambiguous (all four engines), and a genuine firing case is proven positively on ts (an unresolvable `paths`-mapped interface reference). **Falsified against each engine's own pre-fix commit** in throwaway worktrees: rust (`27f4beb^`), java (`a034371^`, with a classpath-fix interaction isolated by using a flat compiled-output layout rather than a nested one), and swift (`7378f4f^`) all reproduce the exact silent exit-0 shape; ts required `npm install` in the pre-fix worktree to run at all, then reproduced identically. |
| rust-deep: closure/coroutine captures and `drop(x)` container walking were silently pure | 2026-08-29 | **CLOSED — candor-rust `ac939b1`** | **SILENT UNDER-REPORT, rust-deep only.** `mir_spike::local_drop_impls` (the implicit-Drop model backing rust's Drop-glue soundness) had two independent gaps: (1) a closure or coroutine that captured an effectful-`Drop` value BY MOVE, then was itself dropped without ever being called, read pure — `Closure`/`Coroutine`/`CoroutineClosure` are their own `TyKind`, not `TyKind::Adt`, so they fell into the walker's catch-all instead of being recursed into; (2) `mem_drop_local_edge` (the edge for an explicit `drop(x)`, which moves `x` into `mem::drop`'s non-local body) only ever resolved `<T as Drop>::drop` DIRECTLY on `T`, so `drop(Wrapper { g: Guard })` (no `impl Drop` on `Wrapper` itself, only a `Drop`-carrying field), `drop(Box::new(guard))`, and `drop(vec![guard])` all read pure, while the IDENTICAL value falling out of scope naturally was already caught — the same effect, two different silent answers depending on which of two hand-rolled paths asked. | Fixed by (1) recursing into `upvar_tys()` exactly as existing arms already recurse into fields/elements, and (2) routing `mem_drop_local_edge` through the SAME `local_drop_impls` walker instead of a narrower direct-only lookup — one shared implementation rather than two that could disagree, matching this session's recurring lesson (rust/java/swift's peek fixes above all reused an existing authority rather than hand-rolling a second one). |
| swift: R33 deinit-glue only fired for the unannotated `let`/`var` binder shape | 2026-08-29 | **CLOSED — candor-swift `65a0825`** | **SILENT UNDER-REPORT, swift only, narrowing R33 (2026-07-18).** R33 charges an effectful `deinit` to the enclosing scope when a `let`/`var` local bound to a fresh construction goes out of scope (swift's sibling of rust Drop-glue). The mechanism lived ONLY in the UNANNOTATED arm of `visit(VariableDeclSyntax)`'s per-binding if/else-if, so the ordinary way to spell an explicitly-typed or Optional local — `let x: Loud = Loud(p)`, `var x: Loud? = Loud(p)` — took the OTHER arm and never reached it: the deinit effect vanished with NO disclosure at all. The wildcard binder (`let _ = Loud(p)`) fell through the identifier-only guard before either arm and missed it the same way. | Extracted the glue into one function, `applyDeinitGlue`, keyed off `rootOf` on the INITIALIZER expression (never the annotation, which is what let the annotated arm skip it before), called from both the annotated-binder arm and a new wildcard-binder check alongside the pre-existing unannotated call site — one shared implementation, the same shape as the rust fix above, so the annotated/unannotated/wildcard binder shapes cannot drift apart again. |
| java records: a component's effectful `equals`/`hashCode`/`toString` override ran unattributed | 2026-08-29 | **CLOSED — candor-java `4eac631`; falsified on 388 real third-party jars** | **CARDINAL SIN.** A record's generated `equals`/`hashCode`/`toString` is a single `invokedynamic` into `java.lang.runtime.ObjectMethods` (JEP 384) — the per-component comparison/combine/format runs inside the JDK bootstrap, never in bytecode the record's own method owns. `handleInvokeDynamic`'s `ObjectMethods` branch correctly skipped the bootstrap's per-component `H_GETFIELD` handles (field descriptors, not call targets, and feeding one to `methodId` would crash the scan) but never reentered the component's own contract method either — so a record component whose declared type overrides `equals`/`hashCode`/`toString` effectfully ran that override completely silent-pure, all the way up to a bare `main()`. Same shape as `StringConcatFactory`'s implicit `toString()` reentry (`"x" + obj`), already closed; left open for records specifically until now. | **Over-charge control, the strongest form used in this session:** candor-java's own output plus **388 real third-party jars** from local Maven/Gradle caches are byte-identical pre/post except ONE (`error_prone_core-2.50.0.jar`), where all 113 newly-surfaced record-contract methods land on `inferred: []` with an honest `invisible: ["com.google.common.collect"]` disclosure — never a fabricated effect. (A second apparent diff was reproduced away as I/O contention from the 388-jar batch; the real one was independently reproduced twice.) `soundness/run_kotlin.sh` and `kappa_libs_probe.py` were NOT run (kotlinc/JDK 21 absent) — pre-existing environment gaps, not introduced by this fix. |
| candor-agents: `deny Unknown` compiled to NOTHING, and `deny Net Unknown` silently dropped `Net` too | 2026-08-29 | **CLOSED — candor-agents `d61121d`; first time this repo has been attacked** | **CARDINAL SIN in the ENFORCEMENT COMPILER, not the analyzer.** `guard.py` hand-rolls its own positional deny-parser instead of reusing the authoritative one in `policy.py`, so it never inherited `policy.py`'s `Unknown` special case: `guard.compile_guard("deny Unknown")` produced `{'deny': [], 'warnings': [], 'notes': []}` — total silence, the identical output an EMPTY policy produces — and `Unknown` names every uncurated MCP server and unresolved capability, so `deny Unknown` is the rule most likely to be load-bearing on the enforcement surface. In the compound form `deny Net Unknown`, `Unknown` was misread as a fictitious agent SCOPE, taking the genuine fleet-wide `deny Net` down with it. | Fixed by recognising `Unknown`/`Unknown[<class>]` as its own token class in `guard.py`; since guard genuinely cannot bind `Unknown` to a `permissions.deny` entry (there is no tool-level enforcement surface for "any unresolved capability"), it now emits an explicit warning rather than going silent. **Notable clean negative measured in the same session:** the rest of candor-agents (`drift`/`observe`/`scan`/`policy`) is heavily hardened, nearly every guard carrying a comment naming the real defect it closed, and every mutation tried against them was already caught — the break was isolated to the one component that reimplemented a shared parser instead of calling it. `stats.py`, `digest.py`, `savings.py`, `log_gate.py`, `agentsmd.py`, and several `scan.py` surfaces (`build_edges`, `classify_units`, cron-unit handling, hook-matcher edging, `--link`'s combined path) were read but explicitly NOT attacked — stated boundary, not a clean bill. |
| the consumer-refusal class: every `integrations/` consumer converted an engine's fail-closed refusal into a PASS | 2026-08-29 | **CLOSED — candor `b9b3a86`, changelog/fixture correction `f398d20`; first time `integrations/` has been attacked as its own surface** | **THE MOST CONSEQUENTIAL CLASS FOUND THIS SESSION** — not a scan-side blindness but a CONSUMER-side one, and it defeats every scan-side hardening rung at once, because ⟨0.21⟩ `unanalyzed`, ⟨0.24⟩ `refused`, ⟨0.28⟩'s armed sinks and ⟨0.30⟩ `incomplete` all exist to make a refusal MACHINE-VISIBLE, and three consumers threw that visibility away. (1) `integrations/claude-code/candor-review.sh`: the Stop-hook scripts collapsed "engine CRASHED" and "engine ran fine but REFUSED to certify because part of the target was unread" into one `rc=2`, and `stop-hook.sh` ALLOWS `rc=2` ("don't block on a misconfig") — reproduced end-to-end, a report carrying `unanalyzed`/`incomplete:true` at exit 2 produced *"review couldn't run (setup, rc=2)… not blocking."* A violation could sit in exactly the part the engine admits it never read. (2) `integrations/github/candor-sarif`: a REFUSED gate document (`ok:false, refused:true`, no `violations` key at all) and an INCOMPLETE one both produced a byte-identical, fully clean empty-results SARIF — a reviewer reading the persistent GitHub Security tab sees an all-clear. (3) `fingerprint/`: an `incomplete`/`unanalyzed` report still produced a maximally confident badge (`structure: 1`, "100% order") with nothing noting a whole module was never read. | Fixed by reading the completeness wire keys at each consumer: the Stop hook now returns `rc=1` (block) on a completeness failure, distinct from a genuine crash (still `rc=2`, proven by an over-charge control); `candor-sarif` uses SARIF's own `runs[].invocations` "tool did not fully succeed" channel (validated against the vendored 2.1.0 schema), leaving `results` untouched so a genuinely clean gate gets no `invocations` key; `fingerprint/` surfaces the gap in `--json`, on stderr, and in the HTML wrapper, with the deterministic SVG staying byte-identical. **THE LESSON, stated for reuse:** fail-closed is a property of a PIPELINE, not an engine — for any new disclosure key, ask what each consumer does when it is present, and specifically whether "the engine declined to answer" is distinguishable from "the engine answered clean". **Owed and explicitly unchecked (filed to candor `BACKLOG.md`, not this document):** `dispatch-widened` (the row above) landed in three engines the same day and no consumer has been checked against it; the `excluded`/`peeked`/`outOfScope`/`judgedElsewhere` family beyond `incomplete`/`unanalyzed`/`judgedNothing`/`refused`; `integrations/vscode` and `integrations/jetbrains` entirely. |
| THE INSTRUMENT SURVEY: 13 of 13 standalone conformance checkers could not fail | 2026-08-29 | **PARTIALLY FIXED — candor-spec `02f27ca`; 7 of 13 hardened, 6 confirmed defeatable and filed** | **THE THIRD "instrument that cannot fail" found this session** (after `mutation-gate.sh`'s own blindness to standalone checkers, and java's `mutation_probe.sh` going a quarter blind silently — see the round above and SOUNDNESS-LOG.md). `mutation-gate.sh` extracts checker BODIES out of `run.sh`; a standalone `conformance/*.py` file is invisible to it by construction. Every one of the 14 standalone external checkers tested — **including `check_honesty.py` (PART 1c), the family's ONE cardinal-sin detector, whose own CONTROLS comment reads "none"**, and `peek_route_equality_check.py` (PART 54), the flagship route-equality check — survived having its body replaced with `sys.exit(0)`. 13 of 13 conclusive attempts succeeded; the gate's real coverage was never "5 of 84 parts", it was 5 parts AND an entire class of 14 checkers it structurally could not reach. | Hardened 7 (severity-selected on verdict/route-equality/disclosure/refusal/completeness): `check_honesty`, `file_set_check`, `peek_completeness_check`, `refused_peek_check`, `peek_route_equality_check`, `exec_capability_check`, `only_check` — each with near-miss poison plus accept-known-good, via a new `run_ext_reject`/`run_ext_accept` runner shape; `only_check`'s poison reproduces a REAL historical bug (the AS-EFF-011/009 collision that shipped in rust and java). **Still open, confirmed defeatable, not hardened:** `incomplete_check`, `fs_position_check`, `clause_check`, `probe_check`, `must_ledger`, `part_declarations`, and the ~66 `run.sh`-embedded comparison parts (never executed against — a 3-part spot-read found the same nameable-checker shape recurs, a LEAD not a measurement). **THE GENERATOR IS NOW OWED**: the same four comparison shapes (identity→truthiness, exact-equality→membership/subset, dropped `isinstance`, absent-key blindness) have recurred a fourth time on a surface never previously touched — `conformance/mutation_poison_gen.py` + `mutation_poison_configs.py` + `retro_test.py` + `generator_canary.py`, built the same day, walk each checker's own Python source via `ast` and emit the matching near-miss family mechanically (see CHANGELOG.md's `## Unreleased` entry for the generator's own detail and its 15/15 retro-test rediscovery of every historical bypass). |
| R75 rust: a callable factory's return was decoded on ONE route out of thirteen | 2026-09-01 | **CLOSED — candor-rust `b33ed3f`** | **SILENT UNDER-REPORT, rust only.** `record_return` (`decls.rs:906`) encodes every `Fn`/`FnMut`/`FnOnce` return under one sentinel, `RET_FN_TYPED`. Two independent decode gaps in two different functions meant eleven of thirteen enumerated routes from a factory's return to a call position lost it: `expr_is_fn_typed`'s `Call` arm matched via `get_ident()`, so only a SINGLE-SEGMENT path decoded (any qualified or cross-module callee lost it, free function or not); and `resolve_elem_trait_leaves` never decoded the sentinel at all, so EVERY route through an `Option`/`Result` unwrap — if-let, while-let, match, let-else — was broken for free, method AND associated-fn factories alike. Only a single-segment plain-`let` call and a field access survived. Ordinary code: `poll()`/`pop()`/`try_recv() -> Option<Box<dyn Fn>>`. **The premise handed to the fix was wrong and testing rather than assuming caught it** — the review characterised the split as method-call vs free-function syntax; it is not. | Fixed at ONE point rather than per call site: `ret_dispatch_leaves()` decodes the sentinel into a synthetic `"Fn"` leaf, consumed by both the `MethodCall` and `Call` arms of `resolve_elem_trait_leaves`, which every binder already calls; plus segment-based matching in `expr_is_fn_typed` and the missing sentinel check on its `MethodCall` arm. **MEASURED, 256-crate A/B against a pre-image verified byte-identical to a rebuild of stashed HEAD on four sample crates: 255/256 unchanged.** The one crate that moved is `jiff-0.2.35`, +2 `Unknown`-only keys, zero lost, zero new effect classes — and the source confirms a real unforced invocation (`util::parse::offseter` returning `impl Fn`, plain-`let` bound, genuinely called to compute the return value): a cross-module multi-segment free call, the gap in the wild. Nine never-called over-charge controls re-run, all still correctly pure. |
| R76 rust: a dispatch-typed value round-tripped through an unannotated tuple literal | 2026-09-01 | **CLOSED — candor-rust `b33ed3f`** | **SILENT UNDER-REPORT, rust only.** `let pair = (cb, 1); let (f, _) = pair; f()` lost the value's dispatch status and the call dropped silently; so did the one-step form `let (f, _) = (cb, 1); f()`. **NOT `Fn`-specific** — a `Box<dyn Doer>` parameter loses it identically, confirmed against a plain trait object, so the class is wider than the closure vein that surfaced it. The tuple-literal element scanner recognised only `Cast` expressions, never a bare `Path` already in `fn_typed_vars`/`trait_vars`, and the two-step case's source `let` recorded no `tuple_of`/`tuple_trait_of` at all. | `tuple_elem_leaves()`/`tuple_literal_leaves()`, used both to widen the one-step destructure's leaf detection and to populate the two-step case. Zero over-charge controls broke; zero corpus regression. |
| R77 rust: enum-variant payloads, and struct-variant fields for ANY type | 2026-09-01 | **CLOSED — candor-rust `67b8e45` (tuple-variant vein) + `e287b8a` (struct-variant fields), both across all four binder forms** | **SILENT UNDER-REPORT, rust only.** `match m { Msg::Cb(f) => f() }` and its if-let/while-let/let-else twins dropped a callable enum-variant payload silently: `visit_arm` never consulted the dispatch-leaves route the other four binder families used, because `type_path` returns `None` for a `dyn`/`impl`/bounded-generic payload — so the payload was recorded in NO index at all. **Struct-variant fields** (`Msg::CbField { f } => f()`) were a MISSING CAPABILITY rather than a routing bug — no binder mechanism existed for them at all, for ANY payload type, callable or not — and were pinned by a canary test that failed the day they closed, exactly as its own comment promised. **Also found, separate class, still open:** `resolve_elem_trait_leaves` has no `Index` arm and does not recognise `Vec::pop()`/`HashMap::remove()` as element-yielding. | New Pass-A index `EnumVariantTraitIndex` threaded through `FileDecls`/`MergedDecls`/`merge_decls`/`decl_index_digest` — the digest's must-hash-every-field test was extended and **proven to fail** when the new field was temporarily un-hashed, rather than assumed to cover it. All four binder sites now try dispatch-typing first and fall back to the pre-existing concrete-type route, which incidentally gives if-let/while-let/let-else tuple-variant handling they never had for ANY type. 9 new tests, **5 revert-tested red** (the other 4 are never-called controls and collision guards, revert-invariant by design and correctly still green). **THE CORPUS A/B CAUGHT TWO REGRESSIONS THE FIX INTRODUCED, neither visible to inspection, one of them a cardinal sin:** (1) *cross-index leaf collision* (reqwest 0.13.4) — an unrelated enum's dispatch-typed variant hijacked a same-named concrete variant elsewhere in the crate, and `proxy::Matcher::intercept` went from correctly `Unknown` to **silently pure**; fixed by `drop_cross_ambiguous_enum_leaves`, which drops an ambiguous leaf from BOTH indexes rather than guessing. **Residual — and the "honest unresolved" half of this sentence is FALSE, corrected the same day: see R90.** The guard does drop the ambiguous leaf from both indexes rather than guessing, and no fabrication occurs — but measured on a two-enum collision fixture, BOTH callers vanish from `functions[]` entirely and `deny Unknown` exits 0. It drops SILENTLY; it does not disclose. It also does not recover reqwest's pre-fix precision on this trigger — 5 functions, 1 of 256 crates. **I took that phrase from the commit's own doc comment and wrote it here without measuring it — the same failure as R81, twice in one day.** (2) *self-collision* (moka 0.12.16) — a bounded-generic payload made `type_path` spuriously succeed on the generic ident's own literal name, colliding with the correct dispatch entry from the SAME variant declaration; fixed by trying dispatch-typing first, mirroring the existing struct-field precedence. **Final 256-crate A/B against a pre-image proven by `git stash` rather than by inspection: ADDED 23, REMOVED 0, CHANGED 44** — zero removals, which is the E1 claim under test, and gained rows ground-truthed against real source (darling_core's higher-order `Fields::map(Field::as_declaration)`, rustls-platform-verifier's `GlobalStorage::External(&dyn Runtime)`) as genuine, not fabricated. Gates: `cargo test --workspace` 642 passed 0 failed, `soundness/run.sh 60` 60/60, 7 fuzz-form variants at 40, `run_cross`/`run_drop` 40/40, `gate-equivalence`, `self-gate`, integration 156, wrapper-smoke 57 — all green; the five strace gates UNRUN on macOS and reported as unrun, not green. **THE STRUCT-VARIANT HALF (`e287b8a`) REUSED THE EXISTING INDEXES RATHER THAN ADDING A THIRD.** A struct-variant field writes under the composite key `"VariantLeaf::field"` into the same two maps the tuple route uses; a Rust identifier can never contain `::`, so that key space is disjoint from every bare tuple-variant leaf **by construction, not by convention** — which means the `drop_cross_ambiguous_enum_leaves` guard, the merge, and the cache digest all cover it for free instead of needing parallel versions that could drift. **The canary was CONVERTED into 10 real tests, not deleted** — all four binder forms' callable case, never-called controls for each, a plain concrete field, `..` rest, multi-field independence, `ref`/`@` bindings, and an or-pattern gap that was MEASURED (confirmed pre-existing in the tuple route too) rather than assumed. 8 of 10 revert-tested red; the 2 that stayed green are revert-invariant by design and say so. **In place of extending the must-hash-every-field test — no new `MergedDecls` field was added, so that test had nothing new to catch — a digest test was added proving sensitivity to the composite key's CONTENT, not merely its presence**; that is the honest substitute rather than a claim of coverage that would not have held. **256-crate A/B against a `git stash` pre-image: ADDED 38, REMOVED 0, CHANGED 4** — zero removals, audited in full per E1. Gained rows ground-truthed against real source, and one is a cardinal sin found in the wild: **hickory-resolver's `Recursor::resolve` was previously SILENT-PURE (zero calls recorded) and now correctly inherits `Clock`/`Log`/`Rand`** through a newly-resolved struct-variant field call; diesel's `AstPass::push_bind_param` gains a correct `Unknown` on a previously invisible `&mut DB::BindCollector` field. One CHANGED row (`rayon`'s `Skip::with_producer`) narrows `direct`/`unknownWhy` with `inferred` unchanged — gating-neutral, measured, and **flagged as not fully traced to a mechanism rather than guessed at**. Gates: `cargo test --workspace` 652 passed 0 failed, `soundness/run.sh 60` 60/60, 7 fuzz-form variants 40/40, `run_cross`/`run_drop` 40/40, `self-gate`, `gate-equivalence`, integration 156/156, wrapper-smoke 57/57, receipt 26/26, stable-toolchain — all green; the six strace gates UNRUN on macOS and reported as unrun. `assert-audit.sh` clean. **Runtime witness added (candor-rust `f2e0b8e`): `pf_enum_tuple` + `pf_enum_struct`**, with a paired control differing in exactly the binder or payload form under test; predicted green at `10b19c7`, so this fix is now falsifiable by the syscall oracle rather than by fixtures alone. |
| R78 swift: a vendored nested package collided two modules and fabricated onto a pure target | 2026-09-01 | **CLOSED — candor-swift `67595c2`** | **FABRICATION, swift only, and a narrowing of R74 rather than a fresh vein.** `swiftModuleOf` (`Driver.swift:171`) returned the segment after the FIRST `Sources`/`Tests` path component, so a target legitimately vendoring a separate SwiftPM package — `Sources/outer2/Vendor/NestedPkg/Sources/NestedTarget/main.swift` — resolved to module `"outer2"`, the same key as `Sources/outer2/main.swift`. R74's `<main>` disambiguation keys off that string, so the pure target's `<main>` inherited the nested target's `Env`. Both halves proved by EXECUTION: the outer entry point prints only its own line; the nested one printed the real `$HOME`. R74 improved this (3-of-3 merged → 2-of-3) without closing it, and the commit's stated known limit covers only "scan `Sources/` directly" — a different case. | `analyze()` derives `nestedManifestDirs` from the walk's own `excluded[].class == "manifest"` signal and shadows `swiftModuleOf` with a boundary-aware version, so a file under the deepest nested-manifest ancestor gets a module key prefixed by that boundary and cannot collide with a name computed outside it. **Over-charge control written and RED-checked FIRST** (a target split across two files with no nested manifest: byte-identical pre/post). Revert test: stashing the fix makes the new test fail with the exact reported shape. All eight of the review's clean negatives re-run byte-identical. Corpus A/B on swift-nio (7563 analysed, including three real nested vendored packages, so the new path genuinely executed), swift-collections, swift-algorithms, swift-argument-parser: **zero diffs, all four**. 995/995 tests; `ci/self-gate.sh` OK. |
| R79 swift: a cross-module global receiver drops the calling function, under a clean bill | 2026-09-01 | **CLOSED — candor-swift `17cbc95` (resolution) + `e441f1c` (clean bill). The disclosure-only shortcut was ATTEMPTED, MEASURED UNSAFE, and reverted first; the fix that landed does not touch the shared fallback at all** | **CARDINAL SIN, swift only, filed as a follow-on by R73's own commit and confirmed still true at HEAD by independent reproduction.** A `public let sharedWorker = SharedWorker()` exported from one module and called as `sharedWorker.doWork()` from another: the callee is present and correctly `Fs`, but the CALLER (`<main>`) is **absent from `functions[]` entirely**. `CallCollector.swift` ~3197–3205 — the terminal member-access fallback, when the receiver is a lowercase bare identifier with `isVar == false` (`globalTypes` is module-scoped, so a cross-module global misses) — sets `owner = nil` and records a bare-leaf `Call` with no receiver context; nothing matches it and it contributes no edge. That feeds `main.swift:1709`'s "mint no unit unless it carries or reaches an effect" filter, which is the SAME filter that correctly omits genuinely-pure functions everywhere else — so this is not patchable by changing that filter. **AND THE ENGINE ISSUES A CLEAN BILL OVER IT:** stderr prints `candor: nothing hidden — every effect sits where its name says it should.` — confirmed to fire in EVERY variant where this resolution fails (cross-package, single-package cross-target, same-file-tree cross-module). An absolute claim the engine cannot support whenever it holds unresolved dispatches, and structurally the same false disclosure this project refuses to let an engine emit about user code. **THE DISCLOSURE-ONLY SHORTCUT IS NOW MEASURED UNSAFE, not merely suspected.** Two agents had judged the terminal fallback "too shared to touch"; a third implemented it anyway — mark the caller `Unknown` when `owner == nil` and the receiver is not a bound local — and measured the result. Even narrowed with an `isBoundLocal` guard it **broke 8 pre-existing tests across unrelated mechanisms**: keypath projected-value charging, fn-ref-as-argument shadowing, three singleton-field fail-closed controls, an accessor-join control, a global-initializer test, and two protocol-shadow-restoration cases. Unnarrowed it added +461/+126/+25/+73 `Unknown` rows across swift-collections/argument-parser/algorithms/nio — mostly untyped-iterator noise, nothing of R79's shape. **Zero `REMOVED` rows in every A/B, so no fabrication in the dangerous direction** — the objection is blast radius, not unsoundness. Reverted, with the pinning test deleted alongside it rather than left to assert a behaviour that no longer exists. **The finding underneath the finding: ~8 sibling mechanisms rely on this fallback's SILENCE as their own deliberate no-op.** That is why a blanket rule cannot work here, and it reframes the real fix — do not change what happens when `owner == nil`; **stop reaching that state** by resolving the cross-module global. A sound lookup via each file's `import` statements (unambiguous single-module match, access-level checked) is still believed reachable and still needs new plumbing. | **The false clean bill is closed by extending the EXISTING guard, not adding a second one.** `emitSurface` (`CandorCore/Surface.swift`) and `tour` (`FixCLI.swift`) each independently computed the same "≥1/3 of the graph is `Unknown`" formula as the sole gate on the unqualified sentence — two copies of one rule, free to drift. Both now call one `CandorCore.unknownDensity`, and **a literal zero `Unknown` is the only condition for the unqualified claim**, with a new lighter qualified tier below the pre-existing 1/3 line. A/B over fresh clones of all four named corpora: **`functions[]` byte-identical** — the change touches only prose. 996/996 tests, `smoke.sh` 148/148, `fuzz.py` 25/25, `fabrication_probe.py` 28/28, `recall.sh` 5/5, `ci/self-gate.sh` OK; `soundness/realworld/run.sh` and `disclosure_recall.sh` SELFSKIP (Linux+strace) and are reported UNRUN, not green. **This does NOT close R79's own instance** — the vanished caller contributes 0 to the density count, so the clean bill over it is still unqualified. It closes the general class of a few real `Unknown`s diluted below 1/3 wherever `Unknown` is otherwise correctly disclosed. Measured caveat, stated not fixed: `smoke.sh` ran against local `candor-spec` HEAD, not CI's pinned commit. **HOW IT WAS ACTUALLY CLOSED, and it is the opposite of the obvious patch.** The reverted attempt changed what happens when `owner == nil`; this one **stops reaching that state**, so the terminal fallback and the ~8 mechanisms relying on its silence are untouched. `DeclCollector` records which globals are `public`/`open`; `Driver` aggregates them into `publicGlobalTypesByModule`; a name absent from the caller's own module is looked up, per calling file and cached, across every OTHER imported project module's **public-only** index. Two or more matches is ambiguous and resolves nothing rather than guessing. **Coordinator-verified on an independent two-module package, ground truth EXECUTED:** `<main>` goes ABSENT → `['Fs']`, and in the SAME fixture a second, genuinely pure cross-module global call (`quietWorker.hum()`) adds nothing — the over-charge control passing in the same breath as the fix. **Access control verified separately: making the same global INTERNAL leaves `<main>` absent**, because a non-public global genuinely is not visible cross-module and resolving it would be fabrication dressed as a fix. 1000/1000 tests (996 + 4 new), `smoke.sh` 148/148, `fuzz.py` 25/25, `fabrication_probe.py` 28/28, `recall.sh` 5/5, `ci/self-gate.sh` OK; the two strace gates SELFSKIP and are reported UNRUN, not green. Revert-tested: only the fix-pinning test goes red, and the three control tests pass with or without the fix — they are not vacuous. 5-run determinism byte-identical. **THE CAVEAT THE AGENT VOLUNTEERED, AND IT IS THE MOST USEFUL LINE IN THE REPORT:** the corpus A/B over swift-nio, swift-collections, swift-algorithms and swift-argument-parser is `functions[]` byte-identical — but `grep -rn '^public let \|^public var '` across all four trees returns **ZERO hits**. None of those corpora contains the pattern this fix targets, so that A/B is a SAFETY control and **not a real-world recall demonstration**; the only recall evidence is the hand-built executed fixture. A zero-diff A/B over code that cannot trigger the change is exactly the flattering number this family has been caught by before. **What that commit called "open by design" turned out to contain a CARDINAL SIN — see R85.** Measured the next day: factory globals (`public let x = makeX()`) and destructured globals do not answer `Unknown`, they drop the CALLER entirely, and `deny Fs <caller>` passes over provable I/O. A named limitation that answers `Unknown` is a limitation; one that answers nothing is a sin with a footnote. The third shape, ambiguous same-name globals, is moot: that program does not compile — which also makes R79's own ambiguity CONTROL vacuous, since it asserts behaviour for an input that cannot exist. |
| R80 java: field-held lambdas were keyed so that static and inherited fields never matched | 2026-09-01 | **CLOSED — candor-java `e53a69b`; pinned by conformance PART 87 (three shapes × two arms, one over-charge control per shape)** | **TWO CARDINAL SINS, java only, both live in published 0.34.0 and both left open by `acbca5c`, the ⟨0.35⟩ fix that claimed the vein.** (1) **static fields** — `GETSTATIC` reads were never tagged with `fieldOrigin`; only `GETFIELD` triggered `wrapField`. The commit's own comment called this "inert, not wrong"; measurement said otherwise, and `deny Unknown` exited 0. (2) **inherited fields** — `javap -v` confirms javac emits the `PUTFIELD` in `Base.install` with constant-pool owner `Base` and the `GETFIELD` in `Sub.fire` with owner `Sub`, so a write keyed `Base#task` never matched a read keyed `Sub#task` and dispatch fell through to the unfixed CHA path. Reproduced independently: `deny Fs Sub` exit 0 over a call that provably writes a file. Neither shape is exotic — a static callback slot, and a protected field with a template method. **A third finding, the over-charge direction:** a static field holding a provably PURE lambda beside one unrelated effectful implementor produced a false-positive exit 1 on both published 0.34.0 and HEAD, falsifying `acbca5c`'s blanket "over-charge control holds" claim for every shape it had not tested. | `Cha.fieldKey(owner,name)` normalises a field access to its DECLARING class via `resolutionOrder` — reusing CHA's existing method-resolution walk rather than hand-rolling a second one — so write-side and read-side keys always agree; `GETSTATIC` reads are tagged the same way `GETFIELD` already was. **Nine field-access shapes enumerated and tested** (instance same-class, static, base-writes/subclass-reads, the reverse direction, `super.`, subtype-reference write, interface-static-via-implementor, setter, constructor/static-init); all close under the one mechanism, each has a test, and **seven go red on revert**, so they are not vacuous. **The row that should have caught this could not:** PART 87 built only the same-class instance-field fixture — its boundary drawn around its own trigger — and was extended in the same round (candor-spec `d67d29f`, `e450253`). **A/B over 325 real third-party jars: ADDED 2, REMOVED 66, CHANGED 318.** The fix's own account of those rows — pre-existing CHA-fanout fabrications through unrelated classes sharing a generic method name (`apply`, `put`) — was traced on THREE examples, and three of 386 is a sample, not an audit, so a full audit was commissioned. It reproduced the counts exactly and ground-truthed all 66 REMOVED plus all 42 lost-effect CHANGED rows via `javap`, zero sampling: **73 of those 108 rows are that story and are correct; 35 are a SECOND mechanism and a cardinal sin — see R83.** So this row closes the keying defect, and its own A/B carried a defect of the opposite kind that was found only because the REMOVAL direction was audited rather than accepted. **The general rule this earns: when a fabrication-fix's headline evidence is a large A/B, the removals are the claim under test, and tracing a few of the biggest is not testing it.** |
| R81 java: field provenance does not survive a getter, or an off-project method reference | 2026-09-01 | **CORRECTED 2026-09-01 — the getter half was FILED AS A CARDINAL SIN AND IS NOT ONE. Measured, it is a sound disclosed imprecision. The off-project-method-ref half reproduces exactly as filed (an OVER-CHARGE). Neither is a regression; both are identical on published 0.34.0 and HEAD** | **THE ORIGINAL ROW WAS WRONG AND THIS IS THE CORRECTION.** It stated that `Widget.fire() { Runnable t = getTask(); t.run(); }` leaves `Widget.fire` ABSENT from `functions[]`, certified pure — the cardinal sin. **It does not.** Measured by the coordinator on the row's own fixture, ground truth EXECUTED (the file really is written), on published 0.34.0 AND on HEAD, byte-identical: `Widget.fire` → `['Unknown'] unresolved:true`, `Main.main` → `['Unknown'] unresolved:true`. `Widget.getTask` is absent, which is CORRECT — it is a pure getter and every engine omits those; reading that absence as the caller's absence is most likely how the original claim arose. Gates: `deny Fs` 1, `deny Unknown` 1, `deny Fs Unknown Widget.fire` 1, and `deny Fs Widget.fire` 0 on BOTH sides — the last is `Unknown` not being `Fs`, exactly as specified, with an actionable remedy the engine names. **The STRUCTURAL gap is real: `Interp.ProvInterpreter.returnOperation` is an unconditional no-op, so field provenance genuinely cannot survive a method-call return.** Its measured CONSEQUENCE in the minimal case is a sound, honest, imprecise `Unknown` — not silence and not fabrication. Whether a richer trigger (a SAM interface with a real project implementor for CHA to latch onto) converts that imprecision into genuine silence is **UNRESOLVED — not tested**, and is stated as unresolved rather than assumed either way. | The getter half needs no fix at this severity; it is the interprocedural value-provenance design already on the roadmap, and it is a PRECISION item, not a soundness one. **The second sub-claim DOES reproduce as filed:** a field truly holding an off-project method reference (`String::length`) falls through to unscoped CHA fanout and is charged an unrelated named `Function` implementor's `Fs` — a fabrication, confirmed identically on published 0.34.0 and HEAD, unaffected by R80/R83/R84, left unfixed as a lower-priority over-charge. **THE LESSON, and it is the family's own rule turned on its author:** this row was written from an agent report that said ABSENT, and I filed it as a cardinal sin without reproducing it. A row that records what it MEASURED can go stale honestly; one that states a CONCLUSION cannot. It was caught because a later agent was told to attack its brief and did. |
| R82 ts: the ⟨0.35⟩ CHA-completeness fix introduced two cardinal sins of its own | 2026-09-01 | **CLOSED — candor-ts `c53baa2`** | **TWO CARDINAL SINS, ts only, both EXECUTED to ground truth, both clean at `7ecda11~1` — regressions introduced by the fix, not pre-existing.** (1) `contextualInterfaceDeclsFor` climbed through any `function wrap<T>(x: T): T` whose parameter and return spelled the same type-parameter name, and **the code called this "PROVEN — not guessed"**. TypeScript certifies type COMPATIBILITY; it cannot certify that a `T -> T` signature IS the identity. A wrapper that ignores its argument and returns a different effectful object made the bait the sole registered candidate, resolved it trivially against an empty body, flipped `allResolved` true, and the dispatching function VANISHED from `functions[]` — pure over code that writes to disk. (2) `localClassMember` read only `cls.members`, never `cls.properties`, so an interface implemented by an object literal matched nothing; the commit's own audit cleared this consumer as "degrades gracefully on a non-class entry" — it degrades by silently finding nothing. (3) A THIRD instance, unassigned, found only by grepping every call site rather than the two the audit-boundary comment named: the workspace-chain union's `c.name?.text` `.filter(Boolean)` silently dropped structural implementors and could publish "pure across all impls". **`7ecda11` shipped ZERO tests** — only `CHANGELOG.md` and `scan.mjs` changed. | (1) NOT deleted, which was the instruction's prior and the prior was wrong: `Object.assign`'s climb is safe because the LANGUAGE guarantees it, so deleting every climb would discard a provable case to punish an unprovable one. `isProvenIdentityReturn` now requires the body to be exactly `return x;` with the checker confirming the same parameter symbol — a proof read from source, the same evidentiary bar `Object.assign` meets. The fabricating fixture reads honest `Unknown[dispatch:...]`; a genuine `return x;` wrapper still resolves precisely, so the tightening does not answer a resolvable case by hedging. (2) fixed by MIRRORING the general dispatch site (`cur.members ?? cur.properties ?? []`), already correct in that same commit, rather than inventing a second rule. (3) fixed conservatively behind its existing opt-in env var. **All five consumers of `interfaceImpls` now enumerated in the commit.** 8 checks added covering both regressions, both control directions and finding 3; they go red on revert. **Corpus A/B re-run WITH `node_modules` INSTALLED** — the stronger test, since absent deps force most external dispatch to `Unknown` and hide concrete-effect fabrication — on freshly cloned hono, zod and axios: fixed is BYTE-IDENTICAL to unfixed HEAD on all three, so `7ecda11`'s real value (+19 hono, +8 zod, all `Unknown`-only or precise re-attributions) is preserved with zero new fabrication. |
| the ⟨0.35⟩ clause and its own checker could not fail on a hedging engine | 2026-09-01 | **CLOSED — candor-spec `44b2622`** | **THE R68(5) HAZARD, committed inside the row written to close R68(5).** An engine that resolves nothing and blanket-tags every lambda-bearing function `Unknown + unresolved:true` satisfied the clause's disjunction on EVERY row — including PART 87's own over-charge control, which asked only "did it fabricate `Fs`" and never "did it stay genuinely pure". A check that is green whether or not any engine is correct. Three further gaps in `cha_completeness_check.py`: `unknownWhy` was never validated though the clause names it as a THIRD conjunct, so absent, empty and off-vocabulary all passed as DISCLOSED — **confirmed on a real report, not a synthetic one** (a candor-swift fixture carries `Unknown + unresolved:true` with `unknownWhy` entirely absent and the old checker said OK); and `endswith('.'+caller)` took the first suffix match regardless of package, so an unrelated same-named function could stand in for a target that was ABSENT — the sin wearing a neighbour's name. | Over-charge control now also fails a hedged pure lambda; route (b) now requires a non-empty `unknownWhy` with a `callback:`/`dispatch:` kind; exact match wins over suffix and multiple distinct suffix matches fail loud as ambiguous. **Residual, documented rather than solved:** a single confusable look-alike with the real target absent is undetectable without an a-priori qualified name. Selftest 5 → 10 cases and **not vacuous** — stubbing `verdict()` to an unconditional pass fails 7 of them. Also fixed: the dirty-engine-tree disclosure read the SIBLING checkout even when `CANDOR_JAVA`/`CANDOR_TS`/`CANDOR_SWIFT` pointed elsewhere — the overrides its own header documents — so a run whose jar came from an override with uncommitted changes reported `DIRTY_ENGINES` empty. A disclosure that reads a different tree than the run does is a false all-clear. |
| R83 java: a field write that is a CONTROL-FLOW MERGE was judged by LINEAR bytecode adjacency | 2026-09-01 | **CLOSED — candor-java `a4fc5e2`; verified by the coordinator (all four policy forms restored). Never reached a published artifact** | **CARDINAL SIN, java only, introduced by `acbca5c` (the ⟨0.35⟩ field-lambda fix) and still live at `e53a69b`. Unreleased — published 0.34.0 does NOT carry it, so no consumer is affected.** `Cha.collectFieldLambdaBindings` decides whether a field write is a "clean, recognised project lambda" by inspecting **the single bytecode instruction immediately preceding the `PUTFIELD`/`PUTSTATIC`** (skipping only labels/linenumbers/frames). The idiom `this.x = supplied != null ? supplied : defaultLambda;` — the standard default-or-caller-supplied callback pattern — compiles to ONE putfield fed by TWO control-flow predecessors merging; javac places the default branch's lambda adjacent to the putfield and leaves the caller-supplied branch reachable only via a `goto`. The linear check sees only the default and marks the field CLEANLY BOUND, so an arbitrary externally-supplied callback is treated as if it could only ever be the safe default. **This is the exact inverse of the method's own documented contract, which is correct as written** — *"if ANY assignment is NOT a recognisable project lambda/method-ref, the field is EXCLUDED from binding entirely"*. The contract is right; the implementation cannot see the assignments it claims to check. Minimal repro, ground truth EXECUTED (the file really is written): `Widget.fire` goes `['Unknown'] unresolved=true` → **ABSENT from `functions[]`**. Exit codes measured on that fixture: `deny Fs` 1→1 and `deny Fs Unknown` 1→1 (**unaffected — the blanket-deny claim in the 0.34.0 advisory survives**), but `deny Unknown` **1→0** and scoped `deny Fs Unknown Widget.fire` **1→0**. The gate that breaks is the disclosure-shaped one — the policy whose entire purpose is "tell me about anything you could not resolve" — plus every function-scoped policy. **35 rows across FOUR unrelated real libraries**, byte-for-byte the same javac shape: AWS S3 SDK `DefaultSelectObjectContentVisitorBuilder$VisitorFromBuilder` (24 rows, six PUBLIC builder setters, where a user's consumer writing to a file is the expected usage), Apache HttpCore5 `RequestHandlerRegistry` ctor (5), Spring Data Redis `FutureResult` ctor (1), Netflix Eureka `InstanceInfo$Builder.intern` (1 removed + 5 CHANGED, where a `Log` effect is silently dropped). | **Found only because the REMOVAL direction of a fabrication-fix's A/B was audited in full rather than sampled** — 66 REMOVED rows, all ground-truthed via `javap` rather than via candor's own report, which would have let the engine adjudicate a question about the engine. The auditor also **self-caught and retracted** a suspected third mechanism in spring-web after re-checking raw JSON against its own paraphrase, and flagged the ADDED 2 as spot-checked rather than proven — both recorded, because an audit that reports its own near-miss is the only kind whose clean findings mean anything. **Fix direction (in flight, and deliberately not a second heuristic):** ASM's `Analyzer` + `SourceInterpreter` yields the SET of instructions that could have produced the value at the putfield; apply the documented contract to that set. **The control that matters more than the fix** is that the 73 legitimately-closed CHA-fanout rows must NOT regress — particularly amqp-client's `WorkPool.enqueueingCallback`, which has TWO separate clean PUTFIELDs and is the case a naive "more than one producer ⇒ exclude" rule would break. Measured history here is 4 defects in 5 fabrication-fixes, two of them cardinal sins, and both directions are live at once. **Latent risk, flagged not confirmed:** two clusters (hazelcast, micrometer ×2) bind a field to an ABSTRACT INTERFACE method-reference identity rather than resolving through CHA to concrete implementors; it lands on the right answer there (both real implementors ground-truthed pure) but is not validated as sound in general. |
| R84 java: a method reference to an ABSTRACT interface method was accepted as a "clean" lambda body | 2026-09-01 | **CLOSED — candor-java `7245f53`; verified by the coordinator on an independent fixture. Never reached a published artifact** | **CARDINAL SIN, java only, introduced by `acbca5c` and untouched by both `e53a69b` and R83's fix `a4fc5e2`.** `indyLambdaTarget` decides a `LambdaMetafactory` target is a recognisable project lambda without checking that the target is CONCRETE. An unbound instance-method reference to an abstract interface method — `this.op = Shape::render;` where `Shape.render` has no body — therefore binds the field to a method that cannot perform anything, and the real implementor's effect is never reached. **This is the "latent risk" the R83 audit flagged and declined to call a defect** (two clusters, hazelcast and micrometer, bound a field to an abstract interface method-reference identity rather than resolving through CHA to concrete implementors; it landed on the right answer there because both real implementors were ground-truthed pure). The flag was correct and the caution was correct: asked properly, it is a sin. Coordinator's independent repro, ground truth EXECUTED (the file really is written): `Widget.fire` **ABSENT**, and `Main.main` reports **`inferred: []` — certified fully pure** — over a call chain that provably writes a file, while `LoudShape.render` sits in the same report carrying `Fs`. Gate impact measured across four policy forms, published 0.34.0 vs HEAD: `deny Fs` 1→1 (**blanket deny unaffected, as with R83 — the effect still surfaces on the implementor**); `deny Fs Main.main` 0→0 (a PRE-EXISTING scoped gap, not introduced here — do not attribute it to this defect); `deny Unknown` **1→0** and `deny Fs Unknown Main.main` **1→0**. As with R83 the loss is the DISCLOSURE: on published 0.34.0 the caller carried `Unknown`, and at HEAD it is silent. | `Candor.handleTargetConcrete(Handle)` sits beside `indyLambdaTarget` and walks `Cha.resolutionOrder` to check `ACC_ABSTRACT`. **All six `MethodHandle` kinds enumerated and each verified with an executed fixture, rather than reasoned about:** `H_INVOKESTATIC`/`H_INVOKESPECIAL`/`H_NEWINVOKESPECIAL` cannot be abstract and are accepted unconditionally; `H_INVOKEVIRTUAL` (abstract-class method) and `H_INVOKEINTERFACE` (abstract, but NOT a default method) are walked and checked. **`interface` is deliberately not used as a proxy for `abstract`** — a default method is concrete and must still bind, and that case has its own control. Acceptance table reproduced exactly: `deny Fs` 1→1, `deny Fs Main.main` 0→0 (the pre-existing scoped gap, untouched), `deny Unknown` and `deny Fs Unknown Main.main` both **restored to 1**; coordinator re-verified on an independent fixture, where `Main.main` went from `inferred: []` to `['Unknown']`. **325-jar A/B isolated from R80/R83 by rebuilding a pre-R84 HEAD jar** (and that pre-image reproduced R83's own 344-row divergence exactly, as a sanity check on the isolation): ADDED 3, **REMOVED 0**, CHANGED 93 — 91 pure disclosure growth, and the 2 that lost a label were ground-truthed via `javap` as the pre-existing all-or-nothing private-functional-param forwarding sink, with the effect still surfacing elsewhere in the same report. Named Mechanism-A clusters byte-identical. **The two clusters that made this a latent risk are now settled by instrumentation rather than by absence of a diff:** hazelcast now DISCLOSES `Unknown` where it was silently pure; micrometer's two land on the same value pre/post because CHA resolves to the same provably-pure implementor either way — confirmed by instrumenting the check, since an unchanged row is not evidence the new code ran. **Found by the R83 fix agent while enumerating siblings of its own defect** — i.e. by asking what ELSE in the neighbourhood reasons from an unchecked assumption, which is the enumeration discipline the round has been enforcing, applied by an agent to code it was not sent to look at. Also enumerated in that pass and filed rather than fixed: `Candor.bindDeferredFields` carries a plausible same-shape linear-adjacency risk (**not fixture-verified — a LEAD, not a measurement**); `Candor.forcedFieldKey` was checked and is safe (it explicitly stops at a branch); `Candor.feedsDeferredFactory`/`lambdaEscapesUninvoked` are forward walks and were NOT examined; ~15 sites in `Literals.java` are advisory literal/path disclosure rather than effect-gating and are out of the cardinal-sin class. **That boundary is stated rather than implied, which is what makes the clean parts of it worth anything.** |
| R85 swift: a FACTORY-initialised or DESTRUCTURED public global drops the caller silently | 2026-09-01 | **CLOSED — candor-swift `457f558`. Pre-existing; NOT introduced by R79's fix** | **CARDINAL SIN, swift only, and it sits exactly where R79's own commit said it was "open by design".** `public let sharedWorker = makeWorker()` (a factory call rather than a direct constructor) and `public let (a, b) = (Worker(), 42)` (destructured) both leave the CALLER absent from `functions[]` entirely. Coordinator's repro, ground truth EXECUTED (`swift build && ./.build/debug/App` writes the file): with `public let sharedWorker = Worker()` the report carries `<main>` and `run` with `Fs`; change that ONE line to `= makeWorker()`, holding everything else constant, and the report contains only `Worker.doWork` — `run` and `<main>` are gone. Gate matrix measured across five forms: `deny Fs` 1 and `deny Fs Unknown` 1 (**caught only incidentally, because the CALLEE is still independently reported — not because the caller was judged**), but **`deny Unknown` 0** (so there is no disclosure either — a true silent gap, not a coarser answer), **`deny Fs run` 0** and **`deny Fs <main>` 0** — an ordinary "the app layer must not touch Fs directly" policy passes over provable I/O. Everything that asks WHO reaches an effect is blind: scoped policies, `path` ("no function matching"), `callers`, `gains`, `tour`. Root cause read rather than guessed: `Driver.swift` populates `publicGlobalTypesByModule` only from `globalPublic`/`globalTypes` during the per-file merge; `globalFactories` is resolved in a LATER, separate pass that writes only into `globalTypesByModule` and never into the cross-module table. The destructured case fails differently — `DeclCollector` records `globalTypes`/`globalPublic` only for the plain-identifier and constructor-call binder shapes, and the tuple-destructure binder never populates `globalPublic` at all. **Two binder shapes and two population sites, one question — the drift class from brief section G, one layer down from the vein R79 closed.** | **THE LESSON, and it is about the previous row rather than this engine: "OPEN BY DESIGN, STATED NOT HIDDEN" IS NOT A SAFE CATEGORY WHEN THE OPEN SHAPE IS SILENT.** R79's commit named these three shapes as known limits and I recorded them as such, which reads as *acceptable*. Measured, two of the three are not limitations at all — they are the same cardinal sin one binder shape over, and the engine prints `nothing hidden — every effect sits where its name says it should` over them. **The distinction that matters is never open-vs-closed; it is DISCLOSED-vs-SILENT.** A named limitation that answers `Unknown` is a limitation. A named limitation that answers *nothing* is a sin with a footnote. Every future "open by design" line must say which of the two it is, and prove it. **Second finding, about the instrument rather than the engine:** R79's own ambiguity control, `testAmbiguousCrossModuleGlobalNameResolvesNothing`, asserts that `<main>` stays absent when two imported modules both declare `public let sharedWorker` — and the coordinator compiled that exact fixture: it is a **compile error** (`ambiguous use of 'sharedWorker'`). The control validates behaviour for a program shape that cannot exist, so it is evidence of nothing; one of the four controls R79 was accepted on could never have failed. This is the "instrument that cannot fail" class, found in a control written the same day the family hardened four other instruments for exactly that reason. **FIXED by making both shapes feed the SAME authority the direct-constructor shape always used**, not by adding a third write site — `DeclCollector.inferGlobalType` is now one function shared by the identifier binder and each positionally-paired tuple-destructure element, and it marks `globalPublic` on every branch including the deferred factory one (R79 had left that branch out of `globalPublic` entirely, though the public/open fact is known at parse time regardless of when the TYPE resolves). `publicGlobalTypesByModule` is no longer written at merge time; it is DERIVED once, after both passes that can populate `globalTypesByModule` have run. Controls, all measured: both sin fixtures flip ABSENT→`Fs`; a genuinely pure factory/destructured global gains nothing; a non-public one still does not resolve; R79's direct-constructor case and its remaining controls unchanged. Revert test: exactly the 2 fix-pinning tests go red, the 4 controls pass either way. 1006/1006 tests, `smoke.sh` 148/148, `fuzz.py` 25/25, `fabrication_probe.py` 28/28, `recall.sh` 5/5, `ci/self-gate.sh` OK; the two strace gates UNRUN, reported as unrun. Corpus A/B on all four projects: `functions[]` byte-identical, ADDED 0 REMOVED 0 CHANGED 0 — **SAFETY only, not recall, and measured as such: none of the four contains a single factory- or destructured-shaped public global.** **AND THE CONTROL PROBLEM WAS WORSE THAN ONE TEST.** R79's `testAmbiguousCrossModuleGlobalNameResolvesNothing` was confirmed uncompilable in BOTH language modes (`ambiguous use of 'sharedWorker'`; Swift 6 fails even earlier on a Sendable diagnostic) — **no reachable Swift program can trigger the `moduleCount > 1` branch it pinned.** Replaced with `testInternalGlobalInOneModuleDoesNotPoisonAPublicSameNameInAnother`, which COMPILES AND RUNS and exercises the real candidate-counting logic. **A SECOND vacuous control was then found in the same file:** R79's `testNonPublicCrossModuleGlobalDoesNotResolve` is also uncompilable (`cannot find 'sharedWorkerInternal' in scope`) — a direct cross-module reference to an internal symbol is definitionally invisible in Swift. Flagged, not yet fixed; it retains some value (it pins that the public/private filter excludes non-public names) but it cannot fail on the input it claims to test. **SO TWO OF THE FOUR CONTROLS R79 WAS ACCEPTED ON COULD NEVER HAVE FAILED — and the coordinator's own independent re-verification of the non-public one used an uncompilable fixture too, and reported it as a passing control.** The conclusion happened to be right; the evidence offered for it was vacuous. **A control over an input that cannot exist is not weak evidence, it is NO evidence** — no correct engine could pass it differently and no broken one would be caught — and this was written on the same day four other instruments were hardened for exactly that property. **When a control asserts an absence, compile and RUN the fixture first: prove the program exists before trusting what the engine says about it.** **RECALL EVIDENCE, obtained 2026-09-01 and previously the missing half of this vein.** Both R79's and R85's A/Bs were byte-identical over swift-nio/collections/algorithms/argument-parser, and two agents separately measured that those corpora contain ZERO top-level `public let`/`public var` — so those runs proved safety and said nothing about recall. A dedicated hunt then searched ~18 real packages plus ~19 GitHub code-search queries, grepping at column 0 to reject indented class properties (Kitura's `TodoListController.public let router` was rejected on exactly that basis). Real, unmodified top-level public globals exist in **Alamofire** (`public let AF = Session.default`), **jamf/Replicator** (`public let fm = FileManager()`), the **Kitura-Starter-Kit** template family and **quran-ios**. Two were A/B'd with the real declarations vendored verbatim: **jamf/Replicator** — a cross-module `fm.removeItem(atPath:)` read `inferred: [], unresolved: false` (proven-pure) pre-fix and `['Fs']` at HEAD, so **`deny Fs` would have exited 0 over real file deletion**; **Alamofire** — `AF.request(...)` read `['Fs']` only pre-fix, missing the real `Session.request` call entirely, and `['Fs','Net','Rand','Unknown']` at HEAD, so **`deny Net` would have exited 0 over Alamofire's own request path**. Both are single-row changes across 51 and 591 functions with zero over-charge elsewhere — the strongest form of this evidence. **And the honest negative: ZERO genuine top-level FACTORY globals were found, and exactly one destructured global (JXKit's version tuple, binding to `Int?`, no effect-bearing methods).** So R85's two shapes are real — proven by executed, revert-tested fixtures — but were NOT found in standard-layout public SwiftPM code despite that search. That bounds what this fix is worth and is recorded rather than left implied. **Two process catches worth keeping:** the hunt found `.build/release/candor-swift` STALE (mtime an hour before the R85 commit) **while `--version` printed the current string** — the standing trap, caught before it contaminated a measurement; and it self-reported a near-miss where a Kitura-based construction gave a byte-identical A/B only because the `Router` type was never vendored, so the receiver could not resolve either way — it had held the wrong thing constant, and said so instead of filing a clean bill. **CORRECTED 2026-09-02, BY MEASUREMENT: the "cannot fail" half of that claim is WRONG, and so is the review-panel finding that repeated it.** A later four-engine panel re-filed BOTH `testNonPublicFactoryInitializedGlobalDoesNotResolve` and `testNonPublicDestructuredGlobalDoesNotResolve` as vacuous on the same reasoning — the fixture does not compile, therefore the test cannot fail. **That reasoning is a non-sequitur for this engine: candor-swift is SwiftSyntax-based and NEVER BUILDS ITS INPUT**, so compilability is not what determines whether an assertion is falsifiable. The falsifiability question is whether the ENGINE's own mechanism is what produces the asserted absence, and that is measurable directly. Coordinator's A/B, one variable changed — `public` added to the two declarations, everything else byte-identical: 3a `run` **ABSENT -> `['Fs']`**; 3b `run` **ABSENT -> `['Fs']`**. The visibility filter is therefore exactly what makes the internal arm absent, so an engine with that filter removed would RESOLVE it and the tests would go RED. **Those controls have teeth.** What remains true from the original row is the narrower and still-important point about the AMBIGUITY control, which pinned a `moduleCount > 1` branch no reachable Swift program can trigger — that one really was vacuous, and it was replaced. **The lesson is not the one first drawn.** "The fixture does not compile" is a genuine red flag but it is not the test; it prompts the question, it does not answer it. The answer is a one-variable A/B showing the engine's verdict moves with the property the control claims to pin. Two separate agents and this coordinator all reasoned from compilability to vacuity without running that A/B, and all three were wrong in the same direction — over-condemning our own instruments, which is the mirror image of the failure this file mostly records. |
| R86 java: a literal argument chosen by a TERNARY is captured from whichever branch sits adjacent | 2026-09-01 | **CLOSED — candor-java `76485cf` + `f9b5ba5`. Pre-existing, and in published 0.34.0** | **CARDINAL SIN, java only, and the R83 question one file over — R83's `Interp.ProvInterpreter` fix never touched this surface.** Every backward-instruction-window walker in `Literals.java` (`firstLiteralArg`, `literalArgsInWindow`, `nearestLiteralArg`, `intLiteralBefore`, `reflectReceiver`, `concatArgHost`, `programHeadLiteral`) bounds its walk at a call, a jump, a `NEW` or a store — but **never at a control-flow JOIN**, a label reached by a `GOTO` from elsewhere. A ternary or switch-expression feeding a literal straight into a call argument compiles to two branches merging immediately before the call, so the walker sees only the physically adjacent branch. Coordinator's repro: `new Socket(cond ? "danger.example.net" : "safe-partner.example.com", 443)` with `net-partner safe-partner.example.com` configured — **`danger.example.net` is ABSENT from the report entirely**, and `deny Net[unknown-host]` exits **0**. **This is worse than an omission: the engine makes a POSITIVE claim about the wrong destination.** It does not say "I could not determine the host"; it says the host is the declared safe partner, and a destination-scoped policy then passes on that basis. Order-dependence confirmed by swapping which branch is dangerous — the captured host swaps with it. `deny Net` (blanket) still exits 1, since `Net` is unconditional regardless of host, so the blast radius is destination-scoped policy, `net-partner` classification, and everything downstream that reads the captured host. **The control that proves the protection mechanism exists and is specifically defeated:** the same policy over a fully-runtime host with no literal at all correctly yields `incomplete: ["Net"]` and exit 1 — so capturing NO literal fails closed, and capturing the WRONG literal fails open. | Not yet fixed. **The fix must bound the window at a join, not extend it** — a wider window would capture both literals and could as easily attribute a literal from an unrelated statement. Where two branches reach one call, the honest answer is BOTH hosts, or `incomplete`, never one of them chosen by bytecode order. Not measured, inferred to share the code path and named so the fix covers them: try/catch merges and loop-carried assignment. **Also unswept: whether other consumers of these walkers (path/tables/reflection-receiver classification) inherit the same wrong-literal claim** — the walkers are shared, so the question is not whether they do but where. **FIXED by BOUNDING the window at a join, not widening it** — `Literals.joinLabels(MethodNode)`, computed once per method, is the set of every label that is a jump/switch-case target or an exception-handler start; all eight backward walkers now stop there rather than walking through. Coordinator-verified: the ternary fixture now claims NEITHER host and emits `incomplete: ["Net"]`, and `deny Net[unknown-host]` goes exit 0 → **exit 1**. The engine stopped asserting a destination it cannot determine, which is the fail-closed direction the pre-existing runtime-host control already demonstrated. **One shared helper, so every consumer inherits it — enumerated in the fix:** Net host (bare `(String,int)` ctor, per-call window, concat-prefix, URL-terminal), Fs path, Exec program head, Db tables, reflection method-name and receiver class. The same hole was also found and closed in the single-store scanners `constStringLocals`/`constUrlLocals`, which were NOT in the list of seven I handed over — a local assigned via ternary has one `ASTORE` whose immediate predecessor differs by branch. **MY BRIEF WAS WRONG ABOUT TWO OF THE THREE INFERRED SIBLINGS, AND THE AGENT MEASURED RATHER THAN ACCEPTING IT.** Switch-expression was genuinely vulnerable in BOTH branch orders and is fixed. **Try/catch-via-local and loop-carried reassignment were already SAFE** pre-fix, via an unrelated pre-existing "two different literals stored to the same local ⇒ ambiguous" check in `constStringLocals`. I wrote those as inferred-to-share-the-path and it read as measured. **AND THE DEFECT WAS FOUND ORGANICALLY IN REAL THIRD-PARTY CODE, not only in fixtures:** `ignite-core-2.16.0` `IgniteLogInfoProviderImpl.ackClassPathWildCard` had an if/else merge before `Paths.get(...)`, and the engine was certifying the else-branch literal `"."` even when the if-branch (the common `idx > 0` case) actually ran — now correctly `incomplete: ["Fs"]`. **Isolated 325-jar A/B against a pre-image built from the exact starting commit** (separating this change from other fixes already in HEAD): **0 functions removed, 0 effect-set changes** — every difference confined to literal-detail and `incomplete` fields. 8 functions newly appear, all pure, all gaining only `incomplete: ["Db"]`, each explained via `javap`: an unrelated-branch literal was previously being absorbed into a distant SQL-literal window. Gates green; `nativeCompile`/`run_kotlin.sh` UNRUN and reported as unrun. Revert-tested: 3 of 7 new cases go red, the 4 controls hold either way. **`assert-audit.sh` flagged an unbacked assertion in the first commit and the tests in `f9b5ba5` are the response** — the tool doing its job on the day it shipped. |
| R87 java: an entry-point marker annotation is only seen at RUNTIME retention | 2026-09-01 | **CLOSED — candor-java `76485cf` + `f9b5ba5`. Pre-existing, in published 0.34.0** | **SILENT UNDER-REPORT in the query surface, java only.** `Candor.markEntryPoints` matches `Rules.ROOT_ANNOTATIONS` via `annoOrMetaMatches(mn.visibleAnnotations, …)` — **`visibleAnnotations` only**, which is ASM's RUNTIME-retention list. A custom lifecycle/scheduler/container marker declared **without an explicit `@Retention(RUNTIME)`** gets Java's DEFAULT `CLASS` retention, lands in `invisibleAnnotations`, and is never seen. That default is the single most common mistake in writing such an annotation, so the failure selects for exactly the real-world case. Measured with only the retention policy varied, everything else held constant: with `CLASS` retention `entryPoint:false` and `candor reachable` answers **`{"entryPoints":0,"effects":{}}`** — which `Query.java`'s own comment calls *"the strongest claim this tool can make — the program performs no effect at runtime"* — over a method that genuinely writes a file; with explicit `RUNTIME` it correctly reports `entryPoints:1, Fs via Job.run`. **`impact <fn>` shares the same `entryPoint()` filter and is affected identically** — and that verb is the agent edit-time feedback loop this family calls its north star. **The `--policy` scan gate is NOT affected** (a `deny <effect>` fires on a function's own direct effects regardless of entry-point status), so the blast radius is the entry-point-scoped query verbs, not the gate. | Not yet fixed, and **the authority to reuse already exists in the same file**: `anyParamAnnoMatches` (the CDI `@Observes` path) explicitly checks BOTH `visibleParameterAnnotations` and `invisibleParameterAnnotations`. The method- and class-level root-marker path never got the same treatment — two implementations of "does this element carry a root marker", one correct. Brief section G: make them share one authority rather than patching the second copy, or this recurs at the third. **FIXED by giving both paths ONE authority:** `anyDeclAnnoMatches(visible, invisible, markers)`, mirroring the `anyParamAnnoMatches` pattern that was already correct — rather than patching the second copy, which is what let the two drift in the first place. Controls measured: `CLASS` retention now `entryPoint:true`; `RUNTIME` still true; **no marker at all still false, 0 entry points** — the control that stops the fix from simply marking everything. `impact` confirmed to share the fix (0 downstream entry points pre-fix, 1 post-fix on the same fixture); `--policy` gates confirmed unaffected. **0 hits across the 325-jar corpus, and that is the EXPECTED result stated as such rather than presented as reassurance:** established libraries use correctly-retentioned framework annotations, while this defect targets bespoke markers — so the corpus cannot exercise it, and the evidence is the synthetic fixture plus its revert test, not the corpus. |
| R88 rust: the bare unannotated `let` binder never records a dispatch binding — the commonest form in the language | 2026-09-01 | **CLOSED — candor-rust `08d9d7c`. Pre-existing** | **CARDINAL SIN, rust only, and the Q3 "separate implementations that drift" question paying out exactly as designed.** Every sibling binder was hardened for dispatch-typed values across R71/R75/R76/R77 — if-let, while-let, match-arm, for-loop, let-else, annotated `let`, tuple destructure. **`visit_local`'s bare, unannotated `let` (collector.rs:2764–2897) never populates `trait_vars` at all, for ANY dispatch-typed RHS** — and it is the most common binding form in Rust. Coordinator's repro, ground truth EXECUTED, with the control in the same file: `fn run_bound_field(&self) { let h = &self.single; h.go(); }` over `single: Box<dyn Doer>` is **ABSENT** from `functions[]`, while `fn run_direct(&self) { self.single.go(); }` — same field, same trait object, direct call — correctly carries `Fs`. So the dispatch machinery is sound and the binder is the hole; that control is what makes this precise rather than a guess. **A second, compounding gap:** `resolve_recv_traits` (collector.rs:1009) has no `Expr::Index` arm although both its siblings `resolve_recv_type` (:478) and `resolve_elem_type` (:525) do, so `self.handlers[0].go()` resolves neither a concrete type nor dispatch leaves. Exit codes on the isolated repro: `deny Fs` 1 and `deny Fs Unknown` 1 (**masked — the CALLEE `Writer::go` is independently reported, so the caller-side miss is invisible**), `deny Unknown` **0** (no disclosure anywhere), `deny Fs main` **0** and `deny Fs run_bound_field` **0**. A `for h in &self.handlers { h.go(); }` positive control in the same file resolves correctly and flips its scoped deny to 1. | Not yet fixed. **The fix is to route the bare `let` through the same authority the other seven binders already use, not to add an eighth implementation** — brief section G, and the reason this shape has now recurred four times in one round. The `Index` arm should likewise be added where its two siblings already have one. Note this sits beside the already-filed, still-open gap that `resolve_elem_trait_leaves` has no `Index` arm and does not recognise `Vec::pop()`/`HashMap::remove()` as element-yielding — **three sibling accessors, one question, and only some of them answer it.** **FIXED by routing the bare `let` through `resolve_recv_traits` — the same authority `.method()` calls already use — rather than adding an eighth implementation.** The `Expr::Index` arm was added to `resolve_recv_traits` AND to `resolve_elem_trait_leaves`, a **third** accessor found to have the identical hole (`for d in grid[i] { d.go() }`). Still open and named: that accessor's `MethodCall` arm does not recognise `Vec::pop()`/`HashMap::remove()` as element-yielding. **THE FIX'S FIRST VERSION INTRODUCED A CARDINAL SIN AND THE CORPUS A/B CAUGHT IT BEFORE IT SHIPPED — the single most valuable event in this fix.** Clearing `trait_vars[name]` BEFORE the trailing `visit_local` walk re-visited the same statement's own RHS made a self-shadowing rebind — `let mut conn = conn.to_connection()…`, the real shape in mysql_async 0.37.0's `Query::run` — lose an honest `Unknown` into TOTAL silence: zero calls, zero unresolved, a fabricated false-clean. **70 REMOVED rows.** Fixed by deferring the mutation until after the trailing walk; REMOVED fell 70 → 8, with a dedicated regression test. This is the measured 4-defects-in-5-fabrication-fixes rate reproducing in real time, and the A/B is the only thing that saw it — no fixture and no review would have. Final 256-crate A/B: **ADDED 19, REMOVED 8, CHANGED 25** across 12 crates, all 8 removals traced to ONE root cause (diesel-2.3.12's `AsChangeset::as_changeset` passed as a value) and audited as a genuine precision gain — `pub use diesel_derives::AsChangeset;` shares the trait's bare name and had misdirected the old literal-path fallback into a spurious `invisible: diesel_derives` disclosure, while every real target is verifiably pure against diesel's source. 655 tests, `soundness/run.sh 60` 60/60, 34 gates individually green; the strace/oracle gates NOT run (Docker down) and reported as unrun. **Runtime witness added (candor-rust `f2e0b8e`): `pf_bare_let`**, with a paired control differing in exactly the binder or payload form under test; predicted green at `10b19c7`, so this fix is now falsifiable by the syscall oracle rather than by fixtures alone. |
| R89 rust: a TRAIT method passed as a first-class value never dispatches | 2026-09-01 | **CLOSED — candor-rust `08d9d7c`. Pre-existing** | **CARDINAL SIN, rust only, the Q2 "callable target with no body" question.** `items.iter().for_each(Doer::go)`, where `Doer::go` is an abstract trait requirement with no default body. The code at collector.rs:2015–2033 pushes a literal `Call{path: "Doer::go"}` and **never checks whether the path's head is a TRAIT** (needing bounded-CHA resolution, which `resolve_recv_traits` already provides for dot-call syntax) **or a CONCRETE type** (a real, resolvable unit). Abstract methods are correctly not recorded as units (decls.rs:120), so the edge matches nothing and evaporates — no `Unknown`, no effect, nothing. The comment at collector.rs:1996 explicitly names this shape as handled, citing `xs.iter().for_each(Conn::send)` — **true only for a concrete type's associated function, which is another comment asserting a property broader than the code delivers.** Ground truth EXECUTED. Exit codes: `deny Fs` 1 and `deny Fs Unknown` 1 (masked by the directly-reported impl), `deny Unknown` **0**, `deny Fs call_it` **0**. | Not yet fixed. The authority to reuse is `resolve_recv_traits`, already used for the dot-call form of the same question. Fixed by extracting the receiver-call CHA fan-out into a shared `dispatch_calls_for_trait_method` helper and routing the trait-method-as-value shape through it — one authority for both spellings of the question, not a second copy. **Runtime witness added (candor-rust `f2e0b8e`): `pf_trait_hof`**, with a paired control differing in exactly the binder or payload form under test; predicted green at `10b19c7`, so this fix is now falsifiable by the syscall oracle rather than by fixtures alone. |
| R90 rust: the ambiguity guard drops SILENTLY where its own comment claims it discloses | 2026-09-01 | **CLOSED — candor-rust `08d9d7c`. Introduced by `e287b8a` (this round), never published** | **SILENT UNDER-REPORT, and a FALSE CLAIM IN A COMMENT — the fifth this round.** `drop_cross_ambiguous_enum_leaves` (model.rs:158) was added by `e287b8a` to stop a cross-index leaf collision, and its doc comment (model.rs:153–155) states it "converts a WRONGLY-ROUTED result into an HONEST unresolved-receiver one". **The first half is true and the second is false.** Measured on a two-enum composite-key collision (`enum A { CbField{f: Box<dyn Fn()>} }` and `enum B { CbField{f: Writer} }`, both fields genuinely effectful, ground truth executed): the guard correctly detects the conflict and drops both entries with no fabrication — **but both callers vanish from `functions[]` entirely and `deny Unknown` exits 0.** It drops silently; it does not disclose. **The engine already knows how to do this correctly one file over:** collector.rs:1821–1838 gives an ambiguous or unresolvable dispatch receiver an honest `Unknown` on the >12-impls / no-local-impl branch. The enum-variant ambiguity path has no equivalent step. **The "provably disjoint composite key" claim in the same commit was ALSO attacked and HELD** — the collision detection genuinely works; only the disclosure is missing. | Not yet fixed: the guard should emit the same `Unknown` disclosure the >12-impls branch already emits, reusing that path rather than adding a second one. **I propagated this commit's false claim into R77's row without measuring it** — the same failure as R81, twice in one day, both times from taking an agent's or a commit's own wording as evidence. R77 is corrected. Fixed by threading the dropped-leaf set through `ElemIndexes`/`CallCollector` so a colliding leaf now sets `unresolved=true` instead of binding nothing silently. **The doc comment is corrected to state what was MEASURED rather than what was asserted** — its "honest unresolved-receiver" claim was false when written and is true only now. Re-verified on reqwest-0.13.4's `Matcher::intercept`, the same collision that motivated the row. |
| R91 ts: `--allow-js` silently discards the whole `tsconfig.json`, not just its `include` | 2026-09-01 | **CLOSED — candor-ts `1a1594a`. Pre-existing since `b68864d` (2026-06-11), six months unexamined** | **NOT a cardinal sin — the degradation is DISCLOSED — but it flips a natural scoped gate from FAIL to PASS, which is the only thing most CI checks read.** `scan.mjs:1416`: `if (fs.existsSync(tsconfig) && !allowJs)`. Passing `--allow-js` bypasses `tsconfig.json` **entirely** — not merely to admit `.js` files, but discarding every compiler option with it, including `paths` and `baseUrl`. Traced to `b68864d`, whose message scopes the intent to "overriding a TS-only tsconfig **include**" for an execa-shaped case; `paths`/`baseUrl` went with it as unexamined collateral. **There is no tsconfig-having project for which passing the flag is safe — even a tsconfig that already sets `allowJs: true` natively is discarded the moment the CLI flag is also passed.** Measured with only the flag varied, on a pure-`.ts` fixture with zero `.js` files (so the flag buys nothing and only costs), ground truth EXECUTED via real `tsc` plus a runtime alias resolver: a `@lib/*` path alias means `callViaAlias` reads `Unknown[callback:writeIt]` instead of the edge-derived `Fs`, and **`deny Fs src.index` goes exit 1 → exit 0**. Unscoped `deny Fs` still exits 1 (the callee is reported directly), and `deny Unknown src.index` and `deny Fs Unknown src.index` both stay 1 — so the documented `deny Unknown <scope>` knob IS the correct remedy and the engine does print the advisory naming it. **The agent's own severity call is the right one and is recorded as it was made: disclosed, therefore not a sin — but a real exit-code consequence on a flag whose AGENTS.md line ("also analyze .js/.mjs sources") gives no hint that it discards tsconfig.** Path aliases are standard in Next.js/Nx/most monorepos, so the shape is common. `grep` of `test.mjs` for `--allow-js` + `paths`/`baseUrl`: **zero hits — unprotected.** | Not yet fixed. The flag should widen the file set while KEEPING the tsconfig's compiler options — `allowJs` is one option, not a reason to drop `paths`, `baseUrl`, `strict` and the rest. **A latent near-miss found in the same pass and correctly NOT claimed as a defect:** `overloadImplOf` (scan.mjs:7117) matches a bodyless signature to an implementation without checking accessor KIND (`canCarryABody` accepts any Get/SetAccessorDeclaration regardless of which kind the node is) — the agent tried and FAILED to construct a reachable TypeScript program that exploits it (same-class mixed abstract/concrete accessors is TS2676; interface+class merged accessors never get minted as units, so `accessorFromSym`'s kind-checked path falls through to a disclosed `Unknown` first). **Analysed, not measured; filed as a latent gap rather than a finding** — which is the distinction that makes the rest of the report trustworthy. **FIXED at `admitJsInConfig` (scan.mjs ~1352): the tsconfig is now ALWAYS read when it exists, and `--allow-js` only forces `compilerOptions.allowJs` before `ts.parseJsonConfigFileContent` runs — so `paths`, `baseUrl`, `strict` and the rest survive.** Acceptance verified by the coordinator on an independent fixture: `deny Fs src.index` exits 1 with AND without the flag. **THE RECORDED PAST FAILURE WAS CHECKED AGAINST THE RECORD, NOT THE PARAPHRASE, AND THE PARAPHRASE WAS WRONG.** `b68864d`'s message describes overriding a "TS-only tsconfig **include**"; the agent pulled execa's actual published `tsconfig.json` at the 2026-06-11 vintage via `gh api` and found it was **`{"files": ["index.d.ts"]}` — no `include` at all**, and TypeScript only defaults to `include: "**/*"` when BOTH `files` and `include` are absent, so a `files`-only config never directory-walks. The fixture was built from the verified shape rather than the description. **The literal "TS-only include" case turned out to be real too and was closed alongside it — zod's own tsconfig is exactly that shape** (`include: ["**/*.ts","**/*.mts","**/*.cts"]`), so each such pattern now gains its `.js`-family sibling. 13 new tests across 4 fixtures. **The A/B answered the question I most wanted answered — does relaxing `include` change the file set beyond adding `.js`? Yes, and correctly:** honouring the tsconfig means honouring `exclude`, `files`, project references and `extends` for the first time under this flag, which the old raw walk ignored entirely. hono/zod/axios with `node_modules` installed: **zero determined-effect regressions in any package's library code, checked exhaustively rather than sampled.** Every removal traced to a precision gain (hono's `RegExpRouter.constructor` went from `calls: []` charged `Unknown` to real edges into two genuinely pure callees) or to correctly dropping out-of-package doc/tooling code the old bypass had swept in (zod's `packages/docs/`, named in zod's OWN `exclude`; axios's `docs/.vitepress/`). Note `deny Unknown src.index` goes 1 → 0 after the fix and that is the intended direction: the alias now resolves to real `Fs`, so there is no `Unknown` left to catch — flagged and explained by the agent rather than left to look like a regression. **Measured limitation, stated not glossed:** 108 of hono's `Unknown`-only removals could NOT be re-verified against original source, because the shared scratchpad's corpus clones were reclaimed under disk pressure mid-audit; they were verified structurally via the callgraph sidecar on a sample of 5 instead. **THE COORDINATOR CAUSED PART OF THAT** — I reclaimed `node_modules` from those exact clones during a disk sweep while the agent was mid-analysis. **A shared scratchpad is a shared instrument: reclaiming space during a wave destroys other agents' evidence, and the guard that protects a repo's evidence dirs does not cover it.** Third finding, independently re-confirmed and correctly NOT upgraded: `overloadImplOf`'s accessor-kind blindness is genuinely UNREACHABLE — traced this time to a specific guard rather than by trial and error (`localName()` at scan.mjs:3329 requires `ts.isClassDeclaration(node.parent)`, and an interface accessor's parent is an `InterfaceDeclaration`, so the bodyless node is never minted into `bodylessDecls` and `overloadImplOf` is never called on it; within-one-class mixed abstractness is separately blocked by TS2676). |
| R92 rust: a SELF-SHADOWING let-else binding silently drops the RHS's own effects | 2026-09-01 | **CLOSED — candor-rust `7c690a5`. Introduced by `67b8e45` (this round), never published** | **Registered earlier the same day as a LEAD — found by inspection, not measured to fire. A Wave 3 audit then built the fixture and it fires.** `visit_local`'s let-else branches (tuple-variant ~2633–2651 and struct-variant ~2652–2672) mutate `self.vars`/`self.trait_vars` for the newly-bound name **before** the trailing `syn::visit::visit_local` re-walks that same statement's own RHS. So when the binding SHADOWS the name it is initialised from, the RHS's own call resolves against the new binding and its effects vanish. R88's fix deferred exactly this mutation (`r88_pending_trait_vars`) for the bare-`let` branch; the let-else branches never got the same deferral. Coordinator-verified with a control that isolates the cause: `let Msg::Cb(f) = f.produce() else { return }; f.go();` reports **`['Fs']`** while the identical shape with non-shadowing names reports **`['Env','Fs']`** — the producer's `Env` is silently lost, `unresolved=None`, zero trace. **It evades the completeness manifest too, and the reason is worth stating: `P::produce` is fully present and correctly effect-tagged in `functions[]`; what is lost is the call-graph EDGE from the caller to it, so `analyzed.count` stays clean.** Traced to introduction: absent at `b33ed3f`/origin-main (the let-else tuple-variant binder did not exist pre-R77), starts exactly at `67b8e45`, unchanged through `e287b8a` and `08d9d7c` — **three commits, none of whose A/Bs caught it, because no fixture tested a let-else self-shadow; only R88's bare-`let` guard existed.** | Not yet fixed; the fix is to defer the mutation the way R88 already does, reusing `r88_pending_trait_vars` rather than adding a third ordering rule. **The audit was BOUNDED and the boundary stated:** the other three call sites were read and are correctly ordered — if-let (~2179/2204) and while-let (~2238) call `self.visit_expr(&el.expr)` BEFORE applying the binding, and the match-arm (~2350/2375) scopes the binding around the body/guard and never re-visits the scrutinee. The vulnerability is bounded to the two let-else branches. **Not established, and said so:** the `for`-loop binder uses a different function and was not examined; and a single-line regex sweep of the 256-crate corpus found zero real instances, which is what could be established cheaply rather than evidence of absence. **Coordinator's own first fixture was UNINFORMATIVE and was rebuilt** — both effects were `Fs`, so the union hid the loss; a fixture whose instrument cannot distinguish the sin from correct behaviour answers nothing, which is section E3's rule one level in. **FIXED by GENERALISING R88's existing deferral rather than adding a third ordering rule** — the single `r88_pending_trait_vars` slot became a `pending_bindings` list applied in one loop after the trailing walk, with the bare-`let` branch's behaviour unchanged. Coordinator-verified: `shadowed` and `control` now both report `['Env','Fs']`; 659 tests (655 + 4), `soundness/run.sh 60`, 7 fuzz-form variants, `run_cross`/`run_drop`, `self-gate`, `gate-equivalence`, `verify-local` all green; the Docker/strace oracle gates NOT run and named as such rather than skipped silently. **AND THE PREDICTION IN THIS ROW CAME TRUE — A THIRD INSTANCE EXISTED.** The row scoped the confirmed bug to the tuple- and struct-variant let-else branches; the agent audited the THIRD let-else form, `some_ok_binding` (`Some`/`Ok` unwrap), found the identical eager-mutation shape **unflagged**, measured the same loss (`['Fs']` instead of `['Env','Fs']`), and fixed it in the same commit. **So this is a CLASS, closed with an enumeration rather than a bug closed with a patch:** four let-else forms plus bare-`let` (R88) all deferred; if-let, while-let and match-arm previously confirmed correctly ordered; and the **`for`-loop binder — the brief's explicitly unexamined item — audited and confirmed SAFE by construction**, since `visit_expr_for_loop` visits the iterated expression exactly once before the binding is applied, so there is no second walk to poison. That enumeration is the deliverable; the fix is the smaller half. **Real-world incidence, stated as not established:** the 256-crate A/B is ADDED 0 / REMOVED 0 / CHANGED 0, and a widened grep across the full ~1489-crate cache found exactly two real let-else self-shadows (`ureq-proto-0.6.1`, `native-tls-0.2.18`) — **both concrete-payload, not trait-dispatched, so they exercise only the `vars` half and not the `trait_vars` half where the cardinal sin lives.** Both scanned pre/post with zero diff. No real-world trait-dispatched trigger was found, which matches this row's own earlier regex sweep and is recorded as a bound on the finding's practical reach rather than left to imply the fix was urgent on shipped code. |
| R93 ts: a `process` destructured from `globalThis` loses its `Env` effect entirely | 2026-09-01 | **CLOSED — candor-ts `e40a995`. Was PRE-EXISTING and present in published artifacts** | **CARDINAL SIN, ts only. Found incidentally while ground-truthing an unrelated corpus row, which is where several of today's best findings came from.** `'NO_COLOR' in process.env` is correctly charged `Env`. The identical check reached through `const { process } = globalThis as any` is **absent from `functions[]` entirely** — not `Unknown`, not pure-with-a-hedge, simply gone. Coordinator's repro, with the two spellings side by side in one file so the control is intrinsic: `a.direct` → `['Env'] unresolved:false`; **`a.viaDestructure` → ABSENT.** Ground truth EXECUTED, not reasoned: a `Proxy` trap on `process.env`'s `has` confirms **both** functions genuinely touch it at runtime. Gates: `deny Env` exits 1 (incidental — `direct` is independently reported), **`deny Env a.viaDestructure` exits 0.** Confirmed on BOTH the pre-image and HEAD, so it is unrelated to today's `c53baa2`/`1a1594a` and is live in published artifacts. **This is the real-world shape, not a synthetic one — it is how hono's `getColorEnabled` reads `NO_COLOR`,** which is how it was found. | Not yet fixed. The mechanism to establish first: whether the loss is in the `globalThis as any` cast erasing the type, in the destructuring binder failing to carry the host identity, or in `process?.env` optional-chaining — **do not assume; the family's worst fixes come from a plausible diagnosis rather than a measured one.** Note this is the same QUESTION as brief §F1's item 3 (separate implementations that drift) applied to host-identity tracking: the direct spelling resolves and the destructured one does not, which means two paths answer "is this the `process` host" and only one is right. **ALL THREE CANDIDATE MECHANISMS THIS ROW LISTED WERE WRONG, and the agent measured instead of picking one.** Not the `as any` cast, not the destructuring binder, not the optional chaining: a 9-arm matrix — each arm ground-truthed with a runtime `Proxy` `has`-trap on `process.env` — showed **every** local binding of the global read ABSENT, cast or not, destructured or plain-reassigned, renamed or same-named, off `globalThis` or `global`. The real cause: `identIsGlobalProcess` treated "this identifier has a declaration inside a project file" as proof of a project-local SHADOW — but a local that merely HOLDS the global gets its own project-file declaration too, so a genuine alias was indistinguishable from a shadow. **That is the brief's §F1 item 3 exactly: two paths answering "is this the host", one wrong.** Fixed with a `processAliasSymbols` pre-pass resolving a local's initializer back to the ambient `process`, giving `identIsGlobalProcess` one authority. Coordinator-verified: `direct` and `viaDestructure` both `['Env']`. Controls measured rather than asserted — a destructured NON-host gains nothing, a function merely mentioning `globalThis` gains nothing, and **destructuring `process` off a genuine project-local shadow object still gains nothing** (the control that stops the fix from simply trusting the name). Revert test: exactly the 7 positive fixtures go red, the 3 guards stay green by design. A/B with `node_modules` installed: **hono ADDED 1 — `utils.color.getColorEnabled` gaining `Env`, the exact real-world shape that motivated the row** — plus 1 CHANGED (its async caller, transitively), REMOVED 0; zod and axios 0/0/0. 1746 tests. |
| R94 java: field-lambda binding trusts a PUBLIC MUTABLE field exactly as it trusts a private final one | 2026-09-01 | **ANSWERED AND CLOSED — candor-java `d6419ae`, and the question was mis-framed.** Both offered answers (state the limit / enforce it) rested on an open-world premise that holds only while the consumer is UNSCANNED; across a CHAIN SPEC ⟨0.39⟩ says the trade stops being acceptable. Answering it produced [[R595]], a published CARDINAL SIN. Originally filed: OPEN — a SCOPE/SOUNDNESS-ARGUMENT question, not a measured defect. Raised by the whole-day corpus audit** | **NOT a sin, and deliberately not filed as one — every verdict examined was ground-truth CORRECT.** The issue is the mechanism's stated justification, not its current output. `collectFieldLambdaBindings` reasons that a field's possible values are whatever write sites this scan observed. For `amqp-client`'s `WorkPool.enqueueingCallback` — **private final** — that is genuinely closed and the argument holds. But `couchbase`'s `CoreTransactionAttemptContextHooks.afterStagedInsertComplete` is **`public`, non-final, and explicitly documented as a test fault-injection hook**, with one write site inside the jar; the mechanism treats it identically. **There is no visibility or finality check anywhere in it.** Within a single-jar closed world the default really is `(ctx,s) -> Mono.just(1)` and the verdict is correct — but the soundness premise is stated **without qualification** and is false for any public mutable field an external caller can reassign after the scan. This is the "comment asserting safety" class from this round applied to a MECHANISM's argument rather than a code comment: the claim is broader than what the analysis can support, and it happens to be true only because of a scope assumption nobody wrote down. | Not a code change yet — the question is whether the closed-world premise should be **stated** (a documented scope limit, so a reader knows single-jar binding does not survive an external reassignment) or **enforced** (bind only private/final fields, and disclose `Unknown` for public mutable ones). **Enforcing it is the fail-closed direction and would cost precision on real code; stating it costs nothing and is strictly better than the status quo, which asserts more than it knows.** Raise with whoever owns the field-binding design. Related: [[R80]], [[R83]], [[R84]] all fixed defects in this same mechanism today, none of which touched its scope argument. |
| R95 ts: a `fetch` destructured from `globalThis` loses its `Net` effect — R93's sibling, different code path | 2026-09-01 | **CLOSED — candor-ts `30fc8ea`. Was PRE-EXISTING and present in published artifacts** | **CARDINAL SIN, ts only. Found by TRANSLATING R93's question to the other host globals (brief §F) rather than by stopping at the shape that was handed over.** `await fetch(u)` is correctly charged `Net`; `const { fetch } = globalThis; await fetch(u)` — **no cast needed** — is **ABSENT from `functions[]` entirely**. Coordinator-verified with both spellings in one file: `b.directFetch` → `['Net']`, `b.viaDestructure` → ABSENT. **This is a DIFFERENT code path from R93** and was not fixed by `e40a995`: the bare-global `fetch` classifier does its own ambient-global check, and `checker.getResolvedSignature` resolves the destructured call straight to `lib.dom.d.ts`'s ambient `fetch` declaration while the Net-charging branch never fires. **The translation also produced a genuine clean negative that bounds the class:** `crypto` does NOT share the hole — its verb table is TYPE-keyed, so it survives destructuring and degrades only to a safe `Unknown` under an `any` cast. `require('fs')`, `Deno`, `Bun` and `localStorage` were inconclusive or not identity-gated at all, and are **explicitly not claimed** rather than swept in. | Not yet fixed, and **deliberately kept out of `e40a995` so that fix's controls stay legible to one mechanism** — folding a second code path into a fabrication-sensitive fix is how this family has produced regressions. The shape of the fix is the same lesson as R93: the ambient-global check and the type-keyed verb table are two mechanisms answering "is this the host", and `crypto` demonstrates the type-keyed one is the sound design. **Prefer converging `fetch` onto the mechanism that already works over patching the one that does not.** **FIXED by a resolved-declaration predicate — the mechanism `crypto` and eleven other globals already use** — added as the LAST arm of the existing chain, so it is purely additive and can only add `Net` to a call already reported pure. **THREE CORRECTIONS TO THIS ROW, all measured.** (1) **The resolution target named here was wrong.** The call does NOT resolve to `lib.dom.d.ts` in any normal scan: `scan.mjs` forces `types: ["node"]`, so every spelling lands on `@types/node/web-globals/fetch.d.ts:23`. That mattered — the fixer's first plan was an `<es-lib>` arm **which would never have fired**, and it found out only by instrumenting and proving the fixture reached the branch (zero hits). §E3, one level in. (2) **Five spellings were silent, not one** — `const {fetch}=globalThis`, `const {fetch:f}=globalThis`, `const g=globalThis; g.fetch(u)`, `(0,fetch)(u)`, `const a=[fetch]; a[0](u)` — all resolving to the same declaration, so one predicate closes all five. (3) **THIS ROW'S "inconclusive" LIST WAS WRONG, and the follow-up it implied is unnecessary.** Sweeping every effect-bearing global on the pre-fix engine: WebSocket, EventSource, XMLHttpRequest, sendBeacon, clipboard, crypto, eval, `Math.random`, `Date.now`, `new Date`, `performance.now` — **and `require`, `Deno`, `Bun` and `localStorage` — are ALL destructure-invariant. `fetch` was the only one broken**, so the "converge all host globals" proposal is moot: they are already converged. **The fixer found its own fix's defect before shipping** — guard 3's first cut read `call.expression`, skipping PropertyAccess callees, so `import * as shim from "./vendorshim"; shim.fetch(u)` **fabricated `Net` over the project's own shim**; it now reads the callee's head identifier, with a fixture. A/B over 10 corpora with `node_modules` installed, keyed wide (16 fields): 6,543 common rows, **ADDED 0, REMOVED 0, CHANGED 3.** **Hit counts reported honestly as THIN: hono 1, supabase 2, the other eight 0 — and the destructure spelling itself has ZERO instances** in those corpora or in 13 packed HTTP/isomorphic packages; the reach comes from `typeof fetch`-typed slots. Real gate move: hono's `service-worker/handler.handle` (default option literally `fetch: globalThis.fetch.bind(globalThis)`) goes `['Unknown']` → `['Net','Unknown']` and its scoped `deny Net` flips exit 0 → 1 — blanket `deny Net` was already red from unrelated `Net` and caught it only incidentally. Revert test: 8 of 14 new rows red, the 6 green ones absence controls whose teeth come from a guard-deletion sweep. `assert-audit.sh` correctly flagged one untested guard half, now worded as the scope statement it is. |
| R96 swift: a closure-typed FIELD is resolved from whatever default the scan saw, contradicting a PUBLISHED guarantee | 2026-09-01 | **CLOSED — candor-swift `3c78535`. PRE-EXISTING, present in published artifacts** | **CARDINAL SIN, swift only, and the only finding this round that breaks a promise made in shipped documentation.** `README.md:44` states: *"A function-typed value invoked (`let f: () -> Void` param, **a closure-typed field `d.f()`**) reads `Unknown` — never silent purity."* Measured, one file, one scan, holding everything constant but the default's body: a `public var f: () -> Void` with an **effectful** default gives `Sink.fire` → `['Fs']`; the identical field with a **pure** default, reassigned externally to something effectful, gives `SinkPure.fire` → **ABSENT** — no `Unknown`, no `unknownWhy`, no hedge. **The discriminator is "did this scan see a write?" — `var` versus `let`, and access level, are never consulted** (`CallCollector.swift:2735` states the rule as "assigned in init / no initializer → Unknown", with no mention of reassignability). That is java R94's premise one language over, except R94 was a scope ARGUMENT with correct outputs and this one **fails silent on real code**. Ground truth EXECUTED: `/tmp/swq-eff2.txt` was genuinely deleted. **Gate consequence, and it is the worst shape found today: `pure SinkPure.fire` — the STRONGEST assertion candor offers — exits 0 over provable file deletion, and so does `deny Unknown SinkPure.fire`, README's own named strictness knob scoped to the offending unit.** Blanket `deny Unknown` exits 1 only incidentally, via the parameter half of the same guarantee, which does honour it. The engine's honest answer already exists one branch away; the default-inference suppresses it. | Not yet fixed. **The fix is to consult reassignability, not to widen the hedge** — a `let` field bound to a visible default is genuinely closed and must stay resolved (that is real precision and the R85 recall evidence shows it matters); a `var` field, or any field whose declaration permits an external write, must fall to the `Unknown` the README already promises. **Also measured and NOT a defect: `open`-class devirtualization is sound in-scan** — a `public var hook: Base` unions `Base.work` and a downstream `Evil.work`. Scanned as a library alone it is absent, which is correct given scope, **but nothing in README or AGENTS.md states that an `open` method may be overridden downstream** — the stated-vs-enforced gap, same as R94's. **FIXED by consulting REASSIGNABILITY, not by widening the hedge.** A `let` field with an initializer cannot be assigned anywhere in Swift, so it stays exactly resolved — the precision R85's Alamofire recall evidence depends on. A `var` field now UNIONS the visible default's effects with `Unknown` + `dispatch:<Type>.<field>`. **Union, not replacement: replacing would have lost `deny Net` on an effectful default, and that arm is a pinned control.** Coordinator-verified: `Sink.fire` → `['Fs','Unknown']`, `SinkPure.fire` → `['Unknown']`, `deny Unknown SinkPure.fire` exit 0 → **1**. **MY ROW UNDERSTATED IT:** I wrote that blanket `deny Unknown` exits 1 incidentally via the parameter half — measured in a fixture WITHOUT that half, blanket `deny Unknown` **also exits 0**, so nothing rescued it. **And one part deliberately NOT changed, correctly:** `pure VarPure.fire` still exits 0, because `README.md:47` / SPEC §4 / AS-EFF-003 say family-wide that a `pure` rule forbids every EFFECT, not `Unknown`. **My row treated that as part of the defect; it is not — it is a four-engine spec question for Tom, not a swift fix.** `unverified --strict` now names the scope and prints `deny Unknown VarPure.fire` as the upgrade, where pre-fix it had nothing to say because the unit was absent. **RECALL, per §E1's hit counters: R96 is SAFETY-ONLY over the four swift-* corpora** — `R96_HEDGE=0`, `R96_RESOLVED=0`, and grep confirms zero `var` closure-field declarations in any of them. The recall arm is **Alamofire: 5 real closure-field invocations, all `let`, all still resolved, zero hedged, zero rows changed** — the over-charge control exercised on real code, on the branch the fix changes. |
| R97 swift: type information is dropped by 22 of 23 binder arms — only one calls `dealias` | 2026-09-01 | **CLOSED — candor-swift `3c78535` + `795a39f`. PRE-EXISTING, present in published artifacts** | **CARDINAL SIN, swift only, and the THIRD appearance of this engine's signature failure** — R33 and R73 were both "the mechanism lived in one binder arm and the siblings never got it". `rootOf` dealiases correctly (`CallCollector.swift:606`, whose own comment example is literally `FM` → `FileManager`), but **of 23 `vars[…] =` assignment sites, exactly ONE calls `dealias`** — line 2343, the `for case let x as T` arm — and that is the one arm that works. `typealias FM = FileManager; let fm = FM.default; try fm.removeItem(...)` → **gone**, while the inline `FM.default.removeItem(...)` is correctly `Fs`, so the alias is the sole variable. Lost through: parameter annotation, annotated local, `for…in` annotation, `if let`, `guard let`, `as!` cast, return type, stored property, module-scope global, alias-of-alias, and module-qualified alias. Verified across THREE effect classes (`Fs`, `Env`, `Net`), each with its plain-spelled twin correctly charged. **Three sibling shapes of the same "binder arm loses the type" class, each with a working control one spelling away:** a closure PARAMETER of host type (`{ (fm: FileManager) in … }` → gone; the same closure CAPTURING rather than taking a parameter → charged — and `CallCollector.swift:2536` asserts of that very line `// explicit { (x: Foo) in } — precise`); `switch case let x as T` and `if case let x as T =` → gone while `for case let x as T` and `if let x = any as? T` → charged, so **two of four cast-binder spellings work**; and `for x in [Host.singleton]` with an inline array literal → gone while a named `let fms: [FileManager]` → charged. **All exit 0 on all six policy forms** — `deny Fs`, `deny Unknown`, `deny Fs Unknown`, `deny Fs <scope>`, `pure <scope>`, bare `pure` — over a program whose only purpose is deleting a file, with no `Unknown`, no `incomplete`, no `unresolved`, no `coverage.uncovered`. **Present in `callgraph` but absent from `functions[]`, so it is analyzed-not-skipped and the completeness manifest stays clean** — the same evasion as rust R92. Instrument calibrated: the same tree with one line changed exits 1 on five of six. | Not yet fixed. **The fix site is unusually well-defined — one `dealias` call per binder arm — but that is exactly the shape that produced this bug: 23 hand-maintained sites where one got the treatment.** Section G says make them share one authority rather than adding 22 more calls that can drift again. **FIXED with THREE authorities, not 22 `dealias` calls** (§G): `rootOf` dealiases its ANSWER once; `typeCastBinder` types `case let x as T` across all three grammars; `leaveShadowScope` owns the type restore for every scope. 21 arms ABSENT→charged, 0 lost. **FOUR SPELLINGS THE ROW DID NOT HAVE, all measured:** (1) `if let x = any as? T`, which my row said WORKS, works only for a plain type — **aliased it was silent**; (2) the closure-parameter case is discriminated by **POSITION, not aliasing** — the plain-spelled `{ (fm: FileManager) in … }` is silent too, so `CallCollector.swift:2536`'s `// explicit … — precise` was true only in call-argument position; (3) the module-qualified alias (`Foundation.FileManager`) was a **documented limitation in `Classifier.swift`'s own comment** — a silent under-report reading as considered, the [[feedback-documented-limitation-is-not-measured]] shape exactly; (4) a seventh instance found **by attacking the agent's own commit** — the line `assert-audit.sh` flagged — where `callAsFunction` reads `vars[name]` directly, bypassing `rootOf`, so `typealias C = Caller; c()` was silent. **Residual pinned as a failing-by-design row:** `let c: (FileManager) -> Void = { fm in … }`, an unannotated param typed only on the variable, is still silent. |
| R98 swift: a binder that SHADOWS its receiver's name loses the effect in its own initializer | 2026-09-01 | **CLOSED — candor-swift `3c78535`. PRE-EXISTING; rust R92's class, one language over** | **CARDINAL SIN, swift only, and it is R92's ordering bug in a different engine — found by translating the question rather than by hitting it again.** In `if let`, `guard let` and `for…in`, when the new binder's name equals the receiver's, the effectful initializer or sequence expression reads pure; the identical construct with a non-shadowing name is charged. Verified across two effect classes. Mechanism: `visit(OptionalBindingConditionSyntax)` (`:3244`) and `visit(ForStmtSyntax)` (`:2268`) clear or rebind the name and **then** `return .visitChildren` — and the initializer is a CHILD of that same node, so the receiver's type is already gone when the call is walked. **The guard that would fix it already exists and is already applied — once.** `visit(OptionalBindingConditionSyntax)`'s own comment reasons about exactly this self-reference (`if let u = u.asURL()`) and handles it for `protoTyped` via `rebindTyped(name, through: initVal)`, while the `else` branch calls `clearBinding(name)` unconditionally and wipes `vars`. `Self.referencesName` (`:4423`) exists and is applied in one binder, to one index. **`let`, `catch let` and `switch case let` pass — and plain `let` passes only ACCIDENTALLY**, because its `vars[name]` write is conditional on `info.root != nil`. | Not yet fixed. rust closed the same class at `7c690a5` by generalising one deferral rather than adding a third ordering rule, and by enumerating every binder including the ones that turned out safe — **do the same here: apply the existing `referencesName` guard across the binder family from one place, and enumerate the arms that are already correct so the list is closed rather than four fixes and an open question.** **FIXED with one rule in three places — the producing expression is walked before the name moves** — and all 17 arms enumerated, with `catch let` and `switch case let` confirmed already-correct and PINNED so the list is closed rather than three fixes and an open question. **MY ROW WAS WRONG TWICE, both measured against the true pre-image so they are not artefacts of the other fixes:** plain `let w = w.kill()` does **NOT** pass — I wrote that it passes accidentally because the write is conditional on `info.root != nil`, but `info.root` is a `String` there and does not survive; and **`while let` and `if case let w?` are also affected**, which the row did not list. **The agent also caught its own fixture contaminated** — a module-scope `let w` of the same name was rescuing three arms via `globalTypes`, so the comparison had two variables until it was renamed. **AND IT INTRODUCED A DEFECT THAT 1023 TESTS DID NOT CATCH AND THE A/B DID:** its first `if/guard case` deferral cleared every binder in `visitPost`, clobbering `typeEnumCaseBinding`'s payload typing, and swift-nio lost `SelectableEventLoop.cancelScheduledCallback → …didCancelScheduledCallback` plus an `Env` reach. Now gated twice and pinned by a test proved red against the clobbering version. **That is the second time today a fabrication-fix's own regression was caught only by the corpus A/B.** |
| R99 rust: host identity lost through a submodule re-export or a nominal alias — BLANKET `deny` FAILS | 2026-09-01 | **FULLY CLOSED — candor-rust `10b19c7` + `c45bbd9` (both stated residuals). PRE-EXISTING, present in published artifacts. THE MOST SEVERE FINDING OF THIS SESSION** | **CARDINAL SIN, rust only, and the ONLY finding this round where a BLANKET policy fails.** Every other sin today was caught incidentally by `deny <E>` because the CALLEE was still independently reported; here nothing anywhere in the report carries the effect, so the whole crate reads clean. Coordinator's repro, EXECUTED (spawns `/bin/true` twice): `via_facade`, `via_alias` and `main` **all ABSENT**; `deny Exec` **exit 0**, `deny Unknown` **exit 0**, `deny Exec Unknown` **exit 0**. No `Unknown`, no `unresolved`, no `incomplete`, `excluded: []`. **Three independent mechanisms, each with an intrinsic control one spelling away:** (1) a **submodule-level `pub use` of an external/std item** is dropped before an edge is built — `resolve_use_base` (`decls.rs:~380`) returns empty for a head segment that is not a local `mod`, and `collect_reexports` (`~440`) does `if from.is_empty() { continue; }`; **the crate-ROOT spelling of the identical thing resolves correctly** via `collect_root_reexports`, which is what makes this precise rather than a guess. Facade and prelude modules re-exporting a dependency are ubiquitous. (2) a **NOMINAL type alias is never recorded** — `decls.rs:1267` records an `Item::Type` only when `is_non_nominal_type` holds, so `pub type Cmd = std::process::Command` and `pub type Client = reqwest::Client` vanish. (3) a **`const`/`static` holding a fn item** is never tracked. **A fourth, same vein: `fn_alias` does not CHAIN** — `let w = std::fs::write; let v = w; v(..)` is absent in both shadowing and non-shadowing spellings, while the one-hop form works; `collector.rs:~2996`'s `single_local` guard tests `vars`/`closure_vars`/`fn_typed_vars` but not `fn_alias`. **The coordinator's earlier Q-A aim was WRONG and this is the measured correction:** `use std::fs as f`, `use std::fs::write as w`, `use std::fs::File as F` and one-hop `let w = std::fs::write` all resolve correctly. The hole is specifically re-export **crossing a module boundary**, and nominal aliasing. | Not yet fixed. **This retires a claim I have leaned on all day and repeated in the published 0.34.0 advisory: "blanket `deny` is unaffected".** That was true of the defects then in hand — because their callees stayed reported — but it was never a property of the engine, and I stated it as though it were. Any future severity call must MEASURE the blanket form rather than reason that the callee will surface. **FIXED at the exact `if from.is_empty() { continue; }` branch that was dropping them**: mechanisms 1–3 are now collected in ONE walk into a crate-wide `mod_aliases`, seeded into every file's `use` map and consumed through **`expand`, the existing authority** rather than a second one (§G). Scoping follows `seed_root_reexports` — `crate::<qualified>` everywhere, the relative spelling only where the name is in scope, never a bare crate-wide name — pinned by an over-charge control (a submodule's own `Client` stays pure under a root `pub type Client`). All four spellings flip to their real effects. **The agent extended past the trigger and found THREE silent caller spellings where the row described one** (`facade::Command`, `crate::facade::Command`, and `use crate::facade;` + `facade::Command` from a sibling file). **Still open and STATED rather than guessed:** submodule GLOB re-exports of external items (`pub use std::process::*`) are still dropped, and Pass A's decl indexes do not see `mod_aliases`, so a struct FIELD typed `facade::Command` is still unresolved. **RUNTIME WITNESSES ADDED (candor-rust `f2e0b8e`)** — the four closed mechanisms have drivers `pf_alias_use`/`pf_alias_type`/`pf_alias_const`/`pf_alias_chain`, all predicted green, and **both open shapes now have one too**: `pf_alias_glob` and `pf_alias_field`, each with a one-variable paired control that passes. `pf_alias_glob`'s report is `functions: []` with `excluded: []` and no disclosure channel set anywhere while the program demonstrably writes a file — **this row's "blanket `deny` fails" signature, reproduced as something a kernel can witness.** **BOTH RESIDUALS NOW CLOSED (`c45bbd9`), and MY SIZING OF THEM WAS BACKWARDS.** I wrote that Pass-A threading is historically the larger job; it was the smaller and safer one — a second merge pass re-expanding four index families through the existing `seed_mod_aliases`+`expand` authority. **The GLOB was the larger job, because a glob import is SHADOWED by an explicit item of the same name**: `mod glb { pub use std::fs::*; pub fn write(..) }` means the LOCAL `write`, and rewriting it to `std::fs::write` would be **fabrication** — the rewritten path leaves the crate and `scan.rs` never attempts a local link for a std-rooted path. The module's own declared names now ride in the alias entry and the lookup refuses any leaf among them; refused also for a private glob, two-or-more exported globs, and a `#[cfg]`-duplicated glob (R105's rule). **THE RATCHET FIRED ON A REAL FIX FOR THE FIRST TIME** — with the fix in and the entries still listed, `run_pf.sh` printed `✗ STALE ALLOWLIST ENTRY` for both drivers and exited 1; both were deleted in the same commit and the driver headers rewritten so a future red reads as a NEW finding. Coordinator-verified: `KNOWN_UNDER_PERFN` now holds only `pf_oncelock_cb` (R101); 688 tests. Per-function oracle recall **27/27, up from 25/25** — the two drivers joined the falsifiable set rather than leaving it. **THE 256-CRATE CORPUS WAS SAFETY-ONLY FOR THE GLOB BRANCH (11 globs recorded, 0 resolved), so a §E1 recall hunt over all 1489 registry crates found 2456 resolutions in 25 — and a real under-report: `pnet-0.35.0` declares `pub mod transport { pub use pnet_transport::*; }`, and `layer4`/`layer4_ipv4`/`layer4_ipv6`/`layer3_ipv4` reported `inferred: []` over a call that opens a RAW TRANSPORT SOCKET, now `["Net"]`** — per-function, since the crate verdict was already exit 2 for an unrelated reason. **The A/B found a defect in the fix itself:** letting a multi-arm `#[cfg]` alias into the decl index cost 5 rows in async-lock and bytes their `inferred: ["Unknown"], unresolved: true`, flipping `deny Unknown` 1→0 — refused, because R105 adjudicates arms at the CALL SITE and a decl index has none. Final: ADDED 15, REMOVED 0, CHANGED 2, all 17 rows ground-truthed. **Guard-deletion sweep: two guards did not turn a test red and both were recorded rather than papered over** — one test was measuring the wrong thing (a `#[path]` file takes its modpath from its filename, so the globs never collided) and was replaced; one guard is not behaviour-bearing and its docstring now says so. **A flaky test was also shipped and caught:** `incremental_scan` mutates a process-global env var and silently de-injected the abort tests' fault — visible only because `gate-run.sh` runs the suite twice. |
| R100 rust: R92's ordering bug is live one level in, in `elem_of` | 2026-09-01 | **CLOSED — candor-rust `10b19c7`. R92's class, and R92's enumeration was the wrong one** | **CARDINAL SIN, rust only.** R92 closed the mutate-before-walk class over BINDERS and deferred only the `pending_bindings` (`vars`/`trait_vars`) half; **~46 other mutations in `visit_local` still precede the trailing walk, and `elem_of` is one of them.** `collector.rs:3011-3013`: `self.elem_of.remove(name)` runs **before** `self.resolve_elem_type(&init.expr)`, whose `.clone()` arm recurses into the receiver and reads the `elem_of[xs]` entry just deleted. **The comment justifying the removal — "Drop any STALE element binding first" — describes exactly the entry the RHS needs.** One-variable A/B, everything else held constant: `let ys = xs.clone(); for y in &ys { y.go(); }` → `['Fs']`; `let xs = xs.clone(); for x in &xs { x.go(); }` → **ABSENT**. `deny Fs b2_shadow` → exit 0. `let xs = xs.clone();` is an ordinary Rust idiom. | Not yet fixed. **The lesson for R92's own row: enumerating the BINDERS was necessary and not sufficient** — the class is "state mutated before a walk of the same node", and the binder enumeration covered one family of ~46 candidate sites in a single function. The next fix should enumerate by MUTATION SITE, not by binder form. **Note the agent's Q-B enumeration came back CLEAN on the shape I predicted** — all five hand-maintained save/restore disciplines (`visit_item_fn`, `visit_item_impl`, `visit_impl_item_fn`, `visit_block`, `visit_pat`) are straight-line with no early returns, and `scoped_binding` saves and restores all ten name-keyed tables and is pinned by its own test. My predicted defect was not there; the real one was one level in. **THE FIX IS THE ENUMERATION LESSON, NOT THE PATCH.** R88 deferred the bare `let`'s write; R92 deferred the three let-else binders'; the class survived both because `visit_local` makes **~46 name-keyed mutations across a dozen tables**, and it resurfaced in `elem_of` and then in `fn_alias` — the latter found by R99's own fixture minutes later. **Enumerating BINDERS was the wrong axis; the fix enumerates the STATE**: one capture/restore window restores the pre-statement state for every read of the statement's own RHS and for the trailing walk. **The per-binder deferrals were REMOVED, not left beside it**, and the revert test proves the subsumption — deleting the window turns 6 tests red including R88's own and all three R92 fixtures. `str_locals`, named in this row as unestablished, is now MEASURED: the earlier fixture could not separate the hypotheses because neither arm resolved a host, and the new one asserts the control resolves one first (§E3) — the shadowed arm was indeed losing it. **Runtime witness added (candor-rust `f2e0b8e`): `pf_shadow_elem`**, with a paired control differing in exactly the binder or payload form under test; predicted green at `10b19c7`, so this fix is now falsifiable by the syscall oracle rather than by fixtures alone. |
| R101 rust: the closed-world CHA premise makes a positive WRONG claim, and its config knob is declared-but-unimplemented | 2026-09-01 | **PARTLY FIXED — the `OnceLock` half is CLOSED (candor-rust `e3054e0`); the CHA half is OPEN and its framing here is CORRECTED; the config-knob claim was false and is retracted** | **CARDINAL SIN plus a FABRICATION, rust only.** R94 asked whether an engine claims more than its scan scope supports; java's answer was "yes, but every output was correct". **Rust's answer is that the output is WRONG.** A `grep` for any visibility check in candor-scan returns **zero hits** — the CHA route (`collector.rs:822`, `:1113`, `:2009`) keys purely on `impls.len() <= 12`, so a `pub` trait in a LIBRARY target with one local impl is treated exactly like a private trait in a binary. Taken past a scope argument by chaining two real crates: library `qc` exports `pub trait Store` with one local `Fs` impl; consumer `qcons` adds `impl Store for Net2` doing `Net` and calls the library's `use_store(&Net2)`. Executed. The consumer's chained report says `consumer_reaches_net` → `inferred: ['Fs'], paths: ['/tmp/qc_local.txt']` — **the function opens a socket, and the report names a file it never touches.** A positive misattribution, not an omission, and `deny Net consumer_reaches_net` → exit 0. **And the assumption is not written down anywhere: `closed-world` is present in `CONFIG_KEYS` (`config.rs:11`) and ABSENT from `CONFIG_KEYS_IMPLEMENTED` (`config.rs:18`)** — the engine behaves as permanently closed-world with no way for a user to say otherwise, while advertising the key. **Second instance, same vein, §F1 Q3 exactly:** an externally-installed callback invoked through interior mutability — `static CB: OnceLock<Box<dyn Fn()>>`, `install()` from outside, `fire()` calling it — is silent on `deny Net`, `deny Unknown`, `deny Net Unknown`, `deny Net fire`, **and even bare `pure`**, which lists 10 violations without `fire` among them because it is absent. **The sibling path answering the same question is RIGHT:** a plain fn-typed parameter correctly reports `Unknown` / `callback:unresolved call`. Two paths ask "what does this opaque callable do"; the `OnceLock::get()` if-let binder never marks the binding fn-typed, so the call resolves as a phantom free-fn and disappears. | Not yet fixed. Fixing the CHA premise is a design call of the R94 shape — **stated or enforced** — but rust differs from java in that the output is already wrong, so "state it" is not sufficient here. The `closed-world` key being advertised and unimplemented should be resolved either way. The `OnceLock` instance should converge on the fn-typed-parameter path that already answers correctly. **RETRACTION, and it is mine.** This row asserted that `closed-world` is *advertised-but-unimplemented* — I read `CONFIG_KEYS` against `CONFIG_KEYS_IMPLEMENTED` and inferred a defect **without running the engine**. Measured: setting `closed-world true` prints a loud, key-specific disclosure — *"recognized by the candor family but not implemented by candor-scan — that gate/mode is NOT active on this scan (the nightly lint / another engine enforces it)"* — clearly distinct from the unknown-key control (`"ignoring unknown config key 'banana-mode'"`), and candor-java has a `ClosedWorldTest`, so the parenthetical is accurate too. **The DECLARED-GATE-SILENTLY-OFF defence was working the entire time.** That is the seventh claim of mine today corrected by measurement, and the second filed into this document as a conclusion drawn from reading rather than running. **THE CHA DESIGN CALL NOW HAS ITS COST MEASURED**, which is what it needed: with bounded local CHA switched off across 256 crates, **1,004 of 11,357 rows move (8.8%) across 68 crates (27%) — but only 11 rows LOSE an effect.** So enforcing open-world trades ~11 concrete attributions for ~1,000 rows becoming `Unknown`. That argues to PROPOSE, not impose. **The misattribution is separable and needs fixing regardless of the policy** — naming a file the function never touches is a positive wrong claim, not a precision trade. The `OnceLock` instance is confirmed by executed ground truth; its convergent fix is a statics-type index feeding the same route the fn-typed parameter already uses, and the agent declined to scope it into this commit rather than ship it unmeasured. **Runtime witness added (candor-rust `f2e0b8e`): `pf_oncelock_cb`, predicted RED** (`fire(pure/omitted)`); its control reaches the same callable through a fn-typed parameter and passes **by disclosure** (`inferred: ['Unknown']`) — **the pair separates "blind to interior mutability" from "blind to opaque callables"**, which is exactly this row's claim about the sibling path. **THE `OnceLock` HALF IS CLOSED, and my mechanism was right but was ONE OF FOUR GAPS — the fix was inert without each.** (1) a `static`'s declared type was recorded in no index (new Pass-A `callable_statics`, plumbed like `lazy_statics` and shadow-gated by the SAME `locally_bound` test the lazy forcing edge uses, lifted into a shared helper so the two cannot drift); (2) `elem_trait_leaves` peeled `Mutex`/`RwLock`/`RefCell`/`Cell` but not the deferred-init cells — `OnceLock`/`OnceCell`/`LazyLock`/`LazyCell`/`Lazy` added, and deliberately NOT to `trait_leaves`, since a `OnceLock<Box<dyn Doer>>` is not itself a `Doer` and does not `Deref`; (3) `get`/`get_mut`/`get_or_init` were missing from the element-preserving accessor list; (4) an `unsafe { .. }` scrutinee was not peeled — **and reading a `static mut` REQUIRES `unsafe`, so that is the spelling real code uses.** `fire` now converges field-for-field on the fn-typed-parameter control. **THE ALLOWLIST IS NOW EMPTY — oracle 28/28, disclosure recall 28/28 under `silent`, `wrong` AND `transitive` (was 27/27 + 1 uncalibrated), program arm 19/19 — the completion criterion the allowlist was built with, and the ratchet's THIRD real-fix expiry.** Nothing is suppressed in either oracle, so a red is now a NEW finding by construction. **I ASKED FOR A CONTROL THAT WOULD HAVE RE-BROKEN THE ROW, AND IT WAS REFUSED, CORRECTLY:** my "a `OnceLock` never `set` gains nothing" is a heuristic over an UNPROVABLE ABSENCE — blind to `#[cfg]` arms, `include!`, macro expansion and `tests/` — and for a `pub` static written by a DOWNSTREAM crate it would re-silence exactly the library-publisher case this row was filed for. Fixture `fire_never` pins the refusal. **AND THIS ROW'S CHA REPRO DOES NOT REPRODUCE — I CONFLATED TWO ROWS.** Rebuilt and EXECUTED in three spellings against both the pre-image and the fix: `consumer_reaches_net` reads `inferred: []` with no `paths` in every one — an OMISSION, not the `inferred: ['Fs'], paths: ['/tmp/qc_local.txt']` positive wrong claim I filed. That misattribution is on the LIBRARY's own `use_store` row. **So there is nothing separable from the policy question after all** — what remains is closed-world bounded CHA over one local impl in a library scanned alone, i.e. the open-world call already priced and queued for Tom, and "the misattribution is fixable overnight regardless of the policy" was wrong. The cardinal-sin half stands: `deny Net consumer_reaches_net` exits 0. **The 256-crate corpus recorded ZERO callable statics — SAFETY-ONLY, said at the time** — so a recall hunt over all 1489 registry crates found the branch reached in 28 and a REAL under-report: **proptest 1.9.0/1.11.0's `scoped_hook_dispatcher` invokes an externally-installed panic hook through `static mut DEFAULT_HOOK` and was reported PURE**; that find is what forced gap 4. **The controls found two defects in the fix itself:** consulting `returns` before peeling made the whole fix INERT once a crate had any local `fn get` (`returns` is keyed by bare method leaf crate-wide) — caught by the over-charge control, not by inspection; and extending that fallback to every adapter cost 4 rows their `invisible` κ disclosure across 78 crates. Cache schema rev13 → rev14, because `#[serde(default)]` would have let a warm cache replay the silence; cold and warm proven byte-identical to a full scan. |
| R102 rust: `run_pf.sh` lacks the KNOWN_UNDER allowlist its sibling has, so a known gap ABORTS the recall calibration | 2026-09-01 | **CLOSED — candor-rust `d6fdda6`. A HARNESS defect, not an engine one** | **THE §H AGGREGATION SHAPE: the detector works and the aggregator discards the result.** `soundness/realworld/run.sh:~40` carries a `KNOWN_UNDER` allowlist whose own comment states the intent — *"tracked so the oracle is a clean gate — green on known gaps, red only on NEW findings."* **`run_pf.sh` has no such mechanism.** Two oracles, one question, one answer — §F1 Q3, in the harness rather than the engine. The consequence is worse than a red gate: `soundness/realworld/recall/disclosure_recall_check.py:117` does `if flag_parse(control): print("ABORT: the control pass is already red…"); return 2`, so a known-open driver does not merely fail `run_pf.sh` — it **aborts the per-function disclosure-recall calibration, and those recall numbers stop being produced at all.** A gate that goes red is read; a calibration that silently stops emitting is not. Affected workflow: `disclosure-recall.yml` (`run_pf.sh` is not a `run:` step anywhere; `disclosure_recall.sh:60` is its only caller). | Not fixed — **verdict aggregation is a coordinator call, not an agent's**, and the agent said so rather than editing the oracle it was asked only to add drivers to. Minimal repair: give `run_pf.sh` the `KNOWN_UNDER` list its sibling already has, and teach `parse_control_pf` to subtract it. **This is a PRE-PUSH BLOCKER for `f2e0b8e`**: three drivers (`pf_alias_glob`, `pf_alias_field`, `pf_oncelock_cb`) are deliberately red against genuinely-open rows, and pushing them without the allowlist would take the recall calibration offline rather than report a known gap. Each red driver's header states this in full so a future reader hitting the red does not "fix" the wrong thing. **THE ORACLE HAS NOW RUN — first runtime ground truth of the session** (Docker restored 2026-09-01; the daemon had been wedged by a 22-day-old `com.docker.backend`, not by disk). `docker run --cap-add=SYS_PTRACE rust:latest`, strace + python3 installed in the container: **25 passed, 0 skipped, 3 failed**, and **the 3 failures are EXACTLY the 3 predicted red** — `pf_alias_field`, `pf_alias_glob`, `pf_oncelock_cb`. All 9 predicted-green shapes passed, all 12 paired controls passed, and the 4 pre-existing drivers passed. **The prediction table was 12/12 correct**, which validates the static reading of the reader's rule as well as the engine. `pf_alias_glob` reports *on-stack-at-Fs but candor missed: `body` `main` `put`* — R99's blanket-`deny`-fails signature, witnessed by the kernel rather than inferred from a fixture. **`run_pf.sh` exits 1 on those three, which is precisely the KNOWN_UNDER gap this row is about**: the reds are correct and expected, and without the allowlist they will take the disclosure-recall calibration offline rather than report a known gap. **FIXED as a SHARED mechanism, not a second list** (§G): `soundness/realworld/known_under.sh` holds both oracles' lists and both helpers, and `run.sh` + `pf/run_pf.sh` source it — the sibling was MOVED onto the shared one rather than copied. Entries are `<driver>\|<row>\|<mechanism>`: `pf_alias_glob` (R99), `pf_alias_field` (R99), `pf_oncelock_cb` (R101). `parse_control_pf` and its program twin move allowlisted drivers into the UNCALIBRATED remainder **by name and row** — not into the falsifiable denominator (they would read as missed catches every pass) and not out of both counts (the silent truncation that checker exists to prevent). **The calibration emits numbers again, first time this session: per-function falsifiable 25, uncalibrated 3, recall 25/25 under `silent`, `wrong` AND `transitive`; program arm 19/19 under `silent` and `wrong`.** **THE RATCHET — this row's deeper hazard — is closed and proven SIX WAYS through the real oracle**, one variable each, image and commit and driver set held constant: allowlisted-and-failing → green with a note (exit 0); `pf_fs` allowlisted-but-PASSING → **STALE**, exit 1; an entry naming no driver → **DEAD**, exit 1; a seeded non-allowlisted under-report → NEW finding, exit 1; that canary made uncompilable → **BUILD FAILED**, exit 1; allowlisted-and-skipped → warn only, exit 0. Stale is a FAILURE rather than a warning, deliberately: the entry must leave in the same change as the fix, or the suppression outlives it. **AND THE DOCKER CANARY FOUND A SECOND DEFECT, OF EXACTLY THE CLASS THIS ROUND IS ABOUT — see R104.** **Left open and reported rather than fixed, correctly** — a second behaviour change to a live CI gate in one push: `run.sh` honours `CARGO_TARGET_DIR` for the BUILD but hardcodes `SCAN="$ROOT/target/debug/candor-scan"`, so it cannot be pointed at an out-of-tree target dir; running the program oracle in a Linux container therefore clobbered the host `target/` with ELF artifacts (restored, and verified Mach-O again). **Also, all five Linux-only gates ran in-container for the first time this session:** `realworld/run.sh` 20 honest / 0 NEW, `run_deep.sh` 14 honest / 0 NEW, `disclosure_recall.sh` exit 0, `oracle.sh 40` exit 0, `oracle_pf.sh 40` 31 passed / 9 skipped / 0 failed — their earlier reds were a missing `dylint-link`, i.e. environment, not repo. |
| R103 ts: an externally-writable callable SLOT resolves to whatever default the scan saw — the whole report reads `functions: []` | 2026-09-02 | **CLOSED — candor-ts `0b360d4`. Was PRE-EXISTING and live in the published `candor-ts@0.34.0`** | **CARDINAL SIN, ts, and the most severe instance of a question that has now gone 4-FOR-4** — java R94 (scope argument, outputs correct), swift R96 (cardinal sin, contradicted a published guarantee), rust R101 (`OnceLock`), and now ts. **It breaks TWO published promises.** `README.md:167`: *"Anything candor-ts can't resolve is `Unknown`, never silently pure: a function-valued parameter or **field** being called…"*; `README.md:198` restates it as a SPEC §4 row. **The discriminator is whether the author wrote an explicit function-type ANNOTATION, never whether the slot can be written from outside** — probed directly through the TS API: `handler = pureDefault` resolves to a `FunctionDeclaration` with `hasBody=true` and candor EDGES to the default, while `handler: () => string = pureDefault` resolves to a `FunctionType` and correctly yields `Unknown[callback:…]`. A `grep` for `ReadonlyKeyword\|isReadonly\|NodeFlags.Const` in `scan.mjs` returns three hits, all inside the string-literal host resolver; **there is no mutability, finality or visibility check anywhere in the call-resolution path.** **THE LIBRARY-PUBLISHER CASE, coordinator-verified, ground truth EXECUTED** (an outside consumer assigns an `Fs` body, `fire()` returns `"fs"`, `/tmp/tslib-EVIDENCE.txt` is written): a two-line library — `function pureDefault(){…}` plus `export class Sink { handler = pureDefault; fire(){ return this.handler(); } }` — produces **`functions: []`, the entire report**, and **`deny Fs`, `deny Unknown`, `pure`, `deny Unknown <scope>` and `pure <scope>` ALL exit 0.** Nothing rescues it, because there is no annotated sibling in the tree to catch incidentally. Six externally-writable shapes resolve to the observed default, five of them silently: public instance property, `static` field, object-literal property mutated after construction, `export namespace` `let`, and `Object.assign`. Two of those additionally make a POSITIVE WRONG CLAIM — an `Env` default is reported as `['Env']` for a slot that actually performs `Fs`. | Not yet fixed. **§F1 item 3 at its purest: the correct rule is already written down ONE FUNCTION AWAY in the same file.** `scan.mjs:2359-2394`'s `reassignedIn`/`unaliasGlobal` carries the comment *"ASSIGNED SOMEWHERE ⇒ its value is not its initializer. Neither answer is available, so say so."* — and a module-level `let modSlot = pureDefault` later reassigned correctly yields `["Unknown"]`, while the class-property path never consults it. Converge the property path onto that rule rather than writing a third. **Genuine clean negatives that BOUND the fix and must not be "fixed":** an exported `let`/`const` is genuinely closed (ESM import bindings are immutable — `h = evil` is a compile error), a `Record`/`Map` registry correctly says `Unknown[callback:unresolved call]`, `globalThis.gHandler` is correct, and a never-written `private` field with an `Env` default correctly reports `['Env']` — that last is the precision control. **`readonly` gives the SAME answer as non-readonly, so the distinction is never consulted at all** (java R94's shape) — recorded, not filed separately. **No test would go red:** `test.mjs` (14,722 lines) contains no fixture of a class field bound to a function reference. **Unexamined and named as the most likely next instance: CommonJS/`--allow-js`**, where `module.exports.handler = evil` is a genuine cross-module write and the ESM-immutability clean negative may not hold. **Before any fix ships, §E1's hit-counter arm is required** — swift's R96 fix was SAFETY-ONLY on its corpora, and nobody has measured how many real npm packages carry unannotated function-typed class fields, so the over-charge cost of hedging is unknown. **FIXED as a UNION, not a substitution — and MY PROPOSED FIX DIRECTION WAS WRONG IN THE STRICT DIRECTION, caught by the corpus.** I said to route the property path onto `reassignedIn`. That function REPLACES the resolution with `Unknown`, and measured on hono it cost `src.client.client.hc` its `Clock` — `["Clock","Net","Unknown"]` → `["Net","Unknown"]` — flipping `deny Clock` from exit 1 to exit 0. **A silent-under-report fix introducing a silent under-report.** The correct authority was the class-override fan-out 200 lines below: edge to the candidate you CAN name, and ADD `Unknown` because the set is not provably whole — ⟨0.35⟩'s own "a non-empty candidate set is not a complete one". The shipped form is additive, so **zero removals and zero effects lost by construction.** Coordinator-verified: `Sink.fire` → `['Unknown']` with `unknownWhy: ['callback:this.handler']`; `deny Unknown` and `deny Unknown <scope>` both 0 → 1. **THREE MORE CORRECTIONS TO THIS ROW, all measured.** (1) **`pure` exiting 0 is CORRECT by spec, before and after** — `pure` is a deny with an empty effect list and denies EFFECTS, not the §4 trust marker (`policy.mjs:1139`; the reference engine agrees; candor-ts was fixed AWAY from the other behaviour on 2026-07-09). This row listed five exit-0s as the sin; **only three flip**, and I overstated the severity by counting two correct behaviours as defects. (2) **The shape count was six and is NINE** — CommonJS `module.exports.handler`/`exports.h` and a constructor-assigned JS instance field are genuine cross-module writes (executed: `lib.fire()` returned the attacker's value), and element access `this["handler"]()` is a ninth, **found only because a comment the fixing agent had itself written asserted it was already handled — measured, it was not.** All nine class/JS shapes are closed. (3) **The over-charge fear was measured FALSE: 21 rows, 0.46%**, not the large fraction I warned of. **`readonly` RULING, measured rather than argued:** under tsc 6.0.3 `const w: { handler: () => string } = r; w.handler = evil` compiles with **no cast and no `any`** — readonly-ness is not part of property assignability — and `r.fire()` then returns the attacker's value; the same widening against `private` is `TS2322`. **The type system enforces one and not the other, and only the enforced one may narrow a sound over-approximation**, so `readonly` stays OPEN and `private`/`#` never written stays CLOSED — worded in code as an ASSUMPTION, since `private` is erased at emit. A/B across 9 configs and 8 packages with `node_modules` installed, keyed on all 18 fields: **278 hits, ADDED 21, REMOVED 0, CHANGED 127, zero effects lost**; all 21 are zod's `static create = (params) => {…}`, ground-truthed from zod's source. **Element access is FIXTURE-ONLY / SAFETY-ONLY — 0 hits on all nine corpora, reports byte-identical with and without that branch — stated in the row rather than discovered later.** 24 fixtures; revert test 15 RED, 9 revert-invariant and every invariant one a control; **the two NO-LOSS rows are revert-invariant by design, so they were proven separately by degrading a copy of the engine to the SUBSTITUTING form, where they go red.** `assert-audit.sh` flagged a claim of the agent's own that was too strong and it was corrected. **STILL OPEN AND PRICED FOR A RULING:** object-literal property, `Object.assign`'s result, and `export namespace let` still resolve through the slot; a measurement-only widening to them takes hits 278 → 984 and over-charge 21 → 106 rows (4×), still with zero removals — **and unlike the class shapes there is no encapsulation keyword to exempt the genuinely-closed cases with.** |
| R104 rust: the ORACLE reported a clean pass over its own build failure — and manufactured a cardinal sin out of a missing report | 2026-09-02 | **CLOSED — candor-rust `d6fdda6`, in the same change that found it. A defect in the INSTRUMENT, found by the canary that was required before trusting the instrument** | **THE INSTRUMENT-THAT-CANNOT-FAIL CLASS, in the one tool whose entire job is catching that class.** A Docker canary — seed a deliberately-broken driver, confirm the oracle FAILS it — was made a precondition of trusting any oracle result, because the daemon had returned from a 22-day wedge hours earlier and a re-wedged daemon's output shape can be indistinguishable from results. **The first canary was rejected by cargo (missing `[workspace]`), and the oracle printed `build failed — SKIP`, summarised `0 NEW failure(s)`, and EXITED 0.** A build failure producing output byte-indistinguishable from a clean pass, inside the syscall oracle. **In `run.sh` the absent-report case was strictly worse than silent:** it fell through to the reader as `funcs=[]` → `pred="-"` → **`NEW UNDER-REPORT`** — the oracle **manufacturing a cardinal sin out of its own failure to produce a report.** So the same instrument could both hide a real finding and invent a false one, from the same root cause. | Fixed in both oracles: `BUILD FAILED`, `NO BINARY` and `NO CANDOR REPORT` are now RED, behind a single loud `retry` that prints candor-scan's exit code and output — a SILENT retry would hide precisely what the branch exists to surface. In `run.sh` the absent-report path is now a `broke`, not a finding. **A driver that never reached the verdict has lost its coverage, and lost coverage is not a pass.** Then the repaired canary was flagged as a NEW failure with its write witnessed in the strace, proving the chain build → strace → scan → verdict is live end to end; the canary was removed and the restored run reproduced the baseline byte-identically. **One residual, MEASURED vs INFERRED separated honestly:** one driver produced no report in one of six full passes, and 60 consecutive isolated scans of that same driver produced 60 reports — the bind-mounted-tree race is INFERRED, not proven, and would not arise on a native CI checkout. The loud retry mitigates it without hiding it. |
| R105 rust: a `#[cfg]`-duplicated alias is resolved by SOURCE ORDER, fabricating the losing arm's effect | 2026-09-02 | **CLOSED — candor-rust `27ad9d1`. Introduced by `10b19c7` (this round), never published** | **CARDINAL SIN *and* FABRICATION, rust only, prevalence MEASURED at 6.6% of a real registry slice.** All three R99 alias-collection routes end in a bare `aliases.insert(...)` (`decls.rs:474/501/524`). When two `#[cfg]` arms declare the same qualified name — the standard platform/feature shim — **the last one in the file silently wins.** candor deliberately analyses EVERY `cfg` branch everywhere else (`mod_path_attrs`'s own doc says so); the alias map is the one place that collapses them to an arbitrary winner instead of unioning or disclosing. **R90 added exactly this guard for enum-variant leaves eleven hours earlier** (`drop_cross_ambiguous_enum_leaves` → `unresolved = true`); the discipline was not carried across. Coordinator-verified with two crates identical but for the ORDER of the two arms, on a host where both resolve to `std::fs::write`: arm order A → `['Fs']`, `deny Fs` **exit 1**; arm order B → `['Env']`, `deny Fs` **exit 0**. The losing arm's effect class is not merely lost, it is **invented**. On the agent's two-class fixture `deny Fs`, `deny Exec` and `deny Unknown` all exit 0 with no `unresolved`, no `incomplete`, no ambiguity marker — nothing. **PREVALENCE, measured with an engine instrument (not regex) over the same 256-crate slice `10b19c7` used: 31 collision sites, 22 distinct keys, 17 of 256 crates — 6.6%, and that is only the a–c alphabetical slice.** Named real instances: `rdkafka-0.39.0` and `quinn-udp-0.5.15` (`pub use log::{…}` vs `pub use tracing::{…}` — **`tracing` wins, and `tracing` is a NON-DEFAULT optional feature, so candor picks the arm a default build does not use**), `hickory-resolver`, `async-nats`, `backon`, `socket2`, `mio`, `nix`, `openssl`, `axum`, `sqlx`, `bytes`, `borsh`. | Not yet fixed. **The fix already exists in this engine, for a different index:** R90's ambiguity guard drops a colliding leaf from both maps and sets `unresolved = true` rather than guessing. Apply the same to `aliases` — union the arms if their effects agree, disclose `Unknown` if they do not, never pick by position. **HONEST BOUND, stated by the auditor rather than extracted:** in every real instance ground-truthed, both arms fall in the SAME effect class (`log`/`tracing` → Log; `libc::c_int`/`c_uint` inert), so no wrong-arm class crossing bites in this slice — **the severity is proven on a constructed fixture and the SHAPE is proven prevalent at 6.6%; the conjunction is not yet measured in the wild.** `rdkafka` is the closest real case: effects attributed to `tracing` when the executed crate is `log` — a wrong-CRATE attribution rather than a wrong class. **Regression status, precisely:** `deny Fs` was also 0 at `7c690a5` (both functions were absent), so the exit code is not new. What `10b19c7` adds is a fabricated effect class and something worse than the old absence — **a confident, complete-looking effect set with no disclosure channel set, so a reviewer asking "did candor see this function?" now gets YES instead of NO.** **FIXED by adjudicating at the CALL SITE, not by picking a winner.** `record_alias` keeps every `#[cfg]` arm, sorted; `alias_join` distributes the caller's leaf over them; `scan.rs` asks the CLASSIFIER with the leaf in hand. Arms that classify alike → charge that effect with the surface withheld and `incomplete` set; arms that differ → `Unknown` + `ambiguous:` (**SPEC §4's existing fifth kind, not a new one**). The cross-FILE half is fixed by the same rule — `cache.rs` had asserted such a collision "is not a program that compiles", and `#[cfg]`-conditional `#[path]` is exactly that program. Coordinator-verified: both arm orders now give `['Unknown']` + `ambiguous:cfg-duplicated alias`, identical. **OVER-CHARGE COST MEASURED AT ZERO: of 89 collision call-sites in 5 of 256 crates, 89 AGREE and 0 disagree** — every real instance is a portability shim (`alloc`/`std` `Box`, `core`/`loom` atomics) indistinguishable to the classifier, which is exactly why union-when-equal was the right shape rather than blanket `Unknown`. Wide 13-field A/B: **ADDED 0, REMOVED 0, CHANGED 0.** **THE A/B FOUND TWO DEFECTS IN THE FIX ITSELF, both now in the diff:** the first cut `continue`d past the κ ledger and lost 32 rows + 20 changed in `bytes-1.12.1`, every one a dropped `invisible: ["extra_platforms"]`, ground-truthed from `bytes/src/loom.rs`; the second still lost 3, because a multi-arm type sorted `core::…` first failed `visit_method_call`'s three std-rooted gates, which read only the first segment. **⚠ A GATE-SEMANTICS TRADE IS NOW OWED TO TOM — see the note below this table.** |
| R106 rust: a seeded bare alias hijacks a FUNCTION-BODY-LOCAL item — and the shipped over-charge control cannot fail | 2026-09-02 | **CLOSED — candor-rust `27ad9d1`. WIDER than filed: the plain `use` spelling has the same hole and PREDATES `10b19c7`, so this is partly pre-existing and IS in published artifacts** | **FABRICATION of both an effect and a SURFACE, rust only.** `lang.rs:976-977` seeds the BARE alias name into the declaring module's own file. A function-body-local item of the same name does not shadow it: `submodule_uses`'s shadowing rule applies only to INLINE SUBMODULES, and nothing applies it to a function body. Fixture, compiling and executed, with the local item deliberately `Fs` and the alias `Exec` so the classes cannot be confused: a body-local `struct Cmd` whose `new` writes a file, under a crate-root `pub type Cmd = std::process::Command`. Pre `7c690a5`: `inferred: ["Fs"]`, `deny Exec` exit 0 — correct. Post `10b19c7`: **`inferred: ["Exec","Fs"]` with `cmds: ["true"]`** and `deny Exec` **exit 1**. The report positively names a command string for a process that is never spawned. | Not yet fixed. **AND THE OVER-CHARGE CONTROL THAT SHIPPED WITH THE FIX STRUCTURALLY CANNOT FAIL** — the §E3 class, in a control written the same day: `r99_a_root_type_alias_does_not_reach_a_submodules_own_same_named_type` (`tests.rs:12001`) places the rival `Client` in `src/local.rs`, a FILE submodule with modpath `"local"` — **a position the `module == modpath` bare-seed branch structurally cannot reach** — and gives it empty `new`/`go` bodies, so its `effs_opt(...).is_empty()` assertion holds for two independent reasons unrelated to the fix. Its calibration siblings are absent from the report pre AND post. **The one position where the bare seed DOES land — the declaring module's own file — is exactly the position the control does not cover, and it is where this defect lives.** Prevalence on real code is unmeasured: the mechanism is proven on a compiling fixture, but no registry sweep for body-local items colliding with a crate-root alias name has been run. **MY ROW WAS WRONG ABOUT THE SCOPE, and fixing only what I described would have left the commoner spelling silent (§9).** The plain `use std::process::Command;` route has the IDENTICAL hole and predates the alias work — measured at HEAD, same `['Exec','Fs']`, same fabricated `cmds:["true"]`. Both are now closed, at the body: body-local items shadow file-level bindings through **one shared item-kind list with `submodule_uses`** rather than a second rule. **And an assertion of the fixer's own was FALSIFIED and the code changed because of it:** "removal can never invent an effect" — with `use std::process::Command as W;` and a body-local pure `struct W`, merely REMOVING the binding made `W::make` fall back to module-relative resolution and tail2-link to an unrelated `other::W::make`, trading an `Exec` fabrication for an `Fs` one. The name is now rebound to an `ITEM_SENTINEL` that matches no definition; post-fix the function is pure. **Residual, stated:** `#[cfg(test)]` body items do not shadow even under `--include-tests`; the walk stops at nested `fn`/`impl`/`mod`. Not measured in java/ts/swift. |
| R107 rust: the capture/restore window's "a table added tomorrow is caught" guarantee is FALSE, and five reads escape it | 2026-09-02 | **PARTIALLY CLOSED — candor-rust `27ad9d1`; the completeness claim is reworded rather than made true, and that residual is OPEN** | **A COMMENT ASSERTING SAFETY, disproven mechanically — the class that was 4-for-4 across the engines yesterday, now found in a guard written to close that very class.** `collector.rs:2738-2739` and `BoundNameState`'s doc claim *"a TABLE added tomorrow is caught by `every_name_keyed_table_is_restored_for_the_rhs_walk`."* The auditor added a 13th name-keyed table to `CallCollector` and poisoned it for every bound name inside the window's scope, exactly as a real binder would: **the guard test PASSED, and the whole 306-test suite passed.** What actually forces the author's hand is that ten exhaustive struct literals fail to COMPILE — but nothing forces adding the field to `BoundNameState`/`capture_bindings`/`restore_bindings`, and the guard's hand-written 12-field enumeration then passes regardless. **The forcing function is the compiler, not the test; the test is a REGRESSION pin for the twelve fields already there, not a COMPLETENESS pin, and it is written as though it were the latter.** | Not yet fixed. **Separately, TASK 3's enumeration: the window is COMPLETE with respect to `visit_local`'s own writes** — `BoundNameState` covers all 12 tables `visit_local` writes, and the other 4 name-keyed tables are never written there (`bound_names` is body-wide and computed once, so pre/post are identical by construction; its source `decls::pat_bound_idents` is a full `visit_pat_ident` walk, so no binder shape escapes it). **But it is INCOMPLETE with respect to reads INSIDE `visit_local`: five reads of name-keyed state are not wrapped in `with_pre_bindings` and run after writes in the same statement** — `tuple_of` (2932), `tuple_trait_of` (2943), `tuple_elem_leaves` inside the binding loop (2988, after the loop's own removals for earlier positions), `expr_is_fn_typed` (3081, after 3019/3034/3045/3068), and the `single_local` check (3141, after `closure_vars.remove` at 3034). Seven other reads ARE wrapped and correct. **And the `fn_alias` self-shadow at 3162 is handled by an explicit `shadowed_alias` local rather than the window — a second mechanism answering the same ordering question, which is precisely what this commit's own doc says removing the per-binder deferrals was meant to avoid.** Exploitation: the closure self-shadow through 3034/3141 IS a confirmed fabrication (`inferred: ["Env","Fs"]` where truth is `Fs`, from a free fn never called) but is **PRE-EXISTING, identical at `7c690a5` and HEAD** — a standing over-charge, not this round's. The tuple-swap read at 2988 could not be isolated: the control arm loses dispatch typing too, so the instrument cannot separate the hypotheses (§E3) — reported unproven rather than claimed. **TWO OF THE FIVE READS I NAMED HAVE NO DIFFERENTIAL, and the agent said so instead of claiming five fixes.** `tuple_of` (2932), `tuple_trait_of` (2943) and `expr_is_fn_typed` (3081) do NOT run after a write to the tables they read — nothing above them writes those. They were wrapped anyway and **labelled as having no differential**. The three that are real: **`tuple_elem_leaves` (2988) was a SILENT UNDER-REPORT** — `let (d, n) = (d, n); d.go()` absent from `functions[]` while the non-shadowing twin `let (e, m) = (d, n); e.go()` reports `['Fs']`; `single_local` (3141) and the closure-rebind marking are fabrications, both **PRE-EXISTING at `7c690a5`**; and `fn_alias`'s `shadowed_alias` local — the second mechanism answering the ordering question the window exists to own — is **deleted**, not left beside it. **THE COMPLETENESS CLAIM WAS RE-PROVEN FALSE INDEPENDENTLY** rather than taken from the audit: a 13th name-keyed table added and poisoned inside the window's scope, and the pin passed with all 390 tests. The compiler forces the field into ten exhaustive struct literals, **none of which is `BoundNameState`/`capture_bindings`/`restore_bindings`.** The comment is now worded as the regression pin it actually is. **OPEN RESIDUAL, and it is the honest one:** a name-keyed table added to `CallCollector` and not registered in `BoundNameState` is outside the window and **nothing in the repo will say so.** Making it genuinely complete needs the field list derived from one place — not done. |
| R108 rust: an eval gate that could not tell "no finding" from "no run" — and its ABSENCE CONTROL printed PASS over a dead instrument | 2026-09-02 | **CLOSED — candor-rust `a7cc542`. A HARNESS defect. MY DIAGNOSIS IN THE ORIGINAL ROW WAS WRONG AND INVERTED** | **NEITHER FAILURE WAS A DETECTION DEFECT. `candor` never ran.** I filed this as two silent under-reports in the architecture gate. Measured: `candor-scan` and the dylint evaluator record both cross-crate edges and act on them correctly — re-running with a loadable host-ABI library gives `[AS-EFF-008] billing::record_activity reaches { metrics.growthtracker.io:443 } outside the allowlist` and `[AS-EFF-009] domain::checkout reaches into a forbidden layer (via infra)`, with `charge_customer` and `subtotal` correctly absent, ground-truthed against the fixture crates' source. **AND MY KEY INFERENCE WAS BACKWARDS.** I wrote that the passing step-3 assertion proved *"the harness is live and the DETECTION is what fails"*. It proves the opposite: `cargo candor policy` uses a DIFFERENT and correct locator (`newest_of`, `cargo-candor:57-73`, whose own comment reads *"Explicit mtime scan, NOT `ls … \| head`"*), so step 3 passing is evidence the ENGINE works, while steps 1–2 never loaded it. **Root cause, one copy-pasted line in both scripts, unchanged since `c572e92`:** the library is selected by `find … -name '*.dylib' -o -name '*.so' \| head -1` — platform-blind and ORDER-based — and guarded by `[ -e "$LIB" ]`. Two defects, both failing in the miss direction: a foreign-ABI Linux `.so` in the shared `target/debug` is listed first and handed to `cargo dylint`, which refuses it; and on no match `basename ""` is empty, so `$LIB` becomes the DIRECTORY and `-e` accepts it — **so `"FAIL: no candor dylib"` has never once been reachable.** The `origin/main` worktree I measured hit the second defect (never built); the local clone hit the first. **AND I CAUSED THE LOCAL ONE:** my Docker oracle run at 00:53:56 dropped a Linux `.so` into the shared `target/debug`, because `soundness/realworld/run.sh` honours `CARGO_TARGET_DIR` for the build but hardcodes `$ROOT/target/debug` for the scanner — **a hazard the R102 agent had reported to me hours earlier and I did not connect.** CI has never seen either defect: ubuntu builds only a `.so` (no ambiguity) and always builds first (no guard hole); `gh run list` shows **`ci success 3cf055d`**, the very commit I measured red. | **THE FINDING THAT OUTLIVES THE BUG, and it is why my confidence was misplaced rather than merely wrong: each script pairs a positive assertion with an ABSENCE CONTROL — and absence is exactly what a DEAD INSTRUMENT PRODUCES.** So the dead run printed `FAIL` on the positive and `PASS` on the control, and that pairing is precisely what made me read it as a precise, trustworthy detection failure. **A control that asserts nothing-is-there cannot distinguish a working engine from an absent one unless something proves the stream was written** — §E3 one level up from "the fixture must compile and run". Fixed with a single locator/liveness primitive (`eval/bet3/_lib.sh`: newest HOST-ABI library by mtime, a guard that fails on empty rather than on `-e` of a reconstructed path, and `assert_live`, which refuses to adjudicate a stream candor did not write — keyed on cargo's INDENTED `Checking`/`Finished`, since dylint's unindented preamble prints even when nothing loads). **`verify-instrument.sh` makes the calibration a GATE**: three dead-instrument states plus a green control, asserting each script exits non-zero, names the reason, and **credits no `PASS`** — falsified against the pre-fix scripts, where it fails 5/5 and reproduces the vacuous `PASS: pure domain::subtotal not flagged` verbatim. All four teeth scripts now rc=0 **with the offending `.so` still present**. **The same locator class survives elsewhere in candor-rust, unfixed and listed:** `integrations/claude-code/test-receipt.sh:59` (degrades to a skip), `install.sh:52`, `tests/integration.sh:20`, `soundness/{run,run_cross,run_drop}.sh` (mtime-correct but platform-blind), and **the recipe published to downstream agents in `AGENTS.md:92` and its two copies under `crates/`.** Read-only cross-engine sweep: the full combination is unique to candor-rust, with two live single-defect instances elsewhere — `candor-spec/.github/workflows/conformance.yml:257` and `candor-swift/soundness/confirmatory/run_frozen.sh:74` — both retaining working guards, neither touched. |
| R109 ts: `localStorage` fails closed ONLY when `lib.dom` is absent — the spelling every browser project uses is silent | 2026-09-02 | **CLOSED — candor-ts `e620af4`. Was PRE-EXISTING and present in published artifacts, in the DEFAULT configuration** | **CARDINAL SIN, ts, and a DOCUMENTED LIMITATION THAT IS FALSE FOR THE COMMON CASE.** `scan-core.mjs:377` states of `web-globals/storage` that *"it is not inert and has not been modelled — it fails closed."* **That is true for the `@types/node` spelling and false for the `lib.dom` one.** With `"lib": ["ES2022","DOM"]` — what every browser and isomorphic project configures — `localStorage.setItem("token", secret)` resolves to lib.dom's `Storage` interface, lands on the es-lib "conventionally-pure" arm, and the report is **EMPTY**: coordinator-verified `functions: []`, `deny Unknown` **exit 0**, over a call that writes caller-supplied secret material to persistent storage. Under `@types/node` the same call correctly yields `Unknown[native:web-globals/storage.getItem]` and `deny Unknown` exits 1 — **so the engine has a right answer and a wrong one for the same source line, selected by tsconfig.** This is [[feedback-documented-limitation-is-not-measured]] exactly: the comment reads as considered, and the consideration covered one spelling. Found outside its brief's scope by the R95 agent, which reported it rather than folding it into a fabrication-sensitive fix. | Not yet fixed. **The fix shape is already in the file, two lines above the false comment** — the `crypto` note handles precisely this es-lib/`@types/node` split, and the same shape applies here: an es-lib arm keyed `parent === "Storage"`. **Needs a four-way parity decision, which is why it was left:** `localStorage` is a browser surface, so java has no analogue, but swift (`UserDefaults`) and the ts/rust WASM paths may. **Also worth noting the R95 agent self-caught an instrument error while finding this** — it nearly filed a `deny Unknown` false-pass that was its own `$?`-after-a-same-line-command-substitution bug, re-measured, and reported the real finding instead. That is the error I have made twice in this session and warned every brief about. **MY ROW WAS WRONG IN ONE PLACE AND UNDERSTATED IN ANOTHER, and the understatement is the important one.** Wrong: the two arms I described were the SAME arm — a tsconfig with `types:["node"]` and no explicit `lib` **still resolves `localStorage` through lib.dom**, so both of my configurations gave `functions: []`. The `@types/node` spelling is reached only by a tsconfig naming `lib` WITHOUT `DOM`. Understated: **lib.dom is the DEFAULT `lib` for any ES target — so the silently-pure half was the DEFAULT configuration and the correct half was the narrow one**, the reverse of how I filed it. (Also `setItem` yields `native:…storage.setItem`, not `.getItem`.) **FIXED with one es-lib branch keyed on the resolved declaration's PARENT (`Storage`), not on identifier text** — converging on the `@types/node` arm's answer, covering the whole interface rather than a verb list. `sessionStorage` falls out of the same predicate, measured not assumed. Coordinator-verified under the default configuration: `stash` and `stashSession` both `['Unknown']` + `native:Storage.setItem`, `deny Unknown` 0 → 1, while `Math.random`/`Date.now` still correctly charge `Rand`/`Clock` — the precision control holding. **THE KEYING DECISION PAID OFF ON REAL CODE:** an identifier-text key would have reported all three of `this.sessionStore.getItem(k)` (@supabase/phoenix), `let storage = self.localStorage; storage.setItem(…)` (redux-persist) and a storage FACTORY's return value (jotai) as pure. A/B over **194 npm packages** with deps installed, keyed wide: **ADDED 5, REMOVED 0, CHANGED 41**, with **113 hits across 8 packages** — not safety-only — and **all 46 moved rows audited in full** (24 direct, 22 transitive proven by calls-closure after a first pass classified some on a name suffix and was re-run strictly), zero fabrications. Revert test: 30 of 41 checks red; the 11 invariant ones say so in their own text, **including the `@types/node` convergence control, which exists because "make both configs agree" is also satisfiable by breaking the correct one.** **The sweep was answered MECHANICALLY, not sampled:** over all 16 `@types/node/web-globals` files, `storage` is the ONLY member that fails closed in this direction. **Near-miss disclosed by the fixer:** `git checkout HEAD~1 -- scan.mjs` for the revert test staged the revert, and its first `--amend` committed a `scan.mjs` WITHOUT the fix while the message described it — **caught only because it printed `git status --porcelain` instead of trusting an echo**, then re-ran every gate from an independent worktree of the final commit. 1825 tests (from 1784). |
| R110 ts: `performance.now()` is `Clock` under lib.dom and SILENT under `@types/node` — R109's split, opposite direction | 2026-09-02 | **CLOSED — candor-ts `3ac9813`. Found by the R109 fixer while sweeping for siblings** | **SILENT UNDER-REPORT, ts, and the mirror image of R109.** `performance.now()` correctly charges `Clock` when resolved through lib.dom, and reports **nothing** when resolved through `@types/node`, because `web-globals/performance` is marked reviewed-pure module-wide. Both fixtures compile; the `@types/node` side is the under-report. **This is the same tsconfig-selects-the-answer class as R109 but pointing the other way** — there, the default config was wrong and the narrow one right; here the narrow config is wrong. Together they establish the class properly: **the two resolution paths disagree in BOTH directions, so neither can be treated as the trusted one.** | Not fixed, and the reason is a control, not timidity: fixing it in R109's commit **would have violated that fix's own Control 1** (the `@types/node` spelling must stay byte-identical) and made its 194-package A/B unattributable — two behaviour changes in one measurement. One-line shape proposed by the finder: a κ rule `web-globals/performance` → `/^(now\|timeOrigin)$/` → `Clock`, mirroring the existing crypto rule. **Ruled out properly rather than left vague:** `navigator.sendBeacon` and `navigator.clipboard` do NOT share this shape — they do not exist on Node's `Navigator`, so the fixture will not compile, which is the §E3 test applied before claiming a sibling. **THE FIX SHAPE I PROPOSED WOULD NEVER HAVE FIRED, and the agent found that by printing the resolved declaration file for BOTH arms before writing any code.** `web-globals/performance` declares `var performance` and nothing callable, so `now` resolves into `perf_hooks.d.ts` — the real cause was **`perf_hooks` sitting module-wide reviewed-pure in the node-core floor under the justification "(in-process timing)", which conflates "does not leave the process" with "reads no clock"** (SPEC §1's `Clock` is *reading wall-clock or monotonic time*). **AND THE PRECEDENT I TOLD IT TO MIRROR IS ITSELF DEAD:** the `web-globals/crypto` κ rule does not fire for the members it names on @types/node 25.9.2 — they resolve into `crypto.d.ts` and a different rule charges them — while its comment claimed to "cover the node spelling". Kept as belt-and-braces, comment corrected. **The arm that was wrong is the OFFICIAL Node base config** — `@tsconfig/node20`/`node22` both set `lib` without `DOM`, fetched and read rather than assumed. Fixed with `[/^(node:)?perf_hooks$/, /^now$/, "Clock"]` — **a verb list of one, the OPPOSITE call from R109's whole-interface key, and the reason is the interface not preference**: every `Storage` member touches the store so a verb list could only under-report, whereas `Performance` is genuinely mixed (`clearMarks`, `getEntries*`, `toJSON`, `addEventListener` and three more read no clock) and widening would **re-open this divergence in the opposite direction**, since lib.dom charges `now` and nothing else. **A THIRD UNDER-REPORT FELL OUT, AND IT IS THE MOST IMPORTANT PART: `import { performance } from "node:perf_hooks"` was silently pure under BOTH configs — invisible precisely BECAUSE the two arms agreed.** Coordinator-verified: under a `@tsconfig/node` shape both the global and the explicit-import spellings now charge `Clock`, `deny Clock` exit 1, and a pure control stays absent. **R109's corpus is SAFETY-ONLY here and the agent said so unprompted** — 0 hits over 20,871 rows, because candor-ts's default for a bare directory is `lib.es2022.full.d.ts`, which INCLUDES lib.dom. The recall arm (same 194 packages under a Node-shaped tsconfig) gives **ADDED 2, REMOVED 0, CHANGED 18** over 30,989 rows with **15 κ-matches across 3 packages**, counter calibrated on a 2-call fixture first; **all 20 moved rows audited in full** — `lru-cache 10.4.3`'s `getNow` was **absent from the report, i.e. certified pure**, and `deny Clock <scope>` on it goes **exit 0 → 1**. Revert: 12 of 35 red. **The agent also caught its own broken instrument** — a `grep -c` confirming the revert matched nothing in EITHER state; re-verified with `cmp` and by the 12 reds. |
| R111 ts: `performance.mark`/`measure`/`eventLoopUtilization`/`timerify` read the clock and are silent in BOTH arms | 2026-09-02 | **CLOSED — candor-ts `8c7484e`. Was measured and pinned by fixture before being fixed** | **SILENT UNDER-REPORT, ts, and SYMMETRIC — this is not a resolution-path divergence like R109/R110 but a member-coverage gap present under every configuration.** `performance.mark("m").startTime` returns the live elapsed value (EXECUTED), and `mark`, `measure`, `eventLoopUtilization` and `timerify` all read the clock while reporting nothing under lib.dom AND under `@types/node`. **It is invisible for the same reason R110's third under-report was: the two paths AGREE, so nothing contradicts anything.** | Not fixed, and the reason is a control rather than caution: User Timing is pervasive in tracing and benchmarking code, so widening needs its own over-charge measurement, and folding it into R110's commit would have made that A/B unattributable — the same discipline that kept R110 out of R109. Two fixtures pin the current (wrong) answer so it cannot move by accident. **THE STANDING CONCLUSION FROM THE R109/R110/R111 TRIPLE, and it is worth more than any of the three: the two tables that answer \"is this web global effect-bearing?\" — `scan.mjs`'s es-lib `parent === X` arm and `scan-core.mjs`'s κ/floor keyed on `declModule` — have now disagreed in BOTH directions and AGREED while both were wrong. So agreement between the two resolution paths is worthless as a signal, and no future reasoning may treat either as the trusted one.** A costed reconciliation was proposed and deliberately NOT built: a single source of truth keyed on the resolved declaration's INTERFACE name regardless of declaring file, ~1 day, covering the es-lib arm's ~8 `parent ===` branches (`Storage`, `Crypto`, `Clipboard`, `Navigator`, `XMLHttpRequest`, `Performance`, `DateConstructor`, `Math`) whose node-side twins live in three different tables — **a partial unification, not a clean one, because the es-lib arm carries locator/`incomplete` logic (XHR argument 1, `sendBeacon` argument 0) that κ has no place for.** Also unswept: the remaining es-lib `parent ===` arms for the same twin-drift, and the OPFS/IndexedDB/`caches` surface R109 named. **MY FOUR-MEMBER LIST WAS THREE. `timerify` does NOT read the clock** — measured under node 22.12.0, it returns a NEW WRAPPER (`t !== fn`, two calls give two objects) and the read happens when the WRAPPER runs, observed as a `PerformanceObserver` entry with its own `startTime`. **Charging it would have fabricated**, and the hole it leaves (`const t = performance.timerify(f); t(1)` is silent) is a **factory-returning-an-effectful-callable** class, filed separately rather than folded in. Two further exclusions measured rather than assumed: `toJSON()` returns a **live reference** to `nodeTiming`, so the read is the later `.duration` access; and **`timeOrigin`/`nodeTiming`/`PerformanceEntry.startTime` are PROPERTY ACCESSES, which the classify site (`isCallExpression \|\| isNewExpression`) cannot see at all — no regex in either table can reach them.** That is a different fact from "judged pure" and is a lead, not a purity ruling. Coordinator-verified: `mark`/`measure`/`eventLoopUtilization` charge `Clock`; `timerify` and `clearMarks` correctly stay pure. **THE DEGREE OF FREEDOM BEHIND R109/R110/R111 IS GONE FOR THIS INTERFACE: the member set is now ONE exported constant read by BOTH tables, asserted by IDENTITY (`vre === CONST`) rather than by two regexes that happen to spell the same thing today.** This is NOT the reconciliation — the other ~8 `parent ===` arms still have separate node-side twins — but it sharpens that estimate: the shared-constant move cost ~20 minutes for one arm, so the ~1 day is mostly the arms carrying locator/`incomplete` logic, not plumbing. **THE OVER-CHARGE FEAR WAS ANSWERED BY MEASUREMENT, NOT ARGUMENT:** both 194-package corpora are byte-identical **and SAFETY-ONLY, recorded as such at the time** — grep on package source finds 0 call sites of the new members, with `performance.now(` in 11 files as the positive control. So a **371-package recall corpus** was built for User Timing: **0.28% and 0.21% of rows move, 3 packages, REMOVED 0**, all **268** moved rows audited in full (19 direct, 14 direct-but-already-`Clock` and visible only on the wide key, 104 transitive — every transitive one proven by closure over recorded `calls` edges, 0 unproven, 0 fabrications). **`eventLoopUtilization` had ZERO real call sites across all 565 packages — every match was JSDoc — so it got its own recall arm: `@fastify/under-pressure`'s `updateEventLoopUtilization` went from ABSENT (certified pure) to `['Clock']`, scoped `deny Clock` 0 → 1.** Revert: 22 of 53 red, 31 invariant by design — **and the revert test caught a gate of the agent's own that could not fail**, a lib.dom `deny Clock` check sitting on a tree that also contained `performance.now()`, so it exited 1 even with the fix reverted. R110's two pinned-gap fixtures were FLIPPED, not deleted. 1911 tests (from 1860). **Residual filed, pre-existing:** marky's `var perf = typeof performance !== 'undefined' && performance` gives a union receiver that does not resolve under candor's default bare-directory config — fail-closed `Unknown`, unchanged — while the node-shaped arm now reads `Clock`; a config-selected difference in what a scoped `deny Clock` sees. |
| R112 rust: the PROGRAM oracle reports "no driver was falsifiable" instead of failing when its scanner cannot run | 2026-09-02 | **CLOSED — candor-rust `1fc6c27`, unpushed. The ROW'S PLAIN-ENGLISH GLOSS WAS WRONG and the agent corrected it; my brief's requested exit code was also wrong and it refused, rightly** | **THE INSTRUMENT-THAT-CANNOT-FAIL CLASS AGAIN, and this one was predicted.** R108 established that `soundness/realworld/run.sh` honours `CARGO_TARGET_DIR` for the BUILD but hardcodes `$ROOT/target/debug` for the SCANNER. Pointed at a container-local target dir, it therefore **silently ran a Mach-O binary under Linux** — and rather than failing, it reported *"no driver was falsifiable this run"* and exited 0. **An empty result where a broken instrument should be a red.** This is the same shape as R104 (the oracle passing over its own build failure) and R108 (an absence control crediting a dead run), now in the third harness of the night. | Not fixed. R104 already made `BUILD FAILED`/`NO BINARY`/`NO CANDOR REPORT` red in both oracles behind a loud retry — **"the scanner exists but cannot execute here" is the gap that class did not cover**, because the binary is present and the failure is architectural. The minimal repair is the one R102's agent named and deliberately did not take (a second behaviour change to a live CI gate in one push): make `run.sh` honour `CARGO_TARGET_DIR` for the scanner path as `run_pf.sh` already does, **and treat "zero falsifiable drivers" as a hard failure rather than a result** — a calibration that adjudicates nothing has not passed. **Worth noting the R99 agent avoided the trap entirely** by running the container against a read-only bind mount of a container-local copy, and verified afterwards that the host `target/{debug,release}/candor-scan` were still Mach-O — the discipline that would have prevented my own R108 misdiagnosis. **FIXED.** The row's gloss ('a machine that has only built `--release`') corresponds to no reachable state: `soundness/realworld/run.sh` unconditionally runs its own debug `cargo +stable build` every invocation, so a prior release-only build never blocks it. The real, code-verified mechanism is `CARGO_TARGET_DIR` — the build honours it, the hardcoded `SCAN="$ROOT/target/debug/candor-scan"` did not (the R108 shape). Now both use `${CARGO_TARGET_DIR:-$ROOT/target}`, mirroring `pf/run_pf.sh`'s already-correct form, plus an existence check. **I briefed exit 3 (SELFSKIP) and the agent refused, correctly: in this family exit 3 means the ENVIRONMENT PRECONDITION is unmet, a benign expected skip; a build that reports success and then leaves no binary is the script contradicting ITSELF and must fail loudly (exit 1), the same class as the untouched `FAIL: candor-scan build` two lines above.** Coding it as a skip would misclassify a real bug as benign — the very failure this row exists to close. Arms: mocked build producing nothing → exit 1 naming the resolved path; real build with `CARGO_TARGET_DIR` redirected → exit 0, binary confirmed `candor-scan 0.35.0 (spec 0.35)`; **pre-fix contrast — a stale FAKE binary planted at the old hardcoded path ran silently with zero warning**, which is the danger closed. The 'a real finding must still FAIL' arm is INFERRED from code identity (the loop, verdict and exit composition are byte-identical pre and post), NOT measured: a Docker Linux+strace run was attempted and Docker's content store errored under the session's resource pressure. Watch it on `realworld-oracle.yml`. |
| R113 ts: `localStorage.x = secret` — the INDEX-SIGNATURE write — persists to disk and is pure in BOTH arms | 2026-09-02 | **CLOSED — candor-ts `31ce3a2`. Verified by the coordinator against a build at `5377eb6`. THE ROW UNDERSTATED IT: 17 of 18 spellings were silent, not 4** | **CARDINAL SIN, ts, and the fourth instance of the both-arms-agree failure mode.** Node 22.12.0 with `--experimental-webstorage --localstorage-file=./ls.db`: `localStorage.x = "via-index-signature"` **persists across processes, identically to the `setItem` call that IS charged**. Measured four ways, all pure: `localStorage.x = secret`, `localStorage["tok"] = secret`, `return localStorage.tok`, and the same under `lib:["ES2022"]` only — while `localStorage.setItem(…)` correctly reports `['Unknown']`, **which is the instrument proving itself able to fail.** Cause: the two curated tables sit under `ts.isCallExpression(node) \|\| ts.isNewExpression(node)`, and the separate property-access site (`scan.mjs:6803`) resolves USER-declared `get`/`set` accessors — sound for those across element access, optional chaining, destructuring, inheritance and compound assignment, all 18 shapes measured — but **a host property declared as a data member in `lib.dom` carries no accessor for it to resolve.** **THIS IS THE ROW R109'S COMMENT DISMISSED:** *"the `localStorage.x = v` INDEX-SIGNATURE spelling is untouched and still pure — pure under @types/node too, **so it is not part of this split**."* Every literal word is true and both arms really are pure. **The CONCLUSION is what R111 exists to refute: both arms agreeing is not a safety property, it is the third failure mode** — R109 was lib.dom-silent, R110 node-silent, R111 both-silent-together, and this is a fourth instance of the third, sitting inside the comment that waved it away. Recall: `debug`'s README publishes `localStorage.debug = 'worker:*'` as its primary browser configuration API, and `util-deprecate/browser.js:64` does `global.localStorage[name]`. | Not yet fixed. **The identity is available and is the SAME one the call arm already keys on:** the probe resolves the receiver to `NOSYM\|Storage\|lib.dom` — no symbol, because it is an index signature — but `checker.getTypeAtLocation(node.expression).symbol.name === "Storage"` with a `typescript/lib/lib.*.d.ts` declaration reaches it **from the receiver instead of the member**. R109's whole-interface denylist argument transfers verbatim. **Writes fail with reads, for one reason:** `document.cookie = v` and `location.href = url` are equally invisible (**ANALYSIS-ONLY — no browser available, and the agent kept them out of its executed list rather than upgrading them**). **RE-MEASURED 2026-09-04 on `npx candor-ts@0.35.0`: STILL OPEN, SILENT, in BOTH lib configurations** — `viaIndexAssign`/`viaBracketAssign`/`viaIndexRead` absent from `functions[]` under `lib:[ES2022,DOM]` AND under `types:[node]`, while the control `viaSetItem` is correctly `['Unknown']` in both. `deny Unknown viaIndexAssign` exit 0, control exit 1. Ground truth executed under `node --experimental-webstorage`: the write PERSISTS ACROSS PROCESSES. Fixtures `scratchpad/stale-engines/``ts/r113/{dom,node,exec}`. **Closed, and the row's own count was low.** `R109` keyed on the resolved MEMBER; `Storage` also declares `[name: string]: any` (identically in `lib.dom` and `@types/node`), so `localStorage.x = secret` resolves to no declaration and there is no accessor to find. Executed on node 22.12.0 `--experimental-webstorage --localstorage-file=./ls.db`, with a SEPARATE process reading both values back: **of 18 spellings that reach the store, ONE was charged (`setItem` — the instrument proving it can fail) and 17 were absent.** `deny Unknown` exit 0 -> 1. The fix keys on the RECEIVER's type symbol and mirrors the six shapes the `process.env` block already enumerates rather than writing a second list, so alias / `window.localStorage` / `sessionStorage` / a `Storage`-typed parameter come free. **Over-charge control is a measurement, not an absence:** over 12 real TS trees / 2,095 rows the predicate ran **29,883 times and charged zero**, byte-identical output, plus shadow controls for a project's own `interface Storage`/`class Storage`. Corpus A/B: npm 21,736 rows ADDED 6 / REMOVED 0 / CHANGED 30; every changed row is in a storage-touching package and each was ground-truthed from package source (`util-deprecate` `global.localStorage[name]` — the exact line this row cited, where BOTH `config` and `deprecate` were absent; `localforage`'s `'setItem' in localStorage` and `localStorage.length > 0`). Revert-red: 58 rows. |
| R114 ts: `process.uptime()` and `os.uptime()` are monotonic clocks reviewed pure BY NAME | 2026-09-02 | **CLOSED — candor-ts `75ee578`. R110/R111's boundary drawn around its own trigger (§9)** | **SILENT UNDER-REPORT, ts, and the cheapest fix in this vein because these are CALLS — the existing κ mechanism already reaches them.** R110 established the criterion in its own comment: *"SPEC §1 defines `Clock` as reading wall-clock or monotonic time; staying in-process is not the question."* Applied one module over, that criterion is violated by two entries in `NODE_CORE_REVIEWED`: **EXECUTED — `process.uptime()` over a 60 ms busy-wait returns a delta of 59.94 ms; `os.uptime()` advances 1 s over a 1.2 s wait.** Both are reviewed pure by name (`scan-core.mjs:596` puts `uptime` in the `process` floor regex; line 530 lists it among `os`'s "platform/arch/cpus/freemem/uptime/EOL"), while `process.hrtime.bigint()`, `Date.now()`, `new Date().getTime()` and `performance.now()` are all correctly `Clock`. | Not yet fixed. **The argument is already written down in R110's own commit and simply was not applied beyond the module it was about** — the boundary was drawn around the trigger, which is this family's most-repeated failure and is §9's whole subject. No new mechanism is needed: these are call sites, so the existing κ rule reaches them, and R111 has already made the `Performance` member set one shared constant asserted by identity — **the same treatment is what stops this recurring a fourth time.** **FIXED, and the sweep past the trigger found THREE MORE that this row never named.** (1) **`process.hrtime()`'s tuple form** — my "no new machinery" was wrong: it is a CALL SIGNATURE on `HRTime`, so κ was asked about the empty token `""` and the floor reviewed that as pure; closed by a strictly-additive retry that re-asks with the property name when the first lookup answers nothing on an empty member, which cannot move a call off an existing effect. (2) **`process.hrtime.bigint()` was SILENT via a MODULE IMPORT** — my row called it correctly `Clock`, true only through the global. A live two-answer disagreement: `scan.mjs`'s global arm charged it while `scan-core.mjs`'s floor listed `hrtime`/`bigint` as reviewed-pure, **under a comment asserting purity *"for the reason `Date.now()` is pure in this engine: nothing charges Clock for reading a timer"* — false in BOTH halves.** (3) **`console.time`/`timeLog`/`timeEnd`**: executed, two timers started 80 ms apart end 80 ms apart — `console.time()` itself takes the timestamp, and SPEC §1's console exemption is about WRITES, not the monotonic read. **The exclusions were each executed and deliberately left, on a stated criterion — "moving is not the criterion; reading TIME is":** `os.freemem` (7912144896 → 7736311808 over 300 ms), `totalmem`, `loadavg`, `cpus()[].times`, `process.cpuUsage`/`resourceUsage`/`memoryUsage`/`constrainedMemory`/`availableMemory`. `os.platform`/`arch`/`EOL`/`machine`/`getPriority` and `process.pid` are the precision control in both arms. **R111's shape applied to the pair that had actually drifted:** `CLOCK_READING_PROCESS_MEMBERS` and `CLOCK_READING_CONSOLE_MEMBERS` are shared constants asserted by identity, and `uptime`/`hrtime`/`bigint` were **REMOVED from the floor list rather than left inert**. A/B isolated against the R115 image: 0/0/0 over 20,777 rows — **and the zero is EXPLAINED, not trusted: 18 counted branch hits, every one on a function already carrying `Clock` from another read in the same body, and the corpus has ZERO `uptime` call sites, so for that half it is safety-only and the commit says so.** |
| R115 ts: `{...instance}` charges a class PROTOTYPE getter that never runs — a fabrication that FAILS a gate | 2026-09-02 | **CLOSED — candor-ts `e5c60bc`. PRE-EXISTING; the OPPOSITE direction from R113/R114, at the same site** | **FABRICATION, ts, and the only finding of this vein that makes candor fail a gate on code that does nothing.** `enumerateGetters` (`scan.mjs:4305`) is called from `{...obj}`, `Object.assign(t, obj)` and `const {...rest} = obj`. **Its own comment states the correct rule** — *"copying an object's **own enumerable** props INVOKES each source getter"* — **and the body does not apply it**: `type.getProperties()` returns inherited prototype accessors, and a class accessor is on the prototype and non-enumerable, so spread never copies it. EXECUTED, counting invocations: `{...c}` → **0**, `Object.assign({},c)` → **0**, `JSON.stringify(c)` → 0, `Object.entries(c)` → 0, while `const {tok}=c` → 1 and `c.tok` → 1 and `{...objectLiteral}` → 1 (the case the mechanism was written for). Isolated, `tsc`-clean: `export function clone(s: Session) { return { ...s }; }` over a class whose getter reads `/etc/token` gives candor `['Fs']` and **`deny Fs` exit 1 — `[AS-EFF-006] src.a.clone performs { Fs }` — while the executed program shows `readFileSync_calls: 0`.** The `JSON.stringify`/`Object.entries` purity is CORRECT, not a gap; the mechanism is wrong only in the direction it does fire. | Not yet fixed. **Narrowing direction per [[candor-denylist-over-allowlist]]: exclude only accessors whose declaration parent is a `ClassDeclaration`/`ClassExpression`** — provably prototype — and leave interface and type-literal accessors charged, since the runtime object could be either. **PRICE HONESTLY UNMEASURED and said so:** the agent instrumented the branch, calibrated it (2/2 hits on its own fixture, both class-prototype), ran it over 6 real repos and got **zero hits** — so this corpus cannot reach the branch and real-world frequency is unknown; the finding is a recall-hunt construction. **Two smaller siblings reported at the same site:** `c[k]` with a dynamic key over a receiver whose type carries an effectful accessor reads PURE, while the `Object.defineProperty` analogue already takes the opposite posture (`Unknown[reflect:defineProperty:dynamic-key]`) — two implementations of one question, drifted (§F1-3); and an object-literal getter read hedges `Unknown[reflect:accessor:?]` rather than edging to a visible local unit — fail-closed, so a precision cost rather than a sin. **FIXED, and my proposed predicate was wrong: `find` over the get-declarations would have failed** — a union or mapped property symbol carries declarations from EVERY constituent, so it must be `every`. Measured: `Session \| { get token(): string }` keeps its charge only under `every`. **Three spellings this row never named were also fabricating and fall to the same predicate:** an inherited base-class getter, a class EXPRESSION, and a **static accessor on the constructor** (own but non-enumerable — descriptor printed, executed 0). **The narrowing opens a real hole and it was pinned rather than asserted away:** TypeScript is structural, so `const s: Session = { get token(){…} }; {...s}` DOES invoke the getter (executed 1). Where the source is a plain binding the literal's accessors are now enumerated directly; **where it arrives via a PARAMETER there is no initializer, and that residual is pinned by a fixture asserting today's WRONG answer** — pre-fix that spelling was charged only by coincidence, the class getter's effects standing in for the literal's. **THREE INSTRUMENTS HELD THE SAME WRONG PREMISE:** the implementation, a unit test (*"the copier inherits the precise Exec"*, now flipped with its fixture untouched), and **`fuzz.mjs`'s `obj_spread` GENERATOR**, which emitted a class and asserted the caller must not be omitted. **`node test.mjs` was green throughout — only the full `npm test` (fuzz) caught the third**, and the generator's teeth were re-verified by reintroducing the hole (seeds 14/15/22 go red). A/B over **25 real TypeScript-source repos** — the 371-package corpus I cited **does not exist**, and a `.d.ts`-only corpus cannot reach a branch that needs a spread in a real body — keyed wide: ADDED 0, **REMOVED 1**, CHANGED 6, 0 on `inferred`, **290 branch hits (got 272, nest 17, typeorm 1) so NOT safety-only.** The single removal, `got`'s `Options.toJSON`, ground-truthed **from source**: `#internals` is a mapped type carrying the class getters' declarations over a plain literal built by `cloneInternals` — **48 fabricated edges, and the function really is pure.** Revert: 11 red, 12 invariant by design, **and it caught a dead assertion of the agent's own** ("something was charged", which the pre-fix engine also satisfies; now asserts the reason). |
| R116 ts: `Object.assign(target, src)` invokes the TARGET's setters and charges nothing | 2026-09-02 | **CLOSED — candor-ts `87d0388`. Verified by the coordinator. THE FIX SHAPE THIS ROW PROPOSED WAS UNSOUND AND WAS REFUSED** | **CARDINAL SIN, ts, found by widening an audit boundary past its own trigger (§9) while fixing R115.** `enumerateGetters` handles the SOURCE's getters; **the target-setter arm does not exist.** Coordinator-verified, EXECUTED (the setter runs once and the file is written): `export class Sink { set token(x: number) { fs.writeFileSync("/tmp/r116-leak", String(x)); } }` with `Object.assign(s, { token: 1 })` → **`viaAssign` ABSENT from `functions[]`**, while the named-write control `s.token = 2` in the same file is correctly `['Fs']`. **So this is one spelling of a question the engine already answers correctly** — which is what makes it precise rather than a coverage gap. Note the scoped `deny Fs <file>` catches it only INCIDENTALLY via the named sibling; a file containing only the `Object.assign` spelling passes. Also measured: `{...c, token: 1}` invokes **0** setters, correctly, because spread builds a fresh object. | Not yet fixed, and it is a row of its own rather than a residual **because charging target setters can FABRICATE and therefore needs its own over-charge control and A/B** — exactly the discipline that made R115's narrowing safe. Sound direction proposed by the finder: for each `Object.assign` source, take the source type's property names and look up `set` accessors on the TARGET type; charge all target setters when a source type yields no resolvable properties. **R115's lesson applies directly to whoever takes this:** its predicate needed `every` rather than `find` over declarations because a union or mapped symbol carries declarations from every constituent, and three instruments — implementation, unit test, and the fuzz GENERATOR — all encoded the same wrong premise, with only the fuzz arm catching the third. **RE-MEASURED 2026-09-04 on shipped 0.35.0: STILL OPEN, SILENT.** `viaAssign` absent; the named-write control is correctly `['Fs']` and the spread control correctly absent (no over-charge). Ground truth executed: the setter really runs and the file is written. **The row's worst claim confirmed:** a file containing ONLY the `Object.assign` spelling (`scratchpad/stale-engines/``ts/r116/onlyassign`) gets `deny Fs` exit 0 and a clean bill — nothing else in the report carries the effect. Fixtures `scratchpad/stale-engines/``ts/r116`. **Closed, and the row's proposed remedy would have introduced a silent under-report.** The row said to take the SOURCE type's property names. TypeScript is structural, so `f(s: {a:number})` can receive `{a:1, token:2}` and the extra key IS copied — a name list from the declared type misses it. The agent used a **denylist of the proven** instead: charge every target setter unless the key set is provable (a fresh object literal, no spread, no computed key). That is [[candor-denylist-over-allowlist]] applied correctly by someone told to attack the brief. **The denylist's value is measured, not argued: it fired 88 times on real code** (got's six `Reflect.set(target,'<literal>',v)` sites; `Options` has 46 setters), i.e. without it, 88 fabricated charges. Executed invocation counts make the precision explicit: 1 for every assign spelling and for `Reflect.set`; **0** for `{...s, token:1}`, **0** for a `value:` descriptor, **0** for a non-setter key. Widened sweep found `Reflect.set(t,k,v)` silent for the identical reason in both key spellings. A/B: npm 21,736 rows ADDED 0 / REMOVED 0 / CHANGED 3 (all `oidc-client-ts`, `unknownWhy` gaining `reflect:accessor:expires_in`, ground-truthed to a real setter calling `Timer.getEpochTime()`); no gate moved. Revert-red: 13 rows. |
| R117 swift: an ANNOTATED closure parameter's type outlives its closure | 2026-09-02 | **CLOSED — candor-swift `a615fc4`. REGRESSION introduced by unpushed `3c78535` (the R97 fix). Never published; found by a review panel over the unpushed diff before any push** | **CARDINAL SIN, swift only, and it is a fix introducing the class it closed — the third time this family has measured that shape.** `CallCollector.swift:2430` added `for p in closureParamNames(node) where p.annotated != nil { vars[p.name] = p.annotated }`. Its own comment asserts *"the write lands INSIDE the closure's own save — the shadow above ran first — so it cannot leak past the closure."* **MEASURED FALSE, structurally and behaviourally.** Structurally: `ShadowSave` (~:2223) holds `opaque`, `opaqueElem`, `depBound`, `protoTyped`, `constStrings`, `fnValueAlias`, `casePayload` — **`vars` is not among them**; `vars` is restored only via `typeScopes`, written only by `scopeBindingType` (~:2336/:2339), which line 2430 never calls; and `shadowName` (~:2159) does not touch `vars` either. Behaviourally, coordinator-measured, ground truth EXECUTED (`swift build && ./App` prints `file still exists after underReport() == false`, i.e. the file really is deleted): `func underReport(_ s: Shouty)` declaring `let c: (Quiet) -> Void = { (s: Quiet) in _ = s }` and then calling `s.act()` on the OUTER `s` reads **`['Fs']` at published baseline `e5c7871` and ABSENT at HEAD `795a39f`**. **TWO controls, each varying exactly one thing, unchanged in BOTH binaries:** `ctlNoAnnotation` (annotation removed) `['Fs']`/`['Fs']`, and `control` (binder renamed `z`) `['Fs']`/`['Fs']`. Gate matrix at HEAD: `deny Fs underReport` **0**, `deny Fs Unknown underReport` **0**, `pure underReport` **0**, and **`deny Unknown` 0 — no disclosure at all, so this is SILENT, not coarse.** Blanket `deny Fs` exits 1 only INCIDENTALLY, because the two controls share the file; the reviewer's isolated single-function fixture has `functions[]` empty and every policy form at 0. | Not yet fixed; a fix agent owns candor-swift. **The structural finding is worth more than the bug: `vars` is not in `ShadowSave`, so ANY raw `vars[...] =` inside a scoped visitor leaks BY CONSTRUCTION** — R97 added one, and the audit must sweep every `vars[` write site rather than the one instance (CLAUDE.md §9). **Measured and explicitly NOT part of this regression:** the identical raw write in `typeClosureParams` (~:2717) for closures in CALL-ARGUMENT position leaks in BOTH binaries — PRE-EXISTING and published, filed as a separate question, not folded into this fix. **A near-miss in my own verification, recorded because it produced a false result first:** my initial gate matrices were run with `exit=$?` on a line also containing `$(printf ...)`, so every status I read was printf's, not candor's — the matrix showed blanket `deny Fs` passing over four violating units. Caught only because that result was not credible. This is the documented hard rule (never read `$?` after a pipe OR after a same-line command substitution) and I broke it twice in the session whose subject is instruments that cannot fail. **A second self-inflicted error, same verification:** my first fixture named the control `underReportUnannotated`, which is a PREFIX of the scope `underReport`, so `deny Fs underReport` matched the control and exited 1 — a scope-match artefact that would have masked the finding. Renamed to `ctlNoAnnotation` before the matrix above was taken. **CLOSED as one of NINE binder forms — see R124, which is the class this instance belongs to.** Coordinator-verified on my own fixture at the fixed HEAD: `underReport` ABSENT -> `['Fs']`, both controls unchanged. |
| R118 swift: the VariableDecl `.skipChildren` rewrite stopped walking `node.attributes` | 2026-09-02 | **CLOSED — candor-swift `a615fc4`. REGRESSION introduced by unpushed `3c78535` (the R98 fix). Never published** | **CARDINAL SIN, swift only, and the same commit as R117 — one commit, two introduced sins, against three closed.** `visit(VariableDeclSyntax)` (~:4825-4831) was rewritten to walk initializers first and then hand-enumerate `binding.pattern` / `binding.typeAnnotation` / `binding.accessorBlock`, returning `.skipChildren`. **`node.attributes` is not in that enumeration, and the `.visitChildren` it replaced walked it** — so the ARGUMENTS of a property-wrapper attribute on a variable declaration are never visited at all. Coordinator-measured, ground truth EXECUTED (prints `attrLocal deleted the file == true`): with `@propertyWrapper struct Tagged` and an effectful `effTag()`, `func attrLocal() { @Tagged(effTag()) var n: Int = 1; _ = n }` reads **`['Fs']` at `e5c7871` and ABSENT at HEAD**. Control `ctlInitializer` — the identical `effTag()` call in the INITIALIZER position of a local `let` — reads `['Fs']` in BOTH binaries, so the only variable is the attribute position. Gates at HEAD: `deny Fs attrLocal` **0**, `pure attrLocal` **0**, `deny Unknown attrLocal` **0** (silent again); `deny Fs ctlInitializer` 1. | Not yet fixed; a fix agent owns candor-swift. **The audit must not stop at this hand-enumeration.** Every `.skipChildren` that hand-lists children is the same shape: the list is a claim that nothing else carries an expression, and it goes stale silently when the grammar or the visitor changes. TAKEN ON REPORT, NOT MEASURED BY ME: a reviewer checked the matching `visit(ForStmtSyntax)` rewrite and found it omits only `node.typeAnnotation`, which carries no expression — no effect. That is the only sibling anyone has checked. **INFERRED, not measured:** a TYPE-LEVEL stored property's wrapper argument is reported unmodelled in both binaries, so no regression there — I did not verify this. **CLOSED — see R124.** Coordinator-verified on my own fixture: `attrLocal` ABSENT -> `['Fs']`, `ctlInitializer` unchanged. The `.skipChildren` audit was completed against SwiftSyntax's GENERATED child lists rather than from memory: `visit(VariableDeclSyntax)` now walks everything but `modifiers`/`bindingSpecifier`/`trailingComma` (tokens only); `visit(ForStmtSyntax)` omits only `typeAnnotation`, consumed explicitly at :2500, and **no visitor in the file keys on a type node**, so it is inert; `visit(DeclReferenceExprSyntax)`'s four returns omit only label tokens. |
| R119 rust: an item declared in a NESTED BLOCK rebinds its name for the WHOLE function | 2026-09-02 | **CLOSED — candor-rust `663aa96`. REGRESSION introduced by unpushed `27ad9d1` (the R105/R106/R107 fix). Never published** | **CARDINAL SIN, rust only.** `src/decls.rs` ~:262, `body_declared_items`, walks the entire function body with `visit_block`, so an item declared inside a NESTED block rebinds that name to `ITEM_SENTINEL` for the whole function rather than for the block that declares it. A `use std::process::Command as Cmd` at file scope is therefore erased by an unrelated `struct Cmd` in any nested block, and the spawn it aliases disappears. Coordinator-measured, ground truth EXECUTED (`executed: spawned=true`): `pub fn spawn_it(p: &str)` calling `Cmd::new(p).status()` and containing `let _n = { struct Cmd { n: u32 } Cmd { n: 1 }.n };` reads **ABSENT**, while `spawn_ctl` — byte-identical except the nested item is named `Helper` — reads `['Exec']`. **The only variable is the nested item's NAME.** | Not yet fixed; a fix agent owns candor-rust. **STATED BOUND ON MY OWN EVIDENCE:** in my fixture blanket `deny Exec` exits 1, but only INCIDENTALLY — `spawn_ctl` shares the file and is independently reported. TAKEN ON REPORT, NOT MEASURED BY ME: the reviewer's isolated single-function fixture has `functions[]` **empty** and all four policy forms at exit 0. **The audit must not be drawn around nested blocks** (CLAUDE.md §9): the same flattening question applies to closures, `match` arms, `if let` bodies, loop bodies, `async` and `unsafe` blocks, and const/static initializers, and the fix agent has been asked to enumerate and test each rather than the one shape handed to it. **Separately filed, PRE-EXISTING and PUBLISHED if true, and NOT verified by me:** a reviewer reports that `use` items are collected with no `#[cfg(test)]` filter, so a production scan can resolve a call through a TEST MOCK when the mock import is written last, with tokio escaping only by line ordering. Handed to the agent as a hypothesis to confirm or refute, explicitly not folded into the R119 fix. **CLOSED AND COORDINATOR-VERIFIED.** Fix scopes an item's shadow to the block that declares it: a name declared directly in the body's statement list shadows the whole body; a nested name is promoted only when EVERY occurrence of it in the body already lies inside the one block that declares it, and otherwise the shadow is dropped — **over-report, never silence**. Ident counting deliberately includes macro and attribute token streams, because an UNDERCOUNT is the only direction that could wrongly license a function-wide shadow. **THE AUDIT WAS NOT SCOPED TO THE TRIGGER I HANDED OVER, and that is what made it worth running.** Every block-introducing construct was built as a compiling, EXECUTING arm and measured against a pre-fix binary: **FIFTEEN were silent** — plain `{}`, `if`, `else`, match arm, `if let`, `while let`, `loop`, `for`, `while`, closure body, `async` block, `unsafe` block, `const` initialiser, `static` initialiser, labelled `'a: {}`. Three were already safe (nested `fn`, `impl`, inline `mod`) and are pinned as arms of the same test rather than assumed. **A SIXTEENTH POSITION IS NOT A NESTED BLOCK AT ALL** and was found by grepping the mechanism instead of re-reading the trigger: a parameter's type resolves where the function is DECLARED, so `fn f(c: &mut Cmd) { struct Cmd {..} ... c.status() ... }` also vanished — R106 fed ONE map to both `seed_vars` and the body walk. The signature now keeps the outer map. Coordinator-verified independently at the renumbered HEAD, ground truth EXECUTED: my original nested-block fixture returns `spawn_it` `['Exec']` (was ABSENT), the signature fixture returns `via_sig` `['Exec']`, and the published baseline `bffac6c` returns `['Exec']` for both — confirming the defect belonged to the wave and not to the shipped engine. Corpus A/B, 1489 registry crates, 252,396 common rows, 15 fields: **ADDED 0 REMOVED 0 CHANGED 1**, with §E1 hit counters proving the changed branch was REACHED (89 dropped shadows in 46 crates, 99 newly-reached names in 44, 11 signature-position names in 6) — **not a safety-only A/B**. The single changed row is `time` 0.3.55 regaining an `invisible: serde_core` disclosure, audited to source as the stated residual firing, `inferred` unchanged. Revert test RUN not reasoned: all three new tests fail on the pre-fix tree, each on its intended assertion. `cargo test --workspace` rc=0, 326+77. `bin/gate-run.sh candor-rust` 25 run / 25 ok; 10 UNRUN (5 provisioning SKIPs, 5 Linux+strace SELFSKIPs), reported as unrun. **COORDINATOR ERROR CORRECTED IN THE SAME BREATH:** the fixing commit and 12 in-tree references numbered this **R109**, which was already taken by the ts `localStorage` row. Renumbered to R119 across both commits and all 12 references before anything left the machine; the diff against the pre-renumber tag is 12 lines, every one containing the ID and nothing else. **And a staleness near-miss worth recording:** a bare `cargo build --release` at the workspace root builds only the `candor` dylib and NEVER `candor-scan`, so a stale scanner binary survives an apparently successful build. Caught because the pre-image binary was absent and the policy runs returned **exit 127**, which is indistinguishable from a violation if only the exit code is read. Build with `-p candor-scan`. **[Label corrected 2026-09-03 by the 0.35.0 release panel: `bffac6c` is NOT the published 0.34.0 — the v0.34.0 tag is `22b5ec2`, six commits earlier; where this row says 'published 3cf055d' read 'post-tag 3cf055d'. The row's conclusion was re-checked against the true tag build where the panel could, and stands.]** |
| R120 ts: the structural-literal arm is narrower than its own comment claims | 2026-09-02 | **CLOSED — candor-ts `02cd0a5`. REGRESSION introduced by unpushed `e5c60bc` (the R115 fix). Never published** | **CARDINAL SIN, ts only, and it is §E2's exact class: the fix carries a comment asserting coverage it does not have.** The commit's comment claims the arm covers cross-module spellings; the reviewer reports it misses roughly five, **including the cross-module shape the comment names**. Coordinator-measured at HEAD: `a.clone` reads **ABSENT** with `deny Fs src.a.clone` exit **0**. (In the same fixture `b.viaMember` reads ABSENT and that one is CORRECT — the two must not be conflated.) | Not yet fixed; a fix agent owns candor-ts. **The count "roughly five spellings" is TAKEN ON REPORT and not measured by me** — the agent has been told to print the resolved declaration at the point the mechanism fires rather than reason about it statically, because two proposed fixes in this repo the previous night would never have fired and both were caught only by printing the resolved declaration first. **Standing constraint carried into the fix:** R109/R110/R111 established that this engine has TWO tables answering overlapping questions and that **agreement between the two paths is worthless as a signal**; if this fix touches a question both paths answer, both must be fixed or the omission stated. The full two-table reconciliation (~1 day, explicitly partial) remains a costed decision for Tom and was NOT authorised here. **CLOSED, and MY CHARACTERISATION OF IT WAS WRONG IN A WAY WORTH KEEPING.** I wrote that the comment claims coverage it does not have. It does not make a positive claim; it states the RESIDUAL as a closed list of two — *"what it does NOT reach is a structural literal arriving through a PARAMETER or a call return"* — **and the list has six members.** The false assertion is the COMPLETENESS of a residual, which is §E2 one step subtler than the shape I described and is harder to spot precisely because it reads as candour. The five missed spellings, found by printing the resolved declaration rather than reasoning: the alias hop, an `as`-cast initializer, a parenthesised initializer, a non-Identifier spread source, and `satisfies` — the last **also** missed by this arm but charged anyway through the TYPE path, and pinned as a control rather than banked as coverage, because R111 measured that agreement between two paths answering one question is worthless as a signal. Fixed with three unwraps and one `getAliasedSymbol` hop, all three `enumerateGetters` call sites (spread, `Object.assign` source, rest binding) — closing one would have read as covering the class. A pure widening: fails toward over-charge, never silence. Revert test: 7 red. **AND MY PARENTHETICAL WAS WRONG TOO:** I wrote that `b.viaMember` reading ABSENT is CORRECT. That holds only if the member is a CLASS INSTANCE — coordinator-verified by execution, a class-instance spread does NOT invoke a prototype getter (not own-enumerable) while a structural-literal spread DOES invoke it exactly once. Where the member holds a structural literal, ABSENT is a miss, and it is filed as an open gap rather than ruled correct. |
| R121 ts: a project-owned `fetch` shim is charged Net through one binding hop | 2026-09-02 | **CLOSED — candor-ts `02cd0a5`. REGRESSION introduced by unpushed `30fc8ea` (the R95 fix). Never published** | **FABRICATION, ts only — the opposite direction from R120, in the same wave.** A locally-defined `fetch` shim reached through a single binding hop is treated as the host global. Coordinator-measured at HEAD: `b.viaDefaultDestructure` reads **`['Net']`** and `deny Net src.b.viaDefaultDestructure` exits **1** over a unit that performs no network access — **a gate that fails on clean code**. | Not yet fixed; a fix agent owns candor-ts. **THE DANGEROUS DIRECTION IS THE FIX, NOT THE BUG.** This family has measured 4 defects in 5 fabrication-fixes, 2 of them cardinal sins: killing an over-charge is precisely where silent under-reports get introduced. The agent has been told to **write the second fixture FIRST** — the pinned control proving the REAL host `fetch` is still charged — before narrowing anything, and that narrowing a sound over-approximation is done with a DENYLIST and never an allowlist, stating which direction the fix fails in before writing it. **CLOSED — AND THE MECHANISM I HANDED THE AGENT WAS WRONG, WHICH CHANGED THE FIX.** I briefed it as *"a local shim named `fetch`, reached through a binding hop, is treated as the host global."* It is not. `getResolvedSignature` resolves the shim CORRECTLY — all four fabricating rows printed `RESOLVED_DECL=vendor/shim.ts:1`. **The arm never asked whether the declaration was the host's**; it tested four proxies, and a project's own OVERLOAD SIGNATURE satisfies three of them (bodyless, named `fetch`, returns `Promise<Response>`), with the fourth defeated by the hop. Taking my mechanism would have produced a fifth callee-keyed patch — the very thing the R95 commit's own comment warns against. This is the sixteenth-plus brief of this session corrected by measurement, and the agent was right to attack it. **THE GUARD WAS WRONG IN BOTH DIRECTIONS, and the second half was nobody's hypothesis.** Beyond the fabrication, `slot.fetch(u)` (with `slot` relatively imported and `slot = { fetch: globalThis.fetch }`) has head `slot`, so the guard rejected on an import that says nothing about which function is called: **the REAL host fetch, absent, `deny Net` exit 0.** Its sibling `const { fetch } = slot` was charged `Net` **in the same file** — one value, two answers, selected by punctuation. Fixed by replacing the head-identifier guard with a DECLARATION-keyed one, denylist direction stated before the code was written: the exclusion fires only for a declaration that is both module-scoped and in our own package. The `crossesPackageBoundary` escape is load-bearing — deleting it reds exactly one row (`import * as nf from "pkg"; const {fetch} = nf; fetch(u)` goes silent). Coordinator-verified independently: `viaDefaultDestructure` no longer charges `Net`; `viaSlotMember`, `viaSlotDestructure` and the `viaHostDirect` control all charge `Net`. Revert test: 6 red, escape deleted 1 red. Battery 2011 -> 2046, rc=0. **Stated widening, pinned not hidden:** a slot declared `typeof fetch` holding a local pure function is now `Net` in both spellings. Sound, imprecise, and now CONSISTENT — the disagreement was the real defect. |
| R122 rust: `#[cfg(any(test, feature = "x"))]` erases a PRODUCTION function | 2026-09-02 | **CLOSED — candor-rust `e391eef`. PRE-EXISTING and PUBLISHED. Reproduces byte-identically on `origin/main` `bffac6c` and at HEAD; NOT introduced by this wave** | **CARDINAL SIN, rust, live in the shipped engine, and the worst-shaped finding of this review round.** `is_cfg_test` fires on `#[cfg(any(test, feature = "x"))]`. `cfg_meta_requires_test`'s own doc says the item *"POSITIVELY requires test"* — correct for `all(test, X)` and **backwards for `any(test, X)`**, where the feature alone satisfies the predicate and the item is compiled into ordinary production builds. Coordinator-measured, ground truth EXECUTED (the process really spawns): a crate with `default = ["extra"]` and `#[cfg(any(test, feature = "extra"))] pub fn prod_under_any(..) { Command::new(p).status() }` reports **`functions: 0`**, `excluded: []`, no `unanalyzed` — **the report reads entirely clean** — and ISOLATED, **all five policy forms exit 0**: `deny Exec`, `deny Unknown`, `deny Exec Unknown`, `pure prod_under_any`, `deny Exec prod_under_any`. The control is the discriminator: the byte-identical body gated on `#[cfg(feature = "extra")]` ALONE reports `['Exec']`. **The only variable is the cfg predicate.** | Not yet fixed. **PREVALENCE MEASURED BY ME, and it is an ecosystem idiom rather than an edge case.** Over the 1489-crate registry corpus: 131 crates contain an `any(...)` cfg mentioning `test`, and **122 (8.2%) combine `test` with a `feature =` predicate** — the production-reachable subset. The commonest shapes are `any(feature = "alloc", test)` (91), `any(feature = "derive", test)` (78), `any(test, feature = "std")` (70) and `any(feature = "std", test)` (46) — **`std`, `alloc` and `derive` are default-on features across most of the ecosystem.** **MY COUNT DIFFERS FROM THE AGENT'S (122 vs its 102) and neither is wrong**: my regex is the broader `feature =` subset, and the narrower reading excludes shapes whose other predicate is not production-reachable (`any(test, miri)`, `any(test, doc)`) — worth pinning precisely before the advisory quotes a number. **Fix ORDER matters and is the agent's finding, not mine:** this must be fixed BEFORE the sibling row R123, because R123's obvious fix is unsound while this predicate is backwards. **CLOSED, and the fix CONVERGED rather than adding a mechanism (§G).** `is_cfg_test` recursed into `any` and `all` alike; the rule is now *"can this `#[cfg]` hold with `test` FALSE"*, implemented as **one Kleene fold (`cfg_fold`) shared with `cfg_eval`, differing only in the leaf** — the two hand-written copies of that fold are exactly what had drifted (§F1 q3). **A SECOND, ORDER-DEPENDENT HALF OF THE BUG was found by auditing the removals**: an unconsumed `= "…"` tail aborted syn's sibling iteration, so `all(feature = "std", test)` read as PRODUCTION. Both halves are closed by the one fold. Coordinator-verified at the fix on my isolated fixture: `prod_under_any` ABSENT -> `['Exec']`, `functions` 0 -> 2, and `deny Exec` / `pure prod_under_any` / `deny Exec prod_under_any` all **0 -> 1**, with `deny Unknown` correctly still 0. **REAL THIRD-PARTY VICTIM, COORDINATOR-VERIFIED END TO END, and it is the best evidence in this row:** `ed25519-dalek 2.2.0`'s **public `SigningKey::generate`** — a key generator taking a `CryptoRngCore` — is gated `#[cfg(any(test, feature = "rand_core"))]` at `src/signing.rs:201`. Scanned with the PUBLISHED binary it is **ABSENT from `functions[]`** (67 rows); at the fix it is `['Rand']` (70 rows). A signing-key generator reading as invisible. Others found by the agent: `ring`'s `Elem::into_unencoded`, `bstr`'s `first_non_ascii_byte_fallback`, `tower-http`'s `is_reserved_dos_name`. A/B over all 1489 crates, wide key (12 row fields + `analyzed`/`excluded`/`resolves`/`coverage`): **ADDED 42 REMOVED 29 CHANGED 29** loc-keyed; ADDED-names 40 / REMOVED-names 27 keyed `(package, fn)` across 30 crates. **`inferred` moved on ONE row — the narrow key sees almost none of this**, which is §E1's whole point restated by measurement. §E1 reach: **864 divergent verdicts across 77 of 1489 crates**, counted in the changed branch — recall, not a safety-only zero-diff — and the instrumented binary's reports are byte-identical to the committed build's. **All 27 removals audited in full from source**: every one inside a `#[cfg(all(feature = "…", test))]` module (combine, redis, proptest, utf8parse, x509-cert). **No effect is lost anywhere**; the only surviving-row losses are 6 `calls` edges in bstr, each replaced by `direct: ['Unknown']` + `unknownWhy: ['ambiguous:same-name local defs']` — a disclosure, not silence. **MY OWN NEAR-MISS, RETRACTED BY THE AGENT:** the curve25519-dalek `Scalar::random` "losing `['Rand']`" that made R123's prototype look unsound is **an artefact of a LOC-keyed read** — it keeps `Rand`, only its `loc` moves. **And my premise was right: R122 WAS the cause of that prototype's loss.** **PREVALENCE — USE THE AGENT'S NUMBER AND ITS DEFINITION, NOT MINE.** `any(...)` with `test` as a direct member plus a sibling: **140 crates (9.4%), 719 sites**; 138 crates / 709 sites have a non-harness-only sibling. Mine (131 / 122) was lower because it did not recurse into nested groups and counted only `feature =` siblings, missing platform and `--cfg` ones. The advisory must quote 140/9.4% **with the rule that produces it**. **Judgement stated rather than taken silently:** `any(test, miri)` and `any(test, doc)` are now SCANNED (non-`test` leaves stay unknown), whose honest cost is that aws-lc-rs's `generate_for_test` and sharded-slab's `Track` are now scanned. `--include-tests` byte-identical over all 20 moved crates. Revert-tested: both new tests fail without the fix. |
| R123 rust: two `cfg`'d `use` lines resolve by SOURCE ORDER, so a test mock captures production | 2026-09-02 | **CLOSED — candor-rust `2e49aa3`. PRE-EXISTING and PUBLISHED** | **CARDINAL SIN, rust, live in the shipped engine.** `use` items are collected with no `#[cfg(test)]` filter, so a production scan resolves a call through a TEST MOCK whenever the mock's import is written last. Coordinator-measured on the PUBLISHED binary, two crates identical in every byte except the ORDER of two mutually-exclusive imports, both EXECUTED and both really spawning a process: `#[cfg(not(test))] use std::process::Command as C;` followed by `#[cfg(test)] use crate::mock::Command as C;` gives `run` **ABSENT**; swapping the two lines gives `run` `['Exec']`. **Source order decides whether a real subprocess is reported** — the same class as R105 (which this wave fixed for ALIASES) one item kind over, which is brief §F1 q7: a key two paths spell differently. | Not yet fixed, and **deliberately not fixed by the obvious patch, which was PROTOTYPED AND MEASURED UNSOUND rather than reasoned about** — the strongest form of this evidence. Applying the `cfg(test)` filter to `use` collection was built in a throwaway worktree and A/B'd over 1489 crates: **ADDED 0 REMOVED 11 CHANGED 39**, and auditing all 11 removals found **`curve25519-dalek`'s `Scalar::random` losing `['Rand']`** — one silent under-report traded for another. This is [[feedback-fabrication-fixes-cause-misses]] in the other direction and is exactly why the naive fix was not shipped. **Fix R122 first** (its predicate is backwards, which is part of why the naive filter misbehaves). **Five sites answer "which `use` items count"; two apply the filter and three do not** — `collect_root_reexports` has no `include_tests` parameter at all. §F1 q3, and the reason a one-site fix cannot be trusted here. **NAMED UNEXAMINED:** `LocalUseCollector` (a `use` inside a function body) is also unfiltered for `cfg(test)` and was NOT measured; `--include-tests` mode is unaffected by construction, also not measured. **STATUS 2026-09-02: the fix exists and is NOT being shipped, on measurement rather than caution.** Rebased on R122's fix it is **ADDED 0 REMOVED 10 CHANGED 39 across 6 crates** (was 0/11/39 across 7) — **the curve25519-dalek loss is gone, exactly as R122 predicted**, which retires the reason the prototype was originally rejected. Reach: 310 dropped `use` items in 59 crates. **It still trades one sin for another, and the remaining trade is worse than the original framing.** It loses `['Log','Unknown']` on 35 rows of tokio's public async `fs` API. Blanket policies do not move; the scoped ones do: `deny Unknown fs::read::read` **1 -> 0**, and **`pure fs::symlink::symlink` 1 -> 0 — candor would certify `tokio::fs::symlink` PURE.** `deny Fs` unaffected. **The residual cause is ONE thing and it is separable — see R128.** So the order is: fix R128, re-run this A/B, then ship R123. **The number that decides R128's fix is named as missing rather than guessed.** **AND THE BODY-LOCAL `use` IS NOW MEASURED, having never been:** both `LocalUseCollector` and `CallCollector::visit_item_use` are unfiltered AND order-decided, confirmed by fixture with an opposite-order control — **20 sites in 12 of 1489 crates**. Two tests now **ASSERT THE DEFECT** (`r123_a_module_level_cfg_test_import_still_decides_by_source_order`, `r123_body_local_cfg_test_import_is_unfiltered_too`), each with its control and a standing instruction to delete them if they ever start passing wrongly — a documented gap that a gate will notice, rather than a comment. **SHIPPED — AND THE DIAGNOSIS THIS ROW WAS HELD ON WAS MEASURED FALSE.** The filed reason for holding it was that tokio's residual fell through R128's silent path. **With R128 fixed the rows still vanished**, and instrumenting the resolver showed `crate::blocking::spawn_blocking` resolving fine (`resolved_local=true`). **The real explanation was sitting in the A/B's own pre-image**: the removed `fs::asyncify` row carries `calls: ['fs::mocks::spawn_blocking']`. **All 35 tokio rows drew their `['Log','Unknown']` from `#[cfg(test)] mod mocks` — the disclosure this row was held to protect was MANUFACTURED BY THE VERY SIN R123 FIXES.** That is the sharpest instance this session of a hazard already in the file: a defect can produce evidence that argues for its own preservation, and the only way through it was reading the pre-image rather than the summary. **The coordinator's brief carried the false diagnosis and the agent broke it — the fourth brief this round corrected by measurement.** Result at HEAD: **`tokio::fs::symlink` reads `['Fs']` and `pure fs::symlink::symlink` STAYS exit 1** — candor no longer certifies it pure. A/B on top of both other fixes: **ADDED 0, REMOVED 7** (4 test fns plus 3 tokio rows whose only content was mock-derived), and **hickory-resolver GAINS a real `Clock` on 6 rows**. §E1 reach 310 `use` items across 59 crates, independently reproducing the earlier agent's figure. The module-scope test is **inverted** and now asserts ORDER-INDEPENDENCE. **The body-local test still asserts the defect and that gap stays open by design** (20 sites in 12 of 1489 crates) — a defect pinned by a failing-if-fixed test rather than by a comment. Cache rev14 -> rev16, so a warm cache cannot serve either old answer. |
| R124 swift: `vars` is not in `ShadowSave`, so every raw `vars[…] =` in a scoped visitor leaks | 2026-09-02 | **CLOSED — candor-swift `a615fc4`. NINE binder forms; two introduced by this wave (R117, R118), SEVEN PRE-EXISTING and published** | **CARDINAL SIN CLASS, swift. This is the row R117 and R118 turned out to be instances of, and finding the class was worth more than either bug.** `ShadowSave` (`CallCollector.swift:2223`) holds seven flag maps and **not** `vars`; `vars` returns only through `typeScopes`, written only by `scopeBindingType`. So a raw `vars[name] = ...` inside a scoped visitor leaks BY CONSTRUCTION, and the question is not whether a given site leaks but which sites are restored. **Of the eight sites that write `vars`, exactly TWO were scope-restored** — `visit(ForStmtSyntax)`'s own `typeScopes[node.id]`, and `typeCastBinder` -> `scopeBindingType`, which `3c78535` added and which WORKS (a measured gain, pinned as `ctlCastBinder`). **Six were raw.** Nine binder forms in total: `closureAnnotated` (:2444, R117) and `attrLocal` (R118) from this wave; `callArgAnnotated` and `callArgElement` (`typeClosureParams` :2753/:2757), `casePayload` and `caseLetOutside` (`typeEnumCaseBinding`, both arms), `optionalBinding` (`visitPost(OptionalBindingConditionSyntax)`), and `nestedLet`/`tupleShadow` (`visit(VariableDeclSyntax)` plain and tuple) — all pre-existing and published. | **Each site is a silent under-report AND a fabrication, and BOTH directions were probed.** `deny Fs`, `deny Fs Unknown`, `deny Unknown` and `pure` all exit 0 on the loss arms; the mirror arms charge `Fs` to an inert receiver whose probe file is observed to SURVIVE. **An earlier audit of this same mechanism probed only the loss direction and filed two wrong clean verdicts** — [[feedback-fabrication-fixes-cause-misses]] pointed at an audit rather than a fix. **MY OWN READ WAS RIGHT AND ONE SITE SHORT:** I named only the annotated arm of `typeClosureParams`; the ELEMENT arm (`qs.forEach { s in … }`) leaks identically. And `scopeBindingType` is the WRONG authority there — it runs before the closure's scope opens and would find the enclosing block — so those sites register against the closure's own node id, the same hand-over `monoClosureParams` already uses. Ground truth EXECUTED by extracting the exact `src` string from the test file, building it as an SPM package and running it: **17 arms delete their probe, 3 `fab*` arms do not.** Revert-tested SITE BY SITE, all eight added lines removed in turn, suite red for each (2,2,1,1,2,2,1,1) — and two sites (`caseLetOutside`, `tupleShadow`) were initially UNPROTECTED, so those arms exist because the revert test caught it. A/B over 5 real corpora keyed on every field: **7,876 common rows, ADDED 0, REMOVED 0, CHANGED 16, `inferred` changes 0**, with an instrumented build proving the corpus REACHES the change (swift-nio: 70 registered / 66 effective on the F1 branch). All 16 attributed to a single site each by an env-gated bisect build calibrated at both endpoints; ground-truthed from source, `BitSet.symmetricDifference` loses five FABRICATED `BitSet.init` overload edges. Gates: `swift test` 1029/0, `smoke.sh` 148/0, `fuzz.py` 25/0, `fabrication_probe.py` OK, `recall.sh` 5/0, `ci/self-gate.sh` OK. **The two gates that SELFSKIP on macOS were RUN UNDER DOCKER rather than reported green** (`swift:6.1`, `--cap-add=SYS_PTRACE`): `soundness/realworld/run.sh` 13 honest / 0 under-reports / 0 fabrications, `disclosure_recall.sh` 13/13 with the fabrication mirror firing on the pure control. Neither is UNRUN. **Near-miss reported rather than buried:** two `_HashNode` rows lose `direct:[Unknown]` + `unknownWhy:[callback:inserter]` while keeping `inferred:[Unknown]`/`unresolved:true` — the disclosure survives, its REASON thins. Traced to a separate latent defect left untouched: the ctor/factory arm of `visit(VariableDeclSyntax)` writes `vars` only when `rootOf` yields a root and **does not clear when it does not**, so a stale type survives a rebind. |
| R125 swift: `_BTree.forEach` lost its `callback:body` disclosure somewhere in the unpushed wave | 2026-09-02 | **CLOSED — candor-swift `5e77173`. MY ROW WAS WRONG: NOT a regression. PRE-EXISTING and PUBLISHED, from `ae3a7fd` (the ⟨0.34⟩ per-caller callback flow)** | **LOST DISCLOSURE, swift.** `_BTree.forEach` (swift-collections) reads `inferred: ['Unknown']` with `unknownWhy: ['callback:body']` at the published baseline `e5c7871` and is **ABSENT at `795a39f`** — a disclosure withdrawn over `try body(...)`, an arbitrary caller-supplied closure. Attributed to the wave and not to the R124 fix, whose own PRE->FIX diff is REMOVED 0. | Not yet fixed and **NOT YET MINIMISED — stated as such rather than guessed at.** Two reduction attempts both landed on pre-existing holes instead of this one, which is why no fixture accompanies this row. Reproduce by scanning `swift-collections` with binaries built at `e5c7871` and `795a39f`. Until it is minimised the responsible commit within the wave is unknown, so this row records a MEASURED difference between two endpoints and explicitly not a cause. **THE PREMISE I FILED WAS WRONG, AND THE REAL DEFECT IS BIGGER.** Bisected across the wave: `67595c2`, `e441f1c`, `17cbc95`, `457f558` all keep the row; **`3c78535` drops it** — **and `3c78535`'s own commit message already audits that exact removal and names the cause.** Its R97 typealias fix makes `_root` resolvable, so `SortedDictionary.forEach` and `SortedSet.forEach` become visible CALLERS of `_BTree.forEach`, and both GAINED `Unknown`/`callback:body` in the same diff (ADDED 46, REMOVED 1). **I filed a row accusing a commit without reading it.** That is the same failure as R81 and R90 — a row written as a conclusion from someone else's wording — and it is the third instance. **WHAT ACQUIRING A CALLER EXPOSED IS A CARDINAL SIN IN THE PUBLISHED 0.34.0 ARTIFACTS.** Two byte-identical higher-order methods whose sole variable is whether anything calls them. Coordinator-verified on a release build of the published baseline `e5c7871`, ground truth EXECUTED (the probe file is really deleted): `Store.hofNoCaller` -> `['Unknown']` + `unknownWhy: ['callback:body']`, **`deny Unknown Store.hofNoCaller` exit 1**; `Store.hofWithCaller` -> **ABSENT**, **`deny Unknown Store.hofWithCaller` exit 0**. Cause: `direct[caller] += Unknown` was written to every CALLER and never to the CALLEE, and the `byCaller.isEmpty` fallback that exists to keep the callee honest simply stopped firing the moment it had a caller. **`pure` exits 0 on BOTH arms in both binaries and is NOT part of this finding** — `pure` deliberately does not read `Unknown` (SPEC §4 / AS-EFF-003), the same distinction R96's row had to make. **FIXED** by marking the callee only when NOT ONE caller resolved, so every caller already carries the identical `Unknown` and nothing propagates anywhere new. Coordinator-verified at HEAD: both arms `['Unknown']`/`callback:body`, both `deny Unknown` exit 1. A/B over 5 corpora keyed on every field, pre-image proved behaviourally: **7,876 common rows, ADDED 59, REMOVED 0, CHANGED(inferred) 34, CHANGED(any) 274**, §E1 branch hits **406**, and **every moved row is one of the 406 marked functions — 0 outside**, which is the measurement behind the no-precision-cost claim. All 59 added rows are `['Unknown']` with `callback:` reasons; no list field lost an entry. **93 real functions stop certifying clean**, including `Mutex.withLockUnchecked`, `Deque._Storage.update`, `NIOAsyncTestingEventLoop.executeInContext`, `sockaddr_storage.withMutableSockAddr` and `_BTree.forEach` — so the row's original subject is closed as a side effect of closing the real one. Audit widened past the trigger (§9): all 17 `direct[…]=` and 15 `whyMap[…]=` writes in `Driver.swift` enumerated; only `chaWithinBound` writes a key that is not the unit under analysis, and both its call sites pass `f.qual`. Sole site. Revert test done BY REVERTING: 17 assertion failures across 4 rows. An existing test (`testClosureLiteralArgStaysOpaque`) **asserted the defect** — *"the Unknown belongs to its caller"* — and is inverted here, with the three ⟨0.34⟩ over-charge controls beside it unchanged and green. Gates: `gate-run.sh candor-swift` 11 run / 11 ok, `swift test` 1033/1033, `smoke.sh` 148/148; the two macOS-SELFSKIP gates RUN UNDER DOCKER — `realworld/run.sh` 13 honest / 0 under-reports / 0 fabrications, `disclosure_recall.sh` 13/13 with the fabrication mirror firing. |
| R126 swift: an opaque callback inside a closure inside a NESTED FUNC reads silent-pure | 2026-09-02 | **RETRACTED — NOT A DEFECT. Measured, not argued away** | **CARDINAL SIN, swift, found while trying to minimise R125 — the useful kind of side effect from a failed reduction.** An opaque callback invoked inside a closure inside a nested `func`, where the closure's receiver is an INSTANCE PROPERTY, reads silent-pure. Three controls each varying exactly one thing: `vA` (receiver is a property) **ABSENT**; `vB` (receiver a literal `[1]`), `vC` (no nested func) and `vD` (direct invoke) all correctly `Unknown`. So the loss needs all three of nested-func, closure, and property receiver. | Not yet fixed. Fixture at `scratchpad/agsw/f5`. **Related but distinct from R124** — this is a disclosure that never forms rather than a type that leaks past its scope, and it survives the R124 fix. **RETRACTED 2026-09-02 WITH THE MEASUREMENT.** The four arms this row rested on differed in **TWO** variables, not one: only the property-receiver arm was ever CALLED. So the row attributed to "nested func + closure + property receiver" what was actually R125's caller defect, and I repeated the conjunction from an agent's report without testing it. A 20-arm sweep holding *has a caller* constant — instance `let`/`var`, `static`, computed, subscript, tuple element, local copy, nested-func param, nested-type property, and literal receivers crossed with direct / one closure / two closures / nested func / nested func in a closure / closure in two nested funcs / `defer` / stored local closure / by-reference pass to a sync invoker — is **ABSENT for all 20** at `e5c7871` and `a615fc4`, and `Unknown`/`callback:body` for **all 20** after the R125 fix. Built as an SPM package and RUN (`arms whose callback did NOT run: none`). **No position-specific hole exists.** A retraction backed by a 20-arm sweep is a better outcome than a fix for a defect that was never there — and it is the second row this round retracted after measurement, which is the process working rather than failing. |
| R127 swift: one RESOLVED caller still leaves a HOF's own row silent | 2026-09-02 | **CLOSED — candor-swift `96211f0`.** Originally filed: **OPEN — the stated residual of R125's fix, pinned by a test rather than left implied** | **KNOWN RESIDUAL, swift.** R125's fix marks the callee only when NOT ONE caller resolved. Where resolution is MIXED — at least one caller resolved and at least one not — the callee's own row is still silent. Pinned by `testMixedResolutionLeavesTheHOFsOwnRowSilent`, which asserts the current behaviour so the gap cannot be closed by accident or drift away unnoticed. | Not fixed, and the reason is a genuine trade rather than difficulty: closing it would push `Unknown` into the PRECISELY-RESOLVED caller — the fabrication ⟨0.34⟩ deliberately removed — and doing it properly needs a per-caller node. **The decision rests on a synthetic case, and that is stated rather than hidden: the mixed arm fired 0 times in 7,876 rows across five corpora**, so the wider fix is unmeasurable on real code today and the only evidence for it is a hand-built fixture where it is a straight over-report. **Incidental measurement worth keeping:** the ⟨0.34⟩ `resolved` branch never fired at all on those corpora — `ae3a7fd` is SAFETY-ONLY over them, so the corpus that would price this residual does not yet exist. **CORRECTED 2026-09-02 BY AN ADVERSARIAL REVIEW: the residual is NOT as narrow as this row and the commit's own note claim.** Both said the mixed arm *"fired 0 times in 7,876 rows"* — a fact about five library corpora — and both let that stand as evidence of NARROWNESS. It is not. The trigger is `anyCallerResolved` (`Driver.swift:2334/2359/2365`): **ONE caller passing a uniquely-named free function, whatever it is**. Reviewer's executed A/B, one variable — adding a `func unusedHelper() { hof(noop) }` that **nothing ever calls** — moves `hof` from `['Unknown']`+`callback:b` to **absent**, and `deny Unknown hof` from **1 to 0**. **That is the identical gate matrix and identical severity as the sin `5e77173` was written to close.** Three further shapes reach it, all measured: a convenience overload `func withRetry() { withRetry(defaultBody) }`; three real closure callers plus one no-op caller; and a self-recursive HOF passing a named function to itself. **The last shape defeats the stated reason for not fixing it:** the commit declines because closing it *"would propagate `Unknown` into the caller that resolved precisely"* — in the self-recursive case that caller **is the function itself**, so the objection does not apply. The row's claim was never that the fix is wrong; it is that the residual's narrowness was UNMEASURED and is one dead helper away in any codebase. The `Driver.swift` note and `testMixedResolutionLeavesTheHOFsOwnRowSilent`'s comment both currently overstate how hard the arm is to reach, and a reader will trust them. **CLOSED 2026-09-24, candor-swift `96211f0`, under the standing cost rule — and the precision cost was RE-MEASURED rather than assumed.** A counter at the decision site (`CANDOR_R127_PROBE`, committed and calibrated — it prints `anyResolved=true allResolved=false` on the fixture, so the zero CAN fail) found **419 HOF deferral sites over 12 real packages: 418 no-caller-resolved, 1 all-resolved, 0 MIXED.** That re-measurement mattered: R563 had changed resolution rates since the row's original figure. `anyCallerResolved` -> `allCallersResolved`. **Gate flip on the fixture: `Box.hof` ABSENT -> `['Unknown']`, `deny Unknown Box.hof` exit 0 -> 1.** (`pure Box.hof` stays 0 both ways — it needs the explicit purity upgrade; only the measured flip is claimed.) The price is asserted, not tolerated: the resolved caller goes `['Fs'] -> ['Fs','Unknown']`. **A/B: 12 entries, 12,346 rows, 0/0/0 on every key — WITH REACH 419 hits across 11 of 12 entries, so it is SAFETY-ONLY on real code and the zero is not a flattering number over an unreached branch.** `testMixedResolutionLeavesTheHOFsOwnRowSilent`, which existed to go red when someone closed this, went red as designed and is rewritten; ⟨0.34⟩'s fabrication control is unaffected by construction. |
| R128 rust: a `use` bound to a crate-local path that names nothing reads as PURE | 2026-09-02 | **CLOSED — candor-rust `98e0a79`. PRE-EXISTING and PUBLISHED. REPRODUCED before it was fixed** | **CARDINAL SIN, rust, and it is the blocker under R123.** A `use` whose target is a crate-local path naming no unit — tokio's production import points at `crate::blocking::spawn_blocking`, a `pub(crate) use` inside a `cfg_rt!` **macro body** — is treated as PURE rather than as a disclosure. Reported as a four-line repro on HEAD with **no `cfg` involved**, so it is independent of R122/R123: the caller `go` hands a closure to a macro-hidden re-export and is **ABSENT**, while its target reads `['Unknown']`. This is why R123's otherwise-correct fix would certify `tokio::fs::symlink` pure: the fix stops resolving through the test import, and what it falls back to is this silent path rather than a hedge. | Not yet fixed. **FILED FROM AN AGENT'S REPORT AND NOT YET REPRODUCED BY ME — treat the mechanism as a hypothesis until it is.** That caveat is not boilerplate here: R125 and R126 were both filed this same round from agent reports without independent checking, and **both were wrong** (one accused the wrong commit, one was retracted entirely). The shape to verify first is the general one, not tokio's: **an unresolvable `use` target should produce a disclosure, and the question is how many other ways a target can name nothing** — a macro body is one, and §9 says do not draw the audit around it. Fix order is fixed by measurement: **R128, then re-run R123's A/B, then ship R123.** **REPRODUCED FIRST, exactly as the row demanded.** The four-line repro was built, compiled and RUN (`ran=true`, a real spawned process): `go` **ABSENT** from `functions[]`. Two controls each varying one thing — drop the macro wrapper; write the call out with no `use` — both read `['Unknown']` at HEAD, **so the mechanism is the MACRO, not the shape**. **One mechanism, THREE spellings**, all item-position `syn::Item::Macro`, each compiled and run spawning a real process: a macro body holding a `pub(crate) use` (`go` ABSENT, blanket `deny Exec` **0**); a macro body DECLARING the `pub fn` (**every** policy form 0 — the target has no row either); and an item-position `include!` (blanket 1, but only incidentally). Real instances ground-truthed from source: tokio's `cfg_rt!`, async-std's `cfg_default!`, nix's `fcntl::open` inside `feature!`, reqwest's `into_url::try_uri` inside `if_hyper!`, and find-msvc-tools' entire `windows_sys` FFI surface. **THE GENERAL RULE FLOODS AND WAS MEASURED RATHER THAN FEARED:** *any unresolved crate-rooted call -> Unknown* hits **66,196 sites in 741 of 1489 crates**. The evidence-based rule that shipped — the owning module is demonstrably unreadable — reaches **325**. That is a 200x difference, and it is the number that made the decision. A/B keyed on every field: **ADDED 193, REMOVED 0, and no surviving row lost a value in ANY field**; blanket `deny Unknown` across all 44 affected crates flipped **0** verdicts. **THREE GUARDS, ALL FROM AUDITING THE A/B RATHER THAN FROM DESIGN** — the first cut charged 794 of 1330 hits on targets candor could actually see: an AMBIGUOUS tail2 is not a hidden target (redis `from_redis_value`, 67 hits); a leaf naming a local type is a constructor (diesel `Grouped`, 85); and a crate-root fn has no tail2 key at all, so **its absence from the index is a fact about the index, not about the code** (log's `logger`, criterion's `black_box`, 160 hits / 34%). That last guard is the reusable one: *absence from an index is only evidence if the index could have held the thing.* Residual over-charge stated: ~34 hits construct a macro-declared type. **Stated under-reports, each pinned by a test rather than a comment:** R128's associated-fn and explicitly-rooted spellings. |
| R129 rust: 565 of 1489 crates emit byte-identical DUPLICATE rows in `functions[]` | 2026-09-02 | **OPEN — PRE-EXISTING and PUBLISHED. Agent-measured; NOT verified by the coordinator** | **REPORT-INTEGRITY DEFECT, rust, not a soundness one — filed because nobody had a number for it.** 565 of 1489 registry crates already emit byte-identical duplicate entries in `functions[]` (rustix 149, num-bigint-dig 130). Pre-existing and unrelated to the R122 change, which adds 20 more in 5 crates. | Not yet fixed, and **its severity is genuinely unknown rather than known-low** — that is the reason to file it rather than a reason to defer it. Duplicates do not obviously lose an effect, but they inflate every count computed from `functions[]`, and this project has already been caught by a flattering count derived from an unsound partial scan ([[candor-crossorg-corpus]]). **Two things to establish before deciding anything: whether the duplicates are ever NON-identical in a field the wide key does not cover, and whether any consumer (`analyzed.count`, the coverage ledger, `gains`, the surface counters) is keyed in a way that double-counts them.** Until then it is an unpriced defect, not a cosmetic one. **MEASURED 2026-09-04 by a plan reviewer and DEMOTED accordingly: over 836 on-disk corpus reports, 440 have duplicate `fn` rows, 4,209 duplicate rows in total, and ZERO groups whose `inferred` differs.** The A/B harnesses key by `fn` (`cmp.py:6`, `arms.py:8`), so duplicates are invisible to the whole verification chain and harmless while identical. **What is NOT measured: whether any OTHER field differs between duplicate rows** — say so rather than implying the row is clean. The severity is therefore 'noise in the report a consumer reads', not a soundness defect. **2026-09-05: THE OPEN HALF IS CLOSED — no field CAN differ, by construction, and what that hides is worse than redundancy.** `scan.rs:2992` builds every `ReportEntry` as `<map>.get(q)` keyed on the QUAL alone (`loc`, `inferred`, `direct`, `calls`, `unknownWhy`, `unresolved`, `invisible`, `incomplete`, `fs`, `paths`, `cmds`, `hosts`, `tables`, `netClass`, `hash`), so nothing in an entry depends on which `FnInfo` produced it. Confirmed on the WIDE key over the shipped 0.35.0 arm (1,509 crates, 268,795 rows): 3,572 duplicate groups, **0 differing in any field**. **But the per-qual maps are UNIONS over the duplicates, so `loc` on EVERY row is the FIRST unit's** — `rand_core-0.6.4`'s two `error::Error::fmt` rows both carry `Rand` although only the `Debug` arm calls `getrandom`, and both `half-2.3.1` `leading_zeros_u16` rows point at arm one. **A duplicate row is a MERGED answer over units that do not agree, printed twice as if each were its own function.** The report also UNDERCOUNTS: 20,495 duplicate UNITS of 647,698 (3.16%) in 884 of 1,509 crates, of which only 6,981 rows in 589 crates reach `functions[]` — the rest are pure functions the report omits. **SAME DEFECT AS R222**, proven three ways, decisively by: crates with a fake-ambiguous index key and NO duplicate unit = **0 of 1,509**. Fix the duplicate, not either symptom. Still unmeasured: whether `analyzed.count` (= 647,698, inflated by 3.16%), the coverage ledger, `gains` and the surface counters double-count. **2026-09-05, after R222's fix: the duplicate-row count RISES, 6,981 → 7,371.** `functions[]` lists only effectful rows, so making 596 more functions effectful surfaces MORE duplicate quals. R129's own subject — a per-qual map is a UNION over units that need not agree, printed once per unit — is untouched and still open. |
| R130 rust: `std::os::{unix,windows,wasi}::fs` had NO filesystem rule — `symlink`/`chown`/`chroot` read PURE | 2026-09-02 | **ALL FOUR ENGINE HALVES CLOSED — rust `550d071`, java `db9d918`, swift `800a281`+`4fb6893`, ts `c4ae525`. PRE-EXISTING and PUBLISHED in rust, java and swift; the ts half found different defects (see below). WHAT REMAINS OPEN IS A SPEC QUESTION, NOT AN ENGINE FIX: swift answers the raw-syscall route with a DISCLOSURE and rust/java CHARGE the effect, so one program gets two gate verdicts** | **CARDINAL SIN, rust, and the shape is embarrassingly simple: `std::fs::` was the ENTIRE filesystem rule.** Everything under the platform-specific modules — `symlink`, `chown`, `lchown`, `chroot`, and the rest of `std::os::unix::fs` and its windows/wasi siblings — matched nothing and read PURE. Found while tracing the tokio difference in R123, i.e. by a question asked in passing rather than by the audit that was commissioned. **Coordinator-verified end to end, ground truth EXECUTED** — `cargo run` created a real symlink and `ls` confirms `/tmp/candor_osfs3_link -> /etc/hosts`. Against the PUBLISHED binary, in a crate containing no portable-`fs` call anywhere: **`functions: 0`, `excluded: []`, and ALL FIVE policy forms exit 0** — `deny Fs`, `deny Unknown`, `deny Fs Unknown`, `pure make_link`, `deny Fs make_link`. At the fix: `functions: 2` and all four `Fs`-bearing forms exit 1. **The control is what makes it a one-variable result:** the same write through the PORTABLE module (`std::fs::copy`) reads `['Fs']` in both binaries, so the only variable is the module path. In a crate that also uses portable `fs`, blanket `deny Fs` exits 1 — but only INCIDENTALLY, via the other call. | **The rust half is fixed with a trait-keyed DENYLIST** ([[candor-denylist-over-allowlist]]): `MetadataExt`/`PermissionsExt`/`OpenOptionsExt` and friends stay pure, `FileExt` does not — **and those exemptions are TESTED, not asserted in a comment** (§E2). A/B: **ADDED 34, REMOVED 0, nothing lost.** **THE ROW STAYS OPEN BECAUSE THE QUESTION IS FOUR-WAY AND HAS NOT BEEN ASKED OF THE OTHER THREE ENGINES.** The generalisable question is not about rust: **does each engine's filesystem rule cover only the PORTABLE spelling, and what does it do with the platform-specific, POSIX or alternate-module route to the same syscall?** That is brief §F1 q3 (separate implementations that drift) crossed with §9 (do not draw the audit around its own trigger). Agents are dispatched for java, ts and swift; the rust fix is not evidence about any of them. **THE JAVA HALF CAME BACK MUCH BIGGER THAN THE QUESTION, AND THE QUESTION WAS TOO NARROW.** It is not that java's `Fs` rule covered one spelling: **THIRTEEN routes across FIVE effects** were silent, and they share one mechanism. Each was isolated in a class containing NO ordinary-spelling call, compiled and RUN so the real effect was observed — a real symlink, a real 0400 chmod, a real `user.candor` xattr, a real file trashed, 807 real OS process handles, a real child waited for, a real DNS lookup resolving `localhost` and failing on `.invalid` — then scanned: `functions: []`, `excluded: []`, **exit 0 under all five policy forms**, with the same-operation control charging and exiting 1 in the same jar. **Fs:** `java.nio.file.spi.FileSystemProvider` — **the SPI every `Files.*` method is DEFINED as a call to** — plus `FileSystem.getFileStores/getRootDirectories/newWatchService/close`, the `*AttributeView` family (chmod/chown/utimes/setxattr via a view received as a parameter), `UserPrincipalLookupService`, and `Desktop.moveToTrash`. **Exec:** `Process.onExit/inputReader/errorReader/outputWriter` and `ProcessHandle.onExit/allProcesses/children/descendants/parent/of`. **Rand:** the `RandomGenerator$Splittable/Jumpable/Streamable` sub-interfaces. **Net:** `new InetSocketAddress(String,int)`, `InetAddress.getHostName`, `Inet4/6Address` receivers, `SSLServerSocket`, `ServerSocketFactory.createServerSocket`, `JarURLConnection`. **Clock:** `GregorianCalendar.getInstance()`. **COORDINATOR-VERIFIED ON THE HEADLINE ROUTE, and java's failure is a STRONGER claim than rust's.** Isolated fixture, ground truth EXECUTED (`/tmp/candor_spi_link -> /etc/hosts` confirmed by `ls`): at the pre-image jar `f9b5ba5`, `app.Spi.viaProvider` is **PRESENT in `functions[]` with `inferred: []`** — not omitted, but **affirmatively listed as effect-free** — and all five policy forms exit 0. At `db9d918` it is `['Fs']` and all four `Fs`-bearing forms exit 1. **In rust the unit VANISHED; in java it is reported as pure.** Same silence, but java's is a positive false statement rather than an absence, which matters for how a reader of the report is misled. **THE MECHANISM (the reusable part):** all thirteen are the classifier keying on the **static receiver type in the bytecode**, in one of three shapes — (a) a subtype-typed receiver of a modelled type, where the supertype re-classification at `Candor.handleMethodInsn` is gated on `ctx.byName.containsKey(min.owner)`, i.e. **PROJECT OWNERS ONLY, so no JDK subtype ever gets the walk**; (b) the layer BELOW a modelled facade; (c) a verb allowlist that forgot a member. Shape (a) had already been patched by hand three times (`MulticastSocket`, `SSLSocket`, Conscrypt) — **each boundary drawn around its own trigger**, which is §9 three times over. The agent widened past that by sweeping **13,324 public JDK types against the 172 owners the classifier models**; `SSLServerSocket`, `Inet4/6Address` and `JarURLConnection` came out of that sweep rather than out of anyone's list. Two §E2 hits: the `Rand` rule's comment asserted the sub-interfaces *"extend it"* (true of the type system, false of bytecode), and the `Process`/`ProcessHandle` rules were ALLOWLISTS in a file whose own `ProcessBuilder` rule argues at length that allowlists under-report silently. A/B over 395 third-party jars keyed on EVERY field: 577,547 common rows, **ADDED 107, REMOVED 0, zero losses on any field**. §E1 counters in all 14 changed branches: **11 reached** (clock.calendar 279, exec.process 111, net.sockaddr 90, fs.filesystem 61, fs.provider 57 …) — a RECALL measurement; three branches got zero corpus hits and are **safety-only over this corpus**, each resting on an executed fixture instead, and that is stated rather than blended in. Real code newly reported: httpcore5 `DefaultAddressResolver.resolve`, okhttp `Util.peerName`, jackson `InetSocketAddressSerializer`, smallrye `GetAllProcessesInfoAction`. Revert test done BY REVERTING: 6/6 positive tests go red; the 7 over-charge controls pass in both arms **and the agent explicitly does not claim they discriminate the fix**. **TWO ERRORS THE AUDIT CAUGHT IN ITSELF, both worth keeping:** its first cut FABRICATED `Fs` on `FileSystemProvider.getPath(URI)`/`getFileSystem(URI)` — four real rows in jetbrains verifier-cli — which the SPI specifies as non-I/O; carved out, pinned, A/B re-run. And `ServerSocketFactory.createServerSocket` first read as ALREADY COVERED because the fixture also called `ServerSocket.close()`, which is charged; re-run with the bind alone, both arms said `functions: []`. **A mixed fixture cannot answer an isolation question** — the same trap that made my own blanket-deny readings incidental three times today. **THE SWIFT HALF FOUND TWO SINS, AND THE FIRST IS THE MOST ELEGANT FAILURE OF THIS ROUND.** candor-swift already HAD a raw-syscall disclosure — R61's `native:<name>` — so `symlink` was never uncovered. **It was gated on the file importing `Darwin`/`Glibc`/`Musl`/`WinSDK`, and Foundation RE-EXPORTS the platform C library.** So the disclosure existed, was correct, and simply did not fire for the import almost every real Swift file uses. Coordinator-verified, ground truth EXECUTED in BOTH arms (each creates a real symlink, confirmed by `lstat` + `S_IFLNK` in the program's own output), isolated — no `FileManager` call anywhere — with **the import line as the only variable**: at the PUBLISHED baseline `e5c7871`, `import Darwin` gives `doWork` -> `['Unknown']` + `unknownWhy: ['native:symlink']` and **`deny Unknown` exit 1**, while `import Foundation` gives **`functions: 0`, `doWork` ABSENT, and all five policy forms exit 0** — no `Unknown`, no `invisible`, no `incomplete`, a positive purity claim over an executed syscall, **for the whole R61 allowlist at once**. At HEAD both arms are identical. Fixed by removing the module gate entirely — the NAME allowlist is the only gate — plus one PROVABLE narrowing: a C function imported into Swift has no argument labels, so `remove(at: i)` cannot be libc's `remove`. **§9 half:** the original boundary had been drawn around R61's own repro, so the audit swept outward and found the `*at` twins of five listed names plus `openat`/`chroot`/`chdir`/`mkfifo`/`mknod`/`creat`/`opendir`/`readdir`/`flock`/`fsync`/`mmap`/`umask`/`waitpid`/`copyfile`/`sendfile`, the `execl*` family and the remaining `set*id` setters — **all silent under BOTH imports**. `execve`/`execvP`/`fexecve`/`posix_spawnp` were in NEITHER table and are now concretely `Exec` and ESTABLISHING, so a runtime command through them can no longer mask an `allow Exec` allowlist. **SECOND SIN (`4fb6893`), and MY BRIEF'S GUESSES WERE WRONG:** I named `Data.write(to:)` and `String(contentsOfFile:)` as suspects and **both are modelled**. What was not: `NSString`/`NSDictionary`/`NSArray(contentsOfFile:)` — `NSDictionary(contentsOfFile:)` being how a decade of plist-reading Swift is written — in a classifying function whose own title is *"ONE FUNCTION, BOTH SPELLINGS"* and which knew three of the six type spellings; `NSData.write(toFile:)`, the bridged twin of the charged `Data.write(to:)`; **`URL` had no κ entry AT ALL** (`checkResourceIsReachable()` is a `stat` and the idiomatic Swift file-existence test, sibling of the always-`Fs` `FileManager.fileExists`), plus `resourceValues`/`setResourceValues`/`bookmarkData`/`resolvingSymlinksInPath`; and `OutputStream(toFileAtPath:)`/`InputStream(fileAtPath:)`, **inexpressible in `kappaFree` because it is keyed on `(name, argCount)` and only the LABEL separates a file open from `InputStream(data:)`**. A/B over 13 real packages, 11,008 common rows, wide key (13 fields): **ADDED 3, REMOVED 0, CHANGED 53**, all 56 audited against source with no sampling, branch counters confirming the corpus reaches the change (20 hits in 3 packages) — a RECALL measurement. Revert-tested by actually reverting at the parent commits: 7/9 and 5/5 rows red, the green ones named as controls. Gates all green, 1047/1047 on macOS AND under `swift:6.1` on Linux; the two macOS-SELFSKIP gates run under Docker. **A MECHANISED `swiftc -typecheck` HELPER (§E3) CAUGHT FOUR OF THE AGENT'S OWN BROKEN FIXTURES on their first Linux run** — `nil` argv for `execve`/`posix_spawnp`, and `bookmarkData`/`checkPromisedItemIsReachable`/`NSURLConnection` not existing on swift-corelibs-foundation. Those rows had been asserting things about programs that cannot exist on the platform CI runs on, which is §E3's exact failure mode caught by a tool instead of by luck. **A CROSS-ENGINE DIVERGENCE THIS EXPOSES, AND IT IS A SPEC QUESTION RATHER THAN A BUG:** swift answers the raw-syscall route with a DISCLOSURE (`Unknown` + `native:symlink`, caught by `deny Unknown`), while rust and java now CHARGE the concrete effect (`Fs`, caught by `deny Fs`). Coordinator-measured at swift HEAD: **`deny Fs doWork` still exits 0** on the symlink fixture. Both answers are honest and neither is silent, but **the same program gets a different gate verdict depending on the engine**, which is precisely what this suite exists to prevent. Tom's call, not an engine's. **THE TS HALF REFUTED MY PREMISE FOR `Fs` AND FOUND TWO REAL DEFECTS IN `Net` INSTEAD — the most useful shape a brief can fail in.** candor-rust's `std::fs::`-only rule has NO ts analogue: κ's `[/^(node:)?fs(\/promises)?$/, null, 'Fs']` is keyed on the **DECLARING FILE, not the import specifier**, so every route lands on it. Measured, not assumed: **31 spellings in one tree, all `tsc --noEmit` clean, all 31 charge `Fs`** — `fs` vs `node:fs`, `fs/promises`, `fs.promises`, named/default/namespace imports, `import fs = require()`, dynamic `import()`, the Sync/callback/promise families, `createWriteStream`, the two-hop `FileHandle.write`/`writev`/`truncate`, `openSync`+`writeSync`, `cp`/`rm`/`opendir`, `realpathSync.native`, `symlink`/`chown`/`lchown`, `watch`, `statfs`, `globSync`, `new fs.WriteStream`. `original-fs` and the Deno/Bun globals are uncovered and **never claimed — a gap, not a sin**, which is the distinction the brief asked for. **DEFECT (a), SILENT UNDER-REPORT, PUBLISHED — and it is an exfiltration primitive.** `@types/node/web-globals/fetch.d.ts` re-exports `WebSocket`/`EventSource` from the **`undici-types` PACKAGE**, so `declIsNodeTypes` is false, the node-core floor is never consulted, and κ had no rule. **This is the FOURTH instance of the R109/R110/R111 tsconfig-split class.** Coordinator-verified, isolated file, `tsc` clean, one variable: at the pre-image, `dial` (`new WebSocket(u)`), `exfil` (`w.send(secret)`), `stream` (`new EventSource(u)`) and `viaSubclass` (`class D extends WebSocket { super(u) }`) ALL read **`inferred: []`** and **every one of `deny Net`, `deny Unknown`, `deny Net Unknown`, `pure`, and the scoped deny exits 0**. At HEAD all four carry `['Net']` and the four `Net`-bearing forms exit 1. **A DETAIL I MEASURED DIFFERENTLY FROM THE AGENT, and it matters:** the agent reported two rows carrying no `invisible`; in my fixture **all four carry `invisible: ['undici-types']`**. So a breadcrumb naming the package IS emitted — **and no policy form reads it, `deny Net Unknown` included.** That is arguably worse than plain silence to reason about: the report contains a true statement that the scan could not see something, and **there is no gate that turns that statement into a verdict.** The gate consequence is identical to silence. `NODE_CORE_REVIEWED` asserts *"`WebSocket` is deliberately absent — it CONNECTS"* and **the name never reached that list** — §E2, a comment asserting the safety of its own diff. Ground truth EXECUTED by the agent against a real 127.0.0.1 listener: the ctor fires `upgrade` once, `ws.send('SECRET-EXFIL')` delivers one frame carrying that exact unmasked payload, `new EventSource(…/sse)` issues one HTTP request. **The `extends`/`super` spelling came from REAL CODE, not enumeration** — crossws `src/websocket/bun.ts` — found by re-scanning a corpus under a node-style tsconfig **after the first cut stopped at its own trigger** (§9, caught by the agent on itself). **DEFECT (b), FABRICATION, PUBLISHED.** ⟨0.29⟩ fixed the payload-as-locator class for Exec/Fs/Db and, in Net, fixed only the POSITION and dgram's `send`. So `export function w(s: net.Socket) { s.write('api.example.com'); }` published `hosts: ['api.example.com']` with **no `incomplete`**, and **`allow Net in src api.example.com` answered `policy ✓` exit 0** over a socket whose destination the scan never saw. `netEstablishing` already excludes use-calls and says so — it governed the `incomplete` branch and **the capture branch never asked it**. A/B over 8 corpora, 2,561 common rows, 16-field key: ADDED 9 REMOVED 2 CHANGED(wide) 102 CHANGED(narrow) 40, **zero effects lost**, both removals ground-truthed from source as genuinely pure `new Headers(...)` builders. **The six library corpora reach the `undici-types` arm ZERO times** — all leave `lib` unset or name `dom` — so over those it is SAFETY-ONLY, and the recall evidence is the real crossws source under a node tsconfig, stated rather than blended. Revert-tested three ways (34 / 28 / 4 rows red respectively), and **one drafted row that could not discriminate either way was REPLACED rather than kept as fake coverage.** **JAVA FOLLOW-UP CLOSED — candor-java `2a6793c`. Three fabrications `db9d918` introduced, plus a published clock sin it sat beside; all four reproduced BEFORE any fix.** **B2 (`FileSystemProvider.getFileAttributeView` charged `Fs` doing no I/O) fixed by sweeping the SPI by SPECIFICATION rather than by corpus** — the discriminator is that every I/O member declares `throws IOException` and exactly five do not. It stays a DENYLIST because two counterexamples exist and are named in the comment (`installedProviders()`, JDK 20's `exists()`), which is the right reason to keep one. **B3 (SSLServerSocket option setters, `getOption`/`setOption`/`supportedOptions` charged `Net`) fixed AND the arms converged**: five hand-copied `method.equals` chains became three predicates defined once and shared by all six socket owners. The agent's reasoning is the §G point exactly — *fixing the TLS acceptor alone would have converged one pair of arms by splitting another* — so it fixed the family. Wire boundary unchanged, pinned by a 17-member control. **B4, the pre-existing published clock sin, fixed and coordinator-verified**: `new GregorianCalendar(TimeZone/Locale/both)` reads the wall clock and was certified pure. Executed — the TZ ctor returns values 1.1s apart while `(y,m,d)` returns identical ones — and measured at the pre-image jar `viaTz` is **ABSENT**, at the fix `['Clock']`, with the fixed-date control staying ABSENT in both (no over-charge). **Arity was never the discriminator; argument KIND is** — which is what the old ARITY-PRECISE comment got wrong. **B1 WAS DELIBERATELY NOT NARROWED, AND THE REFUSAL IS THE BEST RESULT OF THE FOUR.** A wrapper over a real child reaches its capability **through that supertype walk and nothing else** — `calls: None`, because the JDK body is never scanned — so narrowing would have traded a loud over-charge for a SILENT UNDER-REPORT. Measured, not argued. The false clause is rewritten to state its assumption (§E2) and both directions are pinned. §9 sweep: the same shape reaches `FileSystemProvider` and `RandomGenerator` (pinned); `ProcessHandle` is unaffected. **Premise correction:** B1's fabrication class is NOT new — `destroyForcibly`, also inherited-concrete, was already charged at `f9b5ba5`; `db9d918` widened it from one member to eleven. **The instrument was calibrated before it was trusted:** the `2dd1600 -> d8e953c` A/B arm reproduces the earlier fix's numbers EXACTLY (577,547 common, ADDED 107, REMOVED 0). Then `d8e953c -> HEAD`: ADDED 254, REMOVED 118, CHANGED(wide) 1,839 over 8 fields, two mechanisms (+117 Clock gains, -118 from the narrowings). **All 146 lost rows audited in FULL from `javap` — 102 direct-clean, 44 transitive, 0 suspicious, 0 unexplained** — and all 37 direct Clock gains confirmed against their own bytecode. **The agent's own audit tool had a bug it caught and fixed:** its `javap` regex missed the quoted `"<init>"` spelling, so the first Clock pass reported 0 of 37 confirmed. Both audits re-run. Gates: `test` 953/0, `smoke.sh`, `soundness/run.sh 40`, `reentrancy.sh`, `verify-local.sh` all OK. **`ci/self-gate.sh` RUN BY HAND — exit 0, "no violations"** — because `gate-run.sh` structurally cannot see inside a `run: \|` block, and this is the gate from CLAUDE.md's four-red-commits story. `nativeCompile` UNRUN and **verified to fail identically at the merge-base** rather than assumed. |
| R131 java: the receiver-type mechanism behind R130 is NOT fixed — only its thirteen instances | 2026-09-02 | **CLOSED — candor-java `18752cb`. The supertype walk now reaches EXTERNAL owners, denylisted by the CLASSIFIER's own rule shape rather than by a hand list.** Originally filed: OPEN — named by the agent as the highest-value open item, with the fabrication risk already measured** **CLOSED — candor-java `18752cb`.** | **MECHANISM, java. R130's java half fixed thirteen symptoms; the rule that produced them is unchanged.** The supertype re-classification at `Candor.handleMethodInsn` is gated on `ctx.byName.containsKey(min.owner)` — project owners only — so **no JDK subtype of a modelled type ever gets the supertype walk**. Every future JDK subtype of an already-modelled type is silent by default, which is why this shape has now been patched by hand four times (`MulticastSocket`, `SSLSocket`, Conscrypt, and R130's thirteen). | Not fixed, and **the reason is a measured trade rather than difficulty**. Extending the walk to external owners would catch the next sibling automatically, but it is a hot-path change whose fabrication risk was sampled and NOT A/B'd in budget: `javax.sql.rowset.CachedRowSet` extends `ResultSet` (would charge `Db` on a DISCONNECTED rowset — a fabrication), and `LineNumberReader`, `DataInputStream`, `GZIPInputStream`, `ZipInputStream`, `JarInputStream` and `CipherInputStream` all extend the filter-stream `Unknown` rule (would flood `Unknown`). **Found by the same sweep and filed here rather than fixed, analysis-only:** those `java.io.*` filter-stream subclasses are **silent-pure where the rule INTENDS `Unknown`** — a gap in the opposite direction from the flood risk, in the same types. Also `javax.management.loading.MLet`, `com.sun.net.httpserver.HttpsServer`/`HttpsExchange`, `javax.script.AbstractScriptEngine`, `java.util.prefs.AbstractPreferences`. `sun.nio.fs.*` not examined (needs `--add-exports`). **The decision this needs is not "fix or not" but WHICH DIRECTION TO FAIL IN**, and it is a denylist question ([[candor-denylist-over-allowlist]]): extend the walk and carve out the measured fabrication sources, rather than continue enumerating subtypes by hand — the enumeration has now been wrong four times and each round found the previous boundary drawn around its own trigger. **CLOSED 2026-09-24, candor-java `18752cb` — AND THIS ROW'S NAMED FABRICATION SOURCE WAS WRONG.** `CachedRowSet` is **not** a fabrication source: `next()`/`execute()` are ALREADY `Db` at HEAD via the direct `javax.sql.*RowSet` rule, and the non-cursor verbs classify null at the subtype and at every supertype — all four cells unchanged across HEAD / naive / final. **The real source is `SSLServerSocket extends ServerSocket`**: whole-owner `Net` minus a carve-out list that could never have named `setNeedClientAuth`/`setUseClientMode`, because those are declared only on the SUBTYPE — so every TLS server's setup code would have become a `Net` violation. Caught by `SecondSpellingRouteTest#control_sslServerSocketConfigStaysPure`, **already in the tree and RED against the first cut**. So the denylist is asked of the CLASSIFIER — skip a supertype rule that is owner-blanket rather than verb-gated, probed by classifying an impossible method name at the same descriptor — rather than enumerated by hand, which would have been the fifth enumeration of this list. **Zero gains lost to it.** **A SECOND DEFECT CAUGHT BEFORE IT SHIPPED:** the first cut assigned `effect`, which gates `crossDepJoin` (cross-jar inheritance AND `untypedDepReceiver`'s disclosure, on this exact external-owner path), `externalStreamUtility` and `entryAbstractStream` — it would have REMOVED disclosures while its own comment claimed it could only add. The final version unions into `dir` and never assigns `effect`: additive structurally, not by assertion. **A/B, `bin/corpus-ab.py`, 170 real jars / 111,307 classes / 864,426 analysed units: ADDED 372, REMOVED 0, CHANGED 3648; 1,784 rows gained `Unknown` (0.206%), 24 gained `Fs` (0.0028%), 0 rows LOST an effect; REACH 301 hits over 68 distinct JDK members / 23 types in 53 of 170 entries.** Ships under the standing cost rule (0.217% total, well under ~1.5%, REMOVED 0, reach measured, instrument not new). The silence it closes is the `java.io` DECORATOR family — 66 of the 68 reached members — where the engine's own rule says `Unknown` and the subtype spelling read pure. **The 24 `Fs` rows are the only positive claims and were audited IN FULL from bytecode via `javap`, never from candor's own report:** `XmlError.<clinit>` really does `invokestatic java/util/PropertyResourceBundle.getBundle`, and `PropertyResourceBundle` declares no `getBundle` at all (javap: 0, against `ResourceBundle`'s 8), so the JVM resolves `ResourceBundle.getBundle` which loads a `.properties` (23 rows, xmlbeans); `ChildFirstClassLoader.getResource` → `invokespecial java/net/URLClassLoader.getResource` (1 row, kafka-clients). Native parity checked against the generated `jdk-supertypes.idx.gz` directly (26,771 entries), all 12 probed types present. **NOT examined:** `sun.nio.fs.*` (needs `--add-exports`), and CHAINED-dependency types — `transSupers` deliberately does not consult `depSupers`, so the walk cannot fire for them. |
| R132 swift: `Kingfisher#MemoryStorage.Backend.isCached` resolves to the WRONG nested `Backend` | 2026-09-02 | **OPEN — PRE-EXISTING, found while measuring R130 and explicitly NOT introduced by it** | **MIS-RESOLUTION, swift.** `MemoryStorage.Backend.isCached`'s only recorded call edge is `DiskStorage.Backend.value(String,ExpirationExtending)` — **the wrong `Backend`.** Two nested types share the simple name; brief §F1 q7 (a key two paths spell differently), one level in. It already carried that edge's `Unknown` before the R130 fix, so the fix only made the consequence visible by turning that `Unknown` into a concrete `Fs`. | Not fixed and **NOT FULLY DIAGNOSED, which is why it is filed rather than guessed at**: it is not a union (only ONE candidate is recorded), so it does not look like the documented `qualBySimple` over-approximation — it looks like a wrong SINGLE resolution, which is a different and worse thing. The direction matters: an over-approximating union is sound-but-imprecise, whereas picking one wrong candidate can move an effect onto a function that does not have it AND off one that does. **Establish which of the two it is before proposing any fix.** |
| R133 four-way: an `invisible` disclosure arms NO policy form, so a true statement reaches no verdict | 2026-09-02 | **CLOSED as a DOCUMENTATION gap, not a defect — Tom's ruling 2026-09-03, now stated in SPEC.md's `invisible` clause** | **INSTRUMENT / SPEC GAP, possibly four-way.** Measured while verifying R130's ts half: rows whose effect could not be seen carried **`invisible: ['undici-types']`** — a correct, specific disclosure naming the package — while `inferred: []`, and **every policy form exited 0, including `deny Net Unknown`**. The report told the truth and no gate could act on it. That is the [[candor-completeness-manifest]] question one level out: ⟨0.21⟩ made `unanalyzed` produce a fail-closed INCOMPLETE verdict, and `invisible` appears not to have the same force. | Not yet investigated. **Three things to establish before proposing anything:** whether `invisible` is SPECIFIED to arm a gate (it may be correctly advisory, in which case this row is a documentation gap rather than a defect — check SPEC before the code); whether the other three engines emit `invisible` with the same non-consequence; and whether any conformance PART pins the intended behaviour. **Check which side the contract is on FIRST** ([[candor-theory-spec-verification]]) — a theory wrong in the STRICT direction produces a finding shaped exactly like a real defect, and this row is exactly that shape. **RULED by Tom 2026-09-03: `invisible` stays NON-GATING, as SPEC.md:1378-1388 already specifies and PART 4s pins ('invisible OR Unknown'); no engine arms a gate on it. Closed as a documentation note — the row's question was a spec-design decision, not a defect. Making it gate would be a new NON-ADDITIVE rung (the ⟨0.21⟩ `unanalyzed` precedent) and is not planned.** **The measurement stands and was never disputed: an `invisible` disclosure arms no policy form. What was wrong was reading that as a defect. SPEC.md's `invisible` clause now says so explicitly — non-gating BY DESIGN, not by omission; it is a COVERAGE disclosure, and the ⟨0.21⟩ `unanalyzed` manifest is where an uncovered scan reaches a verdict; an engine wanting such a call to gate must take the `Unknown` posture the same clause already offers. Written in lowercase deliberately: it documents the existing design and adds no requirement, so the MUST ledger is unchanged at 529 statements. The row is left in the register rather than deleted so the next person to measure the non-arming finds the ruling instead of re-filing it.** |
| R134 swift: an unqualified implicit-self call to an INHERITED member resolves to NOTHING, under a clean bill | 2026-09-02 | **CLOSED — candor-swift `a42c0d7`. Reproduced by the agent with its own executed SPM package. THE ROW CONTRADICTED ITSELF AND I QUOTED THE STALE HALF INTO ITS BRIEF** | **CARDINAL SIN, swift, and the largest thing this review round found.** `class Sub: Base { func caller(_ p: String) { wipe(p) } }` where `wipe` is inherited from `Base`: `Sub.caller` is **ABSENT** from `functions[]`. Reviewer's ground truth EXECUTED as an SPM package (`probe exists AFTER caller(): false` — the file is really deleted). Isolated matrix: `deny Fs Sub.caller` **0**, `pure Sub.caller` **0**, `deny Unknown Sub.caller` **0**, `deny Fs Unknown Sub.caller` **0**; blanket `deny Fs` 1 only INCIDENTALLY via `Base.wipe`, never naming the caller. **And the engine issues its clean bill over it** — `candor: nothing hidden — every effect sits where its name says it should.` The same false-absolute shape as R79, over a provable file deletion. **Discriminating control, exactly one variable:** the identical file with `self.wipe(p)` gives exit 1 and `calls: ['Base.wipe']`; an own-class `wipe()` with no inheritance also exits 1. So it is the implicit-self spelling ACROSS AN INHERITANCE EDGE and nothing else. Nine shapes built and typechecked; **seven ABSENT** (subclass/inherited across two files and one file, `extension Base` method, protocol-extension default, generic base, two-level `Base -> Mid -> Sub`, `extension Sub`), two correct (`super.wipe()`, protocol-extension witness). | Not fixed. **Mechanism named at the site:** `Driver.swift:1837`'s unqualified arm resolves against `freeFnByName`, `localTypes`, an overloaded sibling, and finally `byQual.contains("\(ep).\(call.leaf)")` — **the enclosing type path ONLY, no supertype climb.** **Two sibling paths answering the same question DO climb**: `super.` (`Driver.swift:1651/1679`) and the property-accessor path, whose own comment at `:1510` reads *"Climb `supertypesOf` exactly as the method-call path does"* — a comment asserting a parity that does not exist (§E2), on top of §F1 q3 drift. **And SOUNDNESS R22 already fixed this exact hole for property accessors reached through an EXPLICIT receiver** (SOUNDNESS.md:376). The implicit-self spelling was never tested and kept the hole — §A.2, an audit boundary drawn around its own trigger, two years of rows apart. **This is what makes R135's sibling fabrication (F4) reachable**, and it is a release-note decision rather than a regression: it is already in the published artifact. **RE-MEASURED 2026-09-04 and NEWLY REPRODUCED — the row previously recorded that it had not been.** `Sub.caller` (implicit self, INHERITED `wipe`) is absent from `functions[]`, while `self.wipe`, `super.wipe` and a same-class control are all correctly `['Fs']`. Ground truth executed: the file is really deleted. Whole gate matrix exits 0 with 'matched NO function' — `deny Fs`, `pure`, `deny Unknown`, `deny Fs Unknown`, all scoped to `Sub.caller`; blanket `deny Fs` exits 1 incidentally, never naming the caller. Fixture `scratchpad/stale-engines/``swift/r134b`. **13 of 18 executed spellings were silent, not one:** two-level `Base->Mid->Sub`, a generic base, an `extension Base` member, a protocol-extension default (struct and via a class), a caller in `extension Sub`, an inherited `class func` from a static caller, a call inside a closure, an argument-labelled call, a protocol requirement witnessed on the base, a conformance on `extension S: P`, and a nested `enum Outer { class S: B }`. Already correct: `self.`, `super.`, same-class, inherited property, inherited subscript. **Mechanism:** `Driver.swift`'s unqualified-call chain keyed every arm on the ENCLOSING type with no supertype climb. The fix climbs `supertypesOf`, **local-only and precise-or-nothing — deliberately no `Unknown` fallback onto an external base**, or `class MyVC: UIViewController { print(x) }` would charge `dispatch:UIViewController.print`. Overloaded inherited members route through `matchOverloads`, because plain `resolveQual` returns empty for a signature-suffixed qual — which is the R32/R44 sin the fix would otherwise have reintroduced. **The clean bill is gone**, and that was part of the defect: it now prints *"most surprising reach — `Sub.caller` performs Fs, 1 hop away via `Base.wipe`"*. A/B over 10 fresh clones, 12,925 rows: ADDED 85 / REMOVED 0 / CHANGED 214 wide; reach counted INSIDE the changed arm, 648 hits, **zero in four of the ten packages — safety-only there and said so**. Ground-truthed from library source (Alamofire `_response`, `asInputStream`, two-level `didFailToCreateUploadable`; nio `Socket.write`; RxSwift `DefaultIfEmptySink.on`), and **77 Alamofire rows gained `Net`+`netClass` — the `deny Net`-over-Alamofire recall.** **ROW HYGIENE DEFECT, and it cost me:** the status cell still said *"Coordinator has NOT independently reproduced this one"* while a later cell said *"RE-MEASURED 2026-09-04 and NEWLY REPRODUCED"*. An update landed in one cell and not the other, the row asserted two things, and I wrote the stale one into the agent's brief. **When a row's status changes, the STATUS CELL is the one that must change.** |
| R135 swift: `flock` is on the raw-syscall allowlist AND is a Darwin struct, so building the lock record reads Unknown | 2026-09-02 | **CLOSED — candor-swift `be4c11b`. FABRICATION regression from `800a281`, never published. Coordinator-verified at both ends** | **FABRICATION, swift, and it is the R130 commit failing its own §9 rule.** That commit carries a *"DELIBERATELY STILL ABSENT"* block excluding `stat`/`lstat`/`fstat`/`statfs` **because they are also Foundation STRUCTS** — and then adds `flock`, which is the fcntl advisory-lock record and is spelled `var fl = flock()` in idiomatic code. The exclusion rule was identified and not swept across the names the same commit added. Coordinator-verified, ground truth EXECUTED (`describeLock() = 3 — touches no fd and no path`; the function only builds a struct and returns a field): at published `e5c7871` `describeLock` is ABSENT with `deny Unknown describeLock` **0**; at HEAD it reads `['Unknown']` + `unknownWhy: ['native:flock']` and **`deny Unknown describeLock` exits 1** — a gate failing on code that performs nothing. **The reviewer calibrated the instrument before trusting it**, which is why the scope is credible: it probed all 84 names in `NATIVE_DISCLOSURE_C_FREE_FNS` with `func probe(_ x: <name>) {}` + `swiftc -typecheck`, and **exactly ONE of the 84 is also a type — `flock`** — with the probe correctly firing on `stat`/`statfs` (the two already excluded) and not on `lstat`/`fstat`/`open`/`close`/`read`/`write`/`remove`/`kill`/`link`. | Not fixed. **The `!argLabelled` narrowing cannot help** — a struct construction carries no labels either, so `flock()` and `flock(fd, LOCK_EX)` produce byte-identical rows. Two candidate fixes, and they differ in what they cost: **drop `flock` from the list** (loses disclosure of the real `flock(fd, op)` syscall, which the reviewer confirmed exists in real code — swift-tools-support-core `Sources/TSCBasic/Lock.swift:146,166`); or **gate the arm on argument COUNT**, since libc `flock` takes two and the struct construction takes none. The second preserves recall and mirrors the existing `argLabelled` flag, which already defaults to the disclosing direction. **The list should be audited for zero-argument entries before the count gate ships** — 'no listed function is zero-arg' is itself a §E2 safety assertion and must be measured, not asserted. **Honest bound the reviewer volunteered:** no corpus instance was found. TSC's two `flock` hits are genuine `flock(fd, …)` calls, checked in a fresh clone. **This is a latent fabrication with an executed repro, not a measured corpus regression** — and the Linux half (glibc also declares `struct flock`) is ANALYSIS-ONLY, since the type probe ran on macOS. **CLOSED — AND THE FIX I PROPOSED WOULD HAVE INTRODUCED A SILENT UNDER-REPORT.** I briefed an argument-count gate and asked for the list to be audited for zero-arg entries first. **They exist:** `fork` and `vfork` are `pid_t fork(void)` (MacOSX.sdk `unistd.h:459,620`; glibc `unistd.h:778,786`). A blanket `argc > 0` gate silences a real `fork()`. The shipped fix is arity **plus a measured nullary exemption set** (`NATIVE_DISCLOSURE_C_NULLARY_FNS = ['fork','vfork']`), and dropping `flock` was correctly rejected because TSC's real `flock(fd, …)` calls would have gone silent. **THE REVERT TEST WAS RUN TWICE AND THAT IS WHAT PROVES THE EXEMPTION EARNS ITS PLACE:** with the fix removed, 3 rows red (struct-not-syscall, the gate verdict, label-vs-arity); with the *tempting simplification* of a blanket `argc > 0`, a **DIFFERENT** row red — `spawnChild` returned `['native:waitpid']` alone with `fork()` gone silent — while all three of the first arm's pins stayed green. Two failure modes, disjoint test coverage, measured rather than argued. **THE AGENT'S OWN INSTRUMENT WAS BROKEN IN THE FLATTERING DIRECTION AND IT CAUGHT IT.** Its first type-probe nearly reported *"no zero-arg entries"*: `fork` returns rc != 0 on macOS because Darwin marks it **unavailable**, which is indistinguishable from *"needs an argument"* unless you parse the diagnostic text. That is the instrument-that-cannot-fail class caught by an agent on itself, and it is the only reason the nullary exemption exists. Audit re-derived on BOTH platforms rather than trusting the handed-over number: 84 names probed under macOS `import Foundation` and Linux `swift:6.1` `import Glibc`. **`flock` is the only name that is also a TYPE, on both** — glibc declares `struct flock` in `bits/fcntl.h`, so **the Linux half is now EXECUTED, not analysis-only**. No allowlisted name resolves with only the Swift stdlib in scope (84/84). Enum-case, static-member and project-local type collisions all measured clean. **Correction to R130's own comment, found by this audit:** its stated reason covers `stat`/`statfs` and **not** `lstat`/`fstat`, which are functions only. All four are admissible under the arity gate and are deliberately left out as a stated under-report needing its own control. Coordinator-verified independently at the fixed HEAD, three arms in one fixture: `buildRecord` (pure struct) **ABSENT** with `deny Unknown` exit 0; `takeLock` (`flock(fd, LOCK_EX)`) still `['Unknown']` + `native:flock`; `spawnChild` (nullary `fork()`) still `['Unknown']` + `native:fork`/`native:waitpid`. **My own fork arm did NOT execute** — Darwin marks `fork()` unavailable, the same diagnostic that broke the agent's probe — so my fork evidence is static and the executed evidence is the agent's Docker run (a real fork reaped with status 7). A/B **stated as SAFETY-ONLY up front rather than discovered later**: 17 packages, 20,929 common rows, wide key, ADDED 0 REMOVED 0 CHANGED 0, with §E1 counters from a third instrumented binary calibrated to fire SUPPRESS/RESCUE/PASS first — **83 arrivals, 83 PASS, 0 SUPPRESS, 0 RESCUE**, so the corpus cannot reach the change. Recall hunt: 34 textual zero-arg calls, all resolving to local declarations; near-miss reported, SwiftPM's apparent `flock(fd, LOCK_EX)` sites are inside a generated-source string literal. Gates 11/11, `swift test` 1054/0 on macOS AND on Linux, both macOS-SELFSKIP gates run under Docker. **A FOUR-WAY QUESTION THIS RAISES, not yet asked:** *is any name on your raw-syscall/native-boundary allowlist also a TYPE, or callable at a different arity than the C function?* rust, java and ts all carry equivalent name lists. |
| R136 ts: a `WebSocket` SUBCLASS with no explicit constructor reads PURE under `lib.dom` | 2026-09-02 | **CLOSED — candor-ts `2a192b0`. Verified by the coordinator. The widened sweep found a SECOND victim 11 lines away** | **CARDINAL SIN, ts, and it is R130's own fix landing in one arm of two.** The declaration-walk (`declaredCtorClassName` -> `ctorRuleName`, `scan.mjs:6456-6481`) was added to the **κ arm only**; the es-lib arm still keys a construction on the CALL-SITE IDENTIFIER (`unaliasGlobal(node.expression).getText()`, `:6347-6350`), so a subclass name never matches `CONNECTING_WEB_CTORS`. `class Y3 extends WebSocket {}` + `new Y3(u)`: under `lib:['ES2022'],types:['node']` it reads `['Net']`+`incomplete`; under `lib:['ES2022','DOM']` it is **ABSENT — no `invisible`, no `Unknown`** — and `deny Net src.y3_implicit` goes **1 -> 0**. Identical sources, identical `node_modules`, tsconfig differing only in `lib`; both arms `tsc --noEmit` clean. Ground truth EXECUTED on node 22.12.0: the subclass construction fires exactly **one real upgrade handshake** against an `http.createServer` listener. Controls: the explicit-constructor sibling `class Y2 extends WebSocket { constructor(u){super(u)} }` charges `Net` in BOTH arms (R130's `super` arm WAS added to both), and `class Y1 extends Headers {}` stays absent in both (no over-charge). | Not fixed. **Fifth instance of the tsconfig-split class** (R109/R110/R111/R130). The standing lesson is now doubly earned: agreement between the two paths is worthless as a signal, AND **a fix landing in one path is invisible to any test that exercises the other**. This is the live consequence of R137's `F4` finding — the commit asserts `CONNECTING_WEB_CTORS` is *"read by the es-lib arm AND by the κ rules … so the arms cannot be widened separately"*, and for the CLASS-NAME half that is false: `scan.mjs:6244` compares two string literals rather than testing the shared set. §E2, in the commit that needed it to be true. **RE-MEASURED 2026-09-04: STILL OPEN, and the severity SPLITS by lib exactly as R109/R110 did.** Under `lib.dom` — the configuration every browser project uses — `y3implicit` is ABSENT and `deny Net y3implicit` exits 0; under `types:[node]` the same source reads `['Net']` + `incomplete:['Net']` and the gate exits 1. Explicit-constructor and non-socket controls correct in both arms. Ground truth executed: a real `http` server observed the upgrade handshake fire. Fixtures `scratchpad/stale-engines/``ts/r136/{dom,node,exec}`. **Closed, and widening past the row's own trigger paid immediately.** The es-lib arm keyed a construction on the CALL SITE's text; a class with no constructor inherits one, so `getResolvedSignature()` returns the BASE's signature while the call site still says `Y3`. R130's `declaredCtorClassName` walk existed on the other arm and the `super(...)` branch, never here. **Second victim, not assigned and found by sweeping the mechanism rather than the instance:** `new Date()`'s clock read tests `getTypeAtLocation(node.expression)?.symbol?.name === "DateConstructor"` — also the call site — so `class MyDate extends Date {}` read PURE in BOTH arms. Closing it required teaching `declaredCtorClassName` the INTERFACE spelling (`interface DateConstructor { new(): Date }`) beside lib.dom's anonymous type literal. Verified by me at `5377eb6` -> HEAD: both `extends WebSocket` and `extends Date` ABSENT -> `['Net']` and `['Clock']`, with `new Date(0)` and a project-class subclass unchanged. **Over-charge control is instrumented so the zero is a measurement:** `extends Headers/Map/Error/URL` etc. drive the new branch **7 times** and gain nothing, no `Unknown` hedge. A/B: npm 194 targets / 21,736 rows ADDED 0 / REMOVED 0 / CHANGED 5; TS source 12 trees / 2,094 rows ADDED 1 / CHANGED 1. All 6 ground-truthed (`@supabase/phoenix` `new this.transport(...)` over `global.WebSocket`; socket.io's `WS.createSocket` was ABSENT ENTIRELY and its caller gained Net). **Keyed on `inferred` alone the npm A/B prints "0 rows changed"** — the wide key is what made it visible. Revert-red: 22 rows. |
| R137 ts: two member-precise κ rules delete the BLIND-PACKAGE disclosure for the whole of `undici-types` | 2026-09-02 | **CLOSED — candor-ts `eeb274e`. FILED AS A DISCLOSURE REGRESSION introduced by `c4ae525` (the R130 fix). Never published** | **LOST DISCLOSURE, ts.** `kappaKnows` is PACKAGE-granular — `KAPPA_RULES.some(([mre]) => mre.test(moduleName))` (`scan-core.mjs:801`) — and never consults the member regex. Adding two `undici-types` rules flips it `false -> true`, switching off the `!kappaKnows(pkg)` disclosure gate (`scan.mjs:4867`) for **every member of the package**. The commit's rationale (*"MEMBER-precise, not whole-module"*) is true of the RULES and buys nothing for the COVERAGE answer. Measured over a 21-file fixture of the `undici-types` globals `@types/node` re-exports, `02cd0a5` vs `c4ae525`, tsc-clean: **5 rows moved from `invisible:['undici-types']` to entirely ABSENT** — `FormData.append`, `FormData.getAll`, `WebSocket.addEventListener`/`removeEventListener`, `EventSource.addEventListener` — carrying no `Unknown` and no `invisible`, **which is a positive purity claim under SPEC §2 rule 3**. Eleven further rows lost `invisible` but kept `Unknown`. **And the report-level `coverage: {uncovered:[{name:'undici-types',calls:16}]}` block disappeared**, taking the stderr advisory with it. `coverage.uncovered` is a CONSUMED VERDICT SURFACE, not cosmetic — `query-core.mjs:376/:1278`, `query.mjs:2578` (`gate --report`, `gains`, `coverageDelta`). | Not fixed. **No effect is hidden TODAY** — every member the reviewer could reach is inert or already `Unknown` — so this is a lost disclosure rather than a demonstrated sin. But the coverage claim is now false for the package's whole surface, and it is false in the direction that reads as reassurance. **The commit's own A/B framing is the §E1 trap:** *"all 62 still carry `Unknown`"* is true of those 62 and not true in general — the five rows above carry nothing, and a narrow key would never have shown it. **CLOSED 2026-09-24, and THE ROW'S OWN PREMISE WAS STALE IN BOTH DIRECTIONS.** It says the trigger was 'unpushed `c4ae525` … never published': `c4ae525` IS an ancestor of candor-ts HEAD and shipped in **v0.35.0**, so the regression WAS published — and it was fixed in the SAME release by `eeb274e` (v0.35.0~3, one commit after the v0.35.0~4 that caused it), which the row never recorded. VERIFIED AT HEAD, not taken from the commit message: `kappaKnows` now takes the member and is consulted member-precisely, and `test.mjs:17277` pins it with the question stated as 'does kappaKnows consult the member AT ALL' rather than 'is this member effectful'. Found by [[R588]]'s hygiene pass; the closing evidence was re-derived here rather than imported from the reviewer that named it. |
| R138 ts: a relatively-imported project shim under its own `package.json` is charged Net | 2026-09-02 | **CLOSED — candor-ts `eeb274e`. FILED AS A FABRICATION REGRESSION introduced by `02cd0a5` (the R121 fix). Never published** | **FABRICATION, ts.** R121 replaced a head-identifier guard with a declaration-keyed one whose escape is `crossesPackageBoundary` — and that escape **overrides the property the deleted guard's comment promised**: *"the module SPECIFIER decides, not file-set membership … even when the scan's shape leaves `./my-mock` outside `projectFiles`"*. Two trees differing by **exactly one 1-line file** (`vendor/package.json`), same `vendor/shim.d.ts` and same pure `vendor/shim.js`: `deny Net src.main` is **exit 0 at `e5c60bc` and exit 1 at HEAD** when the `package.json` is present, and exit 0 in both when it is absent. Ground truth EXECUTED against an `http.createServer` listener on 127.0.0.1: **`REQUESTS ARRIVED = 0`** — both calls return the canned body and nothing touches the network. | Not fixed. **Scoped honestly by the reviewer rather than overstated:** the bare-specifier monorepo/workspace variant (`@mono/mock` symlinked, `dist/index.d.ts`) fabricates on **BOTH** arms — `Net` + `incomplete` + `invisible`, `deny Net` exit 1 at `e5c60bc` AND at HEAD — so that half is PRE-EXISTING and untouched by R121. **Only the relative-import-across-a-`package.json` shape is new.** That distinction is what makes this actionable: the fix must not simply delete the escape, which the R121 work measured as load-bearing (deleting it reds exactly one row, an `import * as nf from 'pkg'` spelling that goes silent without it). **CLOSED 2026-09-24 — same commit, same stale premise as [[R137]].** `02cd0a5` is an ancestor of candor-ts HEAD (v0.35.0~5), so 'unpushed … never published' is wrong; `eeb274e` fixed it in the same release and the row never recorded that. Its commit body names the direction: 'R121's escape fabricated Net over a stray package.json'. |
| R139 rust: a local `macro_rules!` DEFINITION body is invisible to `count_ident`, so R119's promotion still kills the effect | 2026-09-02 | **CLOSED — candor-rust `5f33219`. Was a RELEASE BLOCKER; introduced by `27ad9d1`, not closed by `663aa96`, never published. Coordinator-verified at both ends** | **CARDINAL SIN, rust, and it is the sharpest finding of this round because R119 was written to close exactly this class and left one position.** R106 made a body-declared item shadow an import; R119 narrowed that to *promote only when every occurrence of the ident lies inside the declaring block*. **`count_ident` counts macro INVOCATION token streams but never a local `macro_rules!` DEFINITION body** — while candor's own `local_macros` machinery re-injects that body into the function. The counts come out equal, the shadow is promoted function-wide, and the effect inside the expansion is killed. Coordinator-verified, ground truth EXECUTED (`spawned=true` — `/usr/bin/true` really runs): a crate with `macro_rules! spawn_it { ($p:expr) => { Cmd::new($p).status().is_ok() } }`, a nested-block `struct Cmd`, and `spawn_it!(p)` reads **`['Exec']` on the published `bffac6c` and ABSENT at HEAD**, with `deny Exec`, `deny Exec f` and `pure f` all **1 -> 0** (`deny Unknown` correctly 0 both ways). Reviewer pinned the mechanism rather than reasoning it: `CANDOR_ALIAS_DEBUG=1` prints `BODYSHADOW f :: Cmd` on the fixture and nothing on the one-variable rename control; adding a single outer mention makes the counts differ, blocks the promotion and restores `['Exec']`. **Sixteen of seventeen positions are fixed; this is the seventeenth.** | Not fixed. **§E2, and the assertion is load-bearing:** `count_ident`'s own doc (`decls.rs:419-424`) asserts *"UNDERCOUNTING IS THE DANGEROUS DIRECTION … every position an identifier can occupy has to be reached."* **That sentence is false, and it was written by the commit that needed it to be true.** **Why no instrument caught it, which is the reusable part:** the 334+77 suite is green with the defect present, and **R119's corpus A/B could not have seen it** — this shape produces no `BODYSCOPE-ADD/DROP` because it is identical under the flattened and promoted collectors, so it sat inside the `CHANGED 0` the A/B read as quiet. A zero-diff over a shape the counters cannot distinguish is not evidence. **Prevalence: 0 instances in the 1490-crate registry**, so it is low-frequency — and it is still a regression against the published binary, in the sin direction, in the release candidate. **CLOSED — AND MY STATED MECHANISM WAS WRONG, in a way that decided the fix.** I briefed *"`count_ident` counts macro INVOCATION token streams but never a local `macro_rules!` DEFINITION body"*. **A `macro_rules!` written INSIDE the function body IS reached** — `Stmt::Item(Item::Macro)` -> `visit_macro` -> `in_tokens`, measured, shadow correctly dropped at both PRE and FIX. The real mechanism is that the definition `local_macros` re-injects sits at **FILE level, frequently in ANOTHER FILE**, so it is not in `block` at all and **no deeper AST descent could ever have reached it**. That is why the fix consults `local_macros` instead of widening the walk — a fix built on my wording would have walked harder and found nothing. Sixth brief this session corrected by measurement. Fix at `decls.rs:466-560` / `collector.rs:3800`: `count_ident(block, name, local_macros)` expands a bare `NAME!` through **`collector::macro_template_blocks`, the collector's OWN helper**, made `pub(crate)` rather than reimplemented (§G). Where that helper is opaque (multi-arm, `$(..)*`) the raw arm tokens are counted — a deliberate over-count, decoupled so this count's soundness does not depend on a precision decision in another file. Counting is SYMMETRIC, so a macro invoked inside the declaring block is still promoted (`macro_rules!` is unhygienic for items and types). The recursion guard is push/pop, **not** a once-per-count seen set, and that distinction is now pinned by a test. **§9 — 18 positions audited, every fixture compiled and RUN, with ground truth taken from a marker file touched by a spawned script rather than the function's return value** (three arms return `true` for reasons unrelated to spawning — an instrument choice that prevented three false passes). **Six were silent at PRE and are correct now**: a file-level macro, a macro in ANOTHER FILE, a template invoking a second macro, an invocation nested in another macro's opaque arguments, a REDEFINED macro name, and a self-mentioning template. Already reached: plain expressions, invocation tokens, attribute token lists, `cfg_if!` arms, `#[cfg]`-gated code. Not reached and nothing injects them: doc comments, string literals, `include!` text. **THE INSTRUMENT POINT WAS ADDRESSED, AND IT CHANGED THE READ OF THE A/B.** New `BODYMACRO-REACH`/`BODYMACRO` counters, proven output-inert over 62 crates, exist because **R119's `BODYSCOPE-ADD/DROP` compare against the flattened collector, which gives this shape the SAME answer** — R119's A/B could not have emitted anything about it. Corpus A/B, 1490 crates, wide key: 252,772 common rows, **ADDED 0 REMOVED 0 CHANGED 0**, 1490/1490 exit 0, zero zero-byte results. **The branch WAS reached — 6 promotions flip to DROP in portable-atomic — and the agent traced why none moves a row rather than assuming: `cmpxchg16b` is not a key in that file's `use` map, so the pre-fix shadow was already a no-op.** It therefore reports the A/B as an **over-charge control only**, with the executed fixture as the sole recall evidence. That is §E1 applied against the agent's own result. Revert test done BY REVERTING: `r139_a_local_macro_template_counts_toward_the_body_shadow` fails (`left: []`, `right: ['Exec']`). Each test was separately proven able to fail, and the commit records that only the `invoked twice` arm catches a seen-set recursion guard while the monotonicity test does not — stated rather than glossed. **§E2:** `decls.rs:419-424`'s false *"every position an identifier can occupy has to be reached"* is replaced with a checkable property — *every ident the `CallCollector` resolves through the `uses` map while walking this body* — bounded by the fact that `local_macros` is the only `CallCollector` field carrying token-level syntax from outside the body. **[Label corrected 2026-09-03 by the 0.35.0 release panel: `bffac6c` is NOT the published 0.34.0 — the v0.34.0 tag is `22b5ec2`, six commits earlier; where this row says 'published 3cf055d' read 'post-tag 3cf055d'. The row's conclusion was re-checked against the true tag build where the panel could, and stands.]** |
| R140 rust: two same-name `use` items under mutually-exclusive NON-TEST cfgs still resolve by SOURCE ORDER | 2026-09-02 | **CLOSED 2026-09-10 — candor-rust `20e4ac7`; its two named residuals are [[R371]] and [[R372]], both since closed. Previously: REOPENED 2026-09-10, HALF CLOSED. `20e4ac7` unions the arms on the PATH route (`Runner::new(..)`) in either order. It does NOT reach the `fn_alias` spelling (`let f = Runner::new; f("true")`), where THIS ROW'S OWN SENTENCE still reproduces verbatim — see R371 — nor the decl-side type consumers (R372). A row may not be CLOSED while an identical program with the call spelled differently still returns the answer the row was filed against.** The fix also introduced four defects of its own (R370, R373, R374, R375) and its headline A/B measured an alphabetical prefix (R376). NOTE: the port exists and R287's SPEC clause + conformance PART do NOT yet — the family's own "write the row before the port" rule is currently violated and closing that is the next work** | **CARDINAL SIN, rust — R123's sibling spelling.** `use_item_applies`'s doc calls itself *"THE ONE RULE for does this `use` item bind a name in the build we are describing"* and answers only the `test` half. Two crates identical but for the order of two `use` lines, both `cargo run` printing `ran=true`: `#[cfg(unix)] use std::process::Command as Runner;` followed by `#[cfg(windows)] use crate::mockproc::Runner;` reads **ABSENT at HEAD**; swapped, `['Exec']`. Isolated matrix on the mock-last arm: `deny Exec`, `deny Exec Unknown`, `deny Exec run`, `pure run` **all exit 0**. **The feature-gated arm is sharper**: with a DECLARED, INACTIVE `mock` feature, `cfg_eval` can decide the predicate and `use_item_applies` simply never asks. | Not fixed. **Prevalence measured over the same 1490 crates: 221 crates (14.8%), 451 sites** with two or more same-name module-level `#[cfg]`-gated `use` items and no `test` in any cfg — tokio 16, quinn-proto 9, rkyv 8, borsh 8, rustix 8, serde 7, syn 6. Real instance ground-truthed from source: **`similar-3.1.1`** binds `Instant` to `std::time::Instant` (the arm actually active under `default=['std','text']`) and then to `web_time::Instant`; last wins. **THE GROUND TRUTH QUOTED HERE WAS WRONG AND IS CORRECTED 2026-09-10:** this row said the crate reports *"0 `Clock` rows across 159 functions while `deadline_exceeded`/`duration_to_deadline` call `Instant::now()`"*. Re-measured directly at `64a6a95`: **164 functions and 109 `Clock` rows**, with `deadline_support::deadline_exceeded` and `deadline_support::duration_to_deadline` BOTH already carrying `['Clock']`. The crate's actual delta from `20e4ac7` is three rows gaining `invisible:['web_time']`. The real ground-truthed instance of this row's defect is **`sea-orm-2.0.2`** — see R376 — whose `driver::rusqlite::RusqliteSharedConnection::acquire` and 13 propagation targets gained `Clock` from the fix. **Classified honestly by the reviewer rather than inflated:** that instance is misattribution WITH disclosure (`invisible: ['web_time']`, `coverage.uncovered`, rows read `Unknown`), so `deny Unknown` still catches it. **It is fully silent only when the losing arm is a local pure type**, as in the fixture — and how many of the 451 fall on the silent side was NOT measured. The sound answer for `use` is union/hedge, not picking the other arm: the engine already analyses every `#[cfg]` branch for ITEMS. **SPLIT AND HALF-FIXED 2026-09-09 — candor-rust `5588330`.** The row is really two defects and only one of them is candor-rust's to decide. **DECIDABLE half — FIXED.** `use_item_applies` now consults `is_cfg_inactive`, the authority that already existed: a `#[cfg(feature = "x")]` on a DECLARED-and-INACTIVE feature binds nothing in this build, so the arm is dropped and the real one answers alone — precisely, with no hedge. Measured on the feature-gated pair: ABSENT -> `['Exec']`, and `deny Exec`, `deny Exec Unknown` and `pure run_it` all move **0 -> 1** on the losing order while no policy loses a catch. Both orders are asserted in one test, because the defect is invisible in one of them. **UNDECIDABLE half — STILL OPEN, and the fix was BUILT AND BACKED OUT rather than skipped.** `cfg_eval` returns `None` for `unix`/`windows`/`target_os` (a source scan does not assume a target), so a platform pair cannot be decided here. Routing `collect_use`'s three inserts through R105's `record_alias` join DOES make it order-independent — measured, both orders come back `['Unknown']` instead of one reading ABSENT. **But scan.rs adjudicates a joined arm set to `Unknown` + `ambiguous:cfg-duplicated alias`, and SPEC §4's 2026-09-05 clarification says that is wrong twice over**: the effects *"are the union of the arms"*, and `ambiguous:` is reserved for two separately-written definitions. That is **R287**, open and owed a ruling plus a four-way rung. Landing the join would have made a known-NONCONFORMANT behaviour more prevalent — the move R287's own remedy names as *"the boundary-drawn-around-the-trigger error"*. **Cost of backing it out, stated rather than implied: on the platform pair `deny Exec` and `pure run_it` still exit 0 over a real `Command` under one arm order.** This half is now blocked on a ruling, not on effort. **REMOVAL ACCOUNTING, because 199 rows disappear and a wrong removal and a right one are the same bytes.** A/B over 400 registry crates selected by grepping for the surface: **ADDED 6, REMOVED 199, CHANGED 316**. The ADDED are the catch — `hyper-0.14.32` gains `['Clock']` on four functions (`ping::Recorder::record_data`, `client::pool::Expiration::expires`, `ping::channel`, `Shared::update_last_read_at`), exactly this row's `Instant`-binding shape. Of the 199 removed, **186 carry no effects at all**, and the 13 that do were each checked against their crate's OWN `default` list rather than sampled: `der-0.7.10` (10 rows, `Fs`) has **no `default` key at all** so `pem`/`std` are off; `curve25519_dalek` `EdwardsPoint::random` (`Rand`) sits in a `#[cfg(feature = "group")]` impl and `group` is not in `default = [alloc, precomputed-tables, zeroize]`; `hickory_resolver`'s `persisted_state` (`Fs,Log`) needs `serde` plus `__tls`/`__quic` against `default = [system-config, tokio]`; `async_std::PathBuf::from_stream` is `#[cfg(feature = "unstable")]`; `git2`'s `parse_output` (`Log`) gets its `trace!` from a `#[cfg(feature = "cred")] use log::…` against `default = []`. **Every one is a function that does not exist in a default build, so reporting an effect for it was the over-report.** Coverage gate: 0 regressed, 0 grown, 1 oracle-dropped (`reqwest::Part::file`, `#[cfg(feature = "multipart")]` — the classifier RULE is untouched, so a consumer enabling multipart still gets `Fs`). **RE-MEASURED 2026-09-04 on shipped 0.35.0: STILL OPEN, reproduced almost verbatim with the source ORDER as the only variable.** `scratchpad/stale-rust/``r140a`/`r140b`, both of which really spawn a process: with the unix arm's `use` first, `run_it` reads pure and `deny Exec` exits 0 over a real `Command::new("true").status()`; swapping ONLY the two `use` lines charges `['Exec']` and `deny Exec` exits 1 — last-use-wins by source order, regardless of which `#[cfg]` arm is active on the host. **COSTED AND DELIBERATELY NOT FIXED AT THE ⟨0.36⟩ CUT, 2026-09-09 — the fix was BUILT, it DID NOT FIRE, and the reason is the deliverable.** With R287 now ruled (the union is the right answer), the apparent fix is two edits: route `collect_use`'s bare `out.insert` through `record_alias` (which already joins arms — R105 — and which every other alias route uses), and change `scan.rs`'s disagree branch from the `Unknown` hedge to the union. Both were written and compiled on a branch. **Both fixture pairs measured IDENTICAL to HEAD** — external-arm-effectful and local-arm-effectful, both orders — and the engine's own `CANDOR_ALIAS_DEBUG` printed **no `ALIASCOLLIDE` line at all**, so the collision never reached the consumer that was changed. **CAUSE: `collect_use` HAS TWO CALLERS** — `collector.rs:2170` into `local_uses` and `decls.rs:20` into `out` — and the alias substitution that builds a call's `path` runs through the COLLECTOR's map. The edit fixed the copy that is not consulted. This is R213's double-collection shape in a second place. **THE REAL COST, MEASURED:** making `local_uses` hold joined values means every consumer must split on the separator, and there are **34 read sites of those maps in `collector.rs` alone against 17 join-aware sites in the whole crate** — a partial audit produces malformed paths, which land as fabrication or as silence depending on where. Add the SPEC clause and the conformance PART the shape still lacks (R287) and this is a rung with its own review, not an afternoon. **AND THE HEDGE IS NOT AN ACCEPTABLE HALF-FIX**, which the same day's D-B ruling settled: under §4.0 `{Unknown}` is not ⊤, so hedging both orders would close the ABSENT order and BREAK the `['Exec']` order by withdrawing a determined effect. The union is the only sound answer. **A correction to the review record:** the claim that a LOCAL arm gets no call edge does not hold — it charges `['Fs']` whenever it wins the pick. The port is blocked by the double collection, not by local arms. **Next attempt starts here: fix the double collection first, then union.** **CLOSED 2026-09-10 — candor-rust `20e4ac7`, and that instruction was right.** `collect_item_uses` has TWO call sites — `scan_items`, whose map builds call paths, and `collect_decls`, whose map feeds the decl index; the earlier attempt edited the second, which is why it did not fire. `collect_decls` now takes a throwaway. **THE UNION MACHINERY ALREADY EXISTED AND A `use` ALIAS NEVER REACHED IT:** `fn_alias` holds a LIST of targets for a `let`-bound alias and its call site already pushes every one beyond the first as its own edge, so the row carries the union. This is that same loop, ten lines down. **A COMPANION MAP, NOT A JOINED VALUE** — routing through `record_alias` was the obvious fix and is wrong at 34 read sites against 17 join-aware ones, so `uses` keeps exactly the value it always held and the collision is recorded beside it; zero existing readers changed. Controls: two PURE arms stay pure, two arms naming the SAME target do not double-push, a single `use` is untouched. **A/B 600 registry crates, 40,264 rows both arms: `inferred` ADDED 0 / REMOVED 0 / CHANGED 0 — no effect set moves — with 57 rows GAINING an `invisible` disclosure** (a cfg-duplicated alias to two dependency crates now names BOTH blind spots, e.g. `['sqlite_wasm_rs']` → `['libsqlite3_sys','sqlite_wasm_rs']`) and one gaining a call edge. Every movement is disclosure-direction. Falsified: disabling the union fails 5 assertions. Gates 29/29, four-way conformance OK. **R123's KNOWN-SILENT PIN WENT RED WITHOUT BEING AIMED AT** and was inverted on its own terms (A/B first, as its comment demanded); its residual — `#[cfg(test)]` arms joining the union at the two body-local sites, which over-charges where a test mock has effects the production arm lacks — is stated in the test. **AND THE CONTRACT IS STILL MISSING:** R287's §4 clause for BINDINGS and its conformance PART do not exist, so this port currently violates "write the row before the port". It is UNSHIPPED, so the choice is still open between writing the clause and reverting the port. |
| R141 rust: `std::os::unix::process::CommandExt::exec` — `execvp`, which replaces the process image — has no rule | 2026-09-02 | **OPEN — PRE-EXISTING and PUBLISHED, identical on all five builds. R130's audit boundary was drawn around `fs`** | **CARDINAL SIN, rust.** `550d071` found that `std::os::{unix,windows,wasi}::fs` had no rule and closed it — and translated the question ACROSS ENGINES while never translating it **across sibling modules in the namespace it had just found unruled**. §9 in the commit that cites §9. Ground truth EXECUTED: a consumer crate printed `before`, then `EXECED-THROUGH-LIB`, and never reached the following line — the image really was replaced. A library whose only function is `pub fn replace_me(c: &mut Command) -> io::Error { CommandExt::exec(c) }` reports `replace_me` **ABSENT** with `analyzed.count: 1` and `excluded: []` — absence, not exclusion — and **all five policy forms exit 0**, `pure replace_me` included. **The control makes it a CLASSIFY gap rather than a resolution one:** `FileExt::read_at(f, b, 0)` — same UFCS shape, same `std::os::unix::` namespace — reports `['Fs']`. And `fn generic_exec<C: CommandExt>(c: &mut C) { c.exec() }` is also ABSENT **with no `Unknown` disclosure**. Direct classify probe at `98e0a79` and HEAD: `CommandExt::{exec, pre_exec, process_group}` and the windows `CommandExt::*` all return `None`. | Not fixed. The receiver-typed spelling `c.exec()` IS charged, so this is the UFCS spelling specifically — the same one-spelling-of-two shape as R130 itself. **`pre_exec` runs arbitrary code in the child between fork and exec and is equally unruled.** |
| R142 rust: a `macro_rules!` defined INSIDE a function body is never recorded, so its template's I/O is invisible | 2026-09-02 | **CLOSED — candor-rust `90006a1`. Verified by the coordinator against a binary built at `0bbebe5`** | **CARDINAL SIN, rust.** `collect_decls` walks FILE items only, so a `macro_rules!` declared inside a function body never enters `local_macros` and the effects in its template are invisible. Agent-measured with a fixture that compiles and REALLY SPAWNS: `f` is entirely absent from `functions[]` with **no `invisible`, no `incomplete`, no `unknownWhy`**. | Not fixed, and **not a one-line fix — the reason is the fabrication direction.** `local_macros` is a crate-wide NAME-KEYED map, so hoisting body-local macros into it would risk expanding one function's macro inside another function that happens to use the same name. That is a fabrication trade, not an oversight, and it needs the second fixture written first. **RE-MEASURED 2026-09-04 on the SHIPPED 0.35.0 binary (`587275a`): STILL OPEN, and it is a DIFFERENT AUTHORITY from R206.** The coordinator assumed R206 had closed it; it has not. R206 (`13af25c`+`587275a`) added a `body_macros` overlay read only by `lang.rs`'s `note_local_macro_template`, which feeds the `?`-operand drop-safety VETO — and R206's own fixture (a `?`-wrapped case) is charged on this binary. The crate-wide `local_macros` index that `collector.rs`'s ORDINARY (non-`?`) call-edge resolution reads is built by `decls.rs::collect_decls`, which visits file/module-level items only and R206 never touched. R142's shape has no `?` in it at all, so it exercises exactly the untouched path. Fixture `scratchpad/stale-rust/``r142`, ground-truthed: a body-local macro that really calls `std::fs::write` is ENTIRELY ABSENT from `functions[]` — no `invisible`, no `incomplete`, no `unknownWhy` — while a crate-level twin of the same macro and a plain direct call are both charged `['Fs']`; `deny Fs body_local_macro_write` exits 0 over a real write. **Closed with an OWNED, BLOCK-SCOPED index.** `local_macros` is built by `collect_decls`, which visits file/module items only, so a `macro_rules!` in a function body was in no index `visit_macro` could consult — and R106's body-item shadow additionally rewrites the invocation path to `<body-item>NAME`. The name lives in `ItemMacro::ident`, not the `Macro` node, so recording had to move to `visit_item_macro`, with a restore in `visit_block`. **Verified by me: `body_local` and `blocktok_local` ABSENT -> `['Fs']`, with a pure body-local macro and an empty function unmoved.** **The fabrication trade the row warned about is measured in BOTH directions:** degrading to one shared name-keyed map makes a pure body fabricate `['Fs']` AND the writing one go ABSENT; deleting the block restore fabricates over a program that writes nothing. **The agent's first version of that control had no teeth** — the invocation was inside the block, so it passed with the restore deleted — caught by running the degradation, not by reading it. 1,509-crate A/B wide key: ADDED 6 / REMOVED 0 / CHANGED 4, 0 effects lost, all ten rows audited to source (rayon `par_sort_by_cached_key`, combine, h2, tower-http). **The only withdrawal was a FALSE disclosure**: `time-0.3.55` loses `invisible: serde_core` because the crate-wide name-keyed index had been expanding an unrelated `macro_rules! item` from `src/serde/mod.rs` inside that body — the body-local definition now shadows it, which is Rust's own rule. |
| R143 rust: a MULTI-ARM macro's template is skipped, and the report says nothing at all | 2026-09-02 | **CLOSED AS A DISCLOSURE — candor-rust `11bcedf`. Verified. AND THE ROW'S OWN CLAIM THAT THIS COSTS NO PRECISION IS WRONG** | **CARDINAL SIN, rust, and it is the DISCLOSED-vs-SILENT test failing on a comment.** The code documents skipping multi-arm macros as a deliberate anti-fabrication trade and calls it *"an honest under-report"*. Agent-measured: the report carries **no disclosure of any kind** — no `Unknown`, no `unknownWhy`, no `incomplete`, no `invisible` — over a template that really spawns. **A named limitation that answers `Unknown` is a limitation; one that answers NOTHING is a sin with a footnote** ([[candor-review-round-2026-09-01]], R85's ruling). | Not fixed. The trade itself may well be right — expanding a multi-arm macro without knowing which arm applies would fabricate. **What is wrong is the silence, not the skip.** The cheap correct answer is a hedge with a reason (`Unknown` + `ambiguous:` or an `unknownWhy` naming the unexpanded macro), which costs no precision and makes `deny Unknown` able to see it. **RE-MEASURED 2026-09-04 on shipped 0.35.0: STILL OPEN on the RESOLUTION side.** R203's second commit made the veto-side interior term walk every parseable arm and its own row says `collector.rs` was left untouched so call-edge resolution keeps ONE authority — R48's single-arm rule. That is the path this row is about. Fixture `scratchpad/stale-rust/``r143`: an ordinary (non-`?`) call to a multi-arm macro's `write` arm, doing a real `std::fs::write`, is entirely absent from the report; a single-arm control doing the identical write is charged. **The multi-arm SKIP is right; the SILENCE was the defect.** Now `Unknown` + `ambiguous:unexpanded multi-arm macro_rules!`. Verified by me: `multi_arm` ABSENT -> `['Unknown']` over a macro whose taken arm really writes a file. **THE ROW SAID THIS WOULD COST NO PRECISION. Measured, it costs 4,238 rows across 131 of 1,509 crates — about 1.6%** — all disclosed `Unknown`, 0 concrete effects lost. **And R143's mechanism contributes ZERO of the debt register's 131 shapes:** the register's `match_arms` form is a SINGLE-arm macro whose template holds a match-arm repetition, so it fails R144's parse gate, not the arm count. **My brief's premise "three rows, one mechanism" was wrong both ways — two mechanisms in the register, three rows, three cuts** — and the agent measured that before building on it. Two narrower options were measured and deliberately NOT smuggled in. **A Fable review then corrected BOTH my reasoning and my number — see R257.** **The UNION is excluded DETERMINATELY, not doctrinally:** SPEC §4's cfg union rests on the stated premise *"exactly one of them is built"*, chosen by configuration OUTSIDE the source, whereas a macro arm is chosen by the invocation's tokens INSIDE it — so a union would charge an effect the source PROVES does not occur at that call site. My original citation of R48's *"parse-or-skip never fabricates"* was about a DIFFERENT operation and was a boundary drawn around its own trigger, the error this project keeps logging. **And the noise number I first reported was measured on the wrong population:** 0.8% used ALL crates as the denominator; on crates that PASSED before it is **2.2%** blanket — and the SCOPED `deny Unknown[dispatch]` gate, which I never measured at all, was at **8.1%** until R257 re-kinded the reason. **Post-R257: scoped 0.0%, blanket 2.2%**, the latter being exactly the signal those users ask for. The reviewer named two options I had not: skip the hedge in TYPE position (a type-position macro expands to a type and runs nothing, so the trigger fires where no code exists), and treat an EMPTY arm-union as nothing-to-pick-between. Both sound, neither funded. **What remains the owner's is only whether to fund those; the soundness is determinate.** |
| R144 rust: a `$(..)*`-repetition macro template is unparseable and also silent | 2026-09-02 | **CLOSED AS A DISCLOSURE — candor-rust `31e2b4f`. Verified** | **CARDINAL SIN, rust — R143's sibling.** A template using `$(..)*` repetition cannot be parsed as a block, so it is skipped, and as with R143 the report carries no disclosure at all over a template that really spawns. | Not fixed. Same remedy as R143 and it should be taken together: the skip may be correct, the silence is not. Filing separately because the parse failure has a different cause from the multi-arm ambiguity and a fix for one will not automatically cover the other — which is the sibling-spelling question that produced most of this round's findings. **RE-MEASURED 2026-09-04 on shipped 0.35.0: STILL OPEN on the RESOLUTION side**, the same split as R143. R207's flatten (`856333d`) patches only the veto-side reader in `lang.rs`; that commit's own evidence records measuring — and deliberately NOT applying — the same flattened reader on `collector.rs` (it adds 178 rows / 12 `Unknown` effects). Fixture `scratchpad/stale-rust/``r144`: an ordinary call to a repetition-template macro doing two real writes is entirely silent; a non-repetition control is charged. A template `strip_dollars` cannot parse as a block (`$(..)*` repetition, `$k => $v`, a match-arm list) yielded no arm to walk and said nothing. Now `Unknown` + `ambiguous:unreadable macro_rules! template` — **a DIFFERENT reason from R143's, pinned by a separation control**, because a fix for one does not cover the other. Verified by me: `repetition` ABSENT -> `['Unknown']`. A/B ADDED 923 / REMOVED 0 / CHANGED 3,281, 0 effects lost; 3,495 rows in 143 crates (~1.3%). R207's `kv!` residual is untouched — the construction is still invisible, it is merely no longer silent. |
| R145 rust: `include!` text is never read | 2026-09-02 | **OPEN — PRE-EXISTING and PUBLISHED. Found while fixing R139** | **Under-report, rust.** `include!`-ed source is never read, so anything it contains is invisible. Agent-measured alongside R142-R144. | Not fixed, and **its severity is unassessed rather than known-low** — filed so the gap is on the record. `include!` is used for generated code (build-script output, `env!("OUT_DIR")` tables), which is exactly where an effect can hide from a reader as well as from the scanner. **The question to settle first is whether an unreadable `include!` currently produces a disclosure**; if it does not, this is R143's class again rather than a precision gap. **RE-MEASURED 2026-09-04: STILL OPEN — and the obvious fixture is a TRAP that would make a shallow recheck call it closed.** An IN-TREE `include!("foo.rs")` appears to work, but not for the reason a reader would assume: candor-scan walks every `.rs` file under the crate root independently of any `include!`/`mod` reference — proved with an ORPHANED `.rs` file never included anywhere, scanned identically (`scratchpad/stale-rust/``r145c`). Even that accidental catch MIS-ATTRIBUTES the unit to a module path derived from the file's location (`gen::snippet::foo`) rather than the spliced-in `crate::foo`, so a policy naming the real path still misses it. The row's own stated concern — build-script / `OUT_DIR` generated code — lives OUTSIDE the crate tree, and there the included function (a real Fs write, ground-truthed) is completely invisible: `deny Fs generated_write` reports 'policy rule matched NO function' (`scratchpad/stale-rust/``r145d`, an absolute-path `include!` outside the crate). **Any future fixture for this row must be out-of-tree.** |
| R146 swift: a protocol requirement spelled like an allowlisted C name gets a FABRICATED REASON | 2026-09-02 | **OPEN — PRE-EXISTING, identical pre- and post-R135, so NOT introduced by it. Over-disclosure, not a sin** | **FALSE DISCLOSURE STRING, swift.** `protocol Locking { func flock(_:) }` with a bare `flock(fd)` in its extension yields `unknownWhy: ['dispatch:Locking.flock', 'native:flock']` — the second reason is false: nothing raw-C is involved. Agent-measured with a one-variable control (the requirement renamed `lockUp`): **the effect set and the gate verdict are identical either way** — only the reason string differs. Same class as R130's stated `remove(element)` residual, one construct over. | Not fixed, and the severity is genuinely low — **but the reason string is not decoration.** `unknownWhy` is what a user reads to decide whether a hedge is worth chasing, and `native:flock` sends them looking for an FFI call that does not exist. This project has already ruled that a FALSE disclosure is its own defect class, not a cosmetic one ([[candor-scan-guards]] — a key reported *"ignoring unknown config key"* while being HONOURED). Filed on that precedent rather than on impact. |
| R147 java: socket-stream provenance — a read through a stored `InputStream` carries no `Net` | 2026-09-02 | **CLOSED — candor-java `860ea4d` (+`c75a3d8`). The RED→GREEN upgrade flip the 0.35.0 panel found; coordinator spot-checked `SockRead.poll` back to `['Net']`** | **CARDINAL SIN, java, and it is the finding that removing an over-charge exposed.** `httpcore 4.4.13`'s `BHttpConnectionBase.fillInputBuffer` really reads the socket via `SessionInputBufferImpl.fillBuffer` on a stored `InputStream`, and after B3 it loses its row **with no disclosure**. Its `Net` came **entirely from the fabricated `get/setSoTimeout` pair** — so the correct verdict was being produced by an incorrect mechanism. `fillBuffer` itself is absent at all three commits: **the blind spot is socket-stream PROVENANCE and it predates everything in this wave.** ~13 rows in that jar follow transitively. | Not fixed. **The agent found this by screening all 146 lost rows for a co-located read/write** — 8 had one, 7 keep a disclosure or keep `Net` by another route, and exactly ONE leaf goes silent. That screen is the reusable move: **when you remove an over-charge, ask which rows were only correct BECAUSE of it.** **And it explicitly did not keep a known fabrication as accidental coverage**, which is the right call and the one this project has got wrong before — a fabrication that happens to mask a sin is two defects, not one. **FIXED at the ACQUISITION.** The receiver's static type is the abstract `java.io.InputStream`, which no rule covered; until `2a6793c` the caller kept a `Net` that came entirely from the fabricated `get/setSoTimeout` pair (correct verdict, wrong mechanism), so removing that over-charge unmasked it. A whole-program pre-pass keyed on the acquisition asks `Classifier.classify` what the producing call already charges (§G), so Socket/URLConnection → Net, Process → Exec, `Files.newOutputStream`/`new FileInputStream` → Fs fall out of one rule; folded into `Refresh.wholeProgramDigest` (R163's guard went red until it was). `deny Net SockRead.poll` published 1 → candidate 0 → fixed 1; `tune` stays 0 (the correct removal). Ground truth over loopback TCP. 395-jar A/B, 18-field key, 577,790 rows: ADDED 0, REMOVED 0, 255 `inferred` gains (Fs 250, Net 5), all 25 direct gains audited from `javap` — incl. bsf 2.3.0 `SocketConnection.listen/sendPacket`, the shape verbatim in real code. Revert 4/7 red. **Residuals pinned by test:** a param-bound stream field and a filter wrapping an acquisition — exactly the dynamic oracle's ACCEPTED `[abstract-reader/jfr] Parser.parse` gap. **Panel second pass, both jars, executed off a live loopback socket: the two stated residuals ARE sins — `Factory.poll` (`this.in = connect(h,p)` one project frame away) and `Wrap.readR` (`this.r = new InputStreamReader(this.in)`) ABSENT, `deny Net`/`pure` 0/0. Fixture `scratchpad/panel-fixes-2/java/`.** |
| R148 java: `soundness/dynamic/corpus.sh` prints ERROR and then `RESULT: CLEAN`, exit 0 | 2026-09-02 | **CLOSED — candor-java `3cb977e`, per Tom's 2026-09-03 ruling. Coordinator-verified: the real script in its real location exits 3** | **§H aggregator failure, java.** The harness prints `async-netfs: ERROR (classpath missing)` and then `RESULT: CLEAN` with exit 0 — a driver that never ran reaching the verdict as a pass. Cause is environmental: `AsyncNetFs.java` uses `Thread.ofVirtual()` (Java 21+) on a JDK 17 box, so it never compiles. But the aggregator cannot tell 'did not run' from 'found nothing', which is this project's standing instrument class — the same shape as R104's oracle and R108's eval gates. | Not fixed, and the agent's reason for leaving it is sound: **changing an aggregator's verdict is a separate decision with its own CI risk**, and it should not ride along inside a fabrication fix. The fix is the one R104 already established for the sibling instrument: **an ERROR'd driver must be UNRUN, not CLEAN, and must not exit 0.** Worth doing together with a check that the JDK actually supports every driver it is about to run. **MECHANISM LINE-PINNED by the R163 agent (still not fixed, deliberately):** `compile_all` (`soundness/dynamic/corpus.sh:51-61`) never reads `javac`'s exit code and prints `== compiled ==` regardless; the `async-netfs` entry then hits `ERROR (classpath missing: …)` at `:136` and **`return`s without touching `TOTAL_UNDERREPORTS`**, so one of seven dynamic-oracle legs silently never runs and the gate prints `RESULT: CLEAN`, exit 0. Cause on this machine: `javac` on PATH is 17 while the Gradle toolchain is 21, so `Thread.ofVirtual()` does not compile. Reproduces identically before and after R163. Detection worked; aggregation discarded it. The decision it needs — hard-fail on a missing toolchain, or SELFSKIP loudly — is Tom's, which is why two agents in a row left it alone. **RULED by Tom 2026-09-03: a missing toolchain makes `soundness/dynamic/corpus.sh` SELFSKIP loudly (exit 3, the family convention), never CLEAN and never FAIL. Change to land after the 0.35.0 cut.** **FIXED.** Mechanism: `compile_all` ran seven bare `javac` calls without reading an exit code, so a compile failure left a classes dir silently absent (here `AsyncNetFs.java:29`'s `Thread.ofVirtual()` against a JDK 17 `javac`); `entry()`'s classpath-missing branch then appended an ERROR summary line and RETURNED without touching `TOTAL_UNDERREPORTS`, so that leg evaporated and every other entry being fine still reached `RESULT: CLEAN`, exit 0. Now `compile_one` captures each rc and its first error line, the missing entry is recorded, and `main` checks a genuine finding FIRST (always exit 1) before reporting SELFSKIP. **All three arms executed, not reasoned about:** missing prerequisite -> exit 3 with `RESULT: SELFSKIP - 1 corpus entry could not be analysed (prerequisite missing): async-netfs (javac exit 1 ...)`; the same corpus with that entry excluded -> exit 0 CLEAN; a real finding (a test hook stripping `Fs` from `hof-io`'s report, caught by the real JFR oracle) -> exit 1, NOT 3. Coordinator re-ran the committed script: rc=3, and the final line names the entry and the reason. **Stated, not hidden:** `bin/gate-run.sh` never invokes this script - `bin/gates.sh candor-java` lists it as a `~` BLOCK line inside a conditional CI multi-step, one of the ~91 lines the harness defers to manual reading, so the SELFSKIP classification is unproven in situ; the mechanism it relies on is a bare rc==3 check at `gate-run.sh:197` with no repo-specific logic, which arm (a) satisfies. Wiring the script into an auto-run gate is a separate change, in a repo that agent did not own. |
| R149 spec: `conformance/run.sh` DISCARDS the swift build's status and measures whatever binary is there | 2026-09-02 | **CLOSED — candor-spec (this commit). INSTRUMENT defect, pre-existing. Found by a whole-project review, not by any diff-scoped reviewer** | **THE FOUR-WAY SUITE CAN SILENTLY MEASURE A STALE ENGINE.** `conformance/run.sh:377` runs `( cd "$SW_DIR" && swift build >/dev/null 2>&1 )` in a subshell and **never reads its status**, then proceeds on `[ -x "$SW_BIN" ]` — the mere EXISTENCE of `.build/debug/candor-swift`. So a failed build falls through to whatever binary a previous run left behind, and the suite reports a four-way result for a swift that is not the tree. **This is the instrument I relied on all day**, running it four times and reporting OK each time. | Not fixed — deliberately held while five reviewers were reading these trees, and it must be fixed before the next run is trusted. **Coordinator-verified that it did NOT bite today, behaviourally rather than by mtime:** the debug binary carries both R130 fixes (`native:symlink` present; `URL.checkResourceIsReachable -> ['Fs']`), so today's four runs measured a current engine. **That is luck, not design** — the builds happened to succeed. The fix is to capture the status on its own line and treat a failed build as a hard FAIL, never as a skip and never as a pass — the same ruling R104 already made for the syscall oracle, which printed `build failed — SKIP`, summarised `0 NEW failure(s)` and exited 0. **The suite that catches this class in engines has it in itself.** **FIXED before the next run that would have relied on it.** The status is now captured on its own line and a present-but-unbuildable swift is a **HARD FAIL, exit 2** — never a skip, never a pass. `swift` on PATH plus a `Package.swift` means the engine is DECLARED, and *declared but broken* is exactly the case a green tick must not cover. Guard proven to FIRE, not assumed: simulated against a non-existent tree, `SW_BUILD_RC=1` and the exit-2 path is taken. **And I introduced a bug writing the fix, which is worth recording because it is the same class:** the error message contained `` `swift build` `` inside double quotes — **command substitution, so printing the failure would have RUN a build**. `bash -n` passes it because it is syntactically valid. Caught by grepping the new block for backticks in executable lines. Verified my edit adds no shellcheck findings: 107 exist in this file and rc=1 both before and after the change (`git stash` A/B), and none is in the new block; the file is in no shellcheck gate. **C3 rider from the same review, NOT fixed here and filed as remaining work:** roughly ten further swift blocks (from `:9777`, including PART 80's own `else` at `:15116` which falsely prints *"no swift toolchain"*, and PART 83 at `:15841`) still gate on `-x "$SW_BIN"`. They are bounded — a present-but-unbuildable swift now dies at this guard before reaching them — so they are reachable only through `part.sh` filtered slices, which is where the mislabels remain. |
| R150 rust: `same_artifact` exists TWICE and only the scan-route copy has the device+inode guard | 2026-09-02 | **CLOSED — candor-rust `b29123c` + `5a9334b`, unpushed. The pre-fix behaviour was WORSE than the row said: the gate route DESTROYED the user's own policy file** | **FAIL-OPEN GUARD, rust — §F1 q3 (two implementations of one question that drifted).** `crates/candor-scan/src/scan.rs:160` compares two paths with a `std::fs::metadata` device+inode check, and its own comment cites *"§3.3.1 asks for device+inode and that was read as advisory"*. `crates/candor-query/src/gate.rs:982` is a **separate copy of the same function with no such check** — bare `canonicalize()` with a dangling-symlink fallback, `0` mentions of inode, device or symlink guarding. So a policy and a `gate.json` that are the same artifact **through a hard link** are seen as distinct on the GATE route while the SCAN route catches them. **And the test knows only one copy:** `scan.rs`'s `same_artifact_catches_a_policy_and_gate_json_collision_through_a_dangling_symlink` sits beside the guarded implementation. The unguarded one has no equivalent. | Not fixed. **Direction: fail-OPEN** — the gate route accepts an input/sink collision the spec forbids and the sibling route refuses, so the two answer the same question differently and the weaker answer is the one a `gate --report` user gets. Per §G the fix is to converge on the guarded implementation rather than copy the guard across — `same_artifact_pub` already exists at `scan.rs:148` for exactly this kind of sharing. **Then move the dangling-symlink test to cover both call sites**, because a test that pins one copy of a duplicated rule is how the drift survived. **FIXED by converging on ONE authority:** the guarded body moves to `candor-report::same_artifact` (hardlink device+inode check, dangling-symlink pre-resolve via `resolve_sink_artifact`, then canonicalize-compare) and both `candor-scan/src/scan.rs:160` and `candor-query/src/gate.rs:994` become one-line delegates. **Revert test, and the finding it produced:** with the fix stashed, `--policy policy.P --gate-json verdict-b.json` over a HARDLINK did not refuse — it armed its fail-closed placeholder JSON INTO the hardlink, i.e. overwrote `policy.P` itself, which then re-parsed as 'no rules' and refused on a second read for the WRONG reason. Four arms: distinct files exit 0; hardlink exit 2 refusing, policy byte-identical after; unrelated files exit 0 (the guard discriminates); a relative-path spelling of the same file exit 2 (caught by both old and new — no regression). Unit test `gate_same_artifact_catches_a_hardlink_and_a_dangling_symlink_collision` pins `crate::gate::same_artifact`, the actual CLI call site, not the scan crate's copy. **A real regression the gates caught, worth recording:** the first commit moved edition-2021 code into edition-2024 `candor-report`, where let-chains exist, and `clippy -D warnings` correctly failed with 3 `collapsible_if` — fixed in `5a9334b`, not a false alarm. |
| R151 java: the refresh cache replays STALE dependency effects — `deny Net` flips 1 -> 0 | 2026-09-02 | **CLOSED — candor-java `6ccdfc8`. Independently verified against the PUBLISHED v0.34.0 release asset by an agent that did not write the fix** | **A warmed refresh cache serves a weaker answer than a cold one.** `wholeProgramDigest` (`Refresh.java:319-326`) hashes the chained-dep **KEY SET** and never the `DepFn` **VALUES**, while `inheritDepFn` (`Candor.java:5857-5885`) writes those values into cached output fields. A dep function that keeps its `hash` but GAINS an effect is replayed from cache without it. Executed, one variable: warm cache under dep v1 (`Db`), rerun under dep v2 (`Db,Net`), same app bytecode, same policy, same jar — **stale arm exits 0 reporting `['Db']`** with *"reused 1 of 1"*; the fresh-cache and no-refresh controls both exit 1 with `['Db','Net']`. **Two §E2 hits stacked:** the class comment at `Refresh.java:44-45` claims changes to chained dependencies discard the cache — false; and `bin/refresh-equiv.sh`, described as *"the refresh's entire safety case"*, **never perturbs `CANDOR_DEPS`**, so the guard is blind on precisely this axis. | Not fixed. **Highest user impact of this round: it needs only a warmed cache, which is the normal CI configuration.** Untested sibling axes on the same mechanism, flagged not measured: dep `hosts`/`paths`/`cmds`/`tables` literals and `DepFn.stale` — none is in the digest either. The fix must extend the digest to the dep VALUES and, separately, `refresh-equiv.sh` must perturb `CANDOR_DEPS` — a safety case that cannot vary the input it is defending is not a safety case. **FIX LANDED (`6ccdfc8`), with the strongest evidence package of the round.** Reproduced first, one variable, app bytecode byte-identical across arms (`shasum` equal); both dep versions compiled and EXECUTED — a listener on 127.0.0.1 saw `NO CONNECTION` under v1 and `CONNECTION RECEIVED` under v2. `Refresh.java`, `DepFn.java`, `Loader.java` are byte-identical between `v0.34.0` and the pre-fix HEAD, so the reproduction is a statement about the published code (the agent built the pre-fix jar rather than downloading the artifact — which is why an independent verifier is being run against the real release asset). **TEN AXES, not the one handed over** (§9): effects, hosts, paths, cmds, tables, netClass/incomplete, unknownWhy, `fn`+`calls` (`depCallsByFn`/`depWhyByFn`), and `DepFn.stale` — **six flip a configured gate stale 0 / cold 1** (`deny Net`, `allow Net a.example.com`, `allow Fs /tmp/alpha`, `allow Exec ls`, `allow Db users`, and netClass via propagated `incomplete`); `stale` was already caught incidentally via `depCoveredPkgs`. **The boundary was audited across EVERY non-final, non-memo `AnalysisContext` field**, not the ones named: the rest are derived from scanned class bytes or recomputed at report-write time, and only a shared input read during per-class analyze can go stale in a per-class cache. **Cost measured, not assumed:** guava + hibernate-core chained (23,624 joined entries), three warm runs — digest **218 ms → 269 ms**, whole warm scan 6.79 s → 6.88 s, reuse 87/87 both arms, reports byte-identical. The cheaper discriminator (a digest carried in the dep report) was REJECTED as producer-controlled — a hand-edited report keeps it, fail-open on a legitimate input. **Side finding:** the digest's identity-hash guard was quadratic and untested — 1.19 s of a 1.46 s digest on 15.4 MB; anchored and possessive it is 0.05 s, with a linearity test that fails at 1,262 ms against the old pattern. **Corpus A/B keyed on FIFTEEN fields, §E1 counters: 45,150 executions of the changed branch.** SAFETY: unchanged dep report → `+0/-0/~0` on all six consumers, reuse identical. RECALL: post-fix warm == cold on all six; **pre-fix warm diverged on 3,809 / 2,799 / 1,379 / 523 rows** (error_prone_core, dagger-compiler, dagger-spi, google-java-format) — **5,825 rows lost an effect, 0 gained one.** **Two earlier A/B passes were WRONG and are documented in the commit:** the first perturbed a guava entry none of the six consumers call (7,525 hits, 0 joins — the recall column could not have failed); the second perturbed effect-LESS entries, which adds `crossDeps` keys and invalidated the pre-fix cache for the wrong reason. A third artefact: dropping the `.hierarchy.json` sidecar beside a perturbed report also invalidated pre-fix for the wrong reason. Each is the *fixture reached the code?* question, answered by the agent on itself. Teeth, all revert-tested BY REVERTING: `RefreshDepDigestTest` (end-to-end arm plus a REFLECTIVE arm requiring every `DepFn` field to move the digest — reverted, it names all ten); **`bin/refresh-equiv.sh` gains a `CANDOR_DEPS` axis** that compiles its own two-package fixture, generates both dep reports with the jar under test, refuses to pass unless the perturbation moved a cold report AND the cache engaged, and **FAILS against the pre-fix jar**; `RefreshIdentityHashGuardTest` 7 positives / 7 near-misses / linearity. `ci/self-gate.sh` and `soundness/dynamic/corpus.sh` run by hand, both clean; `nativeCompile` UNRUN and identical at the merge-base. **INDEPENDENT VERIFICATION, against the artifact rather than the code.** A second agent, told not to read the fixer's fixtures, downloaded the published jar with `gh release download v0.34.0` (sha256 `ef77745a…b044`, self-reporting `candor-java 0.34.0` at spec ⟨0.34⟩, build stamp `e57b8f3`) and used the fixed jar (sha256 `fb52aa23…96d5`, stamp `6ccdfc8`). Its own fixture: `chained.Client.call()` v1 reads a file, v2 also opens a socket to a localhost listener — both EXECUTED (`NO CONNECTION` / `CONNECTION RECEIVED: b'*'`); `myapp.App` compiled once against each dep, **`App.class` sha256-identical either way**; dep reports produced by SCANNING each dep version with the jar under test, not hand-written; both jars agree the key `chained/Client.call()V` is stable while the value moves `['Fs']` → `['Fs','Net']` — the exact shape. Matrix, statuses on their own lines: cold v1 → 0 both jars; cold v2 → 1 both; **warm primed under v1, rerun under v2 — published: exit 0, `refresh — reused 1 of 1`, `App.main` `['Fs']` with `Net` silently missing; fixed: exit 1, `reused 0 of 1`, `['Fs','Net']` + `incomplete:['Net']`**; control primed v2 / rerun v2 → exit 1 with `reused 1 of 1` on both, so the fix did not break ordinary reuse. The cache engaged in the defect arm on both jars (non-zero reuse line), so the arm is not vacuous. **Two interface facts the verifier had to derive from source, reported as findings:** `CANDOR_REFRESH` (and `CANDOR_REFRESH_DEBUG`/`_VERIFY`) appear in neither `README.md`, `AGENTS.md`, nor `--help` — zero hits in all three — and reuse is disclosed only on stderr, never in the report envelope. See R164. **Coordinator note:** the verifier's `mkdir` landed in a scratch directory the fixer had already populated under the same generic name; it abandoned that directory unread and worked in a uniquely-named one. The scratchpad is a shared instrument (CLAUDE.md) and two agents on one row will collide on a natural name — worth naming the directory in the brief. |
| R152 rust: the ⟨0.32⟩ refusal marker is consulted by `gate` ONLY — `unverified`/`fix-gate` certify beside it | 2026-09-02 | **CLOSED — candor-rust `996239b`. PUBLISHED, violated a SPEC MUST. Reviewer-executed, then coordinator-verified at BOTH ends** | **SPEC.md:3193 states a consumer resolving a `--report` locator MUST consult the marker and refuse.** `refusal_marker_for` has exactly ONE caller in the repo (`candor-query/src/gate.rs:112-125`); `ReportCompleteness::incomplete()` (`completeness.rs:362-372`) has five arms and no marker arm. Executed against a real scan-produced report beside `d.refused.json`: `gate` exits **2 refused**, while **`unverified --strict` exits 0 printing *"every function in a pure/deny layer is PROVABLY clean ✓"*** and `fix-gate --strict` exits 0. Removing only the marker makes `gate` exit 0, which is the control. **Third instance of a class the umbrella already predicted** — `BACKLOG.md:1266`: *"a new exit-2 cause must fail loudly if the advisory siblings do not share it."* | Not fixed. **The reviewer's first fixture returned the right exit codes for the WRONG reason** (*"no report files at prefix"*) and it caught that before reporting — the verdict above is from a fixture proven to reach the code. That near-miss is worth as much as the finding. **A3, a sibling, is filed as a SPEC question rather than a defect**: the `CANDOR_DEPS` chaining route (`deps.rs:320,337`) also ignores the marker, but SPEC:3193 as written binds `--report` locators only — so it needs a ruling on whether chaining is a consuming route, not a fix. **CLOSED, and coordinator-verified with a twelve-cell matrix rather than a single arm.** Fixture: a crate whose every function is pure, scanned to `.candor/report.rsref.scan.json`, policy `pure rsref` — the shape under which `unverified` prints its strongest sentence — and a marker `.candor/report.refused.json` carrying `refused:true` and a canonical `prefix`. Two binaries (published `bffac6c`, fixed HEAD), three verbs, marker absent and present, every status captured on its own line: **marker ABSENT — all six exit 0** (controls). **Marker PRESENT — `gate` exits 2 on BOTH** (the control proving the marker is what `gate` reads); **published `unverified --strict` exits 0 printing *"every function in a pure/deny layer is PROVABLY clean"* and `fix-gate --strict` exits 0 printing *"no deny/pure boundary crossings ✓"* — the sin, reproduced; at HEAD both exit 2** with `⚠ INCOMPLETE — 1 report(s) under this locator are marked REFUSED (SPEC §3.3.1 ⟨0.32⟩)` naming the prefix and the recorded reason. **The fix converged on the existing authority (§G):** `ReportCompleteness` gains a `refused: Vec<RefusalMarker>` populated once per locator via the SAME `candor_report::refusal_marker_for` that `gate --report` already used — no second marker check — joining `unanalyzed`/`unreadable`/`out_of_scope`/`unread_armed`/`unasked_rules` as a sixth cause of `incomplete()`/`must_hedge()`, and riding both disclosure channels (prose and a new `refused`/`baselineRefused` JSON key). **Which verbs, checked rather than guessed:** `report_completeness` has 15 call sites across eight files; `unverified` and `fix-gate` are the only two whose EXIT CODE reads `comp.incomplete()`, so they now exit 2 under `--strict` as `gate` does. Every other verb (`path`, `tour`, `gains`, `callers`, `fix`, `containment`, `show`, `where`, `map`, `blindspots`, `reachable`, `impact`, `whatif`, `diff`) is disclosure-only by existing family design and now discloses the refusal without an exit change — confirmed by grep AND by running each. One extra site: `gains` used a hand-rolled `attach_manifest()` instead of `CompletenessFields` and would have flipped `incomplete: true` with no visible cause; two lines added. Five regression tests, each proven to FAIL against the pre-fix binary by stashing the fix and rebuilding; an over-charge control pins that descriptive verbs stay exit 0. `cargo test --workspace` 621/0, `verify-local.sh` ✔, `ci/self-gate.sh` OK, `ci/gate-equivalence.sh` OK. **Left alone by instruction, still open:** the `CANDOR_DEPS` chaining route (`deps.rs:320,337`) ignores the marker; SPEC:3193 binds `--report` locators only, so whether chaining is a consuming route is a ruling for Tom, not a fix. **Two instrument notes from my own verification, kept because both produced a false matrix first:** my first run returned exit 2 in ALL twelve cells, controls included — the locator was wrong (`.candor/report.rsref`; the prefix form is `<base>.<crate>.<type>.json`, so the base is `.candor/report`), and the controls are the only reason that read as *instrument broken* rather than *defect confirmed*. And my loop wrote both marker states to the same output files, so the absent-state evidence was overwritten before I could read it — per-state filenames now. **[Label corrected 2026-09-03 by the 0.35.0 release panel: `bffac6c` is NOT the published 0.34.0 — the v0.34.0 tag is `22b5ec2`, six commits earlier; where this row says 'published 3cf055d' read 'post-tag 3cf055d'. The row's conclusion was re-checked against the true tag build where the panel could, and stands.]** |
| R153 swift: four un-dealiased sites sit beside R97's "one dealias authority" fix, and one FABRICATES | 2026-09-02 | **OPEN — PRE-EXISTING and PUBLISHED. Two sites executed by the coordinator's reviewer, two by its hunters** | **§A.2 — R97 converged the dealias question and its tests alias only PLATFORM types, so four project-type sites survived.** All in `CallCollector.swift`: **(a) the singleton-accessor binder (`:4940-4953`)** reads `fields`/`localTypes` on the RAW token where `rootOf` dealiases — `let s = Cfg.shared; s.wipe()` leaves `viaAlias` absent and `deny Fs viaAlias` exit 0, while the identical body via `Config.shared` exits 1. **Fails in BOTH directions** (the fabrication arm was executed too). **(b) KeyPath roots (`:3942`, `:3957`)** — `\W.heavy` and `[W].map(\.heavy)` silent, plain spellings exit 1; note the dealiased sibling sits at `:3955`, one line between them. **(c) `edgeComparableWitness` (`:4186`)** — `sorted()` over `[W]` never charges an effectful `static func <`. **(d) the κ constructor arms (`:3121-3177`)** — shadow guards on the RAW name, κ lookup on the DEALIASED name, so **a project's own `Process` aliased as `Proc` is FABRICATED `Exec`**. | Not fixed. **The safety comment at `:3181-3182` — *"an alias never overrides the project"* — is FALSE, and is the finding of record for the §E2 class**: the arm it defends is the one that fabricates. Fix per §G by routing all four through `rootOf`, and **give the tests a project-type alias**, since aliasing only platform types is what let four sites hide beside a fix whose whole subject was aliasing. **RE-MEASURED 2026-09-04: PARTLY open, and THE ROW'S LITERAL TEXT DOES NOT REPRODUCE — read this before writing a fixture.** (a) The singleton-accessor binder (`let s = Cfg.shared; s.wipe()`) does NOT reproduce on 0.35.0 across four variants (`static let` / computed `static var`, `let`-bound / chained); every one correctly charges `Fs`. Reported as a discrepancy, NOT assumed fixed. (b) KeyPath roots and (c) `edgeComparableWitness` DO reproduce — **but only when the type is itself an ALIAS** (`typealias WA = W; \WA.heavy`, `let arr: [WCmpA]`); the row's unaliased spellings are correctly charged. Aliased arm silent, `deny Fs` exit 0; plain arm exit 1; ground truth executed. (d) The κ constructor arm reproduces **as a FABRICATION, not a silence** — a project's own harmless `class Process` (nothing to do with Foundation), aliased and constructed via `Proc(...)`, is charged `['Exec']`, so `pure viaAliasProjectType` exits 1 on a FALSE violation while the direct spelling correctly exits 0. Fixtures `scratchpad/stale-engines/``swift/r153{,b,d,e,f,g}`. |
| R154 ts: a policy ALIAS line disarms the ⟨0.30⟩ fail-closed INCOMPLETE verdict | 2026-09-02 | **CLOSED — candor-ts `c8e52bc`. Mechanism found in POLICY PARSING, not verdict composition, exactly as the 'peek never attempted' detail suggested. Coordinator-verified: 0.35.0 exits 0 'policy ✓', the fix exits 2 INCOMPLETE** | **An unrelated rule turns a red verdict green.** Same tree, same `.candor/config` carrying `unknown-alias corp = reflect`: `deny Fs` alone exits **2 INCOMPLETE**, correctly naming the out-of-scope effectful file; **add `deny Unknown[corp]` and it exits 0 with *"policy ✓"*.** ⟨0.30⟩'s whole point is that a non-empty `outOfScope` makes the verdict INCOMPLETE — a rung this project accepted specifically because its trigger is bounded to effects the policy DENIES. Here an alias line deletes the disclosure. | Not fixed. Filed with two siblings from the same pass, both also executed and both positive-purity-by-absence: **a class EXPRESSION is invisible to CHA** (`const SubExpr = class extends Base {}` — `viaBase` vanishes; `pure a.viaBase` goes exit 1 -> exit 0 *"policy ✓"*), and a **named** class expression drops the interface-union entry entirely where the anonymous form correctly hedges `['Unknown']` and the declaration form gives `['Fs']`. Three spellings of one construct, three different answers — §F1 q7 at the language level rather than the table level. **RE-MEASURED 2026-09-04 and NEWLY REPRODUCED — the row previously recorded that the coordinator had NOT reproduced it.** Built the ⟨0.30⟩ `outOfScope` shape independently: an excluded file performing `Fs`. `deny Fs` alone → exit 2, `outOfScope` populated, `excluded[].peeked:true`. Adding `deny Unknown[corp]` beside it (with `.candor/config: unknown-alias corp = reflect`) → exit 0 'policy ✓', and **`outOfScope` is ABSENT ENTIRELY while `excluded[].peeked` flips to FALSE — the peek is never attempted, not merely emptied**. Silent once triggered: no `incomplete`, no disclosure at all. Fixture `scratchpad/stale-engines/``ts/r154/pkg2`. **FIXED, and the row was NARROWER than the defect.** Mechanism, proven directly against the published `policy.mjs` before anything was written: `scan.mjs:8751` called `parsePolicy(readFileSync(policyPath), {})` while the §6.2 gate 800 lines below passed the real `.candor/config` alias map. **`{}` is not an empty alias map — it has no `.has`**, and `policy.mjs:458` reads `else if (aliases && aliases.has(cn))`: truthy, then TypeError. That throw landed in the peek's bare `catch`, `peekPolicy` stayed null, `peekAttempted` was never set — so `outOfScope`/`scannedUnder` were ABSENT rather than empty, `peeked` false, and ⟨0.30⟩'s INCOMPLETE verdict never armed. **Three ways the row understated it:** the trigger is ANY `Unknown[<token>]` for which the map is needed, not a defined alias — `Unknown[nosuchalias]` disarms the peek identically and is only non-silent because the gate's own §6.2 refusal happens to hold the exit at 2; a second silent instance the row does not have is a `.candor/config` RESERVED-NAME line (`unknown-alias reflect = native`) beside a used alias, exit 0 → 2; and there is a second symptom independent of the verdict — the ⟨0.33⟩ ASKED-AND-CLEAR answer was deleted too, so `deny Unknown[corp]` over a tree with nothing excluded emitted NEITHER key, the document a no-policy scan produces. `Unknown[reflect]` never reaches the alias arm, and that reflect/corp pair is what isolates it. **Fix:** the vocabulary is parsed once, anchored at the policy file (the anchor the gate already used), memoized, and read by both paths; the peek's `try` now covers the READ only, and alias-DEFINITION errors fold into `fatalPolicyErrors` — **guard-deletion measured**: without the fold, a malformed alias makes the peek publish `outOfScope:['runs']` and `peeked:true` BESIDE `policy NOT evaluated` in the same run. One behaviour change rides along and is PINNED rather than asserted away: `discoverConfigText` exits 2 on an unreadable config and now runs at the peek, so for a policy filed in its own tree the refusal lands BEFORE the envelope is written — report WRITTEN at 0.35.0, NONE now, the §3.1 posture. Suite 2202/0; revert → 4 rows red (two are green on revert BY CONSTRUCTION because the pre-fix peek threw before reaching them, so the fold is pinned by the guard-deletion probe instead, and the commit says so); PART 48 and PART 53 ts arms driven directly on both builds — identical MATCH/OK, nothing pinned has moved. Arms verified by sha256 against a freshly packed 0.35.0 tarball. |
| R155 family: `Llm` is missing from EVERY agent-facing vocabulary copy, including the embedded `--agents` contract | 2026-09-02 | **GATE LANDED (candor-spec `e56b22e`, unpushed) and `Llm` added in spec/umbrella/swift/ts/agents docs; rust and java handed to their owners. The gate found FOUR MORE pre-existing omissions — see below** | **The engines enforce an effect the agent contract never names.** SPEC.md:251 defines `Llm` (⟨0.13⟩, shipped four-way) and the classifier polices it — coordinator-measured, 7 sites in `candor-classify/src/policy.rs`. Coordinator-measured mentions: umbrella `AGENTS.md` **0**, candor-rust `AGENTS.md` **0**, candor-swift `README.md` **0**. **And the sharp edge, reviewer-measured:** the EMBEDDED `--agents` contract — which the docs tell agents to PREFER as version-matched — also omits it (`candor-scan 0.33.1 --agents \| grep -c Llm` -> 0). candor-agents' own `EFFECTS` comment records a pre-⟨0.24⟩ fail-open this omission caused. | Not fixed. **The mechanism is the interesting part: candor-rust's CI drift gate pins repo-doc == embedded copies, so every copy drifts together and the gate stays green.** Nothing pins any copy to SPEC §1. The docs should change, and the durable fix is a conformance-side check of each `AGENTS.md` and each embedded contract **against the SPEC vocabulary** — otherwise the next tier-1 additive rung repeats this exactly. **THE DURABLE FIX IS IN:** `scripts/check_agents_vocabulary.py` derives the vocabulary MECHANICALLY from SPEC.md §1's effect table (11 declarable names; `Unknown` excluded because the SPEC defines it as a visibility marker, not a declarable effect) and checks eight doc copies plus four embedded `--agents` contracts (UNRUN, never green, when a binary is absent). Wired into `conformance.yml`'s `agents-doc-drift` job — **which previously checked out candor-spec alone and would have checked nothing but its own file**; the job now checks out all five siblings (source only), so the gate enforces. §E3: run BEFORE any fix it was red on all 8 docs and all 4 contracts for `Llm` — a gate proven able to fail before it was trusted. **AND THE PREMISE IN MY BRIEF WAS WRONG:** I expected it to stay red only on rust and java. It stays red on four pre-existing omissions unrelated to `Llm` — **candor-ts and candor-agents `AGENTS.md` have never mentioned `Ipc`/`Log`/`Rand`/`Clipboard`; candor-swift `AGENTS.md` never enumerated the vocabulary at all beyond `Net`; candor-java `AGENTS.md` never enumerates effects at all (9 of 11 with zero mentions)**; and swift's embedded contract is GENERATED (`gen-agents-doc.py` + `swift build`), so its doc fix does not reach the binary without a source regeneration. The agent fixed exactly `Llm` as instructed and REPORTED the rest rather than widening its diff — the right call, and the reason these are on the record. Completion of ts/agents/swift is dispatched; rust/java are one-line doc edits handed to the agents that own those trees. **A PUSH-ORDER CONSTRAINT this creates, same class as the `c53baa2` one:** the CI job reads each sibling's `origin/main`, so **every sibling's doc fix must be pushed BEFORE candor-spec's workflow**, or the gate goes red on main for a defect that is already fixed locally. **COMPLETED across the family, with one self-inflicted red on the way.** ts and agents gained the four missing effects, swift gained the full enumeration and a REGENERATED embedded contract (`gen-agents-doc.py` + rebuild, verified live), java gained its first-ever vocabulary line with the embedded resource re-synced (`--agents \| grep -c Llm` 0 → 1; the gate now exits 0 for java). **candor-agents' CI went RED on the doc push:** it embeds `AGENTS.md` through `gen-agentsmd.py` exactly as swift does, the agent regenerated swift's copy but not agents', and agents' own embedded-contract drift gate caught the doc edit without its regen — **the doc-pinned-to-embedded mechanism this row is about, working as designed against the fix for this row.** Regenerated, gate green locally, pushed. rust's one-line edit is queued with the R160 agent; when it lands, every copy is pinned to SPEC §1 by the new gate and the candor-spec workflow can be pushed. |
| R156 umbrella: `candor doctor`'s stale-install check resolves binaries by a different rule than the dispatcher | 2026-09-02 | **CLOSED — candor `2afe88e`.** Originally filed: OPEN — the check added for a real incident silently skips the binary it was added to check. Executed by the reviewer on this machine** **CLOSED — candor `2afe88e`.** | **§F1 q3 in the health tool.** `bin/candor:1382`'s drift loop resolves with `command -v`; everything else in the dispatcher uses `rust_bin` = PATH then `~/.candor/bin` (`:154`). On this machine `candor-scan` is `~/.candor/bin`-only, so **the check silently skips the binary the dispatcher actually runs** — and it was added after the 2026-08-01 stale-query incident, i.e. it has never covered the case it exists for on a standard install. | Not fixed. Converge on `rust_bin` (§G). **Coordinator note: `candor-scan` is not on my PATH either, which is consistent with the reviewer's finding and is why my own `--agents` check for R155 could not run.** **CLOSED 2026-09-24, candor `2afe88e`, and the scope was WORSE than filed — measured, not inferred.** The row said the check skips `candor-scan`; on this machine it skipped **4 of 4 engines, three of them installed and runnable**, because all three live in `~/.candor/bin` only and `command -v` sees none of them. So a check added after the 2026-08-01 stale-query incident had **never once fired on the shape it exists for** — a guard that cannot fire, the class this register keeps finding in its own instruments. Fixed by asking `rust_bin`, the dispatcher's own resolver (§G — one authority, not a second rule), then resolving to a path because `stat` needs one. **VERIFIED BY THE GUARD IMMEDIATELY DOING ITS JOB:** the first run after the fix reported two REAL drifts — `candor-query`'s installed build older than candor-rust's sources (today's `scan.rs` change) and `candor-swift`'s install nine days stale. It went from never firing to catching a live condition on its first run. |
| R157 rust: `coverage-gate-refresh` has NEVER been green — its "stable" step runs nightly and links with a linker it never installs | 2026-09-02 | **CLOSED — candor-rust `588e835` + `b15503b`, and the fix that SHIPPED is not the one this row proposed: instead of the one-line build change, the workflow sets job-level `RUSTUP_TOOLCHAIN` and `RUSTFLAGS: ""` (`coverage-gate-refresh.yml:55`), which also covers the generator's OWN spawned cargo. The workflow is green at `a8c157a`.** **STATUS CORRECTED 2026-09-09 — this cell said OPEN while the fix was already on `main`. Verified against the ARTEFACT AT HEAD, not the commit message.** Original status: INSTRUMENT defect, present since the workflow was created (`fbfa0c1`, 2026-08-27). Coordinator-diagnosed from CI's own log; local reproduction FAILED and that is stated** | **A weekly drift check that has never once run.** The job exists to re-fetch ~74 calibrated crates from live crates.io and diff the coverage manifest — the staleness answer the fast gate structurally cannot give. `gh run list` shows exactly TWO runs ever: today's first scheduled run at `bffac6c` and my `workflow_dispatch` at `5f33219`, **both red at the same step, before any fetch or diff.** **Cause, from the runner's own log:** `info: note that the toolchain 'nightly-2026-06-14-x86_64-unknown-linux-gnu' is currently in use (overridden by …/rust-toolchain)`. The step is named *"Build the stable scanner"* and runs bare `cargo build --release -p candor-scan`; `dtolnay/rust-toolchain@stable` installs stable, but the repo's `rust-toolchain` file overrides any bare `cargo`, so it builds with the pinned NIGHTLY. `.cargo/config.toml` then forces `rustflags = ["-C", "linker=dylint-link"]` for every target, the job never installs `dylint-link`, and every dependency's build script dies at link: `error: linker \`dylint-link\` not found`. **The green sibling in `ci.yml:344` already documents both traps and escapes both** — `RUSTFLAGS= cargo +stable test …`, with a comment explaining that set-but-empty `RUSTFLAGS` overrides the workspace linker and that `+stable` is deliberate. The failing workflow was written 17 days later without either. §F1 q3: one question — *how do we build on stable in CI* — answered twice, and the second copy never checked against the first. | Not fixed — the fix is one line (`coverage-gate-refresh.yml:37` → `RUSTFLAGS= cargo +stable build --release -p candor-scan`, matching `ci.yml:344`) and is held only because another agent owns candor-rust at the time of writing. Evidence that the fix works on the runner is the sibling job, green on every push. **Reproduction limit, stated rather than hidden:** I could NOT reproduce the failure on macOS. With `dylint-link` removed from PATH and an isolated `CARGO_TARGET_DIR`, both `cargo +stable` and `cargo +nightly-2026-06-14` passed `-C linker=dylint-link` to rustc (44 invocations, verified with `-v`) and **succeeded anyway; a fake `dylint-link` placed as the only one on PATH was invoked 0 times, even by a direct `rustc -C linker=dylint-link main.rs`.** I do not know why Apple-target rustc accepts and ignores that flag here, and I am not guessing. The diagnosis rests on the Linux log, not on a local repro. **Two instrument lessons worth more than the fix:** (1) my first three reproduction arms returned **exit 127** — I had stripped `~/.cargo/bin` to hide the linker and hid `rustup` with it. A dead instrument, read as a result, twice in one day. (2) I had earlier reported *"no `rust-toolchain.toml`"* as evidence — the file is `rust-toolchain`, no extension; a `cat` of the wrong name is not a measurement of absence. **PROVEN ON THE RUNNER, THEN THE SAME DEFECT ONE LAYER DOWN.** With the one-line fix (`588e835`) the *"Build the stable scanner"* step went **green for the first time in the job's existence**, and so did the crates.io fetch — the diagnosis that would not reproduce on macOS was correct on Linux. The failure then moved to *"Regenerate covered.tsv / open.tsv"*: `generate.py` shells out to a **bare `cargo run`** for its `classify_check` helper, and that subprocess inherited the nightly pin and the dylint linker exactly as the step's own cargo had — `linker \`dylint-link\` not found` again, **after 74 crates were fetched and 21,999 candidate entries collected.** The diff and issue steps were correctly skipped, so this was still not drift being reported. A per-step `+stable` cannot reach a cargo that Python spawns, so the second fix (`candor-rust`, follow-up commit) sets the decision ONCE at the job level: `RUSTUP_TOOLCHAIN: stable` and a set-but-empty `RUSTFLAGS`. **Both halves measured, not assumed:** bare `cargo --version` inside the repo prints `1.98.0-nightly` and with the variable prints `1.98.0` stable — so the env var beats the toolchain file; and set-but-empty `RUSTFLAGS` overriding the workspace linker is the fact `ci.yml:344` already relies on. **And forcing stable is the RIGHT mechanism rather than a workaround**, checked before choosing it over the alternative of installing `dylint-link` the way `ci.yml`'s ubuntu lane does: `classify_check` depends only on `candor-classify` and serde — no `#![feature]`, no `rustc_private`, no dylint — so it is a stable crate that was being built on nightly for no reason. Installing the linker would have satisfied the error while leaving the wrong toolchain in place. Re-dispatched; **the job's first-ever complete green, or its first-ever real drift finding, is the outcome to check next** — either would be a first. **CLOSED — verified 2026-09-07 by RUNNING the gate, which is the only thing that could close it.** The fix had ALREADY landed in `78fa172` (2026-09-04) and was never exercised: the workflow is weekly, its only failing run sits at `587275a` (2026-09-03), and `78fa172` is NOT an ancestor of it — so the red on the board was a report about code that no longer existed, and the row stayed OPEN for three days for that reason alone. A `workflow_dispatch` on `main` (`34140458495`) now gets PAST the step every previous run died at: **step 4 'Build the stable scanner' succeeds**, as do the generator selftest, `classify_check`, the 74-crate live fetch and the regeneration. `RUSTUP_TOOLCHAIN: stable` + set-but-empty `RUSTFLAGS` at JOB level (not a per-step `+stable`, which cannot reach a cargo that Python spawns) is the mechanism. **The job still exits 1, but at step 9 — the drift diff — which is the gate DOING ITS JOB and its first real verdict ever: 0 regressed (no `classify()` rule lost), 2 oracle-dropped, 128 newly-uncovered.** Those 128 are NOT a finding yet: the gate's own output invokes R212 — the checked-in manifest is historically macOS and this run is the Linux runner, a same-commit run has measured a different row count across the two, and the host-platform cause was never ruled out. **A gate that has never run is not a gate, and a fix nobody exercised is not a fix** — the same shape as the `net_dns` mutation anchor and `gate-run.sh`'s unrun `self-gate`, all three found on one day. |
| R158 umbrella: `assert-audit.sh` credits `eval/RESULTS.md`, `ci/README.md` and a dozen other non-tests as test coverage | 2026-09-02 | **OPEN — INSTRUMENT defect in the umbrella. Reviewer-measured with a 14-file battery; not independently re-run by the coordinator, but the mechanism is read from the regex** | **An auditor that accepts a changelog as a test — the exact shape it was built to catch — one directory over.** `bin/assert-audit.sh` fails a diff that adds a safety assertion with no test beside it. Its `TEST_RE` was widened on 2026-09-02 after it credited **0 of 9** tracked `eval/**/*.sh`; the widening added the alternative `(^\|/)(ci\|soundness\|conformance\|eval)/` — **a whole DIRECTORY, not the `eval/**/*.sh` the measured need named.** Reviewer's battery (assertion `// PROVEN safe` in `scan.mjs` plus ONE companion file, `HEAD~1..HEAD`): `eval/RESULTS.md`, `eval/notes.txt`, `ci/README.md`, `ci/CHANGELOG.md`, `conformance/SPEC-notes.md`, `conformance/.gitignore`, `soundness/TODO.md`, `spec/overview.md`, `test/README.md`, `docs/tests/plan.md`, `verify-nothing.sh`, `src/latest-test.py` — **all CREDITED as test coverage (rc 0)**. Only a root `.gitignore` and root `CHANGELOG.md` correctly fail. `eval/RESULTS.md` is a real tracked file in candor-rust; candor-ts has `eval/RESULTS-sweep.md`, candor-java `eval/corpus-confirmatory/FINDINGS.md` — **so a corpus round that updates a results file beside a `PROVEN` comment passes the auditor.** | Not fixed. Two more in the same tool, same review: **(1)** its header claims rust `#[test]` in-file counts — but `TEST_RE` is applied to `git diff --name-only` and can only see PATHS, so a fix adding a `#[test]` inside `src/scan.rs` with no new file under `tests/` FAILS; the comment asserts coverage the code cannot have (§E2, in the auditor). **(2)** `scan_range` on an EMPTY range prints *"that is not a pass, there is no diff"* and **returns 0** — the prose and the exit code disagree. Fix direction is a DENYLIST of non-test extensions inside those directories, or restricting the directory alternative to the script/source extensions the regex already enumerates elsewhere — never a broader allowlist. The selftest (4 cases) must gain the companion-file battery, because 4/4 green is what it reported over all of this. |
| R159 rust: `coverage-gate-refresh`'s issue step swallowed its own failure — the job's first finding reached nobody | 2026-09-02 | **CLOSED — candor-rust `479a0bd`. INSTRUMENT defect, found on the job's first-ever complete run** | **`gh issue create … \|\| echo "issue may already exist; skipping"`.** On the job's first real drift finding (39 regressed, 158 newly-uncovered) the create FAILED — neither label it passes existed in the repo — the `\|\|` turned that into a green step and a guessed sentence, and `gh issue list --state all` showed **zero issues, ever**. The reporting channel of a finding-reporting job was fail-open. R104/R148's class exactly. | Labels created; the step now skips only on a genuine *already exists* and otherwise prints gh's real error and exits 1. **The finding it failed to report is the subject of R160–R162.** |
| R160 rust: a public forwarder that calls a sibling via `Self::` is ABSENT from the report | 2026-09-02 | **CLOSED — candor-rust `0cce64f`. PUBLISHED cardinal sin; `Self::` static-call resolution had NEVER existed in any scanner. Coordinator-verified pre- and post-fix on the fixture and on rusqlite 0.40.2** | **CARDINAL SIN, rust, and it is rusqlite's front door.** `impl Conn { pub fn open(p) { Self::open_with_flags(p, 0) } pub fn open_with_flags(p, f) { std::fs::read_to_string(p)… } }` — `open_with_flags` reports `['Fs']`; **`open` is ABSENT** from `functions[]` on the manifest-era scanner (`a9ce6ff`), the published `bffac6c`, and HEAD. **The control differs in exactly one token:** `open2` written as `Conn::open_with_flags(p, 0)` instead of `Self::…` reports `['Fs']`. So a `Self::`-qualified static call to a sibling is not resolved as a call edge, the forwarder reaches nothing, and the *mint no unit unless it carries or reaches an effect* filter omits it. Real instance: **`rusqlite::Connection::open`** (and `open_in_memory`) — absent on rusqlite 0.40.2 under all three scanners while `Connection::open_with_flags` reads `['Db','Unknown']`. The 08-28 coverage manifest still records `rusqlite::Connection::open` as covered, so an even earlier scanner reported it; when it was lost is not yet bisected. | Not fixed. **Gate consequence is the R79/R85 shape**: the callee is reported so a blanket `deny Db` fires incidentally, but `deny Db <caller>`, `pure <caller>`, `path`, `callers` and every question about WHO reaches the effect are blind for the function users actually call. **Fix direction:** resolve `Self::name(...)` in a static-call position exactly as `<EnclosingType>::name(...)` — one authority, not a second arm — and sweep `Self::` in every position the existing type-qualified resolution handles (assoc fn, assoc const, trait static, `<Self as Trait>::`). **FIXED, and the row's one dating claim corrected.** I wrote that an earlier scanner must have reported `rusqlite::Connection::open` because the 08-28 manifest lists it covered. **Wrong:** the oldest buildable scanner (0.3.5, `2952f78`, 2026-06-11) is already ABSENT on the fixture and on rusqlite, and `git log -S` over the whole history finds no commit that ever contained a `Self`-headed resolution arm. **`Self::` resolution never existed**; the manifest row is R162's stale-manifest story, not a scanner regression. **Fix (§G, one authority):** `scan_items` binds `Self` in the ordinary `use` map for the length of one impl block (and to the trait inside a trait default body, as that arm already does for `self`); `lang::expand` — the single place a written path becomes a resolved one — then answers every position, so there is no second arm. The value is `expand(<type>, uses)`, not the bare leaf, so `use inner::Far; impl Reach for Far` resolves `Self::touch` to `inner::Far::touch` exactly as `Far::touch()` does. **Position sweep, built and run:** assoc fn, `map(Self::f)`, nested closure, 3-deep chain, trait-default `Self::helper()`, `Self::new()` in a generic impl, impl-for-imported-type — all fixed. `Self::CONST` and `Self::Variant(..)` expand but match no fn unit, so nothing is charged (parity with `K::NAME`/`State::Open(n)`); **a leaf fallback was deliberately NOT used** because it would have linked `Self::NAME` to an effectful free fn `NAME` beside it. **Still under-reporting, stated:** `<Self as Trait>::m` with no default body (its `<Type as Trait>::m` control fails identically, so not a `Self` issue), `Self::required()` in a trait default, `impl Trait for &[u8]`. **Corpus A/B, 1489 crates, HEAD vs HEAD+fix, every field:** ADDED 2621, **REMOVED 2**, CHANGED 9227 — `calls` +7982, `invisible` +2969, `direct` −2094, `unknownWhy` −2094, **`inferred` +929 pure gain, 0 loss, 0 mixed**, `unresolved` +736, `incomplete` +88. §E1: `SELFALIAS` hit **231,409 times across 1037 of 1490 crates, 0 in the PRE arm**. Both removals audited from source — `crossbeam_epoch::Owned::with_tag` at two versions, whose `self.into_usize()` calls `mem::forget(self)`, so no drop runs: POST correct, PRE was an over-charge. **Near-miss reported, not hidden:** that row is right for a shaky reason — the escape gate is LEAF-keyed and suppresses the drop charge for a consumed by-value `self` whenever the function returns a fresh value of the same type leaf, forget or no forget; on a synthetic `via_self` its `G::mk()` control is absent in BOTH arms, so the fix brings `Self::` into parity with an existing silence rather than creating one (R168). The `unknownWhy` −2094 cluster is js-sys, where the reason moves one hop to the resolved callee; `deny Unknown[native]` over js-sys returns 180 violations in both arms. **Gate consequence:** blanket `deny Fs` fires in both arms via the callee; **`pure Conn::open` and `deny Fs Conn::open` go exit 0 → 1 on the lone-forwarder fixture `scratchpad/rsfwd-iso`** (PRE: the rule binds no unit; HEAD: 1 violation). Two coordinator instrument errors on the way, stated so they are not repeated: `pure rsfwd` is a crate-name scope that binds nothing and returned 0 on both arms; and on the multi-fn fixture `rsfwd` the scope `Conn::open` also binds `open2`/`open_with_flags`, so both arms exit 1 there and only the COUNT moves (2 → 3) — an exit-code control needs a scope that binds the forwarder alone. rusqlite 0.40.2: `Connection::open`/`open_in_memory` ABSENT → `['Db','Unknown']`, `functions[]` 276 → 283. 6 regression tests red on revert; the 7th is an absence control recorded as passing both ways. `gate-run.sh` 25/25, re-run by the coordinator after the agent finished: 25/25 again; `soundness/realworld/run.sh` under Docker with the fix in tree: 20 honest, 0 under-reports, 0 fabrications. **One anomaly the agent could not explain and reported:** a single `gate-run` pass of `21 ok, 4 failed` right after its R155 commit, with four subsequent 25/25 runs and the log directory already cleaned — re-run by the coordinator rather than accepted. **Both adjacent free-fn shapes measured against the fixed binary, and both are DIFFERENT mechanisms:** crossterm's `pub fn size() { sys::size() }` is still absent — `sys` is a `mod`, not a `use`, so `expand` leaves `sys::size` and its tail2 does not match the unit keyed `terminal::sys::unix::size` (module-path arm; R169); R165's bare `from_handle(path)` has no `::` at all, so `ctor_leaf_from_call_path`'s `rsplit_once` yields None — not covered. **[Label corrected 2026-09-03 by the 0.35.0 release panel: `bffac6c` is NOT the published 0.34.0 — the v0.34.0 tag is `22b5ec2`, six commits earlier; where this row says 'published 3cf055d' read 'post-tag 3cf055d'. The row's conclusion was re-checked against the true tag build where the panel could, and stands.]** |
| R161 rust: `rusqlite::init_auto_extension` and `Connection::from_handle` went `['Db']` → `[]` between 08-28 and the 0.34.0 release | 2026-09-02 | **CLOSED — candor-rust `59ecdf9`. Alias arm confirmed; the `Option<fn>` arm was MIS-STATED: `.unwrap()()` always disclosed, the silence is in the UNWRAP BINDERS (if-let/match/.map) and only for a BARE fn pointer. Coordinator-verified on rusqlite 0.40.2** | **CARDINAL SIN, rust — an explicit EMPTY effect set is a positive purity claim.** On identical rusqlite 0.40.2 sources: manifest-era scanner `a9ce6ff` → `['Db']` for both; published `bffac6c` → `[]` for both. Both functions hand a raw `sqlite3` handle to FFI (`sqlite3_auto_extension`; `from_handle` wraps `*mut sqlite3`). Bisect over the 16 scanner commits `79546f3..3cf055d` (`crates/candor-scan`, `crates/candor-classify`), each built and run on the same sources: #8 `dea71ef` `['Db']`, #10 `0810f2d` `['Db']`, #12 `9f202ca` `[]`. **Remaining suspects: #11 `02749cf` (*drop glue fired on the BINDER, so 16 of 17 executed positions were silent*) and #12 `9f202ca` (*drop glue: the escape gate suppressed on SOME-path escape*).** `Connection` carries an `impl Drop` that closes the handle, which is why the drop-glue commits are the plausible pair. **Caught only by the coverage-gate job that had never run** (R157): the fast gate checks `classify()` against the manifest and cannot see a scanner change. | Not fixed; one bisect step remains and then the mechanism. **Do not assume the drop-glue commits are wrong in themselves** — they closed sixteen silent positions; the question is whether a guard they added mis-fires on a `Drop`-carrying type whose method reaches FFI. **CULPRIT `02749cf`, mechanism named with an executed fixture and a flip-check.** #10 `0810f2d` `['Db']` → #11 `02749cf` `[]` for both functions; #12 only tightens SOME-vs-EVERY-path escape for branching code and these have no branches. `02749cf` added `escaping_ctor_leaves`: a construction of a `Drop`-carrying type whose bound name reaches the function's tail/return is treated as escaping and the drop is not charged to that frame — the fix for flate2's constructors (R49's class). In `from_handle`, `InnerConnection::new(db,false)` is bound by `let`, then `db` sits inside `RefCell::new(db)` in the struct literal that IS the tail — escaping. Fixture `scratchpad/dropglue-fixture` reproduces the flip on the same three binaries; **removing `impl Drop` makes the charge vanish on BOTH pre- and post-culprit**, so the charge was drop-glue attribution and nothing else. **THE PREMISE ATTACKED, AND HALF OF IT FELL.** Ground truth by execution (3/3 tests): `from_handle` returns WITHOUT dropping — the file still exists after the call — so **`[]` for `from_handle` is the correct per-frame answer, and the pre-culprit `['Db']` was exactly the over-charge `02749cf` set out to remove.** That half of this row is retracted. **`init_auto_extension` is different:** its only route to `sqlite3_close` is the caller-supplied `ax: fn(Connection) -> Result<()>` — an opaque boundary — and the real scan reports a bare `[]` with **no `Unknown` at all**, while the fixture's analogue correctly keeps `Unknown`. Losing the disclosure along with the charge is the sin that stands; the likely cause — a plain `fn(T) -> U` (here via the type alias `AutoExtension`) not being classified as a callback boundary — is stated as a hypothesis, not a finding. **Two adjacent real gaps found while building the fixture** (filed as R165/R166). The mechanism deletes the graph edge to the `Drop` unit with no residual marker, which is what defeats scoped policies, `path`, `gains` and `fix-gate` while blanket `deny Db` still fires via the independently-listed `Type::drop` units. **Sound direction per the family's own rule:** carve out PROVEN-safe escapes only; an escape through a closure or through a free-fn constructor is not proven and should either charge or leave a tagged edge for the transitive walk to resolve. **THE OPEN HALF RESOLVED, and the row's framing fell with it.** Nine callback-parameter spellings, each an executing fixture (the callback really writes a file), scanned on the manifest-era, published and HEAD binaries: `fn(T)->U`, `F: Fn`, `impl Fn`, `&dyn Fn`, `Box<dyn Fn>`, a fn pointer in a struct field, and `unsafe extern "C" fn` all read `Unknown` + `callback:unresolved call` on all three — correct. **`f: Alias` where `pub type Alias = fn(T) -> U`, and `f: Option<fn(T) -> U>`, are ABSENT from `functions[]` on all three** — identical bytes pre- and post-`02749cf`, and reproducible on a fixture with no `Drop` type at all, so orthogonal to the bisected commit. **Why `init_auto_extension` ever showed `['Db']`:** never from its own `ax: AutoExtension` callback — `is_callable_type` has never fired on that alias in any binary. It **inherited** `from_handle`'s over-charge transitively (`from_handle` is in its `calls` on all three). When `02749cf` correctly removed that fabrication, the inherited charge vanished and exposed the pre-existing alias gap. **A correct verdict produced by an incorrect mechanism, unmasked by removing the over-charge — R147's shape exactly.** Two unrelated defects that flipped in one commit. **Mechanism, line-pinned:** `is_callable_type` (`crates/candor-scan/src/lang.rs:311-350`) matches `Type::BareFn` (:313), `Fn*` trait leaves (:318), and for a bare `Type::Path` only the wrappers `Box`/`Rc`/`Arc`/`Symbol` (:337-344); any other named path — an alias, or `Option<…>` — falls through to `false` (:339/:346-348). Every PARAMETER-position call site uses it raw: `seed_fn_typed_vars` (`lang.rs:362-375`), `collector.rs:2801` (closure param), `collector.rs:2980` (annotated `let`). **Only `record_return` (`decls.rs:1617-1639`) first calls `unwrap_result_option` (`lang.rs:734-745`)** — for a function's own RETURN type, never a parameter; that asymmetry is the `Option<fn>` arm. **And the alias table cannot help even if consulted:** the alias recorder (`decls.rs:1061-1073`) accepts only a `syn::Type::Path` RHS (:1065); `pub type AutoExtension = fn(Connection) -> Result<()>` has a `BareFn` RHS and is never recorded. **Same-mechanism proof in both directions:** on the real rusqlite source, one edit — `ax: AutoExtension` → `ax: fn(Connection) -> Result<()>` — turns `init_auto_extension` from `[]` into `['Unknown']` + `callback:unresolved call`; on the bisect fixture, the reverse edit (introduce the alias) makes the analogue **disappear from `functions[]` entirely** while its executed ground-truth test still passes. **Direction:** always silence. With other recorded calls present the loss surfaces as an explicit `[]` with no `unknownWhy`; when the callback is the function's ONLY call, the function is pruned from the report — an affirmative purity claim under SPEC §2 rule 3, the worse symptom. **Fix shape (not implemented; rust editor is busy):** (1) make `is_callable_type` alias-aware; (2) let the alias recorder capture a `BareFn` RHS (and any RHS carrying an `Fn*` leaf); (3) apply `unwrap_result_option` at every parameter-position call site as `record_return` already does. **Not established:** nested spellings (`&Option<fn>`, `Box<Alias>`, alias-to-`Box<dyn Fn>`, double aliases) and the three further `is_callable_type` sites at `collector.rs:1460/1491/1710`. **Instrument note from the agent, worth keeping:** the `HEAD` binary it used was built from a DIRTY working tree (the R160 agent mid-edit in `decls.rs`/`lang.rs`) — results were byte-identical to the two clean binaries for everything scanned, so its conclusions stand, but *`target/release/candor-scan` is not HEAD while an agent is editing that repo*. **FIXED.** `is_callable_type` gained a crate-wide `callable_aliases` index (collected in the same `syn::Item::Type` arm as `prim_aliases`) and now applies `unwrap_result_option`, which only `record_return` had ever done and only to a RETURN type; `elem_trait_leaves`' payload resolver gained a synthetic `"Fn"` leaf for a bare fn pointer — which is why `Option<Box<dyn Fn>>` always worked and `Option<fn>` never did. Executed 10-spelling fixture: `Alias`, `Box<dyn Fn>`-alias, `&Alias`, `Option<fn>` if-let and `.map` all ABSENT → `['Unknown']` + `callback:unresolved call`; purity controls unchanged; 2 tests red on revert. Coordinator-verified: `rusqlite::init_auto_extension` `[]` → `['Unknown']` on the real 0.40.2 source, `from_handle` stays `[]`. Cache rev17. Residuals, stated in code: a double alias and an alias-typed RETURN are still not resolved (Pass A builds the index and cannot consult it). **[Label corrected 2026-09-03 by the 0.35.0 release panel: `bffac6c` is NOT the published 0.34.0 — the v0.34.0 tag is `22b5ec2`, six commits earlier; where this row says 'published 3cf055d' read 'post-tag 3cf055d'. The row's conclusion was re-checked against the true tag build where the panel could, and stands.]** |
| R162 rust: the coverage generator drops `fs_err::file::open` from `covered.tsv` while every scanner reports it `['Fs']` | 2026-09-02 | **RESOLVED as a PROCESS GAP, not a live generator defect; two real generator bugs found beside it, OPEN. Sonnet-investigated, read-only** | **A row the 08-28 manifest lists as covered (`Fs`/`Fs`) vanishes from BOTH fresh files on CI and on a local run against the same fs-err 3.3.1 sources**, although the manifest-era, published and HEAD scanners all report `file::open` as `['Fs']` (exact-segment match). The generator is byte-identical to the one that produced the manifest. So the difference is not the scanner and not the generator's text — it is in the correlation or the covered decision under conditions that differ between the 08-28 run and today (candidate extraction is regex-based over glob order; `by_fn` keys are `module::type::fn` with no crate prefix). | Not fixed. **This matters beyond fs_err: of the 39 'regressed' rows the job reported, 38 are this class or R160/R161** — so the job's headline number mixes a scanner regression, a pre-existing scanner sin, and a generator artefact, and cannot be read until they are separated. Investigation is read-only and scratch-only until the mechanism is known. **EXPLAINED — and the premise was wrong in the reassuring direction.** `fs_err::file::open` is `pub(crate) fn open` (`fs-err-3.3.1/src/file.rs:27`). `is_bare_pub` correctly excludes it, so it is **not a candidate today** and never reaches `classify_check`; `fs_err::create` (`file.rs:32`) is the twin. fs-err 3.3.1 is immutable and still crates.io's latest, so this is not a version change. **How the row got into the checked-in manifest:** `is_bare_pub` landed in `ef00589` on 2026-08-27, the day BEFORE the manifest was committed (`b6f9155`, then `a9ce6ff`) — yet the row survived all three commits untouched from the gate's first commit `fbfa0c1`. Sibling `pub(crate)` false positives the same fix targeted (`sea_orm::DatabaseTransaction::begin/run`, `ureq::connect*`) DID vanish at `ef00589`, so the fix worked; **the checked-in `covered.tsv` was never regenerated end to end after it.** Today's drop is the fix finally taking effect on a clean regeneration. `entries.json` is gitignored, so the exact operational path (partial-pipeline reuse vs hand-edit) could not be recovered — stated as unestablished. **The 39 'regressed' rows, classified:** 5 stale-manifest artefacts of this kind (fs_err ×2, `portable_pty::CommandBuilder::{as_command,current_directory}` and `crossterm::terminal::sys::unix::window_size`, all `pub(crate)` in both cached versions); **1 genuine scanner silence — `crossterm::size`, a bare-`pub` thin delegate `pub fn size() { sys::size() }` with NO self-scan entry while `sys::size` scans as `Exec` — R160's forwarder class in a MODULE-qualified spelling, handed to the R160 agent**; 19 confirmed upstream drift (ureq 2.12→3.4 renamed `Request`→`RequestBuilder`, sea-orm 1.1→2.0 restructured modules); 7 owned by R160/R161; 7 plausible drift not individually traced. **TWO REAL GENERATOR BUGS, OPEN:** (1) `find_crate_dir` picks the newest cached version by **lexicographic** sort — demonstrated: with `elasticsearch-8.5.0-alpha.1` and `8.19.0-alpha.1` both cached, a local run silently analyses **8.5.0** because `"8.1" < "8.5"` as strings, and `VERSION_OVERRIDE` feeds only the CI fixture's Cargo.toml, not this lookup. CI is unaffected (one version per fetch); any local reproduction is not. (2) `root_reexports()` has been **dead code since the first commit** — computed and discarded (`# noqa: F841`) — so an intermediate-module alias like `terminal::supports_keyboard_enhancement` from `pub use sys::…` can never be a guess; **and that exact guess string is in the checked-in manifest and is unproducible by any version of the generator**, which is further evidence the manifest was not produced by this pipeline as-is. Unexplained, stated. **What a correct instrument does, not implemented:** pin the crate VERSION per manifest row (or snapshot the fixture's `Cargo.lock`) so *regressed* can distinguish drift from an unpinned cache; semver-sort `find_crate_dir`; make `classify_check` refuse an `entries.json` older than `generate.py`; wire or delete `root_reexports`. **And the manifest must be regenerated end to end — AFTER R160/R161 land**, or the regeneration bakes the two scanner sins into `open.tsv` as the new baseline. |
| R163 java: `fieldLambdaBindings` is a body-derived shared input NOT in the refresh digest — R151's class, one field over | 2026-09-02 | **CLOSED — candor-java `59683f3`. Was a ⟨0.35⟩ regression, never published. Fixed on the independent verifier's fixture; coordinator-verified on the same fixture; pushed** | **LEAD, java, same mechanism as R151.** ⟨0.35⟩ added `fieldLambdaBindings` to `AnalysisContext` as a whole-program, BODY-derived shared input, read during per-class analyze (`Candor.java:5295` via `Cha.fieldBoundImplementors`). It is not in `wholeProgramDigest`, and `RefreshBodyIndependenceTest` is structurally blind to it — it computes pre-pass outputs from real bodies in both arms *"because those are already in the digest"*, and this one is not. Predicted shape: class A's BODY gains a second lambda write to a bound field while A's structure holds still → bindings `{L1}` → `{L1,L2}` → B's cached delta keeps only the L1 edge. | Not fixed and **not yet demonstrated** — the R151 agent explicitly did not construct a compiling exploit, and this row must not be read as more than a lead until one runs. It deserves exactly the treatment R151 got: reproduce with an executed effect first, then extend the digest, then a test that fails when reverted. Filed rather than folded into R151 because R151 is closed on measurement and this is not. **REPRODUCED.** Structure confirmed with line numbers: populated in `Candor.prepareScan` (`Candor.java:282`) before the refresh split; read in per-class analyze at `Candor.java:5295-5296` → `Cha.fieldBoundImplementors` (`Cha.java:571-580`); **`Refresh.wholeProgramDigest` (`Refresh.java:296-386`) never references it**; `AnalysisContextInputGrowthTest.java:47` pins it as a shared INPUT, so this is a digest-completeness gap rather than R151's accumulator class; `RefreshBodyIndependenceTest` is blind because both arms call `prepareScan` on the unmutated classes first (lines 73-85). Fixture `scratchpad/r163/`: `Widget.bindSecondary()` v1 no-op, v2 `this.task = Effector::act;` — a bound METHOD REFERENCE chosen so javac emits no synthetic lambda and **`Widget`'s structure is byte-identical by `javap -p`**; `Caller.class`/`Effector.class`/`Main.class` sha-identical across arms; executed — v1 writes nothing, v2 writes the witness file. **Matrix at `6ccdfc8`:** cold v1 → `Caller.go` absent (correct); cold v2 → present, `Fs`, `calls:[Effector.act]`; **warm primed v1 then v2 → `Caller.go` ABSENT, byte-identical to cold-v1, `refresh — reused 3 of 4`**; control primed v2/rerun v2 → correct, `reused 4 of 4`. `analyzed.digest` `d7608ec5a5adc4c4` in all four; `CANDOR_REFRESH_DEBUG`'s digest-input dump is byte-identical between v1 and v2 with no `task`/`Effector`/`lambda` in it. **The cardinal sin under the normal CI configuration, in code that has never shipped — which is the only reason it is not in the advisory.** **Why R151's own field audit missed it:** the field was added in the same release the audit was defending, and the audit was of what the report NAMED plus the fields already there. The fix agent has been told to re-audit every `prepareScan`-populated input against the digest, not the one it was handed. **FIXED, and the sweep found nothing else.** `wholeProgramDigest` now folds `fieldLambdaBindings` in as a sorted map of sorted sets, in R151's batched style — the same authority, not a second digest. **§9 done against the engine's OWN authority:** `AnalysisContext.inputNames()` lists 47 shared inputs; each was perturbed one at a time against the pre-fix digest, and **exactly one failed to move it — `fieldLambdaBindings`**; 26 move it; the remaining 20 are excused in writing by category (structure-derived, dep-derived memos of `crossDeps`, or after-analyze-only with every read site named). **Teeth revert-tested by reverting the fold:** `RefreshFieldLambdaDigestTest` — an end-to-end arm that compiles AND executes the fixture in-test, plus a reflective arm over `inputNames()` in which a new field absent from both the probe set and `EXCUSED` fails BY NAME. **`RefreshBodyIndependenceTest`'s blindness is now measured, not argued:** with the fold removed it stays green (1/0) alongside `AnalysisContextInputGrowthTest` (3/0) while the two new tests go red; its doc's *"those are already in the digest"* and `Refresh`'s matching class-doc clause are corrected — `assert-audit.sh` flagged exactly those two lines. `bin/refresh-equiv.sh` gains a field-binding axis that FAILS against the pre-fix jar. **Cost: none measurable, and the branch provably runs** — guava (1,969 classes) digest 14.8/16.8/15.4 ms → 12.1/14.4/15.3 ms, reuse 1,969/1,969 both, reports byte-identical between jars; the debug dump shows 3 bound fields folded on guava and 1 on candor's own classes, so the zero is not over a branch that never executed. **Coordinator-verified on the verifier's fixture against the fixed jar:** cold v1 exit 0 absent; cold v2 exit 1 `['Fs']`; **warm primed v1 then v2 → exit 1, `reused 0 of 4`, `['Fs']`** (was exit 0, `reused 3 of 4`, absent); control primed v2/rerun v2 → exit 1, `reused 4 of 4`. Gate used: `pure Caller.go` — the verifier's `deny Fs` does not discriminate because `Effector.act` violates it in both arms, a point the fixer caught. All gates green including `ci/self-gate.sh`, `refresh-equiv.sh` and `soundness/dynamic/corpus.sh` by hand. |
| R164 java: `refresh` is honoured from the environment and DROPPED from `.candor/config` with an "unknown key" message | 2026-09-02 | **OPEN — two paths disagree; may be deliberate. Reported by the R151 agent** | **Config/env split, java.** `Refresh.forScan` reads `cfg.value("refresh", "CANDOR_REFRESH")`, but `refresh` is absent from `Config.KNOWN_KEYS`, so a `.candor/config` line sets nothing and prints `ignoring unknown config key 'refresh'` — while the environment variable works. A user who moves the setting into config reads the message as a typo warning and loses the refresh. | Not fixed. Possibly deliberate — the config key vocabulary is cross-engine and may not be candor-java's to widen unilaterally — so this is a question before it is a fix: either `refresh` joins the shared vocabulary (SPEC §3.4 / PART 13) four-way, or the env path should be documented as the only one. What is NOT acceptable is the current state, where the two paths silently disagree ([[candor-scan-guards]] is the precedent: a config key that reads as ignored while honoured, or honoured while ignored, is a false disclosure). **Sibling fact from R151's independent verifier:** `CANDOR_REFRESH` itself — the cache the whole of R151 is about — is documented in none of `README.md`, `AGENTS.md` or `--help` (zero hits in each); the verifier derived the interface from `Refresh.java` and `bin/refresh-equiv.sh`'s header. And the only disclosure that a result was REUSED is a stderr line, absent from the report envelope, so a consumer reading the JSON cannot tell a cached answer from a fresh one. Both belong with this row: the env/config split is one symptom of a feature that has no stated contract. |
| R165 rust: a `Drop`-carrying value obtained through a FREE FUNCTION is invisible to the escape gate, so its local drop is not charged | 2026-09-02 | **CLOSED — candor-rust `520bdef`. The first cut FABRICATED on both over-charge controls and was caught by the fixtures written first** | **CARDINAL SIN, rust.** `ctor_leaf_from_call_path` recognises only `Type::method()`-shaped calls as possible constructors. A caller that obtains a `Drop`-carrying value through a bare free function — `let c = from_handle(p);` — and lets it drop in its own scope is charged `['Fs']` before `02749cf` and goes **entirely ABSENT** after (fixture `scratchpad/dropglue-fixture`, `caller_holds_locally`; ground truth executed — the file really is removed in that frame). In rusqlite the direct instance is masked because `Connection::from_handle` IS an associated function, so direct callers re-derive their own construction charge; **any caller reaching it through an intermediate free function inherits nothing**, because the intermediate's report is empty and carries no residual edge. | Not fixed. The undocumented half of `02749cf`'s escape logic: the documented half (closure-tail escapes) says it is conservative in the non-fabricating direction; this half is conservative in the FABRICATING-avoidance direction at the cost of silence. Same sound answer as R161: charge, or leave a tagged edge. **FIXED.** The drop-glue marker consults the crate's own `ReturnIndex` when the callee path names no type — a declared fact, not a name heuristic, so R160's refusal to leaf-fall-back is untouched (that was about resolution guessing). `ctor_type`'s sentinel filter chain extracted to `recorded_return_type` and shared. **The first cut fabricated:** widening only the marker charged `fn forwards(p) -> H { from_handle(p) }` and the `mem::forget` control, because `ctor_leaf_of_expr` is read by the marker AND by `mark_escape`; `returns` was threaded into that one function so both sides learn the fact together. Executed fixture: `holds_free` ABSENT → `['Fs']` (the file really is removed in-frame); escape/forget controls stay absent; 1 test red on revert. Corpus: 17 correct x11rb gains of the `let _ = <free fn>(..)?;` cookie shape. §E1 `R165RET` hit 75,797 times across 1100 crates. |
| R166 rust: `sqlite3_auto_extension` is missing from the classifier's DB FFI leaf list | 2026-09-02 | **CLOSED — candor-rust `5155eb8`, as `Db`. The WHOLE `sqlite3_*` surface audited (185 symbols), not the one name: nine leaves added. Coordinator-verified** | **Classifier coverage gap, rust.** `auto_extension::register_auto_extension` calls `ffi::sqlite3_auto_extension` directly and reads `[]` on the 08-28, 0.34.0 and HEAD scanners alike; `grep` confirms the symbol is absent from candor-classify's DB FFI leaves. Registering an auto-extension mutates global SQLite state that every later connection runs — the effect is not local to the caller. | Not fixed. A one-entry addition to the leaf list, with a fixture that calls it; the question worth a minute is whether `sqlite3_auto_extension` is `Db` (it changes what every later `open` executes) or something the vocabulary lacks (process-global mutable state). **FIXED as `Db`.** Nine leaves added: `auto_extension`, `cancel_auto_extension`, `reset_auto_extension`, `enable_load_extension` (the process-global extension registry — it changes what every LATER `sqlite3_open` executes, so it is not attributable to a caller's own frame) and `serialize`, `deserialize`, `db_cacheflush`, `file_control`, `wal_autocheckpoint` (database-content I/O nobody had looked at). Membership criterion written into the file; the per-connection CALLBACK installers (`create_function*`, `*_hook`, `set_authorizer`, `trace_v2`) deliberately still absent and named, with two boundary-control assertions. Coordinator-verified: `rusqlite::register_auto_extension` `[]` → `['Db']`. Corpus: 4 ADDED + 21 `inferred` gains across FOUR crates — rusqlite 0.32.1/0.39.0, diesel 2.3.12, sqlx-sqlite 0.8.6/0.9.0. **Unexamined and named: the libgit2 table got no equivalent sweep.** |
| R167 rust: a bare `#[test] fn` at module scope is scanned as PRODUCTION code | 2026-09-02 | **OPEN — PRE-EXISTING, over-report direction. Found by the R160 agent in its A/B's added rows** | **Over-charge, rust.** `is_cfg_test` matches only `#[cfg(test)]`; a `#[test]`-attributed fn placed directly at module scope (sqlx-postgres does this in `options/parse.rs`) is treated as production. 46 of R160's 2,621 added rows are such harness functions carrying `['Env','Fs','Log']`. | Not fixed. Fails toward over-report, so not a sin — but it is a fabrication in every report that contains one, and after R122 correctly made `any(test, feature)` production, the family should state the rule for a bare `#[test]` explicitly rather than by omission. **RE-MEASURED 2026-09-04: STILL OPEN, over-report direction as the row says.** `scratchpad/stale-rust/``r167`: a bare `#[test] fn` at module scope appears in the default scan and is flagged by `pure bare_test_fn_writes`, while the canonical `#[cfg(test)] mod tests { .. }` spelling is correctly excluded by default and appears only under `--include-tests`. |
| R168 rust: the leaf-keyed escape gate loses a by-value `Drop` parameter's drop when the function returns a fresh value of the same type | 2026-09-02 | **CLOSED — candor-rust `8d35a2f`. Measured over-charge stated: 4 x11rb rows where `mem::forget` sits one hop away. Coordinator-verified on `dropprobe`** | **CARDINAL SIN, rust — `escaping_ctor_leaves` is keyed on the type LEAF.** A function that consumes `self: G` by value and returns a newly-constructed `G` has its parameter's drop suppressed as if it escaped, because the returned value shares the leaf. `crossbeam_epoch::Owned::with_tag` happens to be correct (it `mem::forget`s), but a synthetic `via_self(self) -> G { Self::mk() }` with NO forget is absent — **and so is its `G::mk()` control, in both arms**. R160 brings `Self::` into parity with this existing silence; it did not create it. | Not fixed. Same family as R165 — the escape gate over-approximates escape — and the same sound answer: key on the VALUE that escapes, not the leaf, or charge. **FIXED.** The parameter-owned route gets `note_owned_param_drop`, which skips the LEAF gate and nothing else — that gate answers about a CONSTRUCTION, and a by-value parameter is not constructed here; its own gate is value-keyed (`escapes.names`) and untouched. Executed ground truth with a drop counter: via_self 1, via_type 1, consume_only 1, forgets 0, returns_self 0 — the scanner now matches all five; 1 test red on revert. Coordinator-verified on `scratchpad/r160/dropprobe`: `via_self`/`via_type` ABSENT → `['Fs']`, `forgets`/`returns_self` stay absent. **MEASURED OVER-CHARGE, 4 rows in 1489 crates, all x11rb `*::replace_connection`:** `into_sequence_number`/`consume` `mem::forget(self)` one hop away — the `with_tag` shape, out of syntactic reach. Refusing it would need a by-value-`self` method index that fails in the SILENT direction on a name collision, which the denylist rule forbids. §E1 `R168OWNED` 142 hits / 43 crates. |
| R169 rust: a forwarder calling a sibling through a MODULE path (`sys::size()`) is absent — a different arm from R160 | 2026-09-02 | **CLOSED — candor-rust `3944e4c` — BUT THE ROW'S STATED MECHANISM WAS WRONG.** Not a module-relative path: a `#[cfg]`-DUPLICATED intra-crate re-export that `reexport_aliases` dropped as ambiguous | **CARDINAL SIN, rust.** crossterm 0.29.0's `pub fn size() { sys::size() }` gets no self-scan entry while `terminal::sys::unix::size` reads `['Exec','Unknown']`. `sys` is a `mod` declaration, not a `use`, so `lang::expand` leaves the path as `sys::size` and its tail2 does not match the unit keyed `terminal::sys::unix::size`. Nothing about `Self` is involved, so R160's binding cannot reach it. | Not fixed. Same family as R160 — a forwarder that reaches an effect is not minted — with a different gap: a module-relative path into a declared child module is not joined to the enclosing module path. For the next rust agent, with R161/R165/R166. **MECHANISM CORRECTED, then fixed.** Not `expand` and not module-relative paths: with ONE re-export edge and both platform modules present the module-relative spelling ALREADY resolved, and with two edges the `self::`- and `crate::`-rooted spellings fail identically — one variable, the second `#[cfg]`-gated `pub use`. The cause is `reexport_aliases`' `from_edges.len() != 1` rule, which dropped a `#[cfg]` platform split whenever it was spelled as two gated `use`s rather than one edge with several definitions — the very fact that function's doc comment says it keeps, and the question R105 had already settled for the alias map. Fixed by unioning, with explicit-import-beats-glob precedence (Rust's own rule, executed control); 2 tests red on revert. `crossterm::terminal::size` ABSENT → `['Exec','Unknown']`, plus `window_size`, `enable_raw_mode`, `disable_raw_mode`, `is_raw_mode_enabled`. The union exposed R170. **The correction is worth as much as the fix: anyone reasoning from my 'module-relative' wording would have looked in `expand` and found nothing.** §E1 `R169MULTI` 225 hits / 48 crates. **Branch-wide A/B, 1489 crates, PRE = `64ab5c4`, one variable per row:** ADDED 611, **REMOVED 0**, CHANGED 1176 (`calls` +913, `invisible` +277, **`inferred` +171 gain / 0 loss / 0 mixed**); with no removals the ADDED rows were audited instead — 476 `[]`, 115 `['Unknown']`, all 20 with a concrete effect traced to source. Gates 25/25 on the branch; Docker oracle (`:ro`): 20 honest, 0 under-reports, 0 fabrications; `disclosure_recall.sh` 28/28. `soundness/oracle.sh`/`oracle_pf.sh` UNRUN in that container — 'no candor report' for every seed, IDENTICAL on the pre-image, so an instrument failure in both arms, not a finding. |
| R170 rust: `reexport_aliases`' claimants index cannot see a module whose `pub use` sits inside `cfg_if!` — a tail2 key it owns can be claimed by another module | 2026-09-03 | **OPEN, GUARDED — the guard is gated to the keys R169's union newly admits, ON MEASUREMENT; a universal guard is a separate change** | **CARDINAL SIN, rust, found by R169's fix.** A module whose re-export is macro-hidden contributes no `Reexport` edge, so it cannot contest a tail2 key it owns: `file::tempfile_in`'s `imp::create(dir)` resolved into tempfile's DIRECTORY module and lost `file::imp::unix::create`'s `Env`. Guarded via `macro_modules` for the keys R169 admits (fixture `a_tail2_key_a_macro_hidden_module_could_also_own_is_not_resolved_by_the_union`). **Applied universally the guard removes 27 rows across 4 crates, eight of them async-std `Path::is_dir`/`exists`/`metadata`/`read_dir`/`canonicalize`/`read_link`/`symlink_metadata`/`is_file` losing `['Fs','Unknown']`** — a cardinal sin traded for a fabrication fix, which this family does not do without the second fixture first. | Guard shipped with R169; the universal form is OPEN. Same family as R142–R145 (macro silence). **RE-MEASURED 2026-09-04 on the real crate the row cites, and the recommended METHOD masks it.** Scanning `tempfile-3.10.0` standalone: `file::tempfile_in`, which really creates and writes a temp file, is entirely absent from the report, while the colliding `dir::imp::unix::create` — same bare name, exported by an ordinary `#[cfg(unix)] pub use` rather than inside `cfg_if!` — is correctly disclosed; `deny Fs file::tempfile_in` exits 0 over real file creation. **Instrument warning for anyone re-checking this: scanning a hand-written CONSUMER crate with `--deps`, which the corpus brief recommends, does NOT reproduce the miss** — `tempfile::tempfile_in` gets charged `['Fs']` there via a separate built-in classifier rule for that well-known API name, masking the claimants-index bug for that caller pattern. The defect is real on the library's own source and on any caller no built-in rule covers. |
| R171 rust: the coverage-gate GENERATOR's own renderer dropped 24 rows, and the drift job diffed on a key the generator can rewrite without any change in coverage | 2026-09-03 | **FIXED on candor-rust branch `cg-generator-fix` (`2bc7fb0`), merged after the 0.35.0 stage. INSTRUMENT defect, four parts, three silent** | **INSTRUMENT, rust — found by separating the weekly job's first real finding.** (1) `classify_check` deduplicates candidates on IDENTITY — the fix whose comment occupies 12 lines — then renders `crate\tpath\teffects\t…` discarding the identity and `.dedup()`s the lines: two distinct public entry points that guess the same crate-root alias and agree on effects became one row. 24 measured (23 fs_err, 1 reqwest `blocking::multipart::Part::file`). The generator printed `core-effect candidates: 1553 \| covered: 1057 \| open: 445` on every run and nothing did the arithmetic. (2) `consumer_path` is chosen by a DIFFERENT rule in each file (covered: first guess classify() resolves; open: shortest), so one candidate has two spellings and the workflow compared a fresh OPEN key to a checked-in COVERED key — a removed rule read as regression AND growth. (3) An inline `mod NAME { }` made a `pub fn` invisible (`depth == 0`) and an `impl` mis-keyed (`self_scan_found: false`, indistinguishable from pure): 138 such functions across 74 crates, `aws_config::ConfigLoader::load` the measured mis-key; 16 real entry points recovered. (4) `find_crate_dir`'s lexicographic version pick (R162, named-not-fixed) silently analysed the WRONG VERSION of 4 of 74 crates locally (elasticsearch 8.5.0-alpha.1 vs 8.19.0, rand 0.9.5 vs 0.10.2, rand_core, etcetera) while CI, holding one version per crate, could never see it. **Provenance finding for R162:** three checked-in rows (`crossterm::terminal::supports_keyboard_enhancement`, `mongodb::client_encryption::ClientEncryption::decrypt`, `sqlx_core::migrate::resolve_blocking`) carry a `consumer_path` NO version of `generate.py` can emit — they are the spellings classify.rs's rule comments use — while their effects column matches today's generator exactly: the path column was hand-edited after generation. **Coordinator's premise was wrong:** I dispatched this as '14 keys the generator drops while the scanner covers them'; exactly ONE was. | FIXED: `entry` identity column in both manifests, closed accounting that exits non-zero, inline-mod module paths, semver `version_key`, dead `root_reexports` DELETED (the guess builder already emits the crate-root alias unconditionally — the over-approximate direction — so wiring it would have REMOVED guesses), workflow diffs on the identity column and fails loudly on a pre-`entry` manifest. **The generator had NO self-test surface**; `generate.py --selftest` (9 checks, pure Python) now runs on every push and before the weekly fetch; three `classify_check` renderer tests. Revert-tested: 3/9, 5/9, 3/3 red. Isolated A/B over the 70 crates whose version did not move: identities 1101 → 1117, LOST 0, GAINED 16 (all read from source), 0 surviving identity changed effects. Still open and named in code: the intermediate-module alias (`sqlx_core::migrate::resolve_blocking`) is never guessed, so such rows land in open.tsv under a spelling no rule matches. `rusqlite::Connection::extension_init2`'s checked-in `Db,Unknown` is unreproducible from any scanner on this machine. |
| R172 rust: R165 + R160 feed the LEAF-keyed construction escape gate — a real in-frame drop beside a returned construction of the same type is now ABSENT | 2026-09-03 | **CLOSED — candor-rust `1bdf5b3` (branch `fix-drop`, merged into main `a8fa8c6` before the cut). The release-blocking regression; coordinator-verified on the panel fixture against the true v0.34.0 build** | **CARDINAL SIN, rust.** `pub fn swap_free(p,q) -> H { let _g = H::new(p); from_handle(q) }` and `impl H { pub fn swap_self(p,q) -> H { let _g = H::new(p); Self::mk(q) } }` (H's `Drop` does Net): published 0.34.0 (tag 736fa64) `['Net']`, 0.35.0 candidate ABSENT; control `swap_type` (`H::mk(q)`) absent in BOTH arms — the leaf gate is pre-existing, the two fixes EXTEND it onto previously-correct code. Ground truth executed (one `H::drop` runs inside each frame). `pure swap_free`, `deny Net swap_free`, `pure H::swap_self`, `deny Net H::swap_self`: published 1, candidate 0. Mechanism: `ctor_leaf_of_expr` now yields `H` for `from_handle(q)` (ReturnIndex) and `Self::mk(q)` inside `mark_escape`, so `H` lands in `escapes.leaves` and `note_construction` suppresses `_g`'s drop — R168 fixed exactly this for PARAMETERS and left the construction half leaf-keyed. Fixture `scratchpad/panel-fixes/``esc/`. Corpus incidence 0 + the 2 crossbeam rows R160 audited as 'correct'. | Not fixed. Key construction escapes on the BINDING that reaches the exit, as R168 did for `escapes.names`. **FIXED: construction escapes are keyed on the SITE, not the type leaf.** `swap_free`/`H::swap_self` back to published 0.34.0's `['Net']`, and `H::swap_type` — the same leaf-gate victim one spelling over — charged for the first time; executed ground truth one `H::drop` per frame; `fwd`/`holds_two` controls hold. 1,504-crate wide-key A/B vs the candidate: ADDED 7, REMOVED 0, LOST 0 — the 7 additions are pre-existing over-approximations the fix unmasks (a method RECEIVER never treated as escaping while `CSlice::into_ptr(self)` forgets it; `Self::is_unique(this)` in an `if` condition read as a construction) — over-report, stated. §E1 7,063 hits / 737 crates; the row's own shape has zero corpus incidence, so the corpus is safety evidence. **The fix was wrong three times before it was right, each caught by the A/B's ADDED column:** a callee path recorded as a site (openssl `MemBio(bio)`), macro-borne sites unmatchable by address (which had LOST a published charge), and site intersection across exits (anyhow `ensure::render`); each is now a named guard with a discriminating red test. Revert 2/3 red plus one per sub-guard. |
| R173 rust: an ordering-blind `?` veto charges a constructor its OWN type's drop; R160 makes it fire on the dominant `x?; Ok(Self::new())` spelling | 2026-09-03 | **CLOSED — candor-rust `94446f5` (branch `fix-drop`, merged). The panel's 106 was 136; 39 of them gone, 97 remain and are NOT this mechanism** | **Over-charge, rust.** `pub fn order(n) -> Result<Wr,()> { gen(n)?; Ok(Self::for_region(n)) }` — nothing is dropped in-frame; candidate charges `Wr::drop`, published absent; control `order_type` (explicit `Wr::for_region`) charged in BOTH arms, so the veto (a `?` early exit treated as dropping a construction that does not yet exist) is pre-existing. Corpus, R160 alone: 302 rows gain a `::drop` edge, 209 constructors charged their own type's drop, **106 new positive claims** (x11rb 36, zstd-safe 8, termwiz 7, tokio 5 — `SemaphorePermit::split` absent → `['Log','Unknown']`; async-io `Async::new` `[]` → `['Log']`). R160's A/B booked these inside '929 pure gain'. Fixture `scratchpad/panel-fixes/``x11/`. | Not fixed. The veto must consider only constructions that PRECEDE the `?` in evaluation order. Fails toward over-report; not a sin, but 106 false rows in a release. **FIXED: the `?` veto is positional and removes only what is live when it is evaluated.** `order`/`order_type` uncharged (the second a pre-existing over-charge), `order_after` and a construction between two `?`s (`order_between`, a control the row did not name) still charged — executed, one drop each on the early-exit path. 1,504-crate A/B: REMOVED 37 / LOST 128, every removal read from source; replaying R160's C45→D26 arm reproduces 302 rows gaining a `::drop` edge and 209 self-charged constructors exactly but counts **136** new positive claims (row gained an edge AND an effect), not 106; 39 disappear incl. the whole 36-row x11rb wrapper family; **97 still stand** (moka 18, tokio 17, allocator-api2 7, termwiz 7) — outside this mechanism, several with the R175 shape (now merged) — re-measure after the union. §E1 106,436 hits / 910 crates. |
| R174 rust: R165's return-index route bypasses `local_type_leaf`'s deliberate std refusal, and is blind to a unit-returning twin | 2026-09-03 | **CLOSED — candor-rust `47f7990` (branch `fix-drop`, merged). Coordinator-verified: `read_it` `['Fs','Net']` → `['Fs']`, `count` `['Net']` → absent** | **Over-charge, rust.** (a) `ctor_leaf_from_call_path` returns None for a std-rooted path ON PURPOSE (the tokio `Acquire` story); R165's new `or_else` then keys the same call on the bare leaf: with a local `fn open(..) -> Conn` / `Conn::new` (Drop = Net), `read_it` (`File::open(p)?`) `['Fs']` → `['Fs','Net']`, `count` (`HashMap::new()`) absent → `['Net']`, `is_dir` (`Path::new(p).is_dir()`) `['Fs']` → `['Fs','Net']`. 3,896 std-rooted `R165RET` hits across 480 crates. (b) R165's 'ReturnIndex already drops a leaf recorded with two return types' is FALSE when the twin returns `()` — `record_return` returns early on `ReturnType::Default`: git2's free `crate::init()` vs `Repository::init` gives 76 functions per git2 version (×3) a phantom `Repository::drop` edge; effect sets unchanged only because `git_repository_free` is unclassified, `path`/`callers`/`gains` already wrong. Fixtures `scratchpad/panel-fixes/``stdcoll/`, `stdcoll2/`. | Not fixed. The return-index route must honour the same refusals as the path route, and a leaf with ANY conflicting definition (including unit-returning) must not resolve. **FIXED:** the return-index route refuses std-rooted callees exactly as `ctor_leaf_from_call_path` does, and a unit-returning twin is a conflicting definition (`record_return` no longer skips `ReturnType::Default`). `read_it`/`count`/`is_dir` back to published; git2's phantom `Repository::drop` edges gone; R165's `holds_free` control still charged. REMOVED 18 / LOST 7 over 1,504 crates, all audited — 8 (syn ×7, windows ×1) are a precision cost: pure functions losing an `invisible` disclosure to the new ambiguity (non-gating, SPEC 1378-1388). **The `R174UNIT` counter is a FLOOR:** it fires only when a typed entry is withdrawn by a LATER unit twin; the reverse order lands in the pre-existing conflict arm with no counter (windows-0.56.0 measured). Side-finding R186. |
| R175 rust: `Self` inside an impl NESTED in a method body resolves to the OUTER impl's type | 2026-09-03 | **CLOSED — candor-rust `3caf9ff` (branch `fix-resolve`, merged before the cut). Wider than filed: nested TRAIT, closure-nested impl, fn-nested impl, generic impl's method all leaked** | **Over-charge, rust.** `A::outer` contains `struct N; impl N { fn eff() { 2 } fn go() { Self::eff() } }`: published absent, candidate `['Fs']` via `A::eff`. Truth: pure. The R160 binding is scoped to the enclosing impl block but a nested impl inside a body does not rebind. Fixture `scratchpad/panel-fixes/``selfscope/`. Corpus incidence unmeasured. | Not fixed. Bind `Self` on entry to EVERY impl block, including those nested in bodies, and restore the outer binding on exit. **FIXED.** `CallCollector::uses` is a `Cow`; `visit_item_impl` and a new `visit_item_trait` rebind `Self` on entry and restore on exit; a non-nominal self type UNBINDS rather than inheriting. 6 fabrications removed on an executed fixture, every R160 gain kept, 8 fabricated call edges dropped corpus-wide (hashlink `impl Default for …Visitor { Self::new() }` had edged to `LinkedHashMap::new`). Revert 5/8 + 1 red. 1,504-crate wide-key A/B: ADDED 4 / REMOVED 3 / CHANGED 41 / EFFECT-LOST 0; the 3 removals are serde_with `impl Visitor for Helper { Self::Value::parse }` whose `invisible:["time_0_3"]` came from expanding `Self` through the OUTER type's import — published has no row either and the post-fix answer equals the explicit spelling's (κ-ledger cost stated). `macro_rules!` inside an impl is not legal Rust; a crate-local macro expanding to a call is silent in tail/`let`/statement position in ALL THREE arms — a residual, not a `Self` gap. §E1 1,559 hits / 342 crates. |
| R176 rust: R169's explicit-beats-glob is applied ACROSS `#[cfg]` arms — one platform's effect is silenced behind the other's positive claim | 2026-09-03 | **CLOSED — candor-rust `154b76b` (branch `fix-resolve`). Coordinator-verified: `size` `['Fs']` → `['Exec','Fs']`. Its own first cut introduced a silence, caught by a fixture** | **Silent, rust.** `#[cfg(unix)] pub use unix::*` (size = Exec) + `#[cfg(windows)] pub use windows::size` (Fs): `size` published absent → candidate `['Fs']` only; unix's Exec is now silent behind a positive claim. Two explicit arms union correctly (`['Exec','Fs']`); explicit-pure-over-glob with no cfg is correct. Fixture `scratchpad/panel-fixes/``cfgx/`. | Not fixed. Explicit-beats-glob is Rust's rule WITHIN one configuration; across `#[cfg]` arms every arm's edge is live and the union must keep the glob edge. **FIXED, failing toward the UNION (over-charge).** Executed ground truth on this host: `size()` really spawns `stty`; the candidate published the WRONG platform's effect with the live one silent. `Reexport` gains `cfg_gated`; explicit-beats-glob applies only when no contributing edge is gated (comparing cfg predicates would be a narrowing on a property the scanner cannot evaluate — refused). Cache rev 18 — and the schema string had said rev16 while the ladder said rev17, so two builds of one version had shared a cache key. **The first cut silenced a key:** widening to the union past `REEXPORT_FANOUT_MAX` (12) dropped it entirely (`go` `['Fs']` → ABSENT); the union now falls back to the explicit set where it does not fit, fixture red without it, 0 corpus hits. `size2`/`size3` unchanged; crossterm/rusqlite byte-identical; R170's guard untouched. §E1 `R176CFGUNION`: 10 keys / 1 crate (cap-primitives `fs::*_impl`) — low incidence is the finding, not a reason to skip it. |
| R177 rust: a callback stored as `Option<Alias>` where `Alias = Box<dyn Fn()>` and invoked through `if let Some(c) = &self.cb { c() }` is ABSENT | 2026-09-03 | **CLOSED — candor-rust `6e8a3c1` (branch `fix-resolve`). Coordinator-verified: `fire_if_let`/`fire_map` ABSENT → `['Unknown']`. Wider than filed; its own fix caused a leaf-collision regression, guarded** | **CARDINAL SIN, rust.** `pub type Cb = Box<dyn Fn()>; struct Holder { cb: Option<Cb> }; fn fire_if_let(&self) { if let Some(c) = &self.cb { c() } }` → ABSENT on published 0.34.0 AND the candidate; `pure`/`deny Fs`/`deny Unknown Holder::fire_if_let` all exit 0; controls exit 1; the direct-typed spelling discloses on HEAD (one variable: the alias). Ground truth `cargo run` executed. Fixture `scratchpad/panel-spec/``rs-alias/`. | Not fixed. Same family as R161; the alias index must be consulted at the unwrap-binder site for a `Box<dyn Fn>` payload, not only for a bare fn pointer. **FIXED.** Not only `if let`: `match`, `.map`, `.as_ref()`, `while let`, `for`, an `Option<Alias>` parameter, a `let` annotation, an alias-typed RETURN and a DOUBLE alias were all absent. `elem_trait_leaves` now asks `is_callable_type` (one authority) instead of being a second implementation, and a per-file pre-pass collects `type NAME = <callable>` to a fixpoint BEFORE the walk that consumes them. 10 rows ABSENT → `['Unknown']` + `unknownWhy`; `deny Unknown Holder::fire_if_let\|fire_map` 0 → 1 (`pure <fn>` stays 0 by design — a `pure` rule forbids real effects, not Unknown). **Regression its fix caused, caught by a fixture:** `callable_aliases` keyed by leaf — `mod a { type H = Box<dyn Fn(&str)> }` beside `mod b { struct H; struct S { hs: Vec<H> } }` displaced the concrete typing and `for h in &self.hs { h.run(p) }` went `['Fs']` → ABSENT; `callable_hedge_only` keeps the hedge beside a concrete element type (0 corpus hits, safety-only). R181's shapes: same-file double alias and alias-typed return now DISCLOSED; cross-file chain still silent (per-file `FileDecls`); the collision not attempted and not widened. A/B shared with R175/R176: EFFECT-LOST 0; published → branch 667 removed / 562 lost, byte-identical to published → candidate. §E1 196 hits / 31 crates. |
| R178 swift: a stored optional closure invoked through `if let c = cb { c() }` or `cb.map { $0() }` is ABSENT — `cb?()` discloses | 2026-09-03 | **CLOSED — candor-swift `17ea885`. Fourteen binder spellings, not two** | **CARDINAL SIN, swift.** Both `typealias` and plain `(() -> Void)?` fields: `fireIfLet`/`fireMap` ABSENT (3 of 8 analyzed); `pure`/`deny Fs`/`deny Unknown Holder.fireIfLet` all exit 0 'policy ✓'; controls exit 1; `cb?()` discloses `callback:computed`. Ground truth executed (5 bytes read). Identical on the true v0.34.0 swift build (`scratchpad/panel-spec/``sw034/`). Fixture `scratchpad/panel-spec/``sw-alias/`. | Not fixed. **FIXED.** Enumerated before writing: `if let`, `if let self.`, `guard let`, shorthand `if let cb`, `.map { $0() }`, `.map { f in f() }`, `switch case .some(let c)`, `if case let .some(c)`, `if case let c?`, `let c = cb ?? {}` — over both a `typealias` and a plain field, plus a fn-typed optional param and local — all ABSENT, byte-identical on the true v0.34.0 build; `cb?()`/`cb!()`/`(cb ?? {})()` were already honest. Six different visitors shared nothing but the missing answer to 'the unwrapped value is a FUNCTION' — one predicate `callableValue`. **A second defect underneath:** `typeName` returned `(nil, true)` for a function type, so `typealias Cb = () -> Void` was in NO alias table and an alias-typed field/param/local was invisible while the plain twin disclosed. Ground truth executed (20 fire, 6 controls quiet). Real code: Alamofire `Adapter.adapt` ×2 / `Retrier.retry` ABSENT on v0.34.0 → `['Unknown'] dispatch:Adapter.adaptHandler`. A/B 5 corpora, 15,861 units: ADDED 3 / REMOVED 0 / CHANGED 62, 0 effects lost, 0 call edges lost. Three regressions caught by the A/B before shipping (a property/builder name collision in swift-nio, a fn-typed param re-bind in Alamofire, and its own false 'opaque BY CONSTRUCTION' comment flagged by assert-audit). swift test 1064/0, smoke 148/0. |
| R179 java: a method reference passed as an ARGUMENT to a JDK higher-order function (`ifPresent(Runnable::run)`, `forEach(Runnable::run)`) is ABSENT — the lambda spelling is disclosed | 2026-09-03 | **CLOSED — candor-java `b6892ca`; PART 87 now carries the arm (`d41cf7d`) and reads green** | **CARDINAL SIN, java.** `Optional.ofNullable(task).ifPresent(Runnable::run)` and `Stream.of(task).forEach(Runnable::run)`: `fireRef`/`fireStream` ABSENT on the 0.35.0 jar AND the published 0.34.0 jar; `pure app.Widget.fireRef` 0, `deny Fs` 0, `deny Unknown` 0; controls `pure app.Store.write` 1, `deny Unknown app.Widget.fireLoop` 1. One variable: `ifPresent(r -> r.run())` is disclosed `Unknown`. Ground truth executed (file exists: true). Not R84 (field-bound abstract ref). The field-stored method-ref toggle `this.task = s::write; task.run()` IS fixed at HEAD (0.34.0: ABSENT) but unpinned — PART 87 has no method-reference arm at all (`# ENGINES: java ts`; java arms are lambda-only). Fixture `scratchpad/panel-spec/``java-mref-*`. | Not fixed. PART 87 needs the arm; the spec CHANGELOG's 'pins it four-way' is false. **FIXED.** `ProvValue.fromIndy` suppressed the hand-off `Unknown` on the ground that 'the body is edged at creation' — false when the handle names a functional interface's OWN abstract SAM. Now carries `samForwarder` and discloses `callback:java.lang.Runnable.run`, byte-identical to the lambda spelling's channel; keyed on `SAM_OF`, NOT `handleTargetConcrete`, which fails closed for every non-project owner and would have disclosed over `System.out::println` (the control). `deny Unknown app.Widget.viaOptional\|viaStream\|viaList` 0 → 0 → 1; executor and `new Thread(task::run)` arms too. A/B isolated: ADDED 46, REMOVED 0, 6 `inferred` gains all Unknown; spring-web 5.3.39 `StandardServletAsyncWebRequest.onTimeout/onComplete` (`timeoutHandlers.forEach(Runnable::run)`) — both entry points. Revert 2/6 red. **Open and separate (R183):** HOFs outside the invoker allowlist. CONFIRMED COMPLETED 2026-09-22, and the row's own first sentence is now stale: it says *Not fixed. PART 87 needs the arm* — the arm exists (`mrefhof`) and both its rows are green. Measured in the full four-way suite after candor-java `d17dc66` ([[R530b]]): zero implementors gives `Widget.fireRef` `Unknown + unresolved:true ['callback:java.lang.Runnable.run']`, and ONE unrelated implementor now gives **`Fs` — the candidate set was COMPLETED**, where it previously stopped at a disclosed Unknown. So R530b's `samLambdaImpls` index closed this row as a side effect rather than by aiming at it. Recorded because a side effect that IMPROVES a result is the kind most likely to be banked without checking, and because this cell has carried contradictory verdicts (`Not fixed` and `**FIXED.**`) in one sentence — which is exactly the shape that made [[R527]]'s status tool read the register wrong. |
| R180 swift: ⟨0.35⟩(b)'s third conjunct fails — a protocol-typed field with a closure-carrying conformer is `['Unknown'] unresolved=True` with NO `unknownWhy` | 2026-09-03 | **WITHDRAWN as a swift defect — the premise was wrong (swift agent `a1f975f`, spec agent `d41cf7d`, coordinator-verified on the fixture). What was wrong was PART 87's swift arm and my reading** | **Disclosed, not a sin; a contract failure.** `sw-proto-{zero,one}`: `Widget.fire ['Unknown'] unresolved=True`, no `unknownWhy` of kind `callback:`/`dispatch:`; `cha_completeness_check.py` would FAIL it — its own docstring records exactly this measurement, then the checker was hardened and swift was not added to the row. The exemption is drawn around the toggle (the trigger). Fixture `scratchpad/panel-spec/``sw-proto-*`. | Not fixed. Either swift emits `dispatch:<Trait>.<method>` or (b)'s kind becomes a SHOULD — pick deliberately; PART 87 gets a swift arm that RUNS. **NOT A DEFECT.** `Widget.fire` has `direct: []` and `calls: ['ClosureTask.go', 'Repaint.go']` — it RESOLVED the dispatch; `ClosureTask.go` is the unit that carries `unknownWhy: ['dispatch:ClosureTask.f']`. SPEC.md:1381 (restated :4254, :4855) requires `unknownWhy` to be ABSENT on a unit whose `Unknown` is purely inherited; emitting it on the caller would claim it as a source, which `blindspots` exists to exclude. With the clause's OWN toggle (a conformer with a visible concrete effect) swift takes branch (a): `Widget.fire ['Fs']` in both arms. **The transferable error:** `unresolved: true` says the entry has a hole somewhere in its reach; `direct` says whether it is THIS unit's hole — I read the first as the second. PART 87 rewritten (`d41cf7d`): swift (a) arms on a concrete-effect conformer, a calibration arm proving (b) can fire (`direct: ['Unknown']` + `dispatch:Task.go`), and the old shape kept as a §2 CONTROL that fails if the caller ever carries an inherited Unknown's reason; java and ts arms audited per cell — all read (a) with `direct: []`. `cha_completeness_check.py:74` still encodes (a) as 'the concrete effect appears' (over-strict → false red, the direction that tells you); a loosening was proposed and declined with reasons recorded in the spec CHANGELOG. |
| R181 rust: R161's residuals are SILENT, not disclosed — double alias, alias-typed return, and a `type Handler = fn(&str)` beside `struct Handler` | 2026-09-03 | **OPEN — PRE-EXISTING silence, both arms (panel fix lens)** | **CARDINAL SIN, rust.** `c::via_b` (double alias) and `c::via_ret` (alias-typed return) ABSENT in both arms; same-name alias/struct leaf collision: `b::drive`/`drive_opt`/`drive_let`/`drive_ref` ABSENT in both arms (control `drive_ctl` `['Fs']`; without the alias all `['Fs']`). R161's row STATED the first two as residuals but as if disclosed. Fixture `scratchpad/panel-fixes/``alias2/`. | Not fixed. Same family as R161/R177. |
| R182 rust: R165's leaf collision is silent — `two::mk -> H` beside `other::mk -> usize` makes `holds_two` ABSENT with no `Unknown` | 2026-09-03 | **CLOSED — candor-rust `23ea87e`. Part of the refusal-class batch; flood measured at 0.602% of analysed functions** | **CARDINAL SIN, rust.** A leaf that resolves to two definitions with different return types is dropped from the index (correct) and the call site then makes a positive purity claim instead of disclosing. Fixture `scratchpad/panel-fixes/``holds_two`. | Not fixed. A refused resolution must disclose, not certify. **Shipped as one batch (candor-rust `23ea87e`, `c91743d`, `4459c74`, `5de2235`, `902823b`, `11f8fc1`) after measuring the flood. **THE 1% FIGURE HAD NO BASIS — Tom said so himself on 2026-09-05: 'a number picked out of thin air, with no basis'. Do not cite '0.852% < 1%' as if a threshold were cleared; nothing was validated by that comparison.** What the measurement supports is the SHAPE below — reason from that. **DISTRIBUTION over 1,509 crates / 647,698 analysed functions: 5,518 functions gain `Unknown` = 0.852%** (2.141% of report rows), in 181 of 1,509 crates. Per-site: R182 3,900 (0.602%), R208 1,253 (0.193%), R184 293 (0.045%), R190(e) 95 (0.015%), R196 2. Per-crate spread: **median 0.0000%, 1,328 of 1,509 crates (88.0%) gain NOTHING**; among the 181 that do, median 1.66%, p90 11.48%, max 33.33% (`psm-0.1.31`, 2 of 6 fns); worst by count x11rb-protocol 1,438/7,213. **HARD INVARIANT HELD: 0 functions lost a concrete effect, 0 removed rows**, in the batch and in every isolated per-site arm. 4,131 new rows; **1,387 rows had no `Unknown` before and do now, so a green `deny Unknown` gate can go red.** 4,269 of the 5,518 carry a new reason themselves, 1,249 inherit it. §E1 reach counters all non-zero.** **R182 specifically:** the drop route's refusals now disclose instead of certifying the caller pure — the single largest contributor, 3,900 functions across 117 crates, and the row's own rule ('a refusal must disclose, not certify') is what the whole batch instances. §E2: the `<amb>` sentinel claim is PINNED BY A TEST (`5de2235`) rather than asserted in a comment. |
| R183 java: a method reference passed to a HOF OUTSIDE the invoker allowlist (`Optional.map(Supplier::get)`) is silent while the lambda spelling discloses | 2026-09-03 | **CLOSED — candor-java `a631fc3`. THE ROW'S PREMISE WAS WRONG and that is the finding: 'widen the invoker allowlist' was never the question. Coordinator-verified 0 → 1 on the fixture's own policy** | **CARDINAL SIN, java.** `Optional.ofNullable(sup).map(Supplier::get)` silent, `map(s -> s.get())` disclosed, both arms measured. `Rules.SYNC_CALLBACK_INVOKERS`/`FOR_EACH_FAMILY` is an ALLOWLIST that deliberately excludes store/lazy sinks; widening it is a separate question with its own fabrication risk (denylist-over-allowlist applies: say which direction it fails first). | Not fixed. **RE-MEASURED 2026-09-04 on the shipped 0.35.0 jar: STILL OPEN, SILENT.** `viaMethodRef` absent from `functions[]`; the lambda twin `s -> s.get()` correctly `['Unknown']`. `deny Unknown viaMethodRef` exit 0 'no violations'; the lambda control exit 1. Ground truth executed via `java -cp`: both spellings write the probe file. Fixture `scratchpad/stale-engines/``java/r183`. **Plan reviewer, 2026-09-04: R183 and R191 are NOT one mechanism and must not be batched — their fail directions are OPPOSITE.** R183 is `Rules.SYNC_CALLBACK_INVOKERS` (`Rules.java:597`) plus `FOR_EACH_FAMILY` (`Candor.java:7453`), where WIDENING the table is the FABRICATION direction; R191's table fails toward silence. Fix R191 first, alone. **FIXED, and NEITHER invoker table was touched.** The row and my brief both framed this as deriving *which HOFs invoke their callback synchronously* — a distinction that is not derivable and **does not have to be**, because `handleInvokeDynamic`'s LAMBDA arm has never known what the sink does either: a lambda is edged at its CREATION site unless it escapes uninvoked or feeds a curated deferred container. `Rules.SYNC_CALLBACK_INVOKERS` and `FOR_EACH_FAMILY` answer a different question — an OPAQUE callback (field or param) at a site with no indy to attribute to. The fix is 17 lines: disclose at the indy creation site, gated on the same `deferred` flag the lambda arm uses, keyed on `isJdkFunctionalSam` (the predicate that already decides the lambda arm), with CHA consulted first as the neighbouring project-owner branch does. **Keying on R191's 732-interface index was TRIED, MEASURED and REJECTED:** it charges `Unknown` over `map(Iterable::iterator)`, `map(Principal::getName)`, `sorted(c::compare)`, `map(m::matches)` where the lambda twins stay pure — a new fabrication on the front door of `Stream`. **That measurement also CORRECTS R179 and R191:** their 'the lambda twin discloses' parity claim holds only inside `java/util/function/` + `Runnable` + `Callable`; for `Iterable::iterator` at an allowlisted invoker they already charge where the lambda does NOT (`feIterRef` Unknown vs `feIterLam` absent, on the pre-fix jar). Left alone — narrowing it is the under-report direction. **Two of my brief's controls were wrong about the pre-fix engine, measured:** `list.stream().map(s -> s.get())` with no terminal operation is ALREADY `['Unknown']` in the lambda spelling, so the reference arm joining it changes which spellings claim it, not what the engine claims; and `Executor.submit` was ALREADY charged by R179 and is unchanged. The rest held: a stored/returned/queued `Supplier` gains nothing, and `forEach(Supplier::get)`, R191's cells, `System.out::println` and `String::trim` are byte-identical. My brief's jar identity was also wrong — `build/libs/candor-java-0.35.0-all.jar` is a HEAD build, not the shipped artifact. **395-jar A/B on every field, 577,836 rows: ADDED 167, REMOVED 0, CHANGED 415 wide / 73 on `inferred`, and it carries RECALL, not just safety** — instrumented at 72 events across 70 distinct sites in 30 jars. **21 sites read from `javap -v`**, every one a caller-supplied callback that really runs: AWS `sdk-core ClientOverrideConfiguration.defaultProfileFile` is literally `Optional.map(Supplier::get)` over a user-settable `Supplier<ProfileFile>`; `spring-web AbstractClientHttpRequest`/`AbstractServerHttpResponse.doCommit` is `commitActions.stream().map(Supplier::get)` over `beforeCommit(..)` callbacks; plus commons-lang3, spring-hateoas, assertj and javac's own `Operators.initOperators`. Zero fabrications. **All 167 added rows AUDITED, not sampled** — 129 claim no effect at all and surface only via the existing 'class declares a capability' rule once a sibling discloses; the other 38 and all 73 inferred changes traced to an instrumented origin, 110 of 111 automatically and the last read from bytecode. Revert → 4 rows red, and each design choice has its own discriminating test verified by degrading it (drop CHA, key on the wide index, drop the `deferred` gate). `theHofTableBoundaryThisRowDoesNotWiden` was REPLACED, not deleted, by `theRemainingBoundaryIsTheOPAQUECallbackNotTheSpelling` — the boundary moved from spelling to opaqueness. **Residual: a reference to a functional interface outside `java/util/function/` + `Runnable` + `Callable` is still charged only at an allowlisted invoker, matching the lambda arm; closing that means widening `isJdkFunctionalSam` for BOTH arms together.** |
| R184 rust: a NON-NOMINAL `type` alias whose leaf collides with a struct declared elsewhere in the crate silently deletes EVERY effect edge through that struct's methods | 2026-09-03 | **CLOSED — candor-rust `c91743d`. 293 functions, 19 crates** | **CARDINAL SIN, rust.** `mod a { pub type H = fn(&str); }` beside `mod b { struct H; impl H { fn run(&self, p) { fs::write(p, ..) } } }` → `b::S::go_vec`/`go_idx`/**`go_direct`** ABSENT while the one-variable control `b::T::go_vec` (leaf `K`) reads `['Fs']`. Not callable-specific — `type H = [u8; 4]` does it too, so the site is `prim_aliases`, not `callable_aliases`. Broader than R181 (param form) and R182 (return index). Fixture `scratchpad/fix-resolve-out/fx-prim`. | Not fixed. A leaf that names both an alias and a nominal type must not resolve silently to either — disclose. **Shipped as one batch (candor-rust `23ea87e`, `c91743d`, `4459c74`, `5de2235`, `902823b`, `11f8fc1`) after measuring the flood. **THE 1% FIGURE HAD NO BASIS — Tom said so himself on 2026-09-05: 'a number picked out of thin air, with no basis'. Do not cite '0.852% < 1%' as if a threshold were cleared; nothing was validated by that comparison.** What the measurement supports is the SHAPE below — reason from that. **DISTRIBUTION over 1,509 crates / 647,698 analysed functions: 5,518 functions gain `Unknown` = 0.852%** (2.141% of report rows), in 181 of 1,509 crates. Per-site: R182 3,900 (0.602%), R208 1,253 (0.193%), R184 293 (0.045%), R190(e) 95 (0.015%), R196 2. Per-crate spread: **median 0.0000%, 1,328 of 1,509 crates (88.0%) gain NOTHING**; among the 181 that do, median 1.66%, p90 11.48%, max 33.33% (`psm-0.1.31`, 2 of 6 fns); worst by count x11rb-protocol 1,438/7,213. **HARD INVARIANT HELD: 0 functions lost a concrete effect, 0 removed rows**, in the batch and in every isolated per-site arm. 4,131 new rows; **1,387 rows had no `Unknown` before and do now, so a green `deny Unknown` gate can go red.** 4,269 of the 5,518 carry a new reason themselves, 1,249 inherit it. §E1 reach counters all non-zero.** **R184 specifically:** an alias/nominal leaf collision now discloses instead of deleting the edge — the shape whose PUBLISHED instance deleted every effect edge through a struct's methods. |
| R185 rust: `Option<ConcreteType>` FIELD + `if let Some(h) = &self.o { h.run() }` is silent in all three arms | 2026-09-03 | **CLOSED — candor-rust `a98dc73`. The row's stated remedy was HALF the fix and moves nothing alone, and the class was wider than the row in two directions** | **CARDINAL SIN, rust.** `elem_type` does not peel `Option` for a field, while the `Vec`/`iter`/`index` forms of the same field resolve. | Not fixed. Same peel `unwrap_result_option` already does for callables. **FIXED 2026-09-09 — candor-rust `a98dc73`, and the remedy above is HALF of it: the half that moves nothing on its own.** Adding `Option` to `elem_type` changes NOTHING, because every binder returns early only for the DISPATCH case and drops a concrete payload on the floor — the answer had no route to the binding. Measured, not reasoned: the peel alone left every row exactly where it was. **The class was wider than the row in two directions.** The row says *"FIELD"* — a PARAMETER `Option<Guard>` is equally silent. And it implies one binder; measuring found five, each needing its own concrete fallthrough beside its dispatch route: **`if let Some/Ok`** (`visit_expr_if`), **`while let`** (`visit_expr_while`), **`match Some/Ok`** — which must live in `visit_expr_match`, because `visit_arm` never sees the scrutinee — **`let ... else`** (`visit_local`, read through `with_pre_bindings` or R92's self-shadow defect returns), and **`Ok` on a `Result`**, which needed `elem_type` to peel `Result` too. `for h in &opt` already worked. Fixing if-let alone would have left four silent. **The SIBLING SHAPES are what make it a wrapper bug rather than a value bug**, and they are in the fixture: the same field as `Vec<Guard>` and as a bare `Guard` both charged `Fs` throughout. **One ASSERTED CONTRACT is inverted on purpose.** `elem_type_covers_the_collection_shapes` asserted `elem_type("Option<Sender>") == None` under the heading *"a non-collection is NOT an element source"*. Every CONSUMER of that function asks the narrower question *"if I bind a name out of this type, what is the name's type?"*, and for `Option`/`Result` the answer is `T` in all of them — `for h in &opt` is legal Rust, `opt.iter().map(\|h\| ..)` binds `T`, and there is no subscript case to fabricate on because `opt[0]` does not compile. The test now asserts the inversion WITH that reasoning, and adds that `Result<(), Sender>` stays `None`: nothing binds a name out of the error type. **A/B over 600 registry crates: ADDED 84, REMOVED 0, CHANGED 391** (32 in the `inferred` view). `REMOVED 0` is the number that matters for a typing widening. Two additions verified against upstream source rather than sampled: `grep-cli`'s `DecompressionReader::close` `['Exec','Log']` (`match self.rdr { Ok(ref mut rdr) => rdr.close() }` — reaps a child) and `bencher`'s `ConsoleTestState::write_log` `['Fs']` (`match self.log_out { Some(ref mut o) => o.write_all(..) }` over an `Option<File>`). Fabrication controls in the test and a separate fixture: a PURE payload through every binder stays pure, and R92's self-shadow shape still inherits the producer's own `Fs`. **It also opened R342:** 11 newly-uncovered entries, because a rule that starts firing makes its consumers' gaps visible — the R302/R337 pattern for the third time. |
| R186 rust: `expand` mis-prefixes a crate-LOCAL type through a glob import — xz2's `use std::io::prelude::*` renders `crate::stream::Stream` as `std::io::prelude::stream::Stream` | 2026-09-03 | **OPEN — found by the R174 fix agent; currently right outcome by the wrong route** | **Resolution defect, rust.** R174(a) removes xz2's six affected rows for the right reason (the `Stream` really is moved into the returned wrapper) but only because the mis-prefixed path happens to be std-rooted and is refused. Any local type whose module name collides with a glob-imported prelude's is resolved to the wrong crate. Direction unmeasured. | Not fixed. |
| R187 rust: R173's positional `?` veto is PRE-ORDER — a `?` inside a LOOP body is numbered before the constructions that follow it, so four plain loop shapes lose a real drop | 2026-09-03 | **CLOSED — candor-rust `c0671b8` (branch `fix-loopq`, merged before the cut). Coordinator-verified: all four shapes back to published parity; SIX more loop shapes the row did not have were also silent and are fixed** | **CARDINAL SIN, rust.** `for it in items { let v = it?; out.push(H::new(v)); } Ok(out)`, `last_loop`, `while step(&mut n)? { g = Some(H::new(..)) } Ok(g)`, `loop { let v = it?; .. }`: published `['Net']` via `H::drop` → union ABSENT; `deny Net collect_loop` and `pure collect_loop` 1 → 0 (all four). Ground truth executed: one `H::drop` inside each frame when the error fires on iteration 2. Attribution with the wave's own intermediate binaries: A172 charged, A174 charged, A174a/ALL absent. Mechanism: the body's `?` is numbered BEFORE the construction after it, so `first_ctor_seq[H] > p` keeps `H` in `acc.leaves`; `out` is correctly dropped but the R172 site gate then sees every `H` site escaping via `method_args` from the root `Ok(out)` and suppresses the leaf. Controls correct: `order`, `order_after`, `collect_loop_noq`, `try_operand`, `ret_err`, `closure_q`. Corpus: all 165 rows R173 changed re-audited by `loc` — 0 instances / 1,504 crates. Fixture `scratchpad/panel-fixes-2/``loopq/`. | Not fixed. A `?` inside a loop is live for everything the loop body builds: on entering `ForLoop`/`While`/`Loop` remember `try_exits.len()`, on exit rewrite every `?` recorded since to the loop's last `seq`. Keeps R173's straight-line gains. **FIXED.** On leaving a `ForLoop`/`While`/`Loop`, every `?` recorded inside it — the CONDITION included (`while step(&mut n)?` is re-evaluated after the body built its value) — moves to the loop's last `seq`. It can only move a `?` later (veto more), so the direction is over-charging; a construction AFTER the loop still outranks it, preserving R173's gains (executed: 0 drops). A `?` inside a CLOSURE stays where R173 put it (new `closure_depth`) — without that carve-out `closure_q`/`loop_in_closure` would have become published's fabrication again. Executed fixture of 15 shapes: the row's four plus nested loops with the `?` in the inner OR outer body, labelled break, `while let`, `break`/`continue` around the `?`, and a closure-`?` in the loop body — all charge and match published; a construction BEFORE the loop stays charged (executed 1 drop — it IS live at the `?`). `deny Net {collect_loop,last_loop,loop_top,while_cond}` and `pure collect_loop`: published 1, union 0, fixed 1. 1,504-crate WIDE-key A/B vs `a8fa8c6`, both arms cold: ADDED 0, REMOVED 0, CHANGED 1 — x11rb `Image::put_impl`, a `while` loop pushing `put_image(..)?` cookies into a `Vec` that really drops on the error path, gains a true `VoidCookie::drop` (effect set unchanged). §E1 `R187LOOPQ` 13,481 hits / 665 crates. Revert red. Cache rev19. |
| R188 rust: R174(b)'s unit-twin conflict withdraws LET-TYPING, not only the drop route — a module-qualified constructor call beside an unrelated unit method of the same leaf loses its receiver type | 2026-09-03 | **CLOSED — candor-rust `6aff3cc` (branch `fix-loopq`, merged before the cut). Coordinator-verified: `go`/`use_go` back to `['Net']`; the ambiguous variant recovers published's `['Net','Unknown']`** | **CARDINAL SIN, rust.** `net::connect(addr) -> Conn` beside `ui::Ui::connect(&mut self)` (unit); caller `go: let c = net::connect(addr); c.send(b)` — the CALL is unambiguous — published `['Net']` calls `net::Conn::send` → union ABSENT (also via `use crate::net::connect`); `typed_go` control `['Net']` both arms; `deny Net go` and `pure go` 1 → 0. Executed. Mechanism: `record_return` now records `RET_UNIT` for every unit fn AND method by bare leaf; `rets[connect]` → None; `c` loses its type; a method call on an untyped receiver is ABSENT with no Unknown. `R174UNIT` fired 181× in the corpus; at the wide key A172 → A174, 7 rows lost a non-drop call edge (diesel ×4 `on_connection_event`, itertools, ring, tokio oneshot) under a pre-existing Unknown — R174's 'REMOVED 18 / LOST 7 all audited' was true at its key and blind to this. The ambiguous-call variant goes `['Net','Unknown']` → `['Unknown']`: disclosed, but `deny Net go` still 1 → 0. Fixture `scratchpad/panel-fixes-2/``twin2/`. | Not fixed. Keep the unit twin as a conflict for the DROP route only (`ctor_leaf_from_call_returns` refuses a leaf in a separate `unit_leaves` set) and leave `recorded_return_type`/let-typing as published — a unit twin cannot be what a `let` binds. **FIXED.** The unit-twin conflict is filed under `unit_twin_key` (`<unit>` + leaf, outside the identifier key space, the existing sentinels' argument) and read ONLY by `ctor_leaf_from_call_returns`; `rets[leaf]` keeps published's answer. Both halves load-bearing with their own red test: writer only → R174(b)'s git2 control `a_leaf_with_a_unit_returning_twin_resolves_to_no_type` goes RED (the phantom `Repository::drop` returns); reader disabled → the new test goes RED. The proposal's wording 'the unit type has no methods' was FALSE (`()` has blanket TRAIT methods, `c.clone()`); the comment says no INHERENT methods and records the trait-method case as a stated limit (published behaviour, over-charge). All four twin fixtures match published; mirrored shapes (unit free fn beside typed method = git2; unit method beside typed free fn = R188; both free; both methods) differ in no arm except the two named cells. 1,504-crate WIDE-key A/B vs `a8fa8c6`: ADDED 8, REMOVED 0, CHANGED 7, 0 effects and 0 call edges lost, all 15 audited from source — syn 2.0.x ×7 `peek_signature` and windows `ID3DInclude_Vtbl::new` restored byte-identical to published (the direction R174's counter named as uncounted); diesel ×4 `AnsiTransactionManager` recover all four `Instrumentation::on_connection_event` implementors, itertools, ring, tokio oneshot recover real edges. `R174UNIT` refusal fires 1,726× / 86 crates. Cache rev20. **Stated, not closed:** the DROP route is still leaf-keyed — it refuses a construction whenever ANY unit fn shares the leaf, even for an unambiguous module-qualified call (R174(b)'s own trade); priced on three collision shapes with no arm difference (statement-position constructions absent in all arms — R193's note). Gates 34/34 clean shell; Docker realworld 20 honest, disclosure_recall complete; `run_deep` fails identically at `a8fa8c6` (instrument). |
| R189 rust: two constructions bound before a branch, each escaping on a DIFFERENT exit, shadowing, and a loop returning from inside — R172's fourth shape | 2026-09-03 | **OPEN — PRE-EXISTING in both arms (panel second pass)** | **CARDINAL SIN, rust.** `either`, `shadow`, `loop_ret` ABSENT in published and union; executed drops 1/1/2. R172's comment 'the leaf INTERSECTION one line up catches it' is false when both exits carry the same leaf under different names — sites are unioned across exits, so the gate sees all escaping. Fixture `scratchpad/panel-fixes-2/``loopq/`. | Not fixed. **MATRIX-VERIFIED 2026-09-07 (39 generated cells, 56 executed GT probes, shipped 0.35.0 binary plus the real published 0.34.0 from crates.io).** Cells reproduce exactly: `either`/`shadow`/`loop_ret` ABSENT in both arms, executed 1/1/2. **The row BUNDLES TWO MECHANISMS.** `either`, `two_returns_flat`, `loop_ret`, `condesc_other_root` are the SITE UNION ACROSS EXITS: `lang.rs:2612` `acc.sites.extend(next.sites)` unions escaping sites over roots while `acc.leaves.retain` intersects leaves, and the gate at `lang.rs:2686-2688` suppresses a leaf iff ALL its sites escaped — two exits each escaping a DIFFERENT site of the same leaf hand it a union containing both. `shadow` is NOT that: it is name-keyed `lets` at `lang.rs:2751-2755`, where `sites.lets` is a `Vec<(String, &Expr)>` so two `let a = ..` are one key — **mechanically R195's defect, not this one**. Split the rows before fixing. **The comment at `lang.rs:2599-2606` is FALSIFIED as written** — `condesc_other_root` (`let g = H::new(p); if f { return Some(g) } Some(H::new(q))`) is exactly a name bound before a branch whose fate the branch decides, and its leaf is NOT dropped from `acc.leaves` because the other exit carries `H` under a different name; executed 1 drop, ABSENT, `pure` exit 0. But it is TRUE of the regression it names (`cond_escape` is charged in both arms), so it is a documented GUARANTEE that is only a documented LIMITATION - and the guarantee is what licensed the blanket union. **NARROWER than the title, and the narrowing is itself the finding:** `pair_ifelse`/`pair_match`/`pair_two_returns` are the same program re-spelled and ARE charged - but by ACCIDENT. No `R172SITE` line fires for them, so they never reach the gate: a vacuous root from the tail `if`/`match` empties the leaf intersection. Delete one `else` and `if c { return a; } return b;` is SILENT (executed 1 drop, ABSENT in both arms, `pure` exit 0). Those cells are not evidence the gate is right. **WIDER than the title:** the second construction need not be BOUND - `condesc_other_root`'s competitor is an unbound inline `Some(H::new(q))`. Fix direction is CHARGING: the sound rule is to intersect sites as well as leaves, which re-opens the anyhow `render` over-charge the union bought off; the denylist that keeps it is to union a site across roots ONLY when the site is lexically inside a root's operand (a `return X` or tail operand is evaluated on that exit alone and can never be live at another), and to intersect sites reached through a `let`/`assign` binding. Not implemented; needs a `bin/corpus-ab.py` wide-key A/B before anyone believes the over-charge cost. |
| R190 rust: R176 is closed around ONE attribute position — cfg on the enclosing `mod`, a same-module caller, cfg-twin DEFINITIONS, `use .. as imp` twins, and the fan-out fallback all still silence a platform | 2026-09-03 | **PARTLY CLOSED — candor-rust `4459c74` closes (e). (c) is PRICED AT 7.02% AND DELIBERATELY NOT SHIPPED. (a), (b), (d) untouched** | **CARDINAL SIN family, rust.** (a) cfg on the enclosing `mod`, not the `use`: `go_mod` published ABSENT → union `['Fs']` with Exec silent behind a positive claim (`has_cfg` reads the `use` item only; `R169MULTI` takes the explicit set). (b) same-module caller `#[cfg(not(windows))] pub use unix_impl::*; #[cfg(windows)] pub use win_impl::size; fn go_not() { size() }` → `['Fs']` both arms: `R176CFGUNION` fires in the alias index but the file's own `uses` map resolves `size` to the explicit import first and never consults it. (c) cfg-twin DEFINITIONS `#[cfg(unix)] mod sys { fn sz() }` / `#[cfg(windows)] mod sys { .. }` (inline or `#[path]`) → caller ABSENT both arms, `pure` 0 — the canonical Rust split. (d) `#[cfg(unix)] use unix2 as imp;` twins → last wins. (e) the fan-out fallback: 13 gated globs + 1 explicit → `['Fs']` both arms — X5's silence above 12, stated in code, absent from R176's row. Fixtures `scratchpad/panel-fixes-2/``cfgmod/`, `cfgfile/`, `fanout/`. | Not fixed. The union must be taken wherever a `#[cfg]` sits on ANY ancestor of the edge or the definition, and the same-module resolver must consult it. **Shipped as one batch (candor-rust `23ea87e`, `c91743d`, `4459c74`, `5de2235`, `902823b`, `11f8fc1`) after measuring the flood. **THE 1% FIGURE HAD NO BASIS — Tom said so himself on 2026-09-05: 'a number picked out of thin air, with no basis'. Do not cite '0.852% < 1%' as if a threshold were cleared; nothing was validated by that comparison.** What the measurement supports is the SHAPE below — reason from that. **DISTRIBUTION over 1,509 crates / 647,698 analysed functions: 5,518 functions gain `Unknown` = 0.852%** (2.141% of report rows), in 181 of 1,509 crates. Per-site: R182 3,900 (0.602%), R208 1,253 (0.193%), R184 293 (0.045%), R190(e) 95 (0.015%), R196 2. Per-crate spread: **median 0.0000%, 1,328 of 1,509 crates (88.0%) gain NOTHING**; among the 181 that do, median 1.66%, p90 11.48%, max 33.33% (`psm-0.1.31`, 2 of 6 fns); worst by count x11rb-protocol 1,438/7,213. **HARD INVARIANT HELD: 0 functions lost a concrete effect, 0 removed rows**, in the batch and in every isolated per-site arm. 4,131 new rows; **1,387 rows had no `Unknown` before and do now, so a green `deny Unknown` gate can go red.** 4,269 of the 5,518 carry a new reason themselves, 1,249 inherit it. §E1 reach counters all non-zero.** **R190 specifically. (e), the refused re-export key, now discloses: 95 functions / 3 crates. (c) is UNAFFORDABLE and the measurement is the finding: 45,472 functions, 7.021% of analysed, 674 of 1,509 crates** — inside the 8–25% false-uncertainty band `collector.rs:118` already records as rejected, and no narrowing tried brings it out (5.288% counting distinct quals only; 4.884% restricted to where the written path selects no single candidate). **Read off the hit log the flood is TWO THINGS THAT ARE NOT THE ROW'S SUBJECT:** primitive-conversion collisions (`u32::from` over several `impl From<_> for u32` — 2,658 of x11rb-protocol's 3,419 hits) and a type name two modules share where the call IS written with its module and `tail2` discards the qualifier — a key-WIDTH resolution gap that wants RESOLVING, not hedging. Both are recorded in `scan.rs` beside the still-open silence, and the r128 test pins `go` as an under-report rather than as a property. **Premise correction: R190(e)'s named fixture `panel-fixes-2/fanout` does NOT demonstrate a silence** — it reads `['Fs']`, the correct Rust answer, reached through the file's own `uses` map, which is R190(b)'s route and not the cap; the genuine cap silence needs a QUALIFIED call and NO explicit arm, and the agent's first repro of that was **E0659, uncompilable — no evidence at all** — replaced with 13 mutually-exclusive `#[cfg(target_os)]` arms that compile and really spawn. |
| R191 java: a method reference to the SAM of an interface OUTSIDE `SAM_OF` — every primitive-specialised `java.util.function` interface — is silent | 2026-09-03 | **CLOSED — candor-java `0d3c272`, unpushed. Was PUBLISHED in BOTH 0.35.0 artifacts, the jar AND the native binary** | **CARDINAL SIN, java.** `isups.forEach(IntSupplier::getAsInt)`, `bsups.stream().forEach(BooleanSupplier::getAsBoolean)` ABSENT on both jars; `deny Unknown`/`pure` 0/0; lambda twin disclosed; executed (both write the probe). `Supplier::get`, `Consumer::accept` via `Map.forEach`, `Supplier<Runnable>` results all disclose. Beside R183. Fixture `scratchpad/panel-fixes-2/``java/`. | Not fixed. Denylist direction: any `java/util/function/*` single abstract method, or read `ACC_ABSTRACT` off the JDK class (loadable). **RE-MEASURED 2026-09-04 on the shipped 0.35.0 jar: STILL OPEN, SILENT, and the row's SCOPE is exactly right.** `viaIntSupplier` (`IntSupplier::getAsInt`) and `viaBoolSupplier` (`BooleanSupplier::getAsBoolean`) are absent while the lambda twin is `['Unknown']`; `pure`, `deny Unknown` and `deny Fs` on either silent function all exit 0, control exit 1. **The sibling spelling `sups.forEach(Supplier::get)` IS correctly disclosed on 0.35.0** — only the primitive-specialised interfaces are silent, which is the row's stated scope and distinguishes it from R183. Ground truth executed. Fixture `scratchpad/stale-engines/``java/r183-191`. **Plan reviewer, 2026-09-04, from the code: this is the cheapest and safest fix in the register.** `SAM_OF` (`Candor.java:6118`) is consumed by `samForwarderTarget` (`:6658`), which returns null → SILENCE for an unlisted interface. **`SAM_OF`'s own comment (`:6110-6117`) claims an unlisted interface 'falls through to the CONSERVATIVE branch … rather than to silence' — that is TRUE for `handoffInvoked` and FALSE for `samForwarderTarget`.** A safety sentence that is wrong about its own diff is the §E2 class this family has been caught by four times. **FIXED by DERIVING the SAM set from the build JDK** (`generateJdkSams` in `build.gradle.kts` → `Candor.samNameOf`) instead of extending the hand-written `SAM_OF` allowlist — 732 interfaces, computed over each interface AND its super-interfaces, ignoring statics, ignoring the Object methods JLS 9.8 permits (`Comparator` declares `equals` abstract; without that exclusion `Comparator::compare` would fall back into silence), minus signatures a `default` gives a body to. Bundled as a resource rather than read at runtime for two measured reasons: a native image has no `.class` files, so a runtime read would make the native binary silent where the jar discloses; and one derivation at build time cannot drift from a second at runtime. `SAM_OF` is UNIONED, not replaced (it holds `TimerTask`, commons-io `IOConsumer`) and its other consumer is untouched. **The reviewer's mechanism confirmed AND the premise under it corrected:** `SAM_OF`'s comment justified the allowlist by 'the interface set is CLOSED', but `isFunctionalIface` admits the whole `java/util/function/` package BY PREFIX, so keying by interface moved the omission rather than closing it. Scoped `deny Unknown` 0 → 1 on `viaIntSupplier`, `viaBoolSupplier` and the NON-`java.util.function` sibling `Iterable::iterator`; concrete refs (`System.out::println`, `String::trim`) and multi-abstract (`CharSequence::length`, 3 abstract methods ⇒ not functional) unchanged — that last is where this stops short of a blanket 'target is ACC_ABSTRACT' rule, which would fabricate over `String` receivers. 395-jar A/B on EVERY field, 577,836 rows: ADDED 0 / REMOVED 0 / CHANGED 0 — **an over-charge control ONLY, explicitly not recall evidence**: the corpus holds 24 method references only the new index answers, across 14 libraries, and 0 of them reach the disclosure site; the executed fixtures carry recall. Revert: 1 of 5 rows red; removing the resource reddens that row AND the index-presence row, so the index's absence is loud. Gates: `test` 979/0, `smoke.sh` 547, `soundness/run.sh 40`, self-gate, refresh-equiv — all green. **Row correction: its `deny Fs` and `pure` cells are TRUE but NOT DISCRIMINATING** — both are 0 on every arm including the lambda control, because the effect these carry is `Unknown`; only scoped `deny Unknown` discriminates. **Residuals stated:** an interface outside the JDK and outside `SAM_OF` still yields silence; jar/native parity unverified here (no GraalVM on this box). |
| R192 swift: `callableValue` answers DeclRef / MemberAccess / `??` / ternary only — a closure reached through a SUBSCRIPT, an Optional-returning member, or a tuple element is not a source | 2026-09-03 | **CLOSED — candor-swift `b9e78e9`, unpushed. EIGHT silent spellings, not the four the row named; coordinator-verified on the panel fixture** | **CARDINAL SIN, swift.** `if let h = handlers["k"] { h() }` (dictionary of closures, alias AND plain), `if let c = list.first, let cc = c { cc() }`, tuple element `pair.0` → ABSENT while `handlers["k"]?()` discloses; `pure`/`deny Unknown H.fireDictIfLet` 0/0; executed. Fixture `scratchpad/panel-fixes-2/``swift/main.swift`. | Not fixed. Same predicate, more source shapes. **RE-MEASURED 2026-09-04 against shipped 0.35.0 (the row was filed the day before, from a review panel): STILL OPEN, SILENT.** `fireDictIfLet`, `fireDictPlainIfLet`, `fireListFirst` and `fireTupleLet` are all absent from `functions[]`, while the disclosed control `handlers["k"]?()` is correctly `['Unknown']`. `deny Unknown H.<fn>` exits 0 with 'matched NO function' for all four; the control exits 1. Ground truth executed: the files are really written. Fixture `scratchpad/stale-engines/``swift/r192-swift`. **FIXED, and the row understated it twice.** `callableValue` gains SUBSCRIPT, element-accessor and TUPLE-ELEMENT arms — and beneath that, **a wider defect the row did not name: `typeName` returns no name for a function type, so EVERY container element index (`arrayElem`, `dictElem`, `tupleElem`, `fieldArrayElem`, `fieldDictValue`, and the param/global twins) dropped a `[() -> Void]` / `[K: () -> Void]` / `(() -> Void, Int)` element outright** — R178's typealias defect one nesting level out, and the reason the alias spelling was half-recognisable while the plain twin was not. Fixed by COMPLETING the indexes with a reserved spelling, R178's own move, not by a parallel index. Eight shapes ABSENT → `['Unknown']` (dict if-let/plain/guard/map, list first ×2, tuple ×2); coordinator re-measured all eight on `scratchpad/panel-fixes-2/swift`. **Real-code recall, and the corpora could not have found it:** six library corpora (Alamofire, GRDB, Kingfisher, swift-collections, swift-crypto, swift-nio) give 11,637 rows ADDED 0 / REMOVED 0 / CHANGED 0 on a 12-channel wide key with the changed branch INSTRUMENTED to 0 hits — safety-only. A 20,290-file sweep found the shape in **Google's Firebase Auth SDK**: `AuthAppCredentialManager.callbackWithReceipt` (`guard let callback = callbacksByReceipt[receipt]` over `[String: (AuthAppCredential) -> Void]`, then invoking that caller-supplied callback) ABSENT → `['Unknown'] callback:callback`, `deny Unknown` moving from 'bound nothing' to a violation, plus 4 caller rows; **only the plainly-spelled half reaches it, so an alias-only fix would have missed Firebase.** Revert → 54 failures. 7 executed-quiet over-charge controls hold. **The agent wrote a FALSE safety assertion and caught it by measuring** — 'the reserved spelling types nothing and can fabricate nothing' is false: two arms key on an element index being non-nil, and a local `extension Array` dispatch arm moves `viaArrayExtEff` ABSENT → `['Fs']` (matching its nominal `[Int]` twin, while the pure twin stays absent) — recall, outside this row's shape, now stated and pinned. **R127 and R146 measured as unrelated** (`Driver.swift:2421` and `:1531/1961`); R146's repro is byte-identical pre and post — nothing to correct in the register. **The ITERATION sibling is NOT closed — see R211.** |
| R193 rust: two silences observed in both arms with the mechanism NOT established — a std glob defeats classification (`use std::fs::*; File::open(p)?` → caller ABSENT) and a method-call-initialised local (`let c = cl.connect(); c.send(b)`) is ABSENT even with no twin | 2026-09-03 | **OPEN — PRE-EXISTING, mechanism unestablished (panel second pass)** | **CARDINAL SIN shape, rust.** `globstd/read_it` and `twinctl::go_method` ABSENT in published and union. Recorded as OBSERVED; the mechanism must be established before a fix is attempted. Fixtures `scratchpad/panel-fixes-2/``globstd/`, `twinctl/`. | Not fixed. |
| R194 rust: R173's pre-order `?` model is wrong for the `?`'s OWN OPERAND — a construction stored into a pre-bound binding inside `{ .. }?`, `match .. ?`, `if .. ?`, `try_for_each(..)?` or a closure-call operand loses its drop | 2026-09-03 | **CLOSED — candor-rust `7b8a394` (branch `fix-operand`, merged before the cut). The panellist's fix sketch was WRONG in two ways and was not implemented; coordinator-verified on the fixture against the published build** | **CARDINAL SIN, rust.** `Expr::Try` is numbered at its PRE-order position (`lang.rs:2942-2948`), i.e. before its operand is walked, so every construction inside the operand gets `first_ctor_seq > p` and survives the veto (`lang.rs:2521`); `out` is vetoed from `names` but the leaf filter never re-consults that. R173's premise — the operand's VALUE does not exist at the exit — is true for the value and false for what evaluating the operand stores into a binding bound before the `?`. Executed (H: Drop = Fs, drop counter, error path): `try_for_each(\|it\| { it?; out.push(H::new()); Ok(()) })?; Ok(out)` 1 drop, published `['Fs']` → final ABSENT; `{ out.push(H::new()); gen(n) }?` same; `match n { 0 => { out.push(H::new()); Err(()) } _ => Ok(()) }?` same; `if n == 0 { out.push(H::new()); Err(()) } else { Ok(()) }?` same; `run_cb(\|\| { out.push(H::new()); gen(n).map(\|_\| ()) })?` `['Fs','Unknown']` → `['Unknown']`. `deny Fs`/`pure` 1 → 0 on four, three fully silent. Controls: `operand_ctrl_straight` (`out.push(H::new()); gen(n)?`) `['Fs']` both arms; `operand_ctrl_value` (`let h = { gen(n)?; H::new() }; Ok(h)`) published `['Fs']` (fabrication) → final ABSENT (correct, R173's gain). Corpus 0 / 1,504 — every one of the 34 drop-edge losers whose body has a `}?`/`})?` operand read from source: all value-position constructions, correct removals. Fixture `scratchpad/panel-fixes-3/``lq3/`. | Not fixed. Record the `Try` at POST-order (after `for_each_child_expr`) and exempt ONLY construction sites on the operand's VALUE SPINE (the operand through `Paren`/`Await`/`Try`/`MethodCall.receiver` — what `Ok(Repr::new(s)?)`, `Async::new(stream)?`, `.map_err(..)?` rely on). Over-charge control that MUST hold: the 279 corpus rows classed construction-after-last-`?` (`scratchpad/panel-fixes-3/``ab/drop-audit.txt`) stay removed, and `operand_ctrl_value` stays absent — a lump post-order without the exemption re-fabricates published's ~280 blanket charges. **FIXED, additively.** The sketch's 'record `Try` at POST-order' breaks R173's own motivating gain: `walk_block` records a binder's `first_bind_seq` AFTER walking its initialiser, so for `let c = make()?;` a post-order `Try` lands on the binder's number and the strict `>` vetoes `c` — the case R173 exists to spare (measured by building it). The pre-order number is untouched; each `?` now ALSO vetoes the leaves its operand constructs OFF the operand's VALUE SPINE, a separate monotone term (`TryExit::interior`) that can only veto more. The spine is enumerated explicitly (denylist direction — anything unlisted stays charged): the operand, `Paren`/`Group`/`Try`/`Await`, a block's TAIL, an `if`/`match` arm's tail, by-value `Call`/`MethodCall` arguments, tuple/array elements, struct fields; it stops at `&` (a borrowed temporary dies here; its by-value twin does not), at a method RECEIVER (the sketch put it on the spine — `H::try_new(n)?.check()?` leaves a receiver that really drops; the exclusion was then measured to have no teeth because `for_each_value_child` never descends a receiver — recorded as an analysis-only control), and at closure/`async` bodies and non-tail statements. Nine shapes (the row's five + `&`-arg, block-arg, async-block-arg, nested operand) published-charged → ABSENT → charged, executed 1–2 in-frame drops each; fourteen executed spine controls stay ABSENT (0 drops) — a lump post-order without the exemption charges twelve of them; reverting the clause reddens the nine. 1,504-crate WIDE-key A/B vs `6aff3cc`: byte-identical over 257,243 rows with §E1 `R194OPERAND` hit 2,824 times in 427 crates (only 5 veto a crate-defined `Drop` leaf, all 5 traced from source and absorbed by the escape route or already charged); published → this reproduces published → `6aff3cc` line for line (0 unattributed); the lost-published-drop-edge set is IDENTICAL (symmetric difference 0 over 342 edges) — the 279 ctor-after-last-`?` removals stay removed; `operand_ctrl_value` stays absent. Battery 45 dirs: only the 5 R194 cells move. Gates 26/26; Docker realworld 20 honest 0/0, disclosure-recall 28/28×3, with the Linux build reproducing macOS cell for cell. Side-finding: `H::try_new(n)?.check()?` and `mk_h_async(n).await?.check()?` are ABSENT on published 0.34.0 and charged by the 0.35.0 line — a published sin this release closes. |
| R195 rust: REASSIGNMENT drops are unmodelled in every arm (`for _ in items { g = Some(H::new()) } Ok(g)` drops in-frame on every iteration but the first) — and R173 exposes it as a red→green flip | 2026-09-03 | **OPEN — PRE-EXISTING silence, all arms; the flip is R173's exposure (panel third pass)** | **CARDINAL SIN, rust.** `reassign_no_q`: executed 1 in-frame drop with 2 items, ABSENT in published, union and final. Consequence: `q_before_loop` and `closure_in_loop_q` were charged by published ONLY through the blanket `?` veto — right for the wrong reason (the R123/tokio-mocks precedent); with 2 items they really drop and the final is silent: `deny Fs q_before_loop` published 1 → final 0. Fixture `scratchpad/panel-fixes-3/``lq3/`. | Not fixed. A rebinding of an owned `Drop` binding drops the previous value in-frame; model the assignment as a drop site. **MATRIX-VERIFIED 2026-09-07.** Counting claim holds and MOVES as predicted: `reassign_no_q` with 0/1/2/3/5 items executes 0/0/1/2/4 in-frame drops, ABSENT at every count. The red-to-green flip reproduced against the REAL published 0.34.0 binary, not a rebuild: `q_before_loop` `['Fs']` at 0.34.0 to ABSENT at 0.35.0, `deny Fs q_before_loop` and `pure q_before_loop` both exit 1 to exit 0. **Right-for-the-wrong-reason established at HEAD with one variable:** identical body, one `?` added or removed - `reassign_loop_q` charged, `reassign_no_q` ABSENT, both dropping 1 with 2 items. **THE ROW'S MECHANISM CLAIM IS WRONG.** The site is NOT unmodelled: `note_ctor_site` (`lang.rs:3320-3339`) runs on every expression the walk reaches including an assignment RHS, and `CANDOR_ALIAS_DEBUG=1` proves it - `mech_reassign_plain` prints `R172SITE H (3 sites, 2 escaping)` and the 2 escaping are exactly this pair. What is missing is the DROP EVENT: `walk_expr`'s `Expr::Assign` arm (`lang.rs:3992-3998`) records `x = ctor` only as an escape ROUTE for the incoming value, and `escape_from_root` (`lang.rs:2756-2762`) then marks every site ever bound to that NAME as escaping. **A name is modelled as holding one value for the whole body.** **WIDER than 'a rebinding of an owned Drop binding':** `*slot = ..`, `s.field = ..`, `v[i] = ..` are the same silence, and `overwrite_local`/`overwrite_place` contain NO construction expression at all - filed separately as R297, because no site-keyed refinement can ever reach them. **One correction to the row's wording:** `closure_in_loop_q` is not fully silent at 0.35.0 - it is `Unknown` (`callback:unresolved call`), so `deny Fs Unknown closure_in_loop_q` stays exit 1 in BOTH arms while `deny Fs` flips 1 to 0. Silent under three policy forms, disclosed under one. Remedy stands and must be widened to place-assignment and to the `lets` shadowing level; the only proven-safe exemption is a deferred `let mut g;` with no initialiser and no prior assignment. An `Option` initialised to `None` is NOT proven safe. |
| R196 rust: the drop route's leaf-keyed refusal (R174/R188's stated trade) IS a silent under-report — an unrelated unit method of the same leaf silences a real drop on an unambiguous module-qualified constructor | 2026-09-03 | **CLOSED — candor-rust `23ea87e`, with R182. Corpus incidence 2 functions in 1 crate** | **CARDINAL SIN, rust.** `let g = net::open(a); g.p.len()` with an unrelated unit `Ui::open`: executed 1 drop, ABSENT in all three arms; controls `holds_free_ctrl`/`drop_ctrl` final `['Fs']`. The 1,726 `R174UNIT` refusals across 86 crates are the priced reach. Fixture `scratchpad/panel-fixes-3/``tw3/`. | Not fixed. A path-aware refusal (refuse only when the CALL is ambiguous) is a new mechanism and needs its own A/B. **Shipped as one batch (candor-rust `23ea87e`, `c91743d`, `4459c74`, `5de2235`, `902823b`, `11f8fc1`) after measuring the flood. **THE 1% FIGURE HAD NO BASIS — Tom said so himself on 2026-09-05: 'a number picked out of thin air, with no basis'. Do not cite '0.852% < 1%' as if a threshold were cleared; nothing was validated by that comparison.** What the measurement supports is the SHAPE below — reason from that. **DISTRIBUTION over 1,509 crates / 647,698 analysed functions: 5,518 functions gain `Unknown` = 0.852%** (2.141% of report rows), in 181 of 1,509 crates. Per-site: R182 3,900 (0.602%), R208 1,253 (0.193%), R184 293 (0.045%), R190(e) 95 (0.015%), R196 2. Per-crate spread: **median 0.0000%, 1,328 of 1,509 crates (88.0%) gain NOTHING**; among the 181 that do, median 1.66%, p90 11.48%, max 33.33% (`psm-0.1.31`, 2 of 6 fns); worst by count x11rb-protocol 1,438/7,213. **HARD INVARIANT HELD: 0 functions lost a concrete effect, 0 removed rows**, in the batch and in every isolated per-site arm. 4,131 new rows; **1,387 rows had no `Unknown` before and do now, so a green `deny Unknown` gate can go red.** 4,269 of the 5,518 carry a new reason themselves, 1,249 inherit it. §E1 reach counters all non-zero.** **R196 specifically:** the leaf-keyed refusal on a unit twin now discloses. Tiny corpus incidence (2 functions), which is the row's own point — the reach was never the argument; §E1 counter 236 hits / 7 crates confirms the branch IS reached. |
| R197 rust: a turbofish generic factory `fn mk<T>() -> T` — `let c = net::mk::<net::Conn>(); c.send()` — is ABSENT | 2026-09-03 | **OPEN — PRE-EXISTING, all arms (panel third pass)** | **CARDINAL SIN, rust.** Effect executed; the binding's type comes from the turbofish, which `record_return` never reads. Fixture `scratchpad/panel-fixes-3/``tw3/`. | Not fixed. **THE STATED MECHANISM IS WRONG ABOUT WHERE THE CODE RUNS — measured 2026-09-08, attempt reverted.** This row's remedy names `record_return` filing a bare generic parameter and `ctor_type` (lang.rs) reading it. I implemented the call-site turbofish read inside `ctor_type`'s `Expr::Call` arm — reading the last segment's `AngleBracketed` argument before the recorded-return fallback — and the fixture did not move. Probing rather than theorising: an `eprintln` at `ctor_type`'s ENTRY **never fires for this fixture at all**, on any variant. `ctor_type` is not on the path that types `let c = net::mk::<net::Conn>();`. So the fix was correct code in a function the failing case does not reach — indistinguishable, from the outside, from a fix that does not work. Not fixed, and the next attempt must START by locating the real reader rather than trusting this row: instrument the entry of every candidate (`resolve_recv_type`, `visit_local`'s binder arms, `dep_bound_vars`) and find which one types the binding, THEN write the turbofish read there. The turbofish read itself is sound and worth keeping — it is a precision GAIN, so its failure mode is a wrong type rather than a lost edge — but it must be placed where the value is actually typed. **The general lesson, and the reason this is written down rather than retried: a row's stated MECHANISM is a hypothesis exactly like its finding, and this one had been carried unchallenged since 2026-09-03.** 411 tests green after the revert. |
| R198 rust: `try_fold` and `map(..).collect::<Result<_,_>>()?` accumulators lose the drops of what they built before the error | 2026-09-03 | **OPEN — PRE-EXISTING, all arms (panel third pass)** | **CARDINAL SIN, rust.** Executed 1 drop each, ABSENT in published, union and final. Same family as R194 (a construction inside the operand of a fallible combinator), one HOF over. Fixture `scratchpad/panel-fixes-3/``lq3/`. | Not fixed. **MATRIX-VERIFIED 2026-09-07 (99 generated cells, 396 policy runs, executed GT on both paths).** Both arms confirmed verbatim from the ORIGINAL fixture at `panel-fixes-3/lq3/src/lib.rs:122-127` - which mattered, because a paraphrase of the shape answers DIFFERENTLY. **The row states ONE finding where there are TWO mechanisms, and both halves are mis-attributed.** (1) **NARROWER than 'try_fold loses it': the silence turns on the CLOSURE'S TAIL EXPRESSION, not on `try_fold`.** `s01_tail_ok_acc` and `s02_tail_if_arms_acc` are byte-identical except the tail (`Ok(acc)` vs `if i<99 {Ok(acc)} else {Err(())}`); the first is SILENT, the second CHARGED. Five `try_fold`/`sum`/`try_for_each` cells with an explicit `Err` arm are charged and were never in this family. Cause: `mark_escape`'s branch point (`lang.rs:3642`) unions LEAVES across arms but intersects NAMES, so a tail of `Ok(acc)` puts `acc` in the unconditional set and the `method_args` fixpoint then escapes everything `acc.push(..)` ever received. (2) **The 'same family as R194, one HOF over' claim is WRONG for the collect arm, and it is falsifiable:** `a05_collect_result_sep` puts the `?` on a separate LATER statement so there is no `?` operand at all, and it is still silent. The `?` is not load-bearing there; the collect arm is a by-value hand-off to a std callee that drops, which is **R200's family**. **THE REAL SHARED CAUSE, and it is one line:** `lang.rs:3527` `syn::Expr::Closure(c) => self.escapes.push(&c.body)` is an UNCONDITIONAL escape route - `escape_from_root` (`lang.rs:2610`) marks every entry on every call including `escape_from_root(None, ..)` (`lang.rs:2472`), and that result is re-united into the answer AFTER the positional `?` filter at `lang.rs:2566-2568`, so nothing can veto it. **This one line is the mechanism behind R209(b), both arms of this row, and 30 of the 50 silent cells in that matrix.** The register carries it as two unrelated rows. Fix direction is DENYLIST only: keep the push as the default and remove the closure from the unconditional set only for enumerated proven-safe consumption shapes; an allowlist of 'these invocations really escape' turns every unlisted spelling silent. Over-charge controls that must stay absent: result escapes by `return`, closure never invoked, and the sharded-slab case named in that line's own comment. **PARTIALLY CLOSED — candor-rust `4297efd`, 2026-09-07.** The shared cause named above (`lang.rs`'s unconditional `escapes.push(&c.body)`) is now withdrawn in exactly one PROVABLE case: the let-bound closure's name is used, and EVERY use is the callee of a call whose value is discarded (`f();`, `let _ = f();`). **The LOOSE fix was built first and REJECTED** — withdrawing the suppression for every let-bound closure moved 18 cells and FOUR were FABRICATIONS against executed ground truth, from two distinct causes: a closure that is never invoked never runs its body (0 drops), and one whose result is returned hands the value to the caller (0 drops). The shipped rule moves **9 cells, all correct, zero fabrications**, including R209(b)'s own verbatim cell. **RESIDUAL, and this row is NOT closed:** five shapes the loose version also reached stay silent — the result bound and kept, invocation inside a block, a closure passed by reference — because their uses are not discarded-callee. Corpus A/B via `bin/corpus-ab.py`: 1,514 crates / 276,938 rows, ADDED 0 / REMOVED 0 / CHANGED 0 in all five key and value modes, which is evidence only because the branch is instrumented and REACHED **189 times across 90 real crates** — in every one the closure body held no drop-relevant construction. Same zero-incidence profile as R194/R199. The sharded-slab case the line was written for is untouched BY CONSTRUCTION: an inline closure still pushes unconditionally. Regression test falsified against the unfixed engine (stashing `lang.rs` alone panics it). |
| R199 rust: a MACRO-borne construction inside the `?` operand (`out.extend(vec![H::new()]); gen(n) }?`) loses its drop — `note_macro_site` never consults the open `?` exits, so R194's term is blind to it | 2026-09-03 | **CLOSED — candor-rust `d55c913` (branch `fix-macro`, merged before the cut). The panellist's direction was implemented and CORRECTED in one place, measured: spine membership must be re-tested INSIDE the parsed tokens, not decided at the macro node. Coordinator-verified on the six cells vs the published build** | **CARDINAL SIN, rust.** `{ out.extend(vec![H::new("a")]); gen(n) }?; Ok(out)` — executed 1 in-frame drop on the error exit; published `['Fs']` → `6aff3cc` ABSENT → `7b8a394` ABSENT. Six spellings identical: `out = vec![..]`, `let v = vec![..]; out.extend(v)`, `out.append(&mut vec![..])`, inside a `match` arm operand, inside `try_for_each(\|it\| { it?; out.extend(vec![..]); Ok(()) })?`. `deny Fs m_macro_*` published 1 → final 0 on all six. Control `m_macro_ctrl` (same macro, straight-line before the `?`) charged in all arms. Mechanism (`lang.rs` `note_macro_site`): it records `first_ctor_seq` at the macro's own position — inside the operand, hence greater than the pre-order `?` — so the leaf survives R173's positional filter, and it NEVER consults `open_tries`, so the leaf never enters `TryExit::interior`: R194's term and its §E1 `R194OPERAND` counter are blind to it by construction. The site then matches `mark_escape`'s renumbered macro site and the R172 gate sees 'all sites escaping'. Corpus: 0 of the 329 rows with a published `::drop` edge the final lacks have the lost leaf constructed inside a macro before a `?` (source read programmatically) — the same 0/1,504 profile as R194. Fixture `scratchpad/panel-fixes-4/sp4/caller` (`m_macro_*`, policies `p_m_macro_*.policy`). | Not fixed. Denylist direction: in `note_macro_site`, treat the macro's contents as on-spine only if the enclosing `ExprMacro` node's own address is in an open spine (a by-value arg `mk_res(vec![H::new()])?`), else insert the leaf into every open exit's `interior` — the loop `note_ctor_site` runs. Controls: `m_macro_ctrl` stays charged; a macro ON the spine stays exempt. **FIXED.** `note_macro_site` is a second, separate site recorder (re-parses the invocation tokens, records `first_ctor_seq` at the macro's position, never consulted `open_tries`). Now: (1) is the `syn::Expr::Macro` node on this `?`'s spine; (2) if so, re-run `value_spine_addrs` over the PARSED TOKENS and exempt only the value positions inside them — a build that exempts the macro whole leaves `idm!(use_h_ref(&H::new(), n))?` silent (a `&`-borrowed temporary inside the tokens dies on the error exit, and `mark_escape`'s own macro parse descends `Reference`). Six `m_macro_*` cells: published `['Fs']` → `6aff3cc` ABSENT → `7b8a394` ABSENT → fixed `['Fs']`; `deny Fs` 1 → 0 → 1; `m_macro_ctrl` charged in all arms. Four more shapes the row did not name also fixed (a `?` inside the macro's tokens; a macro construction in a loop in the operand; the same leaf inside AND outside the macro; the `&` temporary). Degradation matrix read whole: revert RED; coarse whole-macro rule RED on `macro_spine_ref` only. Attacks: nested macro, expansion-is-the-operand, `vec![H::try_new(n)?]`, arm tail vs statement, `format!`/`assert!`, R48 templates (do NOT go through `note_macro_site`) — all as expected. Battery 37 dirs: ROW-DIFFS 0 except the six cells (R200/R201/R202 and all fourteen R194 spine controls unmoved). 1,504-crate wide-key A/B vs `7b8a394`: byte-identical over 257,243 rows; new `R199MACRO` counter 10 hits / 9 crates (all read from source, none with an `impl Drop` leaf — why nothing moves), disjoint from `R194OPERAND` (unchanged 2,824/427); published → fixed reproduces published → `7b8a394` by member set AND content. Gates 26/26 clean shell; Docker realworld 20 honest 0/0, disclosure-recall 19/19 + 28/28×3; `oracle.sh`/`oracle_pf`/`run_deep` unmeasurable in the container — byte-identical verdicts on the pristine `7b8a394` tree, and the dylint crate does not depend on `candor-scan`. **Residual it does NOT reach, filed as R203.** |
| R200 rust: the spine's by-value argument exemption assumes 'the caller inherits through the call edge' — true only for a BARE-typed parameter; a wrapper-typed (`Option<H>`, tuple, array, `Vec`, `T`, `impl Into<H>`) or std callee that DROPS the arg is silent in every arm | 2026-09-03 | **OPEN — PRE-EXISTING silence in all arms (twins without `?` absent in published, `6aff3cc`, `7b8a394`); R194's exemption exposes it as a red→green flip on the `?` spelling (panel fourth pass)** | **CARDINAL SIN, rust.** Callee ABSENT in all three arms, caller published `['Fs']` → final ABSENT, executed 1 drop in the call's dynamic extent on both paths: `eat_opt(Some(H::new()), n)?`, `eat_pair((H::new(),1), n)?`, `eat_arr([H::new()], n)?`, `eat_vec(vec![..], n)?`, `eat_gen(H::new(), n)?` (`T`), `eat_into(H::new(), n)?` (`impl Into<H>`), a local closure param `let f = \|h: H, n\| {..}; f(H::new(), n)?` (the drop runs in THIS unit); std callees that drop the arg: `gen(n).and(Ok(H::new()))?`, `.or(Err(H::new()))?`, `opt(n).ok_or(H::new())?`; `deny Fs b_std_and` 1 → 0. `Box<H>` and bare `H` are fine (callee charged, caller inherits). Disclosed, not silent: fn-pointer callee `['Unknown']`; out-of-crate callee `invisible:[extlib]`. Fixture `scratchpad/panel-fixes-4/sp4/caller` (`a_bv_*`, `b_std_*`). | Not fixed. Cheapest real fix on the CALLEE side: `owned_drop_params` unwrapping `Option`/`Vec`/tuple/array to their drop-relevant leaves (charging direction). The fix agent's 'inherits through the call edge' sentence was the unverified safety claim. **MATRIX-VERIFIED 2026-09-07 (55 generated cells, 14 green controls, frame attribution MEASURED not assumed - a separate PROBE row records the CALLEE's dynamic extent, which is the exact objection R230 raised against the panel's harness).** Every named shape reproduced. **WIDER than stated - none of these are in the row:** `Result<H,()>`, `Option<Box<H>>`, `Box<Option<H>>`, `[H;2]`, `(u32,H,u32)`, `RefCell<H>`, `Option<W>`, a generic over `Option<T>`, a `where`-clause generic, and a closure with a WRAPPER-typed param. The `Box<H>`/bare-`H` fine claims hold. **The fn-pointer 'disclosed' claim holds but is weaker than it reads:** `['Unknown']` means blanket `deny Fs` is GREEN over a real `Fs` effect; only `deny Unknown` bites. **NO INCIDENTAL RESCUE ANYWHERE in the red set** - with `H` and the callee in a dependency and chained via `CANDOR_DEPS`, the caller reports `functions: []`, `analyzed.count: 1`, and all four policy forms exit 0 while its bare-typed twin in the same crate is `['Fs']` exit 1. **Mechanism: `lang.rs:3178-3180` at HEAD (`:2760-2762` in the v0.35.0 tree), inside `owned_drop_params`** - it calls `type_path`, the RECEIVER-TYPING helper, which peels only `Box`/`Arc`/`Rc`. That helper answers 'what type does `x.method()` dispatch to'; it is being asked 'what drop-relevant leaves does this parameter own'. The two questions differ exactly on containers. `elem_type` and `tuple_types`, which would answer correctly, already exist beside it and are unused. This is the brief's section G shape: one path answering two questions. **THE ROW'S REMEDY IS NARROWER THAN NEEDED** - a callee-side `owned_drop_params` fix cannot reach the three std cells (`.and`/`.or`/`.ok_or`, where the drop is inside std) or the two closure cells (no unit to charge); those need a caller-side rule. **AND THE FIX IS BLOCKED ON R230, measured over 1,514 vendored crates:** 506 have a local `impl Drop`; 105 by-value parameters in 21 crates are containers over a different Drop leaf (git2 `Option<Object>`, mongodb `Option<ClientSession>`, lapin `Option<Consumer>`, tokio `Option<Notified>`, arrow-schema `Vec<FFI_ArrowSchema>`, diesel, x11rb, and candor-scan's own tests). **Every candidate body read STORES, FORWARDS or LEAKS rather than drops.** Because the same line charges whether the callee drops or stores, widening converts this row's silence into R230's fabrication at ~105 real sites. **R200 and R230 must be fixed together or neither.** Sound form: collect every type ident in the parameter type (`decls::collect_type_idents` already does this), charge each that is `drop_relevant`, then DENYLIST the proven-non-owning ones - `PhantomData`, `Weak`, `ManuallyDrop`, `MaybeUninit`, fn-pointer and `Fn`-trait types. |
| R201 rust: `Await` pass-through treats the FUTURE's value positions as the OUTPUT's — a hand-written future that owns a constructed value is consumed by `.await` and dropped in this frame, silently | 2026-09-03 | **OPEN — PRE-EXISTING silence in all arms (twins without `?` absent everywhere); the `?` spelling flips red→green on 0.35.0 (panel fourth pass)** | **CARDINAL SIN, rust.** `FutH { h: H::new(), n }.await?` and `mk_fut(H::new(), n).await?` — executed 1 in-frame drop on both paths; published `['Fs']` → final ABSENT; `deny Fs c_aw_mkfut` 1 → 0. The escape model's `for_each_value_child` has the same `Await` pass-through, which is why the non-`?` twins are absent in every arm. `async fn`/`impl Future` with an owned param and async BLOCK operands are charged and inherited correctly. Fixture `scratchpad/panel-fixes-4/sp4/caller` (`c_aw_*`). | Not fixed. **MATRIX-VERIFIED 2026-09-07.** Both named shapes silent, plus two the row does not list: `Box::pin(mk_fut(..)).await?` and a future owning `Vec<H>`. All four stated-fine controls charged. **TRIGGER REFINED, and it is narrower than the row implies: the miss requires the AWAIT'S OUTPUT TO ESCAPE.** `FutH{h:H::new(),n}.await` in STATEMENT position with nothing returned is CHARGED, and binding the future first (`let f = mk_fut(..); f.await?`) is CHARGED. **Mechanism: `lang.rs:4311` (v0.35.0 `:3726`) in `for_each_value_child`, `syn::Expr::Await(a) => f(&a.base)`** - the escape test treats the awaited FUTURE's value children as the OUTPUT's, so a construction inside the future is judged to escape and `note_release`'s escape gate suppresses the charge. It does not escape: `.await` consumes the future by value and yields the output type. The `?`-spine twin at `lang.rs:4371` (`:3786`) has the identical arm. The row's 'the escape model's `for_each_value_child` has the same Await pass-through' is correct and 'the FUTURE's value positions' is the right phrasing; add the trigger condition. **Fix direction: do NOT delete the arm** - an inline `async { .. Ok(H::new()) }.await?` really does pass its value out and deleting it fabricates there. Narrow the pass-through to the proven-safe case only: descend when the base is an `Expr::Async` block, refuse otherwise. Refusing an escape claim is the charging direction, and it is a denylist of the exemption rather than an allowlist of futures. **NOT the same subject as R197** - this is value-flow in the escape model, not type origin; pairing them sends a fixer to `record_return` where there is nothing to find. **FIX ATTEMPTED AND REVERTED 2026-09-08, and the reason is the finding.** The remedy this row records — narrow `for_each_value_child`'s `Await` arm to descend only into an inline `Expr::Async` block — WORKS on this row's own cells (`aw_struct` and `aw_mkfut` moved ABSENT → `['Fs']`, executed) and **breaks ELEVEN R194 spine controls**: `spine_await_chain`, `spine_await_ok` and nine siblings in `a_question_mark_charges_what_its_own_operand_already_built`. Those assert that a construction ON a `?` operand's VALUE SPINE is correctly ABSENT — the exit where that `?` fires is the exit where it returned `Err`, so no `H` was ever built (executed: 0 drops) — and R194's exemption reaches them THROUGH the same `Await` pass-through this row wants removed. Charging them is published's fabrication back again over ~280 registry rows, which is precisely what R194 was written to stop. **So the two rows share one arm and want opposite things from it, and neither can be satisfied by editing the arm alone.** The distinction that would separate them is not escape at all: R201's cells are about a value the future OWNS and `.await` consumes in THIS frame; R194's are about whether the `?`'s Err exit was reached before the value existed. Those are different questions and one boolean arm answers both. Not fixed. **Do NOT retry by narrowing that arm** — the attempt is recorded here so the next person does not spend the evening rediscovering it. The real repair is to give the spine model its own `Await` treatment, keyed on the `?` exit rather than on value-escape, which is the same separate-the-two-questions change R189/R195/R297/R323 need and belongs with them. 411 tests green after the revert. |
| R202 rust: R194's leaf-keyed `interior` cannot tell an off-spine `let` that is RE-WRAPPED into the operand's value — `let h = { let x = H::try_new(n)?; Ok::<H,()>(x) }?` charged with 0 in-frame drops | 2026-09-03 | **OPEN — over-charge, at published parity (published charged it too; `6aff3cc` correctly absent); corpus-invisible (panel fourth pass)** | **Over-charge, rust.** Executed 0 drops; published `['Fs']`, `6aff3cc` ABSENT, `7b8a394` `['Fs']` via `H::drop`. The one R194 fabrication in the battery vs `6aff3cc`; A/B ≡ `6aff3cc` on 1,504 crates. Fixture `scratchpad/panel-fixes-4/sp4/caller` (`d_bt_*`). | Not fixed. Fails toward over-report. |
| R203 rust: a construction INVISIBLE to `lang.rs`'s macro re-parse (a `macro_rules!` template, unparsable tokens, or a nested macro) while the same LEAF is constructed elsewhere after the `?` — the leaf takes that other site's `first_ctor_seq`, survives the filter, and nothing puts it in `interior` | 2026-09-03 | **CLOSED — candor-rust `7edfb08` + `2e8a6c4` (branch `fix-macro2`, merged before the cut). The row's 'blanket fallback' alternative was NOT needed and was not built. Coordinator-verified on the fixture cells against the published build** | **CARDINAL SIN, rust.** Three variants, each executed 1 in-frame drop, each published `['Fs']` → `6aff3cc` / `7b8a394` / `d55c913` ABSENT: (a) a `macro_rules!` TEMPLATE construction `{ out.push(mk_h!("a")); gen(n) }?; let h = H::try_new(m,"b")?;` — R48 expands templates in `collector.rs`, which `lang.rs` never sees; (b) a macro whose tokens do not parse as a comma-punctuated expression list; (c) a NESTED macro. Direct twin `k_tmpl_ctrl` is charged, so the discriminator is exactly the invisibility. Fixture `scratchpad/fix-macro-out/atk/mk` (`k_tmpl_hole`, `k_nest_hole`, `k_unparsed_hole`, `k_tmpl_ctrl`), executed counter. | Not fixed. (c) is reachable from `note_nested_macro_leaves` in the same function; (a) and (b) need either the collector's expansion or a blanket fallback for an unanalysable macro in an operand — a new mechanism needing its own A/B (a lump fallback risks re-fabricating published's ~280 blanket charges). **FIXED by making the sites VISIBLE, not by a new veto concept:** (a) the collector's own R48 `local_macros` + `macro_template_blocks` index, already crate-wide and three lines above the `escaping_ctor_leaves` call, is plumbed into the interior walk; (b) the tokens were not unparsable, only un-parsable AS AN EXPRESSION LIST — `syn::Block::parse_within` reads them; (c) nested and STATEMENT-position macros (`Stmt::Macro`, which `walk_block` discarded) go to the same interior walk, plus the `{ .. }` block boundary in macro tokens that `for_each_child_expr` stopped at — two shapes the row did not name. Every new path writes ONLY `TryExit::interior` (the one term that can only REMOVE a leaf from the escaping set, i.e. charge), gated on an open `?` — over-reach over-charges, cannot silence. Eight shapes published `['Fs']` → `6aff3cc`/`7b8a394`/`d55c913` ABSENT → fixed `['Fs']`, `deny Fs` 1 → 0 → 1, executed 1 in-frame drop each; `k_tmpl_ctrl` charged in every arm; six over-charge controls hold; revert red; whole-template exemption red on `tmpl_spine_stmt_hole` only (two-step spine over the definition). **Second commit, coordinator's ruling:** a MULTI-ARM `macro_rules!` template was still silent vs published because R48's single-arm rule was kept; the interior term now walks EVERY parseable arm — `r_multiarm_hole` published `['Fs']` → ABSENT → `['Fs']` (closed) and `o_multiarm_pure` published `['Fs']` → ABSENT → `['Fs']` (an over-charge at published parity, ACCEPTED and asserted by a test that says why); `collector.rs` untouched, so call-edge resolution keeps one authority and the two questions are opposite (an added effect fabricates; a refused escape over-approximates). 1,504-crate wide-key A/B vs `d55c913` and vs `7edfb08`: byte-identical over 257,243 rows; `R203OPAQUE` 46 hits / 16 crates (jni, mysql_common, syn ×13 — the twelve new ones are syn's 100-arm `Token!`), all read from source, no reached leaf has an `impl Drop`; published → fixed ≡ published → `7b8a394` line for line. Battery 40 dirs: only the R203 cells move. `cargo test -p candor-scan` 383 + 77; gates 26/26 clean shell; Docker realworld 20 honest 0/0, disclosure-recall 19/19 + 28/28×3. **Residuals, over-report or invisible-to-everything:** statement-only tokens ON the spine over-charged (`o_spine_unparsed`, parity); tokens readable in neither form (`quote!{ #x }`) and macros from other crates / proc macros — invisible to the collector too. Process note: `fix-macro-out/bin/candor-scan.NEW` is a PRE-COMMIT build of `d55c913`; the corpus arm was re-run from the tip. **Panel fifth pass: the R203 commits introduced no silence and no corpus-visible fabrication (FIN4 → FIN5 0 differing rows; published → final identical to rounds 3/4). BUT this row's 'tokens readable in neither form … invisible to the collector too' reads as harmless and is NOT: published charged those cells — see R207. And a defect in this row's own `Stmt::Macro` arm: R204.** **Wording corrected per the fifth pass: 'invisible to the collector too' is NOT harmless — published charged those cells. They are R207's accepted residual, ruled by Tom.** |
| R204 rust: R203's `note_opaque_block` `Stmt::Macro` arm skips the template lookup and the expression-list parse — a statement-position macro INSIDE a template body, block tokens or statement tokens is invisible again | 2026-09-03 | **CLOSED — candor-rust `c7fe2c2` (branch `fix-visible`, merged before the cut). Coordinator-verified: the four cells restored to published parity** | **CARDINAL SIN, rust.** `note_opaque_block`'s `Stmt::Macro` arm calls `note_opaque_stmt_tokens` only, while `walk_block`'s arm routes through `note_opaque_macro` — so R203's '(c) statement-position macros go to the same walk' holds one level down and fails one level in. Executed 1 in-frame drop each, published `['Fs']` → `d55c913`/`2e8a6c4` ABSENT: `a6_tmpl_stmt_macro` (`macro_rules! wrap_stmt { ($o,$p) => {{ push_h!($o,$p); }} }` used as `{ wrap_stmt!(out,"a"); gen(n) }?`), `a6b_tmpl_println_tokens` (`{{ println!("{}", $crate::H::new($p).p); }}` — drops on BOTH paths; direct twin `a6e` charged), `a6c_blocktok_stmt_macro` (`idm!({ push_h!(out,"a"); gen(n) })?`), `a6d_stmts_stmt_macro`. `a6a_tmpl_tail_macro` (no `;`) is charged — syn makes it `Stmt::Expr`. Fixture `scratchpad/panel-fixes-5/r5`. | Not fixed. Build the `ExprMacro` host and call `note_opaque_macro(&host, &vec![false; n])` exactly as `walk_block` does. PROTO2 (`panel-fixes-5/proto`): all four charged, FIN → PROTO2 corpus ADDED 0 / REMOVED 0 / CHANGED 0. **FIXED** exactly as the row proposed: `note_opaque_block`'s `Stmt::Macro` arm builds the `ExprMacro` host and calls `note_opaque_macro`, as `walk_block` does. `a6_tmpl_stmt_macro`, `a6b_tmpl_println_tokens`, `a6c_blocktok_stmt_macro`, `a6d_stmts_stmt_macro`: published `['Fs']` → `2e8a6c4` ABSENT → fixed `['Fs']`; `a6a` (tail, `Stmt::Expr`) and `a6e` (direct twin) unchanged. Revert red (387 green). 1,504-crate wide-key A/B vs `2e8a6c4`: 0/0/0. §E1 `R204` 27 → 33. |
| R205 rust: a crate-local `macro_rules!` template invoked BY PATH (`$crate::mk_h!`, `crate::mk_h!`) is skipped by the veto-side template lookup (`name.contains("::") → return`) | 2026-09-03 | **CLOSED — candor-rust `8b3d089` (branch `fix-visible`, merged). Coordinator-verified** | **CARDINAL SIN, rust.** `$crate::helper!` is the canonical spelling for an exported macro calling a helper. `a1_dollar_chain_stmt` (`push_via! → $o.push($crate::mk_h!($p))`), `a1b_dollar_chain_expr`, `a1d_crate_path_direct`: published `['Fs']` → ABSENT; bare-name chain `a1c` charged (control). Corpus: 2 fns / 2 crates (objc2-foundation, windows-core) use a `::`-pathed local template inside a `?` operand. Fixture `scratchpad/panel-fixes-5/r5`. | Not fixed. On the VETO side resolve the leaf against `local_macros` — a wrong lookup can only charge. PROTO2: all three charged, `c1_dollar_chain_spine` stays uncharged, zero corpus rows. **FIXED:** on the veto side the template leaf is resolved against `local_macros` regardless of path prefix (`$crate::`, `crate::`) — a wrong lookup can only charge. `a1_dollar_chain_stmt`, `a1b_dollar_chain_expr`, `a1d_crate_path_direct` published `['Fs']` → ABSENT → `['Fs']`; `a1c` bare-name chain and `c1_dollar_chain_spine` unchanged. Revert red. A/B 0/0/0. §E1 `R205` 6 hits. |
| R206 rust: a `macro_rules!` defined INSIDE a fn body is a `Stmt::Item` that `walk_block` discards and `decls.rs:1919` never indexes | 2026-09-03 | **CLOSED — candor-rust `13af25c` + `587275a` (branch `fix-visible`, merged). The panel's PROTO2 shape was NOT used: it would have introduced a silence (stale per-body overlay), measured** | **CARDINAL SIN, rust.** `a2_fn_local_macro`: published `['Fs']` → ABSENT, executed 1 drop. Corpus: 571 body-local macros in 217 crates; 11 fns / 8 crates use one inside a `?` operand (chrono, jni, mysql_async, mysql_common, rusqlite). Fixture `scratchpad/panel-fixes-5/r5`. | Not fixed. A per-body overlay map filled from `Stmt::Item(Item::Macro)` in `walk_block`, consulted before `local_macros`. PROTO2: charged, zero corpus rows. **FIXED.** `walk_block` fills a per-body `body_macros` overlay as it passes each `Stmt::Item(Item::Macro)`; `note_local_macro_template` reads it. **PROTO2's `body_macros.or_else(local_macros)` — overlay INSTEAD of index — is a cardinal sin:** the overlay is per BODY not per BLOCK, so a `macro_rules! same` inside an already-closed `if` answers for the crate-level `same!` a later block really uses, replacing a constructing template with a pure one — `b2_stale_overlay_hole` published `['Fs']`, `2e8a6c4` `['Fs']`, PROTO2 ABSENT, fixed `['Fs']`. The committed version walks BOTH (a strict superset; the interior term can only charge). `b3_body_shadows_crate_hole` proves the overlay is read. `a2_fn_local_macro` published `['Fs']` → ABSENT → `['Fs']`. **Second instance found one walk past the trigger (`587275a`, R210):** `note_opaque_block` — the walk over a template body, block tokens or statement tokens — discarded `Stmt::Item` for the identical reason, so `idm!({ macro_rules! lm2 {..} out.push(lm2!("a")); gen(n) })?` was published `['Fs']` → `2e8a6c4` ABSENT → R206 alone STILL ABSENT (executed 1 drop); fixed. Revert red for each hunk. A/B 0/0/0 per commit. §E1 `R206BODYMACRO` 59 → 60 in 9 crates (jni 18, mysql_async 20, time 7, chrono 6, h2 6 …); `R206BODYMACRO2` 0 / 1,504 — safety-only corpus evidence, reach proven by the executed fixture. Fixtures `scratchpad/fix-visible-out/r6`, `r7` (executed counter with a CALLEE_DROPS split — the inherited harness counted callee drops as in-frame and could not fail; `panel-fixes-5/r5`'s `gt.rs` has the same limitation, left as round-5 evidence). |
| R207 rust: the 'unreadable tokens' residual of R203 is a SILENCE vs published, not an over-charge — a repetition template is closable by flattening; a macro whose tokens fail BOTH parses and whose template carries a repetition is not | 2026-09-03 | **Repetition half CLOSED — candor-rust `856333d` (branch `fix-visible`, merged). Unreadable half SHIPS OPEN by Tom's ruling ('Accept'), stated in the advisory** | **CARDINAL SIN, rust.** `a4_repetition_tmpl` (`{{ $( $o.push($crate::H::new($p)); )* }}`): closable — flatten `$( X ) sep? *` to `X` for the veto-side parse only; PROTO2 charged, zero corpus rows. `a5_kv_tokens` (maplit-style `kv!("a" => H::new("a"))`) and `a12_match_tokens` (`pick!(n, 0 => H::new("a"), _ => ..)`): tokens fail both parses AND the template has a repetition — silent in FIN, PROTO and PROTO2. Exposure: 387 fns in 51 crates have a both-forms-unreadable macro inside a `?` operand (rustix ~45/version, jni 27, mio 22, polling 12, mongodb 10, syn, socket2, duct); measured corpus silence in the class: 0. The blanket alternative (an unreadable macro makes that `?` veto everything) closes them and changes 7 corpus rows, all jni-0.22.4, all fabrications, 6 BEYOND published parity — measured, not the fix. Fixture `scratchpad/panel-fixes-5/r5`. | Repetition half: not fixed, prototyped. Unreadable half: decision. **RULED by Tom 2026-09-03 ('Accept'): the both-forms-unreadable half SHIPS OPEN in 0.35.0, STATED — silent vs 0.34.0 on that shape, measured corpus incidence 0 / 1,504 crates, exposure 387 fns / 51 crates, blanket alternative fabricates 7 jni rows (6 beyond published parity). Recorded in the 0.35.0 advisory. The repetition half is fixed (R207-repetition, branch `fix-visible`).** **FIXED (repetition half):** new `collector::macro_template_blocks_flat` shares the arm-splitting walk with `macro_template_blocks` (the two cannot drift on arm COUNT, which R48's single-arm rule turns on) and differs only in the token transform — `$( X ) sep? *` → `X`. Veto side ONLY, **measured not argued:** routing R48's resolution through the flattened reader adds 178 rows / 12 `Unknown` effects on the 1,504-crate corpus (jni 59 + all 12 effects, memchr 21/version, windows-result, lexical-write-float); a unit test drives both readers so a future leak goes red. `a4_repetition_tmpl` published `['Fs']` → ABSENT → `['Fs']`. §E1 `R207FLATARM` 236 hits / 10 crates (rustix 62/57/57, jni 44, ring, xattr, trybuild, time-macros …), counted only where the plain reader FAILS. A/B 0/0/0. `a5_kv_tokens`/`a12_match_tokens` ABSENT in `2e8a6c4`, PROTO2 and fixed — the accepted residual, pinned by `kv_tokens_open` (a pin, not a control). |
| R208 rust: same-NAME `macro_rules!` twins — `cache.rs:576` is last-writer-wins by bare name, so which module's template the veto (and R48's resolution) sees depends on FILE ORDER | 2026-09-03 | **PARTLY CLOSED — candor-rust `902823b`: the DISCLOSURE half only. The row's own stated remedy WITHDRAWS and was measured and refused** | **CARDINAL SIN, rust, order-dependent.** `r5` (constructing `same!` in `ma.rs`, pure `same!` in `mb.rs`) → `ma::a3_collision_hole` SILENT; `r5b` (files swapped) → charged, and `mb::a3_collision_pure` over-charged at parity. 324 of 1,504 crates define a `macro_rules!` name more than once (cfg twins: anyhow, aho-corasick, async-compression …); measured corpus rows 0. Fixtures `scratchpad/panel-fixes-5/r5`, `r5b`. | Not fixed. Not fixable in `lang.rs` alone: the index must record duplicates and REFUSE (disclose), never pick by order. **RULED by Tom 2026-09-03 ('Accept'): SHIPS OPEN in 0.35.0, STATED — order-dependent silence on same-name macro twins, measured corpus rows 0, exposure 324 crates; the fix is an index that records duplicates and refuses, a separate mechanism. Recorded in the 0.35.0 advisory.** **Shipped as one batch (candor-rust `23ea87e`, `c91743d`, `4459c74`, `5de2235`, `902823b`, `11f8fc1`) after measuring the flood. **THE 1% FIGURE HAD NO BASIS — Tom said so himself on 2026-09-05: 'a number picked out of thin air, with no basis'. Do not cite '0.852% < 1%' as if a threshold were cleared; nothing was validated by that comparison.** What the measurement supports is the SHAPE below — reason from that. **DISTRIBUTION over 1,509 crates / 647,698 analysed functions: 5,518 functions gain `Unknown` = 0.852%** (2.141% of report rows), in 181 of 1,509 crates. Per-site: R182 3,900 (0.602%), R208 1,253 (0.193%), R184 293 (0.045%), R190(e) 95 (0.015%), R196 2. Per-crate spread: **median 0.0000%, 1,328 of 1,509 crates (88.0%) gain NOTHING**; among the 181 that do, median 1.66%, p90 11.48%, max 33.33% (`psm-0.1.31`, 2 of 6 fns); worst by count x11rb-protocol 1,438/7,213. **HARD INVARIANT HELD: 0 functions lost a concrete effect, 0 removed rows**, in the batch and in every isolated per-site arm. 4,131 new rows; **1,387 rows had no `Unknown` before and do now, so a green `deny Unknown` gate can go red.** 4,269 of the 5,518 carry a new reason themselves, 1,249 inherit it. §E1 reach counters all non-zero.** **R208 specifically, and the premise correction that matters: the reviewer's 'every fix in this family is over-disclosure, which cannot silence' is TRUE for five of the six sites and FALSE for R208 AS FILED.** Its remedy — 'record duplicates and REFUSE' — withdraws an existing answer: measured on `panel-fixes-5/r5b`, `mb::a3_collision_hole` goes `['Fs']` → `['Unknown']`. Only the disclosure half was taken, so the batch's direction stays uniform; **last-writer-wins is byte-for-byte unchanged and the ORDER-DEPENDENCE REMAINS OPEN — now visible rather than silent.** 1,253 functions / 56 crates. |
| R209 rust: a `?` INSIDE macro tokens is no exit for any arm, and an invoked closure's tail construction escapes unconditionally — silent in published, `d55c913` and `2e8a6c4` alike | 2026-09-03 | **OPEN — PRE-EXISTING in all arms (panel fifth pass)** | **CARDINAL SIN, rust.** `a7_q_in_stmt_macro`/`a7b_q_in_expr_macro` (`let g = H::new("a"); idm!(gen(n)?); Ok(g)` — 1 drop executed, ABSENT everywhere); `a8_closure_tail_invoked` (`let f = \|\| H::new("a"); let _ = f();` — the closure tail is an unconditional escape route; 1 drop on both paths, ABSENT everywhere incl. published). Fixture `scratchpad/panel-fixes-5/r5`. | Not fixed. **MATRIX-VERIFIED 2026-09-07, and the two halves are DIFFERENT SUBJECTS that should be split.** **(a) macro-borne `?` - WIDER than stated: 17 spellings, not 2.** Silent in both statement and expression position for a local single-arm macro, a `$crate::`-chained template, a nested INVOCATION, `vec![]`, `println!`, a tt-munching multi-statement macro, `assert!`, a `?` living in the macro TEMPLATE rather than the call-site tokens, construction-and-`?` both inside the tokens, and a macro-borne `?` inside a `for` loop. **WHY R199'S FIX CANNOT REACH IT, established rather than assumed:** R199 taught `note_macro_site` to insert leaves into an ALREADY-OPEN `?`'s `interior`; this is the mirror image, where the `?` is inside the macro and no `?` is open at all. There is exactly ONE `try_exits.push` in the file (`lang.rs:3459`, inside `walk_expr`), and `walk_expr`/`walk_block` are called only on real `syn::Expr`/`syn::Block` nodes - the macro-token walks (`note_macro_ctor_leaves`, `note_macro_site`, `note_opaque_block`) are separate recursions that write only `interior`/`macro_ctor_leaves`/`opaque_interior_leaves`. **No `?` inside macro tokens can EVER become a `TryExit`.** A different mechanism wearing the same clothes, not one more case for R199. **NARROWER in one useful way:** silent only when the macro-borne `?` is the function's SOLE exit - a macro-`?` inside a `try_for_each` closure, or a second `?` written outside the macro, both rescue it. That is why corpus incidence was 0. Fix hazard is the `seq` number: a token-parsed expression has no position in the pre-order counter, and a `TryExit` with the wrong `seq` either vetoes nothing or vetoes every construction in the function. Monotone-safe placement is the enclosing `ExprMacro`'s own `seq` (can only make the `?` earlier, so it under-vetoes in the interior, never over-vetoes) and it must therefore be PAIRED with the `interior` term, not replace it. **Failing direction is under-veto: this fix can leave residual silence and must not be reported as closing the class.** **(b) invoked-closure tail - NOT the same subject, and it has no `?` in it.** Mechanism is `lang.rs:3527` `syn::Expr::Closure(c) => self.escapes.push(&c.body)`, an unconditional escape route re-united after the positional `?` filter. Invoked and NEVER-INVOKED are indistinguishable; arity-blind (one, two and three real drops all ABSENT); `let _ = f()`, a bare `f();`, binding without an explicit `drop`, invoking inside a block, and pushing the result into a locally-dying `Vec` are all silent; the only charging spelling is an explicit `drop(h)`, which is modelled independently of the closure. **This is the same line as R198's real cause - see that row; one line, 30 of 50 silent cells, currently recorded as two unrelated rows.** **PARTIALLY CLOSED — candor-rust `4297efd`, 2026-09-07.** The shared cause named above (`lang.rs`'s unconditional `escapes.push(&c.body)`) is now withdrawn in exactly one PROVABLE case: the let-bound closure's name is used, and EVERY use is the callee of a call whose value is discarded (`f();`, `let _ = f();`). **The LOOSE fix was built first and REJECTED** — withdrawing the suppression for every let-bound closure moved 18 cells and FOUR were FABRICATIONS against executed ground truth, from two distinct causes: a closure that is never invoked never runs its body (0 drops), and one whose result is returned hands the value to the caller (0 drops). The shipped rule moves **9 cells, all correct, zero fabrications**, including R209(b)'s own verbatim cell. **RESIDUAL, and this row is NOT closed:** five shapes the loose version also reached stay silent — the result bound and kept, invocation inside a block, a closure passed by reference — because their uses are not discarded-callee. Corpus A/B via `bin/corpus-ab.py`: 1,514 crates / 276,938 rows, ADDED 0 / REMOVED 0 / CHANGED 0 in all five key and value modes, which is evidence only because the branch is instrumented and REACHED **189 times across 90 real crates** — in every one the closure body held no drop-relevant construction. Same zero-incidence profile as R194/R199. The sharded-slab case the line was written for is untouched BY CONSTRUCTION: an inline closure still pushes unconditionally. Regression test falsified against the unfixed engine (stashing `lang.rs` alone panics it). |
| R210 rust: `note_opaque_block` — the veto's walk over a template body, block tokens or statement tokens — discarded `Stmt::Item`, so a `macro_rules!` defined INSIDE block tokens and used there was invisible even after R206 | 2026-09-03 | **CLOSED — candor-rust `587275a` (branch `fix-visible`, merged before the cut). Found by the R206 fix agent one walk past its own trigger; never published as a candidate** | **CARDINAL SIN, rust (regression vs published, closed before the cut).** `idm!({ macro_rules! lm2 { .. } out.push(lm2!("a")); gen(n) })?`: published `['Fs']` → `2e8a6c4` ABSENT → R206 alone STILL ABSENT → fixed `['Fs']`; executed 1 in-frame drop; discriminator beside it (same use site, body-level definition) closed by R206 alone. R203 → R204's relationship one row over. §E1 `R206BODYMACRO2` 0 hits / 1,504 crates — the 0/0/0 A/B is safety-only evidence; reach proven by the executed fixture `scratchpad/fix-visible-out/r7`. Revert red (388 green). | Fixed. |
| R211 swift: the ITERATION sibling of R192 — a closure invoked while iterating a container (`for f in coll { f() }`, `.forEach { $0() }`, `.map { $0() }`, `dict.values`) is silent | 2026-09-04 | **CLOSED — candor-swift `a1d8396`. NINETEEN silent spellings, not the five the row named; 18 closed, the 19th left open deliberately. Coordinator-verified on the repro** | **CARDINAL SIN, swift.** Five spellings, ground truth executed (each really writes its file), ABSENT before and after `b9e78e9`: `for f in alias { f() }` over `[Cb]`, the same over a plain `[() -> Void]`, `for f in dict.values { f() }`, `plain.forEach { $0() }`, `plain.map { $0() }`. Same signature as R192 one source shape over. **Prevalence: this is the spelling the corpora actually contain** — Alamofire `finishHandlers.forEach { $0() }`, swift-nio `for callout in errorCallouts`, Kingfisher `blocks.forEach { $0() }` — though in all three the enclosing row happened to carry `Unknown` from an unrelated mechanism, so no corpus row moved and an A/B cannot see it. Repro `scratchpad/r192-out/loopvein/main.swift`. | Not fixed. R192 completed the element INDEXES and the binder arms; iteration binds the element through a different path. Do not treat R192 as closing this vein. **FIXED, and widening past the shapes it was handed found 14 more.** The four an audit scoped to its own trigger would have missed: an annotated loop binder (`for f: Cb in fs`), an annotated closure parameter (`{ (f: Cb) in f() }`), its BARE twin `{ (f: () -> Void) in f() }` — no type NAME, so it read as unannotated and was cleared outright — and `for (k, v) in dict`. **Mechanism:** R192 completed the element indexes and gave `callableValue`, the EXPRESSION half, its arms; iteration binds through neither — a `for` pattern or closure parameter records a type name in `vars`, and the invocation site for a bare `f()` reads `opaqueFnLocals`/`fnTyped` and NEVER `vars`, so the binder held R192's reserved spelling, `f()` resolved against nothing, and the caller vanished. All four element binders now route through one `bindCallableElement` extending R192's own `isCallableTypeName` (§G, one authority, not a parallel predicate). **The corpus A/B was NOT a fabrication control this time — it found a real cardinal sin in third-party code:** GRDB's `Configuration.setUp` (`for f in setups { try f(db) }` over a `[@Sendable (Database) throws -> Void]` that the PUBLIC `prepareDatabase(_:)` appends caller closures to) was `inferred: []` and is now `['Unknown'] callback:f`; one variable held, same tree and policy and command, different binary: `deny Unknown Configuration.setUp` goes `policy ✓` exit 0 → AS-EFF-006 violation exit 1. 11,637 rows over six corpora: ADDED 0, REMOVED 0, CHANGED 2 wide (1 narrow), **3 instrumented branch hits against R192's 0**; swift-nio's `close0` gains an `unknownWhy` visible ONLY on the wide key. Revert → 92 failures across 4 of 9 new tests. 9 must-stay-absent controls hold, and the precision-displacement risk that bit R192's agent is pinned: `keepStoreEff`/`shadowAfter`/`shadowBefore` keep `['Fs']` AND the concrete `Store.eff` edge. **An existing control was HIDING this sin and was corrected:** `BinderShadowProcessTests`' `XCTAssertNil(by["aliasLoopShadow"])` over `for g in jobs { g() }` was true for two reasons — executed ground truth shows the fabrication it guards does not run, but a caller-supplied closure does. **`assert-audit.sh` caught this fix writing its own false guarantee** ('disjoint by construction', which holds only for source that COMPILES — a property of Swift, not of the predicate) and it is now worded as the assumption it is. **19th spelling left open deliberately:** `for p in pairs { p.cb() }` over `[(cb: () -> Void, n: Int)]` needs the loop binder to carry a tuple-element index, a different mechanism; bolting it on here is the parallel path §G exists to prevent. |
| R212 rust: a self-scan of reqwest certifies its front doors non-`Net` — `RequestBuilder::send` reports `['Log']` and `async_impl::Client::execute` is ABSENT entirely | 2026-09-04 | **OPEN — DIAGNOSED, and NOT what the gate said. PRE-EXISTING: identical at v0.34.0 `22b5ec2`, `9bc0530`, `587275a` and HEAD `5a9334b` on the same source. Fix deliberately NOT attempted — see R213** | **CARDINAL SIN, rust, with a fabrication beside it.** `async_impl::request::RequestBuilder::send` reports `inferred: ['Log']` with NO `invisible`, NO `unresolved`, NO `unknownWhy`, NO `incomplete` — a bare positive claim. **The `Log` is wholly FABRICATED:** its only other callee is pure, so the entire effect comes from a drop-glue edge to `blocking::client::InnerClientHandle::drop`, a type that exists only in `src/blocking/` while the async `Client` is a different struct. **Meanwhile the REAL edge is dropped silently:** `self.client.execute_request(req)` is unresolved, and its callee honestly reports `['Unknown']` + `unresolved: true` + `unknownWhy: ['ambiguous:module items hidden by an unexpanded macro']`; had it resolved, `send` would be `Unknown`. `async_impl::client::Client::execute` is absent from the report entirely for the same reason. Cause: reqwest declares `async_impl`, `blocking` and `wasm` inside `if_hyper!{}`/`if_wasm!{}` bodies, candor-scan reads BOTH arms, so three byte-identical `RequestBuilder`/`Client` types coexist and resolution is by LEAF NAME — the class is R213. Blanket `deny Net` over the tree exits 1 with 14 violations, all `connect::*`/`h3_client::*` internals, **none of them the three front doors**. **SEVERITY BOUNDED, measured:** a hand-written consumer calling `Client::get(..).send()`, `Client::execute`, `blocking::Client::execute` and `async_nats::Context::publish` gets `['Net']` and `netClass ['unknown-host']` on all four — and still does with the bad self-scan report chained via `CANDOR_DEPS`. **The sin is confined to scanning the library's OWN tree**, which is what the coverage gate and any vendored-dep audit do. | Not fixed, deliberately. The fix is in candor-scan's leaf-keyed type/method resolution (`crates/candor-scan/src/decls.rs`); the async/blocking/wasm facade is a dominant Rust idiom, so a change there moves resolution across a large fraction of every corpus, and BOTH directions are live at once — a silently dropped call edge and a fabricated drop-glue edge. Filed as R213 and left for a dedicated round. **THE PLATFORM QUESTION IS NOW MEASURED, 2026-09-07 — it is REAL, it is EXACTLY TWO ROWS, and it does NOT contaminate the drift it was blocking.** The experiment this row asked for was never run because `coverage-gate-refresh` had never once reached the regeneration step (R157); with that closed, a THREE-WAY comparison is cheap: the checked-in manifest, the fresh LINUX manifest downloaded from the CI run's own uploaded artifact (`34140458495`), and a fresh MACOS regeneration on this machine from the same commit and the same 78-crate calibrated fixture. Compared with `generate.py --diff-manifests` — the authority that generated all three, not a hand-rolled `comm` — **LINUX vs MACOS scores 0 regressed / 0 oracle-dropped / 0 newly-uncovered.** The raw delta is two rows, both `reqwest` and both present only on macOS: `reqwest::Client::execute` (via `blocking::client::Client::execute`) and `reqwest::RequestBuilder::send` (via `async_impl::request::RequestBuilder::send`) — i.e. reqwest's platform-conditional module structure, not a classifier difference. That also explains the asymmetry in the CI verdict: LINUX reported 2 oracle-dropped where macOS reports 0, and it is the same two rows seen from the other side. **Consequence for every prior reading of this row: the caveat was correct to raise and too broad to act on.** A host difference of 2 rows cannot explain a 128-row drift, so the standing instruction to treat coverage-gate output as unconfirmed should now be scoped to `reqwest`'s two entries rather than applied to the whole manifest. |
| R213 rust: a same-named type or method in a sibling module makes receiver typing ambiguous — the call edge is DROPPED SILENTLY instead of disclosing `Unknown`, while the drop-glue route picks the WRONG module's type and charges its `Drop` | 2026-09-04 | **OPEN — the class behind R212; PRE-EXISTING at v0.34.0. Minimal repro built, both arms compile** | **CARDINAL SIN, rust, in both directions at once.** Repro `scratchpad/r212/repro` and `repro2` differ by ONE added method — `b::client::Client::execute_request`, on a same-named type in an unrelated module — with the file-reading code untouched: `a::client::Client::execute` and `a::request::RequestBuilder::send` go from `['Fs']` to **ABSENT (certified pure)**, and the policy `pure a::request` flips from exit 1 (AS-EFF-006) to **exit 0 plus 'candor: nothing hidden — every effect sits where its name says it should.'** No disclosure of any kind on the vanished units. The fabrication half is visible in the same repro: `a::…::send`'s calls include `b::client::Handle::drop`, a type absent from `a`. Identical at v0.34.0, `9bc0530` and `587275a`. | Not fixed. Resolution must disclose on an ambiguous receiver rather than certify — the same rule R182 states — and the drop-glue route must not pick a same-leaf type from another module. Wide surface: this is the async/blocking/wasm facade idiom. **MECHANISM LOCATED 2026-09-08 by probing, and this row's stated remedy is WRONG.** Reproduced on the current build, BOTH halves: `repro2` (one added method on a same-named type in module `b`) leaves `a::client::Client::execute` and `a::request::RequestBuilder::send` ABSENT with `pure a::request` at exit 0 and *"nothing hidden"*; and `repro` shows the fabrication in the other arm — `a::request::RequestBuilder::send` carries `calls: [… , "b::client::Handle::drop"]`, a type absent from `a`. **The remedy as filed says "resolution must disclose on an ambiguous receiver rather than certify". That prescribes the HEDGE, and R190(c) already priced the hedge at 4.88-7.02% of analyzed fns against 0.85% for the rest of the family — and named THIS case as not that problem:** *"the CALL is written with its module and names one definition; `tail2` throws that qualifier away. That is a resolution gap in the KEY WIDTH, not a contest between definitions."* **The actual mechanism, from an `eprintln` at `resolve_target`'s entry:** the path reaching resolution is **`super::client::Client::execute_request`** — the `use super::client::Client` import is carried through LITERALLY. `super::` matches no qual (they are `a::client::…`), so the lookup falls back to the 2-segment `tail2` key `Client::execute_request`, which `b` also owns, and the `len() == 1` filter refuses BOTH. **Suffix matching cannot fix it** — `b::client::Client::execute_request` shares the same suffix; it needs `super::` resolved against the CALLER's module (`a::request` → `super` = `a`), which selects uniquely. Not fixed. **Next step is bounded and named:** `decls.rs:774` has a `Some("super") =>` branch that walks up, yet an unexpanded `super::` still reaches `resolve_target` — so either that branch does not fire for this import shape or its output is not on this path. Probe which, then expand `super::`/`self::` against the caller's module before the tail2 fallback. **Direction: this RESOLVES rather than hedges — it adds the correct edge and no `Unknown`s, so it avoids the flood R190(c) measured.** An attempt at a full-qual index (`by_qual`) was built and REVERTED: the exact-qual key never matches while the path still says `super::`, so it was an inert clause of exactly the kind this register criticised three times the same day. **ROOT CAUSE, one level deeper (2026-09-08):** `decls.rs`'s `visit_item_use` calls `collect_use(&u.tree, String::new(), self.out)` — **an EMPTY base with no module context** — so `use super::client::Client;` is stored as `Client` → `super::client::Client` with the `super::` retained LITERALLY. That is precisely the string the probe saw arriving at `resolve_target`. Note the engine already knows how to do this correctly elsewhere: `resolve_use_base` (`decls.rs:763`) walks `super`/`self`/`crate` against a `modpath` properly, but it is only called from the glob/re-export path (`:985`, `:1032`) and never for the per-file use map that `expand` consults. **Two paths compute one fact — how to resolve a use prefix — and only one of them is correct (§G).** Not fixed, DELIBERATELY, and the reason is blast radius rather than difficulty: threading `modpath` into `collect_use` changes how EVERY `super::`/`self::` import in every scanned crate resolves. It is the charging direction (it adds correct edges), but it is a main-dispatch-route change that needs the full matrix, a corpus A/B with reach instrumented, and four-way conformance — not the tail of an unattended run. **The work is now fully specified: give `collect_use` the module path and route it through `resolve_use_base` instead of storing the prefix raw.** Everything before that is done: both halves reproduced, the remedy corrected away from the 4.88-7.02% hedge, and the mechanism traced to one call with an empty argument. **ATTEMPTED 2026-09-09, BUILT, MEASURED INERT, AND REVERTED — and "fully specified" was wrong in the way that matters.** The sentence above names ONE call site. Probing `collect_use`'s entry shows it is called **TWICE** for the same `use super::client::Client`: once from `scan_items`, which HAS a `modpath` (`"net::request"`), and once from `collect_decls`, which does not (`modpath=""`). Threading the module path into the first is correct, compiles, resolves `super::client::Client` -> `net::client::Client` — and **moves nothing**, because the decl-index map built by the second still holds the literal. Two paths computing one fact, §G, and the row would have sent the next person straight into it. That is instance FIVE this week of correct code on a path the failing case never takes, and the only thing that found it was an `eprintln` at the entry rather than at the edited line. **WHAT THE REMAINING WORK ACTUALLY IS, measured rather than estimated:** `collect_decls` needs a `modpath` parameter; it already takes 24 arguments and carries `#[allow(clippy::too_many_arguments)]`, and its real caller is `cache.rs`'s per-file Pass A entry, which has no module path in scope either — so the thread runs file -> cache -> `collect_decls` -> the inline-`mod` recursion (which must append the module name) -> `collect_item_uses`. **It crosses a CACHED boundary**, so the decl-index digest has to be considered for cache correctness. That is not the tail of an unattended run either, and it is a bigger job than the sentence above implied. **`resolve_use_base` IS the right authority and this was verified, not assumed:** called with an EMPTY `mods` map it resolves `self`/`super`/`crate` from `modpath` alone and returns empty (no rewrite) for every other head — a strict widening. The clap objection recorded in `collect_use`'s own comment does not apply: that comment warns against pushing these through `expand`, which STRIPS the prefix, whereas rewriting `super::client::X` to `net::client::X` ADDS context and produces a more specific literal. Checked: at modpath `builder`, clap's `super::core::display_width` resolves to parent `""` and comes back `core::display_width` — byte-identical to today. **THE REPRO IS NOW COMMITTED, at `candor-spec/fixtures/r213-super-import/`, because the one this row cited had EVAPORATED** — `r212/repro` held only stale `.candor/` reports and `r212/fixture/src/lib.rs` was 0 bytes. Rebuilding it also corrected the row's own description: **two reconstructions did not reproduce at all**, and the defect needs a SAME-LEAF SIBLING elsewhere in the crate (`other::client::Client::execute`). Without it the bare-leaf fallback has one candidate and finds the right one; with it the uniqueness hedge drops the edge. The sibling is not scenery and the row never mentioned it. **Severity, sharpened:** `reported: 1 of analyzed 3`, `pure net::request::RequestBuilder::send` exits **0**, and nothing is disclosed (no `unanalyzed`, no `coverage`, no `excluded`) — but `deny Exec` over the whole crate still exits 1, because the DEFINITION is reported. The sin is FUNCTION-SCOPED, not crate-scoped, which the row did not distinguish. **FRESH MINIMAL EXECUTABLE REPRO 2026-09-16, and it is now the only known over-charge either [[R452]] or [[R454]] touches — worth closing.** `b::Inner::touch`, sharing only its LEAF with `a::Inner { v: Vec<Closer> }`, returns a `u32` field and spawns nothing, and is charged `Exec` **on both sides of candor-rust `40b0f17`** — i.e. the `Vec` arm already charges it today, with no map involved. Pinned by a test that fails loudly in BOTH directions when this row closes. |
| R214 rust: `coverage-gate-refresh` reports 'regressed' when its self-scan CANDIDATE ORACLE becomes more precise, not when `classify()` loses a rule — and the checked-in manifest is generated on macOS but diffed against a Linux runner | 2026-09-04 | **FIRST HALF FIXED — candor-rust `78fa172`, merged and pushed. SECOND HALF (the macOS/Linux split) STILL OPEN, and now stated in the job's own output** | **Instrument, rust — the label is misleading in the REASSURING-sounding direction and in the alarming one at once.** 3 of the 5 rows that run called 'regressed' are `94446f5` (R173) CORRECTLY withdrawing a drop-glue over-charge: `async_nats::Context::publish{,_message,_with_headers}` lost the edge to `PublishAckFuture::drop`, and that future is constructed in the tail expression and RETURNED, so its `Drop` belongs to the caller. classify()'s `Context::publish -> Net` rule is untouched and `crates/candor-classify/tests/coverage_gate.rs` still asserts it on every push — the gate stopped ASKING the question because its oracle got more precise. The workflow's own comment anticipates only the reappears-in-`open.tsv` case, not dropping out of BOTH lists because the effect set went empty or non-CORE. **Second half: the same commit `587275a` regresses 3 rows on macOS and 5 in CI.** Ruled out as causes: crate version, source bytes, gate code, classify(), `RAYON_NUM_THREADS` (1/2/3/4/8), HashMap seeding (5-run determinism), rustc sort tie-breaks. NOT ruled out: the host platform — Docker on this box failed with an I/O error, so no Linux run was possible. | Not fixed. 'Regressed' must distinguish 'classify lost a rule' from 'the candidate oracle stopped nominating'; and the manifest should be generated on the platform it is diffed against, or the diff must tolerate the split. **FIXED, first half.** `generate.py` gains `diff_manifests` / `--diff-manifests`, and the workflow's ~50 lines of bash `comm`/`cut` become one call to it — one authority instead of a reimplementation. A checked-in `covered.tsv` row the fresh run no longer sees now SPLITS: REGRESSED (it reappears in the fresh `open.tsv`, so self-scan still nominates it and classify() lost it) fails at exit 1; ORACLE-DROPPED (absent from BOTH fresh manifests, so the scanner's own candidate list shrank) is reported via a warning annotation and in the drift-issue body but does NOT fail. The reasoning is written into the code rather than a commit message: failing oracle-dropped would make a CORRECT over-charge withdrawal — exactly the R173 shape that produced 3 of the 5 rows on run 33872987298 — read as a permanent coverage regression, which is the cry-wolf failure this row exists to stop. **Discrimination proven on real data:** a constructed regression exits 1; the actual 3-row async_nats / e9cdd23 instance, read out of the checked-in covered.tsv itself, exits 0 with all three still reported; a clean run over the real 74-crate manifest diffed against itself exits 0. Seven new selftest checks (16/16, pure Python, every push), re-run by the coordinator after the merge. **Unasked-for and right, per the aggregation rule:** a warning when a WHOLE crate's entries vanish at once, since a crate-wide self-scan failure would otherwise read as harmless per-entry precision gains under the new category. **Second half NOT established, and the agent correctly flagged that MY BRIEF CONTRADICTED ITSELF** — it asked for the platform cause while forbidding a scanner build, and the Docker arm needs one. The job now STATES the split is unresolved in its own output whenever it reports anything, rather than silently trusting a cross-platform diff. |
| R215 swift: an UNANNOTATED `let`-copy of a container drops the element index entirely — for NOMINAL elements too, not just closures | 2026-09-04 | **CLOSED — candor-swift `4876551`. NINE element spellings plus the SCALAR sibling in the same binder chain, not the two the row named** | **CARDINAL SIN, swift.** One variable, executed ground truth: `stores.forEach { $0.eff() }` → `['Fs']`; `let ys: [S2] = stores; ys.forEach { $0.eff() }` (ANNOTATED) → `['Fs']`; **`let ys = stores; ys.forEach { $0.eff() }` (unannotated) → ABSENT**; `let ys = stores; for y in ys { y.eff() }` → ABSENT. Present on the R192 build and unchanged by R211's. **This is why Kingfisher scored zero hits for R211** — its real `let blocks = pendingBlocks; blocks.forEach { $0() }` is a live instance of THIS, not of R211. Repro `scratchpad/r211-out/adj2`, `adj3`. Distinguish from Alamofire's `finishHandlers`, which is a principled limit rather than this defect: its initializer is a call return, which `elementTypeOf` correctly declines to guess. | Not fixed. `elementTypeOf` must follow an unannotated `let` to its initializer's element type where that is a local container whose type is already known — the same 'the answer is already in hand, it is just not consulted' shape as R192's `typeName` gap. **FIXED, and the row understated it twice.** Nine ABSENT cells on the shipped `a1d8396`, all really writing files: `let ys = stores; ys.forEach`, the `for y in ys` twin, a `[S2]` PARAMETER copy, a copy-of-an-annotated-local, `let ys = self.plain`, a module-scope GLOBAL copy, `let m = dict; for (_,v) in m`, `m.values.forEach`, and a chained copy-of-a-copy. **Widening past the row's shape found the SCALAR sibling in the same `else if` chain** — `let a = one; a.eff()`, `let b = param; b.eff()`, and `let g = fm; try? g.removeItem(..)` over `FileManager.default`, a real file deletion reading silent-pure — fixed together, since splitting would leave two spellings of one question side by side. **Mechanism:** every other initialiser shape in `visit(VariableDeclSyntax)`'s unannotated chain reaches a resolver (ctor/factory and cast/ternary/subscript to `rootOf`, an array literal to its elements) and a plain COPY reached none — the DeclReference arm answered only a `localFreeFns` name and the MemberAccess arm only a singleton accessor; it now consults `elementTypeOf`/`dictValueOf`/`rootOf` in the annotated binder's own order. **Kingfisher recall, one variable held: `DownloadTaskUpdatedCallbackGate.open` ABSENT → `['Unknown'] callback:$0`, `deny Unknown` exit 0 → 1 — this is the line that made Kingfisher score ZERO hits for R211.** Six corpora, 11,639 rows: ADDED 36, REMOVED 0, CHANGED 108 wide (14 narrow), **465 instrumented branch hits against R211's 3 and R192's 0**; zero rows lost an effect; the 16 concrete gains ground-truthed from source, 12 correct and 4 inheriting `Db` through candor's existing sound-union of an ambiguous overload (stated, not narrowed). Revert → 56 failures across 4 of 8 new tests. `swift test` 1088/1088, smoke 148/148. **Its first draft wrote its own false guarantee and measurement withdrew it:** carrying `monoNames` onto the copy was described as a measured over-charge and is not — builds with and without are byte-identical on all five fixtures. **Left open deliberately:** a member-access SCALAR copy (`let a = self.one`) — `rootOf`'s member arm returns the BASE's type for a member it cannot place, and no compiling exploit could be built, so the restriction is worded as an assumption not a measurement; tuple-element access, unresolved even directly; and the Alamofire call-return limit, pinned by a test named as an ACCEPTED UNDER-REPORT rather than an over-charge control. **FIXED — candor-swift `4876551`.** Row-integrity note, 2026-09-09: this row had no TRAILING PIPE, so its remedy cell was unterminated and every column-indexed reader counted four columns in a five-column table. Terminated; no text changed. |
| R216 rust: the corpus A/B caught ZERO of the six silence regressions this round — two PROPERTY gates now do, and they expose a 216-shape debt register of still-open silences | 2026-09-04 | **INSTRUMENT, LANDED — candor-rust `5818af5`, wired into `ci.yml`. Coordinator-verified able to FAIL (red on pre-fix arms, clean at HEAD). The DEBT REGISTER it publishes is the finding** | **The safety net was not working and this measures by how much.** Six cardinal-sin regressions were introduced and caught during the 0.35.0 round (R172, R187, R194, R199, R203's sibling, R210); the 1,504-crate wide-key corpus A/B caught **none** of them — each measured 0 corpus incidence — and every one was found by a hand-built fixture, i.e. by somebody guessing the right shape. Two generated PROPERTIES catch them without guessing: `soundness/gen_q.py` (a `?` inserted where evaluation is identical may only ADD in-frame drops) and `soundness/gen_macro.py` (a single-arm `macro_rules!` must answer as the direct spelling it expands to), with `check_pair.py` classifying VIOLATION / DRIFT / BOTH-PURE / NO-GROUND-TRUTH. **Every generated pair is COMPILED and EXECUTED** (`mem::forget` on the return so only in-frame drops count); a pair that never drops is NO-GROUND-TRUTH and is not judged. **THE BRIEFED PROPERTY WAS VACUOUS AND THE AGENT DISPROVED IT BEFORE BUILDING ON IT:** '`?` vs a `?`-free twin' rediscovers **0 of 6** — with the constructed value escaping by return nothing drops in frame until a `?` creates an early exit, so the twin is charged nothing and no under-report can violate a superset test against the empty set; that twin is R187's own `collect_loop_noq`, whose documented expected answer is ABSENT. Changing the reference spelling to one that also drops is what gave it teeth. **Calibration by retro-rediscovery, attributed by ADJACENT-COMMIT difference** (not 'red on a pre-fix binary' — every pre-fix binary carries every later defect too, so redness at `a8fa8c6` attributes nothing; the agent's own first draft made that error and claimed all six for one gate): R187 12 shapes / R194 18 / R199 6+6 / R203 24 / R204 9 / R210 8 — **6 of 6 within 18 random seeds when BOTH gates run, and neither alone gets past 3 of 6**; they are complements, because `run_q`'s reference spelling is itself silent at HEAD on the macro rows. 11 calibration binaries kept at `scratchpad/gen/bin/`, table in `soundness/README.md`. Coordinator-verified: `run_q` vs PRE-R194 → 11 violations, vs PRE-R187 → 14, vs HEAD → clean; `run_macro` vs PRE-R203 → 22, vs HEAD → clean. **The debt register is the finding: `soundness/known_open.tsv`, 226 shapes — 29% of the gates' own space — every line a STILL-OPEN silent under-report, not an accepted behaviour.** 163 macro-equivalence violations, of which **50 are `body_local`/`blocktok_local`** — a `macro_rules!` defined in a function body or in re-parsed block tokens, which R206/R207/R210 closed for the `?`-interior VETO while the collector's own resolution stays open: **an independent, outside-in reproduction of R142/R143/R144's resolution-side story**. Plus 53 `?`-position drifts (all where a same-leaf value escapes by return and the construction or operand is macro-borne; ground-truthed at 1 in-frame drop on BOTH paths) and 102 BOTH-PURE pairs, counted and printed rather than gated. | Landed. **Stated limits, so nobody reads these as coverage:** blind on the 226 registered shapes; blind to a defect BOTH spellings share (that is what BOTH-PURE counts); blind to FABRICATION entirely — the corpus A/B remains the only fabrication gate and these complement it rather than replace it; one effectful `Drop` on one guard type, so no trait objects, generics, cross-crate edges, `async`, threads or non-`Drop` routes; `candor-scan` only, not the deep dylint engine; and still a composed space somebody wrote down. The register was measured on macOS and the Linux leg is an EXPECTATION, not a measurement — `baseline.sh --check` in CI is the first thing that will say otherwise. |
| R217 java: an OPAQUE callback handed to a HOF was silent while its LAMBDA and METHOD-REFERENCE twins disclosed — and the row's own premise was half wrong | 2026-09-04 | **CLOSED for `java.util.function.*` + `Runnable` + `Callable` — candor-java `18639a9`. Verified by the coordinator against a jar built at `a631fc3`. TWO residuals measured and deliberately left open: R236, R237** | **CARDINAL SIN, java, confirmed on executed ground truth.** But **"silent in EVERY spelling" is FALSE**, and the audit that assumed it would have gone to the wrong lever. Measured PRE (`a631fc3`) → POST (`18639a9`) on my own probe: `xs.removeIf(pParam)` and `xs.removeIf(pField)` and `m.computeIfAbsent(k, fnParam)` ABSENT → `['Unknown'] callback:<iface>.<sam>`; **but their lambda and method-reference twins ALREADY disclosed at `a631fc3`.** **The discriminator is the INTERFACE, not the higher-order function** — `Predicate`/`Function` are inside `isJdkFunctionalSam`'s set so the other two arms already charged; `Comparator` is outside it, and `cmpParam.compare(a,b)` — a DIRECT invoke with no HOF anywhere — is silent too. That half is a different mechanism and is now R236. **The row's suggested fix was also wrong and was not taken:** it named `SYNC_CALLBACK_INVOKERS`/`FOR_EACH_FAMILY`; neither table was touched. `isInvokingHof` was already the engine's authority for "does this library HOF invoke the functional argument it is handed", deciding that exact question at that exact site for the `new EffImpl()` arm. Three arms reach one HOF argument and only the third was unanswered. | Fixed. **Allowlist, so it fails toward SILENCE, never fabrication** — the denylist form was measured wrong at this same site historically. The wider `isHofFunctionalIface` was BUILT, MEASURED and REJECTED: +1,697 rows over 395 jars (596 in assertj-core, 560 in jandex) over `Comparator.comparing(...)` plumbing, while the direct `cmp.compare()` invoke stayed silent — a shape that charges the wrapper and not the call. **395-jar A/B, every field, 578,003 common rows: ADDED 458 / REMOVED 0 / CHANGED 963 wide, 338 `inferred`.** All 796 touched rows accounted for; 187 claim no effect at all and are the existing class-declares-a-capability rule; 0 rows lost anything in any channel; every gain is exactly `Unknown`. Instrumented 351 events / 298 sites / 60 jars, bucketed from `javap` rather than from candor's own report (187 param, 25 field, 128 call-return, 11 mixed). **The most useful thing it found is in the CONTROL, not the fix:** its first over-charge control SURVIVED its own degradation, because `Objects.requireNonNull`/`Optional.ofNullable`/`Stream.of`/`List.add` all erase their parameter to `Object`, so the interface gate excluded them and the allowlist under test was never consulted. It read as coverage of the allowlist and was coverage of erasure — caught only by actually degrading the guard. A null-`declType` skip was likewise written, measured at 0 rows over 395 jars, found to have no discriminating fixture, and DELETED rather than shipped behind a safety comment. **Residual (3), disclosed not silent:** a callback that is an in-scope call's RETURN is hedged rather than resolved (128 of 351 sites). **Two gates did not run on this box and are named rather than counted as passes:** `./gradlew nativeCompile` exit 1 (no GraalVM installed, so the native/jar parity step did not run) and `soundness/dynamic/corpus.sh` exit 3 SELFSKIP (`javac` refuses `Thread.ofVirtual` on JDK 17, upstream of any engine call). Everything else green, including `./gradlew test` 991/0 and `soundness/run.sh 40`. |
| R218 java: one fact is spelled two ways in a single report — `handleInvokeDynamic`'s project-owner branch emits `dispatch:java/lang/Runnable.run` with SLASHES while the other eight producers emit dotted | 2026-09-04 | **OPEN — PRE-EXISTING, found while fixing R183 (§F1 q7: 'when you close a channel, ask what OTHER spelling reaches it')** | **Instrument / wire-format, java.** `xnio-api` 3.8.16 carries BOTH spellings of one fact in one report, so a consumer grouping or matching on `unknownWhy` sees two distinct reasons where there is one. Not a soundness defect on its own, but it defeats exactly the kind of downstream grouping the field exists for. | Not fixed. One producer, or a normalisation at the writer. |
| R219 spec/conformance: NO PART combines an `unknown-alias` policy with an out-of-scope PEEK — the gap that let R154 live | 2026-09-05 | **OPEN — INSTRUMENT/coverage gap, found by the R154 fix agent** | **Conformance gap.** The only alias fixture is `gen_rung024.py`'s `_r9_alias_policy`, and it drives `q_gate` over a HANDWRITTEN report document — never a scan of a tree, so never the peek. PART 53's shapes use no alias at all, and PART 48 exercises the peek with no alias. R154 — a policy alias line disarming the ⟨0.30⟩ fail-closed INCOMPLETE verdict, on every engine that shares the shape — sat in a released engine because the two features are only ever pinned separately. | Not fixed. The natural new PART: an `unknown-alias` policy over a tree with something genuinely out of scope, asserting the peek is ATTEMPTED and the verdict INCOMPLETE. |
| R220 rust + swift (ASK, not yet measured): does the PEEK's policy parse use the same alias vocabulary as the GATE's? | 2026-09-05 | **OPEN as a QUESTION — R154 was exactly this divergence in candor-ts; §F says ask the other three, and the agent could not (it owns one repo)** | **Possible cardinal-sin shape, unmeasured.** In candor-ts the peek passed `{}` where the gate passed the real alias map, and a `TypeError` in a bare `catch` silently disarmed the fail-closed verdict. java is known to have withheld correctly at ⟨0.29⟩ where the other three did not, so **rust and swift are the ones to ask**. This row is a QUESTION with a stated method, not a finding: build the R154 fixture shape per engine (an excluded file performing an effect, plus `deny Unknown[<alias>]` with the alias defined in `.candor/config`) and compare the exit code and the presence of `outOfScope`/`peeked` against the same policy without the alias line. | Not investigated. If an engine passes, say so in this row — a measured negative here is worth as much as a finding. |
| R221 umbrella: `assert-audit.sh`'s `ASSERT_RE` misses the 'cannot change / cannot affect' family of safety claims | 2026-09-05 | **OPEN — near-miss reported by the R154 fix agent about its OWN diff** | **Instrument, umbrella.** Its one safety argument was *'cannot change any run that does not already crash'* — precisely the shape this tool exists to catch — and `ASSERT_RE` has `cannot happen` and `can never` but nothing for `cannot change`/`cannot affect`. The agent reworded its own comment to state the assumption, so the tool now correctly reports 'nothing to defend' — **but it would have reported that for the un-reworded version too.** This sits directly beside R158, where the same tool's TEST_RE was too wide; the pattern is that both halves of this check were calibrated by example rather than by class. | Not fixed. |
| R222 rust: `by_tail2` counts ONE definition twice, so the uniqueness filter refuses a call that is not ambiguous — 11,230 functions hedged on a non-ambiguity, and today those calls resolve to NOTHING | 2026-09-05 | **CLOSED — candor-rust `00e47cd` (`by_tail2` half) + `c8de235` (`by_leaf` half, implementing SPEC §4 `eb6f2f2`). BOTH on `main`, unreleased. STATUS CELL WAS STALE — it still said OPEN while this row's own last cell named both closing commits; corrected 2026-09-07** | **CARDINAL SIN shape, rust.** `by_tail2` pushes `f.qual` per `FnInfo`, and duplicate `FnInfo` rows are common — R129 measured 440 of 836 corpus reports carrying them. So a key can hold two entries with only ONE distinct qual, `resolve_target`'s uniqueness filter sees 'ambiguous', and the call resolves to nothing: **a silent lost edge on a call that was never ambiguous.** 11,230 functions are in that state. Two source shapes seen in `half-2.3.1` (`leading_zeros_u16`, `convert_from_f32_slice`), both collapsing distinct source items onto one qual. **The agent did not finish splitting the mechanism and said so — severity is measured, the cause is not.** | Not fixed. Deduplicate the qual set before the uniqueness test, but establish first WHY distinct source items collapse onto one qual — fixing the symptom without that risks hiding whatever produces the duplicate rows (R129's subject). **CAUSE SETTLED 2026-09-05 — it is R129's cause, and the obvious fix is NOT SAFE ALONE (measured).** **Seven mechanisms, each with a compiling fixture (`scratchpad/dupqual/f*`) and a real-crate instance ground-truthed from source:** two `#[cfg]` arms of one fn (`half` `leading_zeros_u16`, `windows-sys` `SHSTOCKICONINFO::clone` per `target_arch`); two `#[cfg]` arms of a MODULE (R190(c)'s own `sys::sz`); ≥2 impls of one trait for one type differing only in type params (`jiff` `ZonedArithmetic::from` ×3); `impl Trait for [T]`/`(T,)`/`&T`, where `lang.rs:1870` `impl_type_name` returns `None` and the TYPE SEGMENT IS DROPPED (`half` `convert_from_f32_slice`, `x11rb` `serialize_into`, `diesel` `to_sql`); an inherent assoc fn beside a trait method of one name; generic args erased (`typenum` `private_pow`); ≥2 DIFFERENT traits with a same-named method (`rustix` `Uid::fmt` ×6). **Killed with fixtures, NOT mechanisms:** a macro-declared fn (zero units, not two — that is R128/R139), a `pub use` re-export, one file reached by two `#[path]` mods. **The control separating this from R190(c):** two modules each with `Bar::go` are DISTINCT quals sharing a tail — a real ambiguity, untouched by dedup. **PRICE ON SHIPPED `main`** (A/B over 1,509 crates vs `11f8fc1`, dedup on `by_tail2` only, instrument calibrated byte-identical to ship with the flag off): 1,941 rows ADDED, 6 removed, 418 crates touched; **510 functions gain a CONCRETE effect** (Log 157, Rand 150, Clock 111, Fs 110, Db 77, Env 71, Exec 32, Net 19), **of which 347 are SILENT today** — absent, no `Unknown`, nothing to fail closed on. The other ~10,700 of the 11,230 reach a target that is pure anyway, which is why the population is large and the damage is not. `by_leaf` twin: +125 concrete, 974 rows lose a spurious `ambiguous:same-name local defs` — disclosed, so precision not soundness. **The row's premise is right for QUALIFIED calls only:** the same duplicate feeds `by_leaf`, where the bare-call path DOES disclose. **Why the obvious dedup is unsafe, measured not argued:** all 6 removals audited — `rand_core-0.6.4 error::Error::fmt` goes `['Rand']` → ABSENT, because `tail2` discards the crate qualifier so `getrandom::Error::from` resolves to a local `Error::from` and `scan.rs:2389`'s `!resolved_local` guard suppresses the classifier. Three-arm fixture, one variable (`f15`/`f16`/`f17`): with NO local `Error` the `Rand` is charged in both arms; with ONE it is ALREADY LOST on shipped main; with TWO the duplicate was accidentally BLOCKING the mis-resolution. **So dedup does not create that hole — it hands more call sites to it (see R223).** Also: dedup hides R129's cause; `cli.rs:1812` pins TWO report rows as required; `scan.rs:2807` records that deduplicating `all` was tried and reverted; and per-`#[cfg]`-branch keying — the repair that comment names — points the OTHER way and would NOT recover the 510 edges. **PARTLY CLOSED — candor-rust `00e47cd`: the `by_tail2` half only. The `by_leaf` twin is MEASURED and WITHHELD pending a ruling.** On top of R223: 1,941 rows added, 5 removed, **505 concrete gains, 0 losses**; all 5 removals audited from source — each lost an `invisible` it had INHERITED through an edge that existed only because the primary route was fake-ambiguous (curl `FormError::fmt`→`Error::fmt`; digest `D::digest`→`DynDigest::finalize` ×4). **The ordering claim is confirmed by its counterfactual:** dedup alone on `11f8fc1` takes `rand_core error::Error::fmt` `['Rand']` → ABSENT and removes 234 rows; with R223 first it loses nothing. **The `by_leaf` half was built and measured and NOT shipped: +125 concrete effects for −1,400 `ambiguous:same-name local defs` disclosures and −228 rows**, and it turns that reason off for the cfg-twin shape SPEC §4 ⟨0.24⟩ admitted the kind for (8,710 of 19,607 `unknownWhy` entries in a 1,062-report census). Withdrawing an existing answer is the move measured and refused for R208 this release, so the batch's direction was kept uniform: the qualified spelling gains an edge it never had, the bare spelling keeps disclosing — it was never silent, so that half is not the cardinal sin. Checked mechanically before deciding: of 3,603 ambiguity sites in the 59 crates it touches, **all 134 rows it would withdraw had `distinct = 1`**, while 2,237 `distinct = 2` and 824 `distinct = 3` sites are genuine and stay. **2026-09-05, decision taken after a Fable review + a candor-spec port: TAKE the `by_leaf` half.** The deciding measurement is that a genuinely-differing `#[cfg]` twin does NOT go quiet — it goes to the UNION of both arms (`Fs` + `Exec`, order-independently), so this is a hedge replaced by a COMPLETE answer, not a withdrawal; the R208 analogy was INVERTED (that one went concrete → hedge). A distinct-definition site is untouched, a real `Unknown` inside an arm survives by inheritance, and a both-pure twin's row correctly disappears. Corpus: 128 concrete gains, 0 concrete losses. **CORRECTION to this row's own fastrand claim, measured by the porting agent:** the three-arm twin is `global_rng::random_seed`, NOT `Rng::new`, and the flip is SCOPE-DEPENDENT — `deny Rand global_rng::Rng` goes exit 0 → 1, but crate-wide `deny Rand` and `deny Rand global_rng` were ALREADY exit 1 on shipped main, and `deny Unknown global_rng::Rng` was red in BOTH arms. So it is an effect-named gate passing over DISCLOSED opacity, not a silent under-report. The coordinator repeated the overstated version. **SPEC was already on this side and nobody had noticed:** `same-name local defs` appears nowhere in SPEC.md, and SPEC.md:5482-5483 already calls a cfg twin 'one qualified name' and its appearing twice a DEFECT — the binding to the cfg shape lived only in a conformance fixture and its comment. **candor-swift ALREADY conforms** (a `#if os()` twin gives the union), so the clause is not a landmine four-way. Ported in candor-spec `eb6f2f2` (§4 clause, MUST ledger 529 → 530, classified) and `a389f1f` (PART 10 re-pointed to a distinct-definition fixture, plus `armsunion`/`armsswap` — the union-vs-picked-one-arm discriminator no existing test had). **Caveat now filed as R227.** **CLOSED — candor-rust `c8de235`, the `by_leaf` half, implementing SPEC §4 (candor-spec `eb6f2f2`).** Both consumers move together: `resolve_target`'s uniqueness test starts resolving AND the disclosure stops firing on cfg twins, so the hedge is replaced by the UNION, not by silence. A/B vs `00e47cd` over 1,509 crates on every field: ADDED 0, REMOVED 300, CHANGED 2,129 (1,080 on `inferred`), **128 concrete gains (Clock 89, Rand 80, Env 27, Fs 10, Ipc 6, Log 6, Net 4), 0 concrete losses**; §E1 reach 2,059 leaf buckets collapsing in 569 of 1,509 crates. **All 300 removals audited from source — 228 distinct functions, NONE carrying a concrete effect** (134 held only the withdrawn hedge, 94 inherited it). Executed ground truth: the union fixture really writes 8 bytes through the caller and `deny Fs go` goes exit 0 → 1. **Acceptance test, coordinator-verified: `conformance/part.sh 10` exit 1 against shipped `00e47cd` and exit 0 against the new build**, with `rust(arms)` and `rust(arms,swapped)` both `['Exec','Fs']` and `rust(vocab)` still emitting `ambiguous` in both arms. The `≈1,217 by distinct fn` figure in this row could not be reproduced: it is 1,167 rows on a first-of-duplicates key (1,033 in place + 134 removed), and 347 distinct function NAMES. R129's duplicate-row count moves the other way from `00e47cd`'s, 7,371 → 7,299, because 228 functions leave `functions[]`. |
| R223 rust: `resolved_local` suppresses the crate classifier on a call whose tail2 key matched a LOCAL definition — the newtype/wrapper forwarding idiom silently loses the effect it forwards | 2026-09-05 | **CLOSED — candor-rust `9668b2d`, on `main`, unreleased. STATUS CELL WAS STALE — it still said "OPEN — PUBLISHED cardinal sin on shipped `main`" while this row's own last cell said CLOSED with the commit; corrected 2026-09-07. The PUBLISHED artifacts 0.34.0/0.35.0 are still affected; `main` is not** | **CARDINAL SIN, rust.** `scan.rs:2389` reads `classified.filter(\|_\| !suppress_bare_leaf && !resolved_local)` — local resolution deliberately suppresses the crate classifier, an anti-fabrication guard. But `tail2` throws the crate qualifier away, so a call written `<extern>::Type::method` matches a same-named LOCAL definition, `t != f.qual` drops the edge, and the classifier that would have charged the effect is suppressed. **Census over the same 1,509 crates, instrumented at the site (`classified.is_some() && resolved_local`): 123 suppression sites, 99 distinct caller functions, 26 crates; suppressed effects Net 72, Fs 24, Db 19, Rand 6, Env 2; 59 of the sites leave the caller ABSENT from the shipped report — silent.** Worst: `deadpool-postgres-0.14.2` 17, `fs-err-2.11.0`/`3.3.1` 7 each, `cap-std-3.4.5` 6, `rkyv` 3+3. Read from source: `deadpool-postgres/src/generic_client.rs:98` is `tokio_postgres::Client::execute(self, query, params).await` inside `impl … for Client { async fn execute … }`; tail2 `Client::execute` matches the ENCLOSING UNIT ITSELF, the edge is dropped, `Db` is suppressed, and `generic_client::Client::execute`/`::query`/`::query_one`/`::prepare_typed` are all absent from the report **while the same crate correctly charges `Db` on `Manager::create`** — so the scan demonstrably works and the silence is targeted at the forwarding surface consumers actually call. **The dominant shape is the newtype/wrapper idiom forwarding to the same-named method of an external type**, which is why `fs-err` (a `std::fs` wrapper) and `cap-std` are on the list. | Not fixed. This is the key-WIDTH problem again (R190(c)'s second flood mechanism): the guard is right, the KEY is too narrow. Any R222 dedup must land with this, or it hands more call sites to this hole — measured, not predicted. **CLOSED — candor-rust `9668b2d`.** The local definition's authority to SUPPRESS the crate classifier is withdrawn when the only thing tying the call to it is the two segments `tail2` kept; the local EDGE is kept, so the change is strictly additive. `Cargo.toml` is the authority on what is external, and a local qual that is a ≥3-segment suffix of the written path keeps its authority. 53 rows added, 0 removed, **91 functions gain a concrete effect, 0 lose one** over 1,509 crates. Executed ground truth: `wrap::size_of` (a real `stat`, 192 B) ABSENT → `['Fs']`, `deny Fs wrap` 0 → 1; a crate-root-qual variant likewise; a control whose type is named differently is `['Fs']` in both arms; an over-charge control stays absent. `deadpool-postgres` `generic_client::Client::execute` ABSENT → `['Db']`. **PREMISE CORRECTION, measured TWICE: the obvious 'widen the key so resolution refuses' reading is WRONG in the SIN direction** — it dropped 161 concrete effects (rdkafka `Log` ×30, sqlx-postgres ×12, tungstenite `Rand`), and a suffix-corroboration hatch still cost 43, because `c.path` is NOT the written source text: `expand` resolves it through the `use` map and a glob claims every name it could supply, so a dep-looking head is not proof of a dep call. Also measured: a plain suffix test is degenerate for a crate-root qual (`TlsConnector::connect` is a suffix of everything ending in it), so the test needs a ≥3-segment qual. |
| R224 rust: `expand` lets a glob `use dep::mod::*` claim a name the crate DEFINES ITSELF, so a call on a local type is recorded under a dependency's path | 2026-09-05 | **OPEN — latent mis-attribution, benign TODAY; found because it made two straightforward R223 fixes unsound** | **Resolution defect, rust.** `use rdkafka_sys::types::*` causes a call on rdkafka's OWN `client::Client` to be recorded as `rdkafka_sys::types::client::Client::native_ptr`. Harmless at present only because the classifier has no rule for those crates — the moment one is added, the effect is attributed to the wrong crate. Ground-truthed in `rdkafka-0.39.0` and `sqlx-postgres-0.9.0`. **Its practical consequence is already paid:** it is why the obvious R223 fix (treat a dependency-qualified path as unresolvable) dropped 161 concrete effects — `c.path` is not the written source text, and a glob makes a dep-looking head no proof of a dep call. | Not fixed. A glob should not claim a name the crate defines itself; local definitions must win over glob-supplied candidates. |
| R225 instrument: 16 of candor-rust's 29 auto-run gates die with SIGKILL on this macOS box, and every agent has had to reason around them for days | 2026-09-05 | **RESOLVED AS A DECISION, NOT A FIX — 2026-09-24. IT NO LONGER REPRODUCES (measured), AND THE STANDING INSTRUCTION IN THIS ROW IS WITHDRAWN.** The cause was never confirmed and an intermittent environmental fault can return, so this is an accepted-and-watched limit rather than a closure: if it returns, re-file with the disk/swap reading. It was on the shipping-defect list for a day on my own wording — the row itself said 'NOT kept open as a shipping defect' while sitting on exactly that list, which is the contradiction [[R591]] exists to stop. Originally: ENVIRONMENTAL, established identical at merge-base by three independent agents; CAUSE NOT FOUND despite a focused attempt | **Instrument, local only.** Every failure traces to `~/.dylint_drivers/nightly-2026-06-14-aarch64-apple-darwin/dylint-driver` taking `signal: 9 (SIGKILL)`. **What is ruled out, measured:** it is not the change under test (identical at `11f8fc1` and at merge-base, proven three ways by two earlier agents — same failures with the diff stashed, a fresh worktree clean, the diff cherry-picked into that worktree clean); not the binary (running it directly returns rustc's version, rc 0); not code signing or a missing toolchain (`nightly-2026-06-14` is installed, `codesign -dv` is clean); not the dylib (loading it by hand, rc 0); not memory (52% free, machine idle); and **not parallelism — `--test-threads=1` and `CARGO_BUILD_JOBS=2` give the identical 17 SIGKILLs, and the run dies in 0.06s, far too fast for exhaustion.** **A claim that a fresh worktree fixes it did NOT reproduce for me** (14–16 failures in both the main checkout and a detached worktree at the same commit). Narrow invocations pass: `cargo test -p candor --lib ui` is 18/18 green minutes before `cargo test --workspace` produces 17 kills. | Not fixed, cause unknown. **The operational consequence is what matters: gate-run's verdict on this box is not evidence about a diff, and CI on Linux is the arbiter for anything dylint-dependent.** Every agent brief should say so rather than each rediscovering it. **DOES NOT REPRODUCE AT candor-rust `845cb32`, 2026-09-24 — MEASURED, on a quiet box with 40 GiB free.** `cargo test --workspace`, which is the command THIS ROW says produces 17 kills: exit 0, 902 tests, 0 failed, **0 SIGKILL**. The driver demonstrably RAN — 57 dylint ui fixtures across two suites (`ui/closures.rs`, `ui/devirt.rs`, `ui/std_driver_hash.rs` …), 18 + 39 passed — so this is not a vacuous green off a suite that never reached the driver. `bin/gate-run.sh candor-rust` serial and alone: 41 gate lines, **31 run, 31 OK, 0 failed**, 10 skipped (all apt-get provisioning or Linux-only strace self-skips); `cargo test --workspace` is gate 6. 12 consecutive runs of the driver-exercising suite: 12 pass, 0 kills — and this row claims a MAJORITY failure rate (16 of 29), not a flake, so one clean pass is already strong. The driver binary is unchanged since 10 Aug (mtime), adhoc/linker-signed, `codesign -vvv` valid, runs standalone rc 0 — **so the driver is not what changed.** **INFERRED, NOT MEASURED** (no diagnostic reports survive from 2026-09-05): ambient memory/swap pressure from concurrent agent waves, i.e. jetsam. It fits every item this row ruled out, including the one that otherwise does not fit — `--test-threads=1` and `CARGO_BUILD_JOBS=2` changing nothing, because the pressure was AMBIENT (other agents) rather than from that invocation; and 'not memory (52% free)' measured RAM FREE, while swap on this box totals 2 GB, which a four-agent rust wave exhausts. **THE OPERATIONAL CONSEQUENCE IS REVERSED: candor-rust's local gates ARE evidence again.** If it returns, check disk and swap headroom FIRST. Separately: this row's own remedy — 'every agent brief should say so' — was NEVER APPLIED; `grep SIGKILL` over both briefs and CLAUDE.md was empty for 19 days. Applied now, with the corrected guidance rather than the withdrawn one. |
| R226 conformance: `part.sh <id>` reports a FALSE RED when a part builds its fixtures ABOVE its own `[id]` header — PART 4n today, PART 10 until 2026-09-05 | 2026-09-05 | **CLOSED 2026-09-06 — candor-spec `a389f1f` (PART 10) + this range (PART 4n). The CLASS is not closed: see the last cell** | **INSTRUMENT DEFECT, candor-spec, and it fails in the direction that LOOKS LIKE AN ENGINE BUG.** `part.sh` slices `run.sh` at the printed `[id]` markers, so a part whose predecessor ends on a HEADER rather than a `PART n` verdict has `end = limit` and the predecessor's slice swallows the next part's fixture setup. `part.sh 10` therefore built no `vocab.json` and fired its own 'the purpose-built ambiguity fixture produced NO `ambiguous:` reason' DIVERGE against a HEALTHY shipped `00e47cd`; `part.sh 7 10` on the identical tree was green, which is the only reason the two reds were told apart. **PART 4n has the identical shape and is still live:** its `ext/report*.json` are written inside the previous slice, and `part.sh 4n` alone reports FAIL rust / FAIL java / FAIL ts plus a DIVERGE. Swept mechanically over all 88 slices: 4n is the only remaining false-red. 12b/12c under-reach the same way but die LOUDLY on an unbound variable (the documented safe shape); 4h, 4k, 16, 38, 84 read state an earlier part built and are the documented 'ask for both ids' case. **`part.sh`'s own header asserts the boundary rules 'can only UNDER-reach — a slice missing its own tail', which is FALSE in this direction:** an under-reach that loses a part's SETUP produces a hard DIVERGE, not a missing row. Invisible to CI (the full suite runs everything in order) and visible only to the person iterating on one part — exactly who trusts it. | Fixed by the cheap remedy: PART 4n's `mkdir -p "$W/ext"` and its three report heredocs now sit BELOW the `[4n]` header, with a comment saying why they must stay there. **Verified both directions on one tree: `part.sh 4n` alone was exit 1 with three FAILs and a DIVERGE before, exit 0 with MATCH after; the full `conformance/run.sh` is exit 0 / 80 parts both before and after, so the move changed no verdict.** `part.sh --check` still passes all 88 slices. **THE CLASS REMAINS OPEN AND THAT IS THE PART WORTH KEEPING.** Two instances have now been found the same way — by someone hitting a false red while iterating — and the guard that should find them cannot: `part.sh --check` verifies a slice PARSES and names exactly one part, never that it can still ANSWER, so it is blind to setup-annexation by construction. A third instance will be found the same expensive way. The durable remedy is a `--check` that RUNS each slice alone and compares its verdict to the same part's verdict in a full run; anything cheaper re-checks the property that already holds. Not built here — it is a new instrument, not a fix. |
| R227 rust: the withheld `by_leaf` dedup withdraws a hedge that was accidentally COVERING a different open silence — the corpus A/B counted lost EFFECTS, not lost COVER | 2026-09-05 | **OPEN — measured on a fixture by the spec agent, one variable, both binaries built from one tree. NOT a reason to hold the dedup** | **NOT A NEW SILENCE, and that distinction is the row.** Fixture `armmacro`: `#[cfg(unix)] helper()` reads a file; `#[cfg(windows)] helper()` calls `hidden()`, declared by a `macro_rules!` and therefore ZERO units (R128/R139). On shipped `00e47cd` the report ALREADY carries `helper = ['Fs']` with no `Unknown` — the Exec is silent at `helper` TODAY — while the caller reads `['Unknown'] ambiguous:same-name local defs` and so fails closed under `deny Unknown`. With the dedup the caller becomes `['Fs']`: a positive claim over a call that reaches Exec. **The change does not create the hole — the hole is R128/R139's — it removes an accidental cover over it.** | Not fixed, and not a reason to hold: the direction is right and R222's A/B is not wrong, but it answered a NARROWER question than it appeared to. Of the ~1,400 `ambiguous:` disclosures the dedup withdraws, nobody has measured how many sit over an arm carrying a KNOWN silence (a macro-declared callee, R128/R139; a `#[cfg]`'d `use` resolved by source order, R123/R140). **'0 concrete effects lost' is true and is a different question from '0 cover lost'.** Cheap measurement: a corpus pass over the withdrawn rows asking whether any arm's own row carries a macro-declared callee. **MEASURED 2026-09-05 by the porting agent — the row's OWN PREDICTED MECHANISM IS ABSENT, and that is the finding.** Of 1,167 withdrawn direct-reason rows, 961 now reach a target that CARRIES an effect; the other 122 reach one already reported pure, and **all 122 were read from source**. **32 rows (2.7%) sit over an arm whose effect reaches the machine by a route this engine does not model** — inline `asm!` and `core::arch` RNG intrinsics (getrandom `rndr`/`rdrand_u32`/`rdrand_u64`/`is_rndr_available`, 22 rows across 5 versions), `extern "Rust"` logger hooks (defmt `export::{timestamp,acquire,release}`, 5), `ffi::sqlite3_threadsafe` (rusqlite ×2), a `link`ed `GetFullPathNameW` (cmake `fix_build_dir`), libc `poll` (serial-unix), `process::exit` (dylint_driver). **In every case the target is absent from the report on BOTH sides, so the silence pre-exists at the callee and only the COVER moved** — and that cover was equally absent from the thousands of QUALIFIED call sites reaching the same callees. **R128/R139 (macro-declared callee) and R123/R140 (cfg'd `use` by source order) — the shapes this row predicted — appear ZERO times**, checked with a deliberately over-broad heuristic whose 8 hits were all plain cfg twins on inspection. Not a reason to have held the change. **The advisory should name unmodelled FFI and inline assembly, not a macro-declared callee.** |
| R228 swift: a bare identifier ARGUMENT was resolved against a same-named FREE FUNCTION, discharging a callback deferral nobody made | 2026-09-05 | **CLOSED — candor-swift `4876551`, fixed alongside R215. PRE-EXISTING at published 0.35.0 (`a1d8396`), reproduced with an ordinary ctor-typed receiver, so NOT introduced by R215** | **CARDINAL SIN, swift, found by R215's corpus A/B — its first run returned REMOVED 2 and both were real.** `argKinds` recorded every DeclReference argument as `.named(n)`, and the Driver's callback-flow discharges a deferred callback parameter via `freeFnByName[n]`. So GRDB's `OrderedDictionary.merge(_:uniquingKeysWith:)`, which really invokes a caller-supplied closure, was **ABSENT** — certified pure — while its caller carried a REASONLESS `Unknown` (the R180 shape) and an edge to an unrelated `private func combine` in a different file. Repro `scratchpad/r215-out/min4` on the PRE binary, plus `min3` and `bis/m2`. **Fixed by making `namesAFreeFunctionReference` the one authority** — the guard the Driver's unqualified-CALL arm and the fn-reference argument arm already had (§F1.3, two sites answering the same question correctly and a third not) — refusing only names it can place as tracked locals, a denylist. Isolated corpus A/B: ADDED 0 / REMOVED 0 / CHANGED 0 — **safety-only alone, load-bearing in combination with R215**, which multiplies its reach. | Fixed. |
| R229 rust: the SITE walk and the ESCAPE walk read a macro DIFFERENTLY, so one macro-borne construction was certified pure in one direction and fabricated in the other | 2026-09-06 | **CLOSED — candor-rust `238b271` + `96fe300` + `fe0ebde`, renumbered from a colliding R226 in `0345098`; falsified against a pre-fix binary I built myself at `c8de235`** | **Both directions were live at once, which is why neither gate caught it.** `lang.rs` reached a macro invocation twice: the walk that records construction SITES and the walk that decides what ESCAPES each read the tokens their own way, so a construction visible to one was invisible to the other. Where the site walk saw it and the escape walk did not, it was charged though it escaped (fabrication); where the escape walk saw it and the site walk did not, an in-frame drop went ABSENT (**cardinal sin**). **Independent verification, executed ground truth** (`mem::forget` on the return, so the counter sees in-frame drops only): 4 `dies_*` cells drop 1, 8 `esc_*` drop 0. Scanning that fixture with a binary I built at `c8de235` and one at fix HEAD: **PRE 7 wrong cells of 12 — 3 cardinal sins (`dies_tmpl`, `dies_blocktok`, `dies_stmt_tmpl`) and 4 fabrications; POST 1 wrong cell, and 0 cardinal sins.** Every direct (non-macro) twin is correct in both binaries, which is what pins the cause to the macro reading rather than to the escape model. **The fix DELETED a guard rather than adding one:** `macro_ctor_leaves`, a leaf-keyed set that let the site gate certify an escape with no evidence for any leaf any macro in the body constructed. `macro_reading` is now the single reading both walks take. **Scale:** debt register 216 → 141 shapes (75 closed, 0 new); 1,509-crate registry A/B on a wide key **ADDED 0 / REMOVED 0 / CHANGED 0 over 270,874 rows, 0 functions lost a concrete effect**, with a site-recording-only isolation arm byte-identical to both arms, so the two halves are not cancelling. §E1 reach on the DECISION: `R229DECIDE` 1,741 hits / 143 crates, `R229NESTED` 5,794 / 43. | Fixed and verified. **The premise it was dispatched under was wrong and it said so:** the brief called for a liveness redesign to arbitrate between a leaf-keyed escape gate and the `?` veto. That gate has been site-keyed since R172, the two mechanisms both narrow the escaping set so they fail in the same direction and compose, and a liveness pass answers 2 of the 9 rows it was aimed at, not most. The finding above is what was actually there. |
| R230 rust: a by-value parameter of a Drop type is charged a `::drop` edge whether or not the callee drops it — a function that only STORES it is charged identically to one that drops it | 2026-09-06 | **OPEN — mechanism now MEASURED on an unambiguous fixture (0 drops anywhere). The test defect is fixed; the engine change is deliberately not made** | Each of the six asserted that a macro-borne construction on a `?` operand's by-value spine "stays exempt" from R172's site gate. R172 keys on sites and has **no by-value-argument exemption at all** — every one passed because the construction was invisible to the walk R229 fixed, and every one has a direct twin the gate has charged since published 0.34.0. They now assert parity with that twin. **The proposed follow-up — withdraw the caller-side `::drop` edge for a construction moved by value into a callee — is NOT supported yet, and the row is filed OPEN rather than actioned because the evidence offered for it cannot bear the claim.** The justification was "executed 0 in-frame drops", but the harness both the agent and I used forgets only the RETURNED value, so its counter runs for the whole dynamic extent of the call and counts the CALLEE's frame too. It cannot separate the two frames, which is the entire content of the claim. Measured on it, `byval_try` drops 1 — the opposite of the number quoted. **What I did establish, on four executed shapes:** where the callee is resolvable candor already models it dropping its by-value parameter (`use_h_val` carries `calls=['H::drop']`), so the caller's extra edge is redundant, not load-bearing; where the callee is a fn pointer or a closure parameter the caller carries no drop edge at all and discloses `Unknown` instead. So withdrawal looks safe on these, **but there is an untested shape that decides it — a resolvable callee that STORES the value rather than dropping it, where the caller-side edge would be the fabrication and its removal correct for a different reason than the one given.** | **RESOLVED 2026-09-06 by building the shape this row said would decide it — and it moves the row from the caller to the CALLEE.** Fixture: `keep(h: H, v: &mut Vec<H>) { v.push(h); }` STORES its by-value parameter, so it drops nothing, and its caller's `H` escapes into `v`. Executed, **0 drops ANYWHERE** — no frame-attribution question arises, which is what makes this fixture decisive where the earlier one was not. Measured: `keep` is charged `calls: ['H::drop']` -> **Fs. FABRICATION.** Its twin `eat(h: H) { let _ = h; }`, which really does drop, is charged identically and correctly. **So the engine charges a `::drop` edge for ANY by-value parameter of a Drop type, whether or not the body drops it** — the callee cannot tell storing from dropping. `caller_store` is then charged twice over: its own caller-side edge AND `keep`. **This makes the originally proposed fix insufficient, not merely unevidenced.** Withdrawing the CALLER-side edge alone leaves `keep`'s fabrication standing and does not clear `caller_store`, which stays charged through `keep`. The subject is R200's — the callee — and the caller-side edge is the second-order part. Still not fixed here: it withdraws a charge, which is a report-not-decide item under this run's stop conditions. Direction is over-report, the safe one. |
| R231 rust: the STATEMENT-template macro form is still charged a drop-glue edge for a construction that ESCAPES, and neither property gate can see it | 2026-09-06 | **OPEN — pre-existing, measured identical at `c8de235` and at the R229 fix HEAD, so R229 did not introduce it and did not reach it** | `push_h!(v, "a")` expanding to `let x = H::new("a"); v.push(x);` where `v` is returned: executed **0 in-frame drops**, reported **charged** by both binaries. Its direct twin `{ let x = H::new("a"); v.push(x); }` is correctly ABSENT in both, so this is the same macro-reading class as R229 in a form the fix did not cover. Over-report; fails in the SAFE direction. **Why it was invisible to the instruments built for exactly this, which is the part worth keeping:** `known_open.tsv` registers only under-reports — its 141 entries are all `DRIFT`/`VIOLATION` — and `run_q.sh` says in its own header that **the corpus is the family's fabrication gate**. The corpus A/B for R229 was 0/0/0 over 270,874 rows, i.e. this shape has zero corpus incidence. So the property gate does not track this direction and the corpus gate has no instance of it: **an over-report in a rare shape falls in the gap between the two, and only an executed fixture finds it.** | Not fixed. The gap is the finding as much as the cell is: the two gates are complementary by design, and this is the quadrant neither covers. **NOT the same mechanism as R230, settled by the fixtures already built rather than by argument:** R230 charges a by-value PARAMETER of a Drop type regardless of what the body does with it, and R231's cell has no by-value parameter at all. The direct twin `{ let x = H::new("a"); v.push(x); }` — a LOCAL construction moved into a container that escapes — is correctly ABSENT, so that path is sound and the charge here comes from the macro reading, not from the move. Two rows, two cuts. |
| R232 rust: an unknown flag's OPERAND was taken as the scan target, so the ⟨0.32⟩ refusal marker was written into a directory the operator never named | 2026-09-06 | **CLOSED — candor-rust `3950c59`; present in PUBLISHED 0.35.0** | Found as a stray `fn:*size_of/` directory sitting untracked in the umbrella's working tree, left by an earlier mistyped command — the artifact of the bug, not a report of it. `candor-scan . --scope src` refuses correctly (exit 2, right message), but §3.3.1 ⟨0.32⟩ writes the refusal MARKER to the prefix the run WOULD have used, and `prescan_argv` resolved that target by taking the LAST bare token — which on this argv is the rejected flag's operand. **Measured with a decoy at the operand path: PRE clobbered it and wrote the marker to `src/.candor/`, creating that directory; POST leaves it intact and writes to `./.candor/`. On `A B`, PRE marked `B`, POST marks `A` — the one the refusal message actually names.** **The cause is a comment that told the truth when it was written:** `prescan_argv` said it took the last positional *"specifically to mirror this loop"*, and the main loop had since stopped letting the last positional win — it now refuses a second one. The mirror went stale and nothing re-checked it. Two rules now, both read off the main loop rather than guessed: first positional wins, and stop at a token the main loop would refuse, with known valueless flags exempt so `--include-tests .` still resolves. **Harm is bounded and worth stating exactly:** the marker only ever replaces candor's own `report.refused.json`; report and source filenames differ, so no report and no user source can be hit. It is still a write by a run that was refusing to write. | Fixed. Two tests, both RED with only `scan.rs` stashed. `cargo test -p candor-scan` 400 + 79, clippy clean. |
| R233 ts: the same misroute on a DIFFERENT argv, and my first boundary said "rust-only" | 2026-09-06 | **CLOSED — candor-ts `5377eb6`; present in PUBLISHED 0.35.0** | **The boundary error is the finding.** I measured `. --scope src` across the engines, saw ts and swift route correctly, and wrote "rust-only" into R232's commit. Sweeping ONE more argv — `--scope src`, the bad flag before any positional — showed **candor-ts making the identical misroute and clobbering the decoy**, while candor-swift writes nothing and is the shape both now match. The audit boundary had been drawn around its own trigger, which is the failure CLAUDE.md names and which I committed to a message before catching it. **ts's cause is a safety sentence that went stale when a second consumer arrived.** `preScan` deliberately keeps collecting `target` past a token the parse loop would refuse, justified in its own comment: *"policy/target only feed the input GUARDS, where over-collection can only protect a file more."* True when written. The ⟨0.32⟩ marker was later wired to the same variable, and there over-collection is a **write**, not a guard. Separated: `target` still over-collects for the guards, a new `markerTarget` stops. **And the regression test's first version passed against the UNFIXED engine** — the helper passes an absolute target as its first bare token, which latches the target and makes the defect unreachable. Only the stash-and-rerun check caught it; it now runs with `cwd` inside the fixture and the bad flag first. | Fixed. RED without the fix, green with it, two controls green throughout. `npm test` 2205 + 223 + 129 + 16 exit 0; `test:transitive-recall` and `ci/self-gate.sh` exit 0. **All four engines swept on the trigger argv: rust and ts fixed, swift and java write nothing and were never wrong.** A java DIFFERENCE was measured and is filed separately as R234 rather than asserted here. |
| R234 java: writes no ⟨0.32⟩ refusal marker on the argv path where rust and ts write one — MEASURED, cause and obligation NOT established | 2026-09-06 | **OPEN — an observation with its measurement, deliberately not stated as a finding** | Swept while fixing R232/R233. On `candor-java . --scope src` — an unknown flag AFTER a valid target — java exits 2 and writes **no marker**, where candor-rust and candor-ts both write `./.candor/report.refused.json`. **What stops this being filed as a defect is the control:** on a refusal AFTER target resolution (`. --policy /nonexistent/x.pol`) **candor-rust writes no marker either**. So the marker is not written on every refusal in the engine that has the rung most fully, and "java writes none here" is not yet evidence of a missing obligation — it may be the same bounded path, reached differently. **What would settle it:** enumerate which refusal paths §3.3.1 ⟨0.32⟩ actually binds, then check each engine against that list rather than against each other. Engine agreement is the weakest signal available and is the thing being compared here. | Not fixed, not diagnosed. Filed so the difference is not lost, and worded so a later reader does not inherit a conclusion I did not earn. |
| R235 instrument: the `coverage-gate-refresh` red has been standing since 2026-09-02 and is TWO different things — one real row and three benign upstream drifts, scored identically | 2026-09-06 | **OPEN — DIAGNOSED, not fixed. The gate is right to be red; it is the SPLIT that is wrong** | `coverage-gate drift: 5 regressed, 0 newly-uncovered` at `587275a`. The five, run down individually: **(a) `reqwest::…::RequestBuilder::send` and `reqwest::blocking::client::Client::execute` are R212** — already filed, CARDINAL SIN, pre-existing at v0.34.0, fix deliberately not attempted. Those two are a real standing red and should stay red. **(b) the three `async_nats::jetstream::context::Context::publish*` entries are UPSTREAM DRIFT, the benign cause the gate's own comment names.** Measured: at 0.50.0 `Context::publish` delegates to a new `send_publish`, which 0.35.1 did not have — a refactor across a large version jump, not a classifier loss. **The direction that matters is intact:** a downstream caller of `async_nats::Client::publish`, `jetstream::Context::publish` and `reqwest::RequestBuilder::send` is charged **`Net` on all three**, verified on a fresh consumer crate. **And the self-scan's silence on the crate's own `Client::publish` is DISCLOSED, not silent** — checked because "open by design" is not the test. Minimal repro: a method call on a FIELD whose type comes from an unread dependency is absent from `calls`, but the function carries `invisible: [dep]` and the run says on stderr *"their effects are INVISIBLE to the scan (absent from the report, NOT a claim they're pure)"*. That is R133's ruled non-gating disclosure working as decided, not a new sin. **The finding is the instrument:** R214 built `diff_manifests` precisely to separate REGRESSED (fails) from ORACLE-DROPPED (reports only), and an upstream refactor is the ORACLE-DROPPED case — yet these three scored REGRESSED and have been holding the job red beside a genuine row for four days. A red that mixes a live cardinal sin with routine version drift trains its reader to ignore it. | Not fixed. Two actions, deliberately not taken in an autonomous run because one of them MOVES A GATE: regenerate the manifest for the async_nats drift, and check why R214's split scored a refactor as REGRESSED. The reqwest pair must stay red until R212 is fixed. |
| R236 java: `Comparator` and its family are outside EVERY arm, so `xs.sort(cmpField)` AND a direct `cmp.compare(a,b)` are both silent | 2026-09-06 | **CLOSED — candor-java `3b15800`. Verified by the coordinator against a jar built at `18639a9`. The premise held and was NARROWER than the defect** | **CARDINAL SIN, java.** This is the half of R217 whose premise was right, isolated: `Comparator`, `FileFilter`, `FilenameFilter`, `PrivilegedAction` are outside `isJdkFunctionalSam`'s interface set, so **no arm charges them** — not the lambda arm, not the method-reference arm, not R217's new opaque arm. Measured POST-fix: `viaSort(List, Comparator)` ABSENT from `functions[]`, and `viaDirect(Comparator)` calling `c.compare("a","b")` **with no higher-order function anywhere** also ABSENT. **The direct-invoke cell is what makes this its own row rather than a gap in R217's:** R217's mechanism is "an argument handed to an invoking HOF", and this fires with no HOF at all. It is the same subject as R183's residual (2). | Not fixed. The remedy is widening `isJdkFunctionalSam` for **every arm together**, which is why it was not bolted onto R217's opaque arm alone. Note the measured trap: the wider `isHofFunctionalIface` tried during R217 charged `Comparator.comparing(...)` plumbing (+1,697 rows) while leaving this direct invoke silent — so the naive widening buys fabrication and does not buy this cell. **Closed, and the row understated it: the LAMBDA spelling was silent too.** There were TWO interface sets — `isFunctionalIface` gated the three arms that CHARGE a callback, `isHofFunctionalIface` (that plus Comparator/FileFilter/FilenameFilter/PrivilegedAction) gated only the two that resolve a NAMED impl. So a comparator got an edge as `new MyCmp()` and nothing in any other spelling. **`isHofFunctionalIface` is deleted**; one membership authority is now asked by every arm, and the SAM *name* comes from `samNameOf` (R191's JDK index) rather than a second hand list, which is what keeps `reversed`/`thenComparing`/`comparing` out. **That makes R183's own comment a hole read as a stance:** it reasoned *"`(a,b) -> c.compare(a,b)` stays pure, therefore charging `sorted(c::compare)` would fabricate"* — but the lambda was silent for the same reason, not by design. **Independently re-measured by me on a probe kept free of any project `Comparator` implementor** (a single one anywhere in the target puts `Comparator.compare` in CHA range and masks the decisive cells — the agent's first probe hit exactly that): `sortParam`, `sortField`, **`directParam` (no HOF at all)**, `sortLambda`, `arraysSort` all ABSENT -> `['Unknown']`; `listF` `['Fs']` -> `['Fs','Unknown']`. **All five over-charge controls unmoved** — `reversed()`, `Comparator.comparing`, a storing sink, a held-not-invoked supplier, and `xs.sort(null)`. Baseline `removeIf` unchanged. **395-jar A/B, every field, 578,461 rows: ADDED 2,077 / REMOVED 0 / CHANGED 2,814 wide, 534 `inferred`.** Instrumented 1,368 events / 734 sites; the **method-reference arm fired 0 times — safety-only on this corpus, and said in the commit rather than discovered later.** Only `overdeclared` shrank. 29 rows hand-audited from `javap` (pcollections' `KVTree` really does `invokeinterface Comparator.compare` at 7 sites). **R217's deleted null guard returns as `ProvValue.nullConst`** — dataflow, collapsing at a join, NOT `declType == null` which is also null for merges and array reads and would have gone silent on a real callback. |
| R237 java: an INVOKING higher-order function that `isInvokingHof` does not name is silent, even for an interface the set DOES cover | 2026-09-06 | **CLOSED — candor-java `d3dbc45`. Verified by the coordinator. The enumeration is a BUILD-TIME JDK SWEEP, not a longer hand list** | **CARDINAL SIN, java, and it is the cost of the allowlist R217 deliberately chose.** `Supplier` IS inside the interface set, so the gate is not the problem; the HOF is. Measured POST-fix, with a control in the same class: `Optional.orElseGet(supParam)` **ABSENT**, `Objects.requireNonNullElseGet(x, supParam)` **ABSENT**, while `List.removeIf(pParam)` in the same file discloses `['Unknown'] callback:java.util.function.Predicate.test`. Both absent methods really do invoke the supplier. | Not fixed, and the direction is the point: `isInvokingHof` is an allowlist, so every HOF it does not name fails toward SILENCE. That was the correct trade at R217 — the denylist form (`!isStoringContainerCall`) was measured at this same site historically and produced fabrication — but the residual is a live under-report and must not be left implicit in a design note. **The enumeration is the work here**, not the mechanism: what is owed is a swept list of JDK HOFs that invoke their functional argument, not another one-at-a-time addition. **Closed the way the row asked — by enumerating, not by adding names.** `generateJdkHofInvokes` sweeps the JDK with ASM's `SourceInterpreter` for methods that invoke a functional parameter's SAM, then takes a fixpoint through forwarding edges (`List.sort` -> `Arrays.sort` -> `TimSort.sort` -> `binarySort`) and handles derived CHECKCAST and identity wrappers. **1,623 `(name,desc)` entries, 412 new simple names**, validated byte-identical against an independent prototype, and UNIONED with the hand list because `Stream.map` has no body to read. Keyed by `(name,desc)` rather than owner: the collisions that allows are over-reports bounded by the interface gate, measured at 111 admitted-and-inert sites. Verified by me: `orElseGet` and `requireNonNullElseGet` ABSENT -> `['Unknown']`, with the five over-charge controls and the `removeIf` baseline unmoved. **395-jar A/B: ADDED 290 / REMOVED 0 / CHANGED 481 wide, 195 `inferred`.** The recall is one row and it is a real effect the engine had been dropping entirely: undertow's `GSSAPIAuthenticationMechanism.runGSSAPI` gains **Clock+Rand** through `Subject.doAs`. **The cost is one shape** — 221 of the 290 rows are commons-lang3 downstream of `Validate.notNull` -> `requireNonNull(obj, toSupplier(...))`, which is R217's residual (3), a call-return supplier. **One degradation the agent CLAIMED and then retracted:** forcing `jdkInvokesAnyFunctionalArg` true left the over-charge control green, because the per-argument gate holds the line independently; it reddens only when both answer true. The comment says so now. |
| R238 rust: a callback held in a STRUCT FIELD and handed to a HOF that invokes it was reported PURE — TEN spellings, and TWO independent holes | 2026-09-06 | **CLOSED — candor-rust `0bbebe5`. Verified by the coordinator against a binary built at `63fe733`. MY ROW'S MECHANISM SENTENCE WAS WRONG AND WOULD HAVE MISDIRECTED THE FIX** | **CARDINAL SIN, rust.** I filed this on the bare `fn`-pointer field; **ten spellings were silent by one mechanism**, including `Box<dyn Fn>` and a struct-bounded generic field that candor's dispatch index already knew about. Independently verified on my own fixture: 6 of 8 field spellings ABSENT -> `['Unknown'] callback:unresolved call`, the `via_param` baseline unchanged, and **all four over-charge controls unmoved** (a field held but never invoked, a non-callable sibling, a pure inline closure through the same HOF, a type with no callable field). **Gate flip, isolated and reproduced by me** over a library that defines nothing effectful and reaches the callback only through a field: `deny Unknown` **exit 0 -> exit 1**, with the parameter-only spelling exit 1 in both arms as the control. **THE CORRECTION THAT MATTERS: my row said the disclosure "hinges on `f.unresolved` and the field access path never sets it", which reads as ONE hole. There are TWO, in different files, reached by DIFFERENT routes** — (1) Pass A's struct-field arm recorded only `trait_leaves`, which sees an `Fn*` bound but is empty for a bare `fn(..)`, a callable `type` alias, `Option<fn(..)>` and every tuple-struct position; (2) `expr_is_fn_typed` answered only for a NAME, having no `Expr::Field` arm. **Every one of the 26 real-world corpus diffs came from hole (1) feeding the pre-existing `let`-binder hedge — NOT from hole (2). A fix that patched only the half my row pointed at would have passed two fixture arms and changed NOTHING across 1,509 crates.** | Fixed by routing both through `is_callable_type`/`resolve_recv_traits` — the same authorities the PARAMETER path already used, which is why that spelling always worked. Additive: the synthetic `"Fn"` hedge is written only where `trait_leaves` found nothing and never displaces the concrete entry (R177's measured lesson), and `"Fn"` names no local trait so no CHA edge and no concrete effect can come out of it. **1,509-crate A/B, wide key: ADDED 24 / REMOVED 0 / CHANGED 2 over 263,575 rows**, all 26 audited against source rather than against candor's own report — **22 are real instances in published `rustls-ffi` 0.15.x** (`unsafe extern "C"` callbacks supplied by the C caller: `CallbackReader::read`, `Verifier::verify_server_cert`, the session-store broker, …), 2 in `crossbeam-epoch`, 2 reason-channel improvements. **§E1 honesty: `R238DECL` 75,376 hits / 199 crates, but `R238FIELD` 0 hits / 0 crates — the collector half is SAFETY-ONLY over this corpus and the agent said so in the commit rather than letting the A/B imply coverage it does not have.** Gates I re-ran: 4 crates' tests (401+126+80+52+37+22), clippy, gate-equivalence, self-gate, `run_q.sh 60` and `run_macro.sh 60` both CLEAN, `baseline.sh --check` CLEAN, `incremental_equiv.sh` 120 edits byte-identical (cache schema rev22->rev23). |
| R239 ts: the `globalThis.` qualifier defeated FIVE text-keyed arms at once, three of them silently | 2026-09-06 | **CLOSED — candor-ts `f58dc0f`. Found by widening R116's sweep; NOT assigned** | **CARDINAL SIN, ts, three of the five.** Five arms keyed a builtin callee by TEXT, so the qualified spelling matched none of them. Measured in one file, `tsc` clean, on pairs differing ONLY in the qualifier — bare charged, `globalThis.`-qualified **ABSENT**: `Object.assign` -> target setter (R116), `Reflect.set`, `Object.assign` -> source getter (R115/R120), `Object.keys(process.env)`, `Object.keys(localStorage)`. Executed: identical accessor counts, and `globalThis.Object.keys(process.env)` returns the same 66 keys. | Fixed with ONE authority (`globalBuiltinCallee`) that every arm asks — R95's shape, not five parallel patches. **A/B byte-identical over 21,742 rows with 306 real reaches, so it is an OVER-CHARGE CONTROL ONLY and the agent said so rather than presenting a null result as recall evidence**; the recall evidence is the fixture. Fixed rather than filed per [[feedback-always-fix-fixable-silent]]. Revert-red: 8 rows. |
| R240 ts: `s[k] = v` never resolved a setter unless the key was a SYNTACTIC string literal — 19 of 27 spellings silent | 2026-09-06 | **CLOSED — candor-ts `1a3c36b` (a) + `0341b0f` (b). Verified by the coordinator against a build at `f58dc0f`. My row measured 2 cells; the sweep found 19** | **CARDINAL SIN, ts.** `accessorsAt` resolved an element-access key only when it was a string-literal NODE. Everything else returned "no accessor", which the caller reads as pure. **Two things were wrong:** the key's TYPE frequently pins the property set exactly — a literal union, a SINGLE literal type, a string enum, `keyof T`, `K extends keyof T` — and resolving that is reading the declaration, not guessing; and where it genuinely cannot be pinned, absence is a positive purity claim rather than a neutral answer. **Independently re-measured by me, PRE -> POST: `dynUnion`, `constLit` (`const k = "token"; s[k] = v` — the most ordinary spelling in the language, and my row did not have it), `asConst`, `enumKey`, `keyofKey`, `compound` (`s[k] += v`, which invokes BOTH accessors) all ABSENT -> disclosed; both over-charge controls unmoved.** Executed ground truth throughout: a real `fs.appendFileSync` in the setter, counted by reading the log file back. 19 silent under-reports -> 0, with 0 over-charges before or after. | **(a) resolve a pinned key, (b) disclose `Unknown` for an unpinnable one over a receiver that HAS accessors — both landed, and (b) was PRICED FIRST as instructed:** ADDED 0 / REMOVED 0 over 6,851 rows on two fresh corpora; nine npm rows gained a `reflect:accessor:dynamic-key` tag on an `Unknown` they already carried. Nothing flooded. **The audit of (b) is the part worth keeping.** The first cut priced at 0.32% / 18 rows, and auditing those ROWS rather than the count found a FABRICATION the agent had introduced: it guarded "a string key cannot name a symbol-keyed property" and **missed the mirror, one line over** — axios's `AxiosHeaders` rows armed only by `get [Symbol.toStringTag]()` reached as `self[key]`, and all 18 mongoose rows tracing to one `ObjectId.prototype[objectIdSymbol]` with a `unique symbol` key armed by bson's string-named `get id()`. An audit boundary drawn around its own trigger, inside the fix for that very class. Both guards now run both ways with positive twins and degradation checks. **Branch reach, since an unchanged row is not evidence the code ran:** the type-pinning branch resolved 432 of 1,425 dynamic-key accesses on TS source and 1,456 of 5,566 on npm. Real-world recall: got v14.4.1 `options.ts:1080` `this[key as keyof Options] = value` — `Options.merge` went from 5 call edges to 96, including 43 accessors that are themselves `Unknown`. **`inferred` did not move on that row, so an `inferred`-keyed A/B would have reported nothing.** `npm test` 2485 + 223 + 129 + 16, transitive-recall, self-gate, assert-audit all exit 0. |
| R241 ts: six more `lib.dom` host properties are silently pure — `document.cookie`, `location.href=`, `location.assign`, `indexedDB.open`, `caches.open`, `navigator.storage.getDirectory` | 2026-09-06 | **OPEN — ANALYSIS-ONLY, and labelled as such rather than upgraded** | Found while closing R113, which named the first two as analysis-only; four more were found by sweeping the surface rather than the instance. `location.assign` off a qualified global appears **100 times in the npm corpus**. | Not fixed **and not charged**, because there is no browser on this box to execute them and this family's rule is that a row is not evidence until it is falsified against executed ground truth. The agent could have charged them on the strength of the pattern and did not, which is the right call: an upgrade justified by resemblance to R113 is exactly the narrowing-on-an-unproven-premise shape [[candor-denylist-over-allowlist]] forbids. Owed a browser-executed fixture first. |
| R242 instrument: corpus trees under `/private/tmp` lose their FILES while keeping their DIRECTORIES, and an A/B over the skeleton reports "0 changed" — which reads as a clean result | 2026-09-06 | **HALF CLOSED — the guard landed 2026-09-06; the CAUSE is now identified (circumstantially) and the `$TMPDIR` default is a one-line decision. Four swift corpus trees are gutted RIGHT NOW** | **Two independent corpora, both under `/private/tmp`, both hollowed.** (a) `scratchpad/swift-escape/corpus`: Alamofire 0 `.swift`, Kingfisher 0, GRDB 1, `.git` stripped to `hooks/`+`info/`; the only surviving files are `report.refused.json` markers a later scan wrote. (b) The shared ts roster at `/private/tmp/candor-corpus/src` — **the `src/` directory no longer exists at all**, and the six trees under it contributed **0 rows** to a live A/B. **THE DANGER IS THE DIRECTION IT FAILS IN.** A hollowed tree does not error the harness: candor refuses it at exit 2, it contributes zero rows, and a differential over it prints **ADDED 0 / REMOVED 0 / CHANGED 0** — indistinguishable from "my change is safely inert". It has ALREADY produced a false claim: a first R136 A/B was written up as "six real TypeScript repos" and had measured nothing. The agent caught it, amended its own commit and cloned 12 real trees instead — which is where the socket.io finding came from. **Earlier rounds citing those paths were not necessarily so lucky.** **I did NOT establish a cause and am not asserting one.** A tmp-cleaner is the obvious suspect, but mtimes under that tree are archive-extraction artifacts (1980, 1985, 2002) and cannot support the inference, and `/etc/defaults/periodic.conf` had no matching entry on this box. | **(1) IS DONE — `bin/corpus.sh` `d61ba67`.** Both acquisition helpers treated EXISTENCE as acquisition (`[ -d ] && return 0`, `[ -f ] && return 0`), so a hollowed entry was "already got" forever and nothing ever said so. The roster now asserts CONTENT before the round compares anything, and a hollow entry is FATAL rather than quietly absent. **It does not delete and re-clone** — a corpus tree may be someone's evidence, and reclaiming one mid-round has destroyed a measurement here before — it names the path and stops. **Calibrated in BOTH directions, because a guard nobody has watched fail is not a guard:** it fires on the three actually-gutted trees (Alamofire, Kingfisher, GRDB), on an empty directory, on a dirs-only tree with `.git` stripped to `hooks/`+`info/` (the real shape), on files with no `.git`, and on a jar present but truncated to 4 bytes with correct `PK` magic; it is SILENT on a real checkout and on two intact corpus trees. End-to-end: a fully real 16-entry roster reaches `[scan]`; gut ONE entry to 135 directories / 0 files and the run names it, exits 2, and **never reaches `[scan]`**. **(2) IS NOT DONE:** corpus checkouts still default under `$TMPDIR`, and relocating them changes where every past artifact lives — not an autonomous-run decision. Any past number citing `candor-corpus/src` should be treated as unreproducible until re-measured. Related: [[feedback-evidence-dirs-are-sacred]]. **CAUSE, and my earlier check used the wrong instrument.** I grepped `/etc/defaults/periodic.conf` and found nothing. On macOS the cleaner is **launchd**: `/System/Library/LaunchDaemons/com.apple.bsd.dirhelper.plist` runs `/usr/libexec/dirhelper` daily at 03:35 with **`CLEAN_FILES_OLDER_THAN_DAYS=3`** over the per-user `/var/folders/…/T` tree — which is `$TMPDIR`, which is `corpus.sh`'s default. **The variable name says FILES, which is exactly the observed shape: files gone, directories remain.** Deliberately NOT executed (running a cleaner mid-session has destroyed evidence here before), so this is strong circumstantial, not proven. The 1980/1985 mtimes are not an objection — an archive-extracted file is instantly older than three days. **Decision cost, measured: migration is FREE today.** `$TMPDIR/candor-corpus` does not currently exist, so nothing would be orphaned by defaulting to `$HOME/.candor/corpus`; the trade is that a home-dir corpus is never auto-reclaimed, making `disk-guard.sh` the only thing between it and a full disk. The agent recommended moving and did not, per instruction. **Still live: four swift corpus trees in the scratchpad are gutted right now — `swift-nio` is 164 directories and 0 files.** |
| R243 rust AND swift: a callable field whose constraint sits on the IMPL/EXTENSION rather than on the type declaration is still silent — a cross-engine vein | 2026-09-06 | **SWIFT HALF CLOSED — candor-swift `0ff4c9d`. Verified by the coordinator, WHO ALSO FOUND A RESIDUAL (R256). RUST HALF STILL OPEN. AND MY FRAMING OF THE DISCRIMINATOR WAS WRONG** | **CARDINAL SIN, two engines, one shape.** Both build their callable-field index from the DECLARATION's own generics, so a bound expressed elsewhere is invisible. **rust:** `struct ForEachConsumer<'f, F> { op: &'f F }` with `F: Fn(T)` on the impl block — rayon `src/iter/for_each.rs:55`, `iter.into_iter().for_each(self.op)`. Reproduced on a compiling fixture: the struct-bound form is charged after R238, the impl-bound form is still silent. **swift:** `struct Gen<F> { let op: F }` with the callable-ness constrained by `extension Gen where F == (Int) -> Bool`. Measured: `Gen.run`, which does `v.filter(op)`, is **ABSENT from `functions[]`**, while the same engine correctly discloses a directly-typed closure property (`dispatch:Direct.cb`) and a protocol-typed one (`dispatch:Pred.test`). Both over-charge controls (a field held but never invoked; a pure inline closure through the same HOF) stay absent. **This corrects R238's own text, which credited candor-swift with disclosing "all three opaque spellings" and therefore being CLEAN.** It is clean on the three spellings that sweep used and not on this one — the sweep asked the wrong question, and the rust agent is the one who said so. The question that discriminates is not *how* the callback is held, it is **WHERE THE CONSTRAINT LIVES**. | Not fixed in either engine. Deliberately: charging an UNBOUNDED generic field is the +1,697-row over-charge shape candor-java built, measured and rejected during R217, so the remedy has to find the constraint rather than assume the field is callable. **candor-ts and candor-java are UNSWEPT on this specific question** — ask them "is the bound on the type or on the extension/impl?", not the generic opaque-callback question. **MY ROW SAID THE DISCRIMINATOR IS WHERE THE CONSTRAINT LIVES. IN SWIFT IT IS NOT.** Measured by me on one file, PRE and POST: `extension Gen where F: RunnerC` — a bound on the EXTENSION, exactly where I placed the blame — is disclosed **`['Unknown'] dispatch:RunnerC.go` in BOTH arms**, identically to `struct GenD<F: RunnerC>` on the declaration. **The discriminator is the REQUIREMENT KIND:** `recordTypeGenerics` read `.conformanceRequirement` and dropped `.sameTypeRequirement` whole. The two axes look identical only because Swift FORBIDS `F == <concrete>` on a type's own clause, so it can only ever appear on an extension or a member. **So the question to translate to candor-ts and candor-java is `==` vs `:`, not extension vs impl** — and the rust half (rayon's impl-block bound) needs re-examining against that axis too. Five silent spellings, not one: extension-level, via a function `typealias`, on the METHOD, direct `op()`, and — not in my row — the CONCRETE side, `where F == Wiper { op.doIt() }`, also absent over a real deletion. Verified by me: `where F == Wiper` ABSENT -> `['Fs']`, and the agent's own fixture ABSENT -> `['Unknown'] dispatch:Gen.op`. **A/B 16 trees / 4,401 files / 15,058 rows: byte-identical, ADDED 0 / REMOVED 0 / CHANGED 0.** Per-branch reach: fn-type requirement **0 hits**, nominal-type 12 hits in 6 packages. **`where X == <function type>` occurs ZERO times in those 4,401 files — the zero is rarity, not a dead instrument**, and the agent hunted for recall before saying so. **Two defects in its own change were caught by the A/B rather than by review** (a member-level `where Value == NewValue?` recording a bound that named the METHOD's generic parameter; member-level conformance requirements read type-wide moving 4 real rows) — both now gated on `memberLevel`. **And its non-function control had to be rewritten because it survived its own degradation.** **Correction to my own over-charge control:** I had recorded that "a pure inline closure through the same HOF stays absent" holds. That was an expectation about the BROKEN state — post-fix it correctly earns the same `dispatch:` hedge a directly-typed closure property already gets. |
| R244 rust: `trait_fields` still has no `Fields::Unnamed` arm for the GENERAL dispatch question | 2026-09-06 | **OPEN — reported by the R238 agent as a bound it deliberately did not cross** | R238 taught the callable-field index about tuple-struct positions for the CALLABLE case only. The general dispatch question — `struct T(Box<dyn Handler>)`, a trait object in an unnamed field, invoked through its trait method — still has no arm at all. | Not fixed, and the reason is worth keeping: widening it changes DISPATCH rather than adding a hedge, so it can produce concrete effects and CHA edges, and it needs its own A/B and over-charge control. Fold it into whatever cut next touches `trait_fields`. |
| R245 ts: a UNION-typed receiver charged whichever accessor arm the checker listed FIRST — the same program answers differently depending on the order of the union's arms | 2026-09-06 | **CLOSED — candor-ts `31ca04e`. Found while fixing R240 and it has NOTHING to do with dynamic keys; reproduced and REFINED by the coordinator** | **CARDINAL SIN, ts, plain dot access with a literal key.** `accessorFromSym` used `.find` over the property symbol's declarations, so a union receiver contributed exactly one arm. `class Aa { set token(v){} }` (pure) beside `class Bb { set token(v){ appendFileSync(...) } }` (effectful), with `function u1(x: Aa \| Bb) { x.token = "v" }`: executed, `u1(new Bb())` performs 1 real write; pre-fix `u1` was **ABSENT** and `deny Fs` exited 0. **The agent reported the trigger as the order the two CLASSES are written in. Measured, it is not — it is the order of the arms in the UNION ANNOTATION**, which is what the checker's declaration list follows. I could not reproduce it by swapping the class declarations (ABSENT both ways); swapping the annotation reproduces it exactly: `x: Aa \| Bb` -> **ABSENT**, `x: Bb \| Aa` -> `['Unknown']` with ONE call edge. Post-fix both spellings charge every declaration of the wanted kind — two call edges either way. | Fixed. Corpus-confirmed on shipped code: zx `src/core.ts:268` `($.input as ProcessPromise \| ProcessOutput)?.stdout`, and hono `jsx/dom/render.ts:312`. **Reach: 2 fires in 20,812 accessor resolutions on TS source and 0 in ~68,000 on npm — that arm is SAFETY-ONLY over this corpus and the agent recorded it as such rather than letting the A/B imply coverage.** |
| R246 ts: `obj[k] = v` on a receiver typed `any` is still silent — which is nearly every receiver in an `--allow-js` tree | 2026-09-06 | **OPEN — the measured residual of R240(b), and confirmed independently on my own fixture** | R240(b) gates its disclosure on the receiver's DECLARED property set, so a receiver with no declarations gets nothing. Measured: **0 fires across 4,110 unpinned dynamic-key sites in 8 npm packages.** My own probe reproduces it — `(s as any)[k] = "x"` stays ABSENT after the fix while every pinned spelling on the same class is disclosed. | Not fixed, and the reason it is cheap is the reason it is incomplete: gating on declared properties is exactly what kept (b)'s price at zero. Closing it means hedging every `obj[k] = v` on an untyped receiver, which is unpriced and is the flood shape. **This is the half of R240 that is still open, and `--allow-js` trees are where it bites.** |
| R247 ts: the SAME ECMAScript operation answered two ways — `Reflect.set(s,k,v)` over-charged every setter while `s[k]=v` disclosed `Unknown` | 2026-09-06 | **CLOSED — candor-ts `f2d30a8`, converged on DISCLOSURE. Verified by the coordinator. MY OWN PRICING WAS A GUARANTEED ZERO AND THE AGENT SAID SO** | Where the copied key set is unprovable, `enumerateTargetSetters` now discloses instead of charging every setter. **Verified by me at `0341b0f` -> HEAD on a class with three effectful setters: `Reflect.set` and `Object.assign` go from `['Fs']` with THREE fabricated call edges to `['Unknown']` with ZERO; `s[k]=v` was already `['Unknown']`; and the PROVABLE control `Object.assign(o,{a:"1"})` keeps `['Fs']` with its ONE real named edge in both arms.** All three spellings now agree, which is the row's whole claim. **Two tags, and the split is measured rather than cosmetic:** `Reflect.set` takes R240(b)'s own `reflect:accessor:dynamic-key` (one runtime key, at most one setter), `Object.assign` takes `reflect:accessor:dynamic-keyset` (an unprovable key SET, 0..n setters — executed 2). `reasonClass` is prefix-based so both land in `reflect` and no policy needs to know. **The symbol guard deliberately LIFTS for `Object.assign` and that is executed, not argued:** `Object.assign` copies own enumerable string AND symbol keys, so nothing on the target is provably out of reach — sharing R240(b)'s string-key exclusion would have silenced a real setter (`Object.assign(o,{a,[TAG]:9})` invokes a symbol-named setter, measured 1). R240(b)'s two-way test is now ONE helper called by both new sites and by R240(b) itself, which is §G: two paths answering one question is how these spellings drifted in the first place. | **CORRECTION TO MY OWN EVIDENCE, and it is the second time in this round I made this exact error.** I priced this at "316 unprovable-key sites reached, 0 rows lose an effect" and concluded *"every effect that branch charges is either pure or already carried by another path"*. **That conclusion is wrong.** My counter was on the OUTER branch; the agent instrumented the CHARGE site inside it and got **528 entries and 0 charges** across 28 entries / 20,024 rows — no corpus target declares a setter under an unprovable key, so there was nothing to withdraw and my zero was guaranteed rather than informative. **A reach count at the wrong granularity is not a reach count.** The decision was right; my stated reason for it was hollow. **The agent then priced it the way it should have been priced — a RECALL harness:** got 14.4.1's own unmodified `Options` (48 real setters) plus a three-line consumer. `Object.assign(o, userOpts)` and `Reflect.set(o,k,v)` each shed **48 fabricated call edges** — 96 over one real class — with **0 rows losing an inferred effect**, while `Object.assign(o,{url})` keeps its single `Options.set url` edge. Corpus A/B stays ADDED 0 / REMOVED 0 / CHANGED 2 on a wide key, and is labelled **SAFETY-ONLY in the commit and CHANGELOG at the time of writing**. Seven guard degradations each redden a named row; the extraction is shown load-bearing because degrading `keyCouldNameAccessor` reddens R240(b)'s row too. Revert-red 27 assertions. **It also corrected one of its OWN controls:** it had asserted `pure` fires on a disclosure — it does not, `pure` forbids effects and deliberately not `Unknown`, so that assertion would have failed against R240(b)'s long-standing correct answer. `npm test` 2521/0, transitive-recall, self-gate, assert-audit all 0. |
| R248 java: the SLOT-vs-VALUE operand-stack index is REACHABLE — **every periodic-scheduling hand-off in the JVM is silent-pure**, and it is PUBLISHED | 2026-09-06 | **CLOSED — candor-java `0c1d437`, with R274. Verified by the coordinator on his own fixtures against the PUBLISHED v0.35.0 jar. STILL PUBLISH-AFFECTING: 0.34.0 and 0.35.0 carry it** | **CARDINAL SIN, java, shipped.** Five helpers locate a value with `idx = getStackSize() - Σ Type.getSize()`. ASM's `Frame` holds **one entry per value**; `getSize()` counts a `long`/`double` as two. `handoffTaskArg` reads **arg0** of every executor/timer hand-off, so **one** size-2 argument after the task makes it return the RECEIVER and **two** make `idx` negative — it returns `null` and `opaqueTaskHandoff` discloses nothing at all. **Measured by me on `candor-java-0.35.0-all.jar` fetched from `gh release download v0.35.0`:** `EXEC.scheduleAtFixedRate(r,0L,100000L,MILLISECONDS)` **ABSENT**, `EXEC.scheduleWithFixedDelay(...)` **ABSENT**, `FIELD_TIMER.schedule(t,1L,100000L)` **ABSENT**, `FIELD_TIMER.scheduleAtFixedRate(t,1L,100000L)` **ABSENT** — while the same executor with the same opaque `Runnable` through `submit(r)` and `execute(r)` discloses `task-handoff:…`. **Over a tree holding ONLY the silent method, all four policy forms exit 0: `deny Unknown`, `deny Fs`, `deny Fs Unknown`, `pure`.** The reviewer's ground truth is executed — every callback really wrote its file — and falsified against a jar built at the range base, identical. **A FIFTH CORRECTION, from the 2026-09-07 plan review, and it is this row drawing its boundary around its own trigger — the error the row's own round kept logging.** I wrote "five helpers" and named them all in `Candor.java`. **The `slots += Type.getSize()`-then-index-the-`Frame` pattern is in THREE FILES:** `Candor.java` (8 occurrences), **`Interp.java:27, 36, 460`** and **`Cha.java:574`** — verified by grep. Two of those three are unmeasured for fail direction: `Cha.java:574` feeds `fieldLambdaBindings` from a receiver, so a `long`-bearing descriptor there loses a binding **silently**; `Interp.java:36` over-reads the stack for a taint check, which is a **fabrication** candidate. **The one-authority fix must cover all three files, not the one the exploit happened to land in.** **FOUR CORRECTIONS TO THIS ROW'S OWN TEXT, which is why it sat LATENT:** (1) it said *"no public JDK API was found with a functional argument positioned before a long/double"* — **`java.util.Timer.schedule(TimerTask,long)`, `scheduleAtFixedRate(TimerTask,long,long)` and the whole `ScheduledExecutorService` scheduling surface are exactly that**, and are among the most-used APIs on the platform; (2) it said every argument-reading sink has a primitive-free descriptor — `handoffTaskArg` and `receiverProv` are argument-position readers with `long`-bearing descriptors; (3) it named THREE helpers, there are **FIVE** (`callArgs`, `indyArgs`, `receiverProv`, `argAt`, `handoffTaskArg`) — **and `argAt`'s javadoc asserts it is "accounting for long/double double-slots", a safety sentence written beside code that does the opposite**; (4) *"the last argument is correct by cancellation"* is true but useless — **the rule that PREDICTS the defect is: argument k is correct iff no `long`/`double` sits at position ≥ k, and the RECEIVER is correct iff the descriptor holds no `long`/`double` at all.** Written that way the affected surface is enumerable by a descriptor grep instead of guessed at. | Not yet fixed. **Fix once in one helper (count values, do not sum sizes) and delete the other four copies.** Affected surface is exactly `Timer`'s schedule family and `ScheduledExecutorService.schedule`/`scheduleAtFixedRate`/`scheduleWithFixedDelay`; `submit`/`execute`/`CompletableFuture.*Async`/`new Thread(r)` are correct because their descriptors are primitive-free. **A LATENT SECOND BLINDNESS ON THE SAME SITE, analysis-only:** `opaqueFunctionalToHof`'s early return is justified by a comment claiming `opaqueTaskHandoff` "has already disclosed for this exact value" — false whenever the descriptor carries a `long`, because the two arms read different values; unreachable today only because `TimerTask` is not a functional interface. **PUBLISH-AFFECTING: this is in 0.34.0 and 0.35.0 and is Tom's call whether it warrants an advisory.** **Closed by one authority, and the row UNDERSTATED the defect in two ways the matrix found.** Verified by me, release jar -> new build: all four scheduling cells ABSENT -> `['Unknown']` with `submit`/`execute` unchanged, and `deny Unknown` over a class holding only the silent method **exit 0 -> exit 1**. See R274 for the full grid, the fabrication arm, and the two grep hits that are CORRECT and would have been broken by the sweep my row implied. |
| R249 java: a MISSING JDK index resource degrades the engine into silent under-reporting, with no disclosure of any kind | 2026-09-06 | **GATE HALF CLOSED — candor-java `a3bbcdb`, falsified in both directions by the coordinator. DISCLOSURE HALF STILL OPEN. And MY OWN EVIDENCE FOR ONE OF THE THREE RESOURCES WAS STRUCTURALLY INCAPABLE OF SHOWING ANYTHING** | **Measured by stripping each resource from the shipped fat jar and re-scanning the same target.** Removing `candor/jdk-hof-invokes.idx.gz`: exit **0**, **nothing on stderr**, and `Optional.orElseGet(supParam)` / `Objects.requireNonNullElseGet` fall back from `['Unknown']` to **ABSENT** — the R237 cells silently reopen. Removing `jdk-sams.idx.gz` or `jdk-supertypes.idx.gz`: also exit 0, also no message. **Zero stderr lines mentioning an index, a resource, or anything missing, in all three cases.** This is the family's own cardinal-sin shape aimed at its own resource loading: SPEC's whole posture is that a scan DISCLOSES what it could not read (`unanalyzed`, `invisible`, `incomplete`), and an unreadable classifier index is exactly that kind of gap. It says nothing. **It is operationally live, not hypothetical.** The native image bundles these by explicit `-H:IncludeResources=` lines in `build.gradle.kts` (`jdk-supertypes` :126, `jdk-sams` :131, `jdk-hof-invokes` :136). A mistyped or dropped line yields a native binary that silently under-reports relative to the jar, and **`nativeCompile` cannot run on this box (no GraalVM), so jar/native parity for the newest of the three is UNVERIFIED locally** — the agent flagged that itself. | Not fixed. Two things owed: **(1) loading an index must FAIL LOUD or disclose** — at minimum a stderr advisory, and the honest form is an envelope hedge, since the report currently makes a full-confidence purity claim over a classifier that was never loaded; **(2) THE PARITY GATE ALREADY EXISTS AND IS MEASURED BLIND TO ALL THREE RESOURCES.** `native.yml` compares the native report against the jar report over the WHOLE envelope, scanning candor's own `build/classes/java/main`, and it carries a non-vacuousness control (the jar leg must find >=100 functions / >=500 analyzed). That control passes, so the gate looks healthy. **Measured by stripping each resource from the jar and re-scanning the parity target: 610 functions / 1,397 analyzed BOTH ways, 0 rows differing, `analyzed` identical — for `jdk-hof-invokes`, `jdk-sams` AND `jdk-supertypes` alike.** The gate that exists to prove the native binary behaves like the jar cannot see any of the three bundled classifier indexes go missing. This is [[candor-oracle-disclosure-recall]] exactly: the gate was never proven able to fail on this class, and its non-vacuousness control checks that the SCAN found something, not that the RESOURCE was exercised. So the remedy is cheap and specific: give the parity target a fixture that each index demonstrably changes (a `Comparator` sort, an `orElseGet`, a supertype walk), or have the step assert resource PRESENCE in the native binary directly — the envelope comparison alone will never reach it. Two of the three resources predate R237, so this is not that row's defect — it is the class R237 made visible. **CORRECTION TO THIS ROW'S OWN EVIDENCE, and it is the exact error this project keeps logging.** I concluded "blind to all three" from three jar-strip experiments. **The `jdk-supertypes` one was a guaranteed no-op:** `Cha.JdkSupers.MAP` is consulted only inside `if (IN_NATIVE_IMAGE)` (`Cha.java:230`), so on the JVM that index is never read and stripping it from a jar cannot move anything. I read a null from a branch that cannot fire as evidence of blindness — **a null result from a broken instrument is indistinguishable from a real one**, which is the rule I had put in the briefs for the agents. The `jdk-sams` and `jdk-hof-invokes` arms were valid (both are read on both artifacts, and the hof cells demonstrably flipped). The agent re-measured supertypes properly, in a worktree patching `Cha.externalSupersSplit` to force the ClassReader failure that is the native image's real condition: the existing parity target is byte-identical with and without the index. **Same conclusion, on evidence that can carry it.** **GATE HALF DONE — two gates, because the two triggers have different reach.** (1) `./gradlew verifyNativeImageResources` requires a BIJECTION between the resources `processResources` actually produced and the `-H:IncludeResources` patterns read OFF THE `graalvmNative` EXTENSION rather than a second hand-written copy; needs no GraalVM, so it catches a dropped line for all three locally in seconds. (2) `src/nativeParity` + `ci/native-parity.py` + `ci/native-parity-selftest.sh` — a second parity target whose named rows each index changes, asserted on BOTH legs so a failure names the missing resource instead of leaving absent rows to interpret; the verdict moved out of the workflow heredoc into a file so it can be attacked with two JSON documents. The original `main` target is untouched — it caught the ⟨0.32⟩ regression. **Falsified by me, not taken on report:** baseline GREEN; delete the `jdk-sams` `IncludeResources` line -> **RED naming `candor/jdk-sams.idx.gz`**; restore -> GREEN with a clean tree. The agent additionally showed RED on each of four deleted lines, on a pattern matching nothing (the other direction), on a deleted resource, on two stripped jars, and on a simulated native leg without supertypes — with a green control that runs first. **It also rejected a check that would have been unfalsifiable:** grepping the binary for the resource NAME, since the `getResourceAsStream("/candor/jdk-sams.idx.gz")` string literal is present whether or not the resource is bundled. And **its own first falsification attempt passed four times while broken** (a `grep -v` whose escaping never matched, so no line was ever removed) — caught only because the gate still reported the full pattern count. **A fixture choice worth keeping:** `IntSupplier::getAsInt` is NOT sensitive to `jdk-sams` (`isJdkFunctionalSam` short-circuits every `java/util/function/` owner), and `Comparator` is one of 18 entries in `SAM_OF` consulted before the index; `java.lang.Iterable` is in neither, which is why it is the fixture. **STILL OPEN: owed item (1)** — a missing index must fail loud or hedge the envelope. Untouched by design, and note the shape before starting it: **all three loaders already carry a comment explaining why degrading quietly is acceptable, and each was written by the change that needed it to be true** ([[feedback-documented-limitation-is-not-measured]]). |
| R250 all engines: does any OTHER engine bundle a data file whose absence changes classification, with no gate on the bundling? | 2026-09-06 | **OPEN — the QUESTION, asked deliberately rather than the defect assumed; UNSWEPT in three engines** | R249 is candor-java's instance of a shape that is not java-specific: a shipped data file the classifier consults, an artifact-packaging declaration that can silently omit it, and a gate that compares REPORTS rather than checking the file arrived. **Ask each engine the question, not the java defect** — the packaging mechanism differs and so will the failure: **candor-ts** ships from npm and its contents are governed by `files` in `package.json` / `.npmignore`, which is the closest analogue to `-H:IncludeResources` in the family; **candor-swift** declares SPM `resources:`; **candor-rust** may `include_bytes!` (compile-time, and therefore structurally immune) or read at runtime, and which one it does is the whole answer. The R249 agent raised this and correctly did not touch candor-ts, which another agent owned at the time. | Not swept. The test for each is the one R249 turned on: **can the engine's own packaging gate FAIL if the file is dropped** — not whether the file is present today. A gate that has never been watched fail is not evidence. |
| R251 ts: `Reflect.get(t, k)` has NO ARM AT ALL, so an effectful getter reached through it is silent in every spelling | 2026-09-06 | **CLOSED — candor-ts `6ce80f8`. Verified by the coordinator against a build at `f2d30a8`. Premise confirmed exactly as written** | **CARDINAL SIN, ts.** The exact mirror of the `Reflect.set` arm R116 added, and it was never written. Executed: `Reflect.get(new Src(), "token")` invokes a real file-writing getter **once**; the caller is **ABSENT from `functions[]`**; over a tree containing nothing else, `deny Fs only.go` exits **0** and `pure only.go` exits **0**, with both scopes binding (no "matched NO function" warning, so the zero is a verdict and not an unbound rule). **It is silent for the LITERAL-key spelling too**, so this is the whole builtin missing rather than the unprovable-key case — a different and simpler defect than R240/R247. | Not fixed. Small and well-shaped: it is R116's arm written for the other direction, and the getter side needs no `keys` parameter because a `Reflect.get` names exactly one property. Owed its own A/B and an over-charge control (a data property, and a getter that is never read). **Closed by parameterising rather than duplicating.** `enumerateTargetSetters` became `enumerateTargetAccessors(owner, target, keys, kind, unprovable)` — one body answers both directions and the key-set expression is shared between the get and set call sites, because a private `enumerateTargetGetters` would be the exact drift R247 was filed to close (§G). **No `classBodiedGetter` exclusion here, and that is measured:** `Reflect.get` is a LOOKUP, not a copy, so a class instance really does invoke (executed 1). **Verified by me: `Reflect.get(s,"token")` ABSENT -> `['Fs']` through a real edge; `Reflect.get(s,k)` ABSENT -> `['Unknown']` + `reflect:accessor:dynamic-key`.** **The widened sweep says it is ONE NAME, not a family:** `Reflect.has`, `ownKeys`, `deleteProperty`, `defineProperty`, `getOwnPropertyDescriptor`, `getPrototypeOf` all invoke **0** times (executed, literal and class); `Reflect.apply`/`Reflect.construct` DO invoke and were already covered — asserted in the suite rather than assumed. **Corpus A/B, wide key, 172 roster entries / 14,977 rows, 149 asserted non-empty BEFORE comparing (R242): ADDED 0 / REMOVED 0 / CHANGED 1** — node-fetch 3.3.2's `Headers` constructor, a Proxy `get` trap ending in `Reflect.get(target,p,receiver)` over a receiver declaring `get [Symbol.toStringTag]()`, audited in full from package source, gate-neutral. **On a narrow `inferred`-only key the same A/B prints CHANGED 0** — the brief's warning, demonstrated live. **Reach counted at the CHARGE site, not the enclosing branch:** callsites 3, pinned-charge 0, disclose-charge 1 — and that 1 IS the changed row, so the diff is accounted for rather than quiet. |
| R252 ts: `Object.entries` / `Object.values` / `JSON.stringify` over an OBJECT LITERAL with an effectful getter are all silent — and R115's comment asserts the opposite is correct | 2026-09-06 | **CLOSED — candor-ts `9a0cfd9`. Verified by the coordinator. MY ROW TEXT NAMED THE WRONG GATE AND THE AGENT CORRECTED IT** | **CARDINAL SIN, ts.** Executed: each of the three invokes the literal's getter **once**; all three callers are **ABSENT from `functions[]`**; `deny Fs` and `pure` scoped to the three callers both exit **0**. **The interesting half is why it survived.** R115's comment asserts *"`JSON.stringify(c)` and `Object.entries(c)` STAYING PURE IS CORRECT ... (executed: 0 invocations on a class instance)"*. That is **true for a class instance** — a prototype accessor is non-enumerable and genuinely is not visited — and false for an OBJECT LITERAL, whose getter is an own enumerable property. **The comment measured the case it was handed and drew its boundary there**, then read as a general ruling to everyone after. Same shape as R232/R233 and R240: a safety sentence that is exactly true of one spelling. | Not fixed. The fix must keep R115's class-instance case ABSENT — that answer is correct and is the over-charge control. Owed its own cut. **Closed by routing the four readers THROUGH `enumerateGetters`, deliberately**, so `classBodiedGetter` — which IS R115 — preserves the correct class-instance answer by construction rather than by a second guard that could drift. **CORRECTION TO THIS ROW: I wrote that the fix should stop `deny Fs`/`pure` exiting 0. It cannot, and no fix could.** An object-literal accessor is never minted as a UNIT, so `recordAccessorHit` has nothing to edge into and can only disclose `Unknown` — which is exactly what `{...lit}`, `Object.assign({}, lit)` and `lit.token` have always answered over the same literal. The achievable close is silence -> `Unknown[reflect]`, and the agent asserted that as an IDENTITY with those three existing spellings rather than as two lists that happen to agree. **It caught this in calibration and said so: a `deny Fs` framing would have been an instrument that could not fail.** **Verified by me at `f2d30a8` -> HEAD: `Object.entries`, `Object.values` and `JSON.stringify` over a literal all ABSENT -> `['Unknown']`, while the CLASS-INSTANCE control stays ABSENT** (R115's answer is correct and is the over-charge control), as do a data-property literal and `Object.keys`. Degrading `classBodiedGetter` to `return false` reddens exactly those five named rows. **Corpus A/B ADDED 0 / REMOVED 0 / CHANGED 0, and that zero is labelled SAFETY-ONLY in the commit and CHANGELOG at the time of writing.** It instrumented WHY rather than leaving the zero bare: 342 callsites reached, **0** at the charge site, because across those sites the argument types carried **2,621 properties and not one declared a getter of any kind** — so R115's exclusion was doing no work; there was nothing to exclude. **Priced on a RECALL harness instead**, five real published literals vendored verbatim (axios's fetch feature-probe getter, mongoClient's five, zod, hono): 7 caller rows ABSENT -> disclosed, **16 accessor names disclosed, exactly the 16 real getters those literals declare**. **Residuals pinned as tests rather than described:** `JSON.stringify({a: lit})` / `[lit]` / `structuredClone({a: lit})` invoke one level down (executed 1 each) and still report nothing; `console.log("%j", o)` and `util.inspect(o,{getters:true})` invoke (executed 1 each) on an argument no arm reads; `Object.assign`'s SOURCE arm is still text-keyed and reachable by a project's own `const Object = {...}` (pre-existing, not inherited by the new arms). **And a coverage gap in R116/R247's OWN block:** degrading the receiver-declares-an-accessor test reddened only the new R251 row, because no R116/R247 fixture has a target declaring a DATA property but no setter — their controls use `{}` and `Record`, which have no properties at all. |
| R253 ts: the `<module>` initializer is charged an effect it never performs, because an object-literal accessor is never minted as a unit | 2026-09-06 | **OPEN — FABRICATION, executed; found and reported by the R251/R252 agent, verified by the coordinator** | Measured: a file whose entire top level is `export const lit = { get token(){ readFileSync(...) } }` reports **`src.m.<module>: ['Fs']`**. Executed, the getter is invoked **0** times at module load — defining an accessor does not call it. The literal's accessor has no unit of its own, so its body's effect walks up `enclosing()` and lands on the module initializer. **One missing unit causes two problems in OPPOSITE directions**, which is why this is worth a row rather than a note: this fabrication, AND R252's inability to propagate a real `Fs` edge — it can only ever disclose `Unknown`, which is the correction R252's own row carries. | Not fixed. Pre-existing; introduced by neither R251 nor R252. The fix is to mint object-literal accessors as units, which closes both directions at once — and it is a units change, so it needs its own A/B and an over-charge control on a literal whose getter is genuinely never read. |
| R254 ts: R247's split RECURS one spelling over — with a PINNED key, `s[k]` answers `Fs` and `Reflect.get`/`Reflect.set` answer `Unknown` | 2026-09-06 | **OPEN — measured by the R251/R252 agent, reproduced by the coordinator. Fails in the DISCLOSE direction, so a precision gap, not a sin** | R247 converged the three spellings for an UNPROVABLE key. The PROVABLE case still splits, because `Reflect.get`/`Reflect.set` do not consult R240(a)'s `keyLiteralNames`. Verified by me with `const k = "t" as const`: `s[k] = "1"` -> **`['Fs']`**, `Reflect.set(s,k,"1")` -> **`['Unknown']`**; `s[g]` -> **`['Fs']`**, `Reflect.get(s,g)` -> **`['Unknown']`**. **Same operation, same pinned key, two answers — a scoped `deny Fs` catches one and not the other, which is verbatim R247's own complaint.** | Not fixed, and the agent was right not to fold it in: closing it WITHDRAWS disclosures (`Unknown` -> a precise `Fs` edge), so it needs its own pricing exactly as R247 did. It kept the get/set mirror exact instead and documented the residual in `scan.mjs`. **The lesson R247 was filed for did not take on the first pass: converging two spellings of one operation is not done until the PROVABLE and UNPROVABLE halves both agree.** |
| R255 swift: a MODULE-SCOPE function with the same name as a member of `self` claimed the call, and the caller went ABSENT | 2026-09-06 | **CLOSED — candor-swift `513880d`, id allocated in `541d207`. Found by WIDENING R134, not assigned** | **CARDINAL SIN, swift.** `func wipe(_:)` at module scope beside `class Base { func wipe(_:) }`: both `SubA.caller` (inherited) and `OwnB.caller` (own-class) were ABSENT. Executed — the MEMBER is what runs. R134's supertype climb is last in the chain, so it never saw these. **Settled by the compiler rather than by judgement**, which is what makes the fix safe: `error: use of 'wipeC' refers to instance method rather than global function 'wipeC' in module 'shadow'` — there is no arity-mismatch fallback to preserve. Member arms now run first. **Operators are excluded** (Swift resolves them by operand type); gating the arms outright moved 802 rows of pre-existing operator behaviour, so the change is scoped to the ORDERING decision only. A/B 15,057 rows: ADDED 0 / REMOVED 1 / CHANGED 654 wide, 35 shadowed calls in 7 packages. **All five `inferred` moves are GAINS — Nimble's `NMBWait.until` ×4 and `waitUntil` gain `Clock`**, because `throwableUntil` is a `class func` on `NMBWait` while a `private func throwableUntil` sits at module scope in another file: a real silent under-report in third-party code, closed. No effect and no `Unknown` lost across 15,057 rows; the 643 `invisible` losses each trace to a call that previously resolved to the WRONG unit. | Fixed. **Near-miss reported rather than hidden: the operator guard is corpus-covered but NOT unit-covered** — three minimal fixtures all passed with the guard degraded, so the agent DELETED the toothless test rather than ship it, and recorded the reproduction (flip `memberFirst`, rescan Kingfisher). A test that cannot fail is worse than no test, and deleting it is the right call. |
| R256 swift: TWO same-type requirements on ONE generic parameter — the NOMINAL one silently displaces the FUNCTION-TYPE one, and the caller goes ABSENT again | 2026-09-06 | **OPEN — found by the coordinator while verifying R243's fix; an INCOMPLETE fix, not a regression** | **CARDINAL SIN, swift, and R243's own fix does not cover it.** Measured on one file, order-independent: with only `extension Gen where F == (Int) -> Bool`, the caller is `['Unknown']` — fixed. **Add a second extension `where F == Wiper` on the SAME generic parameter and the function-type caller goes back to ABSENT** while the nominal one reports `['Fs']`. Reversing the source order changes nothing, so it is not last-wins on text — the nominal requirement displaces the function-type one. Pre-fix BOTH were absent, so this is an incomplete fix rather than a regression — but the surviving cell is a positive purity claim over a function that invokes a caller-supplied closure. **Why R243's suite cannot see it:** every spelling in `testEverySameTypeRequirementSpellingIsDisclosed` is exercised ALONE on its own type. The collision needs two requirements on one parameter, which no fixture has. **The same shape as R229's and R240's tests — a fixture that isolates each case cannot find an interaction between them.** | Not fixed. Owed a cut with a two-requirement fixture as its first test, and an over-charge control proving the nominal cell keeps its precise `Fs` rather than being hedged down to buy the other one. **RE-MEASURED AND LOCALISED 2026-09-09 — reproduced at HEAD, and the mechanism is now named rather than described.** `Gen.callNominal` is `['Fs']`, `Gen.callFn` is **ABSENT**, `reported 2`. **BOTH requirements ARE recorded** — `DeclCollector.recordTypeGenerics` writes `typeGenericFnParams["Gen"] = {F}` for `F == (Int) -> Bool` and `typeGenericBounds["Gen"]["F"] = "Wiper"` for `F == Wiper`, into two SEPARATE maps, so nothing is overwritten there. **The displacement is in the CONSUMER, `Driver.swift:1382`, and it is an `else if`:** `if let bound = typeGenericBoundsAll[ty]?[param] { … } else if typeGenericFnParamsAll[ty]?.contains(param) { … }`. With a nominal bound present the function-type branch is unreachable — which is exactly why reversing the source order changes nothing, as the row observed but did not explain. **AND IT IS NOT A ONE-WORD FIX, which is the part worth knowing before someone tries.** The two branches write the SAME slot, `fields[ty][field]`, whose value is a `(name: String?, isFunction: Bool)` pair: the nominal writes `("Wiper", false)` and the callable writes `(nil, true)`. **The pair cannot hold both**, so `else if` -> `if` would just make the last writer win instead of the first. This is a data-model limit, not a branch typo. **The right shape, and the reason it is affordable: the disambiguating fact is per-MEMBER and is already parsed.** `Gen.callFn` exists only under `where F == (Int) -> Bool` and `Gen.callNominal` only under `where F == Wiper`; each member's own extension constraint decides which reading applies to it, and `recordTypeGenerics` already sees that constraint — it is what recorded both — and then discards which members it governed. The field index is keyed `(type, field)` while the answer is per-EXTENSION. **So neither a union nor a preference order is the fix; carrying the extension context to the member is.** A union would also fail the row's own over-charge control, since hedging the field to callable is what costs `callNominal` its precise `Fs`. |
| R257 rust: the macro-opacity hedge was kinded `ambiguous:`, which SPEC §6.2 projects to class `dispatch` — so every scoped `deny Unknown[dispatch]` gate gained ~7,700 rows about macro opacity | 2026-09-06 | **CLOSED — candor-rust `b13585b`. Found by a FABLE REVIEW of a question I asked about something else; UNSHIPPED, so it cost nothing in the field** | **A SPEC-CONFORMANCE DEFECT I INTRODUCED IN R143/R144, and the clause forbidding it PREDATES my fix by a day.** SPEC §4's table reserves `ambiguous:` for *"two or more separately-written definitions competing for one bare name"*, and its 2026-09-05 clarification says in terms that an engine which cannot compute the arm union discloses *"a limit of its own resolution rather than an ambiguity in the program"*. **A macro arm is selected by the invocation's own tokens — nothing is competing.** **The prefix is not cosmetic:** §6.2 projects `ambiguous:*` to class `dispatch` (`SPEC.md:4872`), and `candor-classify/src/policy.rs:91` does the same. **Measured on the denominator that matters — crates that PASSED before, over 126 crates, against a binary built at the parent commit:** `deny Unknown[dispatch]` **8.1% flipped (6 of 74) -> 0.0%**; `deny Unknown` blanket **2.2% (1 of 45) -> 2.2%, unchanged**. serde and serde_core were among the six. The blanket figure is deliberately untouched: those users want the signal. The scoped users should never have seen it. | Fixed by re-kinding to `macro:`, which is unlisted in §6.2 and therefore reaches the `unresolved` catch-all — no SPEC change needed. **Verified the projection rather than assumed it:** over `typenum`, `deny Unknown[unresolved]` exits 1 while `deny Unknown[dispatch]` and `deny Unknown[indirect]` both exit 0. **Two process points worth more than the fix.** (1) **I measured the wrong population and reported it to Tom.** I quoted 0.8% using ALL crates as the denominator; on crates that actually passed before it is 2.2% blanket — and I never measured the SCOPED gate at all, which is where the real damage was at 8.1%. **The group you do not measure is the group that gets hurt.** (2) **My justification for ruling out the arm-UNION alternative was a slogan.** I cited R48's *"parse-or-skip never fabricates"*, which is about a different operation — a boundary drawn around its own trigger, the error this project keeps logging. **The real reason is determinate and better:** SPEC §4's cfg union rests on the premise *"exactly one of them is built"*, chosen by configuration OUTSIDE the source; a macro arm is chosen by tokens INSIDE the source, so a union would charge an effect the source proves does not occur at that call site. |
| R258 java: R147's stored-stream charge covers every verb of its OWN predicate except `skip(long)` — the one with a `long` argument | 2026-09-06 | **CLOSED — candor-java `0c1d437`, with R274; same root cause, and the matrix found a THIRD affected verb the row did not name** | `storedStreamAcquisition` gates on `isAbstractStreamIo`, whose verb set is `read*`/`write*`/`skip*`/`transferTo`/`append`, then asks `receiverProv` for the receiver's origin. `receiverProv` computes `getStackSize() - argSlots - 1`; for `InputStream.skip(long)` that is **-1** -> `null` -> the pass returns and the read is silent. **`skip` is the ONLY verb in that predicate with a `long` parameter, so the fix covers its own verb list except for exactly one entry.** `entryAbstractStream` (R17) shares the helper and the hole. **Executed on a real `ServerSocket`/`Socket` pair** — the driver printed `first=A skipped=2 next=DE`, so bytes B and C really left the wire. One class, one field, one variable: `in.read()` -> `['Net']`, `in.read(b,0,2)` -> `['Net']`, **`in.skip(2L)` -> ABSENT**. Isolated with the binder in another package, a skip-only byte mover is absent from the report entirely and **all four policy forms exit 0**. In the non-isolated tree `deny Net` exits 1 — but on `readOne`, never naming the skipper, which is the caught-incidentally shape §F1 warns about. | Not fixed. **Not a regression** — at the range base all three were absent because the pass did not exist; this is a cardinal-sin fix arriving with one of its own verbs uncovered. Fixing R248's helper fixes this too. `StoredStreamProvenanceTest` has no `skip` row, **and across all seven suites added in that range there is ZERO occurrence of `skip`, ZERO of `schedule`, and ZERO `long`/`double` ARGUMENTS in any fixture** — the suite is structurally incapable of seeing either finding. Closed with R248's one authority. **`skipNBytes(long)` is a second affected verb this row missed**, and `Reader.skip(long)` a third; R17's `entryAbstractStream` half, asserted in the row, is now executed — all six policy forms exited 0 on a method that really moved bytes off a socket. |
| R259 ts: a UNION-typed receiver drops every accessor not present in ALL arms — seven whole-object arms plus `Reflect.get`/`Reflect.set`/`Object.assign`-target | 2026-09-06 | **CLOSED — candor-ts `6921c91`. MY PREVALENCE FIGURE WAS WRONG: not 0 — 5 real rows in hono on a fresh clone** | **R245 fixed the union question ONE CALL TOO LOW.** It fixed `accessorsFromSym` — one synthesised symbol carrying several declarations. Every whole-object arm gets its property LIST from `type.getProperties()`, which on a union returns only properties present in EVERY constituent, so an accessor declared on one arm is never handed to the (now-correct) helper at all. Proven against the TS API directly: for `x: Aa \| Bb`, `getProperties() = [other]` and `getProperty("token") = null`. **R245's own commit message states the rule its fix does not implement here** — *"a union receiver is a disjunction — ANY arm may be the runtime value — so the sound answer is the UNION of the arms' accessors."* §9, in the commit that had already written the rule down. Silent in all of: `Object.assign` target setters, `Reflect.set`, `Reflect.get`, `{...x}`, rest-destructuring, `Object.assign` sources, and all four R252 builtins. **The everyday spellings are the point:** `JSON.stringify(x: T\|undefined)`, `structuredClone(x: T\|null)`, `JSON.stringify(x: T\|string)` and `{...(flag ? a : b)}` are all silent, 1 real getter invocation each. Narrowed forms (`x ?? {}`, `x?.p`, `if (x)`) are CORRECT — TS narrows before the site, so the hole needs the union to survive into the operation. Four policy forms on an isolated tree: `pure src.only.leak` **exit 0 with the scope BINDING** (a bogus name prints `matched NO function`; this does not), `deny Fs`/`deny Unknown` scoped **exit 0**; blanket `deny Fs` exit 1 only INCIDENTALLY via the independently-reported accessor unit. | Not fixed. **Prevalence is 0 and the agent led with that rather than burying it:** an instrumented probe calibrated to re-find all 11 fixture sites found **0 in 1,652 rows** over zod/hono/got/zx, and it identified its own two raw hits in `got` as false positives (class-bodied getters a spread genuinely skips). A recall hunt over a real `node_modules` was not run — disk was 6.2 GB with three agents live. **So the severity is fixture-grounded; the mechanism is not in doubt.** **I reported "0 in 1,652 rows" and used it to argue the fix might not be worth its diff. On a fresh clone it is 5 rows plus 1 changed** — hono's `HonoRequest` body accessors, via `JSON.stringify(body)` where `body: BodyInit` has an `ArrayBuffer` arm declaring `detached`/`maxByteLength`/`resizable`. The agent then established those are the **pre-existing R115 residual, not new**: an identical NON-union `JSON.stringify(x: ArrayBuffer)` is charged the same way on BOTH arms, because `ArrayBuffer` is an `interface` in lib.d.ts and `classBodiedGetter` can only exclude accessors declared in a `class` body. **The union arms joined the answer the single-arm receiver already had, which is the convergence the fix is for.** |
| R260 ts: `globalThis.structuredClone(x)` is silent in BOTH directions, six commits after the commit that fixed exactly this class | 2026-09-06 | **CLOSED — candor-ts `86e9bcd` (R281). Verified by the coordinator. THE ROW'S BREADTH WAS WRONG: one table, not five arms** | **`f58dc0f` — in this same range — fixed *"the `globalThis.` qualifier defeated FIVE text-keyed arms at once"*** by routing them through `globalBuiltinCallee`. The bare-identifier family (`ENV_TOUCHING_GLOBAL`) still tests `ts.isIdentifier(c) && has(c.text)`; `globalThis.structuredClone` is a PropertyAccess whose `globalBuiltinCallee` text is `globalThis.structuredClone`, in neither table. **R252 — the range's HEAD commit — then added a NEW arm using the same `ts.isIdentifier` test, so it was born with the hole its sibling commit had just closed.** `window.`/`self.` fail identically by construction. **Verified by me on a tree containing exactly `export function leak() { return globalThis.structuredClone(process.env); }`: `functions[]` is EMPTY, and `deny Env`, `deny Unknown`, `deny Env Unknown` and `pure .` ALL exit 0.** The bare spelling reports `['Env']`. **No incidental catch anywhere — a whole-environment read reported as nothing.** Executed: the qualified call really returns the environment. | Not fixed. The `Env` half is pre-existing at `e40a995`; the getter half is new at `9a0cfd9`. **This is the highest-severity finding of the round** because every other one is caught incidentally by a blanket policy and this one is not. Verified by me: `globalThis.structuredClone(process.env)` now reports the leak row and `deny Env` exits 1 (was an EMPTY `functions[]` and exit 0 on every form). **My row implied five arms were open; `f58dc0f` had already closed those** — the hole was the bare-global table with TWO consumers both testing `ts.isIdentifier`, the second added by R252 six commits after the fix for its siblings. And `window.`/`self.` are now confirmed by two runs rather than my "by construction". |
| R261 ts: an `abstract get`/`set` accessor is edged into a BODYLESS unit and the concrete override is never reached — while the abstract METHOD one line over resolves correctly | 2026-09-06 | **CLOSED — candor-ts `f04b650` (R282). Verified by the coordinator. MY ROW NAMED THE TRIGGER, NOT THE CLASS** | `recordAccessorHit` edges into the accessor unit whenever `hit.local && nodeName.get(hit.decl)`. An `abstract get val(): string;` satisfies both — a unit is minted for a declaration with **no body**, the edge lands on it, the unit is pure, the caller is pure, and `classOverrides` is never consulted. **The method path DOES consult it** (`leakMethod` edges to both `AbsBase.m` and `AbsImpl.m` and reports `['Fs']`), and an INTERFACE-declared accessor correctly discloses `Unknown` + `reflect:accessor:I.token`. Same question, three implementations, one wrong — which is §F1 question 3 exactly. Executed: both spellings run identical `fs.readFileSync` bodies, 2 real writes. `pure src.only.leakAccessor` exit 0, scoped `deny Fs`/`deny Unknown` exit 0; blanket `deny Fs` exit 1 only via the independently-reported override. | Not fixed. Pre-existing at `e40a995`, but it is the same accessor machinery ten commits in this range rework. Verified by me: `leakConcrete` — a CONCRETE base getter with a body, effectful override, receiver typed as the base — ABSENT -> `['Fs']`, alongside the abstract twin, with the method control unchanged. **`recordAccessorHit` never consulted `classOverrides` for ANY accessor; "abstract" was the instance I was handed and the concrete-override shape is the common one in real code.** |
| R262 ts: `keyCouldNameAccessor` treats a TYPE-PARAMETER key as provably not-a-symbol, so every symbol-named accessor is excluded as unreachable | 2026-09-06 | **CLOSED — candor-ts `c99b15e` (R283). The agent's axis replaced mine** | `keyMayBeSymbol` tests `flags & (ESSymbolLike \| Any \| Unknown)`. A type parameter's flags are `TypeParameter` — neither symbol-ish nor wild — so the guard reads FALSE and the CONSTRAINT is never consulted, although `K extends PropertyKey` and `K extends keyof T` can both be bound to a symbol at the call site. `Reflect.get(s, k)` with `k: K extends PropertyKey`, over a class whose only accessor is `get [SYM]()`, reads **PURE**; the non-generic `k: PropertyKey`, `k: symbol` and `k: keyof Src` all disclose, and `k: string` correctly stays silent. Same hole in the `s[k]` element-access arm. Executed: 1 real getter invocation. **The helper's own comment asserts it *"lifts entirely when the key's own type could hold the other kind (`PropertyKey`, `symbol`, `any`, `unknown`)"* — true of those four spellings, false of a type parameter CONSTRAINED to them.** A safety sentence written by the commit that needed it. **It bites when EVERY accessor on the receiver is symbol-named — which is the axios `get [Symbol.toStringTag]()` shape R247's own row cites as its evidence.** | Not fixed. **`K extends keyof T` is silent on ALL FOUR arms** — my row's "narrow" label understated it — and the one arm that discloses does so **by accident**, because `Reflect.set`'s `propertyKey` is non-generic while `Reflect.get`'s is generic. A guard that passes because of an overload's shape is one TypeScript release from flipping. Now reads `getBaseConstraintOfType`; an unconstrained `<K>` fails OPEN. |
| R263 ts: an unnameable-owner MEMBER dispatch is kinded `callback:` (class `indirect`) where SPEC §4 ⟨0.24⟩ names that reclassification and REJECTS it — the exact mirror of R257 | 2026-09-06 | **CLOSED — candor-ts `2777f6f` (R284). THREE sites, and I named one** | `dispatchWhy` falls back to `callback:<member>` whenever no owner string could be formed, and the producing arm builds its owner from `sigDecl.parent?.name` — which an `InterfaceDeclaration` has and a **`TypeLiteral` never does**. **Verified by me: the same program gives `dispatch:src.a.AsIface.m` for `interface X { m(): T }` and `callback:m` for `type X = { m(): T }`** — a pure spelling difference in TypeScript. **SPEC §4 ⟨0.24⟩ rules on this in terms:** *"Reclassifying to `callback:` is what §4's own dividing line reads like — no resolvable owner — but it is false (no function value is involved) and it moves the reason's §6.2 class from `dispatch` to `indirect`, silently NARROWING every `deny E Unknown[dispatch]` gate in the field."* The arm's own comment asserts the opposite. Instrumented over four repos: **917 fallbacks from one arm — 789 with no member (call signature, `callback:` correct), 89 PropertySignature-holding-a-function (defensible), 19 MethodSignature-in-TypeLiteral (14 behind a NAMED alias) and 10 MethodDeclaration that are member dispatch.** Counterfactual restricted to the last two: `deny Unknown[dispatch]` got **0 -> 40**, zx **0 -> 15**, hono 35 -> 119, zod 120 -> 120; **139 of 294 rows (47%) invisible, and the got and zx gates currently PASS ENTIRELY.** The probe was rejected and rebuilt once because a first version made hono go 35 -> 0, which a widening cannot do. | Not fixed. **Not a cardinal sin — the `Unknown` is there, so blanket `deny Unknown` fires. It is the reason-SCOPED form that silently narrows**, which is the form the reason ratchet is adopted in. Together with R257 this is the second wrong-kind finding in one day, in opposite directions: rust over-classified INTO `dispatch`, ts under-classifies OUT of it. Grepping the mechanism rather than the site found two more: the **>12-override arm emitted an UNQUALIFIED `dispatch:Shape.m`** that the frontier's own regex can never resolve — **and the repo's test pinned that bare form and called it "canonical"** — plus a `reflect:accessor:?` owner for a type-alias accessor. Gate impact on fresh clones: `[dispatch]` got **0 -> 63, exit 0 -> 1**; zx 0 -> 15, exit 2 -> 1; `[indirect]` down 29 total, so it is not disarmed wholesale; blanket `deny Unknown` unmoved. |
| R264 rust: a REFUSED FLAG before the target let `--gate-json` DELETE a source file of the scan | 2026-09-06 | **CLOSED — candor-rust `7708067`. DATA LOSS, introduced by my own `63fe733` the same day, found by the review round and reproduced by me. UNSHIPPED** | **Not an under-report — the operator's file is gone.** `-V` was in the main parse loop's valueless set and NOT in `prescan_argv`'s hand-written copy of the same fact, so it set `stopped`, the target was suppressed, `pre_target` fell back to `"."`, and `gate_json_input_collision` was asked *"is this sink under the CWD?"* instead of *"is it under the target the operator named?"*. A sink outside the CWD subtree is invisible to that question, so arming proceeded and the fail-closed verdict document was written **over the source file at parse time, before the walk**. Measured by me from a cwd unrelated to both: `--version <dir> --gate-json <dir>/src/lib.rs` exit 2 and intact; **`-V` the same flag spelled short, exit 0 and DESTROYED**; `--typo` and `--scope` likewise. Clean at `10b19c7` and at `3950c59`. **`63fe733`'s own message describes closing exactly this shape** — *"the contract depends on ARGV ORDER"* — it removed the order dependence from arming and created a new one in the input guard, one flag name apart. | **Two fixes, because a shared list cannot reach the general case.** `VALUELESS_FLAGS` is now one authority both sites read (that closes `-V`); and **when the target is UNKNOWN a source-shaped sink is refused outright** — an unrecognised flag legitimately suppresses the target, so no list helps there. We cannot answer the containment question without a target, but we can answer the half that makes it DESTRUCTIVE, and that half needs no target. Refusing costs at most a stale green on a run already exiting 2; arming costs the file. **Controls verified: a legitimate non-source sink is STILL armed behind a refused flag** (⟨0.27⟩'s stale green does not return), an ordinary `.rs` sink with the target first is still refused, a normal scan still writes its verdict, the stream sink is unaffected. **TWO instrument errors of mine, the same one twice:** my first manual reproduction used `/tmp` as the "unrelated" cwd — on macOS a symlink to `/private/tmp`, the parent of the scratchpad, so the sink WAS under the cwd and the guard saw it, and I nearly filed "did not reproduce". Then the regression test's first version used `temp_dir()` as its cwd while `make_crate` builds there, and **passed against the unfixed binary**. Fixed to the crate root; RED with only `scan.rs` stashed. |
| R265 swift: R255's member-first reorder ignores ACCESS CONTROL and file/module visibility, so a `private` member claims a call Swift binds to the module-scope function | 2026-09-06 | **CLOSED — candor-swift `e5c211c`. Verified by the coordinator on his own fixture. MY VISIBILITY SENTENCE WAS WRONG** | The three member arms key on `overloadedBases`/`byQual`/`supertypesOf`, **none of which carry access control or file scope**. Swift's unqualified lookup stops at the type scope only when the member is VISIBLE at the call site; a `private` member is invisible from another file and an `internal` member from another module, and Swift then binds the global. candor now binds the member regardless. **Verified by me: a `private func wipe` on a class, with the caller in an `extension` in another FILE calling the effectful module-scope `wipe` — PRE `['Fs']`, POST ABSENT.** Four executed spellings all really delete the file through the global: `private` + cross-file extension, `fileprivate` + cross-file, `private` on a base with the subclass in another file, and `internal` across modules. `deny Fs C.caller`, `pure C.caller` and `deny Fs Unknown C.caller` all go **1 -> 0**; blanket exits 1 only incidentally via the global's own row, and the engine prints *"nothing hidden"*. Discriminator: rename the shadowing member away and the caller is charged again. | Not fixed. **The commit's own comment asserts the opposite as settled language law** — *"Swift's unqualified lookup ... never widens to module scope ... NOT A JUDGEMENT CALL — the compiler settles it."* True only for a VISIBLE member, and written by the commit that needed it to be true. **Why its 16-package A/B could not see it:** both real instances it cites (swift-nio `debugOnly`, Moya `unwrapThrowable`) are in the SAME FILE as their callers, so the new binding is right for both; the corpus holds no cross-file instance and the branch was safety-only without anyone saying so. **AMENDMENT: my row said a `private` member is invisible "from another file". It is also invisible to a SUBCLASS IN THE SAME FILE** — `private` is scoped to the declaring declaration and its same-file extensions, and a subclass is neither. **24 of the 94 regressed cells were same-file**, and a fix written to my sentence would have left them. Verified by me: same-file private member with a subclass caller, REF `['Fs']` -> HEAD `['Fs']`, restored. Of the 72 invisible-member cells, the 12 that correctly still bind the member are **exactly `internal` + other-file — same module, so the member IS visible** — confirmed against the executed marker rather than inferred from the axis. |
| R266 swift: two of the three member arms key on the SHORT enclosing type name, so a nested type collides with an unrelated top-level type of the same name | 2026-09-06 | **CLOSED — candor-swift `e5c211c`. MY MECHANISM WAS WRONG IN BOTH THE ARM AND THE DIRECTION** | `enclosingType` is `typeStack.last` — the SHORT name; `enclosingTypePath` is the full nested path. **Arm 2 uses the path and its comment says so** (*"resolved against the FULL enclosing path, so a nested type's sibling call hits its own member precisely"*); **arms 1 and 3 use the short name.** That comment is a direct instruction to test its siblings, and the siblings are wrong. R255 moved both colliding arms in FRONT of the free-function arms, so a collision now pre-empts a call Swift binds to a global. **Silence (introduced):** `enum Outer { class S { func caller() { run() } } }` beside an unrelated top-level `class S` with an overloaded pure `run` — arm 1 matches, caller ABSENT, `deny Fs`/`pure`/`deny Fs Unknown` scoped all 1 -> 0. Same one arm over via arm 3 climbing the wrong type's supertypes. **Fabrication (pre-existing at both commits):** a nested `Outer.S` declaring its OWN pure `run` is charged `['Fs']` with an edge to the unrelated top-level `S.run()` over a program that provably deletes nothing — arm 1 wins over the precise arm 2, so the nested type's own member is never consulted. | Not fixed. Both directions executed. **AMENDMENT, replacing my mechanism entirely:** it is reachable only from a caller in the type's OWN BODY (an extension-declared caller cannot hit it, because `pushType` stores `extension O.T` as one dotted `typeStack` element); it fires via **arm 3**, not arm 1; and **it is a FABRICATION (`Clock,Env`), not the silence my row leads with.** **Found in third-party code by the corpus A/B:** Kingfisher's `MemoryStorage.Backend.isCached` was charged `Clock, Fs` because candor bound `DiskStorage.Backend.value` — a *disk* cache — where the compiler binds the NSCache read fifteen lines above, matched only because both nested types are short-named `Backend`. |
| R267 swift: `memberFirst`'s operator exclusion is a FIRST-CHARACTER test, so every BACKTICK-ESCAPED identifier is classified as an operator — R134 and R255 both miss that spelling | 2026-09-06 | **CLOSED — candor-swift `d7deec6`. The widest of the three, and the ONLY one also red on the shipped v0.35.0 artifact — the agent's ordering, not mine** | `let memberFirst = (call.leaf.first.map { $0.isLetter \|\| $0 == "_" } ?? false) \|\| !freeArmWouldClaim`. SwiftSyntax hands back `` `default` `` INCLUDING the backticks, so the first character is a backtick — neither a letter nor `_` — and the three member arms are skipped onto the pre-R255 free-first path. Swift 5.9 raw identifiers widen this well past keywords. Executed: `` class BaseB { func `default`(_:) { rm } } `` with an inherited call, and the own-class twin, both really delete the file through the MEMBER and both report NO ROW, while the byte-identical program spelled `plain` is correctly `['Fs']`. **Isolating experiment that pins it to `memberFirst` alone: remove the module-scope `` `default` `` so `freeArmWouldClaim` is false, and both callers resolve correctly — `byQual`/`supertypesOf` handle the backticked key perfectly.** | Not fixed. One line: strip backticks before the test, or test "not an operator character" rather than "starts with a letter". **The R255 agent was right to delete its toothless operator control and say so — the guard needed a DIFFERENT attack, not a better fixture for the same one.** 232 red cells at v0.35.0 -> 0. **AND IT RUNS IN BOTH DIRECTIONS, which my row did not say — see R279:** with a PURE backticked member the caller is charged the shadowed global's `Fs` over a program that provably deletes nothing, 116 cells. A fix verified only against the silence direction would have looked complete. |
| R268 swift: a stored property INHERITED from a supertype is not a callable/container source, so iterating it reads silent-pure | 2026-09-06 | **CLOSED — candor-swift `c6a893b` + `830c180`. MY EXCULPATING CLAUSE WAS WRONG, and the agent's OWN FIX then introduced six disclosure losses** | R211/R192/R215 made a container field of `self` a callable source. Those indices (`fieldArrayElem`, `fieldDictValue`, `fieldIsCallable`) are keyed on `enclosingType` and **never climb `supertypesOf`** — while the method-call path (R134, the same range and the same day) and the property-accessor path both do. Three paths answer "what does this member hold"; one does not climb. Executed, isolated package: `class BaseI { let cbs: [(String)->Void] = [bomb] }` + `class SubI: BaseI { func run(p) { for c in cbs { c(p) } } }` really deletes the file; `SubI.run` **ABSENT** under the *"nothing hidden"* clean bill. Every scoped form exits 0; blanket `deny Fs` exits 1 only via `BaseI.init`'s closure literal. Holds for the direct spelling, `let copy =`, and `self.`-qualified. **The inherited COMPUTED property is correct, which is the drift.** | Not fixed. **AMENDMENT: strike "the inherited COMPUTED property is correct".** That is true of the property-accessor path and false of a computed property vending callables — **18 of the 36 silent cells are computed**. The drift is own-type-disclosed vs inherited-absent. **And the follow-on commit is the lesson: the first fix flattened too widely and cost six disclosures, which the UNIT SUITE DID NOT SEE — only the corpus A/B did, after bisecting across five binaries.** `fields` has four membership-only readers. **That is the fourth time this week a silent-under-report fix introduced one**, and the only reason it did not ship is that the A/B was run. |
| R269 swift: three binder forms of a container copy carry no element index — the tuple PATTERN, `case let`, and an ANNOTATED CLOSURE PARAMETER | 2026-09-06 | **CLOSED for six binder forms — candor-swift `c1b9be7`. ONE OF THE FORMS I TOLD IT TO FILE IS NOT A BINDER FORM AT ALL** | All three executed over BOTH a stored field and a function parameter; each really invokes a stored closure that deletes a file and each reports the enclosing function ABSENT, while the direct spelling and the plain `let` copy in the same scan are `['Unknown'] callback:c` — **and ABSENT at `795a39f`, which proves the fixture reaches the changed code.** Scoped `deny Fs`/`pure`/`deny Unknown`/`deny Fs Unknown` all exit 0 on all three. **Distinct from the tuple COPY R215 records as a stated limit** (`let t = tup; t.0.eff()`) — this is the tuple PATTERN whose element is the container, and its direct twin works. R124 already enumerates nine binder forms elsewhere in this engine; this index reads one. | Not fixed. **AMENDMENT: `for case let` is a NESTED-CONTAINER gap, not a binder gap — now R278.** I instructed the agent to file it as one of four new binder forms; its plain-`for` and fully-annotated twins are equally silent, which is what proves the axis. Of the four I named, two survived, one was an `Optional(…)`-wrapper artifact (the natural `guard let z = optField` already worked at v0.35.0), and one is a different class. **The annotated closure PARAMETER remains unreproduced in four spellings and the row says so rather than quietly dropping it.** |
| R270 rust: R257's boundary was drawn around its own trigger — TWO MORE macro-opacity hedges are still `ambiguous:`, and 30 real crates pass `deny Unknown[unresolved]` while holding the hole | 2026-09-06 | **CLOSED — candor-rust `7680680`, with the FULL enumeration I failed to do for R257. My "30 crates" figure was 29** | R257 re-kinded the macro-opacity hedge at **exactly the two lines it was reported for** (`collector.rs:3831, 3855`). The same condition survives at `scan.rs:2960` (`ambiguous:module items hidden by an unexpanded macro`, R128) and `collector.rs:4278` (`ambiguous:re-export key a macro-hidden module could also own`). **R128's own comment already concedes the point** — *"§4's own text describes `ambiguous:` around the two-same-named-defs case rather than the zero-readable-defs one, so this REUSES a kind rather than fitting it exactly."* **Full prefix census over 1,509 crates (274,902 rows, 136,802 carrying `unknownWhy`):** `callback` 90,392, `ambiguous` 22,825, `native` 19,304, `macro` 7,917. Of the `ambiguous` rows, 291 in 45 crates are `module items hidden by an unexpanded macro` and 2 are the re-export spelling — both the wrong kind. **And the gate impact is the mirror of what I measured for R257.** I measured the `[dispatch]` flip; here that is only **1** crate of 1,509. The direction that matters is the other one: **30 crates currently PASS `deny Unknown[unresolved]` while holding a real macro-opacity hole — reqwest ×3, openssl, nix ×4, tempfile, diesel, flate2 ×2, isahc ×2, wasm-bindgen ×4, encoding_rs, lapin, windows-core, parking_lot_core** and more. Executed on R128's own fixture (`ran=true`, a real `Command::status` through a macro-declared target with no report row): `[dispatch]` exit 1, `[unresolved]` exit 0. | Not fixed. **Q9 note recorded rather than left to be found: `ambiguous:re-export fan-out above the cap` has ZERO rows in 1,509 crates**, so any A/B over that branch is safety-only — and on §4's own words it is the clearest case of all, a resource limit rather than a program ambiguity. **Unsettled and deliberately not filed as a finding:** `ambiguous:cfg-duplicated alias` (10 rows / 4 crates) — §4's clarification is written about `#[cfg]` arms, which argues for re-kinding, but the site is two genuinely competing `use` targets, which argues against. **Also stale: `policy.rs`'s doc comment still says "the five §4 kinds are reflect/native/dispatch/callback/ambiguous" and that this file holds the vocabulary once — R257 minted a sixth spelling it has never heard of.** All ten `ambiguous:` spellings enumerated and censused over 1,509 crates (22,826 rows); the two zero-readable-defs spellings re-kinded to `macro:`, the other six left because they genuinely are two separately-written definitions. **Real-binary PRE/POST over the 46 affected crates: `[dispatch]` 1 crate; `[unresolved]` 30 flip but only 29 go green-to-red — lapin was already exit 2.** A/B ADDED 0 / REMOVED 0 / CHANGED 263, all `unknownWhy`-only. **No cache bump, and that was MEASURED not asserted:** a warmed pre-R270 cache read by the new binary is byte-identical to a cold scan on a crate carrying the new reason twice. `ambiguous:re-export fan-out above the cap` confirmed 0 rows in 1,509. |
| R271 rust: `expr_is_fn_typed` answers for the callable's ACCESS PATH but not for the EXPRESSION WRAPPED AROUND IT — `match`, a block, `unsafe {}`, a deref, an index and an `if`'s ELSE branch all fall through | 2026-09-06 | **CLOSED — candor-rust `95d7adc`. Verified by the coordinator on his own fixture with two independently built binaries. THE ROW DESCRIBED HALF THE MECHANISM — see R285** | The arm list is `Path \| Paren \| Group \| Reference \| Try \| Await \| If \| Field \| Call \| MethodCall`; `Expr::Match`, `Expr::Block`, `Expr::Unsafe`, `Expr::Unary(Deref)`, `Expr::Cast` and `Expr::Index` hit `_ => false`. **`Expr::If` reads only the THEN branch**, which is §F1 question 1 exactly. Executed: a crate defining nothing effectful whose callback writes a file prints **`invocations=56`** — every arm really invokes the caller-supplied body — and **12 callers are ABSENT**, through BOTH the parameter and the field access path, so it is not an R238 boundary miss but an older class its sweep walked past. **All four policy forms blind:** blanket `deny Fs` 0, `deny Unknown` 0, `deny Fs Unknown` 0, caller-scoped 0. | Not fixed. **Incidence measured and stated rather than left to the fixture:** a probe binary with the six missing arms, wide-key A/B over all 1,509 crates — **ADDED 2 / REMOVED 0 / CHANGED 27 over 267,360 rows.** Real instances in `getrandom` (a `transmute`d `GetRandomFn` behind `unsafe {}`) and `rustix` vdso wrappers; `getrandom-0.3.4 …init` goes `inferred: [] -> ['Unknown']`, a row that currently reads as a positive purity claim. The probe closes `match`/`block`/`deref`/`unsafe`; `index` and the cast-then-else `if` stay silent even with it. **Closed via ONE authority, `lang::callable_operands`, consulted by both consumers.** Verified by me at `v0.35.0` -> HEAD: `e_match`/`e_block`/`e_unsafe` over a locally-visible `local_eff` go **ABSENT -> `['Fs']`**, with `e_plain` unchanged. **My row's "12 callers ABSENT" was wrong: 136 of 210 opaque cells at `v0.35.0`, 112 at the merge-base, 0 at HEAD** — and the 18 known-effect cells likewise. 1,509-crate wide-key A/B **ADDED 10 / REMOVED 0 / CHANGED 51, zero rows losing an `inferred` member**; the additions include **trybuild `run::Test::check` going ABSENT -> `['Env','Exec','Fs','Unknown']` over a fn that runs cargo and writes files.** §E1: no branch safety-only. **A source-only revert cannot demonstrate red-then-green here — the fix changes a type (`fn_alias` became a LIST for fan-out), so the old source does not compile against the new tests.** I confirmed that rather than reading a green as a pass; the falsification is the two-binary A/B above. |
| R272 rust: R238's synthetic `"Fn"` leaf is asserted in-comment to be incapable of CHA fan-out; a crate that defines its own `trait Fn` falsifies it | 2026-09-06 | **CLOSED AS A WORDING CHANGE — candor-rust `1d55fe6`. The agent BUILT the obvious fix, measured it, and REJECTED it** | The commit and the code both state: *"`\"Fn\"` matches no local trait ... **no CHA fan-out, no `Type::method` edge and no concrete effect can come out of this entry**."* A safety sentence written by the change that needed it to be true (§E2/attack K). Executed: a crate defining `pub trait Fn { fn go(&self); }`, an `impl Fn for fn()` that is pure, and an unrelated effectful `impl Fn for Evil`. `cargo run` confirms the file is **never created** — `call_it` performs nothing — and candor reports it **`['Fs']`**. Blanket `deny Fs`, `deny Fs Unknown` and caller-scoped `deny Fs call_it` all **exit 1: a red gate over a provably pure function.** ABSENT and correct at `63fe733`. | Not fixed, and deliberately not upgraded: `grep -rlE 'trait\s+Fn\b'` across all 1,509 registry crates returns **0 files**. **The correction is the deliverable, not the exploit** — per §E2 the sentence should be worded as the assumption it is (*"`Fn` is not a name real crates give a trait"*) rather than as a guarantee, because a documented guarantee is what licenses the next narrowing. **The deliverable is the refusal.** Re-spelling the hedge `<callable>` removes the over-charge and its 1,509-crate A/B is byte-identical — **and then the agent wrote the second fixture: change one character so `impl Fn for fn()` itself writes the file, and the sentinel build reports the caller ABSENT over a file the executed run really creates.** A cardinal sin traded for an over-charge that is not one. So the commit changes no behaviour: the condition is stated once as the ASSUMPTION it is, four sites point at it, and both fixtures are pinned. **This is [[feedback-fabrication-fixes-cause-misses]] working as intended — write the second fixture FIRST.** |
| R273 instrument: the property gates DETECT a symmetric silent under-report, print it, and then exit CLEAN without registering it | 2026-09-06 | **CLOSED — candor-rust `89c5c2c`. The agent took BOTH options and argued why they are not alternatives** | `check_pair.py` classifies a pair BOTH-PURE when NEITHER spelling is charged although the executed run dropped. `run_q.sh` counts it and prints *"it is a separate silent under-report"* — then **exits CLEAN**; `baseline.sh` reports `both-pure=3` and writes **nothing** to `known_open.tsv`, whose kinds are only DRIFT/DISCLOSED/VIOLATION. `both_pure` never reaches an exit code in any of the three scripts. Executed instance isolated from the exhaustive generator: pair 257 (`ctor=mac_blocktok, ending=drop_h, loop=forloop, mode=looprot`) — `examples/gt.rs` prints `3 3 2 2 r257`, so both spellings really drop 3 `H` values whose `Drop` performs `Fs`, the control `r000` correctly reports `['Fs']`, and **both `r257` and `v257` are ABSENT**. Caller-scoped `deny Fs r257` and `deny Unknown r257` both exit 0; blanket exits 1 only incidentally via `H::drop` being independently reported. | Not fixed. The `mac_blocktok`/`drop_h` vein is known debt (7 `q DRIFT` + 4 `macro VIOLATION` rows). **The gap is that a NEW symmetric silence would print identically to these six and pass every gate — a differential instrument is structurally blind to a symmetric silence, and nothing else carries it.** Either the register gains a BOTH-PURE kind or the gate fails on a new one. Register at `b13585b`: **99 shapes** (10 `q DRIFT`, 85 `macro DISCLOSED`, 4 `macro VIOLATION`), `baseline.sh --check` CLEAN. *"Fail on a new one" is undefined without a register saying which are old, and a register nothing fails on changes nothing* — the pairing already exists for VIOLATION/DISCLOSED/DRIFT and BOTH-PURE was the verdict left out. **Register 99 -> 102** (the whole delta is this commit; R271 and R270 left it at 99, `baseline.sh --check` CLEAN at `7680680`). **Calibrated three ways:** register as written exit 0; BOTH-PURE lines struck exit 1 naming 3 new silences; same input through the PRE-R273 scripts exit 0, reproducing the gap. |
| R274 java: the SLOT-vs-VALUE operand-stack index is THREE cardinal sins, not one, and the widest has NO TABLE bounding it | 2026-09-07 | **CLOSED — candor-java `0c1d437`. Found by the calibrated-matrix rule BEFORE any fix existed; verified by the coordinator** | ASM's `Frame` stack holds **one entry per VALUE**; `Type.getSize()` counts a `long`/`double` as two. **Nine** helpers across THREE files summed sizes to index the stack — `receiverProv`, `argAt`, **`callArg` (absent from R248's own five-helper list)**, `callArgs`, `indyArgs`, `handoffTaskArg`, plus `argsTainted`, `monomorphicReceiver` and `fieldBoundImplementors`. Predictive rule: **argument *k* is misread iff a `long`/`double` sits at position ≥ *k*; the RECEIVER iff one sits anywhere.** **The worst is `Interp.monomorphicReceiver`, which R248 never named and which no table bounds:** a non-null result switches OFF the CHA over-approximation, so `new StringBuilder().append(h.work(1L))` read the StringBuilder as the receiver and gave `inferred: []`, `calls: []` over an executed file write. **Verified by me on the PUBLISHED v0.35.0 jar with two single-variable controls** — drop the `long` or drop the `new` and it is `['Fs']` with its edge. Its surface is every virtual/interface call carrying a `long`/`double`. 70 GENERATED cells, all compiled and executed with the effect observed, against a jar built from the `v0.35.0` tag: **19 silent cells now disclose** (12 hand-off, 6 stream, 1 CHA), and **the same arithmetic FABRICATES** — four cells charged `task-handoff` Unknown against a provably-pure program, and `argsTainted` raised an AS-EFF-007 advisory for `in.skip(2L)` whose only argument is a constant. Both cleared. | **TWO OF THE TWELVE `getSize()` GREP HITS ARE CORRECT AND MY INSTRUCTION WOULD HAVE BROKEN THEM.** `paramLocalSlot` and `Interp.paramSlots` address LOCAL-VARIABLE slots, where a `long` genuinely occupies two — their own doc comments say so, and I read them to confirm. **The discriminator is frame-STACK index (values) vs frame-LOCAL index (slots), not the `getSize()` text: I drew the boundary by grepping a pattern instead of asking the question.** That sentence is now in the code at `argValueIndex` with cross-links from both correct sites. **`Cha.fieldBoundImplementors` was MEASURED NON-SILENT** against my premise that it loses a binding silently — it discloses `['Unknown'] dispatch:app.W.w` — so it was folded in as precision, not as a sin fix. Acceptance run by me, not the author: tests exit 1 with `src/main` reverted to the parent and exit 0 restored; `assert-audit` OK; every `gates.sh` line green except the two known-dark, both of which the agent re-checked rather than assumed. |
| R275 java: the suite's ONE long-bearing hand-off assertion was GREEN ON A FABRICATED REASON — which is why R248 sat latent under a suite that appears to cover it | 2026-09-07 | **CLOSED — candor-java `a5c2fd5`. The most generalisable finding of the round** | `StructuralDispatchTest`'s `timer.schedule(tt, 0L)` is the only fixture in the suite carrying a category-2 argument to a hand-off, and it asserts `Unknown`. **It passed on the broken engine, off the WRONG VALUE:** one trailing `long` made `handoffTaskArg` return the RECEIVER, and that fixture's receiver is an opaque PARAM, which is independently Unknown-worthy. Measured against the published v0.35.0 jar: `viaTimer` -> `['Unknown']`, while `viaTimerNewRecv` (a provable `new` receiver) and `viaTimerTwoLongs` are **ABSENT from `functions[]` entirely**. **My prior hypothesis — that the fixture rooted a VISIBLE task — was refuted, and the truth is worse**, because a fixture that roots the wrong thing looks like a gap while this looks like coverage. | Fixed: both distinguishing spellings added beside the original line, verified red with the source reverted. **The general rule, and it is why the matrix precedes the fix: a fixture can REACH a defect and still be green, because the wrong read landed on a value that answers the same way.** Nothing about the assertion's text reveals that; only a grid that varies the receiver's provenance does. |
| R285 rust: the named-fn-by-value edge lost a KNOWN effect, not a hedge — R271's row described only half the mechanism | 2026-09-07 | **CLOSED — candor-rust `95d7adc`. Found by the calibrated matrix BEFORE any fix existed** | R271 blamed `expr_is_fn_typed`. A generated 270-cell matrix found a second, sharper consumer: the `if let syn::Expr::Path(a)` edge at `collector.rs:2601` peels nothing, so `v.retain(match 0 { _ => local_eff })` — over a fn the scan can READ and knows writes a file — was ABSENT at `v0.35.0` and at the merge-base. 18 executed cells; caller-scoped `deny Fs` and `deny Unknown` both exit 0; blanket `deny Fs` exit 1 only incidentally via `local_eff`. **A fix confined to `expr_is_fn_typed` would have left all 18 red while looking complete.** | **Also recorded, and it is a rule about how this class must be tested: the DIRECT call form `(match ..)(x)` reports `Unknown` on both arms in all 70 cells — a fixture built on it CANNOT go red.** Pinned in the regression test's doc. |
| R286 rust: ⟨0.21⟩'s completeness manifest asserts FULL COVERAGE over functions the engine silently certifies pure | 2026-09-07 | **OPEN — filed, not fixed; a distinct claim from the callback silence and it SURVIVES R271's fix as a class** | On the R271 matrix at the merge-base, the report carried `analyzed: {count: 211}` with **no `unanalyzed` key**, while **112 of those 211 functions each provably invoked an arbitrary caller-supplied callback and were omitted from `functions[]`**. `analyzed` measures PARSE coverage, and nothing in the report distinguishes *"parsed and resolved"* from *"parsed and silently dropped"* — so a consumer reading the manifest for coverage gets an affirmative full-coverage claim laid directly over the cardinal sin. | Not fixed. The fix is a report-shape question and probably four-way, so it is a rung rather than an engine edit. |
| R287 rust: SPEC §4 ⟨0.25⟩ requires the UNION for a `#[cfg]` arm set; `scan.rs` refuses it and calls the union fabrication | 2026-09-07 | **CLOSED 2026-09-14 by the ⟨0.38⟩ rung + PART 89, both halves. The all-project-declared half closed with the clause; the MIXED half was split out as R438 (rust) and R429 (swift) and BOTH are now closed, so this row closes with them.** The split was worth making: a `grep R287` during the half-fixed window could not read it as whole, and the two engines' mixed defects turned out to have different causes (rust: two recording routes; swift: a union edge inside a branch only one arm order enters) that one row would have blurred | `scan.rs:2013` hedges a `#[cfg]`-duplicated alias whose arms disagree to `Unknown` + `ambiguous:cfg-duplicated alias`, its comment stating that charging both would be *"a fabricated effect from the arm that is not compiled"*. **SPEC §4's 2026-09-05 clarification says the opposite for exactly this shape** — the effects *"are the union of the arms"*, PICKING is the fabrication, dropping is a ⟨0.21⟩ purity claim — and adds that `ambiguous:` stays reserved for two separately-written definitions, so a conditional-compilation arm set is not it. 10 rows / 4 crates; `[dispatch]` cost 0, `[unresolved]` gain 3. | **Not re-kinded here, and that is the point:** the remedy SPEC prescribes is the UNION, a four-way behaviour change candor-rust does not own. **Acting on half of it — the kind without the union — is precisely the boundary-drawn-around-the-trigger error R270 exists to record.** Owed a ruling, then a four-way rung. **THE RULING, 2026-09-09, at the ⟨0.36⟩ cut review.** The coordinator's own reading was that ⟨0.36⟩'s union clause probably DID bind this shape — which would have meant candor-rust declaring a rung it does not meet, on a shape no row covers. **It does not bind it.** The clause's words name DEFINITIONS — *several bodies behind one qualified name* — and a cfg-duplicated `use … as X` is two SEPARATELY-WRITTEN definitions, each with its own qualified name, behind one bare local name. That is the reading candor-rust's own test already pins: `tests.rs:4789` classifies `ambiguous:cfg-duplicated alias` alongside the re-export collisions as *"TWO separately-written definitions competing for one bare name — §4's own case"*. SPEC now says so in a scoping paragraph, so the next reader does not infer it from the reasoning. **BUT THE BEHAVIOURAL ANSWER IS THE UNION, and that half is settled — `scan.rs:2013`'s comment is simply wrong.** It calls the union *"a fabricated effect from the arm that is not compiled"*, and the engine ALREADY unions cfg arms inside a single body: `fn go() { #[cfg(unix)] {fs::write} #[cfg(windows)] {env::set_var} }` measures `['Env','Fs']` at HEAD. That comment's principle, taken seriously, condemns the engine's own established behaviour and PART 10's expected answer. **THE PORT IS NOT THE ONE-LINER IT LOOKS LIKE, which is the second reason it is its own rung.** External arms can union straight from `arms()`, but a LOCAL arm gets no call edge through the alias route at all (measured: `calls` absent, no `ALIASCOLLIDE` line — the local arm never enters the alias map), and a plain `use` never reaches `record_alias`, which does a bare `out.insert`. **A latent inconsistency to carry into that rung:** the engine DECIDES feature-gated arms (dropping declared-inactive ones, R140's closed half) and UNIONS only undecidable platform arms — the clause's words say "exactly one of them is built … union" and do not say *undecidable*; the alias clause should. Fixture for the future PART: both written orders, R140's platform-gated pair, and an alias pair whose LOCAL arm is the effectful one. |
| R288 instrument: EVERY wide-key corpus A/B in this family is keyed on the `fn` qual, which COLLAPSES workspace members that share one | 2026-09-07 | **CLOSED — candor umbrella `1ce2a04`. MY MECHANISM WAS WRONG AND THE FIX IT IMPLIED WOULD HAVE DONE NOTHING — see R290** | Measured on R270: the census counted **291** report rows carrying `ambiguous:module items hidden by an unexpanded macro` while the A/B reported **263 CHANGED** — a **10% under-count**, reconciled exactly to 261 distinct `fn` keys plus 30 duplicates across 8 crates (`async-std` 9, `tiff` 13, `flate2` ×2, `mysql_common` ×2, `proptest` ×2). **The rows are not lost, they are MERGED — so a diff can show "no change" for a member whose sibling moved the other way.** Every `ab.py` in the scratchpad lineage inherits the key. | Not fixed. Key on `(package, fn)`. **Until then, treat every ADDED/REMOVED/CHANGED figure in this register as a lower bound**, including the ones filed today. **AMENDMENT: I wrote that the `fn` key "collapses workspace members that share one". It does not.** Verified by me on `async-std`: 7 duplicate-qual groups, **every member byte-identical, ZERO groups differing within one arm** — they are impl arms of a single crate, sharing `package`, `fn`, `loc` and `hash`. **`(package, fn)` merges them exactly as `fn` did.** The loss is MULTIPLICITY, not content, and the danger I stated — a row hidden behind a sibling moving the other way — did not occur on that corpus. The fix is a **multiset of row values per key**, and the under-count is real: 263 -> 293 on the R270 arms, reconciling exactly with that row's own census of 291 + 2. |
| R281 ts: `globalThis.`/`window.`/`self.` `structuredClone` was silent in BOTH directions — the BARE-GLOBAL table the qualifier fix never reached | 2026-09-07 | **CLOSED — candor-ts `86e9bcd`. CARDINAL SIN, executed; the only finding of the round where BLANKET policies were silent too** | `f58dc0f` fixed five MEMBER-keyed arms through `globalBuiltinCallee`. `structuredClone` is a bare global in `ENV_TOUCHING_GLOBAL`, whose **two** consumers both tested `ts.isIdentifier(callee)` — **and R252 added the second consumer with the same test six commits after the fix for its siblings landed in the same range.** Isolated: bare spelling `deny Env` exit 1; qualified spellings `functions[] = []` with `deny Env`, `deny Unknown`, `deny Env Unknown`, `pure <fn>` and `deny Env <fn>` ALL exit 0, scopes binding. Executed: each spelling clones all 66 environment variables including a planted secret. | Fixed at both consumers through one helper with the same `identIsGlobal` guard. Corpus reach: qualified 0, bare spelling 8 — reported as safety-only rather than presented as coverage. |
| R282 ts: `recordAccessorHit` never consulted `classOverrides` for ANY accessor | 2026-09-07 | **CLOSED — candor-ts `f04b650`. CARDINAL SIN, executed. R261's "abstract" was the trigger, not the class** | The index already keys accessor declarations in its own loop; the consumer never asked. Executed with the receiver typed as the base: `abstract get` 1 write / caller ABSENT; **`get val()` with a body plus `override get val()` 1 write / caller ABSENT — the shape R261 missed and the common one**; the method twin correctly `['Fs']`, which is the control. `pure <caller>` and `deny Fs <caller>` exit 0 with the scope binding; blanket `deny Fs` exit 1 only via the independently-reported override. | Bounded exactly as the method path is — receiver-subtree scoped, disclosing past the CHA fan-out limit or on an unminted override. Corpus reach 0 against an enclosing branch of 957, stated as such. |
| R283 ts: `keyCouldNameAccessor` read a TYPE PARAMETER's own flags, and one arm passed only because a `lib.d.ts` overload is non-generic | 2026-09-07 | **CLOSED — candor-ts `c99b15e`. CARDINAL SIN, executed** | Measured against the TS API: `K extends PropertyKey` has flags `TypeParameter`, so `keyMayBeSymbol` reads FALSE while its constraint would answer TRUE. Over a class whose only accessor is `get [SYM]()`: **`K extends keyof Src` silent on all four arms**, `K extends symbol` silent on both `Reflect` arms, and `K extends PropertyKey` silent on `Reflect.get` but **disclosing on `Reflect.set` BY ACCIDENT** — that overload's `propertyKey` is non-generic. | Fixed by reading `getBaseConstraintOfType`; an unconstrained `<K>` fails OPEN. The denylist direction is preserved and pinned with a POSITIVE twin so the control cannot pass by being inert. **A guard that passes because of an overload's shape is one TypeScript release from flipping — that is the part worth remembering.** |
| R284 ts: an unnameable-owner MEMBER dispatch was kinded `callback:` at THREE sites, and R263 named one | 2026-09-07 | **CLOSED — candor-ts `2777f6f`. SPEC §4 ⟨0.24⟩ conformance; the mirror of R257 and of R270** | The producing arm read `sigDecl.parent?.name`, which a `TypeLiteral` never has. Grepping the MECHANISM found two more: the **>12-override arm emitting an UNQUALIFIED `dispatch:Shape.m`** the frontier's `^dispatch:(.+)\.([^.]+)$` can never resolve — its own comment claims the other sites produce none of the 1,234 malformed strings, true of that corpus and false of the code — **and the repo's own test pinned the bare form and called it "canonical"**; plus `reflect:accessor:?` as an owner. Gate impact both directions: `[dispatch]` got 0->63 (exit 0->1), zx 0->15 (exit 2->1), hono 40->137, zod 120->125; `[indirect]` down 29; blanket unmoved; 183 of 1,726 rows changed, 0 effects gained or lost. | **One judgement call, flagged by the agent and ACCEPTED by me rather than left implicit.** Of the sites whose class moves, only 26 are `MethodSignature`/`MethodDeclaration` — what R263 asked for; **76 are `PropertySignature`, a property HOLDING a function value**, which R263's own breakdown filed as defensible callbacks. It moved them because the INTERFACE spelling of the identical declaration was ALREADY `dispatch:` pre-fix, so the two spellings disagreed, and it made them agree in the **WIDENING** direction — which only ever adds rows to a gate. Agreeing the other way would move 44 pre-existing rows OUT of `[dispatch]`, the narrowing direction, which needs a proof it does not have. Reversible in one line if the family rules otherwise. **Residual stated: a fully anonymous inline literal has no owner and stays `callback:`, per §4's "owner AND member both known".** |
| R277 swift: a caller declared in an `extension` of a NESTED type reached NO inherited member, and a same-named global claimed the call | 2026-09-07 | **CLOSED — candor-swift `e5c211c`. Pre-existing at v0.35.0; found by the matrix, not assigned** | `pushType` stores `extension Outer.S`'s whole dotted spelling as ONE `typeStack` element, so `enclosingType` there is `"Outer.S"` while `conformers`/`supertypesOf` are keyed `"S"` at the declaration. The R134 climb looked up a key the declaration side never writes, returned empty, and R255's reorder then handed the call to a same-named module-scope function. **96 cells of the generated 1152-cell matrix, every one executed, silent at BOTH v0.35.0 and `541d207`**, in both effect polarities. Discriminator: move the caller into the type's body and it resolves. | Fixed — the climb uses the last path component and unions a path-keyed climb. |
| R278 swift: a container whose ELEMENTS are containers loses the inner element index in EVERY binder, including the plain `for` | 2026-09-07 | **CLOSED — candor-swift `dd2c9a9` (every declared spelling) and `c6b309a` (the array-literal spelling, the same day). Three residual spellings remain, each MEASURED and each with a stated reason (see the fix cell). This CORRECTS my instruction on R269** | `for z in [cbs] { for c in z { c(p) } }` really invokes a stored closure that deletes a file and the enclosing function is **ABSENT** — no row, no `Unknown` — while the one-container-out spelling in the same scan discloses. **Holds identically for the plain `for` and for a fully annotated `let g: [[(String)->Void]]`, which is what proves the axis is the NESTED CONTAINER and not the binder.** Executed over a stored field, a parameter and a local; every scoped form exits 0. | Not fixed. **Pinned as KNOWN-SILENT in the test suite, so closing this row turns that test red and names itself** — a residual that announces its own closure rather than rotting. **RE-MEASURED AND LOCALISED 2026-09-09, still unfixed on purpose, but the layer to fix is now named.** Reproduced at HEAD on all three spellings (`for z in [cbs]`, over a `[[(String) -> Void]]` FIELD, and over a fully annotated local): each is ABSENT while the one-container-out control `for c in cbs` discloses `Unknown` + `unresolved: true`. **The cause is ONE LAYER EARLIER than this row implies, and than the obvious fix would target.** Probed at the `for`-binder: `elementTypeOf` returns `nil` for the nested sequence — but that is a SYMPTOM. `fieldArrayElem["Holder"]["nested"]` is **nil too**: the decl collector records no element spelling for a `[[X]]` field at all, while the sibling `[X]` field records `"()->()"` correctly. So the outer binder has nothing to take an element FROM, and patching `elementTypeOf` — the site the symptom points at — would be correct code on a path with no input, the failure this register has recorded five times this week. **The shape of a real fix, from the probe rather than from reading:** the decl collector must record an element for a nested container, and the binder needs a way to say *"this name is itself a container of X"*. Note that `arrayElem[name]` ALREADY means exactly that — *name's element type* — so `for z in nested` wants `arrayElem[z] = X`, the INNER element, and no new index is required at the binder end. The missing half is entirely in the collector. **One latent oddity found beside it and left alone:** `elementTypeOf([cbs])` returns `"cbs"` — an array literal's element type is taken as the first element EXPRESSION's name, i.e. a variable name used as a type name. It resolves to nothing downstream so it costs nothing today, but it is a wrong answer rather than a refusal, and it is the reason the literal spelling fails for a different reason than the field spelling does. **RE-MEASURED 2026-09-09 AND THE ROW'S OWN LOCALISATION WAS TOO NARROW — corrected here.** It said *"the missing half is entirely in the collector"*, singular. It is **FOUR sites**, and the extra spellings were not previously measured. At HEAD, with `for g in flat { g.run() }` over a `[Guard]` field charging `Fs` as the control, **all four of these are ABSENT**: the `[[Guard]]` FIELD, a `[[Guard]]` LOCAL with a full annotation, a `[[Guard]]` PARAMETER, and the `forEach` spelling `nested.forEach { z in z.forEach { $0.run() } }`. `grep -rn 'arrayElementName(' Sources/` returns exactly four call sites — `DeclCollector` 504 (global), 1006 (field), 1246 (parameter) and `CallCollector` 5468 (local) — **each inside its own `else if` chain, each storing into its own map, and each losing the nesting independently.** That is R347's duplication shape a third time: the ELEMENT decision is already unified in one function, and the NESTING decision is absent from all four of its callers. **One design was worked out and REJECTED, and the reason is the useful part.** The tempting minimal form reuses the existing index — set `arrayElem[n]` to the INNER element and mark `n` in a companion `nested` set, letting `setArrayElem` (which is already the single writer, and already keeps `opaqueElem` paired exactly this way) carry the extra level. It is contained and it is wrong: `arrayElem[n]` would then LIE about a nested `n` — it would say the element is `Guard` when the element is `[Guard]` — and every OTHER reader of that index believes it. `n.forEach { $0.run() }` would type `$0` as `Guard`, which is a FABRICATION the moment `[Guard]` has a member of the same name. **A representation that is right for the one consumer you are fixing and wrong for the index's stated meaning buys containment with a fabrication**, which is the trade R347 refused on the `Mutex` peel. The honest fix is a sibling map carrying the inner element, written at all four sites from one new authority beside `arrayElementName`, merged in `Driver` where `fieldDictValue` is merged (`Driver.swift:837`) and inherited across supertypes where it is inherited (`Driver.swift:1741`) — `fieldDictValue`'s 14 references are the exact template. **Not started rather than half-landed**: it crosses the Driver merge and the supertype-inheritance pass, and a partial threading is the correct-code-on-an-unreached-path failure this register has recorded five times this week. The four spellings above, and the two over-charge controls beside them (`[[Calm]]` and `[[Int]]`, both correctly absent today), are the fixture the next attempt should start from. **FIXED THE SAME DAY — candor-swift `dd2c9a9`, and the paragraph above is what made it a 45-minute change rather than an exploration.** One new authority `nestedArrayElementName` beside `arrayElementName`, a sibling index written at the field/parameter/local sites, one resolver `nestedElementOf` beside `elementTypeOf`, and TWO consumers — the `for` binder and the closure-param binder. **The second consumer is the part that would have been missed:** after the three `for` spellings went green, `nested.forEach { z in z.forEach { $0.run() } }` was still ABSENT, which is R346/R347's asymmetry again — two consumers of one fact, and fixing the one in front of you leaves the other silent. **Callers ask NESTED FIRST, and the ordering is load-bearing:** `[[T]]` makes `arrayElementName` refuse, but `Array<Array<T>>` makes it ANSWER — with `"Array"`, the container's name rather than any element's — so without nested-first the two spellings of the same type would disagree. Measured: field `for`, field `forEach`, `Array<Array<G>>`, annotated local and parameter all go ABSENT → `Fs`, with `for g in flat` charging throughout as the control. **THE ENGINE'S OWN GATE CAUGHT A HOLE IN THE FIX, which is the most useful thing that happened here.** `NameKeyedStateTests` requires every stored property of the collector to be classified as cleared-on-rebind, deliberately-kept or immutable, *because name-keyed state that is not cleared on a rebind has produced seven defects*. The first draft added the index and **not the clear** — and a stale nested entry is the FABRICATION direction (`let n: [[G]] = …; for z in n {…}; let n = Calm(); for z in n {…}` gives the second binder the first `n`'s element). It is now cleared in `clearBindingTypeOnly` beside `arrayElem` and saved/restored by `snapshotType`/`restoreType` with it, with `ctlRebind` as the fixture. **A gate that demands a DECISION rather than a behaviour is what caught this**; no effect test would have, because the stale entry only bites on a shape nobody had written down. **The over-charge controls each iterate a nested container whose element has a PURE method OF THE SAME NAME** (`[[Calm]]`, `[[Int]]`, and the rebind), so a binder typed from the wrong container surfaces as a fabrication rather than as a silence; all stay absent. **BOUNDARY, MEASURED RATHER THAN ASSERTED — three spellings are still silent and each has a reason:** a module-scope GLOBAL (the global element index is per-module in the Driver, `globalArrayElemByModule`; threading a second one is a larger change than the shape it closes), `for z in (opt ?? [])` (the sequence is not a declared name), and three-deep `[[[T]]]` (one level of nesting is modelled). The two ARRAY-LITERAL spellings in the older pin also remain, and not for want of trying: `elementTypeOf`'s literal arm ANSWERS `"cbs"` for `[cbs]` — the latent oddity this row recorded on 2026-09-07 — and because it answers, the nested arm never runs. **AND THEY CLOSED THE SAME DAY — `c6b309a` — because the stated cause WAS the fix.** The literal arm never had to be made to refuse: `nestedElementOf` got its own literal arm (resolving each element through `elementTypeOf`, which can type it, and requiring the same UNANIMITY the sibling arm requires — a heterogeneous or partly unresolvable literal returns nil, an empty one yields nil), and the binder now asks the NESTED question FIRST. **The reorder is safe by construction rather than by test:** `nestedElementOf` answers only for genuinely nested shapes — its indexes hold nothing for a flat container, and the new literal arm requires every element to resolve to a container — so it can only answer where the flat resolver was about to be silent or wrong. **THE PIN HAS NOW BEEN INVERTED TWICE BY THE ROW IT TRACKS**, once when the declared spellings closed and once here, which is the argument for writing one: both closures announced themselves instead of quietly widening the fix's claimed scope, and the second was found by the pin's own stated cause rather than by a fresh hunt. Suite 1128/1128, gates 11/11, four-way conformance OK. The KNOWN-SILENT pin filed with this row said *"when that row is closed this assertion goes red and names itself"* — it did exactly that, and is now inverted. Falsified: with `nestedElementOf` returning nil the new test fails 6 assertions. Suite 1128/1128, `bin/gate-run.sh candor-swift` 11/11, four-way conformance OK. |
| R279 swift: R267's identifier misclassification also OVER-CHARGED — a pure member's caller charged the shadowed global's `Fs` over a program that deletes nothing | 2026-09-07 | **CLOSED with R267 — candor-swift `d7deec6`** | My R267 row led with the silence only. Executed in both directions on the same matrix: effectful backticked member -> silent under-report (116 cells); **PURE backticked member -> the caller charged `Fs` from the shadowed global over a program that provably deletes nothing (116 cells), scoped `deny Fs <caller>` exiting 1 on a clean program.** | Fixed by the same one-line change. **The point is the row, not the fix: a defect verified only in the direction its row names looks closed when half of it is still open.** |
| R280 swift: a locally-bound ANNOTATED CLOSURE VARIABLE loses its `Unknown` disclosure the moment its enclosing function has a CALL SITE | 2026-09-07 | **OPEN — CARDINAL SIN, pre-existing. Found while pinning R269's UNREPRODUCED claim** | `let g: ([(String) -> Void]) -> Void = { … }; g(cbs)` is disclosed `Unknown` while its enclosing function has no caller, and goes **ABSENT the moment any call site exists** — including one passing a plain `String`, which cannot have re-resolved anything. **Two byte-identical bodies in ONE scan differing only in whether a call site exists, so it is a measurement and not an inference.** | Not fixed. It is callback-flow's deferred resolution rather than a binder index — a different vein with a much wider blast radius — **and the agent filed it rather than widen a binder cut into callback-flow without a ruling.** Pinned as KNOWN-SILENT. Needs an owner. |
| R289 instrument: `gate --report` returns exit 2 on ANY tree containing a `Package.swift`, which would have masked an entire 1524-cell matrix as "could not evaluate" | 2026-09-07 | **OPEN — found before a single number was trusted; id allocated on request rather than invented** | The unpeeked `manifest` exclusion makes every verdict INCOMPLETE, so every cell returns exit 2 uniformly — **which reads as "the policy could not evaluate" rather than "the caller is ABSENT", and a matrix built on that path would have shown no signal at all while looking like it ran.** The scan path (`candor-swift <dir> --policy`) on the byte-identical program exits 0 with *"nothing hidden … policy ✓"*. | Not fixed. **The operational instruction until it is: build swift matrices on the SCAN path and keep the manifest out of the scanned root.** Recorded here and in the suite header so the next person does not lose a day to a uniform failure. |
| R290 instrument: R288's mechanism was wrong — the `fn` key loses MULTIPLICITY, not workspace members, and `(package, fn)` would not have fixed it | 2026-09-07 | **CLOSED — candor umbrella `1ce2a04`, with R288's own numbers reconciled exactly** | Measured against R288's own evidence: the 8 named crates are single-crate registry checkouts whose duplicate rows share `package`, `fn`, `loc` AND `hash` — `async_std#string::extend::String::extend` appears 5× byte-identical from five impl arms. **Census over all 1,509 crates: 274,912 rows, 267,370 distinct `fn`, 7,542 extra rows (2.7%) in 3,901 groups across 599 crates, and the number of groups whose members DIFFER within one arm is ZERO.** So the merge lost multiplicity, not content. Verified independently by me on `async-std`: 7 groups, all byte-identical. **The fix is a MULTISET of row values per key.** Real A/B on the R270 arms over 1,509 crates: old `fn` last-wins **CHANGED 263** — byte-for-byte the legacy script's number, so the comparison is a faithful reproduction rather than a strawman — multiset **CHANGED 293**, reconciling with that row's census of 291 + 2 with nothing left over. | **`(package, fn)` alone is ALSO unsafe, and the agent's first draft had that bug:** the registry holds `flate2-1.1.9` and `flate2-1.1.10`, both reporting `"package": "flate2"` — R288's own defect one level out. **Found only because the legacy row first priced at 187 against R288's stated 263**, and the discrepancy was chased rather than explained away. The corpus ENTRY is now in every key mode. **Rust-shaped, not universal: java's `fn` carries the erased parameter list, so 1,670 rows give 1,670 keys with 87 overload groups — the `fn` key was safe there all along.** |
| R291 instrument: there was NO shared corpus A/B — fifteen ad-hoc scripts, fourteen keyed on bare `fn` | 2026-09-07 | **CLOSED — `bin/corpus-ab.py` + `bin/corpus-ab.test.sh`, candor umbrella `1ce2a04` / `25b3244`** | Fifteen `ab.py`/`ab.mjs` in one session scratchpad and no shared tool. **Measured: 14 of 15 key on bare `fn`; 9 of 15 narrow the compared VALUE, two of them to `inferred` alone.** The exception keys on `hash` and its docstring already asserts a non-zero row count per entry, citing the R242 hazard verbatim — **the knowledge existed in one script and reached none of the other fourteen.** That is §G in one directory. | The tool keys `(entry, package, fn, hash)` over a multiset, compares every field by default, **prints a calibration table pricing your key AND value against the old ones on every run** so nobody has to remember this row, reports REACH separately from the diff, and exits **3** when it cannot compare and **4** when what it produced is not evidence — an empty diff with no reach measured is not a result. All four engines end to end. **Verified by me by degrading it:** forcing every multiset count to 1 turns two selftest rows red including the headline check, and restoring returns it to green. **Its own shell gate was WRONG when first written and passed a deliberately broken build** — it asserted a count that appeared anywhere in the output, which the independent calibration table satisfied while the headline said something else. Residual: nothing forces a future round to use it except the brief. |
| R292 instrument: "refuse an entry that contributed zero rows" is SPEC ⟨0.24⟩'s measured-wrong fix, and it refused 238 of 1,509 real crates | 2026-09-07 | **CLOSED — candor umbrella `1ce2a04`; the guard keys on `analyzed.count`** | **R242's remedy as I wrote it is keyed on the emptiness of `functions`, which SPEC ⟨0.24⟩ names explicitly:** *"KEY THE RULE ON THE INTEGER, NEVER ON THE EMPTINESS OF `functions`"* — measured over 1,997 JVM jars as withdrawing **104 legitimate all-pure claims to catch 6**. Implemented as written it refused **238 of 1,509** rust crates on its first real run. Split on the integer: **147 all-pure** (compared) vs **91 judged nothing** (refused). **And the two are otherwise byte-identical on the wire:** `cfg-if-1.0.4` emits `analyzed {count: 0, digest cbf29ce484222325}` with `functions: []` — the same document an empty directory produces — so `analyzed.count` is the only channel separating a real crate that judges nothing from a hollowed tree. | Three classes now: ERROR, UNJUDGED and ALL-PURE, the first two fatal with explicit overrides and named on every subsequent run. **An entry with rows on one arm and none on the other is deliberately NOT an error — a fix removing a small crate's last row is a finding.** Mutating the guard back to emptiness reddens 3 selftest and 4 gate rows. |
| R293 instrument: `assert-audit.sh` cannot see a test that lives INSIDE the tool or in a workflow step | 2026-09-07 | **OPEN — diagnosed and deliberately NOT fixed by the agent who would have benefited from fixing it** | It failed a commit adding `bin/corpus-ab.py` with a `--selftest` carrying the whole calibration plus a workflow step running it; neither matches `TEST_RE`. **This is the identical shape the 2026-09-02 `eval/`+`verify*.sh` widening was written for** — *"a change that touched nothing BUT CI gates could never be credited"* — one spelling over. | **Not fixed on purpose, and the reasoning is the deliverable: narrowing an auditor so that it admits your own diff is the one direction you should not push it from, and a loud false FAIL is far less dangerous than a widened regex that credits any workflow edit** (R158 measured a bare-directory widening wrongly crediting 12 of 14 non-test companions). Worked around by writing a real shell gate, which was the right move anyway — it tests the exit codes and the cross-process contract that an in-process selftest structurally cannot reach. **Proposed narrow fix if wanted: credit a workflow change only when the ADDED lines contain a `run:` step — an assertion about the diff's content, not a path match.** Separately noted: `corpus.sh --oracles-only` over an empty artifact set exits 1, but only oracle [4] raises it; if [4] were ever skipped a zero-artifact round would read green. |
| R297 rust: assignment through a PLACE drops the old value in-frame and is silent in every policy form, and the dying value has no construction SITE anywhere in the body so no site-keyed refinement can reach it | 2026-09-07 | **OPEN — PRE-EXISTING, measured on the shipped 0.35.0 AND on the real published 0.34.0 installed from crates.io. Found by the R189/R195 matrix agent while attacking the coordinator's grouping; outside both rows as written** | **CARDINAL SIN, rust.** `pub fn overwrite_place(slot: &mut H, v: H) { *slot = v; }` — ZERO constructions in the body; executed **1 `H::drop` inside that frame** (the probe forgets the caller's binding, so the count is unambiguous and no frame-attribution question arises); ABSENT in published 0.34.0 and in 0.35.0; `deny Fs overwrite_place` and `pure overwrite_place` both **exit 0**, and the scoped rule is verified BOUND (a genuinely unbound rule prints `matched NO function`). Siblings identical: `reassign_deref` (`*slot = H::new(p)`), `reassign_field` (`s.slot = ..`), `reassign_index` (`v[i] = ..`), `overwrite_local` (`let mut g = v; g = w; g`). **Why it is separable from R195:** R195's cells all contain a construction whose SITE is recorded (proven with `CANDOR_ALIAS_DEBUG=1`, which prints `R172SITE H (3 sites, 2 escaping)`), so a site-keyed fix can reach them. Here the dying value was constructed in the CALLER, so there is no site in this body at any point — every site-keyed refinement is blind by construction. | Not fixed. Charging direction. Model an assignment to an owned binding OR through a place (`*p =`, `x.f =`, `v[i] =`) of a drop-relevant type as dropping the previous value in-frame, independently of whether a construction site exists. The only proven-safe exemption is a deferred `let mut g;` with no initialiser and no prior assignment, which is syntactically decidable. An `Option` initialised to `None` is NOT proven safe — the previous value may be `Some`, and over-charging it is the acceptable direction. |
| R300 rust: a construction inside a `fn` ITEM declared in a function BODY has no recorded site and no return-index entry, so it is silently suppressed whenever the same leaf is constructed anywhere else that escapes — R206's hole one item kind over | 2026-09-07 | **OPEN — PRE-EXISTING at the shipped 0.35.0. Found by the R198/R209 matrix agent; settled by two SINGLE-VARIABLE comparisons rather than by argument** | **CARDINAL SIN, rust.** Two pairs, each executed at 1 real drop, each differing in exactly one thing. (1) `r10` vs `r11`: identical fn body, identical call site, identical enclosing function — **only the DECLARATION SITE moves**, module-level to body-local. CHARGED becomes **ABSENT**. (2) `t01` vs `t02`: identical body-local fn and call — **only an escaping `Ok(H::new("z"))` is added** to the enclosing function's return. CHARGED becomes **ABSENT**; `t03` holds the same twin with a module-level fn and stays CHARGED. `deny Fs`, `pure`, `deny Fs Unknown` and `deny Unknown` scoped to the caller all exit **0**. Mechanism: `syn::Stmt::Item(_) => {}` discards `Item::Fn` at `lang.rs:3427` and again at `lang.rs:3217` (**three occurrences at HEAD**), so no `ctor_leaf` and therefore no NON-escaping site is recorded; independently `decls.rs:27`'s `fn visit_item_fn(&mut self, _: &'ast syn::ItemFn) {}` is a no-op and the return recorder at `decls.rs:2010` walks item-level `Item::Fn` only. **Bounded to the drop/escape model:** a body-local fn's DIRECT effects ARE charged (a real `std::fs::write` inside one is reported and the file was created), so the same fn item is half-visible to the engine — the brief's section G shape. | Not fixed. Charging direction on both halves: record `Stmt::Item(Item::Fn)` bodies in the site walk so the construction has a site R172's gate can see, and index body-local `fn` returns. Over-charge control: `t01` and `t04` are already charged, so the fix must not double-count. Related: R206 closed `Stmt::Item(Item::Macro)` for `macro_rules!` and left the sibling arms. |
| R301 rust: a SCOPED policy's function match is a PREFIX match, so a rule written for one function silently also binds its longer-named siblings — it defeated two independent measurement harnesses on the same day | 2026-09-07 | **CLOSED as far as an engine can close it — candor-rust `7240b0d` makes the over-bind VISIBLE. The §6.2 semantics question is untouched and still Tom's; the evidence defect that made this dangerous is gone** | **INSTRUMENT / EVIDENCE defect.** `deny Fs either` reported violations for `either_ifelse`, `either_match` and NOT for `either`; `deny Fs t_fish` matched `t_fish_both`; `deny Fs q_vec` matched `q_vecvec`. In every case the scoped column read **exit 1 for a function that is actually SILENT**, i.e. the defect fabricates a PASS-looking gate into a FAIL-looking one and hides a cardinal sin behind a neighbour's name. Both agents fixed it the same way — regenerate every fixture name to remove prefix collisions and assert zero collisions across all names before running — and both reported it unprompted as the correction that changed their matrix. **Why it matters beyond our harnesses:** a user scoping `deny Fs handle` gets `handle_request`, `handle_retry` and `handler` too, so a red gate does not identify which function is red, and a green scoped gate over a short name is not evidence about that name. Distinguish from an UNBOUND rule, which prints `matched NO function`; a prefix match prints nothing and looks bound. | Not fixed, and the fix is a SPEC question rather than an engine one — prefix matching is the documented §6.2 behaviour and narrowing it to exact-match would withdraw existing answers, so it is a report-not-decide item. Minimum action regardless of the ruling: any matrix or A/B in this family that uses scoped policies as evidence MUST assert zero prefix collisions across its function names first. Related: R288 (an instrument with no owner has its defect fixed in one copy and not the other fourteen). **ADDRESSED 2026-09-09 — candor-rust `7240b0d`, and NOT by narrowing §6.2.** Checked first, because the row calls this an engine-vs-evidence question and that had to be settled before touching anything: **the engine's own output was already explicit**, naming `either_ifelse`, `either_match` and the rule that caught them. So there was never an engine bug — only a missing SURPRISE. **What is added is one line on the same channel as `zeroMatch`, under the same rule about the exit code (a DISCLOSURE, never a verdict — measured unchanged at 1):** a `deny`/`pure` rule whose scope names a BARE function and binds more than one now says how many and NAMES them. That converts the asymmetry the row identified — an unbound rule announces itself, an over-bound one did not — into symmetry, which is the whole complaint. **The two QUIET cases are the design, not an omission.** A disclosure that fires on every real policy gets ignored, so a scope binding exactly one stays silent and a LAYER scope (`handlers::`), which is MEANT to bind many, stays silent too. Both are asserted in the test beside the positive case, and the positive case asserts the NAMES rather than the count — the count alone still would not tell you which function your gate was about. **Cost: none.** The counting pass already existed for `zeroMatch`; this reads the same map for `> 1` instead of `== 0`, and only walks the name list for rules already known to be over-bound. **STILL OPEN and unchanged: the §6.2 ruling itself.** Prefix matching remains the documented behaviour, a user writing `deny Fs handle` still gets `handle_request` and `handler`, and whether that should be narrowed to exact-match is a spec decision that would withdraw existing answers. The minimum action this row asked for regardless of the ruling — that any matrix or A/B using scoped policies assert zero prefix collisions first — is now **enforced by the engine telling you**, rather than by every harness remembering to check. |
| R302 rust: the coverage gate's FIRST-EVER real verdict — 128 calibrated entries are newly uncovered, reproduced on BOTH hosts, and the manifest must NOT be refreshed to make the gate green | 2026-09-07 | **CLOSED — candor-rust `a8c157a`, on this row's OWN condition: the review came first, the refresh commit says what every group of rows was, and CI is green. This row's premise needed correcting TWICE before that was possible** | **COVERAGE DRIFT, rust.** With R157 closed the gate ran end to end for the first time and exited 1 at the drift diff: **0 regressed (`classify()` lost no rule — the half that would have been a live defect), 2 oracle-dropped, 128 newly-uncovered.** Reproduced independently on macOS from the same commit and the same 78-crate calibrated fixture: **the same 128, with 0 regressed and 0 oracle-dropped.** Cross-host diff is 0/0/0 (see R212), so the platform caveat does not explain any of it — this is real upstream crates.io movement since the manifest was last committed. Named entries include `sea_orm::database::restricted_connection` and `sea_orm::database::transaction::*` families, `sea_orm::driver::rusqlite::*`, `sea_orm::rbac::context::*`, `sqlx_core::config::Config::try_from*` and `ureq::tls::rustls::RustlsConnector::connect`. Checked-in manifest 1,256 covered / 461 open; fresh 1,317 / 587 on macOS and 1,315 / 587 on Linux. | Not fixed, and the remedy is a REVIEW, not a regeneration. Each newly-uncovered entry is an effectful surface the classifier has no rule for, so the direction of the risk is a SILENT UNDER-REPORT on any crate reaching it — `sea_orm`'s connection/transaction families and `ureq`'s TLS connector are exactly the shapes the cardinal sin lives in. Triage the 128 into (a) genuinely new upstream API needing a classifier rule, and (b) entries an existing rule should already have matched, which would be a rule defect rather than drift. Only after that should the manifest be refreshed, and the refresh commit should say which of the two each row was. **PREMISE CORRECTED 2026-09-07, same day, by the experiment this row should have run first.** This row said the 128 were "real upstream crates.io movement". **They are not — they are OUR OWN SCANNER seeing more.** The oracle IS `candor-scan` (`generate.py` hardcodes `SCAN_BIN = target/release/candor-scan`), so every fresh arm used a scanner carrying today's work while the checked-in manifest was regenerated on 2026-09-03 (`9bc0530`), essentially the v0.35.0 scanner. Separating the two axes by swapping the v0.35.0 tag binary into `target/release` and regenerating against the SAME sources: **crates.io drift alone (checked-in vs tag-scanner on today's registry) = 1 newly-uncovered, 3 oracle-dropped, 0 regressed. Scanner improvement alone (tag-scanner vs HEAD-scanner, identical sources) = 140 newly-uncovered, 0 oracle-dropped, 0 regressed.** Row counts: checked-in 1,256/461, tag-scanner 1,253/447, HEAD-scanner 1,317/587. **So the growth is R257/R264/R270/R271/R272/R206/R209(b)/R303/R304 landing between 09-03 and today**, each of which taught the scanner to see a call it previously missed. **This makes the finding BETTER, not weaker:** the generator's own docstring says an unmatched path in a CALIBRATED crate is treated as a claim of reviewed purity with no disclosure, so each of these is a public entry point where the oracle now sees a concrete effect and `classify()` silently claims pure. **61 of the 128 carry a HARD effect** — 43 `sea_orm` (mostly `Db,Log,Unknown`), 6 `redis`, 3 `deadpool_postgres` (`prepare_typed_cached`), 2 `sqlx_core`, 2 `rusqlite`, 2 `ignore` (incl. `walk::DirEntry::metadata`, `Fs` — a long-established method, which is what first broke the drift hypothesis), 1 `ureq`, 1 `postgres` (`CancelToken::cancel_query`, `Net`), 1 `native_tls_crate`; the other 61 are soft-only (Log/Rand/Clock). **Consequence for the remedy:** the triage split this row asked for is not "new upstream API vs rule defect" — it is "which of these did our own improvements expose", and the manifest refresh belongs WITH the scanner work that exposed them rather than as a separate housekeeping commit. The hard-effect 61 are the priority; a `Db` claimed pure on `sea_orm` is the cardinal sin on a widely-used crate. **ACCOUNTED 2026-09-07 after the 12 rules landed: 128 -> 122, and the arithmetic is the fix working, not a blind spot.** I expected 12 and got 6, and the leading hypothesis (R312's identifier-vs-resolved split leaving my new rules dead in the gate) was WRONG. Measured with `--diff-manifests`: **all 12 targets left the newly-uncovered set**, and **6 genuinely arrived** — `lettre::…::Identity::from_pem`, `reqwest::tls::Identity::from_pkcs12_der` and `::from_pkcs8_pem`, `sqlx_core::net::tls::tls_native_tls::handshake`, `tungstenite::tls::encryption::native_tls::wrap_stream`, `ureq::tls::native_tls::NativeTlsConnector::connect`. Every one is a native-TLS consumer: **R312's gate widening propagated `Fs` through `native_tls` and thereby EXPOSED six real uncovered entry points that were previously invisible because the rule upstream of them was dead.** Net -6 exactly matches CI. **The general shape worth keeping: closing a silent under-report can RAISE the uncovered count, because a rule that starts firing makes its consumers' gaps visible for the first time.** A gate count that fails to fall is not evidence the fix failed. **CLOSED 2026-09-08 — candor-rust `a8c157a`, on this row's own condition rather than around it.** Regenerated at HEAD: **0 REGRESSED, 0 oracle-dropped, 119 newly-uncovered** (the last CI run at `6a2622c` was 0/2/122). Zero regressed is the half that would have been a live defect: no rule was lost by any of the session's classifier work. The 119, by group, which is the accounting this row demanded: **55 `async_nats`** soft-only (`Rand`/`Log,Rand`); **43 `sea_orm`** contained by the ceiling; **12** `mongodb`/`aws_config`/`chrono` soft-only; **5 `elasticsearch`, NEW and created by the session's own fix** — R337 taught candor that `reqwest::Identity::from_pkcs12_der` writes a keychain, so `TransportBuilder::build` and its four `Transport` constructors now carry `Fs` and their own gaps became visible, which is this row's own recorded pattern happening again, and left uncharged for R338's reason (the import is reached only when a client cert is configured, so charging `Transport::single_node` would fabricate on most consumers); **3** `sqlx_core`/`tungstenite`/`ureq`, the residuals R338 priced; **1** `rand::make_rng`, newly effectful because R334 charges the entropy source in argument position. **Gone since `6a2622c`: redis 6 (R335), reqwest 2 + lettre 1 (R337) — those were the live ones.** The manifest now carries a VERSION STAMP for 74 crates, which it did not before, so the diff no longer has to say *"UNKNOWN, not unchanged"* about which upstream versions produced its rows (R311). **And the refresh answered R212 in passing:** the checked-in manifest was generated on macOS and CI regenerated it on Linux, giving **0 regressed / 0 grown** — the two hosts agree on this commit, which is a positive data point for the cross-host question that row left open with Docker unavailable. One residual: `native_tls_crate::imp::security_framework::Identity::from_pkcs12` is covered under one spelling and still open under the other — the R312 split, not yet fully closed. **PREMISE CORRECTED AGAIN 2026-09-08, and this one reprices the whole remaining triage: THE CALIBRATION CEILING ALREADY CONTAINS MOST OF IT.** This row's remedy says "the hard-effect 61 are the priority; a `Db` claimed pure on `sea_orm` is the cardinal sin on a widely-used crate". That was written BEFORE R311's ceilings shipped. Re-split the current gate output (124 rows: 122 newly-uncovered + 2 oracle-dropped) by effect class: **55 carry a hard effect, and 43 of those are `sea_orm`** — every one of them a type that exists ONLY in sea-orm 2.0 (`RestrictedConnection`, `RestrictedTransaction`, `RbacContext`, `RusqliteSharedConnection`, `DatabaseExecutor`). sea_orm's ceiling is **1.1.20**, so any consumer able to CALL them is above it. Measured on a 2.0.2-locked fixture rather than reasoned: `RestrictedConnection::transaction` and `RbacContext::load` come back **PRESENT with `invisible: ['sea_orm']`** — a disclosure — while `DatabaseConnection::transaction` on the same fixture is `['Db']`. **They are sound-with-disclosure, not silent, and the 43 are NOT the priority this row said they were.** The other 67 are soft-only (55 `async_nats` `Rand`/`Log,Rand`, 5 `aws_config`, 5 `mongodb`, 2 `chrono`). Of the 12 non-sea_orm hard rows, only those whose path EXISTS AT OR BELOW their crate's ceiling can be silent for a real consumer — checked one by one against the ceiling version's source: `redis::sentinel::*` is present at redis 1.6.0 (**live — R335, now fixed**), `reqwest::tls::Identity::from_pkcs12_der`/`from_pkcs8_pem` at 0.13.4 and `lettre` `Identity::from_pem` at 0.11.23 (**both live — R337, now fixed**), `tungstenite` `wrap_stream` at 0.30.0 and `sqlx_core` `handshake` at 0.9.0 — **CHECKED 2026-09-08 and both are UNREACHABLE from outside their crates** (`mod tls;` and `mod encryption` are private in tungstenite `lib.rs:32`/`tls.rs:29`; `mod tls_native_tls;` is private in sqlx-core `net/tls/mod.rs:13`), so the gate named two rows no consumer can call and the reachable form of that defect is one level up in R338 — while `ureq::tls::native_tls::NativeTlsConnector::connect` does NOT exist at ureq 2.12.1 — that path is 3.x only, so it is above the ceiling and already contained. **The general lesson for reading this gate: an uncovered entry is a live silent under-report only if a consumer can reach it AT OR BELOW the reviewed ceiling; above it the ceiling discloses. The gate does not know that, so its count over-states the live risk and always will.** |
| R303 rust: my own R209(b) fix FABRICATED — both discarded-call counters counted at ANY CLOSURE DEPTH, so a closure captured by another closure that invokes and discards it was charged a drop its frame never runs | 2026-09-07 | **CLOSED — candor-rust `a9785cc`. INTRODUCED BY `4297efd` THE SAME DAY and found by a review agent attacking it, not by its author** | **FABRICATION, rust, self-inflicted.** `let f = \|\| H::new("x"); Box::new(move \|\| { f(); })` — executed **0 in-frame drops**, charged `['Fs']` by `4297efd`. The discarded call sits at `closure_depth` 1, satisfied `uses == disc`, and the suppression was withdrawn. `mark_escape` cannot rescue it: `for_each_value_child`'s `Block` arm descends only `tail_expr_of`, so an `f` in a NON-TAIL statement never enters `m.names` and the `lets` fixpoint never re-marks the closure. **It fires in real code** — the reviewer found 80 sites in 44 registry crates where every use of a let-bound closure is a discarded call inside another closure, and `hickory-resolver-0.26.2/src/resolver.rs:785` is the fabricating variant verbatim (`let test_fn = ...` used only inside `thread::spawn(move \|\| ...)`); it escaped the author's corpus A/B **only because it is `#[cfg(test)]`**. All four policy forms moved 0 → 1, including scoped. | **FIXED.** Both counters are gated on `self.closure_depth == 0` — the same guard `TryExit::in_closure` already uses. A discarded call inside a nested closure says nothing about whether THIS frame consumes the value. Measured: all NINE original gains retained (99-cell matrix, 0 cells differ from the shipped fix), the fabricating cells absent, corpus REACH fell 196 → 184 hits across 1,563 crates with 0 rows changed — the guard declining exactly the withdrawals that were wrong. Controls `nested_disc` / `nested_let_disc` falsified against the unguarded build. |
| R304 rust: the same fix keyed its counters on a BARE IDENT with no scope, so a DIFFERENT entity's discarded call satisfied the test — and a use spelled in MACRO TOKENS is invisible to the counter entirely | 2026-09-07 | **CLOSED — candor-rust `a9785cc`. Introduced by `4297efd`, found by the same review** | **FABRICATION, rust, self-inflicted.** `let cb: fn() -> u32 = tick; cb(); let cb = \|\| H::new("v02"); n` — executed **0 in-frame drops**, charged. The single-variable control is decisive: renaming the second binding (`let other = ...`) makes it correctly absent, so the answer turned on the reused NAME. A body-local `fn NAME` shadowing a let-bound closure does the same. **The macro half is the sharper one:** `path_uses` never reads macro tokens, so a use spelled there UNDER-counts uses, which makes `uses == disc` MORE likely and withdraws suppression that should stand. It was saved until now only by the new `for_each_value_child` `Closure` arm — the reviewer built an arm-removed binary and `Box::new(idm!(f))` flipped ABSENT → `['Fs']` over 0 drops. **So that arm is LOAD-BEARING, not the belt-and-braces its comment implied.** | **FIXED.** The withdrawal now also requires `name_bindings[name] == 1` (every `let NAME` and every body-local `fn NAME` counted) and that the name appears in no macro token stream (`note_macro_idents`, flattened through nested groups, on both `Stmt::Macro` and `Expr::Macro`). Both are denylists of the exemption: an unrecognised spelling keeps the old unconditional escape and stays silent rather than fabricating. Controls `shadowed_name` / `macro_spelled` falsified against the unguarded build. |
| R305 rust: a closure PARAMETER named the same as the escaping outer binding decides whether its construction is charged — pre-existing, not from `4297efd` | 2026-09-07 | **OPEN — PRE-EXISTING; measured identical on published v0.35.0, on HEAD and on the fixed build, so this commit neither introduced nor reached it** | **FABRICATION, rust.** `let mut v = Vec::new(); let f = \|out: &mut Vec<H>\| { out.push(H::new("x")); }; f(&mut v); v` — executed **0 in-frame drops** (the `H` escapes in the returned `v`), charged `Fs` on all three arms. Renaming the closure parameter `out` → `v` makes it correctly ABSENT. The answer turns on a NAME COLLISION between the closure's parameter and the escaping outer binding, which is the same bare-ident-key weakness as R304 one scope in. | Not fixed. Direction is over-report, the safe one. The fix is to key the escape model's names per-scope rather than per-body, which is the general repair R304's guards only work around — see also R189/R195, where a name is modelled as holding one value for the whole body. Deliberately not attempted alongside the R303/R304 guards: it changes the meaning of every name in the model, and this row exists so it is not conflated with the fix that exposed it. |
| R306 umbrella: R242's corpus move states a CAUSE that points at a directory which never held the corpus, and four sibling corpora in other repos still default under `$TMPDIR` | 2026-09-07 | **CLOSED — candor `6b63314`.** **STATUS CORRECTED 2026-09-09 — this cell said OPEN while the fix was already on `main`. Verified against the ARTEFACT AT HEAD, not the commit message.** Verified: `MONOTONE_WORK:-$HOME/.candor/monotone` in `bin/monotone.sh` and `CORPUS_WORK:-$HOME/.candor/corpus-confirmatory` in candor-java. Original status: the mechanical fix is sound and verified; the recorded REASONING is wrong and the audit boundary was drawn around its own trigger. | **INSTRUMENT defect.** `bin/corpus.sh`'s move to `$HOME/.candor/corpus` works — `CORPUS_HOME` still overrides, `--hollow-check` still does not create the directory, and the new tree is verified non-hollow. But the commit blames `$TMPDIR` and the macOS `dirhelper` sweep, and **`$TMPDIR/candor-corpus` never existed**: both documented R242 victims were under `/private/tmp`, i.e. the `:-/tmp` FALLBACK branch, which `dirhelper`'s domain (the per-user `/var/folders` tree) does not explain. The claim *"migration cost is zero: `$TMPDIR/candor-corpus` does not currently exist"* checked the branch that was never used and reads as proof nothing was orphaned. Six trees under `scratchpad/swift-escape/corpus` are hollow RIGHT NOW (Alamofire 0 `.swift`, Kingfisher 0, GRDB 1, swift-collections/crypto/nio 0-1, all with `.git` stripped to `hooks/info/`) — R242 named four. **Still under `${TMPDIR}` and still treating a surviving `.git/hooks` as acquisition:** `candor-java/eval/corpus-confirmatory/run_corpus.sh:26`, `…-freshdraw/run_corpus.sh:26` (the paper's fresh-draw evidence, same path NAME), `candor-rust/soundness/confirmatory/run_frozen.sh:38`, `candor-swift/soundness/confirmatory/run_frozen.sh:24`, plus `corpus-crossorg`, `transitive-reconcile`, `rq4a-petclinic` and `candor/bin/monotone.sh:35`. And **no test pins the new default**, so a silent revert to `${TMPDIR:-/tmp}` goes unnoticed; `assert-audit.sh candor 40f713d~1..40f713d` FAILS on the commit. | Not fixed. Correct the recorded cause to the observed one (`/private/tmp`, cause not established) rather than the inferred one; port the `$HOME` default and a CONTENT-based acquisition test to the four sibling harnesses; add a test pinning `bin/corpus.sh`'s default. Relates to the standing rule that an audit's boundary must be stated, justified, and not drawn around its own trigger. |
| R307 umbrella: `corpus-ab.py --allow-unjudged` reports that it did NOT compare entries it DOES compare, and counts them in `entries_compared` | 2026-09-07 | **CLOSED — candor `fa81245`.** **STATUS CORRECTED 2026-09-09 — this cell said OPEN while the fix was already on `main`. Verified against the ARTEFACT AT HEAD, not the commit message.** Verified: `COMPARED ANYWAY` present in `bin/corpus-ab.py`. Original status: behaviour is the SAFE one; both the run output and `--help` describe the opposite of what the code does. | **INSTRUMENT / EVIDENCE defect.** With `--allow-unjudged`, `excluded = bad + []`, so unjudged entries stay in `good` and their rows join the diff. Executed on a two-entry fixture (entB judged nothing in the PRE arm, 1 `Net` row in POST): the run prints *"1 entry judged NOTHING and was NOT compared"* and still reports `ADDED 1`. So a 0 → N transition DOES surface, which is the direction you want — but the text asserts the opposite, and `--help` says "They are excluded from the diff". A reader of today's R209(b) headline would compute the corpus as 1,423 crates; the diff actually spans 1,514, of which 1,276 bore any row. | Not fixed. Correct both strings to say the entries ARE compared and that an entry judging nothing in one arm and rows in the other will surface as ADDED/REMOVED. Separately: `reach_entries` stores only a COUNT, not the set, so a reach measurement cannot be cross-checked against the skipped population — store the set. |
| R308 rust: a bare pre-2018 `Fn(..)` trait object makes the whole FILE unparseable, dropping every function in it | 2026-09-07 | **CLOSED — candor-rust `cda70bd`. Both real instances close; the residual is files that are not Rust, which is a different question** | **COMPLETENESS, rust.** `serial-core-0.4.0` — 802 lines, 35 bodied fns, the whole crate — reports `unanalyzed: [{"path":"src/lib.rs","reason":"source failed to read/parse"}]` and **zero rows**. Bisected to a Rust-2015 bare trait object: `fn reconfigure(&mut self, setup: &Fn(&mut SerialPortSettings) -> ::Result<()>)`. Two-line repro, executed: `&Fn(&mut u8) -> ::std::io::Result<()>` beside `pub fn touches_fs(){ std::fs::read("x"); }` gives analyzed=0 / rows=0; the same file with `&dyn Fn(...)` gives analyzed=1 / rows=1. The `-> ` arrow is not required — `&Fn(&mut u8)` alone does it. **Scale, measured over all 1,563 registry crates:** 14 carry any `unanalyzed` file, 81 files total, only 2 wholly unreadable — so disclosure is working and the incidence is small. The largest, `lapin-4.10.0` with 67, is `templates/*.rs` Handlebars templates, not Rust. | Not fixed. Direction is a DISCLOSED loss, the acceptable one, but a whole crate reporting nothing is worth closing: `syn` parses `&dyn Fn(..)` and rejects the 2015 spelling, so either pre-normalise the elided `dyn` or record the parse failure per-ITEM rather than per-file. The construct is a closure trait object — the same shape as R209(b) — so ask whether java/ts/swift have an equivalent whole-file give-up. **FIXED 2026-09-09 — candor-rust `cda70bd`, by the FIRST of the two options: `parse_file_2015_tolerant` retries a rejected file once with bare closure-trait objects normalised.** **The property that makes this safe is structural, not careful: the rewrite only ever sees text `syn` has ALREADY rejected.** A file that parses is returned from the first attempt and never reaches it, so the blast radius is exactly the set of files that today contribute nothing, and the worst case for one of those is that it still fails. **And the parser is the validator** — a bad rewrite is rejected on the retry and falls back; it can fail to help, it cannot smuggle a mis-parse through. **That property was exercised, not assumed.** A `:` trigger looked obviously right and turned `F: Fn() + Sync + Send` — a generic BOUND, where `dyn` is a syntax error — into garbage. `syn` caught it; review did not. `:` is now deliberately absent, and every real trait object is still reached by `&`, `<` or `=`, because a bare `x: Fn()` parameter is unsized and compiles in no edition. **A second instrument shipped with it and paid for itself inside the same commit: `PARSEFAIL` diagnostics behind `CANDOR_ALIAS_DEBUG`.** The `unanalyzed` entry says only *"source failed to read/parse"*, so identifying the construct meant bisecting by hand — which is how this row was found, at a cost of an hour. `syn`'s error already carries the line, column and token; it was being discarded by `.ok()`. With it, the SECOND real instance — `type Action = Fn(&siginfo_t) + Send + Sync;` (signal-hook-registry `lib.rs:140`) — took seconds, and it is the reason `=` is a trigger at all. **MEASURED on all nine registry crates that report any `unanalyzed` file:** `serial-core-0.4.0` 1 unanalyzed / 0 analyzed -> **0 / 28**; `signal-hook-registry-1.4.8` 1 / 20 -> **0 / 40**; the other seven unchanged. ADDED 20 / REMOVED 0 on those nine, ADDED 0 / REMOVED 0 / CHANGED 0 across a 500-crate safety sweep, and the coverage gate reads 0/0/0. **The seven unchanged are correct and the residual is now named:** their unanalyzed files are not Rust — lapin's Handlebars templates, `serde_json/src/features_check/error.rs` (a bare string literal used as a `compile_error!` payload), sea-orm's build tool, aws-lc-rs's util script. **So of the 81 unanalyzed files this row counted, the parseable-Rust half is closed and the remainder are non-Rust files a Rust parser is right to refuse.** The row's other question — whether java/ts/swift have an equivalent whole-file give-up — is NOT answered here and stays open. |
| R309 java: the published ADVISORY's affected list was bounded by three tables and missed a fourth, `Rules.STREAM_CONSUMING_UTILITIES` — eight commons-io and Guava methods silent on the published jars | 2026-09-07 | **CLOSED — candor `f135464` ("ADVISORY corrected: a FOURTH affected table, and an exit code that will not move"), 2026-09-07. The work of this row WAS the correction, and it was made: CORRECTED in all three published places (umbrella CHANGELOG `f135464` + both java release bodies). Found by a review agent auditing what we published; the original text was mine** | **PUBLISHED UNDER-STATEMENT of a cardinal sin.** The advisory named 16 methods across the hand-off, stored-stream and dispatch tables and omitted `Rules.STREAM_CONSUMING_UTILITIES` (`Rules.java:613`, consumed at `Candor.java:4734` via `callArg`). Measured on jars downloaded from the releases, opaque `InputStream`/`Reader` field, ABSENT on 0.34.0 AND 0.35.0 and `Unknown` on 0.35.1: `IOUtils.skip`, `IOUtils.skipFully`, `IOUtils.copyLarge`, `IOUtils.toByteArray(in,long)` in both stream and reader spellings, plus `ByteStreams.skipFully`. Category-1 controls (`IOUtils.copy`, `toByteArray(in)`, `ByteStreams.copy`, `exhaust`) clean on all three jars. **`callArg`'s own javadoc in the fix said it "was absent from R248's own list of affected helpers"** — the boundary was known in the code and inherited by the published text. `IOUtils.copyLarge` and `skipFully` are far more common in real Java than `InputStream.skipNBytes`, which the list DID name, so a commons-io user read the advisory and concluded they were unaffected. | Corrected, not merely noted: the fourth table is now named in the advisory with its eight methods, and the correction says plainly that the earlier text told commons-io users the wrong thing. **Second lesson recorded with it:** "all four policy forms exit 0" was true of the BROKEN engine and set a false re-verification expectation — on 0.35.1 `deny <E>` and `pure` still exit 0 by design (the task is opaque; the fix changes a DISCLOSURE, not a verdict), so a user gating that way sees nothing move and concludes they were never affected. The advisory now directs them to `deny Unknown` / `candor unverified` and the `purity NOT verified` note. |
| R310 umbrella: a SCOPED one-engine cut moved `adopt/` and the IDE pins but not `ENGINE_PIN_JAVA`, so the front door kept installing the DEFECTIVE engine an advisory told users to leave | 2026-09-07 | **CLOSED for source and dev installs — candor `932272b`. Homebrew still serves the old engine until the umbrella is re-cut, which is a publish decision** | **DELIVERY defect.** After cutting candor-java 0.35.1 to close an advisory, `bin/candor:40` was left `ENGINE_PIN_JAVA=""`, which resolves to `ENGINE_PIN` = 0.35.0. So `candor update` fetched v0.35.0, first-use autofetch fetched v0.35.0, and the CI workflow `candor init` generates curled `download/v0.35.0/candor-linux-x64` — **the exact failure `bin/candor:1618`'s own comment describes verbatim**: "otherwise a repo gated by the engine a one-engine patch fixed would have its CI install the unfixed one." The guard that would have caught it, `rs_pin_violations`, only fires when the umbrella is IN the cut, and `--only candor-java` correctly excluded it — so the tooling was right and the delivery was half-done. | **FIXED** for source/dev: `ENGINE_PIN_JAVA="0.35.1"`, verified to resolve v0.35.1 with all three artifacts returning 200 while ts/rust/swift stay on the 0.35.0 family line. **The TESTS encoded the assumption the seam exists to break** — five rows asserted the JVM route uses the FAMILY pin, true only while no engine had ever been pinned separately, so the first real one-engine patch turned them red for being right; they now resolve `ENGINE_PIN_JAVA` as `bin/candor` does, and the two "nothing diverges" controls CONSTRUCT an undiverged state instead of depending on what ships. **Residual:** Homebrew users take the umbrella tarball and keep 0.35.0 until the umbrella is re-cut; the advisory now tells them to check `candor --version` and take the artifact directly. **Standing remedy:** a scoped cut of engine E must set `ENGINE_PIN_E` in the same wave, and preflight cannot see it because the umbrella is out of scope by construction. |
| R311 rust: the coverage-gate fixture pins calibrated crates with `"*"`, so a MAJOR upstream release silently moves the ledger's REVIEWED-PURITY claim onto a version nobody reviewed | 2026-09-07 | **CLOSED — remedy (a) shipped in candor-rust `a8c157a`: the manifest now carries a `# resolved-versions:` stamp for 74 crates, so a major bump is visible in the diff instead of arriving as anonymous newly-uncovered rows. Remedy (b) — pinned vs tracking — was answered by `CALIBRATED_CEILINGS`, which stops the exemption at the reviewed version.** **STATUS CORRECTED 2026-09-09 — this cell said OPEN while the fix was already on `main`. Verified against the ARTEFACT AT HEAD, not the commit message.** Original status: the CAUSE beneath R302, found while triaging it. Measured, not inferred: the fixture, the lockfile and the rules' own cited versions disagree** | **SILENT SCOPE CREEP of a purity claim.** `generate.py --print-fixture-toml` emits `sea_orm = { package = "sea-orm", version = "*" }` — **16 of 74 deps carry a wildcard** — and `cargo fetch` resolved **sea-orm 2.0.2**, while `candor-classify`'s sea_orm rule block states in terms that it was *"verified against sea-orm 1.1.20 source"*. The same gap in the minor direction: rules cite redis 1.6.0 / mongodb 3.8.1, the fixture resolved 1.7.0 / 3.9.0. Because the ledger treats an unmatched path in a CALIBRATED crate as a claim of reviewed purity with NO disclosure (`generate.py`'s own docstring), candor asserts review of a MAJOR version it has never seen. That is what R302's 43 uncovered `sea_orm` entries are: 2.0's new types — `DatabaseExecutor` (`src/database/executor.rs:19`), `RestrictedConnection`, `RestrictedTransaction`, `RbacContext`, `RusqliteSharedConnection` — with verbs (`begin_with_options`, `transaction_async`, `transaction_with_config_async`) that appear **ZERO times** in `candor-classify/src/lib.rs`. **The generator already knows this hazard and only half-guards it:** `version_key`'s comment warns that picking the wrong registry directory means "the manifests then record entry points from a version nobody asked for, and no output says which version was read" — it fixed WHICH directory is chosen, not that the choice is unpinned and unreported. | Not fixed. Two separable remedies, and the first is cheap: **(a) the manifests and the gate output must RECORD the resolved version per crate**, so a major bump is visible in the diff instead of appearing as anonymous "newly-uncovered" rows — this alone would have made R302 self-diagnosing. **(b) Decide whether calibration is version-pinned or version-tracking.** Pinning makes the purity claim honest but freezes the corpus; tracking keeps it current but means every upstream major release silently widens a claim nobody re-reviewed. Pinning with an explicit bump step is the direction that matches the ledger's own semantics. Until then, treat the reviewed-purity claim on any wildcard-pinned calibrated crate as scoped to the version its rules cite, not to whatever resolved. |
| R315 rust: three calibrated crates CITE a reviewed version in their own rule comments and have no ceiling enforcing it — and my sweep for such citations missed all three | 2026-09-07 | **CLOSED — candor-rust `d5c4b7d`. Verified at HEAD: `git2` 0.20.4, `mysql_async` 0.37.0 and `tokio_postgres` 0.7.18 all carry ceilings.** **STATUS CORRECTED 2026-09-09 — this cell said OPEN while the fix was already on `main`. Verified against the ARTEFACT AT HEAD, not the commit message.** Original status: enforcement gap, not yet a live under-report: all three sets of rules were re-verified against the newest resolved source and HOLD. Found by a baseline agent, including the defect in my own measurement** | **R311 CLASS, three more instances.** `git2` (`lib.rs:485`, *"verified against git2 0.20.4"*, resolved 0.21.0), `mysql_async` (`lib.rs:1613`, *"verified against mysql_async 0.37.0"*, resolved 0.37.1) and `tokio_postgres` (cites 0.7.10, resolved 0.7.18) each state a reviewed version and each was absent from `CALIBRATED_CEILINGS`, so the exemption was unconditional however far the source drifted. `mysql_async`'s omission looks like an oversight rather than a policy choice: its sibling `mysql` IS ceilinged. **The reason they were missed is a defect in MY sweep and is the more useful half of this row:** I searched for the crate name in DASHED form only and LINE BY LINE, so `mysql_async 0.37.0` (written with underscores) was invisible, and git2's citation — wrapped across two comment lines — was invisible too. Re-run joining wrapped comment lines and trying both name forms: **27 crates cite a version, not the 24 I reported.** A grep that under-counts reads exactly like a clean result. | Not fixed here; the three ceilings land with the baseline batch. Standing lesson: when sweeping source COMMENTS for a fact, normalise the identifier form and join wrapped lines FIRST — a comment is prose, and prose wraps. |
| R318 rust: the argon2/scrypt/pbkdf2/password_hash entropy rule is BROKEN, not stale — its only match target was deleted upstream, and the documented one-argument `hash_password()` now reads PURE | 2026-09-07 | **CLOSED — candor-rust `6a2622c`, then NARROWED by `7b06dc5` when the fix turned out to fabricate on password-hash 0.5.x (R330).** **STATUS CORRECTED 2026-09-09 — this cell said OPEN while the fix was already on `main`. Verified against the ARTEFACT AT HEAD, not the commit message.** Original status: a LIVE silent under-report on password-hashing crates, measured against resolved source. Found by a baseline agent, independently verified by the coordinator** | **CARDINAL SIN, rust, and on the worst possible surface.** The shared block (`candor-classify/src/lib.rs:1947-1952`) charges `Rand` on exactly one condition: `path.contains("SaltString::generate")`. **`SaltString` has ZERO occurrences in the resolved `password-hash` 0.6.1** — the type was removed in 0.6.0 (0.5.0 has 21 occurrences; verified by grep over both trees). It was replaced by free functions `generate_salt()` / `try_generate_salt()` (`0.6.1/src/lib.rs:233,243`) and, critically, by a DEFAULT TRAIT METHOD `fn hash_password(&self, password: &[u8])` (`0.6.1/src/lib.rs:107`) whose body is `let salt = try_generate_salt()?;` — i.e. **the single most documented call shape in the whole family, `argon2.hash_password(password)`, mints OS entropy and is classified `None`.** The block's `return None` fallthrough then asserts purity for every other path in all four crates. This is the sea_orm shape on a crate family whose entire purpose is a security review. | Not fixed at filing. Direction is CHARGING, so the repair is safe: match `::generate_salt`, `::try_generate_salt`, `::hash_password` and `::hash_password_with_rng` (which draws from the caller's RNG). **`ends_with`, never `contains`** — `hash_password_with_salt` takes an explicit salt and is correctly PURE, and a `contains` match would fabricate `Rand` on it. Ceiling `password_hash` and its three siblings at 0.5.x until the rewrite lands, so the old rule's claim is scoped to the version it was true for. |
| R319 rust: bcrypt grew four more internal-salt entry points between 0.15.1 and 0.19.3 and the suffix rule names none of them | 2026-09-07 | **CLOSED — candor-rust `6a2622c`. Verified at HEAD: the four `non_truncating_*` verbs are present.** **STATUS CORRECTED 2026-09-09 — this cell said OPEN while the fix was already on `main`. Verified against the ARTEFACT AT HEAD, not the commit message.** Original status: silent under-report at the resolved version; the rule was exactly correct when written. | **CARDINAL SIN, rust.** The rule charges `Rand` on `path.ends_with("::hash")` or `::hash_with_result` — which covered bcrypt's whole internal-salt surface at 0.15.1. Resolved 0.19.3 adds `non_truncating_hash` (`src/lib.rs:250`), `hash_bytes` (`:262`), `non_truncating_hash_bytes` (`:271`) and `non_truncating_hash_with_result` (`:293`); each documents *"The salt is generated randomly using the OS randomness"* and the `_with_result` pair reaches `getrandom::fill` directly. **None ends with `::hash` or `::hash_with_result`**, so all four read pure. The explicit-salt family (`hash_with_salt`, `hash_with_salt_bytes`, `non_truncating_hash_with_salt*`) correctly stays pure and must remain so. | Not fixed at filing. Widen the suffix list to the four new verbs — charging direction. Note the shape for the ceiling policy: this is a MINOR-version drift (0.15 -> 0.19 in 0.x terms is major-ish, but the rule broke by ADDITION, not by rename), so a ceiling would have flagged it while a stale-path check would NOT have — the old paths still exist and still work. A ceiling catches additions; a path check only catches removals. |
| R320 rust: `rand`/`rand_core` renamed `OsRng` to `SysRng` and moved it into `getrandom`, leaving the `OsRng` clause in both rule blocks matching nothing | 2026-09-07 | **CLOSED — candor-rust `a70857f`. The open question was answered by MEASUREMENT and the residual turned out to be real** | **STALE RULE, rust.** Both blocks test `path.contains("OsRng")`. At resolved `rand` 0.10.2 and `rand_core` 0.10.1 the type is `SysRng`, defined in and re-exported from `getrandom`; `OsRng` appears in `rand_core` only in a doc comment. Also dead as literal strings after the 0.9/0.10 rewrite: `gen`, `gen_range`, `gen_bool`, `gen_ratio`, `gen_iter`, `thread_rng`, `from_entropy`. The replacement verbs (`random`, `random_range`, `random_bool`, `random_ratio`, `random_iter`, `rng`, `from_os_rng`) ARE already in the rule and still match, so any direct draw is still charged. | Not fixed. **The open question the agent correctly refused to answer from `candor-classify` alone:** whether a call written `rand::rngs::SysRng` resolves, in candor-scan's own path resolution, to crate_name `rand`/`rand_core` (hitting the dead clause) or to `getrandom` (whole-crate exempt regardless). If the former, a handle passed around without ever hitting a suffix-matched verb could be missed. Settle that by reading the resolver and building the fixture, THEN decide the repair — do not delete the clause on the assumption it is redundant. **ANSWERED 2026-09-08 by the fixture the row asked for: it is the FORMER.** candor resolves the crate from the path the CONSUMER writes, and rand 0.10 re-exports getrandom's type (`rand-0.10.2/src/rngs/mod.rs:119`), so `rand::rngs::SysRng` arrives with crate_name `rand` and lands in the `rand` block — the re-export does not carry getrandom's whole-crate exemption across. **The residual is therefore real, and the agent's caution was right: it is not a precision loss, it is a lost SAFETY MARGIN.** `contains("OsRng")` is not a duplicate of the verb list; it is what charges an UNRECOGNISED verb on a type that exists only to draw from the OS. Executed: `OsRng` + an unrecognised verb -> `['Rand']`, `SysRng` + the same verb -> **ABSENT**. The margin protected the deleted spelling and not its replacement. **FIXED — candor-rust `a70857f`:** the clause is mirrored for `SysRng` with the same `clone`/`fork`/`default` exemption, for the same reason (a unit struct, `getrandom-0.4.3/src/sys_rng.rs:34`, so copying the handle consumes nothing). Both spellings and both exemptions are asserted in ONE test so the pair cannot drift apart again; falsified against the pre-fix classifier, where the `SysRng` row fails and the rest pass. **REACH IS ZERO AND THAT IS RECORDED, NOT HIDDEN.** `bin/corpus-ab.py` over 9 entries / 4,807 rows (quinn-proto, crypto-common, rand, rand_core, getrandom, rustls, rauthy, atuin, vaultwarden): ADDED 0 / REMOVED 0 / CHANGED 0 with the marked branch counting **0 hits**. This is a margin for a spelling today's corpus does not yet contain, rand 0.10 being new — **not** a clause that cannot fire, which is the distinction from the dead clauses this register criticised on 2026-09-08 (R331's `TlsConnectorBuilder::build` could never be reached at all; this one is demonstrated firing on a legal, compiling spelling). **And the fixture built to settle this row found a LARGER hole one argument along — see R334.** R320's framing ("a rename residual") would have sent the audit past it, because the hole is present in the OLD spelling too. |
| R321 rust: `terminal_colorsaurus` 1.0.3 added a PURE accessor with the same trailing segment as its effectful free function, and the suffix rule charges `Ipc` to both | 2026-09-07 | **CLOSED — candor-rust `6a2622c`. Verified at HEAD: `path == "terminal_colorsaurus::theme_mode"`, FQN-exact.** **STATUS CORRECTED 2026-09-09 — this cell said OPEN while the fix was already on `main`. Verified against the ARTEFACT AT HEAD, not the commit message.** Original status: OVER-report, the safe direction, introduced by exactly the version drift this exercise is about. | **FABRICATION, rust.** The rule matches `path.ends_with("::theme_mode")` and charges `Ipc`. At 0.4.8 that was unambiguous. Resolved 1.0.3 has TWO: the free `pub fn theme_mode(options)` (`src/lib.rs:157`), which really does terminal I/O via `color_palette(options)`, and `pub fn ColorPalette::theme_mode(&self)` (`src/lib.rs:112`), a pure derived accessor comparing `perceived_lightness()` on two ALREADY-FETCHED colours. Both end in `::theme_mode`; both are charged. | Not fixed. Replace the suffix with an FQN-exact match on the free function (`path == "terminal_colorsaurus::theme_mode"`), leaving the accessor unmatched. **Worth keeping as the counter-example to R319:** the same mechanism — an upstream release ADDING a name — produces a silent under-report in one crate and a fabrication in another, depending only on whether the new name is effectful. A suffix rule is a bet that no future release will reuse the leaf name, and that bet is being lost in both directions. |
| R312 rust: the `native_tls_crate` rule is DEAD IN PRODUCTION — `classify()` is asked the RESOLVED package name while the coverage gate feeds it the CALIBRATED_CRATES identifier, and the gate records the dead rule as COVERED | 2026-09-07 | **CLOSED — candor-rust `6a2622c` (gate widened in the same commit that found it). Status corrected 2026-09-08: this column said OPEN while the remedy column said FIXED, which is how a closed row gets re-filed by the next agent that greps status** | **CARDINAL SIN + INSTRUMENT, rust.** `classify()` receives the resolved package name, `native_tls` (cargo normalises `native-tls`). The rule at `lib.rs:956` is gated `crate_name == "native_tls_crate"` — the CALIBRATED_CRATES *identifier* — so `TlsConnector::new` and `Identity::from_pkcs8` can never match in a real scan, while `eval/coverage-gate/covered.tsv:972-973` records both as COVERED. **Two paths computing one fact, disagreeing in the reassuring direction (brief §G).** Executed: `probe_nativetls_identity_from_pkcs8` and `probe_nativetls_tlsconnector_new` are ABSENT from the report, while the sibling `control_nativetls_connect` charges `Net` through the separate `"native_tls"` arm — so the file parsed and the instrument can fire. **Second half, worse:** under a `native_tls_crate = { package = "native-tls" }` alias the ledger key is the syntactic first segment, which IS in CALIBRATED_CRATES — so the call is silently EXEMPT while no rule can run. Under a plain `native-tls` dependency the same calls correctly disclose `invisible: ["native_tls"]`. Boundary widened past the trigger: `generate.py`'s `RENAME` map has 16 entries and this is the ONLY one where the identifier is not `package.replace('-','_')`. | **FIXED** in the same commit by widening the gate to `matches!(crate_name, "native_tls" \| "native_tls_crate")`. Corpus A/B for that widening ALONE: ADDED 5, CHANGED 133 — redis ×52, ureq ×48, hyper-tls ×6, tcp-stream ×5, tungstenite ×4, sqlx-core ×3 — all genuine propagation from `TlsConnector::new()`, ground-truthed on three of them. **Residual NOT closed:** a consumer who invents their own `package = ` alias for any of the 82 calibrated crates reproduces the same split. Only the RENAME map was swept; arbitrary consumer aliases were not. |
| R313 rust: the `postgres` ceiling I recorded was TOKIO-postgres's version, lifted by my own regex from a comment about a different crate | 2026-09-07 | **CLOSED by REMOVAL — candor-rust `6a2622c`. My defect, found by a rules agent** | **PROVENANCE defect in the R311 machinery itself.** I recorded `("postgres", "0.7.10")`. `postgres` ships **0.19.14**; `0.7.10` appears in the shared PG rule block as a citation about **tokio-postgres** (`query_typed` is tokio_postgres 0.7.10+). My sweep matched a version-shaped string near the crate name without checking which crate the sentence was ABOUT. Consequence: `postgres` permanently exceeded its ceiling — the safe direction, but on a false premise — while `tokio_postgres`, the crate the citation actually concerned, had no ceiling at all. The mirror case is `sqlx_core`, whose ceiling is 0.8.6 while its own comment says "verified against sqlx-core 0.8.6/0.9.0". | **FIXED.** `postgres` is now deliberately ABSENT from `CALIBRATED_CEILINGS`, with the reason stated in the code: no verified baseline exists, and inventing one would be exactly the claim-without-review the table exists to stop. `tokio_postgres` carries the 0.7.18 a baseline agent verified. **The general lesson is the one worth keeping: a version string NEAR a crate name is not a version string ABOUT it** — the same class as R315's dashed-form/wrapped-line misses, and both were mine, and both read as clean results. |
| R322 rust: the calibration ceiling looked up TWO KEY SPACES and crossed neither — R312's shape reintroduced inside R311's own fix, the same day | 2026-09-07 | **CLOSED — candor-rust `37743b0`. MINE. Found by a plan review run BEFORE the work rather than after** | **SILENT-DIRECTION defect in the containment mechanism itself.** The exemption is keyed on `cr` (the syntactic first segment a consumer writes, `scan.rs`); the ceiling lookup was keyed on `real` (the resolved package name after `dep_renames`). `CALIBRATED_CEILINGS` is populated from CALIBRATED_CRATES IDENTIFIERS, so `("native_tls_crate", "0.2.18")` sat under a key `real` can never hold — the ceiling could never fire while `cr` still took the exemption. **The first repair was ALSO wrong and an executed fixture caught it:** checking each key against itself finds a version with no ceiling and a ceiling with no version, and concludes "not beyond review". Under a `package =` alias the lock records `native_tls` while the ceiling is under `native_tls_crate` — the spaces must be CROSSED. Executed both arms: a consumer aliasing native-tls at 0.9.9 against a 0.2.18 ceiling was ABSENT (silently exempt) and now reports `invisible: ["native_tls_crate"]`. **Second defect, same commit:** `registry_lock_versions` read only `{dir}/Cargo.lock`, so a WORKSPACE MEMBER got no version and kept the exemption; `find_workspace_root` already existed for exactly this and was not consulted, and the comment calling the lockless case an accepted residual is what stopped it being measured. | **FIXED.** Version looked up under either key, ceiling tested under both, either one exceeding is enough — the disagreement between two key spaces must never be what grants silence. Workspace root consulted when the member has no lock of its own. 29/29 gates. **Standing lesson: when a fact is keyed twice, the bug is not in either lookup — it is that nobody crossed them.** |
| R323 rust: R304's `one_entity` guard falls back to SUPPRESSION, so an ordinary shadowed or sibling-scope closure name stays silent | 2026-09-07 | **OPEN — PRE-EXISTING, not a regression: measured ABSENT on the pre-fix binary too. Filed because the guard's FALLBACK DIRECTION was never stated, and it is the cardinal-sin direction** | **SILENT UNDER-REPORT, rust, residual of R303/R304.** The R304 guard requires `name_bindings[name] == 1`; when that fails the body is pushed to `escapes`, i.e. SUPPRESSED. So `let c = \|\| H::new("a"); c(); let c = \|\| H::new("b"); c();` — two bindings of one name, both invoked and discarded, **both dropping in frame** — reports ABSENT, as does the sibling-block spelling `{ let c = ..; c(); } { let c = ..; c(); }`. Executed on both binaries: ABSENT before the fix and ABSENT after; the single-binding control correctly moved ABSENT → `['Fs']`. **The point of the row is the framing error, not the cells:** my own review brief asked which BINDING FORMS the guard might miss — the fabrication direction — when the question that mattered was *in which direction does `one_entity == false` fail?* It fails silent, on legal everyday code. | Not fixed. Same root as R305: the counters key on a bare identifier with no scope, so two bindings of one name are indistinguishable from one name used twice. The real repair is per-SCOPE keying in the escape model, which is the same model change R189/R195/R297 need — do not patch it locally. Until then the residual is stated rather than implied, which is the whole reason this row exists. |
| R333 rust: the FALLIBLE draw verbs `try_next_u32`/`try_next_u64` are unmatched — the natural API on rand 0.10's `SysRng` reads PURE | 2026-09-07 | **CLOSED — candor-rust `92d9f35`. Status corrected 2026-09-08 (the remedy column already said FIXED)** | **CARDINAL SIN, rust.** `rand_core` 0.10.1 defines two traits: `RngCore` (`next_u32`/`next_u64`/`fill_bytes`) and **`TryRngCore` (`try_next_u32`/`try_next_u64`/`try_fill_bytes`)**. The rule matches only the first three by suffix, and `ends_with("::next_u32")` does NOT match `::try_next_u32`. Since `SysRng` is a FALLIBLE RNG, the `try_*` forms are its natural API. Executed on the current binary: `r.try_next_u32()` and `r.try_next_u64()` are **ABSENT**, while the `next_u32`/`fill_bytes` controls are `['Rand']`. (`try_fill_bytes` IS charged, by a different route — worth understanding before patching, because whatever catches it may be the better general fix.) | Not fixed at filing — candor-rust was held by review agents. Charging direction: add the three `try_*` suffixes. **R320's framing was wrong and this row supersedes that half of it:** R320 concluded the dead `OsRng` clause cost nothing because "the verb matches still catch a direct draw". They catch the INFALLIBLE ones. The rename from `OsRng` to `SysRng` came with a move to a fallible trait, and the rule tracked neither. **The general shape, third instance today after R319 and R321: an upstream rename is rarely just a rename — it arrives with an API shape change, and a suffix list written against the old shape silently stops covering the new one.** **FIXED — candor-rust `92d9f35`.** Both `try_next_u32` and `try_next_u64` now charge `Rand`, with the infallible controls unchanged. **The fix took two attempts and the first one is the lesson: TWO VERB LISTS answer the one question "is this a draw?" — one in the `rand_core` block and one in `rand`'s — and widening only the first moved nothing.** Correct code on a path the call does not take, which is byte-identical from the outside to a fix that does not work. That is the same failure as tonight's R197 attempt, twice in one evening, and both were caught only by re-measuring rather than by assuming the edit had landed. |
| R330 rust: my own R318 fix FABRICATES on password-hash 0.5.x — `ends_with("::hash_password")` charges `Rand` on the EXPLICIT-SALT, deterministic call | 2026-09-08 | **CLOSED — candor-rust `7b06dc5`. Fixed by ARITY, not by version** | **FABRICATION, rust, self-inflicted, and version-bound.** R318's premise holds only at password-hash **0.6.x**, where `hash_password(&self, password)` is the one-argument default whose body is `try_generate_salt()?`. At **0.5.0** (`traits.rs:33`) and argon2 **0.5.3** (`lib.rs:572`) the same NAME is `hash_password(&self, password, salt)` — the caller supplies the salt and the call is deterministic recomputation. The `ends_with`-over-`contains` guard I relied on is keyed on the 0.6.x name, and at 0.5.x the explicit-salt call IS named `hash_password`. Executed, compiling fixture: two calls with the same stored salt produce byte-identical hashes (`EQ=true`) while `SaltString::generate` differs; `rehash_with_stored_salt` (argon2 0.5.3), `scrypt_explicit_bound`, `pbkdf2_explicit_bound` and the UFCS spelling all went pure → `["Rand"]`. The 0.6.1 half is correct and separately verified. | Not fixed. **The structural finding is worth more than the row: `calibration_exceeded` is ONE-SIDED — it fires only when `resolved > ceiling`, so a ceiling can never see a rule that mis-describes an OLDER version. A version ceiling catches rules that go stale FORWARD; a rule rewritten FOR a new API silently mis-describes the old one, and nothing in the R311 design looks in that direction.** Four of the 70 ceiling rows sit on exactly this cliff. Remedy needs the classifier to see the resolved version — which R311's `lock_versions` now makes possible for the first time — or the clause must be narrowed to a spelling unique to 0.6.x. **FIXED by a THIRD route neither of those named: the call-site ARITY.** Both signatures are `hash_password`, so the path cannot separate them; but 0.5.x is `(password, salt)` and 0.6.x is `(password)`, and the argument count is written at every call site and needs no lockfile. `Call` gains `argc` (cache format rev24 -> rev25), the collector records it at the two visitors that have an argument list, and `candor_classify::password_hash_explicit_salt` adjudicates it. `syn` gives a method call its args WITHOUT the receiver and a UFCS `Trait::method(&recv, ..)` WITH it, so the count is normalized against the `method` flag the `Call` already carried — without that, one of the two spellings keeps fabricating. **The version route was considered and rejected for the reason this row itself found:** `calibration_exceeded` is one-sided, so a ceiling can never see a rule that mis-describes an OLDER release, which is precisely this direction; and it would answer nothing where no lockfile resolves. **Direction, stated so it cannot drift:** `classify` still charges `::hash_password` unconditionally and the arity only NARROWS that charge. An unrecorded arity (`argc == 0` — every synthesized edge, and every cache entry written before the field existed) leaves the charge standing, so the refinement can remove a fabrication but can never create a silence. The `argc == 0` sentinel is only sound because neither signature is reachable with an empty argument list under either spelling; a test asserts that direction rather than assuming it. **A/B, and the value figure is smaller than the defect and worth recording as such.** Falsified against a pre-fix binary built from `92d9f35` in a worktree: the two 0.5.x spellings move `['Rand']` -> ABSENT while the three that must charge (`v06_method`, `v06_ufcs`, `with_rng`) are unchanged. On a real-crate corpus (`bin/corpus-ab.py`, three cloned argon2 consumers — rauthy, atuin, vaultwarden — 3,856 rows) the branch REACHED real code **6 times across 2 entries** and the row diff is **ADDED 0 / REMOVED 0 / CHANGED 0**. That is not a null result, it is the reason the fabrication survived review: in the dominant real idiom the explicit salt is minted one line above the call (`let salt = SaltString::generate(&mut OsRng); argon2.hash_password(pw, &salt)` — rauthy `utils/hash_password.rs:63`, atuin `handlers/user.rs:263`), so a legitimate `Rand` masks the fabricated one at function granularity. **The over-charge is only visible where the salt is LOADED rather than minted — the re-hash / verify-against-stored-hash path — which is exactly the fixture the reviewer built and exactly the call a security review is reading.** Also measured: a second A/B over the eight registry crates that DEFINE these APIs reached the branch zero times, because an intra-crate call is not a dependency call; recorded because a zero-reach A/B is safety-only and says nothing (`corpus-ab.py` refused it, correctly). **THE CLASS WAS SWEPT, not just the instance, and the boundary is stated because the sweep is imperfect.** The class is: a rule keyed on a NAME where the same name carries a DIFFERENT SIGNATURE in another release — Rust has no overloading, so the hazard is entirely cross-version, and it points BOTH ways (a rule written for the new shape fabricates on the old; one written for the old shape misses the new). Swept every `ends_with("::NAME")` and exact-path target in `candor-classify` against every calibrated crate with more than one version in the local registry cache: **50 rule-target names moved arity, of which the sweep's own known positive — `password_hash::hash_password` `{0.5.0: [2], 0.6.1: [1]}` — came back, which is the calibration that makes the rest readable.** Every other hit resolved to one of three non-defects, checked individually: a name belonging to a DIFFERENT crate's rule (the sweep is crate-blind — `::call` fired on `mysql_async` but the rule is scoped `crate_name == "ureq"`), a union across unrelated same-named methods in one crate, or a `#[cfg(test)]` helper (`rand::test::rng(seed)`, `rand-0.10.2/src/lib.rs:324`, which is not public API). **No second instance.** What the sweep CANNOT see, stated rather than implied: calibrated crates with only one version on disk, arity changes outside the versions present, and — the real limitation — it flags arity movement, when the defect requires the movement to change whether an ARGUMENT supplies the resource. That is why the entropy tier is where the class bites and the `Net`/`Db` tiers are not: `ureq::get(url)` is `Net` at any arity. |
| R331 rust: `TlsConnectorBuilder::build` is where native-tls's `Fs` actually happens, and no rule names it | 2026-09-08 | **CLOSED — candor-rust `aa8d881`. Both this row's MECHANISM and its PREVALENCE were wrong, and a plan review caught them; the fix is one line** | **CARDINAL SIN, rust.** `native_tls::TlsConnector::new()` is literally `TlsConnector::builder().build()` (`native-tls-0.2.18/src/lib.rs:481`), and `TlsConnectorBuilder::build` (`:451`) is where `imp::TlsConnector::new` — the openssl-backend cert-file/dir load — actually runs. Executed: `only_connector_new` → `["Fs"]`, `only_builder_build` → **pure**. The builder spelling is the DOMINANT real-world idiom whenever any option is set: `redis-1.6.0/src/aio/tokio.rs:128,134`, `sqlx-core-0.8.6/src/net/tls/tls_native_tls.rs:45`, `tcp-stream-0.34.14/src/native_tls_impl.rs:39`, `ureq-3.4.1/src/tls/native_tls.rs:146` — in all four the enclosing fn is only INCIDENTALLY covered by a neighbouring `TlsConnector::new` or `Identity::from_pkcs8`, so a fresh consumer doing only `builder().danger_accept_invalid_certs(true).build()` reads pure. Sibling checked before claiming a class: `TlsAcceptor::new`/`builder().build()` are NOT affected — `imp::TlsAcceptor::new` on openssl touches no filesystem, so their absence is correct. | Not fixed. Charging direction: add `TlsConnectorBuilder::build` to the same clause. Note it is a CROSS-PLATFORM union like its neighbours — `Fs` on the openssl backend, pure on security-framework and schannel — and the reviewer's wider point should be recorded with it: **no single target platform makes all three native-tls clauses true at once**, which the clause's comment does not say. **FIX ATTEMPTED AND REVERTED 2026-09-08 — the blocker is one level below the rule.** Adding `ends_with("TlsConnectorBuilder::build")` to the clause changed NOTHING, and probing rather than theorising shows why: `native_tls::TlsConnector::builder().build()` and its two-statement sibling `let b = TlsConnector::builder(); b.build();` are **both absent from `functions[]` entirely, with no `calls` recorded at all** — while `TlsConnector::new()` in the same file charges `Fs`. The scanner never types `TlsConnector::builder()` as a builder, so `.build()` resolves to no path and NO suffix can match it. The rule was correct and unreachable. Reverted rather than left in place, because a clause that can never fire is exactly the dead weight this register has criticised three times today (R320's `OsRng`, `etcetera::data_local_dir`, `jiff::Zoned::now_with`). Not fixed. **The real work is receiver typing for this builder chain, not a classifier suffix** — `ctor_type`'s builder-chain walk is guarded by a type-CHANGE blocklist, and something in that path declines `TlsConnector::builder`. Start by instrumenting which guard rejects it. **Third instance tonight of one failure mode — R197, R333's first half, and this — where correct code sat on a path the failing case never takes.** In all three the edit looked right, the suite stayed green, and only re-measuring the fixture showed the fix had not landed. A green suite after a fix is not evidence the fix fired. **CORRECTED AND FIXED 2026-09-09 — candor-rust `aa8d881`. BOTH halves of the paragraph above are wrong, and the correction is the finding.** **(1) The mechanism.** The revert concluded the scanner *"never types `TlsConnector::builder()`"* and that the repair needed receiver typing. Two errors in one sentence. The attempt matched `TlsConnectorBuilder::build` — a method on a foreign-RETURNED receiver, which genuinely cannot be typed without a return index for a foreign crate — while `TlsConnector::builder()` itself is a **PATH call**, recorded and classified exactly like `TlsConnector::new` beside it. Keying on the terminal instead of the constructor is also against this file's own convention (`Command::new`). And *"no `calls` recorded at all"* was read as evidence of untyped receivers when it is simply what a correct scan of an EXEMPT crate looks like: under calibration a call classifying `None` is dropped from the report. **(2) The prevalence.** Measured on identical fixtures whose only variable is the dependency's spelling: under plain `native_tls::` — **not** in `CALIBRATED_CRATES` — the builder chain is PRESENT with `invisible: ["native_tls"]`, a disclosure; under `native_tls_crate`, the `package =` rename that IS calibrated, it is ABSENT with nothing hidden. **The four consumers this row cited (redis, sqlx-core, tcp-stream, ureq) all write the plain spelling and were never silent. The live instances are the crates that RENAME it — reqwest and tungstenite.** R312 taught the RULE both names; the EXEMPTION still keys on the rename, and that gap is what turns a disclosure into a purity claim. **The fix is ` path.ends_with("TlsConnector::builder")`.** A/B over the 25 registry crates that write the surface — entries selected by grepping for it rather than hand-picked: 6,101 rows, ADDED 0, REMOVED 0, **CHANGED 8**, every one a catch. `reqwest-0.12.28 ClientBuilder::build` `[]` -> `['Fs']` and `reqwest-0.11.27` `['Env','Log']` -> `['Env','Fs','Log']` — **`reqwest::Client::builder().build()` is how essentially every Rust HTTP client is constructed, and it read the system trust store with no `Fs`**; `lettre TlsParameters::new` and `TlsParametersBuilder::build` `[]` -> `['Fs']`; `mysql_async Endpoint::make_secure` `['Net']` -> `['Fs','Net']`. **Two follow-ons, both produced by the fix exposing what it exposed.** lettre's enumeration held every `TlsParameters` variant EXCEPT the one named after the backend that does the disk read — the gate's oracle saw `Fs` on `new_native`/`build_native` the moment `builder` started charging; added, both spellings, with `_boring` deliberately left because `boring` is a crate this file has not read. And the R327/R329 ceiling test used this very builder chain as its *"unmatched call"* control — **a control built on a silent under-report stops being a control the moment the under-report is fixed**, which is how the test caught the fix rather than the fix quietly invalidating the test; replaced with `Certificate::from_pem`, unmatched for a reason recorded under R337. |
| R332 umbrella: `assert-audit.sh` was blind to this project's OWN safety vocabulary, on the commit where it mattered | 2026-09-08 | **CLOSED — candor `05edbcc`** | **INSTRUMENT defect.** `bash bin/assert-audit.sh candor-rust bbd7615^..bbd7615` reported *"no safety assertions added — nothing to defend"*. That diff asserts, in comments, *"is deterministic recomputation — correctly PURE"* and *"The explicit-salt family … stays PURE"* — and **one of them was FALSE** (R330, found by a different agent an hour later). `ASSERT_RE` carried proven/guaranteed/inert/by-construction and had no term for `pure`, the word this codebase actually uses when it means "I checked, it is safe". The commit added 117 lines and zero test assertions, which is precisely the `7ecda11` shape the tool exists to fail on. | **FIXED** by widening `ASSERT_RE` with the house dialect (`correctly pure`, `stays pure`, `remains pure`, `is pure`, `verified still`, `deterministic recomputation`). Verified both ways: it now flags both lines of `6a2622c` and fails that range, while a no-assertion commit still passes and the selftest plus release-test's §17 history fixtures stay green. **A detector blind to the local idiom returns the reassuring answer exactly where the idiom is load-bearing.** |
| R334 rust: the ENTROPY SOURCE passed as an ARGUMENT is invisible — `StdRng::try_from_rng(&mut SysRng)` reads pure, and so does the old `from_rng(&mut OsRng)` | 2026-09-08 | **CLOSED — candor-rust `0557f47`. The session's strongest A/B: 18 reach hits, 17 rows moved, 0 removed, and one of them is a KEY-GENERATION function in a real application** | **CARDINAL SIN, rust.** Every entropy rule in the `rand` block keys on the CALLEE — a verb (`from_os_rng`, `next_u32`, `fill_bytes`) or the source TYPE in receiver position (`contains("OsRng")`/`contains("SysRng")`). The idiomatic way to seed a userspace CSPRNG from the OS puts the source in ARGUMENT position, where neither key can see it. Executed on the current binary, one fixture, four rows: `StdRng::try_from_rng(&mut SysRng)` **ABSENT**; `StdRng::from_rng(&mut OsRng)` **ABSENT**; `StdRng::from_seed([0u8; 32])` ABSENT (correct — deterministic, and the control that shows why `from_rng` cannot simply be charged); `StdRng::from_os_rng()` `['Rand']` (the verb form, correctly charged). **Real instances, not a constructed shape:** `quinn-proto-0.11.17/src/endpoint.rs:81` — `StdRng::try_from_rng(&mut SysRng)` seeding a QUIC endpoint's RNG; `crypto-common-0.2.2/src/generate.rs:30` — `Self::try_generate_from_rng(&mut SysRng)`; `zmij` tests. On quinn-proto the enclosing `Endpoint::new` reports `['Unknown']`, so the draw is INCIDENTALLY covered by an unrelated unresolved call rather than charged — the R331 pattern; in a body with nothing else the same call is fully ABSENT. | Not fixed, and deliberately not guessed at. **The charging direction is NOT simply to add `::from_rng`/`::try_from_rng`:** their effect depends on the argument, `from_rng(&mut pcg)` is deterministic, and 802 `from_seed`/`seed_from_u64` call sites in the local registry cache say the deterministic population is the larger one. That is R330's exact shape one crate over — a name whose effect is decided by what is passed to it — and R330's own lesson is that the discriminator must be read, not assumed. The discriminator here is the ARGUMENT'S TYPE, which the scanner does not currently record: `Call` carries `str_arg` (a string literal at position 0) and now `argc`, but nothing about what an argument RESOLVES to. Precedent for reading an argument exists (`resolve_host_arg`, `positional_str_lit`), so the shape of the repair is: record whether any argument resolves to a path naming a calibrated entropy source, and charge on that. **Before building it, price it against the alternative** — a whole-crate `Rand` on `rand` would close this and every future spelling at the cost of fabricating on the deterministic majority, and the block's own comment says that over-report is exactly what the verb list was written to remove. Do not undo that without the number. **BUILT AND FIXED — candor-rust `0557f47`.** The whole-crate alternative was rejected on the number this row already carried: 802 `from_seed`/`seed_from_u64` sites locally. `Call` gains `entropy_arg` (cache rev25 -> **rev26**, so a warm cache cannot replay the old answer), `lang::args_name_entropy_source` reads the list where it is in hand, and `candor_classify::is_entropy_source_ident` keeps the knowledge in the classifier crate. The walk is **RECURSIVE and that is load-bearing** — the source arrives wrapped, and a match on the bare path arm alone passes the first spelling and misses the rest: `&mut SysRng`, a qualified `rand::rngs::OsRng`, position 1 in `ReseedingRng::new(1024, SysRng)`, and `SysRng::default()` as a constructor call are all in the fixture for that reason. **POSITION WAS THE FIX, NOT THE RULE — the third instance in two days of correct code on a path the failing case never takes.** Placed beside the other second effects (where the `Llm` and R338 blocks live) it measured completely INERT: the flag reached the call loop carrying the right path (`rand::rngs::StdRng::try_from_rng`) and the body never ran, because a dependency call resolving through `deps_idx` `continue`s long before it. **Only a probe at the loop's ENTRY showed it**; the suite was green either way. The fact is a property of the argument list alone, so it now sits at the top of the loop where no `continue` can skip it, and the test is falsified against BOTH the missing rule and the wrong position. **A/B — `bin/corpus-ab.py`, 8 entries / 5,190 rows: ADDED 1, CHANGED 16, REMOVED 0, reach 18 hits across 5 entries.** Every row is a genuine catch: `quinn-proto Endpoint::new` `['Unknown']` -> `['Rand','Unknown']` (the instance this row was filed on); **`atuin Key::generate` `['Unknown']` -> `['Rand','Unknown']` and `Key::try_load_or_generate` `['Fs','Unknown']` -> `['Fs','Rand','Unknown']` — a key-generation function in a real application that did not disclose entropy, which is the case this rung exists for**; `crypto_common Generate::try_generate` ABSENT -> `['Rand']`; and 12 rows inside `rand` itself (`random`, `random_range`, `fill`, `ThreadRng::default`, `ReseedingCore::*`), which seed from `SysRng` and are correct. **The over-approximation is deliberate and stated in the code**: naming one of these types as a value is charged wherever it appears in an argument list, including moves that do not themselves draw (`Some(OsRng)`). That is sound, and it is the only place candor can see it — a source handed into a struct field is drawn from in some later function whose own call list never mentions it. |
| R335 rust: redis's whole SENTINEL lookup surface read PURE at redis 1.6.0 — the crate's OWN reviewed ceiling, so the exemption applied and nothing disclosed | 2026-09-08 | **CLOSED — candor-rust `10846ff`. Found by triaging R302 against the ceiling instead of by row count: it is one of only a handful of that gate's 55 hard-effect entries that a consumer can actually reach BELOW its ceiling** | **CARDINAL SIN, rust.** redis's `sentinel` module is how a client finds the current master in a failover deployment; every lookup talks to the sentinel servers over TCP. `Sentinel::master_for` runs `find_master_address` — a `SENTINEL masters` round-trip — and its own doc says it *"will connect to the master node to verify that it considers itself master"*; `SentinelClient::get_sentinel_client` opens a client per address and issues `ROLE` until one answers. Executed on a 1.6.0-locked fixture, **twelve verbs ABSENT** in the receiver-typed spelling a consumer actually writes AND in the fully-qualified one: `Sentinel::{master_for, get_replica_clients, replica_for, replica_rotate_for}` plus their four `async_` twins, and `SentinelClient::{get_client, get_sentinel_client, async_get_client, async_get_sentinel_client}`. The neighbouring rules reached the module only by accident — `::get_connection`/`::get_async_connection` are suffix-matched, so `LockedSentinelClient::get_connection` was covered while `Sentinel::master_for` beside it was not. **1.6.0 is redis's REVIEWED CEILING**, so R311's containment does not apply and every one of them was a positive purity claim. | **FIXED — candor-rust `10846ff`, as a DENYLIST rather than the enumeration its neighbours use, and the shape is the finding.** Enumerating connecting verbs goes stale in the SILENT direction: a release that adds a lookup verb adds a purity claim, which is exactly how this hole appeared. Inverted — the module is `Db` except its config surface — a new verb is charged by default and only a new PURE verb costs a fabrication. **The exempt set is enumerated, not guessed:** every `pub fn` in `redis-1.6.0/src/sentinel.rs` is one of 33 leaf names and `build`/`new`/`set_*` are exactly the ones that open no socket (`Sentinel::build` parses URLs through `into_connection_info`; `SentinelClientBuilder::build` assembles a config struct). Both halves are asserted in one test — a denylist is only sound if its exempt set is right, and charging the module while exempting nothing would fabricate on every builder. The `contains("::Sentinel")` arm is **not** the loose match R321 punished: the module's entire public surface was read first, `pub mod sentinel;` is its only export path, and no `Sentinel`-named item or any of these verb names exists elsewhere in the crate. **REACH IS ZERO AND IS RECORDED AS SUCH — the third zero-reach A/B tonight.** `bin/corpus-ab.py` over 9 entries / 6,621 rows: 0/0/0 with the marked branch counting zero hits. No consumer of redis's sentinel module exists on this machine, and redis's own calls to it are intra-crate so they never reach `classify()`. The A/B is SAFETY-ONLY and proves only the absence of collateral. Value evidence is elsewhere and is stated so nobody has to re-derive it: the fixture in both spellings, the upstream source, and **the coverage gate's own oracle, which nominated six of these from a self-scan of redis carrying `effects='Clock,Fs,Ipc,Net,Unknown'`** — an independent instrument agreeing the surface is effectful. |
| R336 umbrella: `assert-audit.sh` missed a purity assertion a SECOND time, on a NEAR-MISS of its own vocabulary (`stay pure` vs `stays pure`) | 2026-09-08 | **CLOSED — candor `0c18e39`. Same failure R332 recorded, eight days later in the same session, which is the argument for changing the SHAPE of the list rather than adding another word** | **INSTRUMENT defect, second instance.** `bash bin/assert-audit.sh candor-rust 202386b^..202386b` reported *"no safety assertions added — nothing to defend"*. That diff asserts, in comments and in a test message, *"must stay pure"*, *"exactly the ones that touch no socket"* and *"assembles a config struct"* about redis's sentinel builders — the load-bearing claims of an entire denylist, whose exempt set is the only thing standing between the rule and a fabrication on every builder. `ASSERT_RE` carried `stays pure` and not `stay pure`. **A near-miss of the vocabulary reads exactly like an absence of assertions**, and R332's fix — adding the remembered sentences — could only ever postpone that. | **FIXED** by writing the alternatives as SHAPES: any inflection of `(is\|are\|be\|stays?\|remains?\|reads?) pure`, plus the whole family of absence-of-effect claims (`(touch\|open\|perform\|draw\|consume)(es\|s)? (no\|nothing)`, `no round-trip`, `no I/O`). **An absence-of-effect claim IS a safety assertion by definition**, which is why the wide form is right in this list and would be wrong in the test-file allowlist beneath it — that one fails safe by being NARROW (R158 measured 12 of 14 wrong credits when it was widened), this one fails safe by being WIDE. Verified both directions: it now flags `10846ff` (2 lines), the selftest's 6 cases pass, `release-test.sh` §17's real historical fixtures still hold (candor-ts `7ecda11` fails, its repair `c53baa2` passes), and a genuinely assertion-free commit (candor-rust `a875663`) still returns *"nothing to defend"* — no false positive from the widening. **It paid for itself in the same wave.** Sweeping the previous five candor-rust commits with the widened list caught `92d9f35` — R333's fix — landing a classifier change with a safety assertion and **no test anywhere in the range**; that fix had shipped verified by a throwaway fixture only. Closed by candor-rust `3f123ae`, which asserts BOTH rule blocks and is falsified against the `rand`-block half specifically, i.e. against the exact shape that made R333's first attempt look landed when it was not. |
| R337 rust: the native-tls Identity WRAPPERS read pure — a call taking only BYTES writes a keychain file on macOS, in reqwest and lettre | 2026-09-08 | **CLOSED — candor-rust `96cc97f`. The first A/B tonight that moves real rows: ADDED 2 / CHANGED 8 in a real calibrated crate** | **CARDINAL SIN, rust.** candor already charges `native_tls{,_crate}::Identity::from_pkcs8`/`from_pkcs12` `Fs`, and the reason is concrete: on the security-framework backend each creates a real `tempfile::TempDir` and writes a keychain into it — `native-tls-0.2.18/src/imp/security_framework.rs:93` (`identity.keychain`, from_pkcs8) and `:167` (`tmp.keychain`, from_pkcs12). **But candor classifies the call a CONSUMER writes, not the dependency body behind it**, so a consumer reaching that code through its idiomatic wrapper matched no rule at all — and in a CALIBRATED crate an unmatched path is a claim of reviewed purity with NO disclosure. Executed at each crate's reviewed ceiling: `reqwest::tls::Identity::from_pkcs12_der`, `::from_pkcs8_pem` (reqwest 0.13.4) and `lettre::transport::smtp::client::tls::Identity::from_pem` (lettre 0.11.23) all **ABSENT**. | **FIXED — candor-rust `96cc97f`, and the boundary was drawn by a sweep rather than around the two rows the coverage gate handed me.** Every caller of `native_tls{,_crate}::Identity::from_pkcs8\|from_pkcs12` in the local registry is reqwest (0.11/0.12/0.13, all spelling them identically), lettre, and `tcp_stream` — which is **not calibrated**, so its calls disclose rather than claim purity and it needs no rule. Suffix-matched, not exact-path, and that choice was load-bearing: elasticsearch reaches it through the ROOT re-export `reqwest::Identity::from_pkcs12_der`, which a `reqwest::tls::…` exact path would have missed entirely. Narrow on purpose, with the siblings asserted PURE in the same test because they are the fabrication control — reqwest's `Identity::from_pem` is rustls-only, a `Cursor` over a buffer (`tls.rs:373`), and lettre's `Certificate::from_der`/`from_pem` reach `native_tls::Certificate::*`, which imports items with no keychain and no temp dir (`security_framework.rs:206,212`). The `::Identity::` in lettre's suffix is what keeps `Certificate::from_pem`, twenty lines above it, out. **A/B, and after three zero-reach runs this one measures value: `bin/corpus-ab.py` over 7 entries / 6,850 rows — ADDED 2, CHANGED 8, REMOVED 0, every one in `elasticsearch`**, a calibrated crate that calls `reqwest::Identity::from_pkcs12_der` at `http/transport.rs:300` (8.19.0-alpha.1) and `:266` (8.5.0-alpha.1). `TransportBuilder::build` goes ABSENT -> `['Fs']`; its eight callers (`Transport::{default, single_node, cloud, static_node_list, sniffing_node_list}`) go `[]` -> `['Fs']` by propagation. Nothing else in the corpus moved. The propagation onto `Transport::default` is the engine's documented whole-body over-approximation — `build` reaches the import only when a client cert is configured — the same shape as the ureq `testserver` precedent already recorded in `classify`, not a new liberty. |
| R338 rust: a calibrated crate that builds a native-tls connector INTERNALLY discloses its transport effect but not the `Fs` cert-store read — redis, tungstenite, ureq, minreq | 2026-09-08 | **CLOSED — candor-rust `b331a92`. Filed OPEN, then priced the same evening: FOUR of the five crates are deliberately left, each on evidence, and the pricing is the deliverable** | **UNDER-REPORT OF AN EFFECT CLASS, rust — not a purity claim, which is why it is filed separately from R337.** candor charges `native_tls::TlsConnector::new` `Fs` because on the openssl backend it loads the probed cert file/dir from disk. Several calibrated crates construct that connector INSIDE a public entry point, so the consumer's call discloses the transport effect and omits the `Fs`. Executed at tungstenite 0.30.0: `tungstenite::client_tls`, `client_tls_with_config` and `connect` all report `['Net']` — present and disclosed, but a `deny Fs` gate passes over a real trust-store read. The mechanism is visible in one line: `wrap_stream` does `tls_connector.map_or_else(TlsConnector::new, Ok)` (`src/tls.rs:54`), so a caller who supplies no connector gets `TlsConnector::new()`. **The row the coverage gate nominated for this crate — `tungstenite::tls::encryption::native_tls::wrap_stream` — is UNREACHABLE from outside: `mod tls;` is private (`lib.rs:32`, only `client_tls`/`client_tls_with_config`/`Connector` re-exported) and `mod encryption` inside it is private too. Same for `sqlx_core::net::tls::tls_native_tls::handshake`: `mod tls_native_tls;` is private in `net/tls/mod.rs:13`.** So the gate named two rows no consumer can call, and the reachable form of the same defect is one level up. | Not fixed. **The sweep is done and it is what makes this a row rather than a patch:** callers of `native_tls::TlsConnector::new` in the local registry are `redis` (0.27.6 and 1.7.0, in `connection.rs` and three separate `aio/` backends — tokio, smol, async_std), `tungstenite`, `ureq` 2.12.1 (`ntls.rs:10`), `minreq` (`connection/native_tls_stream.rs:19`) and `tokio-websockets`. Each needs its own reading of WHICH public entry point reaches the construction and under what condition — redis's are behind `Client::get_connection` (already `Db`, not `Fs`), tungstenite's behind `client_tls*` and only for `Mode::Tls`. **Charging the entry points is the sound direction and will over-approximate on plain-`ws://`/non-TLS users, which is the same trade R337 already accepted;** price it per crate rather than adding one broad rule. Cross-reference R331: `TlsConnectorBuilder::build` is the third spelling of this same surface and is blocked one level below the classifier, in receiver typing. **PRICED AND FIXED — candor-rust `b331a92`, and the two-line rule is the RESULT of the pricing, not a shortcut past it.** Added as a SECOND effect beside the model-SDK `Llm`+`Net` block with the same guards, because `classify` returns one effect and this is the identical drop that block already exists to prevent — not a new mechanism. **Four of the five crates are deliberately left, each on measured evidence:** `minreq` and `tokio_websockets` are **NOT calibrated**, so their calls already disclose rather than claim purity and no rule can improve them; **redis** builds the connector only for a `rediss://` address (`connection.rs:932`) while the consumer-visible verb is `Client::get_connection`, so charging it would fabricate on every PLAIN redis user — the majority — and was rejected on the trade; **ureq** takes `default_tls_config()` at `agent.rs:265`, and its default features are `["tls", "gzip"]` with `native-certs` **OFF**, so the default build resolves to `rtls::root_certs()`'s `#[cfg(not(feature = "native-certs"))]` arm — `webpki_roots::TLS_SERVER_ROOTS`, **compiled in, no disk read** (`rtls.rs:82`) — and charging it would fabricate on the default build. What remains is tungstenite's two explicitly-TLS-named entry points, where no caller is over-charged: `client_tls` passes `None` for the connector unconditionally and `client_tls_with_config`'s callers are doing TLS by definition. Measured: both move `['Net']` -> `['Fs','Net']` while `connect` and `client` stay `['Net']`. **`tungstenite::connect` is a STATED RESIDUAL, not an oversight** — it serves `ws://` and `wss://` alike, so a blanket charge would hit real plain-WebSocket users; the scheme is usually a literal candor already captures in `str_arg`, which is the shape a future fix should use. The test asserts BOTH effects rather than only the new one: a second-effect mechanism that REPLACES instead of adding is precisely the gate evasion the `Llm` block guards against, and it would read as a pass if only `Fs` were checked. A/B safety-only and said so — 8 entries / 5,056 rows, 0/0/0; no consumer of `tungstenite::client_tls` exists in the local registry, and redis's apparent `client_tls` hits are a struct FIELD (`tls.rs:27`), not a call. |
| R339 umbrella: `assert-audit.sh`'s PROSE arm has now missed THREE times — the approach is retired, not reworded | 2026-09-08 | **CLOSED — candor `47d71a4`. Third instance of one failure in one session; the fix is a STRUCTURAL arm that does not read prose at all** | **INSTRUMENT defect, third instance, and the count is the argument.** R332 widened `ASSERT_RE` with the house dialect after it reported *"nothing to defend"* on a commit carrying a FALSE purity assertion. R336 rewrote the alternatives as SHAPES after it missed `stay pure` where the list had `stays pure`. **The very next commit, `b331a92`, asserted *"COMPILED IN, no disk read"* and *"no caller is over-charged"* and matched nothing again.** The space of ways to assert safety in prose is open-ended, so a prose detector's negative can never mean *"nothing was claimed"* — and its negative is exactly the answer that gets believed. A fourth widening would have been a fourth wording. | **FIXED by adding an arm that does not depend on prose.** In this family the thing being asserted is almost always the same: **an effect-classification RULE changed** — which is structural, and is the question the tool actually wants answered (*you changed what candor believes about an effect; where is the fixture that fails if you got it wrong?*). **Measured against real history rather than argued: candor-rust `a875663` added 24 calibration-ceiling rows with no test and no assertion prose, and the tool returned "nothing to defend".** A neighbouring commit then REWROTE that same table and silently dropped nine entries — R328 — so the structural arm flags exactly the family of commits in which that happened. Deliberately NARROW, one file per engine (`candor-classify/src/lib.rs`, `Classifier.java`, `Classifier.swift`, `scan.mjs`), because breadth is not free in an arm that FAILS a commit: a merely rule-ADJACENT path such as candor-scan's `scan.rs`, which dispatches rather than decides, would make the tool noisy and then ignored. Comment-only and blank additions do not count — documenting a rule asserts nothing new about it. Three selftest cases cover both directions of the new arm plus the comment-only control (9 total). Swept over real history: `a875663` now fails, `b331a92` passes on the rule arm **and says which arm decided it**, a test-only commit still reports nothing to defend. `release-test.sh` §17's historical fixtures still hold (candor-ts `7ecda11` fails, its repair `c53baa2` passes), and §17's own label — which read *"6 cases"* — was refreshed rather than left to expire quietly, which is the stale-comment variant this register has recorded twice. |
| R340 rust: the feature sets `#[cfg]` evaluation reads are a PROCESS GLOBAL, and `cargo test` scans in parallel — a cardinal-sin fix failed 2 runs in 3 while passing alone | 2026-09-09 | **CLOSED for the test harness — candor-rust `5588330`. The PRODUCTION claim is verified, not assumed, and is recorded here so the next person does not have to re-derive it** | **INSTRUMENT defect, rust, found by the R140 fix rather than by looking for it.** `lang::CFG_FEATURES` is a process-global `OnceLock<RwLock<FeatureSets>>` holding the (active, declared) feature sets. `scan_one` writes it once per crate; `is_cfg_inactive` reads it deep inside parsing, and returns `false` outright when `declared` is empty. **`cargo test` runs test functions in parallel threads in ONE process**, so two fixture scans race on it: a neighbour's `set_cfg_features({})` landing mid-scan empties `declared`, `is_cfg_inactive` answers `false` for everything, and a feature-gated fixture silently reverts to the pre-fix behaviour. Measured: the R140 test failed **2 runs in 3** in the full suite and passed **every** time it was run alone. **That is the worst shape a test can have in this repo — a flaky failure among 421 gets re-run until green rather than diagnosed, and what it would have been hiding is a cardinal-sin fix that had stopped working.** | **FIXED for the harness, and the production question was ANSWERED rather than waved at.** The global's own comment claims `set_cfg_features` is *"called once per `scan_one`, which runs sequentially per workspace member, before its parallel Pass B reads them"* — checked, and true: the parallelism inside a scan is Pass B, entirely within one member's scan, so the shipped binary never has two feature sets in flight. **This is a test-harness defect only, and the distinction is the finding: the same global is safe in one process and racy in the other, purely because of who calls it.** The repair is a `SCAN_SERIAL` mutex taken by the fixture HELPERS, at all 83 scan sites — **not by the one test that needs it**, because a mutex held by the victim protects nothing: it is the neighbours' scans that clobber the cell. Verified 5 runs in 5 green, and the suite got *faster* (0.71s -> 0.33s) from the reduced contention. **The standing hazard this leaves:** any future test whose assertion depends on `[features]` inherits the same trap, and would have been flaky rather than red. The lock is in the helper so that inheritance is automatic. A stricter repair — making the feature set travel with the scan instead of living in a global — is a hot-path change with no production defect behind it, and is deliberately not attempted. |
| R341 rust: an effect HANDLE consumed as an ARGUMENT rather than as a receiver is invisible — the UFCS spelling of the very same call reads pure | 2026-09-09 | **OPEN — filed with a committed fixture and a price, deliberately NOT fixed: the discriminator is the argument's TYPE, which needs a new channel, and the obvious cheap fix would fabricate** | **CARDINAL SIN, rust.** `Fs` and `Net` on a std handle are reached through a `Read`/`Write` TRAIT method. Only the RECEIVER position is typed, so the UFCS spelling of the identical call — which names the trait and puts the handle in argument position — resolves to nothing. Executed on a committed fixture (`candor-spec/fixtures/r341-handle-argument`), **`reported: 5 of analyzed 10`**: `f.read_to_string(..)` on a `File` param is `['Fs']` and `Read::read_to_string(&mut f, ..)` is **ABSENT**; `s.write_all(..)` on a `TcpStream` param is `['Net']` and `Write::write_all(&mut s, ..)` is **ABSENT**; `std::io::copy(&mut f, sink)`, `serde_json::from_reader(f)` and `BufReader::new(f).lines()` are all ABSENT. **Exec is NOT affected, and that is the diagnostic half:** `std::process::Child::wait_with_output(c)` charges `['Exec']` in argument position because Exec's rule keys on the callee path's TYPE PREFIX, which matches either way. The hole is exactly where the effect is reached through a trait whose path names the trait instead of the handle's type. Prevalence in the local registry, by shape: 163 crates wrap a value in `BufReader`/`BufWriter`, 34 write `io::copy(&mut …)` (cap-primitives, seahash, futures-rustls among them), 3 write a trait-path UFCS read/write. | Not fixed, and the cheap fix is the trap. **R334's mechanism does not transfer**: it matches an IDENT (`OsRng`/`SysRng`) because the OS entropy source is a value with no acquisition call, whereas a `File` HAS one — `File::open` is already charged — so charging every mention of a `File`-typed value would double-count, and `BufReader::new(cursor)` would fabricate `Fs` on an in-memory buffer. **The discriminator has to be the argument's RESOLVED TYPE**, from `seed_vars` (param -> `std::fs::File`), joined with a small callee list; that is a new channel on `Call` in the shape of R330's `argc` and R334's `entropy_arg`, and it should be priced before it is built. **One caveat to price with it:** `serde_json::from_reader` is a REVIEWED-PURE crate entry (`classify` `lib.rs:306`), and its recorded justification — that the caller had to obtain the handle — holds only when open and read happen in the SAME function. For a handle arriving as a PARAMETER it does not, and that comment is a safety assertion on the exact line a fix would touch. Write the second fixture first. |
| R342 rust: `resp.text().await` read PURE — the response BODY is drained off the socket and only `send()` was charged | 2026-09-09 | **CLOSED — candor-rust `a62ae1e`. Found by the coverage gate the moment R185 landed; three crates plus one wrapper level up** | **CARDINAL SIN, rust, on one of the most-written lines in Rust networking.** `send()` is charged and the methods that DRAIN what it returned were not, so a helper that takes a `Response` and reads it — an extremely common shape — was a positive purity claim on a CALIBRATED crate. Executed at reqwest 0.13.4: `r.text()`, `r.json()` and `r.bytes()` all ABSENT while `c.get(..).send()` was `['Net']`. Verified in source rather than assumed: `text` -> `text_with_charset` -> `bytes` -> `do_bytes`, which is `BodyExt::collect(self.res.into_body()).await` (`async_impl/response.rs:433`). Two siblings arrived in the same gate output and were each read before charging: **`rusqlite::Rows::next`** is `self.advance()?` (`row.rs:40`), i.e. `sqlite3_step` — iterating a result set is how sqlite is READ, and it carried no `Db`; **`grep_cli::DecompressionReader::close`** is `match self.rdr { Ok(ref mut rdr) => rdr.close(), .. }` (`decompress.rs:383`), the already-enumerated `CommandReader::close` one level up reaping the same child — its counterpart was listed and it was not, measured on a consumer fixture as `['Exec']` vs ABSENT. | **FIXED — candor-rust `a62ae1e`, plus ONE WRAPPER FURTHER UP because the fix exposed it the same way:** elasticsearch's `Response::{json,text,bytes,exception}` are `self.response.json().await` (`http/response.rs:102`), reqwest's drain, so `client.search(..).send().await?.json().await?` had the send charged and the read silent. **SCOPING IS THE PART TO REVIEW.** `::text` and `::json` are far too common a leaf to match crate-wide, so both rules require `Response::` in the path and the PURE ACCESSORS on the same types are asserted in the test — reqwest's `status`/`headers`/`url`, elasticsearch's `status_code`/`content_type`. That line is not invented here: this file's own exclusion list already documents `elasticsearch::Response::content_type` as *"a pure accessor reading a header off an ALREADY-received response"*. The body methods are not accessors. Both grep_cli entries are FQN-exact for the R321 reason — `::read` and `::close` are the last leaves that should be matched by suffix. **A/B, entries selected by grepping for the surface: 260 crates, ADDED 10, REMOVED 0, CHANGED 8** — every one a wrapper delegating to the newly-charged call (axum's test-client `TestResponse::{text,json,bytes,chunk}`, elasticsearch's own `Response::*`). **The coverage gate now reads 0 regressed / 0 oracle-dropped / 0 GROWN: the cascade R185 started terminates here**, covered 1360 -> 1378, open 567 -> 562. Three rounds of "closing a silence makes the next consumer's gap visible" (R302, R337, this) and this is the first one that reached a fixed point inside the same session. |
| R343 spec: 22 register rows carried UNESCAPED PIPES, silently shifting every column right — and the audit that reads those columns ran over them the same night | 2026-09-09 | **CLOSED — candor-spec `83ba341`. Folded into the EXISTING checker rather than shipped as a second one** | **INSTRUMENT defect, and it corrupts a reader rather than a renderer.** `SOUNDNESS.md` is consumed BY COLUMN — the stale-status audit run earlier the same night reads column 3 to decide whether a row is open. A row containing an unescaped pipe inside a cell (`\|h\|`, `matches!(a \| b)`, `\|\| path.ends_with(..)`) splits that cell and shifts every column after it one to the right. **Nothing errors and the table still draws**, so the defect is invisible from the rendered page and fatal to anything indexing by position. Measured: **22 of 275 rows**. Sixteen were plain code-span pipes. Five (R162, R197, R201, R213, R331) had an EXTRA remedy cell, appended over time as a new COLUMN rather than as more text in the existing one. One (R215) had **no trailing pipe at all** and had been counting as four columns in a five-column table since 2026-09-04. **Two were written that same session by the agent that then found them.** | **FIXED, and the shape of the fix is the point: it went into `scripts/check_soundness_tables.py`, which already existed, rather than into a new `bin/register-lint.sh` — which is what I wrote first and deleted.** Two scripts answering *"is the register well-formed"* is the two-paths-computing-one-fact shape this register keeps recording. **The existing checker was NOT wrong and its docstring says so in terms:** it checks CONTIGUITY — that a row renders as a row at all — after this file shipped a broken table five times, and it warns explicitly that *"pipe counts were never the failure"* and that a validator measuring the wrong property *"reports green over exactly the defect it was written for"*. That warning is about RENDERING and it still holds. Cell count is a second, independent property about READING, and it is now checked after contiguity passes — a row that does not render has no cells to count. **Each row is checked against the header of the table it is IN.** The file has five tables with different widths; the first audit assumed one and produced **44 false positives** before that was noticed. Both halves are falsified: injecting a code-span pipe fails naming the row, injecting a blank line mid-table fails with the contiguity message, and the clean file exits 0. **One more thing worth keeping.** The mechanical repair pass — escape pipes inside backtick spans — fixed sixteen rows and MISSED the two written that night, because an unbalanced backtick earlier in a row flips the in-code parity for everything after it. A repair that is right for most instances and silently wrong for the rest is the same shape as the defects this register tracks. |
| R344 swift: an OPTIONAL payload bound by a `case` pattern was never typed — `if let` charges and all four MATCH spellings are silent | 2026-09-09 | **CLOSED — candor-swift `fb9fabd`. Found by asking whether the sibling engines had R185's hole, which is the question R308's row says to ask and nobody had** | **CARDINAL SIN, swift.** `if let h = o { h.run() }` types `h` and charges `Fs`. Every MATCH spelling of the same unwrap did not: `switch o { case .some(let h) }`, `case let .some(h)`, the same with a `default:` arm, and `if case let .some(h) = o` all left `h` untyped, so the enclosing function was **ABSENT from `functions[]`** over a real file write. One fixture, the spelling as the only variable; `deny Fs` **exit 0** and `pure` **exit 0**, with nothing disclosed — no `unanalyzed`, no `unresolved`, absent entirely. **The cause was already written down one comment above the site.** `visitPost(SwitchCaseItemSyntax)` states that the pattern *"is walked by `typeEnumCaseBinding` (which CLEARS the binding — `enumCaseValueType` has nothing to say about `Optional.some`)"*, and the two branches that follow answer only the CALLABLE form (R178) and the CONTAINER form (R269). **The concrete nominal payload had no branch at all** — the same asymmetry candor-rust closed as R185 the same day, one engine over: the dispatch route for an unwrap binder existed and the concrete one did not. | **FIXED — candor-swift `fb9fabd`, at TWO sites, and the second is the one that would have been missed.** Fixing `SwitchCaseItemSyntax` alone turned three of four spellings green and looked finished; `if case let .some(h) = o` reaches `MatchingPatternConditionSyntax` instead. That site needed the branch placed OUTSIDE its `casePayloadLocals` guard — **probed rather than reasoned**: `typeEnumCaseBinding` CLAIMS the binder into that set during the pattern walk and then has nothing to say about `Optional.some`, so a guard whose purpose is to stop a later pass UNDOING a real typing was skipping the one case that had none. `vars[bound] == nil` is the honest precondition and can undo nothing. **THE CORPUS A/B CAUGHT AN OVER-REACH, and it is the most useful thing in this row.** The first cut typed the single binder of ANY arity-1 case pattern from the subject's root type — correct for an Optional, where `rootOf` gives `T`, and WRONG for every other enum, where the binder is the PAYLOAD and the subject's type is the container. swift-nio's `EventLoop.makeCompletedFuture(_ result: Result<Success, Error>)` is `switch result { case .success(let value) … }`, and typing `value` as `Result` **cost that function its `Env`: REMOVED 1** — the direction that must never be waved through. `isOptionalUnwrapPattern` is the TYPE check the arity-1 test only looked like. With it the same A/B is 0/0/0, and the three ADDED rows the over-reaching version produced were artefacts of it too. **An arity guard is a shape check and was being read as a type check.** **REACH MEASURED, not inferred.** Alamofire + swift-nio + swift-argument-parser: ADDED 0 / REMOVED 0 / CHANGED 0 with the branch instrumented and **reached 6 times** (swift-nio 5, swift-argument-parser 1). Reached-and-inert is a different result from never-reached, and the counter exists because a grep found 96 occurrences of the pattern while the diff moved nothing — the two questions have different answers. The fabrication control (a PURE payload through the same patterns) is in the test, and both branches were reverted in turn with the suite going red for each. 1126 swift tests, fuzz, fabrication-probe, self-gate and the recall oracle green. **THE FOUR-WAY SWEEP IS FINISHED, which is the question R308's remedy said to ask and nobody had.** Executed on the analogous fixture in each engine rather than reasoned about: **rust** had it (R185 — five binder sites, fixed 2026-09-09); **swift** had it (this row — two sites); **java is IMMUNE, structurally**: `ifPresent`, `isPresent()`+`get()`, `orElse(null)`+null-check and a bare null-check all charge `Fs`, because the engine reads BYTECODE, where the call is `Guard.run()` however the source unwrapped it — the source PATTERN, which is the whole substance of this class, does not survive to the classfile. That is a concrete reason the JVM engine is the family's reference and it is worth having measured rather than assumed. **ts is immune too**, and not for java's reason: all six narrowing spellings — `if (this.o)`, `this.o?.run()`, a local narrow, `!`, a ternary, and a destructured narrow — charge `Fs`, with the pure control absent. **So the class is two engines wide, not four, and both are now closed.** Recording the negative arms matters as much as the positives: without them the next reader has to re-derive whether java and ts were skipped or checked. |
| R345 rust: `.as_ref()` was missing from the element-preserving adapter list, so R185's fix did not cover the idiom most Rust actually writes | 2026-09-09 | **CLOSED — candor-rust `13ed99f`. A hole in a fix shipped HOURS EARLIER the same day, found by testing the sibling spelling rather than the one the fixture used** | **CARDINAL SIN, rust, and it is R185's own residual.** `resolve_elem_type`'s adapter list held `iter`, `into_iter`, `iter_mut`, `clone`, `drain`, `as_slice`, `to_vec`, `values`… and **not `as_ref`**. So `if let Some(h) = &self.o` charged `Fs` (R185, that morning) and `if let Some(h) = self.o.as_ref()` read **ABSENT** — the same statement one idiom over, and `.as_ref()` is how most Rust spells it because `&self.o` does not compile where the payload is moved. Eight spellings measured, seven silent: `o_if_some`, `o_map` (`self.o.as_ref().map(\|h\| h.run())`), `o_and_then`, `r_map` all ABSENT -> `['Fs']`. **`self.o.iter().for_each(..)` charged ALL ALONG, and that is what located the defect.** Two spellings of one question with two answers, differing only in which adapter is on a list — which points at the LIST, not at the binder R185 had just fixed. Without that pair the obvious next move is another binder site, which would have been correct code in the wrong place. | **FIXED — candor-rust `13ed99f`:** `as_ref`/`as_mut`/`as_deref`/`as_deref_mut` join the list. A/B over 500 registry crates that write `.as_ref()`: **ADDED 33, REMOVED 0, CHANGED 83** (4 with an effect delta); 28 of the 33 carry no effects at all. Coverage gate 0/0/0. **I NEARLY BACKED IT OUT AS A FABRICATION AND WAS WRONG, which is the most useful thing in this row.** `anyhow::BacktraceSymbol::fmt` moved `[]` -> `['Env']`, and READING THAT FUNCTION shows it only formats — no env access anywhere in it. I wrote it off as an over-charge. Following the new EDGE instead of stopping at the function: typing `fname` from `self.filename.as_ref()` lets the `{:?}` implicit-stringification edge reach `BytesOrWide::fmt`, whose entire body is `output_filename(.., env::current_dir().as_ref().ok())`. **The `Env` is real and was previously silent.** Reading a function and stopping is the failure this register keeps recording in other people's work; here it nearly cost a genuine catch, and the only thing that saved it was diffing the `calls` list rather than re-reading the source. **A charge that looks like a fabrication and a charge that is one are the same bytes until you follow the edge.** |
| R346 rust: the element-preserving adapter list is an ALLOWLIST and carried four names — sixteen ordinary spellings between a collection and its closure were silent | 2026-09-09 | **CLOSED — candor-rust `0e6d8fd`. Found by asking what ELSE was missing from the list R345 had just added one name to** | **CARDINAL SIN, rust.** `resolve_elem_type` decides a binder's type by peeling element-preserving method calls, and its list is an allowlist — the shape this register keeps recording as going stale in the SILENT direction. With `self.v.iter().for_each(\|h\| h.run())` as the baseline (charges `Fs`), **sixteen ordinary spellings were ALL silent**: `.rev()`, `.take(n)`, `.skip(n)`, `.filter(..)`, `.peekable()`, `.chain(..)`, `.step_by(n)`, `.take_while(..)`, `.by_ref()`, and the `Option<&T>`-returning accessors `.first()`, `.last()`, `.get(i)`. **THE REAL INSTANCE IS A PROCESS SPAWN, and the body was read rather than the name trusted:** `execute-0.2.15`'s `execute_multiple` is `for other in others.iter_mut().take(n) { .. other.spawn()? }` — a real spawn reached through `.take(n)`. **Eight** of that crate's functions gained `Exec` from adding that one name. | **FIXED — candor-rust `0e6d8fd`.** Every name added yields the SAME element as its receiver, which is the only property this function is about. **The EXCLUSIONS are the deliberate half:** `map`/`flat_map`/`flatten`/`zip`/`enumerate` CHANGE the element and stay off, and `windows`/`chunks` yield a SLICE of the element rather than the element — typing the binder as `T` inside one would be a fabrication one index deep. **That residual is PINNED in the test**, so if it ever goes green someone must revisit the pin rather than quietly delete it. `first`/`last`/`get` return `Option<&T>` rather than an iterator and belong for the same reason: the Option's payload IS the receiver's element, so `if let Some(h) = v.last()` and `for h in v.last()` both want exactly what this returns. **A/B over 600 registry crates: ADDED 18, REMOVED 0, CHANGED 29.** Every effect-bearing movement is in `execute-0.2.15` and every one is `Exec` on a function that provably spawns. Fabrication controls in a separate fixture — a PURE payload through `get`-on-a-HashMap, `first`, `filter`, `take` and `rev` all stay pure, because an adapter TYPES the element, it does not CHARGE it. Coverage gate 0/0/0. **The sequence is the lesson and it is worth stating plainly: R185 fixed the binders, R345 found that its own fixture spelling hid `.as_ref()`, and R346 found that `.as_ref()` was one of sixteen. Each fix's fixture was the boundary of the next defect.** An allowlist is only as complete as the last person's fixture, and the cheap way to test one is to write the whole family down and run it, rather than to add the name in front of you. |
| R347 rust: the two element resolvers kept SEPARATE copies of the adapter list and diverged in BOTH directions — each divergence a live silence | 2026-09-09 | **HALF CLOSED — candor-rust `aace16d` makes it ONE list. The guard-chain half was built, measured working, and BACKED OUT because it costs a fabrication; it is PINNED in the test with the row to re-check** | **CARDINAL SIN ×2, rust, and the defect is the DUPLICATION.** `resolve_elem_type` (concrete elements) and `resolve_elem_trait_leaves` (trait-object elements) each carried their own copy of the element-preserving adapter list, and the second's doc claimed it peels *"exactly like"* the first — **a sentence already false when it was written** (it lacked `clone`/`to_vec`). Measured with each resolver as the OTHER's control: the CONCRETE list had no GUARD CHAIN, so `self.m.lock().unwrap().iter().for_each(\|h\| h.run())` over `Arc<Mutex<Vec<Guard>>>` read ABSENT while the identical statement over `Arc<Mutex<Vec<Box<dyn Doer>>>>` charged `Fs`; the DISPATCH list had none of R346's iterator adapters, so `vd.iter().rev().for_each(\|d\| d.go())` read ABSENT while `vd.iter().for_each(..)` charged. **Which silence you got depended on whether the element happened to be a trait object.** `Arc<Mutex<Vec<T>>>` is one of the most common shapes in Rust, which is what makes the asymmetry expensive. | **FIXED for the list itself — `lang::is_element_preserving_adapter`, one authority, both resolvers call it.** Neither list was wrong about its own entries; there were simply two, which is the §G shape this register keeps recording. **THE GUARD-CHAIN HALF WAS BUILT, MEASURED WORKING, AND BACKED OUT — and the price is the finding.** Closing it also needs `elem_type` to peel `Mutex`/`RwLock`/`RefCell` off the TYPE. That works, and the corpus A/B priced it: the peel makes the HOF closure route hand a container element to a closure whose parameter is NOT one. `async-process-2.5.0`'s `!self.zombies.lock().unwrap_or_else(\|x\| x.into_inner()).is_empty()` types `x` — a **`PoisonError`** — as the map's `ChildGuard` element, so `x.into_inner()` resolves to `ChildGuard::into_inner` and **`Reaper::has_zombies` is charged `Exec` for asking whether a map is empty**. A fabrication on a function that spawns nothing. **The closure route must learn that `unwrap_or_else`'s parameter is the ERROR before the type peel is safe.** Pinned in the test with that row named, so the next attempt knows exactly what to re-check rather than rediscovering it. **A/B after backing out, 600 registry crates: ADDED 7, REMOVED 0, CHANGED 10 — every effect-bearing movement is a gained `Unknown`**, the disclosure direction. Coverage gate 0/0/0. **THE SWEEP WAS FINISHED RATHER THAN STOPPED AT THE PAIR IN HAND, and it found two MORE copies — with a negative result worth recording.** `collector.rs:618` and `lang.rs:903` hold a second duplicated list, and it answers the OPPOSITE question: a type-CHANGE BLOCKLIST — *does this method yield a different type, so stop walking to the base receiver?* The two copies also diverge: `resolve_recv_type`'s has `get_program`/`get_args`/`get_envs`/`get_current_dir` and `ctor_type`'s does not, and the comment beside the first says exactly what those four prevent — `c.get_program().to_str()` walking THROUGH to the base `Command` and being charged `Exec` by the whole-type rule. **I could not construct a live instance and say so rather than implying one.** All four read-back accessors were executed against HEAD (`read_program`, `read_args`, `read_envs`, `read_dir`) and every one is correctly ABSENT, with `Command::new(..).status()` charging `Exec` as the control — the route that runs for that shape is `resolve_recv_type`'s, which HAS the four names. **So the divergence is LATENT, not live, and unifying it would be a behaviour change made on a guess.** Left as a recorded finding; R331 already points at `ctor_type` for a different reason and is where a fixture that reaches it would come from. **Four copies of two lists, two unified, two priced and left — that is the honest state, and the count is here so the next sweep starts from it.**|
| R348 swift: `lazy` — the ONE element-preserving adapter Swift spells as a PROPERTY — sat in the adapter list inside a function-CALL guard it can never satisfy | 2026-09-09 | **CLOSED — candor-swift `04d090d`. Found by taking R346/R347's question to the next engine rather than filing them and stopping** | **CARDINAL SIN, swift.** `elementTypeOf` peels element-preserving adapters so the closure parameter of a following `forEach`/`map` is typed from the receiver's element. Its list already contained `"lazy"` — and the entire arm is guarded on `FunctionCallExprSyntax`. **There is no `xs.lazy()` in Swift**: `lazy` is a property, every other member of that family is a method, so the entry could never match anything. Measured against `v.forEach { $0.run() }` as the baseline over an element that writes a file, the whole family was swept rather than the one spelling in hand: `reversed()`, `dropFirst()`, `dropLast()`, `suffix(1)`, `prefix(1)`, `shuffled()` and the `for`-in form of each ALL charged `Fs`. **`v.lazy.forEach { $0.run() }` alone was ABSENT** — a purity claim over a body that writes a file. **The name being present is precisely what stopped this being measured for as long as it was**: a reader auditing the list finds `lazy` there and moves on, which is the documented-limitation-is-not-measured shape with the comment replaced by a list entry. | **FIXED — a second arm for the member-access spelling.** Ordered deliberately AFTER the field arms: a user type may own a `[E]` field genuinely named `lazy`, and `fieldArrayElem`'s reading must win — only an UNRESOLVED `.lazy` reaches the new arm, so it can add element types and never replace a known one. Removing `"lazy"` from the call list is provably inert (`grep -rn 'lazy()'` over `Sources/` and `Tests/` is empty), which is why it is a removal and not a second dead entry left in place. **Falsified**: with the arm removed the new test fails on all five `lazy` shapes — `lazyForEach`, `lazyForIn`, `lazyThenFilter`, `lazyThenMap`, `reversedThenLazy` — while the non-lazy `baseForEach` baseline still PASSES, so the failure is the arm and not the harness. **Over-charge controls are half the row**, because a fix to a silence is where fabrication gets introduced: a PURE element through `.lazy.forEach`, an `Int` element, a `lazy` element read and never called, and the `for`-in form of the pure case all stay effect-free — an adapter TYPES the element, it does not CHARGE it. Reach on real Swift: 10 `.lazy.<method>` sites across 4 files in the SwiftPM checkouts (5 `.lazy.map`, 4 `.lazy.compactMap`, 1 `.lazy.forEach`). Gates: `bin/gate-run.sh candor-swift` 11 run / 11 ok / 0 failed. **R346, R347 and R348 are one finding in three engines' worth of spellings, and the sequence is the lesson**: R346 — the list was too SHORT; R347 — there were TWO lists and they diverged; R348 — the name was THERE and in a position that could not fire. A hand-maintained list of adapter names is the fragile part in every engine, and none of the three failures is the one you would look for by reading the list. The cheap test is to write the whole family down and RUN it. **THE ARC WAS THEN TAKEN TO THE OTHER TWO ENGINES, AND THE NEGATIVE RESULT IS THE USEFUL PART.** ts, 12 spellings — `reverse()`, `slice()`, `filter()`, `concat()`, `[...v]`, `Array.from(v)`, `sort()`, `flat()`, `for..of` over `reverse()`, `at(0)`, `find()` — **all 12 charge `Fs`**, pure control absent. java, 15 spellings — `stream()`, `.filter/.sorted/.distinct/.limit/.skip/.peek/.parallel`, `subList`, `Collections.unmodifiableList`, an explicit `Iterator`, enhanced-`for`, `collect(toList())`, `get(0)` — **all 15 charge `Fs`**, pure control absent. **Neither engine has this defect, and neither one has a hand-maintained adapter list**: java reads BYTECODE, where the lambda is a real method with a real descriptor, and ts asks the TypeScript checker. rust and swift both parse source with no type checker, so both MUST keep a list — and both lists were defective, in three different ways, found within one session of each other. **The class is not "an adapter list goes stale"; it is "an engine without a type checker has to hand-maintain what a checker would derive, and that hand list is the fragile part." That predicts WHERE to look — the two syntactic engines — and, just as usefully, where not to.** |
| R368 umbrella: both spec-leftover scans read GITIGNORED run artifacts and called them shipped source | 2026-09-09 | **CLOSED in `release-preflight.sh` during the ⟨0.36⟩ cut. FILED LATE — the ID was cited in three commits and in shipped code BEFORE this row existed, which is R366's finding repeated by the person who filed it** | On the cut, both scans reported 56 leftover `spec 0.35` strings; every one a `candor-rust/soundness/realworld/**/r.*.json` written by a local oracle run and ignored at that repo's `.gitignore:10`, while ZERO tracked files anywhere still said it. The checks' own message is *"in shipped source/docs/packaging"*, and an untracked artifact is none of those. It reds every release for a person to wave through, and a real leftover is then one line among dozens of known-benign ones — the burying the scan's own comment says it splits fixtures out to avoid. **Not hypothetical: with the filter in, ONE leftover survived, which the 56 had been hiding.** | **FIXED** — both scans filter through `git ls-files` per repo, so only TRACKED paths can fail the run; and `SOUNDNESS.md` is excluded alongside `CHANGELOG*`/`BACKLOG.md`/`*-LOG.md`, because the survivor was a register row quoting measured evidence (`candor-scan 0.35.0 (spec 0.35)`) — a historical fact, not a stale declaration. `*-LOG.md` already caught `SOUNDNESS-LOG.md` and missed `SOUNDNESS.md`, the same kind of document and by far the largest. **THE ID WAS USED BEFORE THE ROW EXISTED.** R366 records the rule — allocate the row, then write the code that cites it — and R368 went into `release-preflight.sh` and three commit messages while the register's maximum was R367. Caught by a planning review that RECOMPUTED the maximum instead of trusting a brief. The rule is not the problem; remembering to apply it is, which is why this belongs in a gate rather than a paragraph — a check that every `R[0-9]+` cited in a repo's tracked files exists in the register would have caught both instances in under a second. |
| R369 rust: a cfg-duplicated alias on the TYPE route — **THE FILED PREMISE IS WRONG AND THE CORRECTION IS IN THE FINDING COLUMN** | 2026-09-10 | **OPEN, RESTATED 2026-09-10. Filed as "a total loss"; two review agents disagreed with each other about it, and a direct re-measurement showed the filed premise does not reproduce. The defect is real; the row's characterisation of it was not** | **CORRECTION FIRST, because the original row is quoted below and must not be read as current.** Re-measured directly on four fixtures that vary ONE thing at a time (`candor-scan` at `64a6a95`, `--json`, all four `cargo build` clean): with the two arms given **DISTINCT** type names (`fs_arm::FsR` / `net_arm::NetR`), `fn f(x: &A1) -> usize { x.go() }` reports **the LAST-WRITTEN arm** — `Fs` arm first gives `['Net']`, swapped gives `['Fs']` — while the uncollided control `g` charges `['Fs']` in both. That is ordinary SOURCE-ORDER MISATTRIBUTION, R140's exact shape on a different consumer, and it is still a cardinal sin because one of the two orders is wrong. It is **not** a total loss. The "resolves to NOTHING" result appears only when both arm types share a LEAF NAME (`fs_arm::R` / `net_arm::R`) — and **there the uncollided control `g` is ABSENT too**, in both orders. So the absence is R213 (same-named types in sibling modules) doing the work, and the fixture this row was filed from could not tell the two causes apart: its control shared the leaf name with the arms. **A control must vary only the thing under test; this one varied nothing.** The live consumer question is now stated accurately by **R372** — the decl-side `uses` map that `type_path` and `record_return` read — and this row is its type-route instance rather than a separate phenomenon. **THE ORIGINAL TEXT FOLLOWS, PRESERVED SO THE MISTAKE IS LEGIBLE:** ~~CARDINAL SIN, rust, and worse-shaped than R140.~~ R140 is the PATH route (`Runner::new(..)`), where two cfg-gated `use` items binding one name let source order PICK an arm. On the TYPE route — a parameter typed by the aliased name, `fn f(x: &A1) -> usize { x.go() }` — a collided alias resolves to **NOTHING**: measured ABSENT for a pure+Fs pair in BOTH orders, and ABSENT for an Fs+Net pair where every arm is effectful. So it is not order-dependent, it is a total loss. The route itself works: the same shape with an UNCOLLIDED alias (`use crate::fs_arm::R as Solo`) charges `Fs`, which is the control that isolates the collision as the cause. | Not fixed. **R140's companion map is the machinery to reuse** — `use_alts` already records every arm beside `uses`, and the path route consumes it by pushing an extra edge per arm. The type route resolves through `resolve_recv_type` → `type_path(ty, uses)`, which asks the single-valued map; it needs the same treatment, and "push an extra edge" is not obviously the right shape for a TYPE (a receiver has one type, not a set), so the consumer design is the open question rather than the plumbing. **Scoped out of R140 deliberately:** R140's row, fixture and A/B are all the path route, and folding a second consumer in would have made the A/B unable to attribute anything. See R362 for the same discipline applied to a different index. |
| R367 ts: three commits chased one question with a SYNTACTIC proxy, in the one engine that has a TYPE CHECKER | 2026-09-09 | **CLOSED — candor-ts `b336fc8`. Supersedes the denylist half of R355/R359 and closes R363's gate loosening** | R284 named the nearest named declaration as the dispatch owner. R355 and R359 then fixed the cases where that walk leaves the type position by enumerating NODE KINDS to stop at — and the enumeration was wrong in BOTH directions, which is why it took three commits and never converged. **R355 too narrow** (missed `interface I { cb: { m(): void } }`); **R359 too broad** once widened to catch that — `ts.isTypeReferenceNode` demoted `type RO = Readonly<{ m(): void }>`, which genuinely HAS `m`, so a firing `deny Unknown[dispatch]` went **exit 1 → exit 0 on a real owner** (R363), the quiet direction, in the staged 0.36.0. **THE INSTRUMENT WAS THE DEFECT:** the question is not *what syntax is this literal sitting in* but *does the type this declaration declares have a property by this name* — a semantic question answered with a syntactic proxy, in candor-ts, which has a checker. | **FIXED — one checker call replaces the list, which is DELETED.** `ownerDeclaresMember` asks `getPropertyOfType(getDeclaredTypeOfSymbol(sym), member)`; exact on all six shapes (`Named`, `Readonly<…>`, a user-defined `Id<T>=T`, `Array<…>`, the interface property, the class-body literal). **FAILS LOUD BY CONSTRUCTION** — no symbol, no declared type, or the checker throwing all answer *keep the owner*, so an unanswerable case over-approximates visibly rather than silently demoting a real owner. The retired list is deleted rather than kept as a record: the repo's own lint refused it as dead code, and it was right — the shapes belong here. Measured: the R363 gate returns to exit 1; all three R359 phantoms stay closed; A/B vs the shipped R359 state over 28 entries / 41,669 rows gives ADDED 0, REMOVED 0, `inferred` CHANGED 0 and **zero `dispatch:` tokens moved either way** — the 38 changed rows are `callback:` details becoming MORE informative, named owners restored where the broad entry had genericised them to `callback:unresolved call`. Falsified: stubbing the predicate to its fail-loud answer fails 7 assertions. Suite 2708/2708, gates 3/3. **THE GENERAL POINT IS THIS REGISTER'S OWN `candor-handlist-vein` FINDING, POINTING BACK AT ME:** an engine WITH a type checker must not hand-maintain what the checker derives — that instrument belongs to candor-rust and candor-swift, which have none — and I spent three commits hand-maintaining it in the one engine that has one. |
| R365 umbrella: `_ci_verdict.py` still taught the PRE-R352 rule as "THE FIX", forty lines above the paragraph calling it the defect | 2026-09-09 | **CLOSED — candor `ba3d2f0`. Found by the fourth review round** | The file's header explained that *"a `success` wins the group outright, wherever gh lists it"* and labelled it THE FIX. That is precisely the rule R352 identified as masking a newer FAILURE in release gate [10]. It arrived with the commit R352 later diagnosed, and **R352, R356 AND R360 each edited this file without removing it** — three chances missed — so a reader going top-down learned the defect as the current design. A trailing clause was wrong twice over as well: the fallback condition is now *no JUDGED entry*, not *no success*, and `live[0]` is the winner in the ordinary case rather than only in the fallback. | **FIXED — the stale paragraph is REMOVED rather than annotated**, and replaced with the rule as it actually stands, in four numbered steps, in one place. **A SECOND FALSE COMMENT IN THE SAME FILE, from R360, is corrected with it:** it said the `NO_VERDICT` change was *"only about which entry represents the group"*. Measured — for a group in which nothing reached a verdict it moves the VERDICT in three arrangements of four (`[cancelled@newer, skipped@older]` and both same-second orders go OK → BAD; `[skipped@newer, cancelled@older]` stays OK). Only ever in the false-RED direction: an exhaustive enumeration of every 1-to-3-entry group found no input where R360 turns a BAD into an OK. **And an audit corrected R360's own commit message**, which claimed the mutation *"fails 2"* — it fails **1** (the recency row), measured two ways. **R356's tie-test half remains UNPINNED**: reverting that predicate at HEAD leaves release-test 469/469 green, because `NO_VERDICT` now strips `skipped` before the tie test sees it. The predicate is not dead — `[cancelled@t, skipped@t]` answers BAD unmutated and OK mutated — so one behaviour survives with no fixture on it, which is the exact condition R360 exists to record. |
| R366 umbrella: the stager and the publisher matched a version heading DIFFERENTLY — R361 fixed that in one function and left it in another | 2026-09-09 | **CLOSED — candor `ba3d2f0`. THIS IS R364'S FIRST HALF UNDER A SECOND ID; treat the two as ONE finding, and see the ID note below** | `find_version_heading`'s docstring claimed it was *"kept BYTE-IDENTICAL to the question `bin/_release_notes.sh` asks"*. It was not — and that is the same sentence R361 retracted for `STAMPED_RE` a few lines above, left standing here. **This is the worse of the two: it runs on ALL SIX ENGINE REPOS every stage, where `STAMPED_RE` runs on the umbrella alone.** The publisher's `sect_by_version`/`sect_by_floor` are PREFIX tests; this was an ANCHORED regex admitting only end-of-line or ` — …`. **Seven of twelve probe headings disagreed, in BOTH directions**, and both divergent shapes already exist in `candor-spec/CHANGELOG.md` (`## 0.26 (…)`, a bare `## 0.15`) — latent only because the heading being matched today happens to be canonical. Reproduced end to end: a bare `## 0.36` floor heading made the stager FOLD and exit 0 while the publisher refused rc=3 — R354's sequence again — **and the remedy the refusal named was a PERMANENT NO-OP**, because on re-run `## Unreleased` is empty and the stager reports SAME forever. | **FIXED — a prefix test on the same two spellings, in the publisher's order.** All 8 probe headings agree; 0 disagreements. Verified idempotent against all seven real changelogs, with `_release_notes.sh` returning rc=0 for each. **THE ID SITUATION, RECORDED BECAUSE IT IS THE HAZARD CLAUDE.md DOCUMENTS RUNNING BACKWARDS.** R365 and R366 were written into shipped code BEFORE they existed in this register, while the register's maximum was R364 — so an agent computing the next free ID by the MAXIMUM rule would have allocated R365 and collided with live code. Filing them here closes that. And R366 duplicates R364's first half: one finding, two IDs, which is what the rule exists to prevent. R364 is retitled to the half that survives (its vacuous-control finding) and its wrong consequence retracted in place. **The lesson is the ordering, not the numbers: allocate the row, then write the code that cites it.** |
| R356 umbrella: R352's tie rule reintroduced a MASKED FAILURE in release gate [10] — the class it closed, in the same gate | 2026-09-09 | **CLOSED — candor `fce12c9`, and SUPERSEDED IN PART BY R360, which found the same class one door over** | The tie test asked `_concl(e) != "success"` while the verdict treats BOTH `success` and `skipped` as acceptable, so a `skipped` twinned with a failure at the same second REPLACED the winner: `[failure, skipped]` → OK, `[skipped, failure]` → BAD. Masked, **and order-dependent**, which is the 2026-08-26 defect this file exists for. Also `x.get("workflowDatabaseId", x.get("workflowName"))` does not fall back on a present-but-null value, so a null id merged every workflow into one group and the newest won across unrelated ones. | Named the OK set in the tie test; `or` instead of a `.get` default. **Both halves were incomplete and R360 finished them.** Found by a review dispatched precisely because R352 was itself a fix. |
| R357 umbrella: R354's postcondition covered ONE of two branches, so the sequence it was written to close survived a re-run | 2026-09-09 | **CLOSED — candor `fce12c9`, and SUPERSEDED IN PART BY R361** | R354 put its newest-section postcondition only in the `else` branch. The sibling `SAME` branch searched the WHOLE FILE for a stamp, so run 1 stamps an older heading and exits 1; run 2 finds that stamp anywhere, prints *"newest dated heading is already stamped"* — FALSE — exits 0, and staging proceeds into the [9b] rc=3 refusal R354 exists to prevent. Measured on the committed pre-fix code. *"A rule on one route and not its sibling is this family's oldest defect"* — this repo's own `_ci_verdict.py` header — and R354 walked into it while fixing the same class. | Anchored the `SAME` guard and tightened the postcondition to the publisher's question. **Both were still wrong and R361 fixed them properly.** |
| R358 swift: a nested rebind stopped shadowing a global of the same name — a FABRICATION introduced by R351 | 2026-09-09 | **CLOSED — candor-swift `70c6102`; its own test comment was FALSE and was corrected in `2f69ceb`** | **FABRICATION, swift, introduced by a fix.** R351's second writer clears `arrayElem`, so a name rebound to a NESTED container appeared in no index the `DeclReferenceExprSyntax` shadow guard consults and fell through to a module-scope global, charging its initializer: `func shadow(_ n: [G], _ q: [[G]]) -> Int { let n: [[G]] = q; return n.count }` → `['Fs']` on a body that reaches nothing. `shadowFlat` (flat rebind) correctly absent — the control that isolates the index. **The hole is OLDER than R351:** `pureNested` fabricates with no flat parameter at all, on both sides, because R278 added the index and never added it to the guard. | Guard now consults `arrayElemNested`. **AND THE COMMIT'S OWN TEST COMMENT WAS A FALSE LIMITATION CLAIM POINTING AT A CARDINAL SIN.** It said `staleFlat`/`staleNested` had no teeth and one clear *"should be removed"*. Measured against the FULL suite: guard reverted → 2 failures; `setArrayElem`'s clear removed → 4; `setArrayElemNested`'s clear removed → 1. All three have teeth, and **deleting the line the comment nominated makes `staleFlat` go ABSENT over a body that writes a file**. The wrong measurement came from a `.replace(..,1)` stub landing in `clearBindingTypeOnly` (the string occurs twice) plus `swift test --filter` excluding the rows that would have failed — two instruments wrong in the flattering direction. Corrected in place with the rule: anchor a stub on the enclosing FUNCTION's body, run the full suite. |
| R359 ts: R355's value-position boundary was drawn around its own trigger — the interface and alias spellings still named phantom owners | 2026-09-09 | **CLOSED — candor-ts `a14c88d`. Residuals in R363** | R355 stopped the ancestor walk at a class `PropertyDeclaration` and not at the interface / type-alias `PropertySignature`, nor at a literal in a type argument. `interface I { cb: { m(): void } }`, `type W = { inner: { m(): void } }` and `type L = Array<{ m(): void }>` all named an owner that does not declare the member. R284 is UNRELEASED, so these were NEW normative fields about to ship — verbatim the argument R355 used for fixing rather than filing. Ground-truthed: zod's `CustomCheck` declares only `_zod`, not the `check` it was credited with. | `ts.isPropertySignature` + `ts.isTypeReferenceNode` added to the denylist. A/B 28 packages / 41,669 rows: ADDED 0, REMOVED 0, `inferred` CHANGED 0. **Two claims in that commit were corrected by review:** the gate-safety inference (`inferred` unchanged ⇒ no verdict can move) is INVALID — `policy.mjs` maps `dispatch:`→`dispatch` and `callback:`→`indirect`, and a measured `deny Unknown[dispatch]` went exit 1 → 0; and the `interface Ext extends Array<string>` control is VACUOUS, because `m2`'s parent is the interface so `namedTypeAncestor` is never called on that path. |
| R360 umbrella: a `skipped` run superseded a FAILURE by RECENCY in release gate [10] — R352's class, third instance | 2026-09-09 | **CLOSED — candor `d82a1cf`** | R356 closed the same-second TIE door and left the recency door open: `[skipped@10:05, failure@10:00]` → **OK**, while `[cancelled@10:05, failure@10:00]` → BAD. Both twins mean *no judgement was reached*, so both must lose to a sibling that reached one. **And `OK_CONCLUSIONS` was NOT "named once" as R356 claimed** — it sat inside the per-group loop while the verdict kept its own literal, which is the two-copy drift that produced R356. | `NO_VERDICT = (cancelled, skipped)` drops both from the group representative; `OK_CONCLUSIONS` hoisted and read by both sites. 11/11 matrix. **A review corrected R356's reachability justification as an over-claim**: job-level `if:` is not sufficient for a run-level `skipped` (all jobs must skip), and in all three cited workflows the conditions partition the event space so at least one job always runs. The fix is still strictly tighter and can never turn BAD into OK. **AND NEITHER R356 NOR R354 WAS PINNED BY ANYTHING** — a mutation test found 0 of 13 gate-[10] fixtures and 0 of 3 stager fixtures changed verdict across those commits, the same unpinned condition under which the ORIGINAL defects shipped green. Five fixtures added (tie both orders, recency, a lone-`skipped` control so "skipped is always BAD" cannot satisfy them, a null-id pair with different names); mutation-verified. |
| R361 umbrella: the stager asked a DIFFERENT QUESTION from the publisher, and R357 spelled its own rule two ways | 2026-09-09 | **CLOSED — candor `d82a1cf`** | R357's `SAME` guard required an em dash (`^## \d{4}-\d{2}-\d{2} —`); its `else` postcondition did not; and NEITHER matches `_release_notes.sh`'s `sect_newest`, which takes the first `## ` heading that is not `## Unreleased`, any shape. Measured on `## 2026-09-10 new work` (no em dash): stager → `SAME … already stamped`, rc=0; publisher → **rc=3** naming that heading. R354's sequence verbatim, surviving the fix written to close it. A non-dated heading above the dated ones does the same. **And the stamp test was STRICTER than the publisher's** — the publisher's glob accepts anything between `(released ` and ` as VER)`, R357 demanded `[0-9-]+`, so `(released on 2026-09-09 as 0.36.0)` was refused by the stager and accepted by the publisher: a false RED that stops a legitimate cut with the file already half-edited. | One helper mirroring `sect_newest`, called by both branches; one permissive `STAMPED_RE`. The comment no longer claims byte-identical when it was not. |
| R362 swift: the shadow guard enumerates FIVE name-keyed indexes and misses at least two — a fabrication on real third-party code | 2026-09-09 | **CLOSED 2026-09-11 — `tupleElem` (candor-swift `88976a6`) was the sixth; `literalLocals`, a LEXICALLY SCOPED set, is the seventh and closes the rest. The function-wide `isBoundLocal` stays REJECTED and stays pinned, because it is the same index with the wrong scope** | **FABRICATION, measured on swift-nio.** The guard does not consult `boundLocals`/`isBoundLocal(n)` — the function-wide set of every local binding name, which **the very next line of the same visitor already uses** for the `propertyEdges` half — nor `tupleElem`. So any local whose type `vars` drops still edges to a same-named module-scope global. `ChatHandler.channelInactive` (`NIOChatServer/main.swift:70`) carries `['Clock','Env','Unknown']` and loses the edge on a one-identifier rename of the LOCAL only; `EchoHandler.channelActive` carries `['Env','Net','Unknown']` and drops to `['Env','Unknown']`. **A `deny Net` gate fails `channelActive` over a network effect it does not perform.** Minimal: `let h = 42` shadowing an effectful global `h` → `['Fs']`; a `let t: (Int,Int) = p` (only `tupleElem` set) likewise. | **FIXED — `literalLocals`, written at the same three sites as `boundLocals` and differing in exactly ONE respect: it is saved and restored with every shadow scope.** That single difference is the row. A function BODY is a `CodeBlockSyntax` and therefore its own scope, so a top-level `let` shadows for the rest of the body and no further — which is what Swift means — while a binding inside an `if` stops shadowing at the closing brace, so the trailing read of the global survives. **Both directions are pinned by one test and neither is safe to assert alone**: the closed case (`T.litLocal` ABSENT) and the rejected case (`T.blockThenGlobal` still `['Fs']`) are opposite answers to *does this name still name the global HERE*. **THE PIN DID ITS JOB AND IS THE REASON THIS WAS CHEAP:** the earlier round wrote the residual as an assertion saying *"when a scoped binder set closes it, this assertion goes red and names itself"* — it went red on the first build of the fix and named the remedy, so the fix was inverted into it rather than rediscovered. **A SECOND GATE ALSO FIRED:** `NameKeyedStateTests` refuses any new stored property of the collector until `disposition` classifies what a rebind does to it, so `literalLocals` could not ship as an unclassified name-keyed set. That test demands a DECISION rather than a behaviour, which is why it catches what a behavioural suite cannot. Suite 1131/1131. **A/B over four real Swift packages (swift-nio, Alamofire, swift-argument-parser, swift-syntax), 11,819 rows: ADDED 0 / REMOVED 0 / CHANGED 8, every one in swift-nio and every one a FABRICATION WITHDRAWN — each traced to source rather than counted.** `NIOUDPEchoClient/main.swift`: `channelActive` binds `let remoteAddress = try self.remoteAddressInitializer()` at line 37 while line 118 declares a module-scope `let remoteAddress = { () -> SocketAddress in … }` whose initializer resolves a socket address — so `['Env','Net','Unknown']` → `['Env','Unknown']` and `netClass:['unknown-host']` → none, **which is the `deny Net` gate this row was filed about no longer failing over an effect the function does not perform.** `NIOChatServer/main.swift`: `channelInactive` binds `let channel = context.channel` at line 71 against a module-scope `let channel = try { … }` at line 173 that binds and connects a server, so `['Clock','Env','Unknown']` → `['Unknown']`. `Unknown` is retained in every one of the eight, so each row stays fail-closed. R358 closed the fourth index and this was the fifth and sixth. **A diagnostic arm adding `\|\| tupleElem[n] != nil \|\| isBoundLocal(n)` measures 1130/1130 green with a 3-package A/B of ADDED 0 / REMOVED 0 / CHANGED 10 (4 on `inferred`) — but `boundLocals` is function-wide and MONOTONE, so a real fix needs the scoping argument thought through rather than that probe pasted in.** R358's commit states the rule (*"a name-keyed index added anywhere must be added to every list that answers 'is this name locally bound'"*) as enforced; it is enforced for four of six, and that sentence should be read as the remedy for this row. |
| R363 ts: the type-position node-kind enumeration is incomplete in BOTH directions — it demotes four real owners and still misses four phantom shapes | 2026-09-09 | **CLOSED BY R367, IN BOTH DIRECTIONS AND WITHOUT BEING AIMED AT — the checker replaced the enumeration, so shapes nobody had listed are handled too** | **Demotes (lost normative detail):** `Readonly<{m():void}>`, `Partial<…>`, `Pick<…,"m">`, `Required<…>` — utility-type aliases whose ENTIRE definition is the literal, so they really do have member `m`. `ts.isTypeReferenceNode` is broad and catches them; the reason class moves `dispatch`→`indirect`. **Still phantom:** `type L = { m(): void }[]`, a tuple `[{ m(): void }]`, `interface E extends Array<{ m(): void }> {}`, and a mapped type. The first is the SAME TYPE as the `Array<{m()}>` R359 just fixed, in its shorthand spelling — the boundary drawn around its own trigger for the third time in this arc. | Not fixed. Reach measured 0: across 7 TS packages and 60 `dispatch:` tokens, an arm adding `isArrayTypeNode`/`isTupleTypeNode`/`isExpressionWithTypeArguments`/`isMappedTypeNode` changes none of them. **And that arm is not free — it degrades hono's `callback:…EffectData` (a tuple alias of function types) to `callback:unresolved call`**, so the enumeration needs doing in both directions at once with the utility-alias case as its control. Note also the shipped comment claims this denylist *"fails loud"*; a broad entry fails QUIET, in the demote direction. **THREE CORRECTIONS FROM AN AUDIT OF THIS ROW, 2026-09-09, and the first is the one that matters.** (1) **THE DEMOTE IS A GATE LOOSENING ON A REAL OWNER, WHICH THIS ROW DID NOT SAY AT ALL.** On `type RO = Readonly<{ m(): void }>` with `deny Unknown[dispatch]`: pre-R359 **exit 1**, at HEAD **exit 0, 0 violations** — measured. R359 noticed the verdict flip and demonstrated it on a PHANTOM, where the flip is a correction; on a utility alias the identical flip removes a firing deny against a member the alias genuinely has. The effect is still disclosed (`callback:m`, so an unscoped `deny Unknown` still fires) — it is a precision loss on a scoped gate, not a silenced effect — but it is a green-on-identical-bytes in the direction nothing names as a loss, and **it ships in 0.36.0**. (2) **The demote class is WIDER than four utility types**: `Omit`, `NonNullable`, `Extract`, nested `Readonly<Readonly<…>>` and a USER-DEFINED identity alias `type Id<T> = T` all demote — it is any alias whose definition is a `TypeReference` resolving back to the literal, so a fix scoped to four names will not close it. (3) **The "7 packages / 60 dispatch tokens" figure is unreproducible and wrong**: the TS-source corpus is 9 packages / 8 scannable, carrying **212 `dispatch:` tokens (133 distinct) over 2,411 rows**. The zero-reach conclusion SURVIVES at ~8× the claimed scale (0/0/0, zero dispatch tokens changed), so it is a bad number under a good finding — but a reader would take 60 as the corpus's real dispatch surface. The hono claim is verified with a correction: the arm does not RENAME the token, it DELETES the named `callback:…EffectData` and it collapses into a `callback:unresolved call` already present on both units, and it is visible only on hono SOURCE, not the npm dist. **CLOSED 2026-09-10 by R367 — candor-ts `b336fc8` — and the manner is the point.** R367 did not extend the node-kind list; it DELETED it and asked the type checker whether the candidate owner's type has the member. Measured after: all four DEMOTE shapes keep their real owners (`Readonly`, `Partial`, `Pick`, `Required`, and the user-defined `Id<T>=T` this row added), and **all four PHANTOM shapes this row listed as still open — `{m():void}[]`, a tuple, `interface E extends Array<{m()}>`, a mapped type — are now `callback:m`**, without any of them being enumerated anywhere. That is the evidence for the general claim: a semantic question answered semantically closes cases the syntactic proxy could not even be told about. The gate loosening this row recorded (`deny Unknown[dispatch]` exit 1 → 0 on `type RO = Readonly<{m()}>`) is closed with it — measured back to exit 1. |
| R364 umbrella: `release-test.sh`'s `grep -qv` controls are VACUOUS — they pass over the very output they exist to forbid | 2026-09-09 | **CLOSED — candor `ad0cb13` ("R364: both `grep -qv` controls in release-test.sh were vacuous"), 2026-09-10. Originally filed: OPEN for the control half. THE MATCHING HALF OF THIS ROW WAS WRONG AND IS RETRACTED BELOW; the divergence it described is real and is FIXED as R366** | `_release_notes.sh`'s `sect_by_version` is a prefix match (`index($0,"## [0.32.1]")==1`); `_stage_changelogs.py`'s `find_version_heading` is an anchored regex allowing only EOL or ` — …`, **despite its own comment saying it is kept BYTE-IDENTICAL to the question the publisher asks**. Measured: a changelog carrying `## [0.32.1] (hotfix) — 2026-08-25` plus stranded `## Unreleased` work → the stager does not see the existing section, writes a SECOND `## [0.32.1]` above it, and the publisher then publishes only the new one. **AND THAT SENTENCE WAS WRONG — RETRACTED 2026-09-09 by an audit of this row.** It reads as measured and was not: it is a plausible inference about `sect_by_version` that nobody ran. `sect_by_version` is a PREFIX test placed BEFORE the `f && /^## /{exit}` rule, so the duplicated heading matches the first awk rule too and **the publisher prints BOTH sections, rc=0**. The defect is a duplicated heading, not data loss — which matters, because the wrong consequence made the motivation wrong and would have sent the next person looking for a disappearance that does not happen. **The MATCHING divergence itself is real, was reproduced end to end, and is FIXED — see R366**, which is this row's first half under a second ID; do not treat them as two findings. Also found: `release-test.sh:4413`'s control is vacuous — `grep -qv 'WARN candor'` succeeds whenever ANY line lacks the string, so the "stays quiet when every marker is well formed" assertion passes over a warning. | **WHAT SURVIVES IS THE CONTROL HALF, and its own audit boundary was drawn around its trigger.** `grep -qv PATTERN` succeeds whenever ANY line lacks the pattern, so a control asserting *"stays quiet when every marker is well formed"* passes over an output that does contain the warning. This row named ONE instance; there are **at least two** — `release-test.sh:4448` (the `WARN candor` control) and `:4194`, the identical shape over `gate-run`'s multi-line output with `grep -qv 'NOT GREEN'`. Sweep for the shape, do not fix the instance. **A trap for whoever re-checks this: on this machine `grep` is a ugrep shim that returns rc=1 for the same `-qv` input and makes the control look sound** — the finding was only visible through `/usr/bin/grep`. The remedy is `grep -q` on the whole captured output, never `-qv` per line. **CLOSED 2026-09-10 — candor `ad0cb13`. BOTH instances fixed, and the shape swept rather than the instance.** `grep -qv PATTERN` succeeds whenever ANY LINE lacks the pattern, so on a two-line output where one line IS the forbidden string it exits 0. Replaced with `grep -q` negated in the shell, at both sites. **Falsified for the `WARN candor` control by making the stager warn unconditionally: the suite fails with *"the warning fires on a correct changelog"* and goes green again on restore — the first time that control has been able to fail at all.** The `NOT GREEN` control needs a mid-run disk crossing to induce, so it is proven at the construct level instead: on output containing the string the old form PASSES and the new form FAILS, and on clean output the new form passes. Both directions, stated because a control proven in only one is half a control. |
| R350 rust: R347's unified adapter list moved TWENTY names into an arm with no `returns` fallback, silencing every trait-object route through them | 2026-09-09 | **CLOSED — candor-rust `4baa37c`. INTRODUCED BY R347, eight hours earlier, and found by the release review rather than by me** | **CARDINAL SIN, rust, self-inflicted.** R347 unified the two element-resolver lists; the dispatch arm's `returns`-index fallback stayed keyed on a three-name ALLOWLIST (`get`/`get_mut`/`get_or_init`). The twenty names R347 moved into that arm therefore hit `return Vec::new()`, and any caller reaching a real effect through a caller-supplied trait object went ABSENT — no row, no `Unknown`, no `invisible`. Measured with the method NAME as the only variable, every fn returning the same `Vec<Box<dyn Doer>>`: `via_fetch` Fs (never on any list), `via_get` Fs (on the old allowlist), and `via_first` / `via_take` / `via_to_vec` / `via_filter` all **ABSENT** over a body that calls `fs::write`. **THE TWENTY ARE DERIVABLE ONLY BY DIFF** — pre-R347 CONCRETE list minus pre-R347 DISPATCH list — because R347's own patch is all `+` (the function is new), which is why the first fix scope I wrote was ambiguous. | **FIXED — INVERTED TO A DENYLIST, and that is the fix rather than a detail.** The seventeen receiver-only names are exactly those whose fallback was MEASURED bad (4 rows lost their `invisible` disclosure across 78 crates — sea-query-derive, wit-bindgen-rust, x509-parser); everything else falls back, byte-for-byte its pre-R347 route. **A name added to the union in future now falls back BY DEFAULT**, which is the safe direction — the allowlist spelling is precisely how R347 caused this, by doubling the union while the allowlist stood still. See `candor-denylist-over-allowlist`. **N-5 CLOSED IN THE SAME COMMIT because it is why this was not caught:** R347's residual pin asserted `fixture_effects(..).is_empty()`, and candor emits only effectful functions, so that predicate cannot distinguish *absent from `functions[]`* (a purity claim) from *present, disclosing `Unknown`* — the two answers it most needed to tell apart. It now asserts the DOCUMENT. The same release had already fixed this exact shape in two other tests (`c1f40ce`); this pin was written after that fix and re-committed the defect. **Two tests, and the second is the one that would have caught it in R347:** a behavioural row with both controls and a pure-element over-charge control, and a DECISION test asserting the denylist is a subset of the union and that all twenty names fall back. Falsified: restoring the allowlist fails the new test on all four victims while both controls pass. **A/B 600 registry crates, 39,958 rows both arms: 0/0/0 on all five keys** — recorded with `--allow-zero-reach`, a SAFETY result not a value one, because the shape has prevalence 0 locally (1,568 crates grepped for a crate-local fn named after an adapter whose signature names `dyn`: no hits). Gates 29/29. |
| R351 swift: `arrayElem` and `arrayElemNested` describe ONE binding and were not written in lockstep — a REGRESSION AGAINST THE PUBLISHED v0.35.0 | 2026-09-09 | **CLOSED — candor-swift `9673ee0`. Introduced by R278 + `c6b309a` the same day; found by the release review, which was told to attack my reorder claim** | **CARDINAL SIN, swift, self-inflicted, and the only finding this session that regresses a SHIPPED artifact.** `setArrayElem`'s own comment has stated the rule since it was written — *"`arrayElem` and `opaqueElem` describe the same binding and must move together … one writer, so the pair can never drift"* — and R278 added a THIRD map for that same binding, joined to neither writer. A name could then carry a live FLAT entry and a stale NESTED one at once, and because `c6b309a` asks the nested resolver FIRST at the `for` binder, the stale one won. `func f(_ n: [[G]], _ p: [G]) { let n: [G] = p; for z in n { z.run() } }` charged `Fs` on the PUBLISHED v0.35.0 and went ABSENT at HEAD. The control differs only in the parameter's declared type and charges on every build. **I wrote the rule down in R278's own commit message — arguing for a separate index over a flag because a flag would buy "containment with a fabrication" — and then built the thing the rule forbids.** | **FIXED — A PAIR OF WRITERS, not one clear.** `setArrayElem` drops the nested entry; a new `setArrayElemNested` (the second single writer) drops the flat entry and its opacity. Published v0.35.0 → HEAD: `regression` Fs→Fs, `unannCopy` ABSENT→Fs, `mirror` ABSENT→Fs, control Fs→Fs, `ctlPure` ABSENT→ABSENT. **So the lockstep also closes TWO silences that predate the release**, which is why a pair of writers rather than a single clear. **A BLANKET CLEAR AT THE PLAIN-`let` BINDER WOULD HAVE BEEN WRONG and was rejected on measurement:** that path deliberately does not call `clearBindingTypeOnly`, because clearing the type maps before the initializer is walked kills self-referential rebinds like `let u = u.asURL()`. **`NameKeyedStateTests` caught HALF of this and its own header names the other half** — *"it says nothing about whether every BINDER SITE takes those paths, and a review found exactly that defect after this file shipped"*. I cited that gate the same morning as having caught a hole in my fix; it had caught the half it can see. Falsified: removing `setArrayElem`'s clear fails the new test on the regression and the unannotated copy while both controls pass. **A/B vs the PUBLISHED v0.35.0, 22 Swift packages, 809→851 rows: ADDED 42, REMOVED 0, CHANGED 27 — of the changed, 24 are strict effect GAINS and ZERO are losses** (3 moved a non-effect field). Suite 1129/1129, gates 11/11. |
| R352 umbrella: `_ci_verdict.py` read a FAILING CI run as OK in release gate [10] — the check that authorises publishing | 2026-09-09 | **CLOSED — candor `b9c55a6`. Found by the release-readiness reviewer; the assertion that pinned it had been inverted by the same commit that broke it** | **A SILENT UNDER-REPORT IN THE RELEASE MACHINERY, which is worse than one in an engine because it makes every other verification unreliable.** The rule was "any success in the workflow group wins", so `[failure(newest), success(older)]` at one sha answered OK. Measured on identical stdin and argv: published v0.35.0 → `BAD ci:failure`, pre-fix HEAD → `OK`. **The trigger is documented, not hypothetical:** `native.yml` and candor-swift's `release.yml` both carry `workflow_dispatch` RECOVERY paths that create a second run at an unmoved sha, and the cron oracles re-run at a sha that has not moved. **THE OVERSHOOT'S CAUSE:** the defect this replaced was a `cancelled` twin — a supersede — and the repair generalised from *a cancelled run is not a verdict* to *a success outranks anything*, which also swallows `failure`. | **FIXED, and the naive wording was measured wrong first.** "Worst wins" fails `idsame` (a success five minutes newer legitimately superseding a failure) and would block every re-run at an unmoved sha — which is what a recovery dispatch IS. The rule: drop `cancelled` entries with a non-cancelled sibling; take the first remaining (gh is newest-first, the assumption this file already documents); and if another remaining entry shares the winner's whole SECOND and is not a success, the group is BAD. **Second-granularity is exactly where "newest" stops being decidable — the defect this file was written for — so at a tie the failing verdict wins.** 9-case matrix, including both cases the naive wording would have broken. **AND `release-test.sh` WAS GREEN OVER IT BECAUSE THE SAME COMMIT INVERTED THE ASSERTION:** tie1's message went from *"the failure must be reported — the genuinely-latest FAILURE hidden shape"* to *"the superseded failure does not leak through"*, and nothing in that fixture distinguishes superseded from genuinely-latest — both entries carry the same whole second. The expectation moved with the code, so 466 assertions could not fail. tie1 and tie2 are now both RED and are the same two facts in both orders, so order-independence is strengthened; `idsame` is the control keeping a genuine supersede green. Falsified: reverting to success-wins fails release-test at exit 1 with the assertion naming itself. |
| R353 umbrella: `rs_pin_violations` used a version-STRING COMPARISON as a stand-in for "does this release exist", and its message claimed the latter | 2026-09-09 | **CLOSED — candor `b9c55a6`. Found by the release-readiness reviewer, which falsified it across four arms** | **AN INSTRUMENT WHOSE ONLY CLEAN ANSWER SERVED THE DEFECTIVE ENGINE.** The not-in-cut arm flags a violation iff the pin equals the version being cut, and prints *"that names a release nobody published"* — which the test cannot know. The two come apart whenever a per-engine build id legitimately equals another's, which this file's own header and preflight [4] both say is BY DESIGN. It happened: candor-java is published at 0.35.1, so a 0.35.1 cut with java out of the set flagged java's TRUTHFUL pin as a violation, and **the only value that passed was `0.35.0` — the release carrying three published cardinal sins**. Verified at `_release_set.sh:157`. | **FIXED — ask the question the message asks.** `rs_release_exists` shells to `gh release view`, and only for a pin that collides with the version being cut, so the common path makes no API call. **FAIL-CLOSED BY CONSTRUCTION:** no `gh`, no network, a rate-limited or auth-failed read all answer "does not exist", so the violation is still reported — a check that went quiet when it could not reach GitHub would turn an unpublished pin into a green light at exactly the moment the quota is exhausted, which is when release verification is under most pressure. Verified both directions: java v0.35.1 → YES, v9.99.9 → no. **Footnote worth keeping: this blocker DISSOLVES under the correct version classification** — java's 0.35.1 collides with a patch's 0.35.1 but not with 0.36.0 — so it would have bitten only because the release was mis-classified as a patch. Fixed anyway, because the latent defect is real. |
| R354 umbrella: `release-stage.sh` reported OK while leaving the umbrella changelog in a state preflight [9b] REFUSES | 2026-09-09 | **CLOSED — candor `b9c55a6`. Found by the release-readiness reviewer, proven end-to-end on a scratch copy** | **A GREEN LINE OVER AN UNCUTTABLE RELEASE.** `_stage_changelogs.py`'s completeness guard counts headings that CARRY `(unreleased)` and compares against those the pattern took (`loose > n`): it detects a MISPLACED marker and is **blind to an ABSENT one**. On the real umbrella changelog the two newest sections carried no marker at all, so `loose == n == 1`, no warning fired, `OK candor: 1 dated heading(s) marked released` printed — and `_release_notes.sh` then selected the NEWEST section, found no stamp, and refused rc=3. **Preflight [9b] fails with a remedy naming the command that had just reported OK**, which is the same shape the existing guard was written for, one direction over. | **FIXED — a POSTCONDITION on the text the run produced.** The completeness question is not *did every marker I found get taken* but *is the NEWEST dated section stamped for this release once this run is done* — because that is the section `_release_notes.sh` publishes, so it is the only one whose state can stop the cut. Asked as a postcondition, it cannot be satisfied by a marker sitting elsewhere in the file. Proven BOTH directions on the real changelog: BAD naming the exact heading and the remedy, then OK once the markers are added. **Two process notes worth more than the fix.** `ast.parse` does NOT catch `return` outside a function — that is a compile-time check — and it gave this fix a FALSE PASS; `py_compile` caught it, and without that the stager would have crashed on the branch it was added for. And my first postcondition read a variable that did not exist in scope, which surfaced only because it was run against the REAL umbrella changelog rather than a synthetic one. |
| R355 ts: R284's ancestor walk left the TYPE position and named an owner that does not declare the member — on real code | 2026-09-09 | **CLOSED — candor-ts `becee79`. Found by the release review; the decision to fix rather than file was Fable's, on a cost I had not counted** | **A WRONG NORMATIVE FIELD, over-approximate rather than silent — not a cardinal sin.** R284 gave a member of an anonymous type LITERAL an owner by climbing to the nearest named declaration, and nothing stopped the climb leaving the type position. Four shapes, measured: a literal in a method BODY (eslint 9.x `SourceCode.traverse` → `dispatch:….SourceCode.enterNode`, where `enterNode` is a method of a literal declared inside that very method); a literal in a class FIELD (zx `ProcessPromise.bus` → names a REAL but DIFFERENT method); a TYPE-PARAMETER constraint (the class named as owner of its own constraint's member); an inline PROPERTY type. SPEC §4 makes the dotted `dispatch:<owner>.<member>` detail NORMATIVE and conformance-compared, and `callers --include-unknown` builds frontier edges from it. **AND R284 HAD NO TEETH, which is why the class was four shapes wide rather than one:** stubbing `namedTypeAncestor` to `return null` reverts the entire headline fix and all six shards still pass. | **FIXED — a DENYLIST of value-position boundaries**, so a node kind nobody foresaw keeps climbing and over-fires visibly rather than silently demoting a real owner to `callback:`. It fails loud. **THE DECISION TO FIX BEFORE THE TAG RESTED ON A COST I HAD NOT COUNTED, and my own leaning was to file and ship:** 0.35.0 ALREADY ships these rows as `callback:`, so publishing the phantom in 0.36.0 and correcting it in 0.36.1 would flip `deny Unknown[dispatch]` **twice on identical bytes inside one week**. Fixing now costs one commit that rides the push the repo needs anyway. The new test asserts BOTH directions — the four phantoms gone, and the two real owners (a named alias, an intersection alias) preserved, which is the direction a too-wide stop condition breaks — plus the anonymous-parameter residual still having no owner and still disclosing. **A/B 28 packages, 41,669 rows both arms: ADDED 0, REMOVED 0, and `inferred` CHANGED 0 — no effect set moves at all, so no gate verdict can move on this corpus.** The 30 changed rows are `unknownWhy` detail only: 2 phantom `dispatch:` removed (eslint's among them) and 28 moving from a class-qualified callback name to a local one. Falsified: removing the stop condition fails the suite at exit 1 with all four phantom assertions named. Suite 2704/2704, gates 3/3. |
| R349 swift AND rust: the fold/tuple-adapter closure parameter that IS the element was never typed — a correct exclusion for parameter 0 read as a decision about the method | 2026-09-09 | **CLOSED BOTH HALVES — candor-swift `3eca7f5` (swift) + candor-rust `1c87f8a` (rust), both 2026-09-15; the row's own body says so and its status cell did not. Originally filed: OPEN — MEASURED with a repro and a control, filed unfixed at the end of a run rather than half-implemented. Found by taking R348's question ("what does this list not know?") to the iterator list beside the one R348 fixed** | **CARDINAL SIN, swift.** `ELEMENT_PAIR_ITERATORS` types EVERY closure parameter from the receiver's element, for `sorted(by:)`/`min(by:)`/`max(by:)` whose closures are `(Element, Element)`. Its comment says *"reduce is deliberately ABSENT: its closure is `(Acc, Element)` — the first param is the accumulator, so element-typing it would mistype the fold state."* **That sentence is correct and it is the defect**: it settles parameter 0 and reads as a ruling on `reduce`, so **parameter 1 — which IS the element — was never typed by anything.** Measured against `v.forEach { _ = $0.run() }` charging `Fs` as the control, over a `[Guard]` whose `run()` writes a file: `v.reduce(0) { a, x in a + x.run() }` **ABSENT**; the shorthand `v.reduce(0) { $0 + $1.run() }` **ABSENT**; `v.reduce(into: 0) { a, x in a += x.run() }` **ABSENT**. `reduce` is one of the most common HOFs in Swift. **THREE MORE FELL OUT OF THE SAME SWEEP, and one of them is the discriminator that proves the axis:** `v.enumerated().forEach { _ = $0.1.run() }` is **ABSENT**, and `zip(v, [1])` iterated as `for (g, _) in …` is **ABSENT** — while `for (_, g) in v.enumerated() { _ = g.run() }` **CHARGES `Fs`**. So `enumerated` itself is understood; what is not is the TUPLE-yielding adapter reaching a CLOSURE parameter rather than a `for` binder. | **NOT FIXED — measured only, and deliberately not started with 35 minutes left in the run.** A half-threaded binder is the correct-code-on-an-unreached-path failure this register has recorded repeatedly, and R278's fix earlier the same day needed a rebind clear that only the engine's own `NameKeyedStateTests` caught — this is not a change to land unverified. **The shape of the fix, from the measurement:** `reduce`/`reduce(into:)` want a THIRD category beside `ELEMENT_ITERATORS` (parameter 0 is the element) and `ELEMENT_PAIR_ITERATORS` (all parameters are) — *the LAST parameter is the element*, which is also the honest description of `enumerated`'s tuple slot. **The over-charge control must be written FIRST**, per this register's own repeated finding that killing a silence is where fabrication is introduced: a `[Calm]` whose `run()` is PURE, through every one of the five spellings, must stay absent — `ctl_reduce` in the repro is that fixture and is currently uninformative only because everything is absent today. **The `(inout Acc, Element)` spelling is a separate trap worth stating: `reduce(into:)`'s first parameter is `inout`, so a fix that types the last parameter must not also start typing the first from the element.** Repro is four lines over a two-class fixture and takes seconds to re-run (`fixtures/r349-reduce-element/`). **TAKEN TO ALL FOUR ENGINES BEFORE FILING, AND THE RESULT PREDICTS ITSELF.** rust is WORSE than swift: with `v.iter().for_each(\|g\| { g.run(); })` charging `Fs` as the control, **six spellings are ABSENT** — `fold`, `try_fold`, `scan`, `enumerate().for_each(\|(_, g)\| …)`, `zip(..)` iterated as `for (g, _)`, and **`for (_, g) in v.iter().enumerate()`**, which is the one spelling SWIFT GETS RIGHT. So the two engines are silent on overlapping-but-different sets, and neither's fix is a port of the other's. **ts and java are CLEAN, pure controls absent in both**: ts charges through `reduce`, `reduceRight`, `for (const [, g] of v.entries())` and `flatMap`; java through `map(..).reduce(..)`, `collect(summingInt(..))` and `flatMap`. **This is the third independent confirmation of the same structural fact** (see R348's cross-engine note): java reads BYTECODE where the closure is a real method with a real descriptor, ts asks the TypeScript checker, and neither hand-maintains a list of which parameter of which HOF is the element. rust and swift parse source with no checker and must, and both lists have the same hole in the same place. **The vein now has predictive value rather than only explanatory value: it said where to look, the look was made, and the defect was there in both engines it named and in neither it excluded.** **CLOSED (swift half) 2026-09-15, candor-swift `3eca7f5`. THE ROW'S OWN PRESCRIBED FIX WAS UNIMPLEMENTABLE, and that was found by trying it rather than by reasoning about it.** This row asked for *"the LAST parameter is the element"*. It cannot be done: `closureParamNames` returns `$0/$1/$2` for a SHORTHAND closure because its arity is not in the syntax, so "last" is `$2`, a name `{ $0 + $1.run() }` never binds — **the commonest spelling would have stayed silent while the named one closed**, i.e. the prescribed fix would have produced a green test over a live cardinal sin. Implemented as an INDEX instead (`reduce` → 1), exact under both spellings, with a test per spelling. **A/B over 15 packages / 3,575 files: ADDED 3 / REMOVED 1 / CHANGED 2, REACH 25 hits across 10 of 15 entries** — all three ADDED are purity claims withdrawn at real folds (swift-log `MetadataProvider.multiplex`, whose `provider.get()` invokes a user-supplied closure; swift-collections `Sequence._sum`; Kingfisher `setProcessors`), and the single REMOVED was traced to ground rather than argued (an RxSwift row inheriting Unknown from a fabricated `Bag.count` edge plus the [[R444]] hedge; its caller's disclosure is unchanged in both arms). **The rust half is being fixed concurrently; a four-way conformance PART pinning "a fold over an effectful element charges" is owed, and needs NO new SPEC clause — ⟨0.21⟩ already supplies the contract, and ts + java are already clean.** **RUST HALF CLOSED 2026-09-15, candor-rust `1c87f8a` — and THIS ROW NAMED SIX SPELLINGS; THERE WERE THIRTY-SIX.** The filed list was `fold`/`try_fold`/`scan`/`enumerate`/`zip`; sweeping past the trigger found the whole comparator family (`sort_by`, `max_by`, `min_by`, `dedup_by`, `reduce`…) plus thirteen single-parameter names the list never had. **The real-code payoff is a CLOSED GATE BYPASS**: `subprocess::Pipeline::popen` — body `for (idx, mut runner) in self.cmds.into_iter().enumerate() { ret.push(runner.popen()?) }` — was ABSENT, and `deny Exec Pipeline::popen` over subprocess-0.2.15 **exited 0 pre-fix and exits 1 now**. A/B over 1,563 crates: **ADDED 103 · REMOVED 0 · CHANGED 137** (7 on `inferred`), reach 670 closure binds / 231 crates + 899 `for`-binds / 329. **BOTH HALVES OF THIS ROW ARE NOW CLOSED AND EACH CORRECTED ITS OWN PRESCRIPTION** — swift's "last parameter" was unimplementable, rust's six-name list was a sixth of the class. A four-way conformance PART is owed; ts and java are already clean and ⟨0.21⟩ supplies the contract, so no new SPEC clause is needed. |
| R370 rust: `note_use_collision` has NO NOTION OF `#[cfg]` — it fires on ordinary legal SHADOWING, and the union then charges a shadowed binding as a real call edge | 2026-09-10 | **CLOSED — candor-rust `4c8420c`. Status corrected 2026-09-11: the fix shipped and this cell still read OPEN, caught by a release panel — INTRODUCED by R140 (`20e4ac7`), found by a review agent told to attack the premise. Latent at 15.5% prevalence, zero instances observed on 1,545 crates** | **FABRICATION with gate teeth, rust.** R140's helper fires on *"this name already had a different value in `out`"* and nothing more. `out` is pre-populated with bindings a later `use` legally SHADOWS rather than alternates with, so the union pushes the shadowed binding as a call edge on a function that cannot reach it. Three routes, each a COMPILING program, all ABSENT pre-fix and all effect-carrying post-fix: an inline `mod inner { use std::vec::Vec as Cmd; }` under a file-level `use std::process::Command as Cmd` (Rust does not propagate a parent module's imports into an inline `mod` AT ALL) reads `inferred:["Exec"]`; a body-declared `struct Cmd` shadowing a cfg-gated pair reads `["Exec"]` **while rustc itself emits `warning: unused import: std::process::Command as Cmd`**; a body-level `use crate::inert::Cmd` over a module-level cfg pair likewise. Gate teeth measured PRE→POST: `deny Exec` 0→1, bare `pure` 0→1, and the SCOPED `deny Exec f` 0→1. The isolating control is byte-identical with the two cfg-gated lines replaced by one plain `use` — absent in BOTH arms, so the collision map is the cause and not the shadowing. **Mechanism located:** `decls.rs:224` `submodule_uses` CLONES the parent's `uses` into an inline module (removing only names the module DECLARES), and `fninfo`'s `use_alts: use_alts.clone()` is not filtered by `body_shadowed_uses` — the function R106 added precisely to enforce this shadowing rule on `uses`. The companion map bypasses R106 entirely. **Prevalence, instrumented over all 1,545 registry crates:** 1,168 collision records, **492 with a `#[cfg]` and 676 WITHOUT**; 239 crates (15.5%) record at least one non-cfg collision — the same order as R140's own defect (221/1,490). A ground-truthed instance with no `#[cfg]` in the file: `tracing-subscriber-0.3.23/src/fmt/fmt_layer.rs` binds `format` to `crate::fmt::format` (a MODULE) at line 3 and to `alloc::format` (the MACRO) at line 7 — two NAMESPACES, both live in the same compiled build, not mutually-exclusive arms. Same class: `axum` `FromRef`/`FromRequest` (trait vs derive macro), `borsh` `BorshDeserialize`, `hyper` `SocketAddr`, `async-nats` `Message`, and **every `use X as _;` pair, which all collide under the name `_`**. **The counterweight is stated because it is the honest bound:** a full 1,545-crate A/B moved 286,019→286,036 rows with ADDED 17 / REMOVED 0 / CHANGED 300, every added row `inferred: []`, and all 17 added, 14 inferred-changed and 20 `calls`/`direct`-changed rows were audited individually — **zero fabrications in the wild**. Demonstrated on minimal compiling programs and latent at 15.5%, not a live incident. | **FIXED — and the FIRST fix was a `#[cfg]` test, which the corpus priced and rejected. That is the part worth keeping.** The arm-set test is **"did THIS ITEM LIST bind the name twice"**, accumulated in a `seen` set across the sibling `use` items of one scope, not "is the item `#[cfg]`-gated". Requiring a `#[cfg]` also silences a same-file pair that is genuinely two LIVE bindings in two NAMESPACES: `tracing-subscriber`'s `fmt_layer.rs` binds `format` to `crate::fmt::format` (a MODULE) on line 3 and to `alloc::format` (the MACRO) on line 7, source order picks the macro, and `Layer::default` lost three real call edges to `fmt::format::*`; `lettre`, `quinn-proto` and `aws-smithy-types` lost blind-spot disclosures the same way — **32 field-value losses over the registry, none of which the test suite could see.** Two `use` items binding one name in ONE list is E0252 unless they are cfg-gated or in different namespaces, and in BOTH of those cases keeping every target is right — so the LIST is the test and the `#[cfg]` is not. What must be excluded is INHERITANCE, and `seen` excludes exactly that: an inline module's map IS its parent's (`submodule_uses`), so a name already there is being shadowed, not alternated with. The other two sub-fixes stand, and each is a different question so none is sufficient alone: (1) the `seen` set above; (2) filter `use_alts` through `body_shadowed_uses` at `fninfo`, so a body item DECLARING the name kills the alias exactly as R106 already makes it kill the `uses` entry; (3) a body-local `use` binding a name must CLEAR that name's `use_alts` — it shadows, it does not alternate — and `submodule_uses` must strip alts for names the submodule rebinds. See R374: the over-charge control that would have caught all three was written and cannot fail. |
| R371 rust: R140 is filed CLOSED and source order still decides the same question one SPELLING over — `let f = Alias::assoc; f(..)` never consults the union | 2026-09-10 | **CLOSED — candor-rust `5780ab9`. PRE-EXISTING; R140's CLOSED status was WRONG and R140 is REOPENED. Found by a review agent handed R140's own sentence and asked to break it** | **CARDINAL SIN, rust.** R140's union fires only in `visit_expr_call` on `Expr::Path`. The `fn_alias` route — `let f = Runner::new; f("true")` — is BUILT from the single-valued `uses` map and never reads `use_alts` at all. Two crates identical but for the ORDER of two `use` lines: `#[cfg(unix)]` first reads **ABSENT** over a real `Command::new(..).status()`; swapped, `["Exec"] cmds:["true"]`. **Unchanged by `20e4ac7` in both directions** — that is R140's own reported defect, verbatim, with the call spelled through a `let`-bound alias. `Alias::CONST` via `struct_lit` is a third uncovered spelling (inert in both arms, recorded so the next sweep starts from it). **The commit's model points at the wrong half of its own analogy**: it says the union is *"exactly what `fn_alias` already does ten lines above"* and reuses that CONSUMER, while the defect is in where `fn_alias` is POPULATED. §9 — the audit boundary was drawn around the trigger spelling. | **FIXED — `alias_targets_of_path` consults `use_alts` and keeps every arm.** `fn_alias` already holds a LIST (R271), so the arms needed no new machinery — only the lookup the call site already does, through `lang::alias_join` rather than `format!` so a joined target cannot lose its suffix (R375). Controls are on the SAME route over DISTINCT type names and each can move (`fake::Runner::mk` writes a file, so a union reaching a control charges `Fs` and fails loudly) — R374's lesson applied rather than restated. Falsified both ways: disable the lookup and the test fails with *"must charge Exec"*; restore it and 434 + 81 pass. **A/B against `4c8420c`, all 1,545 registry crates: ADDED 0 / REMOVED 0 / CHANGED 0 — AND THAT WAS NOT A RESULT UNTIL REACH WAS MEASURED (R376).** Instrumented sweep of the same 1,545: **11 hits in 2 crates**, `serial-unix-0.4.0` (10) and `curve25519-dalek-4.1.3` (1). Every hit is a `#[cfg]`-gated CONSTANT alias — `termios::os::{linux,freebsd,openbsd,macos}::B14400`/`B28800`/`B460800`/`B921600`, and the ifma-vs-avx2 `BASEPOINT_ODD_LOOKUP_TABLE` — so the union adds edges to constants, which carry no effects. **The branch is genuinely reached and genuinely inert on this corpus, which is a different statement from "nothing changed" and is the one the flag exists to force.** The defect it closes is real regardless: the fixture goes red without the fix. **The row that stays open is R140 itself**: a row may not be CLOSED while an identical program with the call spelled differently still returns the answer the row was filed against. |
| R372 rust: `collect_decls`' `uses` map decides EDGES through `record_return` and `type_path`, and R140 handed it a THROWAWAY — the comment licensing that is false | 2026-09-10 | **CLOSED — candor-rust `94dc8b5`, 2026-09-15. Originally filed: OPEN — PRE-EXISTING, mis-stated by R140 (`20e4ac7`). Found by a review agent enumerating both call sites of one function** | **CARDINAL SIN, rust.** `collect_item_uses` has two call sites: `scan_items`, whose map builds call paths and got the real `use_alts`, and `collect_decls`, which got `let mut _decl_use_alts` — a throwaway. The comment at `decls.rs:1910` says *"call paths are built from `scan_items`' map, which is where the collision matters"*, **and that sentence is what licensed the throwaway.** Nine consumers read the decl map — `type_path` at named-field, tuple-field, enum tuple-variant payload, enum struct-variant field and assoc-type positions, `elem_type` twice, and `record_return` for free fns and for methods — and every one resolves a TYPE, which decides receiver resolution, which decides dispatch, which decides whose effects attach to the caller. Measured on two fixtures byte-identical except the ORDER of two `#[cfg]`-gated `use` lines: with the `Fs` arm written FIRST, `caller` (the `returns` index route, `fn make() -> A`) and `field_caller` (the `fields` index route, `struct HolderA { h: A }`) are **both ABSENT**; with the pure arm first, both charge `["Fs"]`. The truth on this host is `Fs` in both orders. The uncollided control `use crate::fs_arm::FR as Solo` charges `Fs` in **every** cell, which isolates the collision as the cause. Both routes are unchanged by `20e4ac7`. | Not fixed, **and one approach is now measured WRONG — that is the useful part of this row.** REPRODUCED INDEPENDENTLY 2026-09-11 before anything was changed, on two fixtures differing only in the ORDER of two cfg-gated `use` lines: `Fs` arm first → `caller` (the `returns` route, `fn make() -> A`) and `field_caller` (the `fields` route, `struct HolderA { h: A }`) **both ABSENT**; swapped → both `['Fs']`; the uncollided control `['Fs']` in every cell. **ATTEMPT 1, REVERTED: give `collect_decls` a real `use_alts` and a LOCAL map whose collided names hold `ALIAS_ALT_SEP`-JOINED values, wired to its nine type consumers.** The reasoning was that the joined spelling is R105's own and the receiver-type consumer is already join-aware — `any_arm_recordable` (`collector.rs:2591`) splits the arms and `alias_join` distributes the method leaf. **Measured: it is strictly WORSE than the defect.** `caller`, `field_caller` AND `make` disappear from the report entirely — no `calls`, no `Unknown`, no disclosure — because the `returns`/`fields` route resolves a type NAME against the decl index and a joined string matches no declaration, so resolution dies before it reaches the join-aware method-call arm. Source-order picking at least answered correctly half the time. **So the join-aware consumer is on the METHOD-CALL arm only, and the decl-index route needs its own.** Reverted, baseline re-verified identical. The next attempt wants a COMPANION map (`type_alts` beside `returns`/`fields`, the shape `use_alts` has beside `uses`) consumed where the receiver type is resolved — not a joined value in the index. Thread the real `use_alts` into `collect_decls` and union at the type consumers. **The design question is the same one R369 records and it is not the plumbing**: "push an extra edge" is the right shape for a CALL and not obviously right for a TYPE, since a receiver has one type rather than a set. **And delete the sentence, whatever else happens** — this is the register's *comment asserting SAFETY* shape, where the reviewer is the author and the claim is the one that makes their own diff correct. **CLOSED 2026-09-15, candor-rust `94dc8b5`.** Both `use`-orders now answer `['Fs']` with edges to BOTH arms. A/B over 1,563 crates: **ADDED 12 · REMOVED 0 · CHANGED 7 (0 on `inferred`)**, reach 44 type resolutions across 31 crates. This is R105's shape on the TYPE route, and it was taken together with [[R440]] while the cfg-alias area was warm — the right grouping, since both are the same index answering by tail. |
| R373 rust: the body-local `use` sites are the SIXTH and SEVENTH answering a question a five-site audit declared unified, and R140's union turned the gap into a fabrication | 2026-09-10 | **CLOSED — candor-rust `4c8420c`. Status corrected 2026-09-11: the fix shipped and this cell still read OPEN, caught by a release panel — the filter gap is PRE-EXISTING (R123); the fabrication is INTRODUCED by R140 (`20e4ac7`). Found independently by both review agents** | **FABRICATION, rust, on top of a pre-existing filter gap.** `use_item_applies`' own doc says *"FIVE SITES ANSWERED THIS QUESTION AND ONLY TWO APPLIED THE FILTER … They all call this now, so there is one authority rather than five hand-rolled loops free to drift apart again."* It enumerates module-level sites only. `CallCollector::visit_item_use` (`collector.rs:2178`) and `LocalUseCollector::visit_item_use` (`decls.rs:22`) answer the identical question for a `use` written INSIDE a body and neither calls it — **so the audit's boundary was drawn around its own trigger**, which is the failure this file's own instructions name. Pre-R140 the gap meant a body-local `#[cfg(test)]` mock could win by source order in a PRODUCTION scan (R123's defect, one route over). Post-R140 the union always includes the test arm, and **in one of the two source orders it converts a CORRECT answer into a WRONG one**: with `#[cfg(test)] use crate::mock::M as C;` written first and `#[cfg(not(test))] use crate::prod::P as C;` second, over a PURE production arm, `run` goes from **ABSENT (correct)** pre-fix to `inferred:["Fs"] calls:["mock::M::new","prod::P::new"]`. The forward order was already wrong pre-fix, so only one arm moves. The module-level control is correct in both, because `use_item_applies` drops the test arm there. `deny Exec` goes 0→1 on a mock-driven body. **The fabricated row carries a positive SURFACE and no `incomplete`/`ambiguous` marker** — while the sibling R105 mechanism deliberately captures NO surface for a collision, *"so an empty surface cannot read as a complete one and let `allow <E> <lit>` certify a claim the engine never made"*. Two mechanisms, one question, opposite postures. **Disclosure gap:** `CHANGELOG.md` (`ea74de1`) says *"no effect set moves anywhere"* and does not mention the residual at all; only the commit message and the test doc do. | Not fixed. Apply `use_item_applies` at both body-local sites, which is the same one authority and makes the doc's own sentence true for the first time. `include_tests` is not in scope at either — `fninfo` has no such parameter and `CallCollector` no such field, **which is exactly the "one site of the five that could not even express the question" that R123 recorded about `collect_root_reexports`, repeating**. `fninfo` has FOUR call sites, all in `decls.rs`, both callers already hold `include_tests`, so the thread is one parameter and four lines and R213's cost warning does not bite here. Note that the `is_cfg_inactive` half needs NO flag and can go in immediately. **Also delete `local_use_alts` (`decls.rs:1416/1418`) or wire it up**: it is built, handed to `LocalUseCollector` and never read by anything — body-local collisions actually reach the union through `CallCollector`. Dead code carrying a `// R140` comment reads as coverage. |
| R374 rust: R140's three over-charge controls CANNOT FAIL — they probe a route the fix does not touch, through a resolution path two filed rows say returns nothing | 2026-09-10 | **CLOSED — candor-rust `4c8420c`. Status corrected 2026-09-11: the fix shipped and this cell still read OPEN, caught by a release panel — INTRODUCED by R140 (`20e4ac7`). Found independently by BOTH review agents, by different methods** | **VACUOUS GATE, rust, and it is why R370 shipped.** The test's own header reads *"THE CONTROLS ARE HALF THE ROW, because a union widens what is charged"*, and asserts `ctl_both_pure`, `ctl_same` and `ctl_single` stay empty. All three are spelled `pub fn ctl_both_pure(x: &BothPure) -> usize { x.go() }` — a METHOD call on a parameter-typed receiver, i.e. the TYPE route — while the change lives in `visit_expr_call`'s `Expr::Path` arm. They cannot reach it in either loop order. **Instrumented, the branch records a collision for `BothPure` and never enters for it**; `Same` and `Single` do not even record one. Worse, the fixture's arms are `pa::P` and `pb::P` — **same-named types in sibling modules, which is R213**, an open cardinal sin that drops the edge on its own — and the type route is R369, filed by this same author hours earlier as a TOTAL LOSS. So the controls assert *"a union does not manufacture an effect for arms that have none"* over a fixture in which **no arm has any effect to manufacture**, on a route the fix does not touch, through a path two open rows say returns nothing. §A.2 exactly: the fixture inherited the blind spot of the bug report. **Proved two ways.** (i) Make one control arm effectful (`pb::P::go` writes a file) and it stays ABSENT in both arms — if the control could discriminate anything, it would move. (ii) The same shape on the PATH route with DISTINCT type names does move, correctly (`calls: ["pb::PB::mk"]` → `["pa::PA::mk","pb::PB::mk"]`) with `ctl_same`/`ctl_single` staying pure — that is the control the commit owed. Also: with the union disabled the test panics on assertion #1 and never evaluates the rest, so *"disabling the union fails 5 assertions"* counts one. `bin/assert-audit.sh candor-rust aa280d1^..aa280d1` flags both lines. | Not fixed. Replace all three with PATH-route controls over DISTINCT type names, and add the control this row is really about: **a union must not manufacture an effect for a name that is not a genuine `#[cfg]` arm set** — R370. The general rule this is the third instance of: a control written against the same fixture as the fix inherits the fix's blind spot, so a control must be shown to MOVE when the code is wrong, not merely to pass. |
| R375 rust: `use_alts` carries `ALIAS_ALT_SEP`-JOINED values and the consumer concatenates with `format!` where `alias_join` is required — and the rationale that justified the companion map is false | 2026-09-10 | **CLOSED — candor-rust `4c8420c`. Status corrected 2026-09-11: the fix shipped and this cell still read OPEN, caught by a release panel — INTRODUCED by R140 (`20e4ac7`). Latent; found independently by both review agents, one of which retracted half of its own first reading** | **FABRICATION (latent), rust.** `note_use_collision` reads `prev` straight out of `out`, and `out` ALREADY holds `ALIAS_ALT_SEP`-joined values written by `record_alias` (`decls.rs:889`) and seeded by `seed_mod_aliases`. Observed verbatim: `arc-swap-1.9.2` `Arc=alloc::sync::Arc<U+0001>std::sync::Arc`, `axum-0.8.9` `FromRef=axum_core::extract::FromRef<U+0001>axum_macros::FromRef`. The consumer at `collector.rs:2255` then builds `format!("{t}::{}", rest.join("::"))` over that — **while `lang::alias_join` exists at `lang.rs:1389` for exactly this case and distributes the suffix over the arms.** `scan.rs:2009` does split on the separator downstream, so the path is not malformed in practice; the damage is that the split happens over the WHOLE constructed path, so `alloc::sync::Arc<U+0001>std::sync::Arc::new` yields `alloc::sync::Arc` — **suffix lost** — and only `std::sync::Arc::new`. On the `pub use` route the manufactured path reaches the R105 hedge and produces `["Exec","Unknown"]` with `unknownWhy:["ambiguous:cfg-duplicated alias"]` where the private-`use` spelling of the same program unions to `["Exec"]` — **two mechanisms answering one question differently, which is the R347 shape**. Live population: 7 joined entries across 6 crates (`jiff` ×2, `redis` ×3, `similar-3.1.1`), and in `similar-3.1.1` the joined string is the ONLY route to the `web_time` arm. All inert on this corpus. **The claim being falsified is the commit's own central design argument** — that a joined value *"would flow into path construction as a literal and produce malformed paths"* and therefore justified a companion map. The companion map does exactly that. | Not fixed. Split arms on `ALIAS_ALT_SEP` when RECORDING the collision, so `use_alts` can only ever hold flat single paths and the consumer's `format!` is safe by construction — better than reaching for `alias_join` at the consumer, because it removes the joined value at the point it enters rather than at each place it is read. **Residual to state either way, and it fails CLOSED so it is not a sin:** the pushed extra edge carries `str_arg: None, argc: 0`, so `cmds`, `incomplete` and `calls` still depend on which line came first — `allow Exec "/bin/echo"` on the lossy arm returns `[AS-EFF-008] … performs Exec with no visible literal — the surface cannot be certified`, exit 1. Recorded because the row now makes a positive `Exec` claim with an empty command set where pre-fix it made no claim at all. |
| R376 rust: R140's headline A/B measured the ALPHABETICAL a–i PREFIX and reported it as the corpus — the general sentence is false on the other 61%, FAVOURABLY | 2026-09-10 | **OPEN as a claim-accuracy defect; the measurement it corrects is good news. Found independently by both review agents, one of which recovered the missing arm** | **CLAIM ACCURACY, rust — and the correction runs the other way, which is why it is worth a row rather than an edit.** The commit message says *"A/B, 600 registry crates … `inferred` ADDED 0 / REMOVED 0 / CHANGED 0 — NO EFFECT SET MOVES AT ALL"* and `CHANGELOG.md` says *"no effect set moves anywhere"*. The 600 were **not a random 39%**: they are the alphabetical `adaptive-barrier` … `indexmap-1.9.3` prefix of a 1,545-crate registry, and the run printed `REACH: NOT MEASURED`. Over the 945-crate complement: wide ADDED 17 / REMOVED 0 / CHANGED 243; `inferred` ADDED 17 / REMOVED 0 / CHANGED 14. Whole registry, both slices: 286,019 pre rows → **ADDED 17 / REMOVED 0 / CHANGED 300** (`invisible` 267, `calls` 19, `inferred` 14, `direct` 1). Zero field-value losses anywhere; all 17 added rows carry `inferred: []`. **All 14 effect-set movements are one crate and they are a REAL CLOSED CARDINAL SIN**: `sea-orm-2.0.2` `driver::rusqlite::RusqliteSharedConnection::acquire` and its 13 propagation targets gain `Clock`, ground-truthed to `src/driver/rusqlite.rs` — `#[cfg(not(target_arch = "wasm32"))] use std::time::Instant;` at line 2, `#[cfg(target_arch = "wasm32")] use web_time::Instant;` at line 10 written second and winning, with `Instant::now()` at line 292. PRE reported `acquire` with `inferred: []`. **So the fix demonstrably closes a silent under-report on real published code, and the commit could not say so because the crate that proves it sorts after `i`.** | Fix the two sentences to state the entry set and the real figures. **The general rule is the point:** a corpus slice taken by `head -n` off a sorted listing is a PREFIX, not a sample, and reporting it as "600 registry crates" reads as coverage — the same shape as `REACH: NOT MEASURED`, which the tool already prints and which this run printed and nobody read. Two cheap gates: record the ENTRY SET, not just the count, in the commit message (§E1 already says put the numbers there — it should say the entries too, since the original 600 are not recoverable from the repo), and treat a `REACH: NOT MEASURED` line in an A/B as unfinished rather than as a result. |
| R377 rust: `fixture_effects` answered "pure" for a function that IS NOT IN THE REPORT, so a purity assertion over a misspelled or module-qualified name could not fail | 2026-09-10 | **CLOSED — candor-rust, in the same commit as R370/R373/R374. Found by FALSIFYING R374's own replacement control and watching the suite stay green** | **VACUOUS GATE (test harness), rust — R374's class, committed by the person fixing R374, within the hour.** The replacement control for R374 was written as `assert!(fixture_effects(&v, "ctl_not_an_arm_set").is_empty(), ..)`; the report names that function `inner::ctl_not_an_arm_set`, because it lives in an inline module. `fixture_effects` filtered `functions[]` for an exact name match and returned an empty `Vec` when there was none, so the assertion held for a function the helper had never found. **Disabling the code under test at all three call sites did not turn the test red** — which is the only reason this was caught, and it is the check R374 says a control must pass. 21 assertions in the file have that shape. **The honest result, stated because the alarming version was available:** a first cut made the helper panic on ANY absent name and 24 tests went red — but under SPEC ⟨0.24⟩ an all-pure unit may legitimately be OMITTED from the report, so absence is how the engine says "pure" and most of those 24 were correct controls. Keyed properly, **all 21 name functions their fixture really declares; the only genuine spelling error in the file was mine.** The hazard was real and the existing suite was clean. | **FIXED — the check is keyed on the FIXTURE SOURCE, not on the report.** `scan_fixture_files`, `scan_fixture_raw`, `scan_fixture_lock` and the one test that builds its crate inline now record the `fn` names their source declares, and `fixture_effects` accepts absence only for a name the fixture actually writes — a declared-but-absent function is ⟨0.24⟩ purity, an undeclared one is a spelling mistake wearing a purity claim's clothes. `fixture_absent` exists for the case where absence IS the claim, so it has to be said out loud. **Exact leaf equality, never a prefix test** — a prefix match would re-create the silent pass this removes, which is the `deny Fs either` / `either_ifelse` trap one layer down. Verified by re-running the falsification: with the gate disabled the control now FAILS with `inner::ctl_not_an_arm_set must stay pure`, and passes with it restored. 431 + 81 green either way otherwise. |
| R378 rust: `use super::X` inside an INLINE MODULE never resolved through the parent's map, so every call through it read SILENT-PURE — and R140's union had been masking it | 2026-09-10 | **CLOSED — candor-rust, same commit as R370. Found because R370's cfg gate REMOVED the accidental mask and a full-registry A/B reported REMOVED 12** | **CARDINAL SIN, rust, PRE-EXISTING and previously invisible.** Inside an inline `mod`, `out` starts as the parent's `use` map (`submodule_uses`), so `use super::proc_alias` names a binding that is right there — but `rebound` returned the literal `super::proc_alias`, which names no local def, and the origin was LOST. Measured on a compiling fixture: `mod inner { use super::proc_alias; pub fn via_super() { proc_alias::Command::new(\"true\").status(); } }` reports **ABSENT** while the byte-identical call in the PARENT charges `['Exec']` — the control that isolates the `super::` hop as the cause. **The discovery route is the point.** R370's cfg gate stops the union firing on a non-`#[cfg]` rebind, and `use super::X` over a parent import is exactly that shape — so R140's union had been pushing the parent's binding as a second edge and papering over this defect for the effect, though not for the row. The A/B that caught it reported **REMOVED 12** (all `inferred: []`, pure `invisible` disclosure rows) — real instances in `sqlx-sqlite` 0.8.6 and 0.9.0 (`connection::worker::rendezvous_oneshot::channel` and `Sender::send`, where `use futures_channel::oneshot;` at file scope is rebound by `use super::oneshot::{self, Canceled};` inside `mod rendezvous_oneshot`), `mio` ×2, `rustls` ×4, `rustix` ×2. **On the corpus the loss was disclosure only; on the fixture it is an EFFECT.** The corpus had no effectful instance, which is precisely why a REMOVED row must be traced to source rather than counted. | **FIXED IN TWO HALVES, AND THE SECOND HALF COST A REGRESSION TO FIND.** `super::X` is re-resolved only INSIDE AN INLINE MODULE, and then only when `X` is a key in `out`. **The first cut had only the second test and shipped a regression** that the next full-registry A/B caught in the REMOVED column: **19 rows lost in `hyper` 0.14.32/1.11.0/1.11.1, two of them carrying a real `Log`** (`Conn::end_body`, `Conn::write_body`). `hyper/src/proto/h1/conn.rs` binds `io` to `std::io` on line 2 and writes `use super::io::Buffered` on line 19 — where `super::io` is hyper's OWN `proto::h1::io` MODULE. `out` is the ENCLOSING module's map only for an inline `mod`, where `submodule_uses` clones it; for a FILE module `out` is that file's own map while `super::` still means its parent, so the head lookup was searching the wrong scope. `submodule_uses` now plants `SUPER_SCOPE_MARKER` — the key `"super::"`, which no `use` can bind because `collect_use` stores bare identifiers and an identifier cannot contain `::`, so it cannot collide and `expand`'s whole-segment matching can never match it. **Both halves are pinned by a test that goes red when THAT half alone is disabled**, and the file-module fixture is hyper's shape verbatim: a sibling module named `io` beside a file-level `use std::io`. Threading a boolean would have touched ten call sites including five in tests; the marker states the condition where it is actually known. **`super::X` is re-resolved when, and only when, `X` is a key in `out`** — the original narrowing, and its argument is unchanged: The narrowing is the entire safety argument and the pre-existing comment is why it is needed: a `super::` path is relative to a module whose path `collect_use` does not know, so resolving one blindly drops the module context and breaks tail2's link to a LOCAL def — clap's `super::core::display_width`, which the comment names. Requiring the head segment to be a `use` KEY separates the two exactly: `core` is a local MODULE and not a key, so it keeps its literal; a parent's imported alias IS a key, and resolving it is the only way to keep its origin. Leading `super::` segments are stripped repeatedly so `super::super::X` is covered. **The general lesson, and it is the expensive kind:** a fix that removes a WRONG mechanism can expose a real defect the wrong mechanism was accidentally compensating for, and the exposure looks exactly like a regression introduced by the fix. The REMOVED column is what distinguishes them, and only if every row in it is read against source. |
| R379 rust: the AS-EFF-008 MASKING GUARD's own allowlist is short, so a benign sibling literal CERTIFIES a DNS lookup of a caller-supplied host — `allow Net <lit>` returns exit 0 | 2026-09-11 | **CLOSED — candor-rust. Found by a hand-list sweep dispatched on the R345–R348/R350 vein, reproduced independently with a compiling fixture and a working control before being believed, and fixed in the OPPOSITE direction to the one this register's own rule recommends — because the corpus said so** | **CARDINAL-SIN-CLASS GATE BYPASS, rust, and it is in the ONE list in the crate built specifically to prevent this.** `is_net_establishing` (`candor-classify/src/lib.rs:3343`) decides whether a `Net` call with NO captured host literal marks the surface `incomplete` — `scan.rs:2696`: `if c.str_arg.is_none() { if eff == "Net" && is_net_establishing(&c.leaf) { … incomplete … } }`. Its 12 names are `connect`, `connect_timeout`, `get`, `post`, `put`, `patch`, `delete`, `head`, `request`, `send_to`, `lookup_host`, `to_socket_addrs`. **The DNS RESOLVER verbs are absent — and `classify()` in the SAME FILE already enumerates them**: line 1032 maps `hickory_resolver::…::lookup_ip` / `::lookup` / `*_lookup` to `Net`, and line 4044 asserts it. Two lists, one question, never connected — the §G shape this register keeps recording. **MEASURED END TO END** on a fixture that `cargo build`s clean against real `hickory-resolver 0.26.2`: `pub async fn masked(r: &Resolver<TokioRuntimeProvider>, host: &str) { r.lookup_ip("good.example.com").await; r.lookup_ip(host).await; }` reports `inferred:['Net'] hosts:['good.example.com'] incomplete:NONE`, and **`allow Net good.example.com` exits 0** over a DNS resolution of a caller-controlled hostname. The isolating control is one verb over and in the same fixture: `TcpStream::connect(host)` beside the same literal reports `incomplete:['Net']` and fails closed with `[AS-EFF-008] … the surface cannot be certified`, exit 1 — **so the guard MECHANISM is sound and only its enumeration is short**, which is what makes this a list defect rather than a design one. **THE DOC'S OWN SAFETY CLAIM IS THE FALSIFIED PART**, and it is why nobody measured this: *"Under-catching an unusual establishing verb is a missed mask (sound-with-disclosure), never a broken gate."* It IS a broken gate whenever a benign sibling literal sits in the same function — which is precisely the scenario the guard's first sentence says it exists to stop (*"else a benign sibling literal masks the runtime endpoint"*). The comment asserts the safety of the diff it sits in, which this register records as the shape nobody checks. | **FIXED — the resolver verbs are added to the ALLOWLIST, and the DENYLIST INVERSION WAS BUILT, MEASURED AND REJECTED. The rejection is the durable half of this row.** `is_net_establishing` now also answers true for `lookup`, `lookup_ip`, `resolve` and — mirroring `classify`'s own test — any `*_lookup`, so the whole record-type family (`srv_`/`txt_`/`mx_`/`cname_`/`ipv4_`/`reverse_`…) is covered rather than the one spelling that was found (R346). **Falsified at BOTH levels, which is the form worth copying:** disabling it turns the unit test red AND returns the live gate to exit 0 on the hickory fixture. **A/B, all 1,545 registry crates: ADDED 0 / REMOVED 0 / CHANGED 39, `inferred` CHANGED 0** — only `incomplete` moves, 39 rows gain it and NONE loses it. Every gained row is a real DNS resolution with a runtime host: `mongodb` ×36 (`AsyncResolver::{srv,txt,ip,cname}_lookup`, `SrvResolver::get_srv_hosts`, and `client::auth::gssapi::canonicalize_hostname`) and `async-rs` ×3. Ground-truthed: `mongodb-3.9.0/src/runtime/resolver.rs:63` is `pub async fn srv_lookup(&self, query: &str)` — the host is a PARAMETER, so the endpoint is structurally invisible and `incomplete` is the honest answer. REACH instrumented: 170 guard firings across 57 crates. **THE DIRECTION REVERSAL, and why it is recorded rather than quietly dropped.** The obvious fix was to INVERT to a denylist of use-verbs: an allowlist's omissions fail SILENTLY (a missed mask certifies a runtime endpoint) while a denylist's fail LOUDLY, which is `candor-denylist-over-allowlist`'s rule and exactly the inversion R350 made one list over. **It was built and A/B'd, and the corpus refused it: 544 rows gained `incomplete` and none lost it.** The cause is a constraint the rule does not know about — **a WHOLE-CRATE-Net crate classifies EVERY method as Net**, so a denylist masks essentially everything in one (`cap-std` 66 rows, `hickory-net` 44). Its measured triggers were conversions, accessors and combinators that hide no host (`into` 24, `from_std` 20, `as_socket` 15, `setsockopt` 32, `map_err` 7, `ok` 5, `try_clone` 4), and — sharpest — **`bind` (31), which this very file has `is_net_local_bind` to exclude because a bind address is where the process LISTENS, never a destination it reaches.** **The exception was already written down one guard over and I did not read across:** `is_cmd_naming_method`'s doc says *"an ALLOWLIST, not 'any method except known modifiers'. A whole-crate-Exec crate classifies EVERY method as `Exec`, so a denylist leaked NON-naming methods"* — the same constraint, the same file, stated for `Exec`. **The general rule was applied without checking the local one, and only the corpus caught it.** The twelve measured leak verbs are now pinned by `the_rejected_denylist_leaks_are_not_establishing`, so the inversion cannot be re-attempted without meeting the number. Original filing text follows: ~~The DIRECTION is the decision, and the family's own rule answers it~~: this is an ALLOWLIST whose omissions fail SILENTLY (a missed mask = a certified runtime endpoint), while the mirror DENYLIST — mark incomplete unless the verb is a known USE-verb on an already-established connection (`write`/`read`/`flush`/`send`/`recv`/`peek`) — fails LOUDLY (an over-mask = `allow Net` fails closed where it need not). `candor-denylist-over-allowlist` says narrow a sound over-approximation with a denylist and state which direction it fails in first; R350 is the same inversion already made once, in this engine, for the same reason. **Whichever direction is chosen, the WHOLE FAMILY must be written down and run rather than the spelling in hand — R346's entire lesson.** Unverified same-shape candidates the sweep flagged and did NOT gate-test: `hyper`/`h3`/`quinn`/`tokio_rustls` `send_request` and `handshake`; some of those take an already-established connection object rather than a raw target, which would make them correctly absent, so each needs its own fixture before it is added. The `Fs`, `Exec` and `Db` analogues (`is_fs_path_arg`, `is_cmd_naming_method`, `is_db_query_arg`) answer the same question for their effects and are the obvious next sweep — none has been checked for this. |
| R380 rust: `resolve_recv_traits`' MethodCall arm peels NOTHING, so `opt.as_ref().unwrap().go()` on a trait-object field is SILENT while the binder spelling charges | 2026-09-11 | **CLOSED — candor-rust. Found by the hand-list sweep, reproduced independently before being believed. R347's shape one route over: two copies of one question, and only one of them walked** | **CARDINAL SIN, rust, on one of the most ordinary ways Rust calls an optional handler.** `resolve_recv_traits` decides the trait leaves of a method call's RECEIVER, which is what drives bounded CHA on a `dyn` field. Its `Expr::MethodCall` arm (`collector.rs:1336`) does exactly one thing: look the method leaf up in `returns` and decode a `<dyn>` sentinel — R42's factory-return fix, never widened. **It does not peel identity adapters**, so a receiver written as a chain resolves to nothing. Its SIBLING `resolve_elem_trait_leaves` peels the R347/R350-unified `is_element_preserving_adapter` list — so the engine already owns the list this arm does not consult, which is R347's finding (*"the two element resolvers kept SEPARATE copies and diverged in BOTH directions"*) repeated at a third site. **Measured on one fixture, `cargo build` clean, four spellings of the same call against `Option<Box<dyn Doer>>` and `Mutex<Box<dyn Doer>>` fields whose impl writes a file:** the binder form `if let Some(h) = &self.inner { h.go() }` charges **`['Fs']`** — full CHA resolution, the control that proves the field and its dyn leaves are recorded — while `self.inner.as_ref().unwrap().go()`, `self.guard.lock().unwrap().go()` and the `let`-bound spelling of the latter are **all three ABSENT: no row, no `Unknown`, no `invisible`.** A purity claim over a body that writes a file, and the only difference is how the receiver is spelled. | **FIXED IN TWO HALVES, AND THE SECOND HALF CORRECTS THIS ROW'S OWN STATED MECHANISM.** (1) `resolve_recv_traits`' MethodCall arm now WALKS THE CHAIN to the receiver, exactly as `resolve_recv_type` has always done for the concrete-type question about the same expression, declining only on a type-CHANGING method. The two resolvers now share `is_recv_type_changing`, extracted so they cannot drift apart again — R347's residual gains a consumer, though `ctor_type`'s THIRD copy is deliberately left alone, as R347 priced unifying it as a guess. (2) **`Option` was missing from `trait_leaves`' wrapper peel** (`Box`/`Rc`/`Arc`/`RefCell`/`Mutex`/`RwLock`/`Cell`), so an `Option<Box<dyn Doer>>` field had NO `trait_fields` entry at all. **This row asserted the opposite** — *"needs nothing else, because the control proves `trait_fields` already holds the field's dyn leaves"* — inferred from the binder control, which charges through the Option-PAYLOAD route and therefore proves something else. After (1) alone the Mutex chains resolved and the Option chain still did not, which is how the claim was caught. **A row's stated mechanism is a hypothesis, including when its author wrote it an hour earlier.** `Result` is deliberately NOT peeled alongside: its `Err` arm is a different type and R347 already priced that shape. **A/B, all 1,545 registry crates, 286,042 → 286,099 rows: ADDED 57 / REMOVED 0 / CHANGED 164 (`inferred` ADDED 57 / CHANGED 24). Every NEW EFFECT CLAIM was traced to a body, which is the whole discipline for a widening.** Of the 57 added: 37 carry `['Unknown']` and 18 `[]` — disclosure direction; **2 carry `['Rand']` and are correct** — `quinn-proto::connection::paths::PathData::reset` calls `config.congestion_controller_factory.clone().build(..)` through a `dyn` factory, and `BbrConfig::build` → `Bbr::new` → `Pcg32::from_rng(&mut rand::rng())` (`congestion/bbr/mod.rs:101`). Of the 24 effect-set movers, **`criterion`'s `Html::generate_plots`/`summarize`/`measurement_complete` gain `Exec` and `Clock`, and that is a CLOSED CARDINAL SIN on published code**: `Html` holds `plotter: RefCell<Box<dyn Plotter>>` (`html/mod.rs:273`) reached via `borrow_mut()` — R380's chain exactly — and the gnuplot backend really spawns processes (`child.wait_with_output()`, *"Waiting for N gnuplot processes"*). criterion's HTML report generation spawns subprocesses and candor reported `['Fs','Unknown']`. The rest gain `Unknown` (`rayon-core`, `objc2`), the safe direction. **REMOVED 0 — nothing lost anywhere.** The over-charge control is in the test: the identical chain over a PURE trait-object field stays pure, because walking a chain TYPES the receiver and does not charge it. Original plan text follows: ~~Peel identity adapters in the MethodCall arm and recurse on the receiver~~, so the chain ends at the `Expr::Field` arm that already works — `as_ref`/`as_mut`/`as_deref`/`unwrap`/`expect`/`clone` are the Option half and need nothing else, because the control proves `trait_fields` already holds the field's dyn leaves. **The `Mutex`/`RwLock`/`RefCell` half is NOT in scope and must not be folded in: it additionally needs `elem_type` to peel the guard off the TYPE, which R347 BUILT, MEASURED AND BACKED OUT** because the type peel makes the HOF closure route hand a `PoisonError` to a closure typed as the container's element and fabricates `Exec` on `async-process`'s `Reaper::has_zombies`. R347 says the closure route must learn `unwrap_or_else`'s parameter is the ERROR first. So the honest scope here is the Option chain; the guard chain stays filed under R347 with its price already measured. **Write the whole family down and run it (R346)** rather than adding the two spellings in hand. |
| R381 swift: R379's GATE BYPASS, in the second engine — the Net establishing tables omit every DNS RESOLVER and `NSURLConnection`, which the sibling κ tables classify as `Net` | 2026-09-11 | **CLOSED — candor-swift `2ea6ec5`. Status corrected 2026-09-11: the fix shipped and this cell still read OPEN, caught by a release panel — found by taking R379's question to the next engine rather than filing it and stopping, which is R346→R348's lesson. Reproduced independently with a compiling fixture and a CALIBRATED instrument before being believed** | **CARDINAL-SIN-CLASS GATE BYPASS, swift — the same defect as R379, in the same shape, one engine over.** `isNetEstablishingMember` / `isNetEstablishingFree` (`Sources/CandorCore/Classifier.swift:1315–1329`) decide whether a `Net`-classified call's locator is an ARGUMENT of that call — which gates both whether a literal host is captured into `hosts` at all and whether a missing one marks the effect `incomplete`. `isNetEstablishingFree` is **two names**: `NWConnection` and `NWListener`. Meanwhile `kappaFree` at line **1585** classifies `getaddrinfo`, `getnameinfo`, `gethostbyname`, `gethostbyname2`, `gethostbyaddr`, `gethostbyname_r`, `gethostbyaddr_r` and `getaddrinfo_a` as `Net` — eight resolver verbs the masking table has never heard of — and `kappaMember` (1249) classifies `NSURLConnection`'s `sendSynchronousRequest` / `sendAsynchronousRequest` / `start` / `init` as `Net` while `isNetEstablishingMember`'s switch has no `NSURLConnection` case at all. **Two tables, one question, never connected — R379's sentence verbatim.** MEASURED on a fixture that builds and runs, with the instrument CALIBRATED first: a function making a `URLSession` request to a literal `api.stripe.com` beside `getaddrinfo(callerHost, "443", …)` reports `inferred:['Net'] hosts:['api.stripe.com'] incomplete:NONE`; **`deny Net` exits 1** — so the gate is live and able to fail — and **`allow Net api.stripe.com` exits 0**. The resolver's destination appears NOWHERE in the report. A policy saying *"this code may only reach api.stripe.com"* returns green over a DNS resolution of a caller-controlled name. | Not fixed at filing. Add the eight resolver free-functions to `isNetEstablishingFree` and an `NSURLConnection` case to `isNetEstablishingMember`, writing the WHOLE family down rather than the two spellings measured (R346). **DIRECTION: keep the ALLOWLIST and do not invert, unless the swift-specific version of R379's counter-example is checked FIRST.** R379 built the denylist inversion, A/B'd it and was refused by the corpus — 544 rows over-masked, because a whole-crate-Net classification makes every method Net and a denylist then masks everything in such a crate. candor-swift classifies per-file and syntactically, so that specific counter-example may not apply — **but 'may not apply' is reasoning, not a measurement, and R379 is exactly the row that records what happens when a general rule is applied without checking the local constraint.** The swift A/B over the four real packages is cheap; run it before inverting. Also flagged by the sweep and NOT reproduced, so each needs its own fixture before being added: `NWBrowser`/`NetService`/`NetServiceBrowser` (classified `Net` by both κ tables, absent from both establishing tables — but a bonjour ctor may carry no host argument at all, which would make its absence correct), and on the `Fs` side `SecItemAdd`/`SecItemCopyMatching`/`SecItemUpdate`/`SecItemDelete`. **The `Fs`, `Exec` and `Db` analogues of this table are the obvious next sweep in BOTH engines — R379 left the same note and neither has been checked.** |
| R382 umbrella: `ci-watch.sh --wait` reported REQUIRED BUT ABSENT over a workflow that RAN AND SUCCEEDED — a FALSE RED in the instrument that authorises releases | 2026-09-11 | **CLOSED — candor. Reproduced both ways on an unchanged HEAD; the CAUSE was located and is now fixed. The TRIGGER is still not established and DOES NOT GATE THE FIX, which is the correction this row needed** | **FALSE ALARM (loud direction) in a release-authorising gate.** At 01:33 `ci-watch.sh --wait` printed, for the umbrella repo, BOTH `integrations ✔ success` AND `integrations.yml ✘ REQUIRED BUT ABSENT — this commit touches bin/release-test.sh`. The run exists: `gh run list --workflow=integrations.yml` gives `d1e295cb completed/success`, and `ad0cb13` IS `HEAD`. The workflow map is unambiguous — `gh workflow list` returns wfid `305647574` → `.github/workflows/integrations.yml`, and the run reports exactly that wfid. **Not a rate limit:** `gh api rate_limit` read `core 5000/5000` at the time, which is checked because this register already records that a rate-limited read is BLOCKED, not green. **Reproduced on the variable:** with `--wait` the ABSENT row appears; without it, on the same HEAD minutes later, it does not — confirmed twice in each direction. **MECHANISM, located by reading:** the REQUIRED-BUT-ABSENT arm compares FILES, and `have_fn` is filled by `path_for_id`, which **falls back to the bare numeric id when the map has no entry for a run's workflow**. That fallback is deliberate and correct for the cancel-in-progress arm (a deleted workflow then resolves to no path and fails closed) but in `have_fn` it fails the OTHER way: the row still prints by NAME while its FILE silently becomes a number, so the file comparison misses and manufactures an ABSENT. **WHAT IS NOT ESTABLISHED, and it is the half that matters for the fix: WHY the map lacked that entry on that poll.** `WF_PATH_FAILED` was 0 (no degraded-map line was printed), so it was not a wholesale failure; a partial or transient `gh workflow list` is the hypothesis and it has NOT been reproduced. **Why this is worth a row rather than a shrug:** the script's own comments say a guard that cries wolf gets deleted, and a FALSE absent is indistinguishable from a REAL one — so it does not merely annoy, it teaches the reader to discount the arm that catches a genuinely unrun gate. This is the instrument `git push is not the end of a verification` depends on. | **FIXED — an unresolvable id is now a NAMED red row and the REQUIRED arm is skipped for that repo.** `path_for_id`'s bare-id fallback is left alone: it is DELIBERATE and correct for the cancel-in-progress arm, where a deleted workflow resolving to no path fails closed. The detection moved to the `have_fn` site, which is the only consumer that fails the other way. **`CI_WATCH_FAULT=drop-map-entry` makes the arm reachable on demand**, and building it taught the lesson twice over: the first version dropped the FIRST map entry, which belongs to whichever workflow `gh` listed first and usually has no run at HEAD — nothing referenced it, the arm never fired, and **the hook looked installed while proving nothing**, which is the exact failure it exists to catch one level down. It now empties the map, so every row is unresolved. Falsified both ways: with the fault, three named `cannot map run to a workflow FILE` rows and exit 1, and NO manufactured REQUIRED BUT ABSENT; without it, byte-identical to before at exit 0. **AND THE ROW'S OWN STOP CONDITION WAS WRONG.** It said *"the trigger must be reproduced before the fix is believed"*. A plan review pointed out that the fix converts a SILENT degradation into a LOUD one — why `gh workflow list` came back short is a GitHub question that does not gate it, and waiting for a once-a-night transient before making the release-authorising instrument stop lying is the documented-limitation-is-not-measured shape. Original remedy text follows: ~~An unresolvable id must be a NAMED row, not a silent fallback into `have_fn`~~ — the same discipline the script applies everywhere else (`wf-expected.py` failure, `gh` failure and the stall arm are all named, red lines rather than quiet degradations). Two sub-fixes: (1) `path_for_id` gets a caller-visible "unresolved" answer distinct from a path, and the `have_fn` accumulation turns that into `✘ cannot map run <id> to a workflow file` rather than poisoning the comparison; (2) the REQUIRED arm is skipped for a repo with any unresolved id, exactly as it already is when `WF_PATH_FAILED` is 1, because a comparison against an incomplete `have_fn` cannot be trusted in either direction. **The trigger must be reproduced before the fix is believed** — a fault-injection hook already exists for this arm (`CI_WATCH_FAULT=drop-row`), and the natural companion is a hook that drops one entry from `WF_PATH_MAP`, which would make this failure reproducible on demand instead of once a night. |
| R383 rust: the Fs masking guard's `!c.method` gate conflates *path is the RECEIVER* with *is a METHOD*, so `OpenOptions::new().open(runtime_path)` is never marked incomplete — `allow Fs <lit>` exits 0 | 2026-09-11 | **CLOSED — candor-rust. Found by the INVERSE probe R379/R381 left as their own follow-on: print every call carrying an allowlisted effect that has NO literal and was NOT masked, i.e. exactly the population a missing establishing verb hides in** | **CARDINAL-SIN-CLASS GATE BYPASS, rust, LIVE, on one of the commonest file-opening idioms in the language.** `scan.rs`'s Fs arm reads `eff == "Fs" && !c.method && is_fs_path_arg(&c.leaf)`. The `!c.method` gate is there for a real reason, stated in its own comment — the path-STAT methods (`p.metadata()`, `p.exists()`) carry the path as the RECEIVER, not an argument, so masking them would be wrong. **But it excludes EVERY method form, and `OpenOptions::open(path)` is a method whose path IS the argument.** MEASURED, instrument calibrated first: a function doing `File::open("/etc/hostname")` beside `OpenOptions::new().read(true).open(caller_path)` reports `paths:['/etc/hostname'] incomplete:NONE`; **`deny Fs` exits 1** — the gate is live — and **`allow Fs /etc/hostname` exits 0**. A policy saying *"this code may only touch /etc/hostname"* returns green over opening a caller-supplied path. The isolating control is in the same fixture: the FREE-fn spelling `fs::read_to_string(caller_path)` beside the same literal marks `incomplete:['Fs']` and fails closed, so the mechanism is sound and only the METHOD form escapes. | **FIXED — `is_fs_path_arg_method`, a pair test on the TAIL of the resolved call path, so std, tokio, `cap_primitives` and `fs_err` are covered by one rule without naming a crate.** Falsified at BOTH levels: disabling it turns the unit test red AND returns the live gate to exit 0 on the fixture; restoring closes it. **A/B, all 1,545 registry crates: ADDED 0 / REMOVED 0 / CHANGED 88, `inferred` CHANGED 0, and ZERO field-value losses** — only `incomplete` moves, 88 rows gain it and none loses it. Every gained row is a genuine path-taking builder call: `tempfile` ×19, `bindgen` ×6, `tokio` ×5, `redox_users` ×5, `fs-err` ×8, `fs-set-times` ×4 and the rest the same shape. Ground-truthed: `fs-err-3.3.1/src/open_options.rs:69` is `pub fn open<P>(&self, path: P)` — the path is a generic PARAMETER, so the locator is a runtime value and `incomplete` is the honest answer. **`argc` does NOT discriminate and the RECEIVER TYPE does** — instrumented, the four shapes are `OpenOptions::open` (method, argc 1, path IS the arg → must mask), `File::read_to_string` (method, argc 1, BUFFER arg → must not), `File::write` (method, argc 1, buffer → must not) and `Path::exists` (method, argc 0, path is the RECEIVER → must not). **The family comes from a registry sweep rather than the one spelling that was found (R346):** across 1,545 crates the path-taking METHOD pairs are exactly `OpenOptions::open` (69 std + 2 tokio + 9 `cap_primitives`) and `DirBuilder::create` (5 std + tokio). Every OTHER method on those same receivers is a builder modifier (`read`/`write`/`truncate`/`mode`) or a combinator (`map`/`map_err`/`or_else`/`with_context`) and takes no path; `std::fs::File`'s 267 method hits are all use-verbs whose path was fixed at open. So the fix is a pair test on the TAIL of the resolved call path — `…OpenOptions::open`, `…DirBuilder::create` — which covers std, tokio, `cap_primitives` and `fs_err` in one rule without naming their crates. **DIRECTION: keep the allowlist.** R379 built the denylist inversion for the Net twin, A/B'd it and was refused (544 rows over-masked); here a denylist over method forms would mask all 267 `File` use-verbs and every combinator on a whole-crate-Fs receiver. **The `Exec` and `Db` twins were checked by the same sweep and are CLEAN-or-LATENT, recorded so the boundary of this audit is stated:** `Exec` lets through only `arg`/`args`/`output`/`spawn`/`status`/`stdin`/`stdout`/`stderr` — the head is fixed at `new`, which IS masked — and `Db` lets through lifecycle and build-then-execute terminals plus the raw `sqlite3_*` C API. **THE SQLITE3 GAP IS LIVE — THIS SENTENCE WAS WRONG AND IS CORRECTED BY R386, FOUND BY A PLAN REVIEW ATTACKING IT.** What follows was measured on an ISOLATED function and is true of that shape only; the vein's actual shape is a benign SIBLING literal, which was not tested here and which exits 0. Original text: ~~The sqlite3 gap is REAL but NOT LIVE and the distinction was measured, not assumed:~~ rust's `is_db_query_arg` contains ZERO `sqlite3_*` entries while candor-swift covers the whole family by prefix (`hasPrefix(DB_FREE_PREFIX)`) — a cross-engine divergence — but a C-string literal (`c"SELECT id FROM users"`) is not captured into `tables` at all, so such a function has an EMPTY surface and `allow Db users` already fails closed on emptiness (verified: exit 1, AS-EFF-008 *"no visible literal"*, with `deny Db` exit 1 as calibration). It becomes live the moment C-string capture is added, which is a plausible improvement, so it is filed rather than dismissed. |
| R384 umbrella: `ci-watch` uses COMMIT AGE as its proxy for push time, and this project's own rule — run the gates BEFORE pushing — makes that proxy wrong every time | 2026-09-11 | **CLOSED — candor. Hit TWICE in one session, on two different push waves, and diagnosed only because the same false red appeared the second time. THE FIX'S FIRST CUT WAS ALSO WRONG AND ITS OWN FALSIFICATION CAUGHT IT** | **FALSE RED in the release-authorising instrument, with a cause that is specific to this project's own workflow.** The `NO RUN AT HEAD` branch already has a guard for the window after a push where GitHub has not created the run yet — added 2026-08-21 after being *"hit twice in one session"*, the same phrase this row earns again. It reads `_age = now - git log -1 --format=%ct HEAD` and treats an age under 90s as *"no run yet"* (pending) rather than a hard failure. Its own comment states the weakness honestly: *"Commit age is the proxy: push time is not observable from here."* **It IS observable, and the proxy fails on exactly the sequence CLAUDE.md mandates:** commit → *run that repo's gate list* → push. `bin/gate-run.sh candor-rust` takes minutes, so by the time the push happens the commit is long past 90s. MEASURED on this wave: commit age **421s**, while the upstream ref `.git/refs/remotes/origin/main` has mtime `02:27:12` — the actual push, **81s** before ci-watch ran. Both of tonight's false reds are this: `NO RUN AT HEAD` for candor-rust while `gh run list` shows `ci`, `realworld-oracle` and `realworld-oracle-deep` all `in_progress` at that exact SHA. **And it defeats `--wait` specifically**, which is the whole point of that flag (*"Without this every use of this script against a fresh push is: run it, see PENDING, run it again"*): `NO RUN AT HEAD` is a hard failure, so the re-exec loop returns instead of waiting. Distinct from R382, which is a different false red in the same instrument via `path_for_id`'s numeric fallback — two independent defects, one script, both found in one night of using it as intended. | **FIXED — the upstream ref's mtime is the push-time proxy, falling back to commit age.** `.git/refs/remotes/<upstream>` is rewritten BY the push, so its mtime is when this machine pushed — local, cheap, no network, no `gh` call. **THE FIRST CUT DID NOT FIRE, AND THE FALSIFICATION IS WHY I KNOW:** the new block was inserted where the old `_age=` line sat, which is ELEVEN LINES BEFORE `_up` is assigned, so `_upref` was `$d/.git/refs/remotes/` (empty), `stat` failed, and it fell silently back to commit age — the exact behaviour it was replacing, wearing the new code's comment. It looked installed and changed nothing. Moved after the ancestry check, where `$_up` exists. **Both arms now measured, the only variable being the ref's mtime:** fresh push → *"no run yet (pushed 0s ago)"*, pending; a 20-minute-old push → `NO RUN AT HEAD`, red, which is the right answer for a workflow that genuinely never ran. **And the arm is now reachable on demand: `CI_WATCH_FAULT=no-rows`**, the sibling of the existing `drop-row` hook, forces the empty-rows branch so the pending-vs-red decision can be SEEN rather than reasoned about. That is how the commit-age proxy survived being wrong — the arm only fires in the seconds after a push, and nobody watching a healthy push looks twice. The ancestry check still runs FIRST (the 2026-09-02 note), so an unpushed HEAD says NOT PUSHED and an mtime can never make one look freshly pushed. Original plan text: ~~Use the UPSTREAM REF's mtime as the push-time proxy, falling back to commit age.~~ `.git/refs/remotes/<upstream>` is written by the push itself, so its mtime is the push time for any push from this machine — local, cheap, no network, and no `gh` call. Fall back to the existing commit-age proxy when the ref is PACKED (no loose file), which preserves today's behaviour rather than regressing it. **Keep the ancestry check first**, exactly as the 2026-09-02 note requires — a young HEAD that was never pushed must still say NOT PUSHED, and mtime would otherwise make an unpushed commit look freshly pushed. **The fix must be falsifiable:** the script already has `CI_WATCH_FAULT=drop-row` for the sibling arm, and this wants the same treatment — a hook that forces the empty-rows branch so the *"no run yet"* line can be seen to fire, rather than waiting for the next real push to find out. **A guard that has now produced two false reds in one night is on its way to being ignored, which is the failure mode this script's own comments keep naming.** |
| R385 swift: R381's SIBLINGS in the same two tables — a bonjour BROWSE and a Keychain WRITE are classified but not establishing, so a benign literal certifies both | 2026-09-11 | **CLOSED — candor-swift `eb7e0bc` (Fs/Keychain) and `cd630c3` (Net/bonjour). The Net half took FIVE attempts; the first four all edited establishing PREDICATES and this path reaches none of them. Both were found because the sweep flagged them REASONED-NOT-MEASURED and declined to claim them** | **CARDINAL-SIN-CLASS GATE BYPASS ×2, swift, and the sweep was right to refuse to assert them without a fixture — one of its two guesses about WHY was wrong.** R381 closed the resolver verbs and `NSURLConnection` in `isNetEstablishingFree` / `isNetEstablishingMember`. These are the same tables, different entries. **(a) Net — bonjour/mDNS.** `NWBrowser`, `NetService` and `NetServiceBrowser` are classified `Net` by BOTH κ tables (`Classifier.swift:1272`, `:1633`) and are in NEITHER establishing table. Measured: a function issuing a `URLSession` request to a literal `api.stripe.com` beside `NWBrowser(for: .bonjour(type: callerType, domain: nil), using: .tcp)` reports `inferred:['LocalNetwork','Net'] hosts:['api.stripe.com'] incomplete:NONE`, and **`allow Net api.stripe.com` exits 0** — also when the browse is ISOLATED in its own file with nothing else to fail the gate. **(b) Fs — the Keychain.** `SecItemAdd`/`SecItemUpdate`/`SecItemDelete`/`SecItemCopyMatching` are classified `Fs` (`:109`/`:117`, the system secure store) and `isEstablishingFree(effect:"Fs")` is `name == "FileHandle"` OR `name == "fopen"`, and nothing else. Measured: `"x".write(toFile: "/tmp/benign.txt")` beside `SecItemAdd(runtimeQuery, nil)` reports `paths:['/tmp/benign.txt'] incomplete:NONE` and **`allow Fs /tmp/benign.txt` exits 0** over a write to the Keychain. **CALIBRATION FIRST, so neither is a dead instrument:** `deny Net`, `deny Fs` and `deny LocalNetwork` each exit 1 on the same fixture. **THE SWEEP'S REASONING WAS HALF RIGHT AND THAT IS THE USEFUL PART:** it declined to claim (a) because *"a bonjour ctor may carry no host argument at all, which would make its absence correct"*. It carries no HOST — but the absence is still wrong, because the effect is claimed with a surface that omits it and is then marked COMPLETE. Not-having-a-locator and having-an-invisible-one are different, and only the second is safe to leave unmasked. | **Fs HALF FIXED — `isOpaqueLocatorFree`, establishing-yes / capture-no.** One authority, two consumers ("does this establish" and "is its literal withheld"), so R347's two-copies shape cannot start here. Falsified both directions; fabrication control holds (a LITERAL bonjour type yields `hosts=None`); over-reach controls assert `NWConnection`/`getaddrinfo`/`connect`/`fopen` are NOT opaque, because withholding THEIR literals would destroy the surface the engine exists to publish. Suite 1133/1133, gates 11/11. **THE A/B IS NOT EVIDENCE HERE AND IS NOT CITED AS ANY:** 0/0/0 over the four Swift packages, and a direct check found ZERO files in any of them containing `SecItem` or `NWBrowser` — the corpus never reaches the branch. `corpus-ab` refuses that number by design and R376 is this register's record of what citing one costs. **Net HALF CLOSED ON THE FIFTH ATTEMPT — the first FOUR were all wrong in the same way, and the record is the point.** Patches went to the free-call site, the ctor site, `isNetEstablishingMember`, and the member site's literal withholding. Every one built clean, kept the suite green, and left the fixture BYTE-IDENTICAL — the correct-code-on-an-untaken-path shape, four times over. **Instrumenting ALL FOUR `est`-bearing `recordSurfaces` call sites settled it: `recordSurfaces` has NINE call sites, not the three I had been working from, and the bonjour browse reaches NONE of them.** On the fixture the probe fires exactly twice — `eff=Net est=true` for the benign `URLSession` call (which correctly marks nothing, its literal is present) and `eff=Fs est=true` for the Keychain fix. `NWBrowser`'s `Net`+`LocalNetwork` are recorded on a path that **never calls `recordSurfaces` at all**, so `incomplete` cannot be marked for it by ANY change to an establishing predicate. That is why four correct-looking edits did nothing. **The inert edits were REVERTED rather than left in place: an `isNetEstablishingMember` case for a shape that never reaches the predicate is R348's exact defect** — a list entry in a position it can never match, which reads as coverage to the next auditor. **FIXED by setting `incompleteSurfaces` DIRECTLY at the three sites that insert the effect** — the alias/free spelling, the ctor, and the member verb `start()` — because there is no surface machinery on those branches to route a predicate answer through. Probed the entry first: the fixtures reach the CTOR and MEMBER sites, not FREE, so FREE was patched anyway and carries its OWN fixture (`NetServiceBrowser().searchForServices`) rather than shipping as an untested guess (R348). Measured: `allow Net api.stripe.com` 0 → 1, `deny Net` and `deny LocalNetwork` both 1 as calibration, and the FABRICATION CONTROL holds — a LITERAL bonjour type still yields `hosts: None`, so no destination is invented. Suite 1133/1133, gates 11/11 — which also means the same question should be asked of every other effect recorded off that path. Original design note follows, unchanged and still correct: ~~DO NOT simply add these names — that is the R381 trap, one row up, and it would fabricate.** A bonjour `type:` is a SERVICE TYPE (`_http._tcp`) and a `SecItem` query is a CFDictionary; neither is a host or a path. Adding them to the establishing predicates while the literal picker scans the whole argument list would capture `"_http._tcp"` INTO `hosts` — exactly how R381's first cut captured `"443"` as a host and made the gate fail for an invented reason. **The right posture is ⟨0.29⟩'s bind/listen rule, which this file already records: WITHHOLD THE LITERAL and mark the surface incomplete.** `is_net_local_bind` exists because a listen address must never enter `hosts`; a service type and a Keychain query are the same kind of thing — a locator the DESTINATION surface cannot express. So: mark establishing (a missing locator fails closed) AND capture nothing into `hosts`/`paths`. The consequence is that every bonjour browse and every Keychain call becomes incomplete for `allow Net`/`allow Fs`, which is loud and correct — `allow Net api.stripe.com` should not be able to certify code that browses the local network. **`LocalNetwork` is already a separate, working channel** (`deny LocalNetwork` exits 1), so the disclosure exists; what is missing is that the `Net` surface claims completeness it does not have. Verify after the fix that a LITERAL bonjour type does not appear in `hosts`, which is the fabrication control this row's own trap demands. | **THIRD ROOT, FOUND BY THE RELEASE PANEL AND MEASURED 2026-09-11:** `NetService` was asserted `isOpaqueLocatorFree` in `ClassifierTests` and given `Net` by `kappaMember` for any non-pure verb, while appearing in NONE of the three sites this row's fix patched — they test `NWBrowser` and `NetServiceBrowser` only. **That is R348's shape (a predicate entry in a position nothing reaches) INSIDE the fix whose own commit message names R348 as the trap it avoided.** Measured with the gate calibrated: `svc.resolve(withTimeout: 5)` beside a benign `URLSession` literal left `incomplete` EMPTY and **`allow Net api.stripe.com` exited 0**. Fixed at all three sites. **AND THE ROW'S OTHER DEBT IS PAID:** the Net half was CLOSED on the strength of a hand-run that commit `cd630c3` described as a fixture and never added to the tree — the suite is PROVABLY blind to that branch (four inert attempts all passed 1133 tests). `BonjourSurfaceProcessTests` now pins all three roots plus the fabrication control in-tree. Two of three roots is what a κ family looks like when it is fixed from the spelling in hand rather than written down and run (R346).
| R386 rust: the Db masking list has NO `sqlite3_*` entry, so a benign sibling literal certifies ARBITRARY caller-supplied SQL — and R383 recorded this as NOT LIVE by measuring the wrong shape | 2026-09-11 | **CLOSED — candor-rust `31d4ac6`. Status corrected 2026-09-11: the fix shipped and this cell still read OPEN, caught by a release panel — found by a PLAN REVIEW attacking R383's own reasoning, not by a sweep. The row it corrects is mine, written four hours earlier in this same session** | **CARDINAL-SIN-CLASS GATE BYPASS, rust, LIVE.** `is_db_query_arg` (`candor-classify/src/lib.rs`) decides whether a `Db` call with no captured SQL literal marks the surface `incomplete` (`scan.rs`, the `eff == "Db"` arm). It contains **zero `sqlite3_*` entries**, while `classify` in the same crate maps `sqlite3_exec`/`sqlite3_step`/`sqlite3_prepare*` to `Db`. candor-swift covers the whole family by prefix (`hasPrefix(DB_FREE_PREFIX)`) — so this is ALSO a cross-engine divergence. MEASURED, instrument calibrated first: `conn.execute("INSERT INTO users (id) VALUES (1)", [])` beside `sqlite3_exec(db, caller_sql, …)` reports `inferred:['Db'] tables:['users'] incomplete:NONE`; **`deny Db` exits 1** and **`allow Db users` exits 0** over arbitrary caller-supplied SQL. **WHY R383 SAID OTHERWISE, AND IT IS THE DURABLE PART.** R383's fix column states the gap is *"REAL BUT NOT LIVE"* because *"a C-string literal is not captured into `tables` at all, so such a function has an EMPTY surface and `allow Db` already fails closed on emptiness"* — and that measurement was real, on an ISOLATED function using only the C API. **But the entire vein this session mined (R379, R381, R383, R385) is the BENIGN SIBLING LITERAL shape, and that is the shape R383 did not test.** The literal does not have to come from the masked call; it only has to come from the same FUNCTION. A high-level `rusqlite` call supplies it and the raw C API rides along. **A conclusion drawn from the one shape that happens to be safe, in a row whose own subject is that exact class, is the comfortable-conclusion failure — and it took an adversarial plan review to find it, not a sweep and not a corpus.** | Not fixed at filing. Add the `sqlite3_*` SQL-bearing family to `is_db_query_arg`, keeping the ALLOWLIST direction R379 measured and R383 re-confirmed — a denylist over Db verbs would mask every lifecycle and build-then-execute terminal in a whole-crate-Db crate. **Write the family down and run it (R346):** `sqlite3_exec`, `sqlite3_prepare`, `sqlite3_prepare_v2`, `sqlite3_prepare_v3`, `sqlite3_prepare16` and the `16_v2`/`16_v3` spellings all take SQL; `sqlite3_step`/`_close`/`_open*`/`_serialize`/`_deserialize` do NOT and must stay out — the over-mask control matters as much here as the fix, because `sqlite3_open_v2` takes a FILENAME and masking it would claim a Db surface for an Fs locator. **Then ask the same question of the OTHER engines' Db lists**, since swift already covers this family and java/ts have not been checked at all — that asymmetry is how this was found. |
| R387 swift: a SEARCH-PATH Fs locator is claimed as an effect and captured into no surface, so a benign path literal certifies `FileManager.url(…, create: true)` | 2026-09-11 | **CLOSED — candor-swift `5fdf481` ("R387/R467: close the search-path gate bypass"), 2026-09-17; recorded only in [[R467]]'s row until now. Originally filed: OPEN — the THIRD live instance of R385's class, found by the static census R385's failure prompted rather than by another sweep** | **GATE BYPASS, swift, LIVE.** The census enumerated all 44 `directEffects.insert` sites in `CallCollector.swift` and asked of each whether its branch reaches ANY surface machinery. Three were bonjour (R385, now closed). This is the fourth and it measures live: `CallCollector.swift:4124` inserts the `Fs` effect and the `searchPathClasses` privacy classes for `FileManager.default.url(for:in:appropriateFor:create:)`, and captures NOTHING into `paths`. Measured, calibrated first: a function doing `"x".write(toFile: "/tmp/benign.txt")` then `FileManager.default.url(for: .documentDirectory, in: .userDomainMask, appropriateFor: nil, create: true)` reports `inferred:['Fs'] paths:['/tmp/benign.txt'] incomplete:NONE`; **`deny Fs` exits 1** and **`allow Fs /tmp/benign.txt` exits 0** over a call that CREATES a directory. | Not fixed. **This is R385's posture, not R386's, and the difference is the point.** The locator here is not a runtime value — it is an ENUM naming a well-known directory — so it is DETERMINABLE. But it is not EXPRESSIBLE as a path literal a policy can name: `.documentDirectory` resolves inside the app container on iOS and inside the sandbox on macOS, which is exactly why `searchPathClasses` DELIBERATELY declines to map it (its own comment records that mapping it made every iOS app exit 1 demanding a macOS-only TCC key). A locator that is knowable but not nameable is still a locator the surface cannot express — ⟨0.29⟩'s bind/listen rule — so: mark `Fs` incomplete at that site and capture nothing. **The over-mask is real and must be priced before shipping:** every `FileManager.url(for:)` would then read incomplete for `allow Fs`, including `create: false` lookups that only stat. Decide whether the `create:` flag gates it — a lookup arguably has no destination to hide — and measure BOTH arms on the four Swift packages before choosing, because this one has real reach where bonjour and Keychain had none. **The census's other result is a VERIFIED NEGATIVE worth as much as the finding:** of 44 insert sites, the remaining 40 either reach a surface site on the same branch, or carry a non-surface effect (Camera/Mic/LocalNetwork/EventKit), or are locator-less property reads — which R385's own rule excludes, since not-having-a-locator and having-an-invisible-one are different. **So the class is FOUR sites, not a vein**, and the plan that called it a vein was inflating one finding into a pattern because the three before it were patterns. |
| R388 rust: the format-hole parser knew TWO of the NINE `std::fmt` traits, so six formatter impls were silently dropped | 2026-09-11 | **CLOSED — candor-rust `eee590d`. Status corrected 2026-09-11: the fix shipped and this cell still read OPEN, caught by a release panel — a hand-list-sweep claim I had NOT verified, reproduced before being believed. Its sibling claim from the same sweep did NOT reproduce, which is why it was treated as a hypothesis** | **CARDINAL SIN, rust.** `FmtHole` carried a `debug: bool`, and `collector.rs` turned it into `if hole.debug { ("Debug", "fmt") } else { ("Display", "fmt") }` — two traits. A type implementing only `LowerHex` and formatted `{:x}` was therefore checked against `Display`, found not to implement it, and DROPPED SILENTLY, because the resolve-or-skip path here never hedges to `Unknown`. **Measured PRE vs POST on one fixture, `cargo build` clean, eight types each implementing exactly one formatter whose `fmt` writes a file:** PRE — `{:x}`, `{:X}`, `{:o}`, `{:b}`, `{:e}`, `{:E}` **all ABSENT**, while the `{:?}` and `{}` controls charge `['Fs']`. POST — all eight charge. **Six of nine traits silently dropped**, and the two that worked are exactly the two the code knew about, which isolates the mapping as the cause. | **FIXED, AND THE A/B IS AN EMPTY DIFF WITH REACH MEASURED — which is the only version of that number worth reporting.** Registry A/B: ADDED 0 / REMOVED 0 / CHANGED 0, which `corpus-ab` refuses on its own (*"the single most flattering and least informative number available"*). So the changed branch was instrumented and swept: **842 hits across 246 of 1,545 crates — 16% of the registry** — dominated by `{:02x}` (138), `{:x}` (130), `{:X}` (108), `{:02X}` (81), `{:016x}` (68), `{:p}` (53), plus `#x`, `#p`, `b` and `o`. **So the branch is reached constantly and NOTHING MOVES:** types implementing `LowerHex`/`UpperHex`/`Octal`/`Binary`/the exponent traits in published crates essentially never perform effects in their `fmt` bodies. Reached-and-inert is a different claim from nothing-changed, and only the second is available without the sweep — R376 is this register's record of reporting the wrong one. The silence the fix closes is real regardless: the fixture goes from ABSENT to `['Fs']` on six of the eight formatter types. **FIXED — `FmtHole` names its trait, and the whole family is written out (R346) rather than the two spellings that were implemented:** `x`→`LowerHex`, `X`→`UpperHex`, `o`→`Octal`, `b`→`Binary`, `e`→`LowerExp`, `E`→`UpperExp`, `p`→`Pointer`, `?`→`Debug`, none→`Display`. **`?` IS TESTED FIRST AND WINS, which is its own trap:** `{:x?}` and `{:X?}` are `Debug` with hex-formatted integers, NOT `LowerHex`/`UpperHex` — reading the trailing type char alone would route them to the wrong trait, the same class of error as knowing only two traits, one layer down. Width and precision before the type char (`{:>8x}`) are handled by taking the LAST char of the trimmed spec. **A control that could not compile was discarded rather than reported:** the first attempt formatted a non-`Debug` type with `{:x?}`, which rustc rejects, so its two ABSENT readings were no evidence at all (§E3). The working control implements BOTH a pure `Debug` and an effectful `LowerHex` on one type; `{:x?}` charges in BOTH arms because method resolution is by name on a type with two `fmt` impls — a pre-existing imprecision this change neither introduces nor fixes, and worth stating so the control is not mistaken for a discriminator it cannot be. |
| R389 the per-repo GATE LIST for candor-spec certified nothing: `gate-run.sh` replayed CI's `working-directory:` as a `cd <repo> &&` prefix from INSIDE that repo, so every gate died on `cd: candor-spec: No such file or directory` | 2026-09-11 | tooling (`candor/bin/gate-run.sh`, `candor-spec/lean/check.sh`) | a FALSE FAIL in a release gate — unrunnable reported as failed, and a list that could never return anything but zero | **CLOSED — candor `7f4ec78`.** Found during the 0.36.1 cut, when candor-spec reported 0 ok / 4 FAILED while all three runnable gates pass when invoked the way they actually execute. CI checks this repo out at `$GITHUB_WORKSPACE/candor-spec` (conformance.yml `path: candor-spec`), so the steps' `working-directory:` names the repo ITSELF; `gates.sh` renders that faithfully, and `gate-run.sh` had already `cd`'d to the repo root. The strip removes the leading segment matching the repo's own basename and KEEPS any remainder, so `cd <repo>/sub && x` becomes `cd sub && x` while `cd lean && bash check.sh` — a real subdirectory — survives untouched, which is the control; dropping `working-directory:` wholesale is what `gates.sh:135` already warns against. **My first cut matched only the exact-root form and left the nested `working-directory: <repo>/<subdir>` shape still double-cd'ing; its own fixture caught it, which is the R346 rule (write the WHOLE family into one fixture, not the spelling in hand) paying out inside the fix that cites it.** The fixture was then CALIBRATED against a deliberately un-fixed tool, and that run condemned one of its own three assertions as vacuous: it grepped the captured output for `No such file or directory`, which appears there 0 times either way because the gate's stderr goes to a per-gate LOG FILE. It now asserts on the echoed command instead. Applied BEFORE the skip pre-pass, because with the prefix attached `_lead` was `cd` and the "interpreter is not installed here" arm never saw the real command word. **Blast radius measured, not assumed: 4 gates, all in candor-spec; the other six repos emit no `cd` prefix.** Second half: `lean/check.sh` exited **2** when elan is absent, so an unrunnable gate read as a FAILING one — the third spelling of the class the `${{ }}` and python/python3 arms already handle. Now exits 3 per the convention `gate-run.sh:202` states and candor-rust's strace gates adopted 2026-08-31, so the verdict is INCOMPLETE rather than either green or red. |
| R390 swift: `NetService`/`NetServiceBrowser` never charge `LocalNetwork`, and the R385 fix's own comment asserts they do | 2026-09-12 | swift | SILENT UNDER-REPORT — a privacy-manifest verdict | **AGENT-MEASURED with an executed repro and a `deny <E>` calibration arm; NOT yet coordinator-verified.** `bonjourDescriptorArg` (CallCollector.swift:1625/3896/4107) matches only a `.bonjour`/`.bonjourWithTXTRecord` argument. `NetService` and `NetServiceBrowser` are DEFINITIONALLY mDNS and never carry such a descriptor, so they charge Net but never `LocalNetwork` — while `c302758` added `NetService` to all three sites for the Net half and left this one, and the diff asserts *"`LocalNetwork` remains the positive channel and still fires"*. FALSE for the root that same commit introduced. Repro, one variable (the API spelling of one mDNS browse): `NWBrowser(for:.bonjour(type:t,domain:nil))` vs `NetServiceBrowser().searchForServices(ofType:t,inDomain:"local.")` — `deny LocalNetwork` exits 1 naming ONLY the NWBrowser arm. Consequence: `privacy-manifest --verify` tells a Bonjour app it needs no `NSLocalNetworkUsageDescription`. Same for `NetService(...).publish()`, `NWListener(service:)`, `NWConnection(to:.service(...))`. **CLOSED 2026-09-15, candor-swift `3eca7f5`, and the FALSE VERDICT was reproduced before the fix.** One variable — the API spelling of one mDNS browse: `NWBrowser(for:.bonjour(...))` charged `LocalNetwork` and named the key; `NetServiceBrowser().searchForServices(...)` charged `Net` ONLY, `deny LocalNetwork` exited **0**, and `privacy-manifest` printed *"no privacy-sensor reach found; no usage-description keys required"* — with `--verify` against an EMPTY `Info.plist` returning `ok:true`, exit 0. That is the tool answering a compliance question wrongly, not merely omitting a row. Fixed with ONE authority (`isMdnsOnlyRoot`) consulted by all three charge sites. **The declared exclusion's reason was true of the wrong thing**: *"not separable by type (NWBrowser/NWConnection also serve ordinary networking)"* holds for the Network framework and is FALSE of `NetService`/`NetServiceBrowser`, which ARE the Bonjour API. **Swept past the trigger** per the audit-boundary rule: `NWConnection(to:.service(name:type:))` and `NWListener(service:)` were silent too and are closed by the `name:`+`type:` SIGNATURE rather than by three more names. **REACH 0 across 21 real Swift packages** (census calibrated first — a first attempt returned 0 for tokens known to be present), so the claim is bounded to the fixture and said so. See [[R445]]: the stale `why` string is what made this read as CONSIDERED. |
| R391 swift: the three bonjour entries in `isOpaqueLocatorFree` are UNREACHABLE — R348's shape, inside the fix that cites R348 | 2026-09-12 | swift | a list entry in a position nothing can reach, reading as coverage | **PROVEN BY EXECUTION, not by reading.** Classifier.swift:1409. The three bonjour branches (CallCollector.swift:1624/3885/4102) hardcode `incompleteSurfaces.insert("Net")` and RETURN before any `isEstablishingFree`/`isOpaqueLocatorFree` call, so the predicate is consulted at ZERO sites for those names. The agent renamed them `__DEAD_*`, rebuilt, and every bonjour fixture was byte-identical with identical gate verdicts; `BonjourSurfaceProcessTests` passed 3/3; the ONLY failures in the 1136-test suite were the three list-membership assertions in ClassifierTests.swift:296 — i.e. the tests pin the LIST, not the behaviour. The `SecItem*`/Fs half of the same predicate IS live. **R348's shape occurring inside the R385 fix — the second time this session a fix reproduced the class it names.** PREMISE WENT STALE BETWEEN FILING AND WORKING, and the safe direction was NEITHER of the two the row offered. The claim that the three bonjour entries are consulted at zero sites was true on 2026-09-12 and is FALSE at HEAD: commit 3eca7f5 (R415/R432, 2026-09-15 — three days AFTER this row was filed) gave locatorForFree an .opaque arm deriving from the predicate, and an in-process probe counts 1-2 hits per bonjour fixture. They ARE inert, proven by renaming all three to __DEAD_ and getting byte-identical JSON across nine fixtures, with the same rename on NWListener flipping incomplete:[Net] to hosts:[8080] as the calibration. DELETE WAS REFUSED as unsafe: it re-arms firstStringLiteral for those names, and the only reason nothing publishes _http._tcp today is a routing detail one reorder away from R381's 443 and R432's 8080, both live defects in the same file. GATING THE INSERTS ON THE PREDICATE WAS ALSO REFUSED: it converts an unconditional fail-closed insert into one gated by an INCLUSION LIST, where a forgotten name under-reports. Landed instead: isBonjourRoot is now the single authority consulted by the predicate AND all three charge-site guards, which were three more copies of the same list — the R346/R347 shape. The entries are now load-bearing: removing NetService reddens 10 of 1,233 tests, where before they were pinned only by list-membership assertions, which was this row's actual complaint. CLOSED candor-swift 162ca7c. |
| R392 swift: a comment claims the module-qualified bonjour site 'carries its own fixture'; no such fixture exists | 2026-09-12 | swift | FALSE COVERAGE CLAIM — not a defect, but a regression there is invisible | CallCollector.swift:1628-1631 reads *"THIS SITE IS NOT REACHED BY THE MEASURED FIXTURES … so it carries its own fixture rather than being left as an untested guess."* It does not: no test in `Tests/` uses a module-qualified bonjour spelling, and `ModuleQualifierSpellingProcessTests`' pair list has no bonjour row. The agent deleted only that line, rebuilt, ran the full suite: **1136 tests, 0 failures.** The site is LIVE and behaves correctly at HEAD, so this is a false coverage claim rather than a defect — the [[feedback-documented-limitation-is-not-measured]] class in its SAFETY-ASSERTING variant. CONFIRMED AND CLOSED (candor-swift 162ca7c) — and the sibling this row did not name was equally unprotected. Guard-deletion: removing the module-qualified site's incompleteSurfaces.insert(Net) — the exact line whose comment claimed it carries its own fixture — leaves 1,229 tests and ZERO failures; removing the bare-ctor copy also zero; only the MEMBER copy is protected. THE REASON ALL THREE READ AS COVERED IS A MASKING EFFECT WORTH GENERALISING: every bonjour fixture in the tree constructs the browser AND calls a verb on it in the same function, so the member site's insert masks both ctor sites and the row reads identically whether they are present or absent. BOTH CTOR SITES ARE LIVE, not cosmetic — the discriminator is a factory that constructs and returns while the verb is called elsewhere, and with either insert deleted a Bonjour registration with a caller-supplied service name gives hosts:[api.stripe.com], no incomplete, and allow Net api.stripe.com at EXIT 0. That is R385's masking evasion reopened through the one spelling nothing measured. Four ctor-only fixtures added, each calibrated by deleting the site it pins. |
| R393 swift: `fopen(path, mode)` — the mode literal silences the fail-closed marker. R381's fix was scoped to its own trigger | 2026-09-12 | swift | **CLOSED — candor-swift `3c27f86`, INCIDENTALLY: R395's shape test routes `fopen`'s mode string `"r"` to the incomplete branch, so it no longer stands in for a captured locator. Pinned by its own case because an incidental closure reopens quietly. RESIDUAL: the picker is still positional-by-accident for non-resolver free calls — safe only while no Fs establishing call has a PATH-SHAPED non-locator argument.** GATE BYPASS, was shipping | **AGENT-MEASURED with an executed repro and a `deny <E>` calibration arm; NOT yet coordinator-verified.** CallCollector.swift:3759-3762. The positional locator picker is gated on `isNetResolverFree` ONLY; every other establishing free call still uses `firstStringLiteral`, which scans the whole argument list. `fopen` takes the locator FIRST and a string MODE second. A/B, one variable (whether arg 1 is a plain literal), same file/binary/policy/runtime path/benign sibling: `fopen(p,"r")` → `incomplete` ABSENT, `allow Fs /tmp/benign.txt` **EXIT 0**; `fopen(p,mode)` → `incomplete:["Fs"]`, AS-EFF-008, exit 1. `deny Fs` exits 1 on both (calibration). The `"r"` is then discarded by the slash filter (R395), so the fabrication never appears and the row looks clean. **R381's LOCATOR-POSITION trap one spelling over — the audit was drawn around its own trigger.** |
| R394 swift: `shellOut(to:at:)` reports the WORKING DIRECTORY as the command | 2026-09-12 | swift | **CLOSED — candor-swift `7b38dbe`.** FABRICATION + **GATE BYPASS** | **AGENT-MEASURED with an executed repro and a `deny <E>` calibration arm; NOT yet coordinator-verified.** Same mechanism as R393 on the Exec table. `shellOut(to: runtimeCmd, at: "/tmp/work")` → `cmds: ["/tmp/work"]`, no `incomplete`, `allow Exec /tmp/work` **exit 0**. ShellOut's signature is `shellOut(to:arguments:at:)`; `at:` is not the locator. Net is clean here only BY LUCK — `NWEndpoint.Port("443")` and `.service(name:type:)` nest their literals inside a call and `firstStringLiteral` scans only top-level arguments. That is not a property anyone designed. **FIXED WITH A DECLARED TABLE, not another special case: `locatorLabelsForFree(name)` states which argument holds the locator and the single free-call site consults it. R381 found this identical bug on `getaddrinfo` and fixed it FOR THE RESOLVER FAMILY ONLY — the audit drawn around its own trigger — which is why `shellOut` still had it. The table also ~~RETIRES the residual stated on R393~~ — **FALSE, see R415**: the table has three entries and the call site still falls back to the whole-argument scan, so every establishing free call outside it stays positional-by-accident (`fopen` was safe only because `"r"` fails a downstream path-shape test, i.e. by luck). Measured either side of the edit, one variable: `cmds:["/tmp/work"], incomplete:none` → `cmds:none, incomplete:["Exec"]`. CONTROL: a literal command still captures its head `ls` and is not incomplete, so this is not blanket over-masking. A name absent from the table behaves exactly as before, bounding the blast radius by construction. `LocatorPositionProcessTests`; 1142 tests, 0 failures. |
| R395 swift: a BARE RELATIVE FILENAME is seen, discarded, and the surface is then declared complete | 2026-09-12 | swift | **CLOSED — candor-swift `3c27f86`, calibrated (all 3 cases fail with the fix removed and the engine REBUILT).** GATE BYPASS — PRE-EXISTING AND PUBLISHED (no later than `6f9ef15`, 2026-07-09) | **AGENT-MEASURED with an executed repro and a `deny <E>` calibration arm; NOT yet coordinator-verified.** `recordSurfaces` case "Fs" (CallCollector.swift:2050) and its twin `recordTwoPathFs` (:1993) insert a literal into `paths` only when it contains a slash or begins with a dot or tilde. A bare filename is DROPPED — and because `literalForLabel` returned non-nil, `anyMissing` stays FALSE, so `incompleteSurfaces.insert("Fs")` never fires. **The engine sees the destination, throws it away, then claims the surface is complete.** COORDINATOR-VERIFIED IN SOURCE (both sites read; the `anyMissing` path confirmed). All of these exit 0 under `allow Fs /tmp/benign.txt` beside a benign sibling write, and fail closed in isolation: `String(contentsOfFile:"credentials.json")`, `FileHandle(forReadingAtPath:"id_rsa")`, `write(toFile:"exfil.txt")`, `copyItem(atPath:"id_rsa",toPath:"stolen.key")` (BOTH locators dropped, `anyMissing` false). Outside the reviewed diff; found while sweeping for R393. |
| R396 rust: R370 reintroduced its own class on the BODY-LOCAL route — block-disjoint shadowing read as an arm set | 2026-09-12 | rust | FABRICATION (+ fabricated `incomplete` noise); latent, zero corpus reach | **AGENT-MEASURED with an executed repro and a `deny <E>` calibration arm; NOT yet coordinator-verified.** collector.rs:143 (`local_use_seen`) + :2267-2269; decls.rs:20/:42. R370's rule is *"did THIS ITEM LIST bind the name twice"*. At module level `seen` is correctly per-`collect_item_uses`; for body-local `use` items it is per FUNCTION BODY, not per BLOCK. Two `use` items binding one name in DISJOINT BLOCKS of one body is ordinary legal Rust shadowing, not an arm set, so the union pushes an edge the code cannot reach — **verbatim the sentence R370's own doc-block uses to describe what it closed.** Compiling repro: an `if`/`else` whose two blocks each `use` a different `go`, with the call in the `else` — v0.36.0 ABSENT, HEAD `inferred:['Fs']`. The `match`-arm and closure spellings behave the same; `shadow_sibling_blocks` additionally gains a fabricated `incomplete:['Fs']`. **No test covers it; the suite is green with it.** |
| R397 rust: R380's chain walk resolves THROUGH a type-changing method absent from the 21-name hand list | 2026-09-12 | rust | FABRICATION; latent, zero corpus reach | **AGENT-MEASURED with an executed repro and a `deny <E>` calibration arm; NOT yet coordinator-verified.** collector.rs:359 (`is_recv_type_changing`) and :1360-1377. `self.inner.as_ref().unwrap().describe().go()` — `describe` is not on the list, so the walk returns to the `dyn` field and dispatches `go` through CHA to `Bad::go`, while the real receiver is `Plain`, whose `go` is pure. v0.36.0 ABSENT → HEAD `['Fs']`. **[[candor-handlist-vein]]: a 21-name allowlist standing in for "every method that changes its receiver's type" — `map`, `filter`, `and_then`, `ok`, `expect`, `first`, `parse`, `collect` are all absent from it.** The fix DOES pass its own revert test, which is why the suite did not catch this. |
| R398 rust: R380's `Option` peel makes every `Option` INHERENT method a CHA dispatch site against the payload trait | 2026-09-12 | rust | FABRICATION; latent, zero corpus reach | **AGENT-MEASURED with an executed repro and a `deny <E>` calibration arm; NOT yet coordinator-verified.** lang.rs:301. With `Option<Box<dyn Doer>>` and a trait carrying `take`/`insert`, `self.b_opt.take()` and `self.b_opt.insert(...)` are ABSENT at v0.36.0 and `['Fs']` at HEAD. `Option`'s inherent surface (`take insert replace map filter iter zip flatten contains cloned and or`) is the richest of any wrapper on that list, so it collides most with real trait method names. **PRE-EXISTING for `Cell` (`get`/`set`/`take`) and `RwLock` (`read`/`write`) — R380 WIDENED this class, it did not create it.** |
| R399 rust: the sentence R379 WITHDREW as false survives verbatim on both sibling guards — and both are live gate bypasses | 2026-09-12 | rust | **PARTIALLY CLOSED — candor-rust `c27d249`.** GATE BYPASS ×2, live — and a comment that licensed not fixing them | **AGENT-MEASURED with an executed repro and a `deny <E>` calibration arm; NOT yet coordinator-verified.** R379 withdrew *"under-catching here is a missed mask (sound-with-disclosure), never a broken gate"* and proved it false. The identical sentence still ships on `is_fs_path_arg` (candor-classify/src/lib.rs:3464) and `is_db_query_arg` (:3672) — and R383 and R386, IN THAT SAME DIFF, are the counter-examples to both. Repros compile against the real crates: `allow Db users` with mysql `query_drop(caller_sql)` beside a literal query → **EXIT 0** (control, verb changed to `query(caller_sql)`: exit 1); `allow Fs /etc/hostname` with cap-std `Dir::write(caller)` or `unixfs::chown(caller,..)` → **EXIT 0** (control, `OpenOptions::open(caller)`: exit 1). Missed spellings, each checked against the crate's own source: mysql `query_drop`/`query_iter`/`query_first`/`query_fold` (`queryable.rs:29,50,126`, `Q: AsRef<str>`), postgres `copy_in`/`copy_out` (`client.rs:492,520`), `chown`/`lchown`/`chroot`, cap-std `Dir::open/write/read/remove_file/create_dir`. **`mysql`'s `query_drop` is that crate's PRIMARY API — not latent the way `sqlite3_get_table` was — and cap-std is the crate candor advertises capability conformance against.** R383's "only two path-taking method pairs in the registry" claim was re-measured over all 1,547 crates via the diff's own `CANDOR_MASK_DEBUG` probe (9,811 UNMASKED lines) and is TRUE OF THAT CORPUS; its BOUNDARY is the registry, which holds cap-std's source and almost no cap-std consumer. **CLOSED HALF, coordinator-measured end to end:** both false sentences STRUCK at the source (preserved struck-through, not deleted); nine verbs added — `chown`/`lchown`/`chroot`, mysql `query_drop`/`query_iter`/`query_first`/`query_fold`, postgres `copy_in`/`copy_out`. `allow Fs /etc/hostname` went exit **0 → 1** over `unixfs::chown(caller,…)` beside a benign literal, with both controls holding (an unmasked runtime chown still fails closed; a fully literal path still certifies). **PRICED: A/B over 1,552 registry crates, 286,892 rows per arm — ADDED 0 / REMOVED 0 / CHANGED 5.** All five GAINED `incomplete` and lost nothing, every one a library wrapper whose own parameter is the locator (`fs_err` chown/lchown/chroot, `postgres` copy_in/copy_out) — 0.0017%, entirely fail-closed. Test CALIBRATED: with the three verbs removed it fails with its own message. **STILL OPEN, and the boundaries are stated rather than implied: (1) mysql's `query_*` produced NO changed rows — that crate is not exercised in this registry snapshot — so that quarter rests on the review agent's fixture and the crate's signatures, NOT on this corpus; (2) cap-std's `Dir::open`/`write`/`read` are DELIBERATELY not added, because the method guard matches on path SUFFIX and `ends_with("Dir::write")` would fire on any type named `Dir` — a far wider blast radius than three POSIX verbs, needing its own measurement.** |
| R400 rust: `super::super::X` strips N levels and resolves against ONE — HEAD is worse than a miss, it is confidently wrong | 2026-09-12 | rust | **FABRICATION HALF CLOSED — candor-rust `6b7acba`. UNDER-REPORT HALF STILL OPEN, deliberately.** | **AND THE VERIFICATION FOUND A SECOND DIRECTION THE ROW DID NOT NAME — a FABRICATION.** With the bindings reversed (grandparent `Vec`, parent `Command`), `a::b::c::definitely_pure()` — a body that constructs a `Vec` and nothing else — reports **`['Exec']`**. An effect charged to a function that cannot have it, which is the direction this engine is least able to be trusted wrong in. FIXED by stripping exactly ONE `super::` and REFUSING depth ≥ 2. **The grandparent's binding is not recoverable at this site and that is why it refuses rather than reaching further:** `submodule_uses` builds each inline module's map FROM its parent's, so by the time `c` is walked the grandparent's binding has been SHADOWED by the parent's — `out` holds one scope, not a stack — and resolving depth ≥ 2 needs the module path `collect_use` deliberately does not track, the same context whose absence made a blind re-resolve drop 19 rows from `hyper`. **So the UNDER-REPORT half is NOT closed: depth ≥ 2 keeps its literal and still reads silent-pure.** Stated rather than quietly folded into a CLOSED. PRICED over 400 registry crates: ADDED 0 / REMOVED 0 / CHANGED 0, **with REACH PROVEN** — `corpus-ab.py` REFUSED the first run outright (*"an empty diff with no reach measurement … is indistinguishable from having measured nothing"*), so the branch was instrumented and measured **74 hits across 7 real crates** (diesel, bindgen, crossterm), all resolving identically after the change. **COORDINATOR-VERIFIED 2026-09-14, with a working CALIBRATION arm — the row's claim holds and the gate consequence is worse than the row states.** Compiling repro, three nested modules: `a` binds `Runner = std::process::Command`, `a::b` binds `Runner = std::vec::Vec`, and `a::b::c` writes `use super::super::Runner` (= `a::Runner`, a real spawn). Measured on HEAD: **`a::b::c::go` is ABSENT FROM `functions[]` ENTIRELY** while the ONE-level calibration arm `a::e::go_one_level` — same shape, same target, one `super::` — reports `['Exec']`, and a `direct_spawn` control also reports `['Exec']`. So the instrument can see this shape and this call is the thing it cannot see. **THE GATE:** with the prefix-collision check done first (no other name shares the prefix), **`deny Exec a::b::c::go` exits 0** over a real process spawn. A bare `deny Exec` exits 1 — but only because the `direct_spawn` control catches it, which is precisely why this survived: the whole-scan form hides it, the same structural reason R429 needed a scoped rule. **AGENT-MEASURED with an executed repro and a `deny <E>` calibration arm; NOT yet coordinator-verified.** lang.rs:2458: the stripper is a `while let` on `strip_prefix("super::")` — N levels off, one level of scope back. Compiling repro: three nested modules where the innermost does `use super::super::Runner` and the two outer ones bind that name to `std::process::Command` and `std::vec::Vec` respectively. `a::b::c::go` SPAWNS A PROCESS and is ABSENT at HEAD and at v0.36.0. **Worse at HEAD than a plain miss: R378's `expand` now confidently returns `b`'s `std::vec::Vec`.** Pre-existing, not a regression. |
| R401 rust: R380's wrapper peel misses `Pin`, `HashMap` lookup, `Weak` and `ManuallyDrop` — one spelling over, per R346 | 2026-09-12 | rust | **HALF CLOSED — candor-rust `efb8edb` (`Pin`, `ManuallyDrop`). `Weak` and a `HashMap` VALUE remain OPEN.** | **THE SPLIT IS NOT ARBITRARY — it is the criterion this file already states.** `elem_trait_leaves`' own comment says a wrapper belongs in the DIRECT-dispatch peel only when the receiver genuinely IS the inner type, because peeling one that is not fabricates a receiver (which is why `OnceLock` is excluded there). `Pin` and `ManuallyDrop` qualify by Deref and nothing weaker — `impl<P: Deref> Deref for Pin<P>`, `impl<T> Deref for ManuallyDrop<T>` — so `p.go()` COMPILES. `Weak` and a map VALUE do not Deref; their payload is reached through a named accessor (`upgrade()`, `get()`), which is the ELEMENT route. **I WROTE THE ELEMENT HALF AND REVERTED IT, MEASURED INERT:** adding `get`/`upgrade` to `is_receiver_only_adapter` and `Weak` to the element containers moved `p_map`/`p_weak` NOT AT ALL. Shipping it would have been a change that cannot be shown to fire, claimed as a fix — the *CHANGED 0 is not evidence* trap, in the direction where it is least visible. **THE LEAD FOR WHOEVER FINISHES IT, because it is the non-obvious part:** a MINIMAL crate whose only function is `f[0].go()` over `&Vec<Box<dyn Doer>>` does NOT charge, while the identical function in a larger crate DOES — so a crate-wide guard gates the element machinery (`has_dyn_return` is one such flag, whose own comment says it *"keeps the `resolve_recv_traits` hot-path guard closed on the overwhelming majority of crates"*). **Any fixture for the element route must be large enough to open that guard, or it measures the guard and not the fix.** PRICED over 400 registry crates: **ADDED 3, REMOVED 0, CHANGED 25**, every one in the DISCLOSURE direction — 21 are `async-compression`'s Decoder/Encoder poll bodies going `[] → ['Unknown']` with `unresolved:true` and `callback:unresolved call`: functions previously reported PURE that reach a `Pin<Box<dyn _>>` the engine can now SEE and honestly cannot resolve. 7 crates of 400 touched. **CORRECTED 2026-09-14: the verification below said the FIELD route was broken; that was MY MATCHER, not the engine (see R430, withdrawn). The four wrappers fail on BOTH routes, which is what the row always claimed.** Re-measured by printing every row: `p_box`/`p_arc`/`p_vec` and `FBox::run`/`FBoxC::run` all charge, while `p_pin`, `p_map`, `p_weak`, `p_md` and `FPin::run` carry nothing. **COORDINATOR-VERIFIED 2026-09-14 — on the PARAMETER route, and the first fixture's calibration FAILED, which is what found the rest.** The first repro used struct FIELDS and every arm came back ABSENT *including the `Box` calibration*, so it proved nothing about the wrapper list; a fixture whose control fails is not evidence in either direction. Rebuilt on the parameter route, where the calibration holds: `p_box`, `p_arc` and `p_vec` (`Vec<Box<dyn>>` by index) all report `['Exec']`, while **`p_pin` (`Pin<Box<dyn Doer>>`), `p_map` (`HashMap<_, Box<dyn>>` via `.get(k).unwrap()`), `p_weak` (`Weak<dyn Doer>`) and `p_md` (`ManuallyDrop<Box<dyn>>`) are ABSENT** — exactly the four this row names, with `direct_spawn` as the all-clear control. The `Vec`-indexes-but-`HashMap`-does-not split is confirmed as stated (§F1-6's *one shape of a container*). **AGENT-MEASURED with an executed repro and a `deny <E>` calibration arm; NOT yet coordinator-verified.** lang.rs:301. All compile, all ABSENT at HEAD while `Box`/`Arc`/`Rc`/`Option`/`RefCell`/`Mutex`/`RwLock`/`Vec[i]`/`[T;N]` charge: `Pin<Box<dyn Doer>>` (the dominant async spelling); `HashMap<String,Box<dyn Doer>>` via `.get(k).unwrap().go()` — **`Vec<Box<dyn>>` INDEXING works and map lookup does not, which is §F1-6's "one shape of a container"**; `Weak<dyn Doer>`; `ManuallyDrop<Box<dyn Doer>>`. `Result<Box<dyn Doer>,E>` is deliberately excluded and its comment says so honestly. **R346's rule: the fix's own fixture was the boundary of the next defect.** **`Weak` HALF CLOSED 2026-09-15, candor-rust `76ac6b0`**, together with [[R446]] — and the row's *"a `HashMap` VALUE"* framing is what had hidden the real class (the axis is the BINDING SITE, not the container). The CONCRETE map value remains open with its reason recorded on R446. |
| R402 instrument: `ci-watch.sh:761`'s HEAD-scoped `gh run list` has NO `--limit`, and the remedy was applied to its sibling in the same change | 2026-09-12 | instrument (`candor/bin/ci-watch.sh`) | BOTH a false RED and a false GREEN are reachable; latent today | **COORDINATOR-VERIFIED 2026-09-14.** Every `gh run list` in the file read by hand: the three LIVE call sites carry `--limit 1` (`:369`, the earlier-commit safety net), `--limit 12` (`:725`, the median) and `--limit 100` (`:879`, the HEAD-scoped page). The fourth (`:437`) sits inside `if [ "$SELFTEST" -eq 1 ]` with a STUBBED `gh()` that returns 7 without touching the network, so a page size cannot apply to it. The agent's mutation measurement stands. **CLOSED — candor `73e5181`. All three live call sites in this file are now scoped (`--limit 1`, `--limit 12`, `--limit 100`); the fourth is a selftest stub. AGENT-MEASURED by MUTATION (a 474/474 baseline in a scratch copy, one mutated file per run); not yet coordinator-verified.** `gh run list --commit "$sha" --json …` with no `--limit` defaults to **20**, and that single page is shared by every workflow in the repo. **`BACKLOG.md:95-137` already writes this fix and splits it two ways — per-workflow `--limit 1` where no commit is known, and `--commit <sha> --limit 100` where one IS known. The second half was applied to `release-preflight.sh:824` and `:861` and NOT to `ci-watch.sh:761`, in the same change.** The file's other two call sites are both scoped (`:354` `--limit 1`, `:710` `--limit 12`); this is the only unscoped one left. A required workflow squeezed off the page gives `REQUIRED BUT ABSENT` over a workflow that RAN — the false absent R382's own comment calls indistinguishable from a real one. A non-required one squeezed off gives **no row at all**, and the earlier-commit safety net cannot rescue it because `:987` does `[ "$lsha" = "$sha" ] && continue` — "already judged above", when it was never judged. HEADROOM MEASURED, which is why this is latent: rows at HEAD are candor 3, candor-rust 3, candor-spec 2; the busiest sha in the umbrella's last 100 runs carries 6, against 20. |
| R403 instrument: `ci-watch.sh` has NO automated coverage of any kind — R382 and R384 both landed in it with no gate beside them | 2026-09-12 | instrument (`candor/bin/ci-watch.sh`) | **CLOSED — candor `75f633f (the `6472ac7` this cell cited until 2026-09-15 does not exist in the umbrella — a hash written from memory, in the register the whole plan is read from)`.** The comparison arm is covered and MUTATION-PROVEN. | **CLOSED 2026-09-14.** `report_earlier_reds` became callable when R428 extracted it, so the decision table is now drivable with no network. Six cases, each differing from its neighbour in ONE field: earlier-commit failure → rc 1; success → quiet; failure AT head → quiet (already judged); in_progress → quiet; cancelled WITH a cancel-in-progress group → pending; cancelled WITHOUT one → rc 1. **MUTATION-PROVEN:** deleting the `rc=1` that reddens an earlier-commit failure turns the arm red and the suite FAILED; restoring it returns OK. **THREE HARNESS BUGS ON THE WAY, each the shape this file exists to catch:** (1) the definition sat BELOW `--selftest`, so three cases printed ✔ while bash said `command not found` — vacuous, now guarded by `command -v`; (2) `out="$(report_earlier_reds)"` runs in a SUBSHELL, so the counters it set died with it and every case read rc 0 on a working function; (3) the stubs were defined in the selftest's own shell and `unset -f`d afterwards, DELETING the real `label_for`/`path_for_id`/`workflow_cancels_in_progress`/`fetch_earlier_commit_rows` so five later arms tested functions that no longer existed — a harness that damages what it tests. Each case now runs in a subshell and returns its counters through a file. **PARTIALLY CLOSED — candor `73e5181`: `--selftest` is now a ONE-LINE step in `release-scripts.yml` (one line deliberately, so `gates.sh` lists it runnable rather than as a block line — R406's lesson), verified to run as an umbrella gate line and to pass rc 0 with no network. **STILL OPEN, and stated rather than implied: the COMPARISON arm remains uncovered** — the selftest's own comment concedes it "classifies triggers and never touches the comparison", and driving that needs a `gh` stub answering the run-list surface from inside the repo loop. AGENT-MEASURED by MUTATION (a 474/474 baseline in a scratch copy, one mutated file per run); not yet coordinator-verified.** `grep -rn "ci-watch"` over the umbrella returns only comments outside the file itself. **`release-test.sh` has no ci-watch section** — its 47 `say` headers cover release-stage, release.sh, spec-bump, preflight, release-verify, changelog-lag, verify-local, verify-umbrella, probe, `_ci_verdict.py`, gates/gate-run, disk-guard and assert-audit, and not this. **`bash bin/ci-watch.sh --selftest` is run by nothing** — no workflow, no `verify-local.sh`, no `gates.sh candor` line. **`CI_WATCH_FAULT` appears only inside `ci-watch.sh`**; all four hooks are manual-only. The selftest passes and covers the stall arm, `gh_call`, `wf_path_map`, `resolve_required` and `resolve_sha`, but its own comment at `:790` concedes it "classifies triggers and never touches the comparison". |
| R404 instrument: the `drop-row` fault hook's stated postcondition is FALSE on a single-workflow repo — one hook over from the comment warning about exactly this | 2026-09-12 | instrument (`candor/bin/ci-watch.sh`) | a fault hook that looks installed and proves nothing | **CLOSED — candor `73e5181`. The hook now REFUSES loudly below two rows instead of exercising a different arm under this arm's name. Coordinator-verified BOTH directions on live repos: `candor-agents` prints `fault drop-row CANNOT be exercised` and the verdict stays OK; `candor-rust` prints `REQUIRED BUT ABSENT — realworld-oracle.yml runs on every push` and goes NOT GREEN. AGENT-MEASURED by MUTATION (a 474/474 baseline in a scratch copy, one mutated file per run); not yet coordinator-verified.** `:793` promises *"a REQUIRED BUT ABSENT line must follow"*. On `candor-agents` (one workflow) dropping the first row empties `rows`, so control falls into the `[ -z "$rows" ]` branch at `:827` and prints `NO RUN AT HEAD, and these were required:` — a different arm, different text, and the promised line never appears. Nothing asserts the postcondition. **This is precisely the failure the ADJACENT `drop-map-entry` comment says it was rewritten to avoid ("the hook looked installed and proved nothing").** Corrects the brief's premise: `drop-map-entry` does NOT exercise the REQUIRED BUT ABSENT arm — post-R382 it exercises the SKIP of that arm (`unresolved_ids=1` at `:910` gates it off at `:1041`). The arm is reachable only via `drop-row` on a repo with at least two rows, measured: baseline 3 ✔ rows rc=0; with the fault, `✘ REQUIRED BUT ABSENT — ci.yml runs on every push`, rc=1. |
| R405 instrument: `release-test.sh` is INTERMITTENTLY RED on `main` (§15b, ~1 run in 3), and its diagnostic prints nothing on the only path it fires | 2026-09-12 | instrument (`candor/bin/release-test.sh`) | a release gate that is intermittently red, and unreadable when it is | **COORDINATOR-VERIFIED 2026-09-14, under the exact condition rather than by re-reading the fix.** Two `conformance/run.sh` processes were in flight (from a `gate-run.sh candor-spec` launched for R406) — precisely the machine-wide state `probe.sh`'s `quiet_tree_check` refuses on. `release-test.sh` reported **`SKIPPED: section 15b — a build or conformance run from another agent is still in flight after 10s, which makes probe.sh's machine-wide guard refuse`** and finished `OK — 448 assertions (3 SKIPPED — NOT a pass)`, rc 0. So the intermittent RED is now a DISCLOSED SKIP carrying its reason, which is the whole of what this row asked for: a refusal and a rendering failure are no longer indistinguishable. **A concern I had while verifying turned out to be unfounded and is recorded so it is not re-raised:** the CONTROL arm above the GNU one calls `bad` with no `probe_refused` check, which looks like the same hole one arm over — but the guard is at SECTION level, so 15b skips before that arm runs. Also note 448 vs the 474 of an idle machine: an assertion COUNT is only comparable on a quiet box. **PARTIALLY CLOSED — candor `d0d1dca`; the CAUSE is now measured rather than plausible.** AGENT-MEASURED by MUTATION (a 474/474 baseline in a scratch copy, one mutated file per run); not yet coordinator-verified.** First full run of the real tree: `release-test: 1 FAILED, 473 passed`, rc=1, at §15b `probe.sh` — *"provenance timestamps went blank under a GNU-shaped date:"* with an EMPTY tail. Re-run of the identical tree: 474/474. Scratch copy: 474/474. The fixture in isolation: 0 failures in 40 iterations — it needs suite context and the cause was NOT isolated. **What is certain: `:4124` ends by piping `$gnu` into `grep built`, so the only way it fires with an empty tail is that `$gnu` holds NO provenance block at all — `probe.sh` died in `quiet_tree_check` (`probe.sh:100-107`, a machine-wide `pgrep`) rather than rendering blanks. A refusal and a rendering failure are indistinguishable to the reader.** Its siblings in §13b all dump the whole capture. §13b itself backgrounds processes named to match that guard (`:3606` conformance/run.sh, `:3620` cargo build) — a plausible mechanism, both `kill`+`wait`ed. `release-scripts.yml` runs this file on every push to `main`. **COORDINATOR NOTE: this retires my own 2026-09-11 attribution of a DIFFERENT release-test failure to concurrent gate runs. A clean serial re-run is consistent with concurrency AND with flakiness, and I recorded it as confirmation — the question was closed too early.** **CAUSE CONFIRMED 2026-09-12 by forcing it:** `probe.sh`'s `quiet_tree_check` greps the WHOLE MACHINE for `conformance/run.sh`, `swift build`, `gradlew`, `cargo build`, `cargo test` and DIES if any is running — anywhere, started by anyone. §15b then read that refusal as a result. **AND THE PREMISE WAS CORRECTED BY THE MEASUREMENT: §13, §13b and §15b ALREADY wait 10s and skip themselves when the machine is busy** — with a build-shaped process alive, all three skipped with reasons and the suite reported `448 assertions (3 SKIPPED — NOT a pass)`. So the common case was always handled, and **the observed ~1-in-3 red is the RACE after that check** (quiet at check time, a build starts during the section), which is exactly why it survived 40 isolated iterations. Fixed: the diagnostic dumps the whole capture, and a refusal is recorded as a SKIP with its reason quoted rather than a red row — R389's principle, one instrument over. **VERIFICATION BOUNDARY, stated not implied: the `probe_refused` predicate is verified in ISOLATION (fires on both refusal texts, not on a real provenance line); the INTEGRATED path has NOT been observed firing, because forcing that race reliably is the thing nobody could do.** RESIDUAL: 15 call sites invoke `probe.sh` in this suite and several expect success, so they share the exposure; only the one observed red is fixed, and claiming the class closed from one instance is the audit-boundary error this register keeps finding. **READING RULE: a run on a busy machine silently yields FEWER assertions (448 vs 474) — counts compare only when the machine is quiet.** |
| R406 instrument: `gates.sh` cannot run `ci/self-gate.sh` (candor-java) or `conformance/run.sh` (candor-spec) — both are block-scalar lines | 2026-09-12 | instrument (`candor/bin/gates.sh`) | the printed per-repo gate list cannot run the most expensive gate in the family | **CLOSED for candor-spec 2026-09-14 — candor-spec `3d08c4d`.** Both runnable conformance legs now carry their engine paths in `env:` with a one-line `run:`; `gates.sh candor-spec` goes 5 runnable lines → 7, listing `conformance/run.sh` for the first time. **MY FIRST CUT MADE IT WORSE AND THE GATE LIST SAID SO:** written as `bash "$GITHUB_WORKSPACE/candor-spec/conformance/run.sh"` it is correct in CI and unset on a developer box, so `gate-run.sh candor-spec` reported TWO FAILING GATES where it had previously reported none — an invisible gate turned into a false RED, which is the same class this block of work exists to remove. Now `cd candor-spec && bash conformance/run.sh`, the form the file's other gates already use. The THIRD leg stays a block scalar deliberately and is annotated: its `CANDOR_JAVA_JAR` is a shell substitution over a downloaded artifact, `env:` cannot run a command, and nobody can run it locally without first fetching the published jar. **HALF CLOSED — candor-java `86468ae`: `ci/self-gate.sh` is now its own one-line step, `gates.sh candor-java` lists it 1 time (was 0), and running the full list for the FIRST TIME gives `bash ci/self-gate.sh` **OK** — 10 gates, 9 ok, 1 failed (`nativeCompile`, GraalVM absent locally, covered by the `native` workflow). **candor-spec's half is OPEN and is NOT a formatting change, and the reason is now MEASURED rather than asserted.** Its conformance steps set `$GITHUB_WORKSPACE`-based env vars across continuation lines. I hypothesised the vars were redundant — `run.sh` already defaults to sibling discovery — and CHECKED before acting: **in the conformance job, `tombaldwin/candor-rust` is checked out at path `candor`** (`conformance.yml:121-125`), while the local default is `../../candor-rust`. The names genuinely differ, so a bare one-line `run:` would have broken the family's four-way gate. (The 126 `candor-scan` lines in a completed CI log confirm rust really does participate, so there is no hidden three-way run either.) The fix is a wrapper that branches on `GITHUB_WORKSPACE`, but it must serve THREE legs with different engine sets — 3-engine, 4-engine strict, and released-artifact — so it is a designed change rather than a quick one, and is deliberately not being rushed at the end of a session. AGENT-MEASURED by MUTATION (a 474/474 baseline in a scratch copy, one mutated file per run); not yet coordinator-verified.** Nothing is SILENTLY dropped — all seven repos were checked for job-level `defaults: run: working-directory:`, composite actions, `uses: ./` and `workflow_call`, and **none exist today**, so those parser blind spots are not currently reachable. What IS dropped is `run:` block-scalar bodies: named as `~` lines and counted INCOMPLETE, never run. Counted as STEPS (one block = one CI gate entirely unrun): **candor-spec 44%, candor-java 28%, candor-swift 22%, candor 21%, candor-ts 20%, candor-rust 9%.** Two by name: **`bash ci/self-gate.sh` is a block line in candor-java's `ci.yml` and appears ZERO times in the runnable gate list — the exact gate CLAUDE.md's cautionary tale is about** ("candor-swift's main sat RED for four commits… the failing gate — self-gate — was the one I never ran"); candor-swift's is a one-line step and IS run. And **`conformance/run.sh` appears three times in candor-spec's list, all as block lines, zero as gate lines.** Not a false green (`gate-run` exits 2 INCOMPLETE), but the list that exists so the set does not drift cannot run either. |
| R407 instrument: `gate-run.sh --dry-run` always exits 0 and prints NO verdict line | 2026-09-12 | instrument (`candor/bin/gate-run.sh`) | a preview that answers 0 where the real run answers 2 | **CLOSED — candor `73e5181`. Coordinator-verified both directions: candor-spec's preview went rc 0-with-no-verdict to **rc 2** with `DRY RUN — nothing was executed, so this is NOT a verdict` + `INCOMPLETE — 1 would be skipped, 29 block line(s) cannot be auto-run`; candor-agents still answers 0 and says why. A preview makes NO claim about whether a gate would PASS — only about what it could not RUN. AGENT-MEASURED by MUTATION (a 474/474 baseline in a scratch copy, one mutated file per run); not yet coordinator-verified.** `:288` `[ "$dry" = "--dry-run" ] && exit 0` sits ABOVE the `NOT GREEN` / `INCOMPLETE` / `OK` block. Measured: `gate-run.sh candor-spec --dry-run` gives rc **0** with last line `4 gate(s) run, 0 ok, 0 failed, 1 skipped, 29 block line(s) not auto-run` and no verdict sentence; the same repo's real run is rc 2. A reader previewing a repo gets a zero exit and nothing telling them the preview means nothing. |
| R408 instrument: `head -1` pin extraction takes the FIRST key match per file — the narrow-key shape, one comment away from judging the wrong line | 2026-09-12 | instrument (`candor/bin/release-verify.sh`, `release-preflight.sh`) **CLOSED — candor `2afe88e` (the `release-verify.sh` arm; the `checkpin` arm was closed earlier).** | latent; a version-bearing comment above a pin would misdirect the verifier | **AGENT-MEASURED by MUTATION (a 474/474 baseline in a scratch copy, one mutated file per run); not yet coordinator-verified.** `release-verify.sh:237` and `release-preflight.sh:97/403/509/526`. `adopt/candor.yml` ALREADY carries four lines matching `CANDOR_JAVA_VERSION` — a comment at `:6` and an interpolation at `:84` among them — and only `:81` is the real pin. It happens to be first. **This is the narrow-key class the corpus brief measures at 400 vs 16,461; latent here only by line ordering.** **CLOSED 2026-09-24, candor `2afe88e`.** The last `head -1` pin reads were `release-verify.sh`'s two engine pins, which decide whether a crate ABOVE the family line is excused — so the wrong line there excuses the wrong version. **`ENGINE_PIN_RUST` already appears on TWO lines of `bin/candor`** (the literal at :67 and the env-override fold at :79); today only one can match a numeric pattern, so `head -1` was right BY LUCK, which is the state this row was filed about rather than a state to leave. Now calls `pin_version` from `_release_set.sh` — **which that script already sourced**, and which the other R408 sites already use — so it REFUSES (rc 2) when one key matches differing versions, distinct from ABSENT (rc 1) which stays silent exactly as `head -1` did. The `^` anchor is kept, so a commented-out pin still cannot be read as the pin: strictly the old behaviour plus a refusal. **CALIBRATED BOTH DIRECTIONS on a COPY:** an injected second `ENGINE_PIN="0.1.0"` returns rc 2 naming both versions and noting `head -1` would have answered by line order alone; the real file returns 0.39.2, byte-identical to the old sed. release-test.sh OK — 474 assertions. |
| R409 java: the Fs surface-incomplete marking is ALLOWLIST-shaped at ONE site, so a benign sibling literal certifies a caller-controlled path — in the REFERENCE ENGINE | 2026-09-12 | **CLOSED — candor-java `e93c773`.** The locator is now read from the DESCRIPTOR (a `Path`/`File` operand at the receiver or ANY argument), so the handle carve-out falls out of the types rather than a verb list, and determinedness comes from a new `pathLit` on the analyzer's existing provenance. 122 jars, 221,864 rows: ADDED 0 REMOVED 0 CHANGED 8,174, all GAINING `incomplete`, none losing one. 8,138 already failed closed (disclosure only); exactly 36 were certifiable before and are not now, ALL 36 audited, three ground-truthed in `javap`. PART 88 java: a1arg ok a2recv ok. Original class was: java | **GATE BYPASS, live and PUBLISHED** — R383's shape, third engine **AND THE FIX'S OWN COMMIT TITLE OVERSTATED IT.** candor-java `e93c773` is titled *"the last masking bypass in the reference engine"*, and R421 is a masking bypass in the reference engine — `new FileOutputStream(userPath, true)` beside a benign literal, `allow Fs` exit 0, measured identical on the 0.36.1 jar so it was already there when that title was written. The commit is pushed and its message is immutable, so the correction lives here: **R409 closed the Path/File-DESCRIPTOR tail; the String-operand tail is open as R421.** Same shape as R414's overstated "CLOSED four-way" corrected the same day — in both cases the closure was written from the arms that PASSED, and inherited the boundary of the fixture rather than of the class. A CLOSED row must name the spellings its pin does NOT express. | **COORDINATOR-MEASURED end to end, and PREDICTED FROM SOURCE FIRST by a Fable review that read `Candor.java:5221` without running anything.** `ctx.surfaceIncomplete…add("Fs")` fires only inside the branch matching `Path.of`, `Paths.get` or a `PATH_CTOR_OWNERS` constructor AND `pathArgIsSingleString(desc)`. A `Path` PARAMETER consumed by `Files.write(p,b)` never enters it, so nothing marks the surface incomplete and a benign sibling literal certifies the invisible destination. **Measured on candor-java 0.36.1, one variable — the presence of the benign sibling:** with `Files.writeString(Path.of("/tmp/benign"),"x")` beside `Files.write(p,b)`, `allow Fs /tmp/benign` exits **0**, `candor-java: no violations`; with the sibling REMOVED the same function is caught by `AS-EFF-008` ("performs Fs with no visible literal — the surface cannot be certified"). CALIBRATION: `deny Fs` exits **1** naming that exact function, so the gate could fail. **THE FAMILY WAS SWEPT RATHER THAN THE SPELLING IN HAND (R346), and the sweep BOUNDS the finding: Net, Exec and Db are all CAUGHT** — `allow Net api.stripe.com`, `allow Exec ls` and `allow Db users` each exit 1 over the identical sibling-literal shape, because those three use the GENERAL rule (a call contributing no visible locator leaves the surface incomplete) while **Fs alone is an allowlist**. That is [[candor-denylist-over-allowlist]] measured INSIDE one engine: the three denylist-shaped effects are sound, the one allowlist-shaped effect is not. Fix by marking Fs incomplete wherever an Fs locator argument is not a captured literal, NOT by lengthening the constructor list — lengthening it is what left rust's R383 one spelling short twice. **THE FIX DIRECTION IS SETTLED AND HAS IN-FILE PRECEDENT: java ALREADY does per-call literal attribution for Net and Db via `Literals.literalArgsInWindow`, whose own doc-comment says it exists to kill "an AS-EFF-008 gate EVASION where a benign URL literal certified a runtime-computed host" — the exact defect this row reports, solved for two effects and never migrated to Fs**, which stayed on the `Path.of`/`Paths.get`/ctor construction model (`Candor.java:5209-5221`). That also explains the sweep result above: the three effects on per-call attribution are sound and the one that is not is Fs. **The migration is NOT a one-line change and must not be done by window alone**: `literalArgsInWindow` bounds at the previous call, so `Files.write(Path.of("/var/data"), b)` finds no literal in its OWN window and a naive port would mark a fully-DETERMINED path incomplete — over-masking the commonest idiom and making `allow Fs` unusable. The determined case has to be credited through the `Frame<ProvValue>` provenance frames already built at `Candor.java:4306`, or by crediting a path CONSTRUCTED in the same statement. **Price it with an A/B over real jars before shipping: the failure direction is noisy rather than silent, but a fix that fails closed on every ordinary write is a fix nobody can adopt.** **PRICED 2026-09-12 WITHOUT BUILDING THE FIX, and the measurement CONTRADICTS this row's own stated fear.** I wrote above that a naive migration would "over-mask the commonest idiom and make `allow Fs` unusable". Measured over 8,359 functions (candor-java's own classes plus the 5-jar corpus — joda-time, sqlite-jdbc, jackson-core, gson, commons-lang3): **2,982 carry Fs, and 2,579 of them — 86.5% — are ALREADY marked `incomplete`.** 327 carry a captured path. **Only 379 have NEITHER**, and that is the CEILING on what any such fix could newly mark: 12.7% of Fs-carrying functions, 4.5% of all functions. So java is already heavily hedged on Fs and the marginal cost is far smaller than the fear — the argument for deferring on over-mask grounds does not survive its own measurement. **THE CEILING IS NOT THE OVER-MASK:** the 379 are a mixture of genuinely-determined paths (which a fix must NOT mark — that part is the real cost) and genuinely invisible ones (which are R409 itself, and which the fix exists to mark). Separating them needs the `Frame<ProvValue>` credit, which is still the work. **Corpus caveat, stated: 5 jars plus self is thin, and joda-time alone supplies half the Fs functions** — a wider jar corpus would sharpen the number, not the conclusion. **REFINED, and the first number was the wrong denominator.** The 379 above counts functions carrying Fs at ALL, and most Fs in a real tree is INHERITED — the population is dominated by candor-java's own test helpers (`AdvisoryBoundTest.report`, `runCli`, `compileNetFixture`) which inherit Fs from a callee and have no locator of their own; a callee's incompleteness already propagates to them, so they are not this fix's business. Counting **DIRECT** Fs performers instead: **457 total — 83 carry a captured path, 210 (46%) are ALREADY `incomplete`, and 235 have NEITHER.** That 235 is the true ceiling: **2.8% of all functions**, not a sweeping over-mask. It still mixes two kinds and the fix must separate them: a call CONSUMING a path-typed argument whose locator was never captured (R409's defect, which must be marked) versus a HANDLE use-verb that takes no path at all (`stream.close()`, `fd.read()` — must NOT be marked, the ts engine keeps an explicit `FS_USE_VERBS` list for exactly this). **That is a DESCRIPTOR-level distinction, which java has and the syntactic engines do not — so the fix may be smaller than the `ProvValue` credit this row first assumed.** Named candidates from the 235, for whoever takes it: `Candor.copyClassesForUnion`, `Candor.compileForPeek`, `Candor$JdkSams.load`. **SAMPLED FROM SOURCE, and it surfaces the DESIGN QUESTION the implementer must answer before writing anything.** The report's `calls` field records intra-project edges only (121 of the 235 list no calls at all), so the path-consumer / handle-verb split cannot be derived from reports — it needs the descriptor. Reading two named candidates directly: `copyClassesForUnion(Path root, Path dest, String)` reaches `Files.isRegularFile(root)` / `Files.isDirectory(root)`, and `compileForPeek(List<Path>, List<Path>, Path)` is the same shape — **path-typed PARAMETERS, genuinely invisible locators, R409's defect rather than handle verbs.** But both are STAT calls, and that is the crux: **is `Files.exists(p)` / `isDirectory(p)` an Fs reach whose locator must be gated?** Existence checks are ubiquitous, so marking them moves the number a lot. ~~**candor-rust has already ruled on the neighbouring case … a path-stat whose path is the RECEIVER (`p.exists()`) is excluded, because the caller gates on `!is_method`.**~~ **THAT SENTENCE IS WRONG AND IS NOW R414.** It is not a ruling but a syntactic accident: the same list INCLUDES the ARGUMENT forms (`metadata`, `canonicalize`, `read_dir`), so rust answers the identical question two ways — and the receiver form is a LIVE GATE BYPASS in the shipped 0.36.2, measured: `p.exists()` beside a benign literal exits **0**. So this is a TWO-ENGINE question under ONE ruling, not a java-only decision with a rust precedent to lean on. java's `Files.isRegularFile(root)` is the ARGUMENT form, which that ruling does not cover. Settle that one question first — it decides whether the population is ~235 or a small fraction of it — and it is a spec-shaped decision, not an implementation detail. **ITS RECORDED VERDICT WAS AN AUDIT BOUNDARY DRAWN AROUND ITS OWN TRIGGER — established 2026-09-17 by [[R464]].** This row recorded *"Net, Exec and Db are all CAUGHT"*. Measured a second time: Net and Db are genuinely caught, and **Exec was checked only on the two owners INSIDE its own guard** (`ProcessBuilder.<init>`, `Runtime.exec`), which is the entire set that guard knew. Six other Exec spellings bypassed it. The verdict was not wrong about what it tested; it was wrong about what it covered, and it read as coverage for weeks. **When a row records an effect as CAUGHT, record the SET the check enumerated, not the effect name.** |
| R410 ts: `NET_ESTABLISHING` is an 11-name allowlist with no resolver verbs, so a benign sibling literal certifies a caller-controlled DNS target — and the run PRINTS "nothing hidden" | 2026-09-12 | ts | **CLOSED — candor-ts `587e70d`.** GATE BYPASS, live and PUBLISHED when found — the FOURTH engine in this class | **COORDINATOR-MEASURED end to end; PREDICTED FROM SOURCE FIRST by a Fable review that read `scan.mjs:7146` without running anything.** `NET_ESTABLISHING` holds request/get/post/put/patch/delete/head/options/connect/createConnection/fetch — no `lookup`, no `resolve` — while `dns.resolve` DOES classify Net (`scan.mjs:6660`). So a resolver call carries the effect and contributes no host, and nothing marks the surface incomplete. **Measured on candor-ts at HEAD, one variable — the benign sibling:** `fetch("https://api.stripe.com")` beside `dns.lookup(host, cb)` gives `hosts:["api.stripe.com"]`, `incomplete: null`, and `allow Net api.stripe.com` exits **0**. Sibling REMOVED: the same function is caught by `AS-EFF-008` and exits 1. `dns.resolve` behaves identically, so this is the resolver FAMILY, not one verb. CALIBRATION: `deny Net` exits 1. **AND THE RUN PRINTS AN AFFIRMATIVE FALSEHOOD** — `candor: nothing hidden — every effect sits where its name says it should.` over a destination that is hidden. That line comes from the masked-literal differential, not the policy verdict, so it is a SECOND check wrong in the same direction. **CORRECTION, measured after this row was first written — the original sentence said ALL FOUR ENGINES treat "there was a string" as proof of completeness, and that is broader than the measurement.** The simple shape (a literal write beside a parameter write) was then run on every engine: **rust CATCHES it** (`AS-EFF-008` on `fs::write(p,b)`) and **swift CATCHES it** ("reaches a structurally-invisible Fs endpoint a visible literal cannot mask"). The class IS four-way, but the reachable SPELLING differs per engine and so does the mechanism: java keys on whether the FUNCTION published any Fs literal (R409); ts keys on an establishing-verb allowlist that omits resolvers (this row); rust keys on a path-taking-call allowlist that omits method forms and `chown` (R399); swift finds a literal but takes the WRONG ARGUMENT (`fopen`, R393) or DISCARDS it (bare relative filename, R395). One invariant, four mechanisms — which is exactly why the fix is the invariant and not four lists.** A four-way class is what SPEC clauses and conformance PARTs exist for — see the invariant queued in candor/BACKLOG.md §3. |
| R411 conformance: PART 12, the flagship GATE-VERDICT differential, pins the arm that WORKS and is blind by construction to the arm that does not | 2026-09-12 | **CLOSED — candor-spec `39f0236`.** Both arms landed together with R409's fix, as the held draft required. **PROVEN TO DISCRIMINATE AGAINST A REAL PRE-FIX BINARY — the 0.36.1 jar, built 11 hours before R409 — rather than against an injected fault:** `gen_masking`'s new `fs_param` cell reports `m→0 c→0 [EVASION]` (the cardinal gate-evasion, an engine PASSING a masked-denied program) while the OLD `fs` cell reports `m→1 c→0 [ok]` ON THE SAME JAR IN THE SAME RUN — two rows, opposite verdicts, which is this row's claim demonstrated rather than argued. PART 12 on that jar shows java missing `AS-EFF-008 masked` where rust carries it, and FAILS. Both green four-way on HEAD. **The fix is one line of insight: every old fixture CONSTRUCTED the masked path inline (`Paths.get(p)`, `"/etc/" + m`), which is the one shape every engine's path-construction branch already marks — so two instruments written independently for the same evasion were blind the same way. The new arms hand the write the PARAMETER ITSELF, untouched.** Also fixed a hazard the arm created: two cases now share the effect `Fs`, and the coverage table printed two rows both labelled "Fs" — which reads as a duplicate and is how one gets deleted later. Rows are labelled by case id. Original: four-way (conformance) | **ARM WRITTEN AND MEASURED FOUR-WAY — candor-spec `ecb4c86` (`conformance/gate/R411-DEFECT-ARM.md`); HELD, not landed, until R409 is fixed.** AN INSTRUMENT THAT CANNOT FAIL on the masking class — the boundary drawn around its own trigger | **COORDINATOR-MEASURED.** `conformance/gate/policy` declares `deny Fs` and `allow Fs /var/data`, and every language's fixture is the same single method — java's is `public void save(String p, byte[] b) { Files.write(Paths.get(p), b); }`. **There is NO benign sibling literal anywhere in it**, so the function publishes no Fs literal at all and every engine correctly answers `AS-EFF-008` uncertifiable. That is the CONTROL arm of the masking class, and PART 12 pins only it. The DEFECT arm — the identical runtime write with a benign literal write beside it — is absent from the fixture, so the differential cannot see R409 (java exit 0) or R410 (ts exit 0) and never could. **This is the family's own rule — "an audit's boundary must be stated and justified, and must not be drawn around its own trigger" — instantiated inside the flagship gate differential**, and it is the third "instrument that cannot fail" shape found in this file's history. FIX: add a SECOND method to all four gate fixtures that writes one benign literal and one parameter path, and extend PART 12's `EXPECT` with its `AS-EFF-008` row. **MEASURED 2026-09-12 against the shipped 0.36.1 engines, and the prediction held with one correction — ts had by then been FIXED (R410), so the split is three-to-one, not two-to-two:** rust, ts and swift all flag `masked` with `AS-EFF-008`; **java does not.** That asymmetry is the discrimination that makes this a real PART rather than a second control. **NOT LANDED, and the reason is stated rather than deferred:** the suite is strictly pass/fail with no xfail or declared-divergence mechanism, so committing the arm puts `conformance` RED on `main` until R409 lands. The arm, its four-language source, its measured table and its `EXPECT` delta are therefore written IN-TREE at `conformance/gate/R411-DEFECT-ARM.md` — durable and reviewable — and land in the same change as the java fix. The java arm must take a `Path` PARAMETER, not a `String`: `Paths.get(p)` enters java's construction branch and IS marked incomplete, which is exactly why the existing `save` fixture passes and why this one had to be built differently. **AND IT IS NOT ONE INSTRUMENT, IT IS TWO — found 2026-09-12 by running the four-way suite after this row was filed.** `conformance/run.sh` also carries a **CROSS-ENGINE GATE-MASKING differential — masked-literal allowlist evasion (AS-EFF-008 opaque)**, built for precisely this class, covering 16 (effect × engine) cells, and its **Fs × java** cell reports `m→1 c→0 [ok]` — GREEN — while R409 is a live java Fs bypass. Both are true because `gen_masking.py`'s java program is `Files.write(java.nio.file.Path.of(p), …)`: **`Path.of(p)` ENTERS java's construction branch and IS correctly marked incomplete**, so the cell passes. R409's spelling — `Files.write(p, b)` where `p` is already a `Path` — never reaches that branch. **PART 12's fixture makes the identical choice** (`Files.write(Paths.get(p), b)`). So two independent instruments, written years apart for the same evasion, both CONSTRUCT THE PATH INLINE, which is the only shape the engine handles — and neither can ever see a path that arrived as a parameter. That is the audit-boundary-around-its-own-trigger rule operating at the level of the whole conformance suite, and it is why a live four-way class survived every standing gate. **The defect arm in `conformance/gate/R411-DEFECT-ARM.md` is therefore owed by BOTH instruments, not one.** |
| R412 ts: inverting `NET_ESTABLISHING` into a use-verb DENYLIST is NOT a drop-in — it over-charges on an OPEN-ENDED set, and the measurement says why | 2026-09-12 | ts | BUILT, MEASURED, and DELIBERATELY NOT SHIPPED — the durable rule is receiver-based, not list-based | **COORDINATOR-MEASURED end to end, then reverted.** R410 closed the resolver instance; the SHAPE problem it left is that `NET_ESTABLISHING` is an INCLUSION list, so forgetting a member UNDER-reports — the opposite of `FS_USE_VERBS`/`EXEC_USE_VERBS` in the same file, where forgetting over-charges and is safe, and the reason candor-java's Net surface is sound (it uses the GENERAL rule). Inverting it looked like the structural fix. **Built it: a Net call is establishing unless the member is a known use-verb on an already-connected handle. Full suite GREEN at 2708/0 after one real correction, and the A/B priced it at 16 of 550 rows (2.9%), all in `got`, ADDED 0 / REMOVED 0, nothing lost.** Those aggregates say SHIP. **Tracing the rows to a body says do not, and that is the finding.** All 16 trace to ONE over-charge: `request.listenerCount(…)` in `got/source/core/timed-out.ts`, an EventEmitter query on an already-connected `ClientRequest` that names no destination — plus fifteen callers inheriting the marker through the join. **Every handle method absent from the denylist becomes a false `establishing`, and that set is open-ended** (`listenerCount`, `eventNames`, `rawListeners`, `prependListener`, `setMaxListeners`, `listeners`, `getMaxListeners`, …): accuracy would be proportional to how much of Node's API the author happened to enumerate. **THE DURABLE RULE IS RECEIVER-BASED, not list-based: a call on a MODULE (`http.request`, `dns.lookup`) is establishing; a member call on a handle VALUE is a use-verb BY CONSTRUCTION, whatever it is named.** That needs the checker's receiver type, which ts has. **Two things this cost nothing to learn and are worth keeping: (1) the suite already encoded the inclusion list's knowledge — an R130 test caught `send`, a use-verb on a socket but the ESTABLISHING verb on dgram; (2) inverting the rule made `NET_ESTABLISHING` unreachable and ESLINT reported it unused — R391's dead-list shape caught by a linter rather than by renaming and rebuilding, which is a structural defence candor-swift does not have.** |
| R413 instrument: checks [5b] and [9] can CONTRADICT each other once a repo is tagged mid-cut, and [9]'s own suggested remedy writes a FALSE claim into a published changelog | 2026-09-12 | instrument (`candor/bin/release-preflight.sh`) | **CLOSED — the fix is in `release-preflight.sh` and coordinator-READ 2026-09-14; the status cell had never been updated.** | **CLOSED. Verified by reading the shipped code, not only the row:** `[9]` now skips a repo whose `v$WANT_VER` tag already exists, with the measurement and the reasoning in the comment — and it asks the same question `release.sh`'s `rel()` skip branch already asks, so the two authorities agree rather than being two lists for one question. A repo NOT yet tagged is unaffected, which is the case the check was written for. The row's detail had said FIXED since the 0.36.2 cut while the STATUS cell still read as open, which is why it kept appearing in the queue. **COORDINATOR-MEASURED during the 0.36.2 cut, and it BLOCKED that cut twice.** The family cuts in two stages: engines first, then the umbrella after the pins move. candor-ts was tagged and published at `v0.36.2`, then took two more commits (the `mcpName` field and the registry job's `continue-on-error`) while the umbrella half was still in flight. That left it unable to satisfy both checks: **[5b] demands the changelog describe what the repo ships** — source moved since the tag and CHANGELOG.md had not — while **[9] demands `## Unreleased` be empty when cutting**. Satisfying [5b] populates the section; satisfying [9] empties it. **AND [9]'s printed remedy is the dangerous part: "rename it to `## [0.36.2]`" would file post-tag commits under a RELEASED version's heading — a false claim in a published changelog, which is precisely the outcome neither gate should be able to force.** FIXED: [9] now skips a repo whose `v$WANT_VER` tag already exists, because such a repo has by definition shipped and its `## Unreleased` is NEXT-version staging rather than content stranded by this cut. It asks the same question `release.sh`'s `rel()` skip branch already asks, so the two authorities agree; a repo not yet tagged is unaffected, which is the case [9] was written for. **The general shape is worth keeping: [9] already scoped itself to "THE CUT SET, not the family" and was still one distinction short — being IN the cut set and having ALREADY BEEN CUT are different states, and a two-stage release makes the difference reachable.** |
| R414 rust: a RECEIVER-form path stat (`p.exists()`, `p.metadata()`) charges Fs, captures no locator and marks nothing incomplete — so a benign sibling literal certifies it | 2026-09-12 | **PARTIALLY CLOSED — and the first cut of this note said "CLOSED four-way", which was an OVERSTATED CLOSURE in the register itself.** rust `9bbf119`, java `e93c773`, swift `a06e8f9` close the `URL`/`Path` receiver shape; ts is declared inexpressible. **SWIFT IS NOT CLOSED FOR THE `Files` PACKAGE (`File`/`Folder`/`Storage`) — 11 of its 12 receiver verbs are SILENT, measured 2026-09-13 by a review that vendored the real dependency and COMPILED the fixture: `allow Fs /tmp/benign` exits 0 over a caller-supplied file DELETION, printing "nothing hidden".** The one verb that is caught (`createFile(named: "z")`) is caught incidentally, because `"z"` is not path-shaped and R395's fail-closed arm fires — spelling luck, not coverage. See R418. **THE DEFERRAL THAT PRODUCED THIS ROW'S WRONG WORD DOES NOT SURVIVE MEASUREMENT.** It was deferred on the grounds that no swift corpus here carries `Files`, so a fix would ship UNPRICED — which priced the FIX and never priced the HOLE. Pricing the hole needed one dependency and ten minutes. That is the same error this register already records against java R409 ("my own 'unusable over-mask' deferral was wrong"), repeated, and the register then compounded it by writing CLOSED.  Pinned by PART 88 (`gen_stat_locator.py`) arm a2recv and by SPEC §2 ⟨0.37⟩, both landed with the fixes rather than after them. rust also closed the SECOND half nobody had named: a DETERMINED receiver was neither captured NOR marked, so a policy naming a different directory certified it — `Path::new("/tmp/elsewhere").exists()` under `allow Fs /tmp/benign` was exit 0 and is now exit 1. Original class was: rust | **GATE BYPASS, live in the SHIPPED 0.36.2** — and it retires a 'ruling' this register asserted | **FOUND BY A THIRD FABLE REVIEW OF A PLAN, then COORDINATOR-VERIFIED INDEPENDENTLY.** R409's row claimed *"candor-rust has already ruled on the neighbouring case — a path-stat whose path is the RECEIVER (`p.exists()`) is excluded, because the caller gates on `!is_method`"*. **That was never a ruling. It is a syntactic accident of the `!is_method` gate**, and the same list (`candor-classify/src/lib.rs:3473`) INCLUDES the argument forms `metadata`/`symlink_metadata`/`canonicalize`/`read_dir` — so rust answers the identical question YES for `fs::metadata(p)` and NO for `p.metadata()`. Measured on the shipped 0.36.2 binary (verified newer than source), one variable: **`p.exists()` beside `fs::write("/tmp/benign")` reports `direct:['Fs'] paths:['/tmp/benign'] incomplete:NONE` and, ISOLATED, `allow Fs /tmp/benign` exits **0** with zero violations and prints `candor: nothing hidden — every effect sits where its name says it should`** — an affirmative false statement over a caller-controlled stat. Calibration: `deny Fs` exits 1 on the same fixture. Controls: the argument form `fs::metadata(p)` IS caught, and `fs::write(p)` IS caught. **`p.exists()`/`p.is_file()` are among the commonest Fs spellings in Rust, so this is a large share of the 9,811 UNMASKED lines a count-ratchet would have dismissed as the safe case.** SEVERITY IS A RULING, not a measurement: an existence bit is not a write, and whether a stat's locator must be gated is the same question R409 raises for java — `fsKind` (`Candor.java:8033`) already classifies `exists`/`isDirectory` as an Fs READ, so the family's own tables argue the fail-closed direction. **Write that ruling as a SPEC clause with a TWO-SPELLING conformance PART (argument form and receiver form, each beside a benign sibling) before either engine is touched** — it decides R409's population and fixes rust's split in the same stroke. **MEASURED FOUR-WAY 2026-09-12 by the stat PART's own four arms, and IT IS NOT RUST-ONLY: every engine that can express a receiver-form stat is SILENT on it.** rust `p.exists()`, java `f.exists()` on a `File` parameter, swift `u.checkResourceIsReachable()` — all three report `paths:['/tmp/benign'] incomplete:None` beside a benign sibling. ts has no receiver-form stat and is declared inexpressible. **So this row's title understates it: the receiver form is a FAMILY-WIDE gap, and the argument form splits 3-1 the other way** — rust, ts and swift all mark `fs::metadata(p)` / `existsSync(p)` / `fileExists(atPath:)` incomplete, and JAVA alone is silent on `Files.exists(p)` (which is R409's class reaching the stat surface). |
| R415 swift: `locatorLabelsForFree` narrowed the positional picker by THREE NAMES and the call site still falls back to a whole-argument scan — a live SPEC ⟨0.29⟩ non-conformance | 2026-09-12 | swift | **SEVERITY RESTORED: it WAS a live gate bypass. `execvP` half CLOSED — candor-swift `d05241f`. The schema half (`posix_spawn`) stays OPEN.** | **MY SEVERITY DOWNGRADE, MADE EARLIER THE SAME DAY, WAS WRONG AND IS RETRACTED.** I looked for a gate bypass, failed to build one from the Db spelling that triggered the row, and wrote into this cell that *"the Exec ones take C arrays rather than string literals"*. That is true of `execv`/`execvp`/`execve` and FALSE of `execvP(file, search_path, argv)`, whose second parameter is a `const char *` — found by a plan review READING THE SIGNATURE, which is the one thing I did not do. **A row's stated MECHANISM is a hypothesis; so is a row's stated BOUNDARY, and I had just corrected a severity on the strength of one.** MEASURED, calibrated (`deny Exec` → 1 on the same file first): `execvP(runtimeFile, "/usr/bin:/bin", &argv)` → `cmds: ["/usr/bin:/bin"]`, `incomplete: none`, **`allow Exec /usr/bin:/bin` EXIT 0** over a caller-controlled command. Fabrication and masking from one sibling literal. CLOSED for the whole `exec*` family, not the measured name — the other three landed on argument 0 only because their second argument is an argv ARRAY, which is spelling luck of the kind this register keeps paying for. **STILL OPEN: `posix_spawn`/`posix_spawnp`,** whose locator is argument **1**. This table's `""` means the FIRST unlabeled argument and `literalForLabel` returns nil the moment that argument is not a plain literal, so `[""]` lands on `&pid`. They resolve correctly TODAY by the same luck, and closing them needs the POSITION schema — the type changes, not the entries. **VERIFIED with a covered-vs-uncovered pair, one variable.** `fopen` IS in the table: `fopen(runtimePath, "r")` reports `incomplete: ["Fs"]` — it fails closed and does not capture the MODE. `sqlite3_open_v2` is NOT: `sqlite3_open_v2(runtimePath, &db, flags, "unix-dotfile")` reports **`incomplete: None`** — a runtime-controlled locator whose surface reads COMPLETE, because the whole-argument scan found the VFS-name sibling and `lit != nil` kept the incompleteness guard from firing, even though that literal was then discarded downstream. That is SPEC ⟨0.29⟩'s position rule violated, and PART 51 pins it four-way. **BUT THE SEVERITY IN THIS ROW WAS TOO HIGH AND IS CORRECTED:** I could not construct a GATE BYPASS from it. `allow Db users` fails closed on the uncovered call anyway, because a FILENAME is not SQL so no `tables` entry is published either way — the divergence is in the `incomplete` FLAG, which a completeness-manifest consumer reads and a policy here does not. The bypass would need an uncovered establishing-free call whose sibling literal PASSES the surface's own shape test; the uncovered Fs names (`FileHandle`, `File`, `Folder`) are all single-argument, so no sibling exists, and the Exec ones take C arrays rather than string literals. **Stated because a row that overstates its severity gets fixed in the wrong order.** **AND THE FIX IS BIGGER THAN THE ROW IMPLIES — this is the useful finding.** The row prescribes *"a TOTAL table from (receiver kind, member) to either a locator POSITION, or INHERITED, or OPAQUE"*. `locatorLabelsForFree` returns `Set<String>` of LABELS, where `""` means *the first UNLABELED argument* — and `posix_spawn(pid, path, …)`'s locator is argument **1**. A C call has no labels, so every argument is unlabeled and **the current schema cannot express "the second one" at all.** Making the table total therefore requires changing its TYPE to carry positions, not just adding names to it — which is why adding the missing names alone would look like a fix and silently keep picking `posix_spawn`'s locator by accident. **CLOSED 2026-09-15, candor-swift `3eca7f5`, as ONE change with [[R432]].** `sqlite3_open_v2(runtimePath, &db, 6, "unix-dotfile")` reporting `incomplete: None` was reproduced and now reports `["Db"]`. **`posix_spawn`'s "correct by TYPE, not by luck" reasoning was VERIFIED before anything near it was touched** — `path` is the only parameter a Swift string literal can inhabit — and declared anyway. The locator table's type changed from `Set<String>` to `label/position/opaque` because a set of names could not express "argument 1" at all. R431 was NOT reintroduced: the corpus probe caught a real GAIN (`sqlite3_prepare_v2 → "PRAGMA journal_mode"`) that a literal-only picker would have lost. |
| R416 rust and ts LOSE a locally-bound literal path, so a fully DETERMINED destination is reported uncertifiable — the over-charge control of the stat PART, red on its first run | 2026-09-12 | **CLOSED — candor-rust `58242a4`, candor-ts `d9c52d6`.** rust peels `Path::new`/`PathBuf::from` in `resolve_str_expr` and ONLY those two (both documented identity over the string; `join`/`with_extension`/`canonicalize` transform it and still resolve to nothing). ts's path position now falls back to the strict `constStringValue` the Net surface has used since ⟨0.29⟩. **Found by PART 88's OVER-CHARGE CONTROL on its first execution, not by a defect hunt** — which is the entire case for writing a control arm beside a defect arm. Original class was: rust, ts | OVER-MASK (fails closed, but makes idiomatic code uncertifiable) | **FOUND BY THE OVER-CHARGE CONTROL ARM ON ITS FIRST EXECUTION**, which is the arm this PART was given precisely because a defect arm alone is R411's shape. **rust:** `let p = Path::new("/tmp/benign"); fs::write(p, b"x")` reports `paths:None incomplete:['Fs']` and `allow Fs /tmp/benign` REFUSES it — while the inline form, a plain `&str` local (`let p = "/tmp/benign"`) and a `const` are all captured correctly. **It is the wrapper call `Path::new(...)` that breaks the chain.** **ts is worse:** `const p = "/tmp/benign"; fs.writeFileSync(p, "")` loses it too — a plain string local, which rust credits. **java and swift both CREDIT the constructed local** (`Path p = Paths.get("/tmp/benign")`, `let p = "/tmp/benign"`), so the family splits 2-2 on what "Captured" means. **WHY IT MATTERS BEYOND PRECISION: any fix to R414 that keys on "the locator was not captured" will COMPOUND this in rust and ts** — the engines that most need the receiver-form fix are the two that already over-mask a determined path. "Captured" has to be defined as a VALUE fact (the locator position's value is statically determined) with engines permitted to under-approximate, not as a syntactic window. Fails CLOSED, so it is a usability and precision defect, not a soundness one. |
| R417 rust: the CAPABILITY-`Dir` filesystem API (`cap_std::fs::Dir::write(caller, …)`) is masked by NOTHING, so a benign sibling literal CERTIFIES a caller-chosen path and `allow Fs <lit>` exits 0 | 2026-09-12 | **CLOSED — candor-rust `58242a4`.** Mask-by-default on a `Dir` receiver with a signature-checked denylist of the path-free methods. Original class was: rust | **CARDINAL-ADJACENT: GATE BYPASS (AS-EFF-008)** | **LIVE, MEASURED, with the free-fn spelling as the isolating control in the same fixture.** `control(caller)` = `fs::write("/tmp/benign",…)` then `fs::write(caller,…)` reports `incomplete:['Fs']` and `allow Fs /tmp/benign` exits 1, correctly. `mixed(d,caller)` = the same benign literal then `d.write(caller, b"pwned")` on a `cap_std::fs::Dir` reports `paths:['/tmp/benign'] incomplete:NONE` and the SAME policy exits **0**. A third arm proves the effect was seen: `alone(d,caller)` is `direct:['Fs']` — candor classified the call, resolved its path in full to `cap_std::fs::Dir::write`, and simply had two names in `is_fs_path_arg_method`. **NOT A RESOLUTION GAP — AN ALLOWLIST LENGTH.** **HOW IT WAS FOUND:** a BACKLOG item said "`is_fs_path_arg_method` should consult `is_fs_path_arg`" — and that entry had already named cap-std `Dir::open/write/read/…` as the target and already said "A/B before believing it; widening is a typing change and two earlier ones over-reached". It was right about the vein and right about the hazard. What it could not say is the MECHANISM: consulting `is_fs_path_arg` LITERALLY is unsound, because the two lists collide on 8 leaves (`write`/`read`/`open`/`copy`/…) and an unscoped union masks all 267 `File` handle use-verbs in the registry. Building the probe rather than the union is what turned a correct hunch into a measured exit-0 — the entry is right once SCOPED BY RECEIVER TYPE, and that scoping is the whole fix. **FIX AND ITS DIRECTION:** on a receiver segment of exactly `Dir` the default is now MASK, with a short DENYLIST of the provably path-free methods (`try_clone`, `entries`, `dir_metadata`, `remove_open_dir`, `open_parent_dir`, the handle conversions) — because 24 of the 49 methods declared on `cap_std::fs::Dir` are already `is_fs_path_arg` leaves and most of the rest take a path too, so an ALLOWLIST here is the R346→R348 hand-list vein by construction. R399 settled that an under-catch on this guard is a broken gate, not a missed mask. Scoped on the receiver segment, so `cap_std`, `fs_utf8`, `cap_async_std` and `openat` are covered without naming a crate. **THE DENYLIST'S OWN FIRST CUT CONTAINED A BYPASS:** `read_dir` was excluded on the reasonable-sounding grounds that `entries()` is the self-only form, and `cap_std::fs::Dir::read_dir` is `fn read_dir<P: AsRef<Path>>(&self, path: P)`. Caught by reading signatures rather than recognising names; every remaining entry was then checked the same way. **REACH, MEASURED over 1545 registry crates so the row cannot be read as bigger than it is: the `Dir` arm fires ZERO times.** cap-std is IN the corpus as a crate but has no call sites in it, so R417 is REPRODUCIBLE AND LATENT here, not live at scale — the exit-0 is real, the blast radius on this corpus is nil, and both halves of that belong in the row. **The number that generalises is the other one: of 1658 Fs mask triggers, only 78 (4.7%) are METHOD-form, and all 78 are the two names the list already had** (`OpenOptions::open` 69+2, `DirBuilder::create` 5+2). So the method half of this guard is under a twentieth of its firings and was never stress-tested by real code — which is the mechanism by which a two-name allowlist rots invisibly, and the reason the follow-up question is worth asking of the other three engines rather than filed as a rust curiosity. A corpus A/B would have shown this fix changing NOTHING; only a fixture could find it. **GATED:** `a_capability_dir_method_path_cannot_be_certified_by_a_sibling_literal` carries all three arms — the defect, the free-fn calibration control, and a `try_clone` OVER-MASK control that prices the mask-by-default decision. Proven to discriminate against a rebuilt revert. **Also added: the Fs twin of R379's trigger probe (`CANDOR_MASK_DEBUG` → `R417MASK`), which the Fs side never had** — without it an A/B showing no change cannot tell a safely-narrow rule from an unreached one. |
| R418 swift: the `Files` package receiver shape is SILENT on 11 of 12 verbs — `allow Fs <lit>` exits 0 over a caller-supplied file DELETION | 2026-09-13 | swift | **CLOSED — candor-swift `ef7d0f9`.** Fixed as THREE coordinated parts, because the obvious one alone opens a new hole: `isReceiverLocatorMember` learns `File`/`Folder`/`Storage` but ONLY the single-locator verbs; `recordFilesTwoPath` answers `move`/`copy`/`rename`/`create*`, where the receiver is the SOURCE or PARENT and an argument carries the rest (see R424); and `LOCATOR_CTOR_ARG` learns `File`/`Folder`, without which the widened predicate makes `File(path: "/tmp/x").delete()` — a locator in plain sight — fail closed. PRICED against the corpus this row's deferral said did not exist (`JohnSundell/Publish`, 95 swift files, 27 importing Files): **0 real paths lost, 1 FABRICATED path removed (see R425), 5 destinations newly NAMED, 34 functions newly incomplete** — the last verified genuine by reading the call sites (a `File` PARAMETER being read; a runtime-computed output path; a call where BOTH locators are parameters). Publish writes wherever it is pointed, so incomplete is the correct verdict and the pre-fix *complete* was the false one. 7 tests, the 3 defect arms falsified against a rebuilt pre-fix engine | MEASURED by a review that vendored `JohnSundell/Files` as a real SPM dependency and COMPILED the fixture rather than reasoning about it: `public func deleteIt(_ f: File) throws { try "x".write(toFile: "/tmp/benign", ...); try f.delete() }` reports `paths:["/tmp/benign"] incomplete:NONE` and `allow Fs /tmp/benign` exits 0, printing *"nothing hidden"*, over a caller-supplied DELETE. Calibrated: `deny Fs` exits 1 naming the function, so the instrument can fail. Swept the whole of `FILES_MEMBERS`: `read`, `readAsString`, `write`, `append`, `delete`, `move`, `copy`, `rename`, `empty` and a non-literal `createSubfolder` are all silent; only `createFile(named: "z")` is caught and only INCIDENTALLY, because `"z"` is not path-shaped so R395's fail-closed arm fires. **Spelling luck is not coverage.** R414 deferred this because no swift corpus here carries the dependency, so a fix would ship UNPRICED — which priced the FIX and never priced the HOLE; pricing the hole took one dependency and ten minutes. The rationale also claimed the masked case 'needs the value to arrive as a parameter', which reads as a narrowing and is not one: a `File`/`Folder` parameter is the library's idiom. **The limitation was recorded ONLY in a doc comment in Classifier.swift — not in the CHANGELOG, not in AGENTS.md, and R414's row said CLOSED four-way**, which is why that row is now corrected too. |
| R419 swift: a MUTATING method on a `URL` is not a locator MOVE, so R414 publishes a positive claim about the WRONG destination — and `allow Exec /bin` certifies `/bin/<caller>` today | 2026-09-13 | swift | **CLOSED — candor-swift `c3bc15f`.** A member CALL on a name is now recorded by `LocatorMoveScanner` alongside assignment/`&inout`/property writes, and judged at the point of use against a per-binder-kind ALLOWLIST of calls that provably do not move a path. The fabricated locator is WITHDRAWN, not corrected — the true destination is unknowable, and `cmds`/`paths` going absent with `incomplete` set is the honest answer. **Allowlist, not denylist, and the row's stated direction is therefore overruled with a reason:** the MOVERS are ordinary Foundation spellings (`appendPathComponent`, `deleteLastPathComponent`, `append(path:)`, `standardize`, `resolveSymlinksInPath`), so a denylist of movers would have to be COMPLETE to be sound — the hand-list vein, in an engine with no type checker. The sound default is *moved*. **WHETHER CALLS COUNT AT ALL DEPENDS ON THE BINDER'S KIND:** a `Process` is a CLASS, so no call can change which object the name denotes; that consumer passes `inertCalls: nil` and `testConfigureThenLaunchStillCertifies` fails if anyone makes the rule global. **THE MEASUREMENT THAT MATTERED CAME BACK EMPTY:** an A/B over 17,944 units / 8,163 effectful functions across 14 real Swift projects reported CHANGED 0 with no hollow repos — and a REACH PROBE showed CONSULT 0, so the decision point was never reached and the A/B was evidence of nothing. The corpus's 28 literal-bound locators all sit in `Tests/` dirs, which candor excludes as harness targets. Over-mask spot-checked directly instead: the realistic `URLRequest` idiom (`setValue`/`addValue`) certifies, an unknown mutator fails closed, and no URL path-mutator appears anywhere in the 2,700-file corpus. 8 tests, falsified pre-fix | `LocatorMoveScanner` records assignment, compound assignment, `&inout`, property writes and binder counts — but NOT mutating method calls. `URL` is a VALUE type whose path is edited in place (`appendPathComponent`, `deleteLastPathComponent`, `append(path:)`, `standardize`, `resolveSymlinksInPath`), so `locatorNameIsStable` returns true and the binder's ORIGINAL literal is published as the call's destination with `incomplete` absent. Measured, compiled, five fixtures: `var u = URL(fileURLWithPath:"/tmp/benign"); u.appendPathComponent(secret)` publishes `paths:["/tmp/benign"]` where the true destination is `/tmp/benign/<caller>`; `appendPathComponent("../../etc/passwd")` publishes `/tmp/benign` for a write to `/etc/passwd`; three `deleteLastPathComponent()` calls publish the deepest path for a write to `/tmp`. **The gate OUTCOME is unchanged from pre-⟨0.37⟩ — PRE was silent, POST is WRONG — so this is not a new bypass but it converts silence into a false positive claim, which the corpus brief ranks as worse than silence.** It also moves `FolderDesktop` in BOTH directions across two fixtures. **THE ROOT DEFECT IS LIVE ON Exec AND PRE-DATES ALL OF THIS:** `var u = URL(fileURLWithPath:"/bin"); u.appendPathComponent(s); p.executableURL = u; try p.run()` reports `cmds:["/bin"] incomplete:NONE` on BOTH the pre- and post-⟨0.37⟩ binaries, so `allow Exec /bin` certifies execution of `/bin/<caller-supplied>`. Fix direction: a mutating call on a tracked name is a MOVE, carved out by a DENYLIST of provably non-moving members, never an allowlist. |
| R420 swift: `URL(string:)` publishes a URL STRING as a filesystem path, and the protected-folder class is silently lost | 2026-09-13 | swift | **CLOSED — candor-swift `c3c48b1`.** A `file:` locator is DECODED through Foundation's own parser (percent-escapes and the authority form handled by the thing that defines them, not by string surgery), and a NON-file scheme names no filesystem destination: withheld AND disclosed, never silently dropped — dropping it while leaving the surface complete is R395's defect. **The structural fix is the one that matters: FOUR sites inserted into `paths` directly, which is how one could publish a URL string while another published a path.** They now route through a single `insertFsPath`, and the protected-folder classification moved INSIDE it so it runs on the NORMALISED path — it used to run on the raw literal, which is exactly where this hid. A scheme requires the remainder to begin with `/`, so a relative name merely containing a colon is not mistaken for one (its own control arm, because a later tightening could otherwise fail closed on real paths). 6 tests; the headline assertion is that the two spellings AGREE, which is the only form a fix cannot satisfy by changing both in the same wrong way | `resolveConstString`/`locatorCtorLiteral` treat `URL(string: X)` and `URL(fileURLWithPath: X)` identically — correct for the Net surface they were built for, wrong when the result lands in Fs `paths`. Two spellings of ONE destination disagree: `URL(fileURLWithPath: "/Users/tom/Desktop/secret.txt").checkResourceIsReachable()` yields `inferred:["FolderDesktop","Fs"]` and is named by `deny FolderDesktop`; `URL(string: "file:///Users/tom/Desktop/secret.txt")!.checkResourceIsReachable()` yields `inferred:["Fs"]`, `paths:["file:///Users/tom/Desktop/secret.txt"]`, `incomplete:NONE`, and `deny FolderDesktop` is SILENT. `pathClasses` keys on a `/Users/...` prefix and the scheme defeats it; because a literal WAS captured, R395's incompleteness guard never fires either. **An app whose privacy manifest lacks `NSDesktopFolderUsageDescription` verifies green.** Same family: `URL(string: "https://example.com/a")!.checkResourceIsReachable()` publishes an https URL as an Fs path — fails closed against an allowlist, so lower severity, but it is a false claim in the report. |
| R421 java: the String-locator tail reaches NO masking guard — `new FileOutputStream(userPath, true)` beside a benign literal exits 0 | 2026-09-13 | java | **CLOSED — candor-java `7c6e204`.** The rule SPLITS ON HOW MANY STRINGS the ctor takes. **ONE** is unambiguously arg 0, so it is now CAPTURED when determined — which also closed an over-mask nobody had filed: `new FileOutputStream("/tmp/log", true)` was previously uncertifiable, a determined path nobody recorded. **TWO OR MORE** cannot be told apart, so the surface is DISCLOSED and no capture is attempted, which is why this cannot introduce a false path. **The first draft of the fix was wrong and the correction is the lesson:** it stayed silent whenever every String operand was a compile-time constant, reasoning that a destination visible in the source is not a masked one. It is, when nobody RECORDS it — `new RandomAccessFile("/etc/passwd", "rw")` beside a benign literal measured EXIT 0, a fully determined destination certified by a sibling. **Visible-to-a-reader is not visible-to-the-gate.** PRICED via `bin/corpus-ab.py` over 119 real jars / 515,029 rows: **71 CHANGED, 3 ADDED, 0 REMOVED**, every change in the disclosure direction, and only ELEVEN a DIRECT call site — each read by name and each a genuine runtime-path open (a `FileHandler` log path, aerospike's metrics writer, commons-io's `RandomAccessFileMode.create`, feign's `appendToFile`), which is exactly the surface `PATH_CTOR_OWNERS`'s own comment says must not be maskable. 3 tests, falsified against a rebuilt pre-fix jar — and confirmed to have actually RUN by reading the JUnit XML, because gradle first reported `BUILD SUCCESSFUL in 261ms`, a pass that executed nothing | `pathArgIsSingleString` rejects `(String,boolean)`, `(String,String)` and `(String,Charset)`, and `FS_LOCATOR_TYPES` excludes `Ljava/lang/String;`, so these constructors enter NEITHER guard: `new FileOutputStream(userPath, true)`, `new RandomAccessFile(userPath, "rw")`, `new FileReader(userPath, UTF_8)`. Measured: a method doing `Files.write(Paths.get("/tmp/benign"), ...)` then `new FileOutputStream(argv[0], true).write(42)` reports `paths:["/tmp/benign"] fs:["write"]` with NO `incomplete`, and `allow Fs /tmp/benign` exits 0. **Identical exit 0 on the 0.36.1 jar, so pre-existing and not introduced by ⟨0.37⟩.** Control: `new File(parent, child).delete()` IS caught, via the File receiver. R409's new `FS_LOCATOR_TYPES` javadoc names this boundary honestly as 'no worse' — but a live AS-EFF-008 evasion in APPEND-MODE file writing belongs in this register, not only in a code comment. That is the standing rule that a documented limitation reads as CONSIDERED, which is what stops it being measured. |
| R422 rust: `str_locals` was never block-scoped, so a nested-block SHADOW published a FALSE destination and dropped the disclosure — and R416 WIDENED it to the dominant path spelling | 2026-09-13 | rust | **CLOSED — candor-rust (this session). CARDINAL SIN: a fabricated destination AND a lost `incomplete`, gate exit 0.** | `pub fn exfil(user: &str, verbose: bool) { let target = Path::new(user); if verbose { let target = Path::new("/tmp/candor-log"); drop(target); } let _ = fs::write(target, b"secret"); }` published `paths:["/tmp/candor-log"]` — a literal from a binding that is OUT OF SCOPE and was dropped — with `incomplete:NONE`, and `allow Fs /tmp/candor-log` exited 0 printing *"nothing hidden"*, over a write whose destination is caller-controlled. **Measured both directions: exit 1 BEFORE R416, exit 0 AFTER, so this session's R416 widened an existing hole to `Path::new(lit)`, the dominant rust path spelling.** The plain-`&str` form was already certified at both commits. Cause: `str_locals` is keyed by BARE NAME and `visit_block` restored `body_macros` and `trait_quals_by_param` but not it; `scoped_binding`/`capture_bindings` already scope BINDER forms (`for`, closures, match arms), so a plain `let` in a nested block was the one spelling with no restore. Fixed by saving and restoring `str_locals` in `visit_block`, which closes the pre-existing plain-`&str` form too. Verified on four spellings (if-block, loop, match arm, plain `&str`) with an unshadowed determined locator still certifying. **THE CORPUS COULD NOT FIND THIS:** 1,554 crates and 286,904 rows show ADDED 0 REMOVED 0 and zero rows losing a marker — the shape is not in the corpus. It took a reviewer told to attack the premise and CONSTRUCT a counter-example, which is a measured limit on 'the A/B is the fabrication gate'. My own docstring asserted the opposite (*'crediting the inner value FABRICATES nothing'*), and the `assert-audit` gate shipped this session flagged four other lines in the same range and not that one. |
| R423 java: `java.nio.file.Path.toUri()` is a filesystem STAT classified PURE — the same defect as `File.toURI()`, one type over, and the sweep that found the first could not see the second | 2026-09-13 | java | **CLOSED — candor-java (this session). CARDINAL SIN in the REFERENCE ENGINE: absence from `functions` is a ⟨0.21⟩ purity claim.** | Measured on JDK 21: `Path.of("/tmp/x").toUri()` is `file:///tmp/x/` when x is an existing DIRECTORY and `file:///tmp/x` when it is not, so the returned value is a filesystem observation. `boolean exists(Path p) { return p.toUri().toString().endsWith("/"); }` is a stat written sideways: the function was ABSENT from `functions` entirely and `deny Fs` exited 0. Control, same shape one type over: the `File.toURI` spelling exits 1 — because THIS SESSION fixed that one. **THE STRUCTURAL CAUSE IS WORTH MORE THAN THE VERB: `java.io.File` is a DENYLIST (whole-owner Fs minus `isPureHandleAccessor`'s exemptions), so auditing it means re-reading every exemption — which is how `File.toURI` was found. `java.nio.file.Path` is an ALLOWLIST, and NOTHING makes anyone re-read what an allowlist OMITS.** The audit was drawn around the owner it was handed, on the very sweep that proved the class existed — CLAUDE.md's standing rule that an audit's boundary must not be drawn around its own trigger, failing at the moment it was most clearly applicable. Priced by review over 325 jars: ADDED 11, REMOVED 0, ground-truthed in `javap` (spring-core `PathResource.getURI`/`getURL`, liquibase-core x5, jetty-util x2, testcontainers, hazelcast). Fixed with its over-charge control: pure `Path` algebra (`resolve`/`getParent`/`normalize`) still certifies. |
| R424 swift: the two-locator `Files` verbs let a literal SOURCE mask a runtime DESTINATION — `s.rename(to: callerString)` certifies under `allow Fs <source>` | 2026-09-13 | swift | **CLOSED — candor-swift `ef7d0f9`.** | FOUND WHILE FIXING R418, and it is the reason that fix is not one line. `move`/`copy`/`rename`/`moveContents`/`create*` have TWO locators: the receiver is the source (or parent folder) and an argument names the rest. Treating them as plain receiver-locators would have published the SOURCE and marked the surface complete, leaving the runtime destination invisible — the same AS-EFF-008 masking the row was opened to close, one locator over, and INTRODUCED BY THE FIX. Measured live pre-fix: `let s = try File(path: "/tmp/benign"); try s.rename(to: dest)` exits 0 under `allow Fs /tmp/benign`. Answered by `recordFilesTwoPath` on the `FS_TWO_PATH_MEMBERS` model: record every locator that resolves, mark Fs INCOMPLETE if ANY does not. **This is the `candor-allowlist-chain` shape again — a fix's own fixture is the boundary of the next defect** — and it was caught only because the fix's test arms were written to cover the WHOLE verb family rather than the verb in hand. |
| R425 swift: a written file's CONTENTS were published as a filesystem PATH — `createFile(named:contents:)`'s payload entered `paths` | 2026-09-13 | swift | **CLOSED — candor-swift `ef7d0f9`.** | A FABRICATION, found by the R418 corpus A/B rather than by a fixture. `JohnSundell/Publish` writes a `.gitignore` with `try folder.createFile(named: ".gitignore").write("""…""")`, and the engine published the whole ignore-file BODY — `'.DS_Store\n/build\n/.build\n/.swiftpm\n/*.xcodeproj\n.publish'` — as a path in `paths`. A positional literal was being read as a locator with no label to say it was one. Fixed by the same change: `recordFilesTwoPath` is label-KEYED, so `contents:` can never be mistaken for a destination. **Note the direction — this is the over-charge mirror of R418 in the same call, and an `allow Fs` list could have been tripped by DATA.** It is also the answer to *"did the fix lose a real path?"*: across the corpus exactly two paths were removed and the other, `.publish`, turned out to be a defect in the first cut of the fix (a label table written from memory instead of from the package's signatures), not a defect in the idea. |
| R426 swift: R418's own two-locator fix made THREE arms of the suite red, and one of my R419 arms was VACUOUS | 2026-09-13 | swift | **CLOSED — candor-swift `c3bc15f`.** | Not a shipped defect — a record of what the fix-on-fix collision cost, because it is the third time this shape has been paid for. (1) R419's call-invalidation broke R418's own over-mask control: `f.delete()` is a CALL on the locator name, and a Files verb acts ON the path without CHANGING it. Fixed by `FILES_NON_MOVING_MEMBERS`, with `move`/`rename` excluded BY OMISSION so the sound default holds for a verb added later. **`mutating` is not the test and reading it as one gets this backwards** — `File` is a struct and NEITHER `move` NOR `rename` is marked `mutating`; they write through a `Storage` CLASS the value references. Determined from the package source, not the declaration keyword. (2) R418's two-locator arms used `rename`, which R419 then made red for a SECOND reason — switched to `copy`, which is two-locator and does not move the receiver, so the arm isolates masking again. (3) **One R419 arm was vacuous:** the rename test PASSED against the pre-R419 engine because R418's two-path rule reddened it first. Rewritten with a DETERMINED destination so the write that follows is the only thing that can fail it. The lesson is the standing one — `candor-allowlist-chain` — a fix's own fixture is the boundary of the next defect, and a test that passes on both sides of a change is measuring something else. |
| R427 gate-run: a missing GraalVM made candor-java's gate list permanently NOT GREEN, with no defect in the repo | 2026-09-13 | tooling | **CLOSED — candor `09e854e`.** | `./gradlew nativeCompile` FAILED on a developer box because GraalVM's `native-image` is not installed. The pre-run skip check inspects only the LEADING word, so `./gradlew` is a path the repo owns and RUNS — then fails inside on the absent toolchain. Nine gates green, one red, verdict NOT GREEN, nothing wrong with candor-java. **This is the exact sibling of the `${{ }}` arm already in that file**, which exists because an unexpandable GitHub expression *"made candor-java permanently red for a reason that is not a defect in candor-java"* — a missing toolchain is the same thing by another spelling, and only one of the two was handled. Matched on the TOOL'S OWN sentence, not on the rc, and deliberately NOT generalised to *rc=1 plus 'not found'*: the file's own design note says a gate that RUNS and then hits a missing tool has really failed and rc cannot tell those apart, which is still right. Attributed as the header demands rather than assumed — reproduced serially on a REVERTED tree — and CALIBRATED BOTH WAYS: a synthetic genuine `nativeCompile` failure still reports FAIL. Counted as UNRUN, never passed; the verdict stays INCOMPLETE. |
| R428 ci-watch: the earlier-commit red guard was UNREACHABLE from the one path it was written for — the summary printed `no run expected ✔` over a verdict it never read | 2026-09-13 | tooling | **CLOSED — candor `f3a415a`.** | ci-watch already carried a check for *"a failure on an earlier commit is not erased by a quiet one on top"*, and its comment names the exact case: *"candor's row read 'no run expected' right after a push that did change `bin/`, because HEAD was a CHANGELOG-only commit on top of it. That run happened to have passed. **Nothing would have said so if it had not.**"* The check sat inline at the END of the per-repo body, and the `[ -z "$rows" ]` branch above it — the branch that PRINTS "no run expected" — ends in `continue`. So the guard existed, was correct, and **could not fire in the scenario its own comment describes.** **FOUND BY THE TOOL'S OWN FAULT INJECTION, not by reading it:** `CI_WATCH_FAULT=stale-red` recolours every completed non-HEAD run as a failure, and against the umbrella it still printed `✔ no run expected` and `ci-watch: OK`. **A guard that cannot be made to fire is not a guard** — and this is the first time a fault hook here has caught its own arm being UNREACHABLE rather than wrong, which is a different defect class from the eight false greens above it. Fixed by extracting to `report_earlier_reds()` and calling it on BOTH paths; no logic changed, only whether it runs. Calibrated both ways, and the recipe is in the function header because it needs the live API and cannot live in `--selftest`. **Live the same day:** the umbrella's `shell-lint`/`release-scripts`/`integrations` all ran on `09e854e` and passed, a docs-only commit landed on top, and ci-watch reported the repo green without reading them. |
| R429 swift: a `#if`-duplicated `typealias` BINDING is resolved by SOURCE ORDER — the losing arm's effect is dropped and a scoped `deny Fs` is decided by which arm was written first | 2026-09-13 | swift | **CLOSED 2026-09-14, candor-swift `dac7a01` — the SECOND close, and the first one's failure is the lesson.** The first fix put the union edge INSIDE the typed-local-receiver branch, entered only when the dealiased root is a project type; for a MIXED set (project enum beside a FRAMEWORK type — what real code writes) the picked root is the framework one in one arm order, the branch is never entered, and the project arm is dropped. `deny Env go` answered exit 1 and exit 0 on the same program written both ways. **Order-dependence is this row's OWN signature and it survived the row, because the closing fixture had TWO PROJECT ARMS** — it sat entirely inside the narrowing that hides the defect. Now: the union runs BEFORE the dispatch chain, over EVERY arm INCLUDING the picked one (skipping it was a THIRD order-dependence — no branch handles an unresolvable framework arm, so the set answered `["Env"]` when that arm was picked and `["Env","Unknown"]` when it was not, caught only by asserting the two orders are IDENTICAL rather than that the same gate fires), and an arm the engine could not read marks the caller's SURFACE incomplete (an `Unknown` alone does not stop `allow Fs <lit>` — the Fs surface still reads COMPLETE, so the readable arm certifies for the unreadable one). Bounded to sets WITH a project-declared arm: an ALL-FRAMEWORK alias (`NSColor`/`UIColor`) is untouched, with a fixture saying so, because the unresolvable-arm branch would otherwise put a new `Unknown` on the commonest shape in the ecosystem. **EVIDENCE WITH ITS LIMIT: PART 89 is green on all nine arms both engines and its xfail list is empty, but the corpus A/B over 878 real Swift files (two real apps, swift-syntax, candor's own sources, 9,258 rows) measured REACH 0** — those trees contain no conditional typealias at all — so `corpus-ab` refused the 0/0/0 as evidence and the blast radius on code that HAS the shape is bounded by construction and fixtures, not measured. Weaker than R438's basis; stated rather than smoothed. Earlier state: **REOPENED 2026-09-14 — the fix closed the ALL-PROJECT-DECLARED case and left the MIXED arm set picking. `CLOSED` was wrong.** | **CLOSED ON THE SECOND CLOSE — candor-swift `dac7a01`, 2026-09-14. Originally: REOPENED BY THE PRE-RELEASE REVIEW, and the narrowing that closed it is what hid the rest.** The fix made only arms in `declaredTypes` eligible — a corpus-driven narrowing that killed a 29-row over-charge — so an arm set MIXING a project-declared type with a FRAMEWORK one falls back to the single-valued last-writer-wins this row was filed against. **COORDINATOR-VERIFIED at HEAD on a forced clean rebuild**, one variable (which `#if` arm the project type sits in): `typealias Impl = FileManager \| EnvBox` → `deny Env probe` exit 1; arms swapped → **exit 0 with `probe` ABSENT, reported PURE**, over a real `setenv`. **AND PART 89 CANNOT SEE IT: all six of its swift arms are project-declared enums, so the part sits entirely inside the narrowing.** A green there is evidence about the narrowed case only — which is the ⟨0.34⟩ failure one level up: prose citing a rung that nothing pins for the shape that matters. This row's own text already named `Color = NSColor \| UIColor` as a real instance of the mixed shape and I closed it anyway. FOUND BY CARRYING OUT THE 2026-09-12 UNION RULING, not by a hunt: the ruling scopes to the two engines with conditional imports (rust `#[cfg]`, swift `#if`), and probing swift's binding spelling to decide whether the conformance PART could express it measured this instead. `DeclCollector.swift:650` is `typeAliases[node.name.text] = underlying` — a plain map assignment — and `Driver.swift:851` merges them **last-writer-wins**, which its own comment says. A `#if`/`#else` pair declaring one alias name over two different types therefore keeps only the arm written LAST. **MEASURED on the shipped 0.37.0 binary, two programs identical but for the ORDER of the arms** (`typealias Impl = FsImpl` / `= EnvImpl` under `#if os(macOS)`, called as `Impl.act("/tmp/benign","x")`): Fs-arm-first reports `inferred:["Env"]`, Env-arm-first reports `inferred:["Fs"]`. The losing arm is not hedged, not disclosed — it is GONE, with `unknownWhy` absent and `incomplete` absent. **THE GATE CONSEQUENCE, and it needed a SCOPED rule to see:** a bare `deny Fs` fires on BOTH orders, because the helper `FsImpl.act` carries `Fs` on its own and the deny lands there — so the bare form hides this. Scoped to the function, with the prefix-collision check done first (`go` is the only name with that prefix), **`deny Fs go` exits 0 on the Fs-arm-first program and 1 on the other.** A program that writes a file passes a scoped deny because of where its `#else` sits. This is R105's defect in a second engine, on the route the ruling was about, and SPEC ⟨0.36⟩'s own text already calls it out: *"resolution by SOURCE ORDER was and remains the cardinal sin under ⟨0.21⟩, independent of this clause."* Fix direction: the same one R105 took in rust — keep EVERY arm, distribute at the call site, UNION when the arms' effects agree or differ, and withhold the surface. Never pick by position. |
| R430 ~~rust: a `Box<dyn Trait>` STRUCT FIELD reads pure while the identical wrapper in a PARAMETER charges~~ **WITHDRAWN — NOT A DEFECT. The engine is correct; the measurement was wrong.** | 2026-09-14 | rust | **WITHDRAWN 2026-09-14, same day it was filed. NO ENGINE CHANGE — there was nothing to fix.** | **THE ROW WAS AN ARTIFACT OF MY OWN MATCHER.** The report keys functions as `FBox::run`; my extraction script asked for names ending `FBox.run` — a DOT where the qual uses `::` — so every field-route row read ABSENT and I filed the absence as a silent under-report. Re-measured by printing EVERY row the report carries instead of probing for names I expected: `FBox::run ['Exec']`, `FBoxC::run ['Exec']`, `Registry::field_for ['Fs']`, `Registry::field_foreach ['Fs']`. The field route was working the whole time. **THE THING THAT CAUGHT IT is the in-tree test `collection_of_trait_objects_iteration_dispatches`, which PASSES on source my CLI run said was silent.** A test and a CLI disagreeing about identical bytes is either a real harness/product divergence or a broken measurement, and it has to be resolved before either is believed — here it was the measurement. **Kept rather than deleted, with its reason, because a withdrawn row deleted without one is a row somebody re-files.** Two durable lessons: a probe that asks *"is the name I expect present?"* fails SILENTLY when the naming convention differs, where printing the whole set cannot — this is [[candor-oracle-disclosure-recall]]'s rule (*calibrate the instrument, never a copy of it*) applied to a one-off script; and the pattern that would have caught it immediately is the one this register already demands of engines, a CONTROL: `p_box` charged in the same scan, so the scan was not broken and the difference was in how I ASKED. The fix-order note it created (*R430 before R401*) is VOID — R401's fix has no such prerequisite. |
| R431 swift: a table HIT loses const resolution that the FALLBACK performs — a DECLARED locator is weaker than an undeclared one | 2026-09-14 | swift | **CLOSED — candor-swift `8cd5bba`.** | **CLOSED by using `resolvedForLabel` for a table hit** — literal FIRST, then the resolver, at the DECLARED label only, so the table's whole purpose (never reach a sibling) is preserved and runtime locators still fail closed on both sides. **CORPUS: CHANGED 0 / ADDED 0 / REMOVED 0 over 17,944 units — with REACH MEASURED, because an empty diff is indistinguishable from a change nothing reached.** The probe counts table HITS and, separately, GAINS (old picker nil, new one resolves — the behaviour change itself, not a proxy): **13 hits across 14 real Swift projects, ALL `shellOut`, and 0 GAINS.** Every real call passes its command as a direct literal, so both pickers agree and the empty diff has a reason. **The review that found this estimated ~35 affected call sites; the measured figure for THIS change is 13, and the difference is not an error in either.** That estimate counted `File(path`/`Folder(path`/`FileHandle(for` too — names OUTSIDE the table, which take the fallback and are untouched by this commit. They are what the TOTAL table of R415 would touch, which is the point: this fix is what makes that one affordable. The free-call site applies `literalForLabel` to a table hit but falls back to `firstStringLiteral`, and only the latter runs `resolveConstString` on a non-literal argument. So a name IN the table loses a const-bound locator that a name OUTSIDE it resolves — the declared path is the weaker one. Measured, one variable (covered vs uncovered, same `let` spelling): `let p = "/tmp/x.txt"; fopen(p, "r")` → `incomplete:[Fs]` while `let p = …; FileHandle(forReadingAtPath: p)` → `paths:["/tmp/x.txt"]`; same split for `getaddrinfo` vs `NWConnection` and for `shellOut` vs `posix_spawn`. **`resolvedForLabel` ALREADY EXISTS** (literal first, then the resolver at that label) and is used by the two-path Fs site; the free-call site never adopted it. **This blocks R415's real fix:** making the table TOTAL on top of `literalForLabel` would extend this loss to every establishing name — counted in the r418 corpus at `shellOut(` 20, `File(path` 7, `Folder(path` 6, `FileHandle(for` 2 non-test call sites. Fix first, then make the table total. Found by a plan review of R415, not by a hunt. |
| R432 swift: `NWListener(using:on:)` publishes its PORT as a HOST — the ⟨0.29⟩ listen rule violated on the free spelling | 2026-09-14 | swift | **FALSE DISCLOSURE (report-level). OPEN.** | `NWEndpoint.Port` is `ExpressibleByStringLiteral`, so `NWListener(using: .tcp, on: "8080")` hands the whole-argument fallback a top-level string and it enters `hosts` as `["8080"]`, complete. SPEC ⟨0.29⟩'s rule that a listen address must never enter `hosts` is cited in this engine's own source (`Classifier.swift:1414`) and is violated by the one FREE-CALL spelling of listen. The INT spelling `on: 8080` reports `incomplete:[Net]`, which is the right answer for both. Report-level — no certification could be built from it — but it names a host that does not exist. `NWListener` belongs in the OPAQUE arm of R415's schema; **and that arm must DERIVE from `isOpaqueLocatorFree` rather than copy it**, or it is two lists for one question (R347). Found by a plan review of R415. **CLOSED 2026-09-15, candor-swift `3eca7f5`**, in the same change as [[R415]]. `NWListener` added to `isOpaqueLocatorFree`, and the locator table's `.opaque` arm **DERIVES** from that authority rather than copying it — copying an authority is how a 14-function carve-out was introduced in this family before. |
| R433 java: the String-locator tail is closed for `PATH_CTOR_OWNERS` and WIDE OPEN for every other Fs owner — `new PrintWriter(userPath)` is charged `Fs` and enters NO masking guard | 2026-09-14 | java | **CARDINAL SIN (silent under-report / gate bypass), LIVE at HEAD and PRE-EXISTING.** | FOUND BY THE PRE-RELEASE REVIEW OF R421, whose boundary was drawn around `PATH_CTOR_OWNERS` (`Rules.java:206`). The classifier charges `Fs` on other owners whose locator is also a leading `String`, and none reaches `pathArgIsSingleString` or `FS_LOCATOR_TYPES`: `PrintWriter`/`PrintStream`/`Formatter` (`Classifier.java:244`), `ZipFile`/`JarFile` (`:103`), `getResourceAsStream`/`ResourceBundle.getBundle` (`:218`). **COORDINATOR-VERIFIED on the shipped build:** `new PrintWriter(argv[0])` reports `['Fs']` with `incomplete` ABSENT — a caller-chosen destination whose invisibility is not disclosed — and beside a benign literal `allow Fs /tmp/benign` prints *no violations*. **THE ASYMMETRY IS THE BUG, stated in one line:** `new Formatter(new File(argv[0]))` exits 1 and `new Formatter(argv[0])` exits 0 — same destination, same call, two verdicts, because `FS_LOCATOR_TYPES` excludes `String`. The review reports the same shape on **Exec**: `System.load(argv[0])` (`Classifier.java:471`) masked by a benign `ProcessBuilder("git",…)` literal — loading a caller-chosen native library. **AND THE STALE COMMENT IS EXACT:** `Candor.java:8112` justifies excluding String by saying it leaves the tail *"(`new RandomAccessFile(runtimePath, "rw")`, `getResourceAsStream(runtimeName)`) exactly where it was — no worse"*. R421 closed the FIRST example in that parenthesis and cited that very sentence as its lesson, and left the SECOND live with the comment now reading *still fine*. Fix: derive the boundary from `Classifier` (ask the authority), never a second hand-list beside `PATH_CTOR_OWNERS`. **PARTIALLY CLOSED — candor-java `18fbe94`, published in `v0.38.3`; the residual is NAMED in that commit's CHANGELOG entry and was never carried back to this row.** Outstanding: `Class.getResourceAsStream(String)`, an INSTANCE call. And a second gap the fix cannot have covered, because **`18fbe94` derives its boundary from `effect == Effect.FS`** — the Exec sibling this row names (`System.load(argv[0])` masked by a benign `new ProcessBuilder("git")` literal) is outside that derivation and is unmeasured on HEAD. That is the audit-boundary trap: a fix scoped to the effect it was handed. |
| R434 java: R421's new one-String CAPTURE converts a fail-closed verdict into exit 0 — a fix introducing the class it closed | 2026-09-14 | java | **GATE BYPASS, INTRODUCED BY R421 (candor-java `7c6e204`), NOT YET PUBLISHED.** | R421 made a single-String path ctor CAPTURE its literal when determined. That is right in isolation and it is a new source of benign `paths` entries — so a function whose Fs surface used to be EMPTY (empty ⇒ uncertifiable ⇒ fail-closed) can now carry a literal that satisfies an allowlist while an UN-GUARDED sibling (R433) stays invisible. **COORDINATOR-VERIFIED, one variable, same fixture on both engines:** `{ new FileOutputStream("/tmp/benign/log", true); new PrintWriter(argv[0]); }` under `allow Fs /tmp/benign` → PRE `a10e2ac` `paths:None incomplete:None` **exit 1**; POST `7c6e204` `paths:['/tmp/benign/log'] incomplete:None` **exit 0**. **This is the shape this register keeps recording — over half of one round's findings were fixes reintroducing their own class — and it is mine, found before publication rather than after.** THE REMEDY IS R433, NOT A REVERT: the capture is correct; the exposure is that R433 is open. Closing R433 closes this. **RELEASE-RELEVANT: the ⟨0.38⟩ cut must not ship until R433 is closed**, because the cut would publish a gate that answers *no violations* over a caller-chosen file write in a shape 0.37.0 failed closed on. **PUBLISHED 2026-09-16 in candor-java `v0.38.3`** — this row read "NOT YET PUBLISHED" until 2026-09-17, which was true when written and expired silently. Verified by ancestry, not by memory: `git merge-base --is-ancestor 7c6e204 v0.38.3` succeeds. **A status that ages into a lie is the shape [[R445]] and [[R449]] record**, and this one cost a night's planning: it ranked a shipped fix as outstanding work. |
| R435 java: a BOLDED safety assertion in shipped code is false — *"THIS ADDS NO CAPTURE, ONLY DISCLOSURE"* twelve lines above the capture | 2026-09-14 | java | **FALSE SAFETY ASSERTION (doc); the code is correct, the sentence is not.** | `Candor.java:5240` reads *"THIS ADDS NO CAPTURE, ONLY DISCLOSURE, and that asymmetry is the whole design"* and `:5243` *"`pathArgIsSingleString` remains the sole authority over what enters `paths`"*. Both are contradicted by `:5278`, which writes into `pathsDirect` on a descriptor `pathArgIsSingleString` REJECTS. The commit MESSAGE carries the correct qualifier (*"adds no capture IN THE AMBIGUOUS CASE"*); the in-code comment drops it, and the in-code comment is what the next reader believes. **R434 is the exact consequence of that sentence being wrong.** This is `feedback-documented-limitation-is-not-measured`'s louder variant — a comment asserting SAFETY, 4/4 engines wrong on 2026-08-31 — recurring in the same session that cited it. **AND THE TOOL BUILT FOR THIS WAS BLIND:** `bin/assert-audit.sh` returned *"no safety assertions added — nothing to defend"* on this range; its `ASSERT_RE` (`:76`) has no pattern for *adds no X*, *cannot introduce*, or *sole authority*. Fix both the sentence and the regex. CLOSED IN-TREE at candor-java fd4199c on 2026-09-14 — the ROW was never updated, which is why it still read as open six days later and why I dispatched an agent onto it. The line numbers had moved (5240 to 5390), the bolded sentence had gained its qualifier, and assert-audit.sh's ASSERT_RE had already been widened; all four R435 spellings now match. IMPORTANT CAVEAT FOR soundness-status.py: that tool measures ROW HYGIENE, not reality — it correctly reported that this row claimed nothing, and the defect was fixed. An open count derived from it is an upper bound. The agent verified the CURRENT assertion by execution rather than accepting the fix's word, which is how R516 was found. |
| R436 swift: `URL.append(path:)` was exempted by an inert-call set BORROWED from another type family — `allow Fs` exits 0 over a caller-controlled path | 2026-09-14 | swift | **CLOSED — candor-swift `5ef4be9`.** | R419's allowlist `.union`ed `FILES_NON_MOVING_MEMBERS` into the URL set so a `File` binder's `f.delete()` would not withdraw its locator. Both families declare **`append`** and it means opposite things: appending to a file's CONTENTS moves nothing; `URL.append(path:)` is the modern mutating path mover. MEASURED, one variable against `appendPathComponent`: `paths:["/tmp/benign"]`, `incomplete` NONE, **`allow Fs /tmp/benign` EXIT 0** — and R419's own probe printed `R419CONSULT u calls=["append"]` with no REFUSE, so the rule RAN and ruled it inert. **THE COMMENT THIRTEEN LINES ABOVE THE UNION LISTS `append(path:)` AMONG THE MOVERS** — not an oversight about an unknown spelling but a set contradicting the sentence justifying it. **The fix LICENSED the bypass:** without the union, `append` on a URL would have been unknown and failed CLOSED, so R419 converted *not yet considered* into *considered and ruled safe*, the state that stops a thing being measured again. Fixed by choosing the inert set from the BINDER'S KIND, not by subtracting the colliding name — a subtraction is a hand-list that must stay complete as either family grows. |
| R437 instrument: the R408 fix turned a CAUGHT stale build-constant into a GREEN — `pin_version` returned empty for both ABSENT and REFUSED | 2026-09-14 | instrument (`candor/bin/release-preflight.sh`, `_release_set.sh`) | **CLOSED — candor `7fc173b`-series.** | `grabver` is `[ -n "$v" ] && { …collect… }` — right for an absent key, catastrophic for a refusal: the repo dropped silently out of the build-version set and the agreement check then printed **"✔ all self-declared build versions agree"** over a repo it had never read. Its three sibling callers all happened to check loudly; this one did not. MEASURED on a COPY of `candor-query/Cargo.toml` with the package version stale and the path-deps current — the exact shape check [4] exists to catch — where the PRE-R408 `head -1` had answered with a hard `✘`. **A false GREEN in the gate that authorises the release, introduced by the fix for a false green one check over.** Fixed in the API rather than the caller: refusal returns **2**, absence returns **1**, so the distinction lives in an exit code a caller cannot spend by accident. Two siblings from the same review, both mine: `CI_WATCH_FAULT` leaked into the R403 selftest subshells (an EXPORTED fault variable — which the file's own recipe tells you to set — took umbrella gate 14 red for nothing), and the `seen_wf` dedup was asserted by nothing because every case fed one row; 8 single-line mutations found it, and the added two-row case is mutation-proven. |
| R438 rust: a MIXED `#[cfg]` alias arm set PICKS the external arm and PUBLISHES its literal — `allow Fs <lit>` certifies a program that does something else in its other configuration | 2026-09-14 | rust | **CLOSED 2026-09-14, candor-rust `7ffba33`. PART 89 b7mixed/b8mixedrev/b9mixedallow all green and their xfails retired — by the part reporting `XFAIL ARM PASSED`, which is how I learned it rather than by re-reading the rows.** The local arm of a genuinely MIXED set is recorded into the alias map so R105's adjudication fires, and each unclassifiable arm becomes a call EDGE (the classifier cannot name a local arm, so an edge is the only thing that stays correct when that function is edited); a `#[cfg]`-gated DEFINITION counts as an arm too. 1,556-crate A/B: **ADDED 6, REMOVED 0, CHANGED 158**, reach 108 across 39 crates. **THREE CUTS LOST ROWS AND THE A/B FOUND ALL THREE — the reusable part of this row:** (1) gating on "declared twice under `#[cfg]`" swept in the ALL-LOCAL set, whose arms all classify `None`, so the branch charged nothing and `continue`d past the ordinary handling — **8 rows DISAPPEARED**, `openssl-sys` `try_pkg_config` losing `["Env","Exec","Fs"]`, i.e. the fix introduced the class it was closing; (2) a cfg-gated DEFINITION lives in NEITHER map, so joining only the `use` arms suppressed the resolution that used to find it and simdutf8's four `from_utf8` entry points went from `Unknown`+`unresolved` to ABSENT; (3) hedging the charge-nothing state to `Unknown` added 259 rows, then 47 after narrowing, on PURE arithmetic where the "arm set" is two `#[cfg(target_pointer_width)]` TYPE aliases — and prevented nothing once (2) was fixed. **The hedge was replaced by an INSTRUMENT** (`CANDOR_R438_SILENT`), because "this does not happen" needs a counter and not a comment — and it immediately showed the state occurring **960 times over 1,556 crates**, 53 of them undisclosed by the κ ledger, all sampled ones pure-type arm sets. Pre-existing (REMOVED 0); see R440. Original evidence: **GATE BYPASS (AS-EFF-008). Not order-dependent — the external arm wins BOTH ways.** | The UNION landed for arm sets the classifier can type; an arm naming a LOCAL target (`crate::…`) classifies to `None` and is dropped, so a set mixing `std::fs::write` with a `crate::` target resolves to the std arm alone. **PANEL-MEASURED, held constant (call site, local fn, policy; varied only whether the sibling arm is `std::` or `crate::`):** two LOCAL arms → `inferred` carrying BOTH arms' effects with both `calls` present, `deny Env probe` **1**; MIXED → `inferred:[Fs] paths:["/tmp/benign"]`, no `calls`, no `incomplete`, `deny Env probe` **0** in BOTH orders. So it is a PICK that also CERTIFIES — worse than a `deny` miss, because `allow Fs /tmp/benign` passes a program that sets an environment variable in its other configuration. **PREVALENCE PROXY: 1,173 of 3,052 registry files with ≥2 cfg-gated `use` items mix a local root with an external one — the ordinary portability shim, not a corner.** **This is R287's own recorded residual** (*"a LOCAL arm gets no call edge through the alias route at all"*), whose remedy column prescribed three fixtures; PART 89 shipped one. Fix direction: the local arm must become a CALL EDGE as on the definition route, not a `classify()` table lookup — and the absent `incomplete` says the mixed case never reaches that branch at all, so there is a second site upstream in path collection. |
| R439 ts: a `package.json` subpath-`imports` CONDITION MAP is a mutually-exclusive arm set, and candor-ts resolves ONE condition and drops the other with no disclosure — the call edge to the unpicked arm is absent, so a scoped `deny Fs <fn>` exits 0 over a real write | 2026-09-14 | ts | **CLOSED 2026-09-15 (candor-ts `60c578f`, shipped in v0.38.2) — the detail is at the END of this cell. As FILED it was: SILENT UNDER-REPORT (⟨0.21⟩), LIVE at HEAD, and it is ⟨0.38⟩'s shape in the one engine SPEC §4 declared OUT OF SCOPE.** Measured 2026-09-14 by differential: two trees identical but for which condition NAME carries which file — `{"node": pure, "browser": fsarm}` vs `{"browser": pure, "node": fsarm}` — answer `deny Fs src.main.caller` **rc 1 and rc 0**. Both arms are inside the scan root and BOTH ARE ANALYZED (`analyzed.count` 6, and `src.fsarm.go` appears in `functions[]` carrying `Fs` + `paths:['/tmp/condmap-victim.txt']`); what is dropped is the CALL EDGE from `caller`, which therefore never appears. `unanalyzed` is `null`, `incompleteSurfaces` is `null`, the run exits 0 and prints `no violations` | tsc is configured `moduleResolution: NodeNext` (`scan.mjs:1393`), so TypeScript's own resolver picks the condition set for the configured mode and candor-ts never sees the others. **The pick is made by the SCANNER's configuration, not by anything written in the tree** — which is worse than ⟨0.21⟩'s source-order pick, not better: source order is at least visible to a reader of the file. ⟨0.38⟩'s own three legs hold verbatim — exactly one arm is built, every target is in the source, the engine holds the complete target set — so dropping is a purity claim. **A BARE `deny Fs` CANNOT SEE THIS** and that is why it survived: `src.fsarm.go` is present with `Fs`, so the unscoped rule fires either way; only the function-level attribution is wrong. Same discriminator as R429 — use a SCOPED rule, and check prefix collisions first **THE PLAN.** **NOT a ⟨0.38⟩ non-conformance** — PART 89 declares ts an exclusion and the clause binds source-level arms. It IS a ⟨0.21⟩ one. Two halves, and they are not the same size: **(a)** the DISCLOSURE half is small and unblocking — a condition map with more than one arm is a surface the engine can see in `package.json` while resolving, so the unpicked arms belong in `unanalyzed` (the ⟨0.21⟩ manifest) and the caller's surface marked incomplete; that alone turns rc 0 into a disclosed rc 2 and kills the silent case. **(b)** the UNION half is a ⟨0.39⟩ question, because charging the unpicked arm's effects to the caller is a four-way behaviour change and ts is the only engine whose arms are selected by a resolver rather than written in the source. **Do (a) before any cut that claims ⟨0.38⟩ closes the conditional-arm family; do NOT do (b) inside this rung.** Fixture owed on both: the differential above, and the over-charge control that a SINGLE-arm condition map (the overwhelmingly common shape) still resolves and still certifies off its literal — that control is what stopped ⟨0.38⟩'s own surface clause from being written too strong **RE-MEASURED 2026-09-15, AND MY OWN REMEDY WAS WRONG.** I filed this planning to disclose via `unanalyzed`. That cannot work: **both arm files ARE analyzed.** On the fixture, `src.fsarm.go` appears in `functions[]` carrying `['Fs']` with `analyzed.count` 6 — nothing is unanalyzed, so the ⟨0.21⟩ manifest is the wrong vehicle. **The defect is purely a MISSING CALL EDGE** from `src.main.caller` to the unpicked arm, which makes this R429/R438's shape in a THIRD engine rather than a coverage gap. Two consequences that change the fix: a BARE `deny Fs` FIRES (the arm function carries Fs on its own, so the unscoped rule lands there) and only the FUNCTION-level attribution is wrong — same discriminator as R429, use a SCOPED rule; and the caller is ABSENT from `functions[]` entirely in the silent arm order, which under ⟨0.21⟩ is an affirmative purity claim rather than a missing disclosure. **The remedy is therefore one of: (a) the call EDGE to each arm (the union half, a ⟨0.39⟩ question because it changes what the report SAYS across engines), or (b) `Unknown` + `unknownWhy: ambiguous:condition-map <spec>` on the caller, which removes the purity claim without claiming the other arm's effects.** (b) is the disclosure half and is the smaller change. **NOT ATTEMPTED: the hook is the call-edge construction in `scan.mjs`, which I did not locate cleanly, and R438's three row-losing cuts are what a hurried engine change in this family costs.** `importPkgOfHead` (scan.mjs ~2769) already walks an ImportSpecifier to its module specifier and is the likely place to detect a multi-condition subpath import **CLOSED 2026-09-15, candor-ts `60c578f`** — by remedy (b), the DISCLOSURE half, exactly as scoped above. At a call whose head binds to a subpath-`imports` specifier whose condition object resolves to ≥2 DISTINCT files the scan ANALYSES, the caller now takes `Unknown` + `unknownWhy: ["ambiguous:condition-map #impl"]`. Both arm orders then report the SAME thing and both exit 2 (⟨0.30⟩ fail-closed) where they previously disagreed rc 0 / rc 1 — AGREEMENT is the property under test, since which condition wins belongs to the consumer's build and not to the code. Remedy (a), the UNION half, is deliberately NOT done: it is a ⟨0.39⟩ question and unioning in ONE engine ahead of the spec would charge a caller for a body that may never run. **THE BOUNDARY, STATED AND MEASURED RATHER THAN DRAWN AROUND THE TRIGGER.** Flagged only when the arms land on analysed project files: arms leaving the project are external calls the κ ledger already carries, and the common dual `{"import":"./dist/x.mjs","require":"./dist/x.cjs"}` is build output outside `include`, so it stays quiet — one source compiled twice. **The sibling route was swept, not assumed.** chalk is a REAL instance of the shape (`#supports-color` → `{node: index.js, browser: browser.js}`, node arm 3 env/tty/os refs, browser arm 0) and its binding is READ (`const {stdout} = supportsColor`), never called, so the call-site hook cannot see it. Measured with a CONTROL whose only variable is the condition map: `src.main.<module>` is ABSENT from `functions[]` when the Env-carrying module is reached through a condition map **and equally ABSENT when it is imported DIRECTLY**. So module-init effects do not propagate to importers at all in candor-ts — uniform, pre-existing, and unchanged by condition maps, therefore not this row and not a hole this fix leaves. (Whether an importer should inherit its imports' module-init effects is a separate open question; chalk's `source.index.<module>` shows module→module EDGES do exist, so it is worth its own row if anyone wants it.) **VERIFICATION.** 7 new fixtures in `test.mjs`, CALIBRATED — with the hook neutered 4 go RED (3 positive + the mirror) while both precision controls stay GREEN, so the arms discriminate in the direction the defect lives. Full ts suite rc=0 (2796 + 223 mcp + 129 lsp + 16 watch + 25 fuzz, 0 failed). Four-way `conformance/run.sh` rc=0 on clean trees. `bin/corpus-ab.py` over 7 real TS repos (axios chalk execa got hono zod zx, 550 rows): ADDED 0 REMOVED 0 CHANGED 0 on every key including the wide value — and **REACH was 0, which the tool REFUSED as safety-only rather than letting it read as a clean result**; recorded here per §E1 instead of waved through, because the corpus holds no multi-arm condition map over analysed files. So the A/B proves no over-firing; the fixtures and the chalk probe prove it fires. The diff is purely ADDITIVE (217 insertions, 0 deletions), so no effect the existing resolution finds is displaced and no firing gate can go green. **The over-charge control that matters:** chalk's `#ansi-styles` is a plain STRING (one arm) and does NOT fire — that is what stops the rule degenerating into "any `#` import is ambiguous", which would put `Unknown` on most well-typed packages. |
| R440 rust: a `#[cfg]` alias arm set that classifies to NOTHING and resolves to NOTHING leaves the call with no trace — no charge, no edge, no disclosure — because R105's branch `continue`s past all ordinary handling | 2026-09-14 | rust | **PRE-EXISTING, found by an INSTRUMENT rather than by a diff, and measured rather than argued.** While fixing R438 I wanted to claim this state could not happen. It can: `CANDOR_R438_SILENT` counts it at **960 occurrences across 54 of 1,556 crates**, of which **53 are UNDISCLOSED** — the κ ledger records a dep-crate arm in `invisible`, which covers the other 907, but an arm whose head is not a declared dependency (`alt.contains("::") && deps.contains(acr)` is the ledger's own condition) reaches no disclosure by any route. Not introduced by R438: that fix's A/B reports REMOVED 0 | Every sampled undisclosed occurrence is a PURE-type arm set — `core::sync::atomic::AtomicUsize::new` \| `loom::sync::atomic::AtomicUsize::new`, `net::addr::SocketAddrV4::octets` \| `std::net::SocketAddrV4::octets`, `HeapVec::get_mut` \| `crate::stackvec::StackVec::get_mut`. So the silence is over calls that reach nothing, and the under-report is of NOTHING — which is why it has sat here unnoticed. **That is an argument about the sample, not about the class**, and the class is the one this register exists for: a call site that leaves no trace cannot be distinguished from a pure one by any consumer. **THE FIX IS NOT A HEDGE, AND THAT IS MEASURED TOO** — R438's first cut hedged this state to `Unknown` and added 259 rows, then 47 after narrowing, all on pure arithmetic where the arms are `#[cfg(target_pointer_width)]` TYPE aliases. The right shape is what closed the simdutf8 half of R438: find the arm route that is not being recorded and record it AT ITS CAUSE. Owed: enumerate what the 53 actually are (type aliases? dev-dependency shims like `loom` that are absent from `deps`?) and decide per route. `loom` is the likely first answer — a dev-dependency IS a declared dependency for this purpose, and teaching the κ ledger that would disclose a chunk of these for free **CLOSED 2026-09-15, candor-rust `6c81620` — AND THIS ROW'S OWN CENTRAL CLAIM WAS FALSE.** The row said every sampled undisclosed occurrence was a pure-type arm set, i.e. *"the under-report is of NOTHING"*, and a product review repeated it. **A full census of ALL 62 undisclosed occurrences — not a sample — found mio's `Waker::wake`**, `pub use`d as `mio::Waker`, ABSENT over two `WakerInternal::wake` bodies that each write an fd and each carry `Fs`. The earlier sample missed it because a PORTABILITY TWIN (`unix::X` / `windows::X`) is precisely the shape a tail-keyed index cannot see — so the sample was biased by the same defect it was sampling. **A SAMPLE IS NOT A CENSUS, and this row is the price of writing one up as though it were.** A/B: ADDED 2 · REMOVED 0 · CHANGED 2, reach 12 / 4 crates. **THE `loom` LEAD IS MEASURED NOT WORTH TAKING** — it is 14 of the 58 remaining occurrences, all pure atomics and `Arc` ctors in a test-only config, and taking it would contradict the documented runtime-vs-harness split for zero disclosure value. Of the 58 that remain, the number whose arms reach ANY effectful local row is **zero** — now a census, not a sample. |
| R441 umbrella: `ci-watch.sh` printed `OK — every workflow enumerated at every HEAD concluded success` over a repo it NEVER CHECKED — `SKIP (not checked out)` printed a row, ran `continue`, and touched `rc` | 2026-09-15 | umbrella | **FALSE GREEN IN THE INSTRUMENT THAT AUTHORISES RELEASES, and it is the one script whose entire thesis is that the summary must never be greener than its rows.** MEASURED: six of the seven repos symlinked into a scratch ROOT with candor-swift left out produced exactly that line at **exit 0**, with candor-swift never checked at all. Not a hypothetical — `anya.local` is a second machine with the same layout, a fresh box clones repos one at a time, and a moved or renamed directory is indistinguishable from an absent one. **CLOSED same day, candor `05e156a`**, calibrated BOTH ways: a missing repo now gives `NOT GREEN` rc 1, all seven give `OK` rc 0 | It is the SAME SENTENCE this file already applies to an unknown ARGUMENT forty lines above — *"silence about a name we cannot check reads as a pass"* — and the branch one over from it was the exception. That is the R402 shape and the `candor-denylist-over-allowlist` mirror: **an audit that fixed the instance it was handed and not the sibling four lines away.** RED rather than PENDING deliberately: pending means "ask again" and `--wait` cannot clone a repo. The remedy rides on the row per the family UX rule that a failure carries the way out. **The general lesson is the expensive one: this row was found by an agent told to make the guard FIRE, in a cluster the coordinator had already declared closed.** |
| R442 umbrella: `release-test.sh` §13b's IN-FLIGHT and BUILD-IN-PROGRESS rows pass VACUOUSLY on a busy machine — the grep is `a build is running`, so another agent's `swift build` satisfies a row headed `cargo build` | 2026-09-15 | umbrella | **A FALSE PASS, which is the direction that does not announce itself.** MEASURED by deleting both fixtures entirely — no fake `conformance/run.sh`, no fake `cargo` — and watching all four rows still print ✔. Part of a full sweep of R405's stated residual: **25 assertion predicates across the 15 `probe.sh` call sites under a mid-section refusal gave 1 protected, 5 FALSE PASS, 16 FALSE FAIL, 3 correct.** A false FAIL is loud; the five false PASSes are the finding. **CLOSED candor `05e156a`** | Fixed by ATTRIBUTION, not by a wider grep: a control probe is taken before the fixture starts, and if the machine already refuses, the rows SKIP with their reason instead of passing. Two siblings came with it — §13's differ-check row asserts exit 2 and a busy refusal is ALSO exit 2 (the section comment had named this and the remedy had gone to the section guard, which only covers a machine busy BEFORE the section starts); and `:4122` is the sibling the original R405 remedy missed, **four lines above the one it reached**. Made forceable first via `PROBE_FAULT=in-flight\|building` — reproducing it for real means spawning a machine-wide `cargo`-shaped process, which would trip every other agent's guard, and that is precisely why nobody could force it before |
| R443 umbrella/all: the TWO GATE ROUTES disagree about disclosure — `candor-query gate` certifies a `pure` rule over an `{Unknown}` function with `policy ✓`, exit 0 and NO advisory, while `candor-scan --policy` over the identical tree and rule prints three advisory lines naming the remedy | 2026-09-15 | all (measured on rust) | **MEASURED 2026-09-15 on a 1-function fixture** (`pub fn via_callback(f: impl Fn()) { f(); }`, rule `pure via_callback`). The function carries `inferred: ['Unknown']`, `unknownWhy: ['callback:unresolved call']` — the disclosure channel is working. Then: **ROUTE A `candor-query gate --report … --policy …` → rc 0, advisory lines 0**, output is exactly `policy ✓ (the report's own signature — no re-scan, no re-derivation)`. **ROUTE B `candor-scan --policy` → rc 0, advisory lines 3**, naming the exact upgrade `add deny Unknown via_callback`. Same tree, same rule, same verdict, opposite disclosure. **ROUTE A IS THE ONE CI USES**, because gating a precomputed report is the whole point of the query layer — so the silent route is the deployed one | This is the **R347 shape (two routes computing one fact, a defect fixed in one and not the other) landing on the GATE ITSELF**, which is the surface the product's claim rests on. It is spec-CONFORMANT — SPEC §4.0's table does not make `{Unknown}` violate `pure` — so this is not a ⟨0.21⟩ break and not a cardinal sin; it is a DISCLOSURE asymmetry, and the cardinal-sin rule's own test is DISCLOSED-vs-SILENT, which route A fails. Consequence for a first-day adopter, and this is why it matters more than its severity suggests: `candor init` *"proposes rules that currently pass"*, and `adopt/arch.policy` proposes no `Unknown` rule in any of them — so a new user's `deny Net com.acme.domain` goes green through a DI-wired port and nothing ever says purity was unverified. **The fix is the cheap half: make route A print route B's advisory.** The expensive half — whether `deny`/`pure` should FAIL CLOSED on `Unknown` by default — is a SPEC question and is deliberately NOT bundled here: `deny X Unknown` is unadoptable where Unknown is ~20% of functions (eval/unknownwhy-sweep: 171 of 845), which is exactly why the default is what it is. Found by a Fable product review, then reproduced by the coordinator before filing **WIDENED 2026-09-16 BY MEASUREMENT, AND IT IS NOT A RUST BUG — IT IS THE SHARED LAYER.** The row was filed "all (measured on rust)". Measured on three engines with the same one-function fixture (a function carrying `Unknown` from an unresolved callback, rule `pure <fn>`): **EVERY ENGINE'S OWN `--policy` ROUTE DISCLOSES** — candor-scan 3 advisory lines, candor-ts 4, candor-java 3, each naming `add deny Unknown <fn>` — and **the CROSS-ENGINE QUERY LAYER DISCLOSES NOTHING, over ANY engine's report**: `candor-query gate` answers `policy ✓ (the report's own signature)` at rc 0 with ZERO advisory lines over a rust report AND over a java report, and `candor-ts query.mjs gate` answers `no violations` over a ts report. So the split is not between two rust code paths; it is between the engines (which all disclose) and the shared gate the engines' reports are consumed by (which does not). **THIS IS AN ARGUMENT ABOUT ARCHITECTURE, AND IT POINTS AT BACKLOG §6d S5.** `candor-query` IS the back half already — it reads any engine's report, and candor-java and candor-swift expose NO query verbs at all, so for those two engines it is the ONLY query route there is. The one genuinely shared component in the family is missing a disclosure that all four engines independently implement. **That is the S5 thesis stated as a defect rather than as a design opinion**, and it is why the row's "cheap half" is not cheap: `unverified_holes` is `pub(crate)` in candor-scan and candor-query owns a separate implementation, so a correct fix is a shared core and a third copy would make it worse. Consequence for adopters is unchanged but now applies to every engine: a precomputed-report gate — the one CI runs — certifies an `Unknown`-carrying function in silence. **CLOSED 2026-09-16, candor-rust `c53f5d4` — AND THE COORDINATOR'S STATED REASON IT WOULD BE EXPENSIVE WAS WRONG.** I wrote that `unverified_holes` being `pub(crate)` in candor-scan, beside a `cmd_unverified` in candor-query, meant the duplication WAS the defect and a correct fix needed an extraction — i.e. that this was blocked on §6d S5. **False, and checkable in one grep I did not run**: the hole PREDICATE has been single-sourced since ⟨0.24⟩ as `candor_classify::policy::unverified_hole_rule` + `rule_and_upgrade`, and BOTH routes already called it — candor-scan's `unverified_holes` is a ~20-line adapter over it. What was duplicated was plumbing and RENDERING, not the judgement. I inferred duplication from two file locations instead of reading what they call, which is the same error shape this register records elsewhere as *a sentence that makes your own diff correct is the one nobody verifies*. THE FIX ADDED NO THIRD COMPUTATION: one `unverified::hole_set` called by `cmd_unverified` AND `cmd_gate` over the same entries and the same `sig` the gate was judged from, plus one `policy::unverified_note_lines` for the words — **candor-scan's own rendering block was DELETED and replaced by a call to it**, so the wording is single-sourced too. Advisory only: exit codes and `--gate-json` documents verified byte-equal. Disclosed in the violation and pass arms and in NONE of the four refusal arms, so the routes agree over the same set of RUNS as well as of functions. **MEASURED over 400 registry crates: ADDED 0 / REMOVED 0 / CHANGED 0 on all five key+value combinations with REACH 466 hits across 233 entries; route A gained a disclosure it never had on 250 entries / 7,749 functions; route A notes in the PRE arm across the whole corpus: ZERO.** Route-agreement harness: 0 drift on route B, 0 moved on route A, and route A set != route B set on 0 of 389 runs reaching a verdict. Falsification: 3 of 4 new tests FAIL in a pre-fix worktree; the 4th is the over-charge control and passes on both arms, which is what a control is for. |
| R444 **REWRITTEN 2026-09-20 — BOTH readings this row offered were WRONG.** The hedge is not correct-but-misnamed; it is FABRICATED. And the mechanism is not an operator call on untyped operands: **`extension Int: SomeProtocol` anywhere in a package makes ordinary `Int + Int` arithmetic read `Unknown`**, attributed to a protocol that declares no operator at all | 2026-09-15 | swift | **FALSIFIED WITH ONE VARIABLE, both fixtures `swift build`-verified.** Arm A: a protocol declaring no operator, `func addInts(x,y) { x + y }` — function ABSENT (pure), `deny Unknown` exit 0. Arm B adds ONLY `extension Int: Tag {}` — `inferred:["Unknown"]`, `unknownWhy:["dispatch:Tag.+"]`, `deny Unknown` **exit 1**. **The filed mechanism was also wrong**: the operator visitor's protocol-dispatch arm was instrumented and records ZERO hits on RxSwift. The real producer is `Driver.swift:2241`, the external-supertype fallback, reached by the TYPED `Type.op` edge. Two parts: `extension Int: P` puts `Int` into `localTypes`, so the `localOperand` gate — written precisely so *"a stdlib `Int + Int` has no local unit and stays pure"* — fires for stdlib types; then `resolveQual("Int.+")` finds nothing and the fallback picks `extSupers.first(where: !STD_PURE_PROTOCOLS.contains)` **without ever checking that the supertype declares the member**. `STD_PURE_PROTOCOLS`' own doc scopes it to *"a PROJECT type conforms to an EXTERNAL supertype"*, and the adjacent comment says it *"never fabricates over real project code"* | **SCALE, four of four real corpora: swift-nio 2,051 supertype hedges and 298 rows whose ONLY effect is an `Unknown` from one; RxSwift 1,083/124; alamofire 144/12.** swift-nio declares `extension Int: AtomicPrimitive` and eleven siblings, and `AtomicPrimitive` declares only `atomic_*` properties — so `AdaptiveRecvByteBufferAllocator.record` reads `Unknown[dispatch:AtomicPrimitive.!=, .%, .&&, .<=, ...]` **over pure integer arithmetic**. Direction is OVER-DISCLOSURE, not a cardinal sin — but it is the [[R485]]/[[R490]] class: a reason-scoped `deny E Unknown[dispatch:Tag.+]` binds on integer arithmetic, and it makes plain `deny Unknown` unusable on any package that conforms a stdlib type, which is exactly what pushes users to scoped policies. **NO FIX LANDED, DELIBERATELY AND RIGHTLY**: both candidate fixes REMOVE an `Unknown`, which this register's own record names as where silent under-reports get introduced, and the *"name a true owner instead"* option needs a REASON-STRING RULING that lives in `candor-spec` — **mine to make, not the engine's**. Repro is a six-line fixture plus three discriminators RULED 2026-09-20, and the ruling is that NO NEW RUNG WAS NEEDED — the contract already answered. ⟨0.24⟩ reserves the DOT-FREE dispatch: detail for a dispatch whose owner the engine could not form, and <owner-type>.<member> is normative, so the dotted form ASSERTS that the owner declares the member. Picking a supertype off a list without checking does not produce an imprecise detail, it produces a FALSE one. SPEC now says so explicitly, citing this row and its numbers, because an engine did the other thing while satisfying every shape check PART 10 makes. NO VERDICT MOVES: §6.2 maps every dispatch:* to the dispatch class dotted or not, and ⟨0.24⟩ already calls the dot-free detail free text that is not conformance-compared — what changes is which string an engine is ENTITLED to write. THE RULING DELIBERATELY DOES NOT CURE THE OTHER HALF, and the clause says so: the hedge itself is a separate question, and a hedge that should not exist is not cured by renaming it. Int + Int over stdlib types should not be charged Unknown at all; that half remains this row's engine defect and is candor-swift's to fix. |
| R445 swift: `APPLE_PRIVACY_KEYS`' `why` column is STALE FOR 56 OF ITS 57 ENTRIES — ~40 keys still carry the string `"not modelled"` while the engine's own adjacent line reads *"56 of Apple's 57 documented usage-description keys are modelled"* | 2026-09-15 | swift | **MEASURED. The strings are INERT** — `PRIVACY_UNMODELLED_KEYS` filters to the one real gap (`NSFileProviderPresenceUsageDescription`, whose reason IS accurate), so no verdict is wrong because of this. **But one of the stale sentences is the `NSLocalNetworkUsageDescription` one, and it is what made R390 read as CONSIDERED for as long as it did** — a documented limitation that had expired, sitting exactly where someone would look before deciding whether the Bonjour gap was known | This is `feedback-documented-limitation-is-not-measured` in its THIRD and most expensive variant: a comment that was TRUE WHEN WRITTEN and expired because the thing it points at moved. It cost the time between the local-network work landing and R390 being found, because the note said the question had been asked. **The fix is mechanical** (re-derive each `why` from whether the key is in `PRIVACY_UNMODELLED_KEYS`) **and the gate is the interesting part**: a `why` that disagrees with the filter should be a build error, not prose, or it will expire again. Cheap, and it removes a whole class of false reassurance from the one table a privacy verdict is read from **CLOSED 2026-09-16, candor-swift `2877858`, and the fix is STRUCTURAL rather than a re-derivation.** The count was 47 entries carrying the literal `"not modelled"`, not the ~40 this row estimated. Rather than rewrite 56 sentences to be true today, **the reason no longer travels with the universe**: `APPLE_PRIVACY_KEYS` is now a bare `[String]` (57 keys, verified identical to HEAD in SET AND ORDER) and `PRIVACY_UNMODELLED_WHY` carries a reason only for a genuinely unmodelled key. **The stale state is now UNREPRESENTABLE for the 56 — there is no field left to rot**, which is the difference between fixing this row and fixing it again in six weeks. INERTNESS WAS MEASURED THREE WAYS, not reasoned: structurally (`why` has exactly two readers, `PrivacyManifestCLI.swift:623` and `:1089`, and both filter through `PRIVACY_UNMODELLED_KEYS`); executed (`--verify` prints exactly one `why`, the accurate one); and by a worktree A/B at `3eca7f5` whose two binaries gave BYTE-IDENTICAL output over a fixture proven to reach the code. `corpus-ab.py` was correctly NOT used and the reason stated — nothing the scanner reads changed, so it would have had zero reach, and the worktree A/B measures that directly instead of asserting it. |
| R446 rust: a container's ELEMENT reached through an ACCESSOR RECEIVER resolves to NOTHING on both routes — `v.get(0).unwrap().run()` over a `Vec<Box<dyn Doer>>` reads silent-pure | 2026-09-15 | rust | **CARDINAL SIN. CLOSED same day, candor-rust `76ac6b0`.** Found by attacking R401's framing rather than implementing it: the row said *"a `HashMap` VALUE"*, and **that framing hid the class** — the axis is the BINDING SITE, not the container. `for` / `if let` / index already resolved an element; the RECEIVER position did not, so the same `Vec` answered differently depending on how the element was reached. 7 spellings went ABSENT→`['Exec']`; 4 over-charge controls unmoved. A/B over 1,563 crates: **ADDED 7 · REMOVED 0 · CHANGED 29 (0 on `inferred`)**, reach 13,628 accessor binds across 768 crates | `lang.rs::is_element_yielding_accessor`. **Stated residual, measured rather than assumed:** the CONCRETE map value (`m[k].run()`, `m.get(k).unwrap().run()` over `HashMap<String,G>`) is STILL absent — `elem_type` has no map arm, and adding one hits the fabrication route R347's note already records. Left open deliberately, with the reason, rather than closed by a fix that would trade this silence for an over-charge |
| R447 rust: an element-yielding accessor is walked as a BUILDER STEP, so the OUTER method is attributed to the RECEIVER's own type — `c.get("x").run()` reports the caller `['Exec']` over a body that spawns nothing | 2026-09-15 | rust | **FABRICATION, PRE-EXISTING, and DELIBERATELY NOT FIXED — the fix was BUILT and BACKED OUT ON MEASUREMENT.** `impl Cfg { fn get(&self,k)->Calm; fn run(&self){spawn} }`. The obvious repair (return at the accessor arm instead of walking on) measured **REMOVED 14** on the 1,563-crate A/B — one of them quinn's `SendStream::poll_write` LOSING its `['Unknown']`, because `Pin::get_mut`, `UnsafeCell::get` and `NonZeroI32::get` are not container accessors. **Trading a silence for a lost disclosure is the wrong direction**, so it was reverted and filed instead of shipped | This is the counterweight to [[R446]] and they share one mechanism, so read them together. Needs its own diff and its own A/B, with the accessor predicate narrowed to things that genuinely yield a CONTAINED element — note the failing examples are all single-field wrappers whose `get` returns an INNER value, which is the same shape and the reason a blunt predicate cannot separate them. **Do not fix this by widening the accessor list**; that is the allowlist direction this family has been burned by (see `candor-allowlist-chain`) **CLOSED 2026-09-16, candor-rust `a9a7a2b` — AND BOTH HALVES OF THIS ROW'S FRAMING WERE WRONG.** **(a) "An element-yielding accessor" is the wrong AXIS.** Renaming `get` to `pick` in the fixture — a method on no accessor list at all — fabricates IDENTICALLY. Nothing about `get` causes this; the BUILDER-CHAIN WALK in `resolve_recv_type` does. So narrowing the accessor predicate could never have reached it and widening could only have worsened it: the standing instruction *do not fix this by widening the accessor list* was right for a different reason than the one given. **(b) The fabrication is the SMALLER HALF** — see [[R451]], a cardinal sin in the same walk. The fix consults the crate's OWN declaration keyed on (TYPE, method) via `impl_ret_key`, never the leaf-keyed return index, which is the hijack `resolve_recv_type`'s comment has always refused; it refuses at the write site on blanket impls, generic returns and fluent `-> Self`. Cache schema rev26 → rev27, because the decls changed shape and `decl_index_hash` cannot notice — it is computed FROM them. |
| R448 rust: `cmd_unverified` looked reason classes up by BARE `e.func` in a map keyed by `entry_key` (the `hash` when present), so on EVERY REAL REPORT the hole predicate ran with NO reason classes at all — a narrowing `deny Unknown[class]` never saw the classes it narrows on | 2026-09-16 | rust | **FOUND BY WIRING THE TWO ROUTES TOGETHER — it was invisible while they were separate, and that is the argument for the R443 fix restated.** `ReportSignature`'s OWN doc comment names the hazard verbatim: *"a name-keyed lookup into a hash-keyed map does not error, it returns None."* Real candor-rust reports carry `hash = pkg#fn`, so the miss was total rather than partial. MEASURED on one fixture with `deny Unknown[indirect] via_callback`: `gate --report` exits **1** with an AS-EFF-006 violation on the function, while `unverified` called it a hole and printed **"The gate still PASSES — this is advisory"** — the verb whose entire job is *"your green gate is not provably green"* asserting the OPPOSITE of the gate about the same function. **CLOSED in the same commit, candor-rust `c53f5d4`** | Direction of harm is OVER-disclosure, so no hole was ever lost — what was lost was the truth of the sentence printed underneath them, which is worse than it sounds for a verb whose only product IS that sentence. **SWEPT, NOT SPOT-FIXED**, per the audit-boundary rule: every lookup into a `ReportSignature` accumulator in `candor-query/src` was checked — `fix.rs:644` already used `entry_key`, `unanswerable_pairs` reads keys off `sig.all`, and this was the only wrong one. A doc comment that names a hazard is not a guard against it: this one named it exactly and the bug sat one call away. |
| R449 swift: `APPLE_PRIVACY_KEYS`' doc comment has read *"`PrivacyKeyUniverseTests` pins the arithmetic"* since 2026-08-05 and **that file never existed anywhere in the repo** — the comment asserting the ratchet was the ENTIRETY of the ratchet | 2026-09-16 | swift | **THIS IS WHY [[R445]] WAS POSSIBLE, and it is the same failure one level up.** R445 was 56 false `why` strings; this is the test that was supposed to catch them being cited by name and never written. Verified: `git log --all -- '**/PrivacyKeyUniverseTests.swift'` returns exactly ONE commit — the one that created it while closing R445. **CLOSED by that same commit, candor-swift `2877858`**, with the file now real and calibrated in four arms | `feedback-documented-limitation-is-not-measured` has a fourth variant and this is it: not a comment that EXPIRED, but one that was **never true when written**. A comment naming a guard reads as evidence the guard exists, and it is load-bearing precisely because nobody greps for a filename in prose. The general form is worth more than the instance: **a comment that names a test, a gate or a ratchet is a CLAIM about the repository and should be checkable mechanically.** [[R450]] is the sweep that question produced. |
| R450 swift: of 28 backticked TEST SYMBOLS named in `Sources/`, two do not resolve — a pin cited in prose that no longer names anything | 2026-09-16 | swift | **FOUND BY SWEEPING PAST [[R449]]'s TRIGGER rather than stopping at it.** Both are benign: the second, `CNativeDisclosureTests` (`Classifier.swift` ~:2062), genuinely exists as `CNativeDisclosureArityProcessTests.testTheLabelGateCannotSeparateTheStructFromTheSyscall` — READ to confirm it asserts both directions — so the commit corrects the name rather than writing a test. Fixed in candor-swift `2877858` | **NO REPO-WIDE GATE WAS BUILT, AND THE BOUNDARY IS STATED — this is the judgement worth keeping.** The census has a 2-in-28 FALSE-POSITIVE rate: `FooTests`/`MyAppTests` are backticked placeholders in prose about path heuristics, and one real name wraps across two `///` lines. A gate over that needs a hand-written exclusion list, which is **the exact vacuous shape the R445 gate was built to avoid** — a hand list checked against a hand list is two-sided drift. So the sweep was done once, by hand, and its result recorded; automating it would have produced a ratchet less trustworthy than the absence of one. Revisit only if a parser can resolve the symbols properly. |
| R451 rust: the builder-chain walk collapses a DELEGATION onto the delegator — where the delegating method shares its name with the step's declaring type, the walk forms the ENCLOSING FUNCTION ITSELF, a self edge contributing nothing, and the caller goes ABSENT over a body that really performs the effect | 2026-09-16 | rust | **CARDINAL SIN, in widely-depended-on crates, read against source: `tokio_postgres::Client::cancel_query` (`self.cancel_token().cancel_query(tls)` — OPENS A TCP CONNECTION and reported PURE), `async_process::ChildGuard::drop` (`self.get_mut().kill()` — reported PURE), `sea_orm::Select::stream_partial_model` (A DB QUERY, reported PURE), `criterion::Criterion::bench_function`, `tokio::Notify::notify_waiters`, `redis::RandomReplicaStrategy::route_read`.** Found while fixing [[R447]] and it is the larger half of it. **CLOSED same commit, candor-rust `a9a7a2b`.** A/B over 1,561 registry crates: **ADDED 1,297 / REMOVED 0 / CHANGED 2,254, REACH 5,560 corrections across 328 entries**. Of the 2,254 changed, 564 gain an effect and 1,583 keep the same effect set with a CORRECTED EDGE; **0 lose one**. Of 1,297 added, 121 are hard-effect, 8 read against source, **0 fabrications** | **THE FIRST VERSION OF THIS FIX MEASURED REMOVED 170 — 106 of them losing `['Unknown']`, seven times worse than R447's own backed-out attempt — and the A/B is the only reason that is a paragraph rather than a shipped regression.** Mechanism traced rather than guessed: mio-0.8.11's `registry.selector().deregister(fd)` moved off the WRONG-BUT-UNIQUE `Registry::deregister` onto a `Selector::deregister` mio declares in THREE files, which resolves to nothing — and an unresolvable typed call is dropped SILENTLY ([[R452]]). 78 of the 170 were that single cascade. The second gate fires only when the returned type declares the outer method EXACTLY ONCE; a second declaration withdraws the key. **Declining keeps the over-charge rather than buying a silence with it**, which is the trade this register asks for. **INDEPENDENTLY CONFIRMED 2026-09-16 BY A DIFFERENT INSTRUMENT, ON A DIFFERENT PLATFORM, AND THE AGENT NEVER SAW IT.** candor-rust's WEEKLY `coverage-gate-refresh` (scheduled, ubuntu runner, 74-crate self-scan) landed on `a9a7a2b` hours after the fix and reported `0 regressed / 37 newly-uncovered` — and the rows it names are the ones this row names: `tokio_postgres::client::Client::cancel_query effects='Ipc,Net'`, `tokio_postgres::transaction::Transaction::cancel_query`, `redis::...::RandomReplicaStrategy::route_read`, `sea_orm::executor::select::Select::stream_partial_model effects='Db,Unknown'`. **`0 regressed` is the load-bearing half**: the instrument that would notice `classify()` losing a rule found none, which is the same claim as the A/B's REMOVED 0 arrived at independently. ATTRIBUTION, done rather than assumed: of the 37, **14 are NOT explained by crate-version churn** (async_nats 11, tokio_postgres 2, redis 1 — none appear in the run's own R311 version-changed list), while sea_orm 12 and mongodb 11 are confounded with `sea-orm-2.0.2→2.0.3` and `mongodb-3.9.0→3.9.1` and cannot be attributed either way from this run. **The checked-in coverage manifest now needs a refresh, and R212 is why that is not a one-liner:** the checked-in copy was generated on macOS historically and this run is Linux, and a same-commit run has measured a DIFFERENT row count across those platforms before with the cause never ruled out. So a macOS regeneration could disagree with CI for a reason that has nothing to do with this fix. **Refresh on Linux, or close R212 first.** Note the drift job is SCHEDULED WEEKLY, not push-triggered, and `ci.yml` runs only the generator's `--selftest`, never a diff against the checked-in manifest — so this is a drift REPORT and blocks no release. **THE SECOND GATE CANNOT BE RELAXED — MEASURED 2026-09-16 AFTER [[R452]] CLOSED, NOT ASSERTED.** R452 was the stated precondition for relaxing it, so the relaxation was BUILT behind an env flag, A/B'd against the committed binary, and REVERTED (tree byte-identical afterwards): **ADDED 565 · REMOVED 110 · CHANGED 1,846**, with 69 removals losing `['Unknown']` across 42 crates and **140 changed rows losing an effect** (x11rb `Image::put` loses `Log`; nix/jiff/time/indexmap lose `Unknown`). Down from R451's original 170, not closed. The residual is exactly the spelling R452 deliberately does NOT cover — a corrected receiver whose `Type::method` has ZERO claimants and is not macro-hidden, where the only available hedge is the 14,409-caller flood R452 priced and refused. **So the gate stays, and `model.rs`/`collector.rs` now say that AT THE GATE ITSELF** rather than leaving the next reader to rediscover it. |
| R452 rust: a typed call whose `Type::method` resolves to NO UNIT — absent, or ambiguous across a `tail2` collision — is dropped with NO `Unknown`, NO `unresolved`, NO `unknownWhy` | 2026-09-16 | rust | **CARDINAL SIN, PRE-EXISTING, OPEN — and PINNED by a test that asserts the WRONG ANSWER ON PURPOSE**, so the day it is fixed the pin fails loudly instead of the fix passing unnoticed. Measured: `bare(s: &Sel) { s.deregister(3) }` over a spawning body is ABSENT from `functions[]` and passes `deny Exec` while naming only a decoy — in BOTH arms of the R451 A/B, so it is independent of that change. Found because it CAUSED 78 of the 170 removals the first R451 attempt measured | **This is NOT the documented "honest miss" for std receivers.** The type is LOCAL, and the engine already HAS the vocabulary for exactly this disclosure — `macro:module items hidden by an unexpanded macro` — and does not reach for it. So the silence is a gap in wiring, not a limit of the analysis, which is the distinction that makes it fixable. **It is why [[R451]]'s second gate exists, and closing it is the PRECONDITION for relaxing that gate** — which would in turn recover the corrections R451 currently declines. Fix this before widening anything in `resolve_recv_type`. **CLOSED 2026-09-16, candor-rust `fceeb85` — AND THIS ROW'S "WIRING GAP" DIAGNOSIS WAS HALF WRONG, which decided the fix.** The row said the engine *"already HAS the vocabulary"* (`macro:module items hidden by an unexpanded macro`) and therefore only needed wiring. **The vocabulary is there; the EVIDENCE is not.** R128's hedge asks `macro_hidden_owner` whether the call path's OWNING MODULE is macro-hidden and requires a `crate::`-rooted path — but a receiver-typed method call is formed from the RECEIVER'S TYPE, so its path is `Sel::deregister`: no module to ask the question of and no `crate::` head. **Wiring R128 to method calls is STRUCTURALLY IMPOSSIBLE**, verified by instrumenting the call loop (`typed=true, resolvable=false, t2n=0, path=Sel::deregister`) rather than by reading. Two new decl indexes were built instead. THREE CHANGES, EACH PRICED SEPARATELY: resolve where the WRITTEN path settles it (via `arm_exact_target`, which R440 already built and which refuses on a second match, so it can only pick a definition the source spelled out — reach 5,331 sites / 3,598 callers / 225 crates; arboard `Get::html` and crossbeam-channel `Receiver::try_recv` read against source); ambiguous tail → `Unknown` + `ambiguous:same-name local methods` (12,888 sites); macro-hidden → `Unknown` + `macro:…` (1,024 sites). A/B over 1,561 crates: **ADDED 8,626 · REMOVED 0 · CHANGED 8,369**, reach 19,243 across 421 entries, and **0 of the 8,369 changed rows lose an effect**. **THE NARROWING IS THE WORK, AND IT WAS PRICED AGAINST A PREVIOUSLY-DECLINED THRESHOLD.** Hedging every unresolved typed method call would hit 14,409 callers across 230 of 250 crates — `str::trim`, `Vec::push`, the documented honest miss. Module evidence alone still hedged 581, almost all std combinators on a mis-typed receiver with nothing hidden. Requiring `fn <name>` in the macro text cut it to **95 while keeping every real catch** (aho-corasick `StateID::as_usize` via `index_type_impls!`, bitflags `Flag::bits`). Total cost: 11,224 rows newly carry `Unknown` = **1.44% of 676,572 analysed units, against the 4.88–7.02% R190(c) priced and DECLINED** — which is the comparison that makes this shippable rather than a matter of taste. Cache schema rev27 → rev28, caught by the reflective `every_merged_decls_field_is_folded_into_the_digest` table refusing to compile until both new fields had mutators. |
| R453 rust: a TEST WAS PINNING A CARDINAL SIN — `two_different_definitions_sharing_a_tail_are_still_an_ambiguity` asserted that `caller(p) { a::Bar::go(p) }` must be ABSENT, over a body that really calls `std::fs::write` | 2026-09-16 | rust | **THE ABSENCE WAS A §4 PURITY CLAIM OVER A REAL FILE WRITE, KEPT ALIVE BY A TEST FOR AS LONG AS THE TEST EXISTED.** Its stated reasoning — *"resolving it would guess"* — is false for this fixture: the call is WRITTEN `a::Bar::go`, which names exactly one claimant. Found while closing [[R452]], because the new resolution made the test go red and the test turned out to be wrong rather than the fix. **CLOSED in the same commit, candor-rust `fceeb85`**: it now asserts `['Fs']`, and a SECOND arm (`blind`, the same tail reached through a RECEIVER) holds the refusal the test actually existed to guard | **A test is a guard AND a claim, and this register has now seen three shapes of the claim being false in one session:** a doc comment citing a test file that never existed ([[R449]]), a doc comment naming a hazard one call from the bug ([[R448]]), and this — a test asserting the cardinal sin as correct behaviour. **A red test is not automatically a regression; it is a disagreement, and either side can be the wrong one.** The tell here was that the fixture's call names ONE claimant, so no guess was required and the test's own justification did not apply to the code it pinned. |
| R454 rust: a map's CONCRETE value was never an element — `elem_trait_leaves` has had a map arm since R46 and `elem_type` never did, so `HashMap<String, Box<dyn Doer>>` charged and `HashMap<String, G>` was SILENT | 2026-09-16 | rust | **CARDINAL SIN, and [[R446]]'s residual named TWO spellings where ALL SIX were silent** — `m[k].run()`, `m.get(k).unwrap().run()`, `for v in m.values()`, `m.values().for_each(..)`, and the parameter and local forms, every one ABSENT over a `HashMap<String, G>` whose `run()` spawns, while every `Vec<G>` control beside them charged. **Which silence you got depended on whether the value happened to be a trait object** — that is R347's own §G shape one level up: R347 unified the ADAPTER list the two resolvers peel with and left the CONTAINER-SHAPE list they dispatch on as two copies. **CLOSED 2026-09-16, candor-rust `40b0f17`.** A/B over 1,561 crates, PRE `fceeb85`: **ADDED 18 · REMOVED 0 · CHANGED 51** (4 on `inferred`), reach **3,811 element resolutions across 307 crates**, 0 changed rows lose an effect. Reach large and movement small because a map's value is usually plain data — where it is not, the silence was total. Read against source: tinytemplate `TinyTemplate::render`, diesel `serialize_query`/`BindData::value`, hickory-resolver, plotters `FontMap::get_fallback`, wit-parser `MergeMap::map_name` | **[[R347]]'s PREDICTED FABRICATION ROUTE CANNOT FIRE FOR A MAP, and that was measured before shipping rather than argued around.** `iter`/`into_iter`/`drain` are element-preserving and on a map yield `(&K, &V)`, so a single-binder closure would take the VALUE type — but that spelling DOES NOT COMPILE (a tuple has no `run`), and the spellings that do occur (`for (k,v) in &m`, `.for_each(\|(k,v)\| ..)`) route through `resolve_elem_tuple`, which has no map arm and so contributes NO binding rather than a wrong one. Those two remain a STATED under-report with a named boundary, pinned by a test so the next attempt starts there. **The one over-charge is [[R213]] and the fixture PROVES it pre-dates this:** 14 lapin rows gain a drop-glue edge because `owned_drops` is leaf-keyed, and the discriminating fixture `b::Inner::touch` — sharing only its leaf, returning a `u32`, spawning nothing — is charged `Exec` on BOTH sides of the commit. The map arm hands an already-broken index a new and CORRECT fact. Cache schema rev28 → rev29. |
| R455 rust: `ci/verify-binary.sh` — the RELEASE-ASSET gate — refused a healthy binary on the macOS arm64 leg with `candor-query --version failed`, then PASSED on a re-run of the IDENTICAL SHA | 2026-09-16 | rust | **A FLAKY GATE ON RELEASE ASSETS, and it blocked the 0.38.3 cut.** Measured: the failure landed on `786c602`, whose only change was a version bump, while `40b0f17` one commit earlier passed and the Linux leg printed `verify-binary: OK` throughout. The macOS log shows NO `scan =` line, which places the failure between the two `--version` reads — scan's succeeded, query's did not — because both echoes come after both assignments. **CAUSE NOT CONFIRMED.** The only construct there that can fail non-deterministically is `"$Q" --version 2>/dev/null \| head -1` under `set -euo pipefail`: both binaries print TWO lines, so `head -1` can close the pipe before the second write, hand the producer SIGPIPE, and `pipefail` then makes the substitution non-zero. **40 local runs of the exact command-substitution form did not reproduce it**, so that remains a hypothesis | **FIXED BY REMOVING THE CLASS RATHER THAN THE INSTANCE** — the pipe is gone, the first line is taken with `${v%%$'\\n'*}`, and with no pipe there is no SIGPIPE to race on whatever the cause turns out to have been. That is the right trade for a gate whose green authorises a published binary: **a harness FAIL is indistinguishable from a real one**, and this register's own rule is not to explain a FAIL by flakiness until it fails to reproduce serially — which it did, twice, locally and in CI. The 8-arm selftest still passes and the real binaries verify at 0.38.3. **A useful accident while fixing it:** the first real run correctly refused a STALE local `candor-scan` at 0.38.2 against an expected 0.38.3, which is the version arm earning its keep on a binary I had forgotten to rebuild. |
| R456 rust-deep: the dylint engine STAMPS `spec: 0.38` from the shared constant while writing a **pre-⟨0.21⟩ report** — no `analyzed`/`unanalyzed` manifest, no coverage ledger — so the family's own `candor-query gate` tells a user to *"re-scan with a current engine"* over output that claims to be current | 2026-09-16 | rust-deep | **FILED AS A CANDIDATE SPEC §7 item-8 VIOLATION, not as a conclusion** — item 8 requires an engine to declare the spec version it implements. VERIFIED: `src/lib.rs:3360` sets `spec: candor_report::SPEC_VERSION` (0.38) and `:3362` writes through the TWO-argument `to_packaged_report_json`, which passes `None` for the coverage ledger and no manifest (`candor-report/src/lib.rs:739-744`); **zero occurrences of `unanalyzed`, `incomplete_surfaces`, `out_of_scope`, `file_set`, `scanned_under`, `net_partners`, `blindspots` or `peeked` in `src/lib.rs`**; `interface_union` hard-coded false with the comment *"it lags the stable backend on config surfaces"*. The lint crate is at **0.5.9**, unchanged since 2026-07-09, against candor-scan's 0.38.3. NOT bit-rotted — it builds and its gates are green on every push — **frozen and spec-drifted**, which is a different thing and the one that matters here | **THIS INVERTS BACKLOG §6d S3.** The coordinator's framing attacked the banner's FIRST clause (*"advisory floor"*) and accepted its second (*"the nightly engine is the sound gate"*). The second is the wrong half: the lint IS sound on the effect lattice and fuzzer-gated, but it is not a SPEC-CURRENT gate, so routing users to it — S3's options 1 and 3 — would send them to an engine whose output the rest of the product discounts, into a pipeline enforcing a 0.38.x pin. **And SPEC §7 item 7 OBLIGES the floor to disclose that it can under-report silently**, so deleting the disclosure outright is not available either. The remaining move is to fix the SECOND clause and keep the first, ideally replacing *"under-reports"* with the MEASURED floor: the syscall oracle runs 19 drivers per push with `KNOWN_UNDER_PROGRAM=()` — zero silent-pure on executed Fs/Net/Exec — and calibration reports 0 false positives over 76 curated-pure crates. **"Advisory" overstates the pessimism and "sound gate" overstates the alternative.** |
| R457 rust: a PRODUCTION source file named `*_test.rs` is excluded wholesale as a `test-module` on the FILENAME alone — `regex-cli/cmd/compile_test.rs`, the real source of the `regex-cli` binary, is dropped, narrowing that crate's `cmds` from `[cargo, git, rustfmt, ucd-generate]` to `[rustfmt, ucd-generate]` | 2026-09-16 | rust | **DISCLOSED, NOT SILENT — so not a cardinal sin, and that is why it has survived.** The report carries `excluded: [{class: 'test-module', count: 1, reason: 'a `tests.rs`/`*_test.rs` file module is a #[cfg(test)] tree whose test-ness is declared at the `mod` site, invisible when walking files'}]`. REPRODUCED minimally 2026-09-16: a crate whose only source is `src/compile_test.rs` containing a `Command::new("cargo")` reports **`functions: NONE`** with that exclusion. The reason is honest about its own heuristic — test-ness IS declared at the `mod` site and a file walker cannot see it — but the consequence is that a real binary's effect surface silently shrinks for anyone reading `cmds` | Found while investigating candor as a TESTING tool (`docs/testing-tool-investigation.md`), which is why it had not surfaced: the exclusion only bites when you ask what a crate SPAWNS, and nothing in the normal gate path asks that. **The fix direction is not obvious and should be priced before it is chosen** — reading the `mod` site is the correct answer and is what the reason text already implies, but the walker exists precisely because the `mod` site may be in a file the walk has not reached. Narrowing the filename rule (e.g. only under `src/` when a sibling `mod` declares `#[cfg(test)]`) is the DENYLIST direction this family prefers; widening it is not. Note the exclusion is load-bearing for real `*_test.rs` test trees, so a naive removal trades this for a large fabrication. **CLOSED 2026-09-16, candor-rust `de7f33a`, and the brief's "this is hard" clause was FALSE.** I wrote that the walker exists *because the `mod` site may be in a file the walk has not reached*. It does not — the walk has the whole tree on disk before anything is parsed. **MEASURED over 1,608 crates / 365 stem matches: for 351 the declaring `mod` (or the file's own `#![cfg(test)]`) is readable; only 14 are not, and those keep today's answer.** Census: **309 cfg-test mod-site + 25 inner-attr + 14 no-declaration = 95.3% genuinely test, 17 = 4.7% PRODUCTION source the old rule dropped** — `aws_lc_rs::test`, `ring::deprecated_test`, ratatui's `pub use self::test::TestBackend`, and `rusty_fork::fork_test`, **which really spawns and is now charged `Exec`**. `is_test_file_stem` becomes a CANDIDATE filter and `decls::test_stem_file_is_test_module` supplies the verdict from evidence, via the engine's own `is_cfg_test` so R122's `any(test, feature)` correction is INHERITED rather than re-derived. **THE NAIVE REMOVAL IS 152× WORSE AND THAT IS NOW A MEASUREMENT, NOT AN ARGUMENT: narrowing ADDED 37 / REMOVED 24 / CHANGED 98 against naive removal ADDED 5,638.** The exclusion is load-bearing and stays. **REMOVED IS 24, NOT 0, AND THE ACCEPTANCE CRITERION I SET WAS VIOLATED — the agent reported that rather than rounding it off, which is the behaviour this register wants.** All 24 are ratatui-core losing `['Unknown']`, traced to ground by a control holding everything constant but the FILENAME: rename the file and the PRE binary produces the POST answer exactly; delete it and PRE reproduces PRE exactly. So the mechanism is [[R458]], pre-existing and live today — **but the effect in the report is still a DISCLOSED `Unknown` becoming a SILENT absence, which is the cardinal-sin direction. R458 must close before the next cut**; this repo's standing rule is that a fixable silent under-report is fixed and shipped, never accepted as a low residual. A useful trap recorded with it: a first census keyed on *"does the attr mention `test`"* misclassified `#[cfg(any(test, feature = "test"))]`; the engine's own authority got it right. |
| R458 rust: a GENERIC-typed receiver resolves onto the crate's SOLE concrete impl and certifies purity — dropping an `Unknown` hedge over a type parameter a CONSUMER supplies | 2026-09-16 | rust | **CARDINAL-SIN DIRECTION, PRE-EXISTING, LIVE TODAY, and exposed rather than caused by [[R457]]'s fix.** `Terminal<B>::size` is `self.backend.size()` on a type PARAMETER. While `src/backend/test.rs` was excluded the crate had no in-crate `impl Backend` and the call hedged `Unknown`; admitting the file gives it exactly one (`TestBackend`, pure) and the engine resolves the generic onto it, certifying purity **with no disclosure**. A consumer using a real backend gets a purity claim over I/O. EVIDENCE is a control holding everything constant but the filename — rename `test.rs` → `membackend.rs` and fix the one `mod` line, and **the PRE binary produces the POST answer exactly** (455 analyzed, 90 rows, `Terminal::size` absent); delete the file instead and **PRE reproduces PRE exactly** (426, 111, `Unknown`) | **Not reducible to a hand fixture — the reporting agent said so rather than shipping a fixture that did not reproduce it**, and the rename/delete control is the honest boundary. The unsoundness is the inference *"the only impl I can see is the only impl there is"*, which is false for a public generic by construction; it is the closed-world assumption the java engine handles with a BOUNDED CHA and an explicit sealed/enum carve-out. **Blocks nothing today but must close before the next cut**, because R457's landing converts 24 disclosed `Unknown`s into silent absences in ratatui-core. **RETIRED 2026-09-17 AS A DUPLICATE OF [[R101]]'s OPEN-CHA HALF, and my proposed reframing was a CATEGORY ERROR.** I was about to argue this is a scan-boundary question that `outOfScope`/`unanalyzed` already serves. It is not: `SPEC.md:969-1010` defines all three keys as claims about **files of the target** — `unanalyzed` is files opened and unreadable, `excluded` is files the scan chose not to open, `peeked: true` MUST mean every file of that class was READ. **A downstream implementor cannot be enumerated, opened or peeked**; it is in a crate that does not exist yet. No key can name it. **AND THE "CHA ALREADY RESOLVES IT PRECISELY" ARGUMENT IS CIRCULAR** — a 1-implementor public interface is "exact" only under the closed-world assumption this row disputes. The argument that survives is COST, and it is already priced three times: R101 measured CHA-off at 8.8% of rows moving over 256 crates with 11 losing an effect; [[R190]](c) priced 7.02% and was DECLINED; [[R443]] records ~20% Unknown making `deny Unknown` unadoptable. **THE REAL TARGET IS THE CHAIN JOIN, NOT THE IN-CRATE ANALYSIS, and it is measured in both engines.** Unchained, a consumer of a library with a `pub` trait and one pure impl is DISCLOSED — rust `invisible:['a']`, java `invisible:['p']` plus a coverage row. **Chained via the dep report, the disclosure is consumed as an open-world fact and the sin lands: rust reports the consumer ABSENT (certified pure) while its own impl does Fs; java reports `inferred:['Fs']` — an effect the consumer never performs, while the Net it DOES perform stays silent.** That is a fabrication and a silence in one row. java's own `ReportWriter.java:461-480` already says "the consumer's own implementers are, by construction, outside this scan" and publishes the ≤12 union regardless; rust already computes `dispatch_sites` per fn (`collector.rs:305-320`), marked NOT part of the public wire schema. **Leave SPEC §4's bounded-CHA clause alone — do not hedge in-crate, it has been priced and refused three times.** |
| R459 rust: THE MIRROR OF [[R457]] — a `#[cfg(test)] mod X;` file module whose FILENAME does not match the convention is SCANNED AS PRODUCTION, so test code is charged into the report | 2026-09-16 | rust | **FABRICATION, LIVE TODAY, and measured: 78 file instances across 71 crate-versions contributing 286 report rows.** Named instances: **axum charging `Net` from `src/test_helpers/`**, futures-rustls 9 `Net` rows from `src/common/test_stream.rs`, aws-config `Fs`/`Log` from `src/test_case.rs`, snapbox 63, jiff 32, tokio `src/fs/mocks.rs`, hyper `src/mock.rs`, diesel and tower-http `src/test_helpers.rs`. Found by asking the OPPOSITE question to R457 — the same filename heuristic failing in the other direction — which is the audit-boundary rule doing its job | **The authority to close it now EXISTS**: `decls::test_stem_file_is_test_module` already reads the mod site, so this is a matter of asking it for every file rather than only for stem candidates. Two costs to price first, and neither is free: it REMOVES rows by design (so the A/B's REMOVED will be non-zero and each removal must be traced to a body), and it turns a per-candidate mod-site lookup into a per-file one. **Do this AFTER [[R458]]** — R458 is a silence and this is a fabrication, and the family's ranking is unambiguous about which goes first. **FIX WRITTEN AND MEASURED 2026-09-17 (candor-rust `b45a169`, on `night-rust`) — HELD, NOT MERGED, because its own stop condition fired.** A/B over 1,608 crates, clean rebuilds on an idle box, re-run end to end after a machine restart and **identical to the digit**: ADDED 0 · REMOVED 541 · CHANGED 52, reach 135 files / 101 entries; the `--include-tests` arm byte-identical (1,607 entries, 373,506 rows per arm, all zeros). **506 of the 541 removals are rows in correctly-excluded cfg-test files. The other 35 were classified rather than waved through**: 21 rustix `#[test]` fns whose rows existed only via macros defined in a now-excluded file (20 carried `inferred: []`); **12 rustls fns losing a NAME-COLLISION FABRICATION** (`Hash::start` was resolving ring's `digest::Context::new` to the crate's own `verifybench::Context::new`) — removing those is correct; 2 with an inline `#[cfg(test)]` block; **and ONE that moves in the cardinal-sin direction — `rustix-0.38.31 :: io_uring::io_uring_layouts` loses a disclosed `Unknown` and becomes absent**. **THAT ONE ROW IS [[R458]]'s CLASS — an exclusion EXPOSES it, it does not create it**, and R458 is already open and already measured on ratatui-core. So the merge order is forced: **rule on R458 first; closing it removes this change's only disclosed→silent row and the acceptance criterion then holds exactly.** Held on the agent's own recommendation, which is the correct call — a fabrication fix that buys a silence is the trade this register exists to refuse. **R122's `is_cfg_test` inheritance is load-bearing and was MEASURED, not assumed:** axum 0.8.9's `#[cfg(any(test, feature="__private"))]` keeps all 13 rows while 0.7.9's plain `#[cfg(test)]` loses all 15 — and a census the agent wrote for this change, keyed on a regex for "mentions `test`", called BOTH of them test. The engine's own authority got it right. **MERGED 2026-09-17 (candor-rust `b45a169`) — THE HOLD WAS WRONG AND THE ONE ROW WAS MISATTRIBUTED.** I recorded its single disclosed→silent removal as [[R458]]'s class and held the change on it. Checked at source: `rustix-0.38.31 :: io_uring::io_uring_layouts` is a **`#[test] fn`** (`src/io_uring.rs:1427`), and its PRE `Unknown` was `macro:unreadable macro_rules! template check_struct` — `check_struct` lives in `src/check_types.rs`, declared `#[cfg(test)] #[macro_use] pub(crate) mod check_types;` at `lib.rs:154`, a CORRECT exclusion. Both facts verified independently. **So ZERO PRODUCTION FUNCTIONS move disclosed→silent**, and R458's fix would not move this row by a byte. Of the 35 non-excluded-file removals: 21 are [[R167]]'s class (`#[test] fn` scanned as production), 12 remove a real fabrication, 2 are inline `#[cfg(test)]` blocks, and this one. **THE STOP CONDITION I WROTE WAS STRICTER THAN THE PROPERTY THAT MATTERS.** I asked for "REMOVED equals the cfg-test files AND NOTHING ELSE"; the property worth defending is "no PRODUCTION function loses a disclosure". A criterion that over-specifies blocks correct work and, worse, **attributes the block to the wrong cause** — naming this R458's class would have re-filed it as R458 the next time it appeared. The POST absence is a DIFFERENT pre-existing silence: **a macro invocation with no local definition is dropped with no `Unknown` and no coverage entry** (measured in four spellings: bare `mac3!()`, `use zzz::mac; mac!()`, `zzz::mac2!()` — all silent, while `zzz::f()` gives `invisible:['zzz']`). Filed as [[R474]]. |
| R464 java: the Exec masking guard was keyed on an OWNER LIST, not derived from the classification — so a benign `new ProcessBuilder("git")` literal certified `System.load(argv[0])` under `allow Exec git` | 2026-09-17 | java | **LIVE GATE BYPASS, measured on HEAD with one variable: `cmds:["git"]`, `incomplete` ABSENT, `allow Exec git` EXIT 0, "no violations" — while `deny Exec` exits 1 on the same fixture, so the gate could fail. SIX spellings exit 0 pre-fix**: `System.load`, `System.loadLibrary`, `Runtime.load`, `Runtime.loadLibrary`, `Desktop.open(File)`, `Desktop.browse(URI)`. **AND THE BRIEF'S SWEEP LIST WAS MOSTLY WRONG, which is the transferable part.** `Runtime.exec(String)`, `new Socket(String,int)`, `DriverManager.getConnection(String)` and `ResourceBundle.getBundle` are all CAUGHT — Net (4 spellings), Db (2) and Fs (2) are sound on this shape. **The discriminator is not "takes a leading String". It is whether that effect's masking guard is DERIVED from the classification or KEYED ON AN OWNER LIST.** Fs and Net derive; Exec's entire guard was `owner == ProcessBuilder.<init> or owner == Runtime.exec`. **CLOSED, candor-java `fd4199c`** | **THIS RETROSPECTIVELY INVALIDATES [[R409]]'s RECORDED VERDICT** *"Net, Exec and Db are all CAUGHT"* — that was measured only on the two owners INSIDE the guard, so it was an audit boundary drawn around its own trigger, and it read as coverage for weeks. Fixed by stating the Net general rule for Exec (`capturedCmdHere` mirroring `capturedHostHere`) with a **DENYLIST of operand types that provably cannot be a program, derived from a census of all 693 Exec call sites across 324 jars** — not from taste. A rule keyed on "carries arguments" alone marks 41 sites including `pb.directory` and `waitFor(long,TimeUnit)`, which would make `allow Exec <head>` unusable. Priced over 1,181,397 rows: **41,509 CHANGED, 0 ADDED, 0 REMOVED**, the only field moving is `incomplete` and every one GAINED. 677 rows lose certifiability, the largest being JNA — whose benign `/sbin/ldconfig -p` literal was certifying `Native.load` of a caller-chosen library. |
| R465 java: R433's residual was recorded with ONE name and has TWO — `ClassLoader.getResourceAsStream` (the INSTANCE spelling) exits 0 as well as `Class.getResourceAsStream` | 2026-09-17 | java | The row named the static sibling as covered and the instance one as residual; both instance spellings are live. **CLOSED, candor-java `fd4199c`** — and the receiver-kind test R433 said the engine *"does not yet have"* turns out to be an authority the branch ALREADY asks: `PATH_CTOR_OWNERS` is the list of types CONSTRUCTED FROM A PATH, which is exactly what makes a later instance call a use-verb | Census of all 1,138 instance `Fs` calls with a leading String splits cleanly — 452 use-verbs carved out (`FileWriter.write` 449), 686 disclosing. **A row that says a capability is missing, when the codebase already computes it one branch over, is the same false-reassurance class as [[R449]]** — it stops anyone looking. |
| R466 java: `ProcessBuilder.command(...)` REPLACES the constructor's program and no locator MOVE is recorded, so a function that runs `argv[0]` publishes `cmds:["git"]` | 2026-09-17 | java | **A FABRICATED POSITIVE CLAIM, OPEN, and VERIFIED BY EXECUTION rather than by reading** — the same shape printed `MOVED-AND-RAN` from `/bin/echo` while `git` never ran. Not a bypass any more: [[R464]] closes the gate half, so `allow Exec git` now exits 1 on it. What remains is the report asserting a program that is not the one executed | Fix direction is already ruled four-way by [[R419]]'s swift precedent: **a mutating call on a tracked binder is a MOVE, and the locator is withdrawn.** Not done tonight because it is a locator-model change rather than a guard extension, and the gate is no longer bypassable. |
| R460 rust: a spawn whose `Command` arrives as a RECEIVER — parameter, field or conversion — has no `Command::new` in the function for the masking guard to fire on, so a benign sibling literal certifies it | 2026-09-17 | rust | **LIVE GATE BYPASS. `allow Exec git` exits 0 over `fn go(cmd: &mut Command) { Command::new("git").status(); cmd.spawn(); }` — `cmds:['git']`, `incomplete` NONE, and the run prints "nothing hidden — every effect sits where its name says it should".** Calibrated three ways: `deny Exec` → 1, `allow Exec notgit` → 1, the masked fn alone → 1; the ONLY variable producing exit 0 is the benign sibling literal. **THE COORDINATOR'S DIAGNOSIS WAS WRONG AND THE CORRECTION NARROWS THE CLASS**: the builder-chain shape the brief named (`Command::new(argv[0]); c.spawn()`) is ALREADY caught and carries `incomplete:[Exec]`, and most of tokio's 17 `cmds`-less `Exec` entries do too — so D7's recall figure is not this class. **CLOSED, candor-rust `636837c`** | **THE CATCH THAT PAYS FOR IT, ground-truthed from crate source: `aws-config`'s `CredentialProcessProvider::credentials` takes a USER-SUPPLIED command string from the AWS profile, wraps it in `sh -c`, and spawns it — reporting `cmds:['cmd.exe','sh']` with no `incomplete`, so `allow Exec sh` CERTIFIED it.** tokio's own `process::unix::spawn_child(cmd: &mut Command)` is the in-repo instance. A/B over 1,608 crates: **ADDED 0 · REMOVED 0 · CHANGED 52**, `inferred` unmoved on every row, reach 217; all 52 audited, every move a GAIN of `incomplete`. Over-mask priced and accepted rather than carved out: 101 of 217 triggers are `Result`/`Option` combinators mis-typed as `Command`, costing 4 gate-affecting rows — **on a masking guard an under-catch is a broken gate, so no carve-out was added.** This is [[R464]]'s class in a SECOND engine, found independently in the same wave by a different agent. |
| R467 **CLOSED — candor-swift `5fdf481` ("R387/R467: close the search-path gate bypass"), 2026-09-17.** swift: `FileManager.url(forUbiquityContainerIdentifier:)` materialises a destination with no later use site, so like [[R387]] it carries no `incomplete` and a benign sibling literal certifies it | 2026-09-17 | swift | Found by sweeping past R387's trigger rather than stopping at it, measured in the identical posture by the same fixture, and **shipped in the same change (candor-swift `5fdf481`)**. Fail-closed on the flag: only a literal `false` counts as not creating | The distinction that decided BOTH rows, and it inverts R387's own suggested remedy: **a LOOKUP hands back a URL and the general unreadable-locator rule fires at the caller's later write — those rows already carry `incomplete`. A call that CREATES has no later site.** Marking every `FileManager.url(for:)` would have added a redundant second `incomplete` to rows that already disclose, and asserted AS-EFF-008's own words ("reaches a structurally-invisible Fs endpoint") of a call that reaches no endpoint. |
| R468 swift: the `--target` stderr note claimed a scan closure that the `excluded` array then contradicted — a FALSE DISCLOSURE | 2026-09-17 | swift | `--target ArgumentParserEndToEndTests` printed *"scanning 4 target(s) […]. This verdict covers that closure ONLY"* over a report with **0 functions from `Tests/` and 67 `harness-target` exclusions**. Cause is ORDERING, not logic: the note printed the RESOLVER's closure while the harness/test exclusions run after it. **CLOSED, candor-swift `5fdf481`** | Both notes now DERIVE from the files that survived into `sourcePaths`, and **name** the uncovered members rather than omitting them — silence would trade a false statement for a missing one. Verified on SwiftPM and on `.xcodeproj`, each with the mirror control. A derived note cannot drift; a restated one already had. |
| R470 umbrella: a per-engine pin naming a version that exists on crates.io but has NO GitHub release asset degrades `candor update` to a source build SILENTLY | 2026-09-17 | umbrella | **SELF-INFLICTED AND LIVE FOR ~90 MINUTES on 2026-09-17.** `ENGINE_PIN_RUST` was moved to 0.38.4 for a redaction republish; the same variable builds the release-asset URL at `bin/candor:1521`. The crates were published, the release was not, the asset 404'd, and the front door fell back to `cargo install` — reinstating the toolchain requirement v0.38.2 existed to remove. The comment at `bin/candor:1509` (*"the curl simply 404s for an OLDER pin and falls through to cargo, so this needs no version test"*) was true when written and became the hole: a NEWER pin with no assets degrades silently. **CLOSED, candor `97f183a`** | Fixed as a single `rust_asset_url` authority the front door now CALLS rather than retyping, so the check cannot drift from the thing it guards, plus `candor doctor` integration and preflight `[3c]`. **FOUR states, and the third is the point: PASS / FAIL / INCOMPLETE (network error) / SKIP (no prebuilt for this platform) — "could not reach GitHub" and "the asset is not there" must never collapse.** Calibrated against real history including the sharpest case: **0.38.1 is a REAL published crate with NO release asset**, and the guard returns 1 on it — the exact shape that got past everything. |
| R473 family: a CROSS-REPO SHA PIN orphaned by a history rewrite keeps working — `git checkout` of an unreachable commit SUCCEEDS, so running the production step is not a test that the pin is sound | 2026-09-17 | family | **FOUND BY CI, NOT BY EITHER SWEEP. Self-inflicted by the 2026-09-16 client-redaction rewrite.** candor-swift's CI and release workflows pin candor-spec to a SHA; the rewrite orphaned it and every run since died with `fatal: reference is not a tree`. **The dangerous sibling was silent**: `adopt/candor.yml` — the workflow ADOPTERS COPY INTO THEIR OWN REPOS — fetched `candor-sarif` from raw.githubusercontent at an orphaned SHA and **still returned HTTP 200**, so every adopter's CI was working on borrowed time with nothing on our side to correlate a future failure against. MEASURED, and every obvious check passes on a dead pin: `cat-file -t` says dead, but **`curl` returns 200 AND `git clone --filter=blob:none` + `git checkout <dead-sha>` returns rc=0 with HEAD on the dead commit.** Both pins fixed, remapped through `.git/filter-repo/commit-map`, content verified before swapping | **THE ONLY DISCRIMINATOR IS REACHABILITY FROM A REF: `git for-each-ref --contains <sha> \| wc -l` — 0 means orphaned.** Verified both directions (orphaned 0, repinned 10) and it works on a `--filter=blob:none` clone, so the guard costs two lines in a job that was already cloning; no mirror needed. Now armed in candor-swift's ci.yml (×2) and release.yml, calibrated to FIRE on the orphaned pin and pass on the live one. **The class rule: a pin is not a ref, so `--mirror` does not list it, and it is not an artifact, so an artifact sweep misses it — it survives every check because it still looks like a perfectly good 40-hex string. Enumerate what POINTS AT a rewritten repo, including repos never rewritten themselves: candor-swift was clean in every sweep and is the one that broke.** |
| R474 rust: a MACRO INVOCATION WITH NO LOCAL DEFINITION is dropped with no `Unknown`, no coverage entry and no `invisible` — the expansion could do anything and the report says nothing | 2026-09-17 | rust | **SILENT, PRE-EXISTING, and found while re-auditing [[R459]]'s single unexplained removal.** Measured in FOUR spellings, all silent: a bare `mac3!()` with no definition anywhere; `use zzz::mac; mac!()`; and `zzz::mac2!()` fully qualified — while an ordinary `zzz::f()` in the same crate correctly yields `invisible: ['zzz']`. So the engine discloses an unreadable external FUNCTION and says nothing at all about an unreadable external MACRO, whose expansion is arbitrary code | `candor-scan --agents` has **zero** mentions of "macro", while `README.md:29` states macros are handled — the [[R445]]/[[R449]] false-reassurance shape again, in the document a user consults to decide whether to trust a report. The disclosure vocabulary already exists (`macro:module items hidden by an unexpanded macro`) and is reached only when a LOCAL definition is macro-hidden; an absent definition takes a different path and reaches nothing. Fix direction: the `invisible` route that already fires for an external function. **THE PREMISE DID NOT SURVIVE MEASUREMENT — REFUSED 2026-09-17, and this row was WRONG as I filed it.** Re-measured one spelling per crate with `zzz` declared in `[dependencies]`: `use zzz::mac; mac!()`, `zzz::mac2!()`, expression-position `zzz::mac4!()` and `let _ = zzz::mk!()` **ALL yield `invisible:['zzz']` plus `coverage.uncovered zzz`** — identical to the ordinary `zzz::f()` control. **The variable that decides is whether the crate is declared in `Cargo.toml`**: with no `[dependencies]` section EVERYTHING goes silent, including the ordinary-function control — so the original fixture almost certainly omitted it, and its control would have been silent too. `expand()` resolves a macro path through `uses`, which is exactly why those spellings work. A bare `mac3!()` with no definition anywhere does not compile. **WHAT IS GENUINELY SILENT is a narrower set**: `#[macro_use] extern crate zzz; mac!()`, `use zzz::*; mac!()`, attribute macros (`#[zzz::instrument]`) and derive macros — the spellings that do NOT go through `uses`. **Priced and declined**: `macro_use` appears in **9 of 1,608** registry crates and an external-rooted glob in ~19, and a fix needs a std-builtin denylist or it attributes `println!` to a glob-imported crate. The agent judged that a poor trade against polluting its A/B's attribution and left it unfixed, which is the right call. **I filed this row from another agent's prose rather than from its numbers — the failure mode this register already records twice.** `README.md:29` is an overclaim only at those edges. |
| R475 **CORRECTED 2026-09-17 — as first filed this row was wrong in three places.** The real defect is a TOGGLE: a library whose public abstraction has ZERO local implementors gives a chained consumer a disclosed `Unknown`; add ONE PURE implementor to that library and the consumer is SILENTLY CERTIFIED PURE. That is [[R72]]'s toggle, which R72 proved rust cannot host in-crate, instantiated across the chain in BOTH engines | 2026-09-17 | all four (measured identical) | **LIVE ON REAL CODE, not a fixture.** `ratatui-core`'s `Terminal::size` dispatches `Backend::size` over its sole local impl `TestBackend` (pure); `ratatui-crossterm`'s `CrosstermBackend::size` is `['Ipc']`. An app chained onto BOTH reports `app_size` ABSENT, and `deny Ipc app_size` and `pure app_size` BOTH exit 0. Unchained it reports `app_size []` plus `invisible:[ratatui_core, ratatui_crossterm]`, which also exits 0 because invisible is non-gating ([[R133]]). **So chaining does not flip a gate — it DELETES the disclosure that R133's ruling depends on being present.** Note the effectful impl lives in a THIRD crate, neither the dispatching dep nor the consumer, so any fix must reach across all three | **THREE CORRECTIONS TO THE ORIGINAL FILING, all mine.** (1) *The java/rust contrast was FIXTURE-INDUCED, not an engine difference* — the rust arm's dep impl was pure and the java arm's effectful; run the cross arms and all twelve cells match, `deny Net app_freefn` exits 0 on both. (2) *The fabrication half is RETRACTED*: the consumer's inherited `['Fs']` is the bounded-CHA over-approximation `SPEC.md:4318-4320` explicitly licenses, inherited unchanged, and rust does the same — it is NOT a distinct fabrication and NOT independently fixable, since removing it leaves [[R101]]'s retracted shape, silence rather than truth. The one sin is the silent OWN effect, identical in both engines. (3) *The cited java mechanism was the wrong code*: `ReportWriter.java:461-480` sits inside `appendInterfaceUnions`, which returns immediately unless `workspaceChain()`, so it never ran in the flag-off arm and the flag-on arm gave the same answer; the `Fs` arrives via the ORDINARY join at `Candor.java:5985-6019`. I read that comment as *states-the-premise-and-publishes-regardless* when it is the producer REFUSING to copy the `closedWorldResolvable` term — **the opposite of what I filed.** Root cause of all three: I re-imported R101's ORIGINAL, RETRACTED framing through the java fixture instead of re-deriving it. **NEITHER HALF IS A PLAIN DEFECT — both engines conform** (`SPEC.md:4318-4320` names the downstream implementor as the accepted under-approximation, and chaining rule 3 at `SPEC.md:1624-1628` makes absence a purity claim modulo the producer's §4 standing), so the remedy is a TIER-1 ADDITIVE RUNG in the 0.23 shape, in three inseparable parts: (a) the producer marks rows `dispatchesOn: ["Backend::size"]` transitively and EVEN WHEN THE ROW IS OTHERWISE PURE; (b) a crate implementing a FOREIGN trait emits an `interfaceUnion` entry keyed under the trait's crate — today both engines' producers cover LOCAL traits only, and without this the only real-world instance measured is missed; (c) the consumer join unions per key its own impls plus every chained entry, which ⟨0.25⟩'s ambiguous-key union rule already handles. **PRICED over 1,608 crates / 694,497 functions:** producer side is WIRE BYTES ONLY, no verdict changes — 7,908 functions (1.14%) carry the key directly, 18,034 (2.60%) transitively, of which 3,885 are pure today and become new rows (+1.3% rows); consumer side is confined to at most 13 crates (0.8%) that implement a dep's trait effectfully, and only by ADDING a real effect, with NOBODY hedging. **The alternative the family keeps re-pricing — hedge `Unknown` in-crate — costs 2.60% of functions across 435 libraries (x11rb 70%, rustls 20%) for every consumer including those with no foreign impl; the union design dominates it. The `pub`-scoped hedge had never been priced; now it has, and it still loses.** A consumer-only *escaping-value* rule was considered and REFUSED: both engines charge a closure by SYNTACTIC CONTAINMENT (`libk::keep(\|\| net)` yields `['Net']` even though `keep` never calls it) and charge a passed trait impl nothing, so extending containment to a foreign body would be a minted edge, which `SPEC.md:4256-4262` lists as fabrication **PINNED FOUR-WAY 2026-09-17 by conformance PART 92** (`conformance/gen_chained_dispatch.py`): a THREE-package chained differential — abstraction+dispatcher, a SECOND package implementing that FOREIGN abstraction effectfully, a consumer chained onto both — with the consumer's function under test BYTE-IDENTICAL source across the toggle's arms, asserted by the generator rather than trusted, because the fixture-induced cross is the mistake that produced all three corrections above. Measured on RELEASED artifacts only (candor-scan 0.38.4, candor-java 0.38.3, candor-ts 0.38.3, candor-swift 0.38.3; never a working tree — two engine trees were dirty at the time). **THE TOGGLE IS NOT rust+java, IT IS FOUR-WAY AND IDENTICAL**: all four report the consumer ABSENT on the foreign-effectful arm (four declared (arm, engine) XFAILs, and a PASSING xfail is a FAILURE there, so the first engine to port ⟨0.39⟩ retires its own line), while all four correctly keep the zero-implementor `Unknown`, stay pure over an only-pure library, keep a SEALED union exact with no new hedge, and disclose via `invisible` when unchained. The producer side has no engine at all: `dispatchesOn` absent and `interfaceUnion` null in every producer report, four ways, so obligations 1 and 2 are unimplemented everywhere and are recorded in `conformance/must-ledger.json` as a debt with a named remedy — the contract does not yet fix the key SPELLING outside rust, so a wire-shape row written today would invent the contract rather than pin it. Calibrated by fault hook: rendering the zero-impl arm's DEPENDENCY with the one-pure-implementor variant reddens all four engines (exit 1), which is what proves the chained dependency's CONTENT reached the consumer's scan. CLOSED 2026-09-20 — ported in ALL FOUR engines: candor-rust df4cf3f + 69dd565, candor-java b2a3feb, candor-swift 9ae9ea5 + 68f89a0, candor-ts ee844f0. PART 92's (arm, engine) xfail table is now EMPTY, every line retired by the engine that earned it, and four-way conformance is green across all 24 cells of its six arms. |
| R476 rust: [[R243]]'s rust half is WORSE than [[R458]] was — with the trait bound on the `impl` block rather than the struct, a generic FIELD receiver is silent at EVERY implementor count, including zero | 2026-09-17 | rust | **SILENT, and it does not degrade gracefully the way bounded CHA is supposed to.** Measured: with the bound on the impl block, `Terminal::dims` returns `[]` with NO `Unknown` at 2 implementors (one of them `Fs`), and also at **0** implementors — where the engine's own contract says it must hedge. With the bound on the STRUCT instead, the same program resolves or hedges correctly. **ratatui-core reproduces R458's shape only because its `where B: Backend` happens to sit on the struct** (`terminal.rs:398-400`) | So the row everyone was looking at was the FORGIVING spelling. The 0-implementor case is the tell: `dispatch_calls_for_trait_method` hedges on an empty candidate set by design, so reaching `[]` with no `Unknown` means the dispatch machinery was never entered at all — a resolution gap, not a CHA bound decision. Fix this before anything in the R101/[[R475]] chain-join family: **an in-crate silence at zero candidates is unambiguous, while the chain-join question needs a spec ruling.** **CLOSED 2026-09-17, candor-rust `f0065ad`, and the matrix was measured BEFORE anything changed.** Six programs, all `cargo build`-ed first — bound-on-impl vs bound-on-struct × 0/1/2 implementors. Bound-on-impl was ABSENT at 0, 1 and 2; bound-on-struct gave `Unknown` at 0, ABSENT at 1 and `['Fs']` at 2. **One correction to my framing: at ONE implementor both spellings are legitimately absent** (the sole impl is pure), so only the 0 and 2 cells were defects. **CAUSE: the index was never built.** `collect_decls` built `trait_fields` from `generic_bounds_of_generics(&s.generics)` — the STRUCT's own generics — so a bound on the impl block was invisible, the field never entered the index, and `dispatch_calls_for_trait_method` was never called. That is why zero candidates were silent too, which was the tell. A/B over 1,608 crates: **ADDED 3,292 · REMOVED 4 · CHANGED 1,049**, reach 3,648 joins across 407 entries; of the additions 2,446 are `Unknown`-only, 698 present-but-pure and **33 carry a concrete effect** — three ground-truthed against source (jiff `ConcatenatedTzif::available` gains a real `Fs`; tower `ConcurrencyLimit::load` gains `Log`; rayon `Chain::len` gains `Unknown` at 52 implementors, which is SPEC §4's mandate working). **[[R243]]'s rust half is closed with it** — rayon `ForEachConsumer::consume_iter`, the function R243 names, goes ABSENT → `Unknown`. **THE FIRST CUT INTRODUCED A CARDINAL SIN AND THE A/B CAUGHT IT — 12 rows.** Removing *whatever* the shadowing `fields` entry happened to be took tokio's `watch::Receiver::borrow`, `borrow_and_update` and `Sender::borrow` from a disclosed `Unknown` to ABSENT, because `trait_leaves` PEELS A WRAPPER: `value: RwLock<T>` answers the probe while its `fields` entry names a real type. The removal is now confined to an entry that is the parameter's OWN NAME, with a regression control that fails on the broad version. **And the fix was INERT until that shadowing entry went with it** — the first post-build matrix was byte-identical to pre-fix and only the reach counter proved the join had fired, which is `feedback-prove-the-fixture-reaches-the-code` catching a real inert cut. All 4 REMOVED audited to one mechanism (a phantom `<param>::method` path colliding with a real local leaf, in diesel and tonic); **zero rows lost `incomplete`, `declared`, `netClass` or any concrete effect.** Cache rev30 → rev31, with an EXECUTED reproduction of the un-bumped case. |
| R477 java: [[R464]] closed the ARGUMENT form of the Exec masking guard and left the RECEIVER form OPEN — `allow Exec git` still exits 0 over `f(ProcessBuilder b)` with a benign sibling literal | 2026-09-17 | java | **LIVE GATE BYPASS IN THE REFERENCE ENGINE, and PART 91 found it on its FIRST EXECUTION.** Reproduced independently of the harness on candor-java `fd4199c`: `public static void f(ProcessBuilder b) { new ProcessBuilder("git").start(); b.start(); }` reports `inferred:['Exec']`, **`cmds:['git']`, `incomplete: None`**, and the gate prints "candor-java: no violations" at **exit 0** over a caller-supplied spawn. R464 fixed the same class for the argument form the same day and this spelling was never measured | **THIS IS EXACTLY WHY THE PART EXISTS, and it repeats PART 88's history: a4local caught [[R416]] on ITS first execution too.** The rust twin of this ([[R460]]) WAS closed — rust's fix keys on the receiver — so the family had one engine right and one wrong with nothing cross-engine to say so. **Declared as an XFAIL keyed on (arm, engine) rather than left red**, because the arm is correct and the engine is not; a PASSING xfail is itself a FAILURE here, so the entry retires itself the moment java is fixed. Fix direction is already proven next door: extend R464's DERIVED boundary to the receiver position, never a second owner list — the owner-list keying is what R464 removed in the first place. **CLOSED 2026-09-17, candor-java `5440749`, and verified independently of the harness: the same fixture now reports `incomplete:['Exec']` and `allow Exec git` exits 1 where it exited 0.** The locator is read from the DESCRIPTOR, mirroring `fsLocatorDetermined`: an `Exec` call's receiver could be carrying the program unless it is a handle to an ALREADY-RUNNING process. **A denylist (`EXEC_LAUNCHED_HANDLE_TYPES` = `Process`, `ProcessHandle`), not a second owner list — so a spawner nobody listed DISCLOSES rather than slipping through**, which is the direction R464 established. Derived from a census of **882 Exec call sites across 242 jars**: of 380 argument-less instance calls, 367 are handle verbs and 13 are program-carrying receivers that launch. PART 91's `(e2recv, java)` xfail retired in the same change, which is what that mechanism is for. **THE CORRECTION IS WORTH MORE THAN THE FIX: determinedness cannot be `newType`.** The first cut read it and **[[R464]]'s own over-charge control went red on the first test run** — `new ProcessBuilder("git","status").redirectErrorStream(true).start()`, the chained-builder idiom R464 had already measured as making `allow Exec` unusable if marked. That forced a separate `ProvValue.allocChain` ("built by THIS method", surviving a store/load and a self-returning builder step), kept distinct from `newType` because `newType` must NOT survive a call: a `T`-returning method may return a subtype, and narrowing CHA to `T` would skip the override. A/B over 236 jars / 715,774 rows: **ADDED 0 · REMOVED 0 · CHANGED 31**, `inferred` unmoved, all 31 audited and every one a GAIN of `incomplete`. **Cost stated honestly: 0 of the 31 were certifiable before** — on that corpus the fix is added disclosure only, because no function there both spawns a foreign builder and carries a benign literal. |
| R478 rust: the ELEMENT route has the identical impl-block blind spot as [[R476]] — `field_elem_trait` is built from the struct's own generics and has no join, so a container field is silent at EVERY implementor count | 2026-09-17 | rust | **SILENT, measured on a compiled 8-spelling fixture while closing R476.** With the bound on the impl block: `Vec<B>` reached by index (`self.bs[0].size()`) or by for-loop, and `Option<B>` reached through an `if let Some(x) = &self.b` binder OR a direct `as_ref().unwrap()` chain, are all silent whatever the implementor count | Same cause as R476 one route over, and it is the [[R347]] §G shape again: **two resolvers that answer one question, fixed in one and not the other.** R476 joined impl-block bounds into `trait_fields`; `field_elem_trait` needs the same join. Note one cell behaved differently under R476's two candidate removals (the `Option` direct-chain worked under the broad removal and not the narrow one, because its `fields` entry is `Option`, not the param name) — so the element route's fix must be priced on its own, not assumed to follow. **CLOSED 2026-09-17, candor-rust `e7dbcc1`.** `field_elem_trait` now carries the same `<gen-field>` key space as `trait_fields`, and `resolve_impl_bound_fields` joins BOTH routes from ONE bounds half — the impl blocks record the bound once. Index, for-loop and `if let` spellings all fixed. Pre-fix matrix measured first, compiled: **13 of 14 shapes were ABSENT in all three implementor cells**. |
| R479 rust: a TUPLE-STRUCT position has no general `trait_fields` route at all — `struct TupT<B>(pub B)` with `impl<B: Backend>` and `self.0.size()` is silent | 2026-09-17 | rust | **SILENT, PRE-EXISTING (it predates [[R476]]), reproduced on a compiled fixture.** `decls.rs`'s `Fields::Unnamed` arm records only [[R238]]'s callable answer, deliberately — so a tuple position never enters the dispatch index by any route | Distinct from [[R478]]: that one is a missing JOIN on an existing route, this is a missing ROUTE. The deliberate narrowness of the `Fields::Unnamed` arm is recorded in the code, which is why this survived — **a comment stating why a branch is narrow reads as a decision and stops anyone asking whether the narrowness is still right** ([[R445]]/[[R449]]'s shape). Price it with R478; they share a fixture. **CLOSED 2026-09-17, candor-rust `e7dbcc1`, AND IT WAS WIDER THAN THIS ROW.** The missing `Fields::Unnamed` arm also silenced `struct T(pub Box<dyn Backend>)` — **a newtype over a TRAIT OBJECT, with no generics and no bound anywhere**, so the row's framing as a generic-position problem was too narrow. That spelling has nothing to do with bound placement. |
| R482 rust: a `field_elem` entry naming the struct's OWN generic parameter shadows the element route — `resolve_recv_type`'s `Index` arm asks `field_elem` and returns early | 2026-09-17 | rust | **SILENT, and NOT a bound-placement bug — which is what makes it a separate row from [[R478]].** `struct VecIdxS<B: Backend>` — the bound on the STRUCT, the FORGIVING spelling that [[R476]] established resolves correctly — was absent at every implementor count via `self.bs[0]`, while its for-loop sibling in the same struct charged `['Fs']`. So one container, two access spellings, opposite answers. **CLOSED in the same commit, candor-rust `e7dbcc1`** | Found by building the 14-shape matrix rather than by testing the shapes the row named — the same reason [[R479]] turned out to cover a newtype over a trait object. `Vec<B>` yields the `field_elem` string `"B"`, the parameter's own name, which is exactly the shadowing class R476 narrowed its removal to avoid. |
| R483 rust: `T: ?Sized` is the REMOVAL of a bound and `bound_leaves` recorded it AS one, so `["Sized"]` displaced the concrete `fields` entry | 2026-09-17 | rust | **PRE-EXISTING for named fields and found by reading the A/B's REMOVED column rather than its ADDED one.** The fixture's `PlHold::go` really reads a file and is ABSENT at every implementor count on the PRE-fix binary. **The agent's first cut of [[R479]] then handed the same leaf the same power one index over: ADDED 759 / REMOVED 19, of which 13 were cardinal-sin** — tokio `parking_lot::Mutex::{lock,try_lock,get_mut}`, async-lock's `MutexGuard::drop` family, regex-lite, fancy-regex. **CLOSED, candor-rust `e7dbcc1`** | This is the second time in two days that a fix in this area introduced the class it was closing and only the REMOVED audit caught it ([[R476]]'s first cut cost 12 rows the same way). **A marker trait that REMOVES a constraint must not be read as one that adds it** — `?Sized`, and by inspection the same question should be asked of any future opt-out bound. Final A/B: ADDED 832 · REMOVED 6 · CHANGED 230, all six removals traced to ONE body (diesel names a generic parameter `Statement` while also having a `struct Statement` with `impl Drop`; `owned_drops` is leaf-keyed, so a phantom entry made `WithCacheStrategy` own a Drop type it does not own and R182 hedged `Unknown` on every ambiguous `default()` in the crate — `i32::default()` is provably pure, so this is a FABRICATED hedge being withdrawn). |
| R484 rust: `self.b.as_ref().unwrap().size()` on an impl-block-bounded `Option<B>` is still silent — the chain types `self.b` from `fields`, whose entry names the WRAPPER (`"Option"`) rather than the parameter | 2026-09-17 | rust | **MEASURED RESIDUAL, PINNED RATHER THAN OMITTED, and deliberately not fixed in `e7dbcc1`.** Its `if let`, `let`-rebind, `.map()` and struct-bound spellings all charge; only the direct `as_ref().unwrap()` chain is silent, because [[R476]]'s removal is narrowed to entries that are the parameter's OWN name and this one is not | **Widening that removal is precisely the cut that cost R476 twelve rows in the cardinal-sin direction**, so it needs its own A/B and its own pricing — which is why it is a row and not a follow-on commit. The reporting agent declined to invent a number for it, correctly. Adjacent and separate: `self.bs.iter().next().unwrap().size()` is silent in BOTH bound spellings and on the pre-fix binary too — a different, older adapter-chain gap that should be measured before either is touched. |
| R480 java: **the whole-type-plus-denylist fix for `java.lang.ProcessBuilder` was never generalised to THIRD-PARTY subprocess builders, which remain on the verb allowlist that `Classifier.java:353` records as MEASURED to under-report silently.** A builder configured HERE and launched elsewhere slips both masking branches: `new ProcessExecutor().command(userCmd).execute()` (zt-exec), `new GenericContainer(img).withCommand(userCmd).start()` (testcontainers), commons-exec `DefaultExecutor` + `CommandLine`, and im4java `IMOperation`/`*Cmd` | 2026-09-17 | java | **LIVE GATE BYPASS, measured BEFORE AND AFTER [[R477]]'s fix — it is unaffected by it.** Each exits 0 under `allow Exec git` beside a benign literal, exits 1 with the sibling removed, and `deny Exec` exits 1 on both. Signatures verified with `javap` against the real corpus jars, so the stub fixtures match the SHIPPED APIs rather than a guess at them | **This is a CLASSIFIER gap, not a guard gap, and that distinction is the fix direction**: neither `ProcessExecutor.command` nor `GenericContainer.withCommand` is charged `Exec` at all, so no masking branch can reach them — the guard is working correctly over a call it never sees as a spawn. Found by the reporting agent measuring the residual its OWN fix comment named, rather than asserting it. Note the population: both are widely-used test/process libraries, so this is not an exotic spelling. **AUDIT BOUNDARY WIDENED 2026-09-17 — I filed this from the two instances I was handed; a sweep of all 10 owner-scoped `Exec` rules found FOUR payload-carrying types, not two, and TWO payload carriers absent from the classifier entirely: `org.apache.commons.exec.CommandLine` and `org.im4java.core.IMOperation`/`ImageCommand`. The program-setting verbs `.commandSplit`, `.withCommand`, `.addArgument` and `.addImage` are NEVER named anywhere in `Classifier.java`; `.command` and `.parse` ARE named but for unrelated owners (`ProcessHandle$Info.command` is an `Env` rule), so an owner-blind grep would have reported them covered. The remaining six Exec types are single-shot calls that carry no payload across calls and are NOT in the class. `Classifier.java:1247` states the gap as deliberate — *"The setX config setters stay pure (verb-gated)"* — which is the documented-limitation trap: it reads as CONSIDERED, so it was never measured against the ProcessBuilder doctrine 900 lines above that contradicts it. **CROSS-ENGINE SWEEP 2026-09-17 (static read, no build — an engine was mid-measurement): JAVA IS THE LONE OUTLIER, in the same direction as the original defect its own comment describes.** rust charges at CONSTRUCTION (`std::process::Command::new` is itself `Exec`), and every rust/`duct` spelling names the program there, so a split build/launch is already caught; ts models `execa` as a whole-module κ rule with a NULL member-regex, i.e. every member charged; swift charges CONFIGURING (`t.arguments = argv` / `t.executableURL = u` on a `Process`, `CallCollector.swift:1430`) and models `ShellOut`/`Subprocess`. The java-specific shape is the EMPTY CONSTRUCTOR — `new ProcessExecutor()` names no program, so charging construction cannot catch it and only the config verb can. `Classifier.java:363` says of the FIRST version of this bug *"java was the lone outlier"*; the fix made java match for the JDK type and left it the outlier for third-party ones. Not claimed: that the other three are COMPLETE over their own third-party ecosystems — only that none of them uses the verb-allowlist shape for the types it does model. **FIXED candor-java `1e98b2d`, and the row's FRAMING IS CORRECTED: this was a DISCLOSED under-report, not a cardinal sin.** None of the four packages is in `Rules.KAPPA_COVERED_PREFIXES`, so every floored call carried `invisible: ["org.zeroturnaround.exec"]` and the "classifier doesn't cover N packages" advisory — the honest floor working as designed. The LIVE GATE BYPASS is real and is why this had to be fixed (13 arm-only methods, all `inferred: []`, and ALL FOUR policy forms exit 0 including `deny Unknown`), but absence here was never an unqualified purity claim. Fix is whole-type `Exec` plus a named pure denylist; three of the four are whole-PACKAGE, which also closes a spelling an owner-equals rule can never see — a call on `PostgreSQLContainer` or `DaemonExecutor` emits THAT owner, and the supertype walk that rescues a project subclass cannot see an unscanned library one. A/B over 325 jars: ADDED 191, **REMOVED 0**, CHANGED 614 — all 614 audited rather than sampled, and no effect-bearing field lost anything but `Unknown`. The consumer control (compiled against the real jars, not the libraries scanning themselves) is what caught two over-charges before shipping: `container.getMappedPort(6379)` and a closed pure enum's `toDockerNotation()`. `Classifier.java:1247`'s *"setX config setters stay pure"* sentence is deleted and replaced by the measurement |
| R485 rust: the ZERO-IMPLEMENTOR dispatch case is disclosed under the WRONG REASON CLASS — rust says `callback:unresolved call` where java says `dispatch:p.Store.put`, and §4's vocabulary makes java's the correct one: nothing was passed as a callback | 2026-09-17 | rust | **DISCLOSED, NOT SILENT — this is a vocabulary defect, not a soundness one, and is filed so it is not mistaken for one.** Found while settling [[R475]], on the 0-impl arm of the same 3x2x2 matrix: both engines correctly return `['Unknown']` and both gate closed, so no verdict differs. `collector.rs:1785-1810` sets `unresolved = true` with NO reason attached, and the `callback:` class is then supplied downstream by default | **Why it matters despite costing no verdict: `deny E Unknown[class]` is REASON-SCOPED** (the ⟨0.24⟩/⟨0.25⟩ rung), so a user writing `deny Exec Unknown[dispatch]` to catch exactly this shape gets a PASS on rust and a FAIL on java for identical code. A cross-engine reason divergence is invisible to every gate that does not scope by class, which is why it survived. Fix is a reason on the `unresolved` path, not a behaviour change **CLOSED candor-rust `acc808f`/`7fdb241`, pushed, CI green on all four workflows. The audit found FIVE wrong sites of eight, not the one I handed over.** `grep` for `self.unresolved = true` found 8 writers: the row's own site plus `charge_stringify_bound` (>12 impls) became `dispatch:`, and THREE became `ambiguous:` rather than `dispatch:` — an ambiguous local trait leaf and two R90 variant-leaf collisions — because when two distinct local traits share a leaf **no owner can be formed**, which is §4 ⟨0.24⟩'s `ambiguous:`, not a dispatch. Three genuine callback sites were correctly left alone. **§4 was verified by READING the clause, not by copying java**: its table licenses `dispatch:` for an unresolved dispatch with a resolvable owner type and member, and its dividing line is whether that owner exists. `mark_unresolved` is now the single writer and EVERY site passes a reason including the three correct ones — recording only at the corrected sites would have withdrawn `callback:` from a body hitting both kinds and silently narrowed `deny E Unknown[indirect]`. A/B over 1,608 crates / 308,268 rows per arm: **inferred key ADDED 0 REMOVED 0 CHANGED 0** — provably inert on effects — with 12,745 rows differing in `unknownWhy` ALONE, all 12,417 in the withdrawal direction audited in full: 0 lost a reason, 0 lists shrank, 0 emptied. Gate-level over 200 crates `deny Unknown` 149 → 149 |
| R486 java: **the SAME class one owner over, and SILENT rather than disclosed — `scala.sys.process`.** `scala.sys.process.Process$.apply(String)` classifies to `null` while `ProcessBuilder.run` is charged `Exec`, and `scala` IS in `Rules.KAPPA_COVERED_PREFIXES` — so the floored call is not ledgered at all | 2026-09-17 | java | **CARDINAL SIN, and strictly worse than [[R480]] which it otherwise mirrors exactly.** Because the package is declared covered, an arm-only function reports `0 functions reach effects` with `coverage: null` — **no `invisible`, no advisory, nothing** — and `deny Exec` exits 0. R480's four packages at least floored to a disclosure; this one claims coverage it does not have. **The coverage-prefix list is therefore a SECOND, independent way to lose a call, and it converts a disclosed under-report into a silent one** | MEASURED 2026-09-17 with a faithful owner/method stub read off the real library signatures, **NOT yet executed against real scalac output** — that is the one gap between this row and R480's evidence, and it is the first thing to close. Found by the R480 agent sweeping for the class rather than stopping at the instance it was handed, which is the audit-boundary rule paying out twice in one day **STATIC CONFIRMATION 2026-09-17 (coordinator, read-only, no build): the rule is at `Classifier.java:2290` and it is PREFIX-scoped but VERB-GATED** — `owner.startsWith("scala.sys.process")` AND method in `run` / `$bang*` / `lazyLines*` / `lineStream*`. So the launch verbs are charged and `Process$.apply(String)`, the constructor that CARRIES THE PROGRAM, is not. That is [[R480]]'s defect verbatim — a payload-carrying member uncharged because only the launch verb is enumerated — living in the language-stdlib rule rather than a third-party one, which is why the covered-prefix list applies to it and makes it silent. The evidence gap is now narrowed to END-TO-END execution against real scalac output; the rule's shape itself no longer rests on the stub **CLOSED candor-java `101aa67`, pushed, CI green. CONFIRMED END-TO-END against real `scalac 3.8.4` — `javap` shows scalac emits `scala/sys/process/Process$.apply`, so the stub was faithful — AND IT IS WORSE THAN THIS ROW SAID: the six arm methods are ABSENT FROM `functions` ENTIRELY, not merely `inferred: []`, which under ⟨0.21⟩ is a CERTIFIED PURITY CLAIM.** All four policy forms exit 0. Calibrated on the same tree: adding `Process("id").!` turns `deny Exec` red, so the exit 0 is an under-report and not a broken scan. The process-composition operators `#\|`, `#&&`, `#\|\|`, `###`, `#>`, `#<` were all uncharged for the same reason — the allowlist never named them. **One correction for future readers: in a MIXED class the arm methods DO appear carrying `declared: ["Exec"]`. That is the §5 class-capability envelope (`Candor.java:4014`), NOT a disclosure of the function's effects, and must not be read as one** |
| R487 java: `net.bramp.ffmpeg` — the R480 shape again, DISCLOSED. `FFmpegBuilder.setInput`/`addOutput` and `FFmpegExecutor.createJob` all classify to `null` with only `*.run` charged | 2026-09-17 | java | Same payload-carrying-builder shape as [[R480]]: the program and arguments are assembled by verbs the classifier never sees as a spawn. `net.bramp` is NOT in `KAPPA_COVERED_PREFIXES`, so unlike [[R486]] this floors to `invisible` and is disclosed | Lower priority than R486 for exactly that reason — a disclosed under-report with a gate bypass, not a sin. Closes with the same whole-type-plus-denylist treatment R480 used; the denylist needs the pure builder read-backs enumerated with `javap` first, which is how R480's two over-charges were caught **CLOSED candor-java `101aa67`. The sweep found more than this row stated: `FFprobe.probe`, `FFcommon.version`, `FFmpeg.codecs/formats/pixelFormats/isFFmpeg` and `FFcommon.path(List)` all FORK the binary and none is named `run` — and `javap -c` shows `FFmpeg.<init>(String,ProcessFunction)` ends in `invokevirtual version()`, so `new FFmpeg(path)` ITSELF FORKS.** Only a whole-type rule catches a forking constructor. Deliberately NOT whole-package: `net.bramp.ffmpeg` is mixed, half of it a media-metadata model, so the rule names `builder`/`job` plus the six forking types with six closed pure enums carved out by name after `javap` |
| R488 **`candor-query`'s frontier arm returned `possibleViaUnknownDispatch: []` for EVERY candor-scan report — a §3.1 FALSE ALL-CLEAR — because it split quals on the last `.` and a rust qual (`Impl::op`) has none** | 2026-09-17 | rust (candor-query) | **FOUND AND FIXED IN THE SAME ROUND AS [[R485]], and it is the fix-makes-it-reachable pattern rather than a fix-introduces-the-class one.** It was UNREACHABLE before: rust's only dispatch reason was the dot-free spelling, which takes the over-list branch first. R485 made the dotted path rust's normal case and exposed it. **The assumption was written down in the very file that would have caught it** — *"Names are dot-separated (the swift/JVM report shape this arm serves)"* — which is the documented-limitation trap: a comment stating a premise reads as CONSIDERED and stops it being measured. Measured `[]` → `[{fn: dispatcher, viaDispatchOn: op}]` | Filed under a real ID because the agent called it `R485b` in its commits; that alias appears in candor-rust `acc808f`/`7fdb241` and means THIS row. Pinned by `..._colon_separated_quals`, the only one of ten `callers_include_unknown*` tests that reddens on revert |
| R489 candor-scan emits `macro:` reason kinds that are OUTSIDE §4's closed set of five, in four spellings, on real code | 2026-09-17 | rust | **MEASURED LIVE, 54 rows in the 1,608-crate A/B — not a fixture artefact.** Sound in effect: they hit `ReasonClass`'s `unresolved` catch-all, so `Unknown[*]` and `Unknown[dynamic]` still bite and no gate is bypassed. But the producer is NON-CONFORMING to §4's vocabulary, and **PART 10 cannot see it because no fixture makes one** — the same blind spot that let [[R485]] survive, one vocabulary over | Not a soundness defect; a conformance one. The remedy is a fixture that PRODUCES the kind, then either admit `macro:` to §4's set or map it to an existing kind. Do not close by widening PART 10's acceptance without a fixture, or the part passes for an engine that emits anything **MY FIGURE IN THIS ROW WAS WRONG BY TWO ORDERS OF MAGNITUDE: I wrote *"54 rows"*, which was the CHANGED count from one A/B, not the census. The real population is 9,290 `macro:` entries across 330 of 1,608 crates (20.5%)** — candor-rust's FIFTH-LARGEST reason kind, after `callback` 81,119, `ambiguous` 35,317, `dispatch` 28,311 and `native` 23,034. Dominated by `macro:unexpanded multi-arm macro_rules!` and `macro:unreadable macro_rules! template`, with `macro:module items hidden by an unexpanded macro` the widest by crate count (137). **AND THE REMEDY THIS ROW OFFERED IS NOT AVAILABLE — decidably, not as a matter of taste: NONE of §4's five kinds projects to `unresolved`, so any re-kinding MOVES the class**, which [[R270]] already priced in both directions (`deny Unknown[dispatch]` loses it on 1 crate; `deny Unknown[unresolved]` GAINS 30 that pass today holding the hole). **So this needs a SPEC CLAUSE admitting `macro:` — or an equivalent opaque-source kind whose §6.2 class is `unresolved`, which preserves every existing scoped verdict while making `Unknown[macro]` newly expressible. That is additive, and it is mine to write.** A PART 10 fixture VERIFIED to produce the kind against both arms is in the agent's report; **added today it DIVERGEs, because `macro` is in none of CANON/REGISTERED/MIGRATION/TOLERATED — which is this row's whole point, and why the fixture and the clause MUST land together** **CLOSED as SPEC ⟨0.39⟩ (the clause once drafted as 0.40) — `macro:` is now a sixth §4 kind, with PART 10's CANON widened and a purpose-built `macrohidden` fixture pinning it ON THE CALLER.** Calibrated rather than assumed: removing the macro from that fixture turns PART 10 red (exit 1) and restoring it returns exit 0. **The class stays `unresolved`, so the rung moves no verdict by construction** — and the finer `Unknown[macro]` filter was REFUSED with its price stated, since it would withdraw these rows from `unresolved` and break every gate already written with the broad filter ([[R270]]: 30 crates that pass today while holding the hole). Recorded so a future reader does not think the finer filter was simply forgotten |
| R490 **candor-rust holds the §4 reason vocabulary TWICE — the nightly dylint lint is a SECOND PRODUCER in the same repo**, and it emits `generic-iter:<method>` (outside §4's five) and `dispatch:std::io::Write`, a Rust PATH where §4's detail is NORMATIVE `<owner-type>.<member>` | 2026-09-17 | rust (dylint lint) | Published in `.candor/baseline.candor.Cdylib.json`, so it reaches consumers. **This is the exact hazard the classifier's own comment warns about — an engine holding one vocabulary in two places — and here it genuinely does**, which is why the spelling has already drifted between the two copies. Same class as [[R289]]'s fifteen-copies-of-one-instrument finding: a defect fixed in one copy is not fixed in the other | Rank above [[R489]] despite both being vocabulary-only: a DIVERGENT SECOND COPY is worse than a single non-conforming spelling, because it will keep diverging. The fix is to make the lint consume the engine's vocabulary rather than restate it **CLOSED candor-rust `2543da2`. FOUR divergences, not the two this row named — and TWO OF THEM WERE DOT-FREE, which is [[R488]]'s defect in the other producer.** The lint emitted `generic-iter:<method>` and `iter-combinator:<method>` (both → `callback:`, whose §4 row names `opaque-iterable`/`bound` verbatim), `deref:unresolvable overloaded auto-deref` (→ ⟨0.24⟩'s reserved DOT-FREE `dispatch:`), `dispatch:<rust path>` and `dispatch:stringify:<Trait>`. **§3.1's dispatch frontier keys on the DOT, so the lint's commonest dispatch reason could never enter `possibleViaUnknownDispatch` at all** — the same false-all-clear shape as R488, in the second producer, found because this row said to look. **Fixed as ONE TABLE, not one spelling**: `candor_classify::policy::Kind` holds §4's five, every lint producer spells through it, and `ReasonClass::classify` matches on `Kind::ALL` instead of its own copy — so the drift mechanism is structurally unreachable, and the control the code's own comment said to add *"at the same commit as a typed representation"* is added and iterates `Kind::ALL` rather than a hand list. **Re-drift is now a RED TEST and was falsified** (reintroducing one raw `"native:` literal reddens it). The `classify` refactor was PROVEN inert rather than assumed: 1,603 crates / 281,893 rows, **ADDED 0 REMOVED 0 CHANGED 0**, over a corpus where 155,003 rows carry `unknownWhy` spanning all five kinds, so every arm of the rewritten decision is exercised. Self-scan: effects moved on 0 rows; **17 rows move CLASS `unresolved`→`indirect`, flagged rather than buried**; `iter-combinator:` has ZERO reach and is stated as such |
| R491 java: `org.codehaus.groovy.runtime.ProcessGroovyMethods` was gated `method.startsWith("execute")` while every other member DRIVES a live `java.lang.Process` — `getText`/`getIn`/`getOut`/`getErr`, `leftShift` (`proc << input`, the payload carrier), `consumeProcessOutput`, `waitForOrKill`, `pipeTo`/`or` | 2026-09-17 | java | **SILENT — `org.codehaus.groovy` is κ-covered, so the floored call is not ledgered.** Third instance of [[R480]]'s allowlist defect found in one sweep, and the second silent one after [[R486]]. Measured on a consumer compiled against the real `groovy-4.0.22.jar`, holding the EMITTED CALL INSTRUCTION constant (no groovyc available, so only the source language differs): pre-fix only `spawn` appears and the other six are absent with `coverage: null`; post-fix all seven are `Exec` | **CLOSED in the same commit `101aa67`.** Note the shape — `leftShift` FEEDS the child process, so this is not only a missed disclosure but a missed data path into a spawned program |
| R492 **THE COVERED-PREFIX GRANT IS AN UNQUALIFIED PURITY CLAIM OVER EVERY UNMODELLED MEMBER OF THE NAMESPACE, AND THE LIST MIXES TWO KINDS OF ENTRY THAT DESERVE DIFFERENT TREATMENT** — language runtimes whose pure surface would otherwise flood `invisible` (defensible) and APPLICATION FRAMEWORKS granted on a rationale that was true only for the members someone happened to look at | 2026-09-17 | java | **MEASURED on one consumer compiled against the real jars, 13 methods, 10 of them ABSENT FROM `functions` ENTIRELY with `coverage: null` and no `invisible`; `deny Net` exits 0.** Instrument calibrated — the controls `Files.readAllBytes`→`Fs` and `ProcessGroovyMethods.execute`→`Exec` both fire. Silent members found: `ResourceGroovyMethods` 18 × `Fs` (`deleteDir`, `renameTo`, `eachFile`, `newPrintWriter`, …); `SocketGroovyMethods` `withStreams`/`accept`/`leftShift` × `Net` — **not in the classifier AT ALL**; `org.apache.commons.csv` `CSVParser.parse(File\|Path\|URL)` and `CSVPrinter.printRecords(ResultSet)` × Fs/Net/Db; `org.springframework.core.io` 26 × `Fs`; and `com.sun`, on which **JNA SQUATS** — `Native.loadLibrary`, `NativeLibrary.getInstance`/`getFunction` × Exec/Fs. **Two contradict their own grant rationale in writing**: `commons.csv` was granted as *"CSV stacks (pure-relative over caller sources)"* and holds ZERO rules while its `File`/`URL` overloads open the resource themselves, and `com.sun` was written for the JDK and silently extends to JNA, whose entire purpose is loading native code | **This is the documented-limitation trap operating at the level of a POLICY LIST rather than a comment.** `Rules.java` already names the remedy — *"the real fix is to MODEL that specific member (precision), not to drop the namespace's coverage"* — but nothing enumerates which members still need it, so the grant silently outlives the survey that justified it. Boundary of the audit, stated: 51 prefixes, two instruments (a classifier-side owner census and a library-side census over members whose `direct` — not `inferred` — effects are concrete), every candidate confirmed on a COMPILED CONSUMER rather than from the table. Known false-positive mode declared: a library's own bytecode owner is not always what a consumer's call site emits (Kotlin multifile facades), which is exactly why confirmation was required. Clean negatives WITH evidence: jackson, twilio, commons-lang3, guava base/math, `com.google.maps.model` (0 concrete-effect members), the three scoped hibernate prefixes, and kotlin/kotlinx once the facade artefact was accounted for **CROSS-ENGINE SWEEP 2026-09-17 (coordinator, read-only): JAVA IS THE LONE OUTLIER, and the other three differ from it IN KIND rather than in degree.** Every engine has some notion of 'do not disclose this call', but only java's is a NAME GRANT over a namespace. **rust** (`lib.rs:2918`) suppresses the floor disclosure for exactly FIVE sysroot crates — `std`, `core`, `alloc`, `proc_macro`, `test` — and a name match alone is NOT sufficient: it is ANDed with `is_real_sysroot_frontier`, an identity check written against a *core/alloc impostor* crate. Java's 51 prefixes are matched by NAME with no identity check, which is exactly why **JNA squatting on `com.sun` works**. **ts** (`scan.mjs:8491`) states the opposite discipline outright — an unmodelled external call takes *"the κ-ledger `invisible`/`blind` disclosure an unmodeled call gets (**never silent-pure**)"*. **ts and swift's own `COVERED` concept is EARNED, NOT ASSERTED**: a package is covered because a TRUSTED LOADED REPORT covers it (swift `Deps.swift:18` — envelope `package`/`packages` plus each entry's hash prefix), which is §2 rule 3's chained-coverage claim backed by a document, not a hardcoded name. **So the defensible form of this construct is a grant that is either tiny and identity-checked, or derived from an actual report — and java's is neither.** This is the third time today java has been the lone outlier on a class ([[R480]]'s builder allowlist and [[R486]]'s covered prefix being the others), which is itself the finding: the same discipline gap keeps producing different defects |
| R493 java: **`org.jetbrains` is κ-covered and has ZERO owner literals in the classifier** — and the prefix also covers `org.jetbrains.exposed`, a SQL framework, so `Database.connect`, `transaction{}`, `insert` and `selectAll` would all be silent `Db`. `io.ktor` classifies 3 owners, all CLIENT-side, leaving `io.ktor.server.*`, `io.ktor.network.sockets.*` and `io.ktor.utils.io` unmodelled | 2026-09-17 | java | **INFERRED, NOT MEASURED — and filed that way deliberately.** These came from a grep-level census during [[R492]]'s audit; no jar was available in `soundness/lib` and the agent did not fetch one, so nothing here has been confirmed on a compiled consumer, which is the standard every R492 finding met. Same status for `org.ehcache` (covers the shaded `…shadow.org.terracotta.*` tree, 73 unclassified `Fs` members) and `org.redisson` (only `create` classified, leaving `RedissonCache.get/put`, `JCache` and `SentinelConnectionManager`) | **Do not close this by reasoning; fetch the jars and run R492's census.** These are the two highest-risk of the 25 covered prefixes with no jar in `soundness/lib`, which is why they are recorded rather than dropped — but an INFERRED row that gets promoted to a finding without measurement is how [[R474]] and [[R475]] both went wrong this week **NOW MEASURED AND CLOSED (candor-java `b6027b6`); 46 of 52 covered prefixes censused, and BOTH HEADLINERS WERE WORSE THAN THIS ROW SAID — absent from `functions` ENTIRELY with `coverage: null` AND `invisible: null`, which under ⟨0.21⟩ is a certified purity claim, not a hedge.** `org.jetbrains`: a 10-method Exposed data layer (connect, `transaction{}`, insert, selectAll, update, deleteAll, schema create/drop, raw `exec`) compiled with kotlinc 2.4.10 against exposed 0.52.0 reported **`0 functions reach effects`** and `deny Db`, `deny Net` and `deny Unknown` ALL exited 0 — calibrated on the same tree, one `DriverManager.getConnection` reddens it. `io.ktor`: an HTTP server binding :8080 with two routes, a `respondFile`, a raw TCP connect and a `0.0.0.0:9000` bind — same result, `deny Net` exit 0, calibrated with one `java.net.Socket`. **Seven more confirmed by the same sweep**: `commons.codec` (the *"pure CPU"* grant with zero rules and 12 File/Path overloads that open the file — the `commons.csv` contradiction one package over), `org.dbunit` ([[R480]]'s allowlist shape again), `commons.validator` and `commons.beanutils` (both granted on a survey sentence asserting they carry NO effectful members), `org.supercsv`, `org.ehcache`, `org.redisson` |
| R494 **A MEMBER CLASSIFIED TO THE WRONG, WEAKER EFFECT PRODUCES NO DISCLOSURE AT ALL — the engine believes it knows.** `software.amazon.awssdk.auth.credentials.ProcessCredentialsProvider` was charged `Env`, and `resolveCredentials()` calls `executeCommand()` which does `new ProcessBuilder(List).start()` — **the provider whose entire purpose is running an external credential helper (`aws-vault`, SSO helpers) forked a process under a PASSING `deny Exec`** | 2026-09-18 | java | **CONFIRMED INDEPENDENTLY by the coordinator with `javap -c` on `auth-2.25.60.jar`**: `resolveCredentials()` → `invokespecial executeCommand()` → `new java/lang/ProcessBuilder` → `ProcessBuilder.start()`, and `resolveCredentials` is the public entry point. **This is NOT the [[R492]] class even though it was found by that sweep, and the difference is the whole row: an ABSENT member floors to `invisible` and discloses (outside a covered prefix), whereas a MIS-CLASSED member emits a positive, confident, WRONG answer that reads as complete coverage — no `invisible`, no advisory, nothing to notice.** A weaker-but-plausible effect is the worst case in the family's terms, because every disclosure channel it has is satisfied | CLOSED in `b6027b6` — fixed to Exec/Net, **with the profile and environment providers asserted to KEEP `Env`** so the fix cannot have merely moved the error sideways. **The general question this opens and does NOT answer: nothing in the family looks for a member classified to a WEAKER effect than it performs.** Every instrument here hunts absence. An over-charge is caught by the A/B's ADDED column and an absence by `invisible`, but a wrong-but-present answer is invisible to both **CORRECTION 2026-09-18 to the call chain BOTH the agent and I recorded: the caller of `executeCommand()` is `refreshCredentials()`, NOT `resolveCredentials()`.** The path runs `resolveCredentials` → a `software.amazon.awssdk.utils.cache.CachedSupplier` → (callback) `refreshCredentials` → `executeCommand` → `ProcessBuilder.start()`. I asserted the direct chain after grepping `javap -c` output WITHOUT CONTEXT and reading two adjacent matching lines as one call — the filtered-view-as-verdict mistake. **The finding is unaffected** (calling the provider can still fork, and the fix to Exec/Net is right), but note what the corrected chain shows: in a SELF-SCAN candor disclosed the gap honestly, `resolveCredentials` carrying `invisible: ["…utils.cache"]` rather than claiming purity. **κ's answer for CONSUMERS is what erased that disclosure** |
| R495 java: two covered-prefix candidates that could NOT be confirmed and are deliberately NOT fixed — `org.apache.struts` (`RequestProcessor.doForward`/`doInclude`, 63 census NULLs) and `org.displaytag` | 2026-09-18 | java | **CANDIDATES, NOT FINDINGS.** Both need a servlet container on the compile path, which this box does not have, so neither was confirmed on a compiled consumer — the bar every [[R493]] finding met. The census README declares a known false-positive mode (a library's own bytecode owner is not always what a consumer's call site emits), so an unconfirmed census hit is exactly the thing that mode produces | Recorded rather than dropped because they are the last two of the 52, and recorded as CANDIDATES because promoting an unconfirmed hit to a finding is how [[R474]] and the first filing of [[R475]] both went wrong. Also clean and worth not re-deriving: `org.hibernate.criterion` measured CLEAN against hibernate 5.6.15 — the package was REMOVED in 6.x, which is why the 6.5.2 jar showed nothing |
| R496 **`DefaultCredentialsProvider.resolveCredentials` — the single most common AWS credentials call — is classified `Env` while candor's own scan of the library says its body reaches `Exec`.** Same for `ProfileCredentialsProvider`, `WebIdentityTokenFileCredentialsProvider` and `internal.LazyAwsCredentialsProvider` | 2026-09-18 | java | **FOUND BY A NEW INSTRUMENT ON ITS FIRST REAL RUN — `soundness/kappa_census/weaker_claim_census.py`, built for [[R494]]'s class after R494 established that nothing in the family looks for a member classified to a WEAKER effect than it performs.** Self-scan of `auth-2.25.60.jar`: `DefaultCredentialsProvider.resolveCredentials` has `inferred: ['Clock','Env','Exec','Unknown']` while κ answers `ENV`. The `Exec` is real and is the ordinary AWS path — `javap` shows 18 references to `ProfileCredentialsProvider`, and a profile carrying `credential_process` forks, which is exactly what [[R494]] fixed one class over. **[[R494]]'s fix did not reach these four: they are LIVE in the current jar as well as the pre-fix one, which is why the calibration run showed identical output for both classifiers — that identity was the finding, not an instrument failure** | **CANDIDATES by the instrument's own rules until confirmed on a COMPILED CONSUMER**, which is the bar every [[R493]] finding met — the body evidence and the `javap` chain are strong but the census declares two false-positive modes and one of them is that κ may deliberately model a NARROWER call-site contract. That defence is weak here: candor's discipline is sound over-approximation, so a reachable `Exec` is disclosed, not silently narrowed to `Env`. Note the severity — a consumer calling `DefaultCredentialsProvider.create().resolveCredentials()` receives `Env` with NO `Unknown` and NO `invisible`, because κ's answer preempts the disclosure the library's own scan carries **CONFIRMED on a compiled consumer and CLOSED (candor-java `ee90451`, CI green).** Pre-fix `DefaultCredentialsProvider.create().resolveCredentials()` gave `inferred:['Env']`, `invisible:null`, `unresolved:false`, and `deny Exec`/`deny Fs`/`deny Net`/`deny Exec Unknown` ALL exited 0; post-fix `['Env','Exec','Fs','Net']` and all exit 1, with a `new ProcessBuilder("id")` control firing on the same tree. **The narrow-contract counter-argument was considered and REJECTED on a stated reason: which arm of the chain fires is decided by the machine's `~/.aws/config`, never by anything in the caller's code — that is precisely the input candor cannot see, so the sound answer is disclosure.** **TWO SPELLINGS THIS ROW DID NOT NAME, and the second is worse than the row itself.** (1) THE INTERFACE: `AwsCredentialsProvider p = DefaultCredentialsProvider.create(); p.resolveCredentials()` emits `invokeinterface` on the INTERFACE owner — not hypothetical, `mongodb-driver-core`'s MONGODB-AWS authenticator does exactly this, so a fix naming only concrete classes would have been evaded by the idiomatic form. (2) `resolveIdentity(ResolveIdentityRequest)`, the v2 SDK default method whose whole body is `invokeinterface resolveCredentials()`: κ named no rule and the package is κ-covered, so it was not mis-classed but **SILENT** — `0 functions reach effects`, `coverage:null`, `invisible:null`, a ⟨0.21⟩ purity claim over the ENTIRE credential chain. **One candidate REFUSED on evidence**: `WebIdentityTokenFileCredentialsProvider`'s `Exec` is CHA smear over the interface call — `javap -c` shows it delegating to one STS-backed factory and nothing that forks — so it gets `Env+Fs+Net` with `assertFalse(contains(EXEC))` pinning it against a later widening. A/B 371 jars / 1,215,638 rows: **ADDED 0, REMOVED 0**, CHANGED 2820; every changed row only GAINED, and **every row that gained Exec/Fs/Net already carried `Env`** (1698/1698 in mongodb) — the reach was already established, only the label was wrong |
| R497 **`candor path` resolves an ambiguous suffix SILENTLY and returns a NEGATIVE VERDICT ABOUT A DIFFERENT FUNCTION.** `candor path ProfileCredentialsProvider.resolveCredentials Exec` prints *"InstanceProfileCredentialsProvider.resolveCredentials does not perform Exec (inferred: [])"* | 2026-09-18 | java, rust, swift, ts — ALL FIXED | **Both functions are present in the report and there is NO ambiguity warning.** Reproducible on `auth-2.25.60`. This is not a classifier defect — it is a QUERY defect, and it is in the cardinal-sin direction on the surface a human actually reads: a user asking whether a specific function performs `Exec` is told NO, about a function they did not ask about, in a sentence that names the wrong owner without flagging that it substituted one | **Ranks above an ordinary UX bug because the answer is CONFIDENTLY WRONG rather than unhelpful.** The family's whole contract is that a negative is a claim; a negative about a substituted subject is a fabricated claim. Remedy must be to REFUSE an ambiguous suffix (exit 2, naming the candidates), never to pick one — the ⟨0.24⟩ `ambiguous:` discipline applied to the query surface. Found by the R496 agent while ground-truthing, not by any gate **REPRODUCED BY THE COORDINATOR 2026-09-18 and it is sharper than filed — TWO stacked defects, and an EXACT MATCH EXISTED AND WAS PASSED OVER.** (1) *The suffix match is not SEGMENT-ANCHORED*: `ProfileCredentialsProvider` is a literal suffix of `InstanceProfileCredentialsProvider`, so the selector matched INSIDE a longer identifier. `...credentials.ProfileCredentialsProvider.resolveCredentials` is present in the same report and is the only dot-anchored match, so anchoring alone would have answered correctly. (2) *With two bare-suffix candidates it PICKS instead of refusing.* **Note the asymmetry that is the whole defect: `path` ALREADY refuses at exit 2 when ZERO functions match — it is only MANY that it answers silently.** **CROSS-ENGINE: java is the lone outlier again.** candor-rust anchors (`matching.rs:22-23` requires the preceding characters to be `::` or `.`) and candor-ts anchors (`query-core.mjs:1376`, `/[.$#]$/`). **And the family has ALREADY ruled this exact class one ARGUMENT over**: candor-swift's `FixCLI.swift:1778` records that `path caller Fsz` used to print *"caller does not perform Fsz"* at exit 0 — *"a typo scored as a confident NEGATIVE, in the verb people reach for to check one specific claim"* — and grew a guard for the EFFECT argument. R497 is that defect on the FUNCTION argument of the same verb, which is why it survived: the guard was built for argument 2 and never for argument 1 **FIX IS BOTH HALVES, and neither alone is right:** anchor the match on a segment boundary (`.`/`::`/`$`/`#`) FIRST — which resolves this case to the function actually asked about — and THEN refuse (exit 2, naming the candidates) if more than one survives. Anchoring alone leaves genuine ambiguity silent; refusing alone would reject a question that has one exact answer. Prefer an EXACT dot-anchored match over a bare suffix rather than counting it as ambiguity **FIXED candor-java `92994fd`, CI green — AND MY BRIEF'S PREMISE WAS WRONG, which the agent caught and I have verified.** I told it *"the other engines already anchor, you are bringing java in line"*, citing `matching.rs:22-23`. That function is real but **`path` and `impact` do not call it**: `callers.rs:459` and `:623` both do `find(func == fn_arg).or_else(find(func.contains(..)))` — unanchored, first match wins. I read those two sites myself to confirm. **So java is now the ONLY engine that is correct here.** **The defect is also worse than I reproduced it**: the function actually asked about carries `inferred: [Clock, Env, Exec, Fs, Net, Unknown]`, so the answer was a **FALSE NEGATIVE ON THE REAL QUESTION**, not merely a wrong subject — and `impact` said *"0 effectful functions transitively call it"* where the real subject has 4. Half 2 is load-bearing: `path resolveCredentials Exec` has FOURTEEN dot-anchored candidates on that jar and pre-fix answered a confident negative about `AnonymousCredentialsProvider` while three of the fourteen perform `Exec` | **Verb-sweep boundary, stated:** `show`/`callers`/`whatif` answer over the WHOLE best-tier set, so many WIDENS an answer rather than substituting a subject; `fix` is anchored and picks but PREFERS a tier match that performs the effect, so it cannot emit *"nothing to hoist"* while a sibling performs it — left alone deliberately. `path`/`impact` are the only two call sites of `.fn().contains(<selector>)` in `src/main`. **Conformance does not pin any of this** — `run.sh:4447-4455` pins only the ZERO-match case — so the divergence was unpinned in every engine 
| R498 java: `org.eclipse.jgit.api.Git.open` reaches a real `new ProcessBuilder(String[]).start()` via `FS.readPipe` on a platform-selected `FS` implementation | 2026-09-18 | java | **CANDIDATE, not a finding — no compiled consumer yet**, which is the bar every [[R493]]/[[R496]] finding met. Surfaced by the full-corpus weaker-claim sweep as one of only 9 DIRECT chains among 42 `Exec` candidates. The direct-chain shortlist is the part of that sweep worth acting on CONFIRMED, NOT REFUTED — closed in candor-java 97212f3 and f2749b1, both pushed, ci and native green. Compiled consumer with the library out of scope: pre-fix inferred [Fs] with deny Exec and deny Exec Unknown BOTH exit 0; post-fix [Exec,Fs] and both exit 1. Held constant — same candor jar, same jgit jar, same policy, same tree — and a sibling doing new ProcessBuilder(id).start() reddens deny Exec in BOTH arms, so the pre-fix exit 0 is an under-report and not a broken scan. RUNTIME PROOF BEYOND javap: run with a logging git shim first on PATH, the single call forks TWICE (git --version, then git config --system); with no git on PATH it forks bash --login -c which git. WHICH ARM FIRES IS DECIDED BY THE MACHINE'S PATH, never by the caller's code — the input candor cannot see — so disclosure is the sound answer, exactly as R496 settled for AWS, and there is no arm that forks nothing. | Two others from the same shortlist are UNTRACED (`rocksdb`, and one where `candor path` resolved a different overload — see [[R497]], which is why that tracing stalled). One was REFUTED and is recorded as [[R499]] SEVERITY, MEASURED RATHER THAN READ OFF THE LIST: org.eclipse is NOT in KAPPA_COVERED_PREFIXES, and the pre-fix report carried invisible:[org.eclipse.jgit.api] plus the coverage advisory ALONGSIDE the Fs — so this is a DISCLOSED under-report and a live gate bypass, R480's direction, NOT R486/R509's cardinal sin. R508's version question is CLOSED by fifteen published jars, each compiled against AND run under the shim: Git.open(File) does not exist before 3.7.1, and every version 3.7.1 through 7.3.0 forks. Unlike commons-exec there is no published version where the charge would be an over-charge on every machine. ONE CORRECTION TO THIS ROW'S WORDING: FS.DETECTED / FS.detect() fork NOTHING on either PATH arm — the fork is not at FS SELECTION but at repository materialisation, through FS.getGitSystemConfig. A SECOND MECHANISM was found by sweeping rather than stopping at the instance: a jgit COMMIT runs the repository's own .git/hooks/*, measured with an executable pre-commit hook, so CommitCommand.call and PushCommand.call fork too. |
| R499 **REFUTED — THIS ROW WAS WRONG AND ITS REMEDY WOULD HAVE INSTALLED A SILENT UNDER-REPORT ON THREE PUBLISHED RELEASES.** java: `org.apache.commons.exec.environment.DefaultProcessingEnvironment.getProcEnvironment` is OVER-CHARGED `Exec` by [[R480]]'s whole-package rule — in commons-exec 1.4.0 that class calls only `System.getenv()`| 2026-09-18 | java | **The safe direction (a fabrication, not a silence), and R480 chose whole-package deliberately over an allowlist — but it is real and it MANUFACTURED A CENSUS HIT**, appearing as a body-side `Exec` that the weaker-claim sweep then had to refute by hand. That is the concrete cost of an over-charge in this family: it is not merely imprecise, it pollutes the ground truth of every instrument that reads bodies | Fix by carving the class out of R480's package rule by NAME after `javap`, the same discipline that produced R480's other carve-outs. Do NOT narrow the package rule itself — that is the allowlist R480 exists to remove **REFUTED 2026-09-19.** `javap -c` over published commons-exec **1.0 / 1.1 / 1.2** shows `DefaultProcessingEnvironment.getProcEnvironment → createProcEnvironment → runProcEnvCommand → new DefaultExecutor(); Executor.execute(getProcEnvCommand())` on literal argv `cmd /c set` / `/bin/env` / `/usr/bin/env` / `env`. **It FORKS.** Only 1.3 gutted those methods to `return null`, and 1.4.0 — the only version in `soundness/lib`, which is why I filed this — is post-gutting. I spot-checked 1.4.0 myself and it is consistent; the 1.0-1.2 evidence is the agent's `javap` plus a compiled consumer with measured gate exits. **The classifier is VERSION-BLIND ([[R508]]), so carving the class out would have been correct for 1.3/1.4 and a cardinal sin for 1.0-1.2** |
| R500 **INSTRUMENT: `weaker_claim_census.py`'s oracle COULD NOT RETURN A NEGATIVE, and I wrote it.** `KappaQuery` printed the Java enum constant (`ENV`) while reports carry the spec name (`Env`), so `body − \|κ answer\|` never subtracted: EVERY classified member with any reach was reported as a hit, each listing κ's own answer as *"missing"* | 2026-09-18 | instrument (`soundness/kappa_census/`) | **This breaks the project's own rule — *"0 violations is not evidence until the oracle is PROVEN able to fail"* — INSIDE THE INSTRUMENT WRITTEN TO ENFORCE THAT DISCIPLINE.** Worse than the bug: the symptom was visible and I explained it away. The pre-fix and post-fix classifiers produced IDENTICAL output, I noticed, and concluded *"the identity WAS the finding"* — then published that as a lesson in the README. The four candidates were genuine, but they arrived in a report that could not have said otherwise, which is a coincidence and not evidence | **CORRECTED LESSON, now in the README with the wrong one quoted beside it: when a before/after run does not discriminate, the FIRST question is whether the instrument can return a NEGATIVE — not whether the rows are insensitive. Prove the negative before interpreting the positive.** Fixed by asking the authority (`Effect.specName()`) instead of spelling a second copy of the table in the reader — a second copy is exactly how the two names diverged. **Second declared defect, same file**: the census queries the κ table rather than what the engine charges at a call site, so it is blind to every CO-EMIT and the five AWS owners still show as hits AFTER being fixed. Remedy is one `coEmittedEffects` authority called by both the writer and the reader |
| R501 rust: `dep_join_hit` SUPPRESSES [[R452]]'s `ambiguous:` disclosure — a joined row keeps `Unknown` and `unresolved: true` but LOSES `unknownWhy: ["ambiguous:same-name local methods"]`, so `deny E Unknown[ambiguous]` stops firing while bare `deny E Unknown` still does | 2026-09-18 | rust | **PRE-EXISTING, and WIDENED by the ⟨0.39⟩ port rather than introduced by it** — measured on 2 bson rows during that port. **This is [[R485]]'s class exactly**: no effect moves and no verdict changes for an unscoped policy, so every gate that does not scope by reason class is blind to it, and a user who writes the scoped form to catch precisely this shape gets a PASS they did not earn | **The port agent DECLINED to fix it inside the rung, correctly.** The repair touches R452's flood control, which rests on its own 14,409-caller measurement, and a change to that belongs in a round where it can be measured rather than inside a port whose A/B is answering a different question. Recorded with its own ID so the decline is a deferral with an owner and not a silence **CLOSED candor-rust `2543da2`, CI green on all four workflows — and HALF OF IT IS A MEASURED REFUSAL.** Reproduced function-scoped: `bson-2.15.0` scanned twice with the SAME binary, the 59 dep reports the only variable — standalone both rows carry `unknownWhy: ["ambiguous:same-name local methods"]` and are named by `deny Unknown[dispatch]`; chained, neither is. Traced to the gate with an instrumented build, not inferred. **Audit boundary swept beyond the site handed over: of six `unknownWhy` writers, THREE are suppressible by `dep_join_hit` (R452's site, the `native:extern fn` FFI boundary, and [[R128]]'s macro-hidden hedge) and three are structurally unreachable from it** — `suppress_bare_leaf` is mutually exclusive with `dep_join_hit`. **THE REFUSAL: ungating on `dep_join_hit` outright is not a flood (152 sites / 17 crates vs R452's refused 14,409) but it charges `Unknown` to 22 rows the join answered CONCRETELY** — reqwest `Response::status`, hyper `ClientTask::poll`, aws-config `AssumeRoleProviderBuilder::build`. The join keys on `crate#tail2`, i.e. on strictly MORE of the written path than the collision that would hedge it, and under §4.0 `(E,∅)` and `(∅,{r})` are INCOMPARABLE — so hedging there WITHDRAWS a determined effect. Refused and pinned: the test's arm 2 reddens on that over-reach exactly as arm 3 reddens on the revert. Shipped fix is narrow — a dep join handles the call only when its own answer was not itself `Unknown`. A/B 1,603 crates with each crate's own deps chained and the dep set held constant: **ADDED 0, REMOVED 0, CHANGED 46, `inferred` CHANGED 0**; all 46 audited, every one GAINS the reason and every other field byte-identical. Per-site reach AMBIG 127, NATIVE 29, **MACRO 0, R128 0 — the two zero-reach sites are safety-only on this corpus and are written down as such rather than presented as covered** |
| R502 **INSTRUMENT/PRACTICE: `re-baseline.yml` pins re-baselining to Ubuntu on a stated reason that is STALE at HEAD** — *"the macOS dylint driver currently ICEs (rustc_private 'missing value for assoc item in impl'), so this is the supported path"* | 2026-09-18 | instrument (candor-rust) | **MEASURED FALSE 2026-09-18**: `cargo dylint` ran cleanly on macOS 25.6 with the pinned `nightly-2026-06-14`, producing all four self-scan reports in 9 seconds — which is the only reason the R490 self-scan A/B exists at all. **Textbook [[R443]] shape: a comment that reads as CONSIDERED, so it stops being measured, and then expires because the thing it points at moved** | **Deliberately NOT fixed, and the restraint is right**: moving that job off Ubuntu is a behavioural change nobody has priced, and [[R212]]'s macOS-vs-Linux row-count difference is a live reason to keep the baseline generated on ONE platform. The defect is the STALE JUSTIFICATION, not the pinning — fix the sentence to say *"one platform, because R212"*, which is a reason that cannot expire, rather than citing an ICE that no longer happens |
| R503 rust: **candor-scan publishes `dispatchesOn` as the TRAIT LEAF (`Backend::size`) while candor-java publishes the FULL ENTRY HASH — two wire values for one ⟨0.39⟩ field, produced by the first two ports** | 2026-09-18 | rust | **Neither engine invented a second spelling WITHIN itself, which is exactly the ⟨0.34⟩ drift shape: every engine correct by its own lights and the field meaningless across them.** The clause now pins the value to obligation 2's rule — fully qualified in the OWNING package's namespace, the namespace that package's entry hashes use — so a consumer can join a `dispatchesOn` value against the producing package's entry keys with no per-engine rule. java's port established this costs java NOTHING (a JVM entry hash already IS the ⟨0.23⟩ rule); rust is the one that moves | **Coordinated change, not a one-liner: rust's own consumer join READS the leaf**, so producer and consumer move together or the rung breaks in-engine. Pin it with a fixture whose abstraction is NOT at the crate root — rust's port already learned that lesson the hard way, its first fixture passing either way because the trait sat at the root **CLOSED candor-rust `69dd565` — AND THE ROW UNDERSTATED IT. This was not one field with two spellings; rust held THREE.** The foreign `interfaceUnion` key was ⟨0.23⟩-qualified, the LOCAL union entry hash was leaf-keyed, and `dispatchesOn` was a third — so the engine disagreed with itself internally before it disagreed with java. All three are now `iface#backend::Backend::size`, from a new Pass-A walk recording each locally-declared trait's module-qualified path; an ambiguous leaf is REFUSED, never guessed. The consumer now uses the value AS GIVEN instead of prefixing the row's own package — **which is load-bearing for [[R504]], because prefixing the DISPATCHING crate names the wrong owner the moment the dispatcher is not the owner. The two rows are one change, not two.** Backward compatible: a value with no `#` is still read the old way, so chaining onto a ≤0.38.4 dependency report keeps its disclosure **A/B over 1,599 crates / 317,431 rows: `inferred` ADDED 284, REMOVED 347, CHANGED 0 — no existing row's effect set moved, as predicted. REMOVED 347 is the alarm column and every removal was audited**: 270 are pure dispatch-only rows whose only members were std FORMATTING traits, which the pre-fix wire spelled `crate#Display::fmt` — **naming an owner (`core`) the crate does not have, a key no consumer could ever join** — and §4 permits excluding formatting; 53 are local union entries whose hash became qualified, 42 reappearing under the new key and 11 colliding with a REAL entry that is strictly richer (same `["Unknown"]` plus `loc`/`unknownWhy`), i.e. one duplicate row fewer. Zero removals carried an effect. **The audit also caught a defect reasoning did not: the first cut published nine `io#Write::write_all` rows — a key naming a package that does not exist — and the comment at that site ASSERTED it was filtered against the manifest, which was true of obligation 2's key and false of the new one.** A fresh comment, wrong the day it was written; found by reading the ADDED column, not by a gate|
| R504 rust: **`dispatch_sites` records LOCAL-trait dispatch only** (`trait_declares_method` over `trait_decls`), so a MIDDLE package dispatching over its OWN dependency's trait names nothing and the ⟨0.39⟩ chain breaks one hop short | 2026-09-18 | rust | **Found by candor-java's port, read out of candor-rust's SOURCE rather than assumed, and measured silent in java on a four-package chain before being closed there** (INVOKEINTERFACE, non-κ owner). This is the middle-package hole: A declares the abstraction, B depends on A and dispatches over A's trait, C consumes B — B names nothing, so C never learns the member to union on | **PART 92's fixture is THREE packages and cannot express this — the arm that would catch it needs FOUR.** That is the row to write alongside the fix, and it is the same lesson as every audit-boundary row here: the fixture that closes a defect is the boundary of the next one. Note rust ships ⟨0.39⟩ today with this hole, so the rung is real but not yet complete in that engine **CLOSED candor-rust `69dd565`; PART 92 gained the four-package arm `c6_middle_package` (`candor-spec a441b30`) and rust's cell on it is retired.** **The arm's most informative cell is java's, and java PASSES** — so the reference engine's middle-package fix is exactly as wide as its port claimed, which was the thing I most wanted to know and the thing a three-package fixture could not ask. Pre-fix rust read `eff=∅ invisible=['iface']`; post-fix `eff=['Net']`. swift and ts xfail it, as they xfail c1 — they have not ported ⟨0.39⟩ at all. All five existing controls stayed green with the fourth package present, so the topology change moved nothing else **`c6` is deliberately NOT in `CROSS_ARMS`** — its consumer calls `middle` where c1's calls `iface`, so the byte-identical-consumer cross cannot hold. What it DOES support is stated in the generator: c6 differs from c1 in exactly ONE structural fact, which package holds the dispatching function, so *"passes c1, fails c6"* reads precisely as *"obligation 1 is scoped to abstractions the producer owns"*. Honest residue recorded rather than fixed: PART 92 scans each dependency UNCHAINED, which is why rust's c6 consumer carries `invisible: ['iface']` where java's is `∅` — an over-disclosure the arm does not assert, left alone rather than changing a shared loop four other arms depend on|
| R505 **INSTRUMENT: `ci-watch.sh` printed `NOT PUSHED` for a commit that WAS on the remote, in SIX of the seven family repos** — `@{u}` fails there (no upstream configured for `main`) and the fallback `origin/HEAD` is a symbolic ref set in NONE of them, so `merge-base --is-ancestor` ERRORED and the `!` turned that error into a confident verdict | 2026-09-18 | instrument (`candor/bin/ci-watch.sh`) | **MEASURED on candor-spec with the commit verifiably on the remote — `git ls-remote` agreeing with the local `origin/main` — while ci-watch said it was not pushed.** The branch only fires when no workflow runs are found for HEAD, i.e. in the ordinary window right after a push before CI starts, which is exactly when a reader is most likely to act on it | **It failed RED, which is what the code's own comment promises (*"the conservative direction — red, never a false green"*) — and that promise is the thing this row is about. Red for a WRONG REASON is not conservative in the sense that matters: it sends the reader to re-push a commit that is already pushed, in the one line a reader trusts.** A guard whose failure mode is a plausible wrong explanation is worse than one that says *"I could not tell"*. Fixed to fall back to `origin/<branch>`, which the push itself writes and which resolves in all seven repos, with `origin/HEAD` kept last so a detached HEAD still compares against something. **Calibrated BOTH directions rather than only the fix**: an injected unpushed commit still prints NOT PUSHED (now naming `origin/main`), and a pushed HEAD reports the real CI state |
| R506 java: **[[R480]]'s own carve-out was a SHIPPED CARDINAL SIN.** It mapped `org.apache.commons.exec.environment.EnvironmentUtils.getProc*` to `Env`, and in commons-exec **≤1.2** that delegates straight into the FORKING chain — so a benign-looking `Env` charge stood over a real `Exec` | 2026-09-19 | java | **MEASURED on a consumer COMPILED AGAINST the real `commons-exec-1.2.jar`, library out of scope**: pre-fix `Arm.readEnv` reports `inferred: ["Env"]` with no `Unknown`, and `deny Exec`, `deny Unknown` and `deny Exec Unknown` ALL exit 0; post-fix `inferred: ["Exec"]` and `deny Exec` exits 1. The instrument was calibrated — a control one owner over, outside the carve-out, was `Exec`/exit 1 pre-fix. **This is the fabrication-fix pattern exactly: a carve-out written to kill an OVER-CHARGE introduced a SILENT UNDER-REPORT**, and it shipped | **The test PINNED THE DEFECT** — `ThirdPartySubprocessBuilderTest` asserted `Effect.ENV`, so the suite defended the sin. CLOSED in `92994fd` by deleting the carve-out, **with the price stated rather than hidden**: on 1.3/1.4 this is now an over-charge, and since `classify` returns ONE effect the `Env` read is no longer charged. Found only because [[R499]] sent someone to `javap` three older versions |
| R507 **rust, swift and ts all carry [[R497]]'s class, and java is now the only engine without it** — `path`/`impact` resolve a selector by unanchored `contains` and answer about the first match | 2026-09-19 | rust, swift, ts — ALL FIXED | **rust**: `crates/candor-query/src/callers.rs:459` (`cmd_impact`) and `:623` (`cmd_path`) are byte-identical `find(func == fn_arg).or_else(find(func.contains(..)))` — I read both to confirm. **swift**: `Sources/candor-swift/FixCLI.swift:1815`, whose comment says it *"mirrors the Rust reference"* — the copy inherited the defect with the design. **ts**: anchored (`query-core.mjs:1832`, `:1732`) but still takes `targets[0]`, so it needs the REFUSAL half only TS HALF CLOSED (ee844f0), which completes R507 in all four engines. Worse than filed for ts and on the channel that matters least forgivingly: path resolveCredentials Exec --json returned {effect: Exec, fn: resolveCredentials, path: []} at EXIT 0 — a false negative on the MACHINE channel, about a subject it never named — while a sibling ran execSync; and impact returned a blast radius belonging to the other function. Both now exit 2 naming every candidate on the human channel, --json, and the MCP tools. | **My brief for [[R497]] asserted these engines were already correct, citing rust's `matching.rs` — which exists but is called by `show`/`callers`/`whatif`/`fix`, NOT by `path`/`impact`.** The agent attacked that premise and was right; I verified the two rust sites myself. **A conformance PART would pin this and none does** — the suite pins only the ZERO-match case, which is why one class sat unfixed in four engines. Fix is R497's: anchor on a segment boundary preferring an exact match, then refuse at exit 2 naming candidates when more than one distinct name survives **RUST'S HALF CLOSED (`e5080df`), and it was measured IN THE WILD, not only on a fixture.** Over 159 crates.io reports the pre-fix binary answered `path handshake Clock` on `mongodb-3.8.2` with *"`<lazy>::cmap::establish::handshake::BASE_CLIENT_METADATA` does not perform Clock"* at exit 0 — **a determined negative about a CONSTANT**, while the segment-anchored subject `cmap::establish::handshake::Handshaker::handshake` does perform it. **Query-level reach over 37,046 realistic selectors: 5,948 (16.1%) resolve differently pre vs post; 1,179 were a SUBSTITUTED SUBJECT; and 121 would answer a determined negative about an effect the real subject performs.** The instrument was calibrated rather than trusted — a stratified sample through both binaries gave 400/400 predicted-divergent changed AND **600/600 predicted-unchanged byte-identical**, so it can report *"no change"* and is not vacuous; 60/60 of the false-negative class confirmed one at a time. Gates 31/31 including `soundness/run.sh 60` **The boundary was swept and PINNED IN BOTH DIRECTIONS.** `show`, `callers`, `callers_via_callgraph` and `whatif` filter the WHOLE best-tier set — many WIDENS rather than substitutes — and `fix` is anchored, picks, but prefers a tier match that PERFORMS the effect, so it cannot say *"nothing to hoist"* while a sibling performs it; all left alone deliberately. `path`/`impact` were the only two call sites of an unanchored `.func.contains(<selector>)` that BOTH pick arbitrarily and phrase a determined negative about the picked name. **A regression test now pins the other direction too: `show` and `callers` must keep answering, at exit 0, the very selector `path`/`impact` refuse — so a later "consistency" change cannot quietly extend the refusal to the verbs that were already right.** **A/B is zero-REACH by construction and said so**: `corpus-ab.py` diffs REPORTS, `candor-scan` is untouched and `candor-query` writes no report, so the arms are byte-identical and the evidence is the query-level measurement above|
| R508 **STRUCTURAL: the classifier is VERSION-BLIND — a κ rule is keyed on owner+method and cannot say *"this owner forks in ≤1.2 and does not in ≥1.3"*, so one rule is necessarily wrong for some published version of the library** | 2026-09-19 | java (and every engine with a name-keyed table) | **MEASURED INSTANCE: commons-exec.** `DefaultProcessingEnvironment.getProcEnvironment` forks in 1.0/1.1/1.2 and returns `null` from 1.3 — so `Exec` is correct for three published releases and an over-charge for two, and NO single rule is right. [[R499]] was filed against the 1.4.0 jar that happens to sit in `soundness/lib` and its remedy would have been a cardinal sin for anyone on ≤1.2 SWEPT 2026-09-20: all 21 javap-derived carve-outs enumerated with the version range checked per carve-out, and TWO were wrong — see R509 (Exposed 1.x, a shipped cardinal sin on a covered prefix) and R510 (testcontainers, an audit-boundary failure rather than drift). Nineteen were clean against every published version reachable, several swept exhaustively: zt-exec all 10 published versions, commons-exec all 7, im4java both, scala across 2.11-3.10, testcontainers 12 versions, exposed 0.30.1 to 1.5.0. Stated plainly rather than implied: ehcache was checked at ONE version and ffmpeg at 6 of 13. | **Not a defect to fix so much as a LIMIT to state and then choose a direction within.** The safe direction is the over-charge (loud) rather than the carve-out (silent), which is what [[R506]] settles for this instance. Worth knowing what it costs elsewhere: every carve-out derived with `javap` in [[R480]], [[R486]], [[R487]] and [[R493]] was derived from ONE version of each jar, so each is a candidate for this same failure. **The census in `soundness/kappa_census/` reads one jar per prefix too** THE GUARD IS BUILT: soundness/rule_fires.sh — for each κ-covered library it downloads ONE jar (the latest release), enumerates members whose owner is κ-covered by asking the AUTHORITY (Candor.kappaCovers, not a second copy of the prefix list), and asserts at least one rule still matches. A rename takes that count to zero. In-scope set is authority-derived rather than parsed out of Classifier.java: a library is in scope iff its pinned jar already matches at least one rule, so a pure-by-grant prefix has baseline 0 and can never raise a phantom — 84 of 372 jars. CALIBRATED FOUR WAYS, and arm D earned its keep by initially PASSING WHILE MEASURING NOTHING: the offline path resolved no version, every row printed 'offline: no jar', and the gate exited 0 — the R500 shape again, caught only because the calibration was written first. It also FIRED INDEPENDENTLY on its first real run, flagging io.ktor 15 matches to 0 at 3.6.0 with no fault injection; adjudicated (Ktor 3 left a one-class stub and the rules live on in ktor-server-core), with both known zeros recorded as a denylist naming the evidence and the sibling so neither can swallow a real regression. Noise floor 2 of 84. COST 33s cold, 71 downloads, 83 MB — and the recommendation is a WEEKLY job plus a pre-release sweep rather than a blocking PR gate, on the argument that the run is NOT HERMETIC: its verdict depends on what Maven Central published today, not on the commit, and a gate that reddens for reasons the PR did not cause is the shape that gets disabled. |
| R509 **java: a SHIPPED CARDINAL SIN, and the purest [[R508]] case yet — [[R493]]'s Exposed rules key on `org.jetbrains.exposed.sql.QueriesKt` (0.52.0), and Exposed 1.0.0 MOVED THE LIBRARY to `org.jetbrains.exposed.v1.jdbc.QueriesKt`. Every owner-equals misses, the block falls to `return null`, and `org.jetbrains` IS κ-covered — so that null is a CERTIFIED PURITY CLAIM** | 2026-09-20 | java | **MEASURED with kotlinc 2.4.10 against the real exposed-core/exposed-jdbc 1.5.0 jars, library out of scope**: `0 functions reach effects`, `analyzed.count: 16`, `coverage: null`, `invisible: null`, and `deny Db` / `deny Unknown` / `deny Net` **all exit 0** over connect + transaction + insert + selectAll + `SchemaUtils.create` + `JdbcTransaction.exec`. **Held constant**: the same candor binary and the same kotlinc against the same API shape at **0.52.0** gives 9 functions `Db` and `deny Db` exit 1. Calibration: `DriverManager.getConnection` reddens the same tree | CLOSED `9667565` by keying on what did NOT move — the class simple name plus the module-relative segment, scoped inside `org.jetbrains.exposed.`. **Price stated rather than hidden: that is looser, so a future `QueriesKt`/`SchemaUtils` anywhere under the prefix is charged `Db` sight-unseen, including the 1.x `r2dbc` module, which was not measured.** **ZERO corpus REACH — and that IS the finding, not a caveat**: a census of all 372 jars found 0 referencing `org/jetbrains/exposed/v1`, because the corpus pins ONE VERSION PER LIBRARY, which is precisely R508's blind spot. The compiled consumer is the evidence |
| R510 java: **three names in `TESTCONTAINERS_READ_BACKS` are not read-backs — and this is NOT version drift but an AUDIT-BOUNDARY failure, which is why it is filed apart from [[R508]]** | 2026-09-20 | java | The set was enumerated off `GenericContainer`/`ContainerState`, but the RULE covers the whole `org.testcontainers.containers.` package. `getContainerInfo` is ABSTRACT on `ContainerState`, and the package's other implementation `ComposeServiceWaitStrategyTarget` answers it with `inspectContainerCmd(id).exec()` — in 1.7.3 through 2.0.5, **1.19.8 included**, i.e. in the very version the set was derived from. `getHost`/`getContainerIpAddress` reach `DockerClientFactory.getOrInitializeStrategy()`. Measured on a consumer compiled against the real 1.19.8 jar: pre-fix `inferred: []` with `deny Exec` and `deny Unknown` both exit 0 and a `c.start()` control at exit 1; post-fix all three `Exec` | **THE TEST DEFENDED THE DEFECT — for the third time this week.** `control_testcontainersPureAccessorsAndEnums` asserted `getHost()` null; deleted and replaced with the measured assertion. Price: `GenericContainer.getContainerInfo()` and a started container's `getHost()` are genuinely field reads and now over-charge. **Residual stated**: `getContainerId`/`getMappedPort`/`getPortBindings`/`getBoundPortNumbers` reach the inspect only THROUGH `getContainerInfo()` and stay carved out, because charging `getMappedPort(int)` was the measured over-reach that denylist exists to prevent CLOSED candor-java 9667565 — the carve-out was corrected and the test that asserted the defect was replaced with the measured assertion. |
| R511 **un-gating ⟨0.23⟩ puts a BODILESS SYNTHETIC entry in every report, and every reader that treats an entry as a UNIT must filter it — candor-swift found FOUR such readers in its own engine by sweeping, and one is VERDICT-AFFECTING** | 2026-09-20 | swift FIXED; rust, java, ts UNCHECKED | **The verdict-affecting one: ⟨0.29⟩ PEEK ATTRIBUTION.** A union entry's `loc` is `""`, so it matched neither list and published an `outOfScope` finding with an EMPTY PATH — and under ⟨0.30⟩ a non-empty `outOfScope` makes the verdict INCOMPLETE, **so an un-gated engine turns a GREEN GATE into EXIT 2 on any package containing a protocol.** Found by a test that COUNTS findings, not by anything that reads them. The other three: the query model (`path` could resolve a selector onto a synthetic entry; `fix` could compute a hoist plan naming a body that does not exist); `gate --report`, measured at three violations where the in-process gate reports two, the third naming `Ui.Backend.size`; and coverage registration CONFIRMED IN A SECOND ENGINE 2026-09-20: candor-ts's port hit the same class in a different reader — a synthetic union entry was SCORED as an AS-EFF-006 violation, taking deny Unknown from exit 0 to exit 1 over a package whose own stringify resolved its dispatch precisely. That is the FABRICATION direction, where swift's peek variant was fail-closed. candor-java already had the guard (Policy.java:778) and ts had none, so three of four engines needed a different reader fixed. The class is now confirmed structural rather than engine-specific. | **RUST AND JAVA HAVE ALREADY UN-GATED ⟨0.23⟩ AND ARE UNCHECKED FOR THIS — that is the priority, not the swift fix, which is done.** The class is structural rather than engine-specific: it follows from the rung itself, so any engine that un-gates without sweeping its own readers inherits it. Swift found all four only by grepping every reader of a `functions` list in its own engine; two of them ([[R497]]-adjacent query paths) are exactly the kind a fixture would not reach COORDINATOR SPOT-CHECK 2026-09-20, rust: the trigger is NARROWER there than swift's — a crate with a pub trait AND a local impl emits ZERO interfaceUnion entries, so swift's 'any package with a protocol' does not transfer as stated. What IS established for rust is the RISK and not the defect: grep finds exactly ONE filter of interface_union outside the producer (callers.rs:231, yesterday's frontier fix), and none in the peek/outOfScope path, the query model or gate --report, while its own A/B counted 179 local union entries across 1,608 crates. java by contrast filters explicitly (Policy.java:382, :778 — 'the report entries that are NOT units'). So rust needs a fixture that actually PRODUCES a union entry before anyone calls it clean; a glance is not that. CONFIRMED AND CLOSED IN RUST (candor-rust 5afcef4) — and my spot-check was half wrong in a way that mattered: 'a pub trait with a local impl emits zero union entries' holds ONLY when the trait is nested in a module, which is a SECOND defect (R513), not the narrowing I reported it as. At the crate root the same code emits a union row on a plain default scan. REACH measured at 220 of 500 registry reports (44%) carrying at least one union row, 1,413 rows — not a corner case. Four chokepoints fixed rather than 30 copies. Direction per reader: the peek attribution is FAIL-CLOSED and verdict-affecting (a path-less outOfScope row made the verdict INCOMPLETE at exit 2 under a deny rule whose only finding was the synthetic row), while gate --report, load_entries_inner and the stderr summary are FABRICATION — gate --report reported 2 violations where the same engine's in-process gate reported 1, i.e. §6.2 and §3.1 disagreeing INSIDE one engine. Global effect on 500 crates: fabrication only, no exit flips; but on a SCOPED rule 41 of 41 verdicts changed, 3 red-to-green and 38 from a fabricated AS-EFF-006 to the report's true INCOMPLETE, so the phantom row was masking the real verdict every time. |
| R512 ts: **a STRUCTURAL implementor of a FOREIGN abstraction publishes no `interfaceUnion` entry** — `registerStructuralImpl` filters to `projectFiles`, so `const x: ForeignIface = { m() {…} }` is invisible to obligation 2 | 2026-09-20 | ts | **PRE-EXISTING and NOT widened by the ⟨0.39⟩ port — reported by the porting agent rather than quietly left.** It is [[R475]]'s own shape one spelling over: the consumer supplies an effectful implementor, the engine can see it, and the chain does not carry it. The difference from R475 is only HOW the implementor is spelled — a structural object literal rather than a named `implements` clause — which is exactly the axis ⟨0.38⟩'s union clause and PART 89 were built around for a different key | Worth ranking against [[R511]] before anyone takes it: R511 is verdict-affecting in the fail-closed direction and live in engines that have already un-gated; this one is a silent miss on a spelling that TypeScript makes idiomatic. **The fixture that closes it should be the one that proves the named form still works**, or the fix trades one spelling for the other CONFIRMED AND CLOSED (candor-ts 54d76a6). Two-arm falsification with the consumer and the abstraction asserted BYTE-IDENTICAL across arms by the render script, so the only variable is how the implementor is spelled: the named implements form carried ['Net'] with deny Net exit 1 both before and after, while the structural form was [] with no invisible and deny Net at EXIT 0 before, ['Net'] and exit 1 after. Two adjacent holes went with it — a foreign implements on a class EXPRESSION, which .name?.text called named while its members mint as <structural>.m. The second half of the fix is the one that matters: the union emitter now reads an unnamed implementor's member through its minted unit instead of hedging the whole entry to ['Unknown'], because without it every package writing const plugin: SomeDepIface = {...} would publish Unknown regardless of what it does — closing the silence by flooding the other channel, which is exactly what c3_pure_only exists to catch. A/B over 524 npm packages: ADDED 387, REMOVED 8, CHANGED 46, with all 337 added union entries under a FOREIGN prefix naming a real package present on disk, zero local-prefix and zero unnameable. Every one of the 8 losses was traced to a body rather than counted. See R519 for the consequence the agent flagged. |
| R513 **rust: a trait+impl NESTED IN A MODULE publishes NO `interfaceUnion` row, so a chained consumer reads `inferred: []` with NO `invisible` — a silent under-report, and the module form is the NORMAL case in real crates** | 2026-09-20 | rust | **MEASURED on two dep crates byte-identical but for `pub mod backend { … }` around the code, each chained into a consumer**: at the crate root the union row `deproot#Backend::size` is published and the consumer's `go` carries `["Net"]` with `deny Net` at exit 1; nested in a module, NO row is published, `go` carries `[]` with no `invisible`, and **`deny Net` exits 0**. Mechanism at `scan.rs:4138-4175`: the LOCAL leg looks `inferred` up by `{ty}::{method}` while `trait_impls`' values are the self-type AS WRITTEN (`decls.rs:3127` pushes `Net1`, never `backend::Net1`), so the real key `backend::Net1::size` never matches and the row is skipped. **The FOREIGN leg has a `by_tail2` fallback for exactly this; the local leg does not** | **This is [[R475]]'s own shape reopened by a SPELLING, and it is why nobody could reproduce [[R511]] in rust — my own probe put the trait in a module and saw zero union rows, which I reported as a narrowing when it was a second defect.** Deliberately NOT fixed by the R511 agent: it is a PRODUCER change that adds rows across many crates and needs its own A/B and four-way, not a rider on a reader sweep. Fixture kept at `scratchpad/rustagent-r511/modnest` CLOSED candor-rust 96b547a. The local leg gets the same by_tail2 fallback the foreign leg has had, with the same refusal — one candidate only, never guess between two units under one tail. Both arms falsified against a pre-fix binary with the crate-root control HELD: module-nested went from no union row, inferred [] and deny Net exit 0, to depmod#backend::Backend::size, inferred [Net] and exit 1. A/B over 1,599 crates: ADDED 3,277, REMOVED 0, CHANGED 12, all twelve strictly additive field-by-field. The io#Write::write_all class is STRUCTURALLY IMPOSSIBLE on this leg — that defect was the foreign leg, whose owner comes from a path, while this leg's owner is crate_name by construction — and the agent said so rather than merely reporting a clean column. 400 additions carry a concrete effect across 138 crates, five ground-truthed to bodies. Gate level over 486 affected crates: deny Unknown and deny Net each moved ZERO verdicts, and that oracle was calibrated to a real spread first because the initial run printed all-zero from a command substitution resetting the exit status — the run was discarded rather than reported. |
| R514 **INSTRUMENT: PART 58 pins *"an `outOfScope` entry names the file its function is in"* — the exact clause [[R511]] violated — and its fixture HAS NO TRAITS, so the clause was never exercised and the part could not fail** | 2026-09-20 | instrument (conformance PART 58) | A path-less `outOfScope` row is precisely what PART 58 forbids, and rust shipped them: the producer diff over 500 crates found `tokio-rustls` 0.26.4 and 0.26.5 publishing `(io::AsyncRead::poll_read, '', excluded)`. **The part was green throughout** because a synthetic union row only appears when the scanned crate has an abstraction, and its fixture has none | **The vacuous-guard class, which this suite has recorded before — a control that cannot fail is not a control.** Remedy is one arm with a trait in the fixture, not a new part. Worth pairing with the observation from [[R511]]'s sweep that the defect had **44% REACH** — 220 of 500 real reports carry at least one union row — so this is a clause with a fixture that misses the common case, not an exotic one CLOSED 2026-09-20 in conformance/run.sh. PART 58's rust arm gains a CRATE-ROOT trait and impl, and the checker gains assertion C — no outOfScope entry may carry an empty path — stated over the WHOLE array rather than over the two functions the fixture names, which is the half that was missing. Verified non-vacuous rather than assumed: the fixture now publishes exactly 1 interfaceUnion row (Sink::put), so the part finally produces the input the defect needs; it passes because rust is fixed at 5afcef4. Assertion C calibrated both ways on the real report — clean gives bad=0, and adding the row rust actually shipped (io::AsyncRead::poll_read with an empty path) gives bad=1. The trait MUST sit at the crate root: R513 records that a module-nested one publishes no union row at all in candor-scan, so nesting it here would silently restore the exact blind spot this arm exists to remove. |
| R515 java: **the `org.ehcache.xml` descriptor gate was derived at 3.10.8, where the XML entry point takes a `URI` — but `ConfigurationParser(String)` (3.0.0-3.5.3) and `public parseXml(String)` (3.6.0-3.6.3) both reach `DocumentBuilder.parse(String)`, which is INVISIBLE to that gate. `org.ehcache` is κ-covered, so the `return null` was a certified purity claim** | 2026-09-20 | java | **MEASURED on a consumer compiled against the real ehcache-3.6.0 jar, library out of scope, arms differing only in `Classifier`**: pre-fix `functions: []`, `coverage: null`, `invisible: null`, and `deny Fs` / `deny Unknown` / `deny Fs Unknown` / `deny Net` **all exit 0**; post-fix `Fs` and `deny Fs` exits 1. A `Files.readAllBytes` control reddens the PRE arm, so the instrument could fail. This is [[R508]]'s own named residual — the one carve-out its sweep checked at a SINGLE version — closed by javap over all 56 published 3.x releases | **THE PREMISE WAS ATTACKED AND NARROWED, and the method is worth copying: `ConfigurationParser` is PACKAGE-PRIVATE in 3.0.0-3.5.3 and public only from 3.6.0, so the consumer-reachable window is 3.6.0-3.6.3 and not all 25 releases — found because the CONSUMER FAILED TO COMPILE, not by reading bytecode.** Price stated: `Fs` follows the SAXBuilder precedent for a `String systemId`, so on an `http://` systemId it is a MISLABEL rather than a silence, and it costs nothing from 3.7.0 where neither member takes a `String`. **A/B over 371 jars: ADDED 0 REMOVED 0 CHANGED 0 — ZERO REACH and measured as such**, since one jar of 372 holds any `org.ehcache` class and no constant pool names the method. Same structural cause as [[R509]] |
| R516 java: **R435's OWN CLASS RECURRING IN THE SENTENCE R435 WROTE, ONE COMMIT LATER.** The fix at `fd4199c` added *"A SINGLE String operand is not ambiguous and IS captured, twelve lines below"* — but capture needs TWO conjuncts, one String **and** `PATH_CTOR_OWNERS.contains(owner)`, and the capture had moved ~95 lines down, not twelve | 2026-09-20 | java | **FALSIFIED BY EXECUTION on the shipped jar**: `new PrintWriter("/tmp/pw.txt")` is one determined String, Fs-classified, and **NOT captured** — no `paths`, `incomplete:["Fs"]`. The clause beside it, *"in the ambiguous case this adds no capture, only disclosure"*, was CONFIRMED across five arms with `allow Fs /tmp/benign` exiting 1 on every one. So the sentence was half right, which is why it read as checked | **It points the FLATTERING way — it claims MORE capture than happens, so what it hides is a disclosure rather than a silence, and that is exactly why nobody re-read it.** The comment was fixed, NOT the code: the behaviour is correct and is the direction [[R425]] measured, where capturing on position alone published 2,910 fake `paths` over 119 jars. **And the conjunct was UNPINNED** — all five arms of `StringLocatorTailMaskingTest` tested the one-vs-two-String half and none the owner half; the new arm was falsified against a mutated engine and is **the only one of six that fails there**, which is simultaneously the proof the other five never covered it |
| R517 **INSTRUMENT: `bin/assert-audit.sh` is a DIFF auditor with NO WHOLE-TREE MODE, so a false safety assertion that survives its own commit is invisible forever after** | 2026-09-20 | instrument (`candor/bin/assert-audit.sh`) | **MEASURED against the live regex.** Run over `fd4199c~1..fd4199c` it printed [[R516]]'s false assertion and **PASSED**, because tests changed in the same range — and the tool says so itself: *"THIS TOOL CANNOT TELL you whether those tests actually exercise those assertions."* The stale clause then lived in the tree for six days, outside the reach of every later run. **220 emphatic spans and 174 vocabulary lines currently shipped in candor-java alone are unreachable unless someone happens to touch that line again.** Vocabulary holes probed individually: `IS captured`, `already answered`, `cannot drift` (while `can never disagree` matches), `never fires` (while `never happens` matches), `is a no-op`, and **the entire 66-line `byte-identical`/omitted-when-empty additivity family — the largest single family of safety claims in that engine, entirely invisible** | **Of the two lines it flagged in the agent's own commit, it flagged NEITHER engine comment** — it caught the test-side ones. Remedy is both halves: a `--tree` mode, because *the negative of a range audit is the answer that gets believed*; and the vocabulary additions in the file's own idiom — `already (…\|answered\|reflects)`, `cannot (…\|drift\|disagree\|over-attribute\|fabricate)`, `never (happens\|fires)`, `is a no-?op`, `byte-identical` PARTIALLY CLOSED 2026-09-20 — and HALF the recommendation was DECLINED on the file's own recorded evidence. The --tree census mode is added (candor/bin/assert-audit.sh): it enumerates the vocabulary-net lines and emphatic spans across a whole tree, explicitly NOT as a gate, because candor-java alone ships hundreds and a check that failed on those would be disabled within a day — the same posture kappa_census and rule_fires.sh take. THE VOCABULARY ADDITIONS WERE REFUSED: the file's own R339 note records that the vocabulary arm MISSED THREE TIMES and that the third miss retired the approach rather than its wording, so adding five more alternatives is a fourth attempt at an abandoned method. The census's value is that it makes the POPULATION enumerable even where the DETECTOR is imperfect. Note also that NEITHER existing arm could have caught R516 anyway: the structural arm keys on classifier files and R516 was a comment in Candor.java, which RULE_RE does not cover and where comment-only diffs explicitly do not count. |
| R518 **INSTRUMENT: PART 92's arms vary the package TOPOLOGY and never the SPELLING, so the part that exists to pin ⟨0.39⟩ could not have failed on either [[R513]] or [[R512]]** | 2026-09-20 | instrument (conformance PART 92) | **VERIFIED BY THE COORDINATOR: `grep` finds ZERO occurrences of `mod ` in `gen_chained_dispatch.py`** — every rust fixture, the sealed arm included, puts the abstraction at the CRATE ROOT. R513 is exactly the module-nested spelling, and its reach was 3,157 module-qualified additions across 255 packages, so this is the common case and not an exotic one. **PART 92 was green with an EMPTY xfail table throughout, and that greenness carried no information about this shape.** [[R512]] is the same rung missing a DIFFERENT spelling — a structural object literal where ts handles the named `implements` form | **The same vacuous-guard class as [[R514]]/PART 58, now in the part built to pin this very rung — which is the sharper instance, because PART 58 was an old part meeting a new rung while this one was written FOR the rung and still varied the wrong axis.** Remedy is one arm with the abstraction in a module, not a new part. The generalisable lesson the R513 agent drew is the row: **six arms that differ in WHICH PACKAGE holds what, and none that differ in HOW THE ABSTRACTION IS WRITTEN** CLOSED 2026-09-20: PART 92 gains c7_nested_abstraction — the abstraction and an EFFECTFUL implementor declared inside a MODULE, the consumer dispatching on the abstraction itself. CALIBRATED AGAINST A PRE-R513 BINARY built from 96b547a~1, and that calibration was not a formality: THE ARM WAS VACUOUS THREE TIMES, each for a different reason, and each time it PASSED against the pre-fix scanner. (1) I first nested the PURE TestBackend while the effect came from the foreign effimpl, which uses the leg that always had a fallback. (2) I then made the nested implementor effectful, but the consumer still delegated to iface::term_size, so the dep resolved its own dispatch by bounded CHA and the effect arrived through the ORDINARY chain join. (3) I gave the arm its own consumer body and app_body ignored it, returning dispatch for everything but sealed. Only after the third fix does it discriminate: PRE it FAILS on rust with missing Net while c1 stays OK, and at HEAD both pass. java and swift DECLARE the arm inexpressible with reasons — java has no re-export so nesting forces three variables instead of one, and swift has no submodules within a target — and a SKIP cell is PRINTED, because a cell that says nothing reads like a pass. |
| R519 **CONFIRMED AND LIVE IN v0.39.0 — four of the eight re-attributed rows are ORPHANED, and one takes a real gate from exit 1 to exit 0.** `mintStructuralMembers` mints EVERY property of a literal, withdrawing the enclosing function's containment charge for all of them, while the emitter can only answer for properties an `InterfaceDeclaration` DECLARES — so **minting withdraws on a BROADER condition than publication restores** | 2026-09-20 | ts — LIVE IN v0.39.0 | **PRE-EXISTING ASYMMETRY, surfaced (not created) by [[R512]]'s fix, and reported by the agent rather than banked as a clean A/B.** Measured on `ee844f0` BEFORE the fix: a factory returning a LOCALLY-typed effectful literal was already ABSENT, while the same factory returning a FOREIGN-typed or UNTYPED one was charged. R512 moves the foreign spelling onto the local side, so the engine now has ONE rule where it had two — the consequence is real and the population is exactly the 8 rows that lost an effect in that A/B, all by RE-ATTRIBUTION: `sortAST` passes a `graphql` visitor literal to `visit()`, and its `Unknown` now sits on the sixteen visitor methods rather than on `sortAST` PANEL RE-MEASURED 2026-09-20 AND THE ROW'S TRIGGER WAS WRONG — I drew the audit boundary around the instance I was handed, which is the failure CLAUDE.md names explicitly. It is NOT 'a union contextual type with a mapped arm'. It is ANY function-valued property that no registered interface DECLARES. Measured 0.38.3 vs 0.39.0 on a single package: viaUnionLit (union with a plain type-literal arm) ['Rand'] to []; viaIndexed (an index-signature interface, NO UNION AT ALL) ['Rand'] to []; viaExtra (a declared member plus one extra) ['Rand'] to []. Controls held: viaUntyped keeps ['Rand'] in both arms and viaMethod still publishes MethodShaped.run. **Index-signature handler maps are ordinary TypeScript**, so the population is far wider than graphql's ASTVisitor. AND IT IS NOT CROSS-PACKAGE-ONLY: in a SINGLE-PACKAGE scan with no chain and no unscanned dispatcher, pure and deny Rand both go exit 1 to exit 0. COORDINATOR RE-RAN THE A/B INDEPENDENTLY 2026-09-20, own entry list built by filtering the 53-entry corpus rather than reusing the agent's (the two lists came out IDENTICAL at 47 — the 6 dropped are pure-JS packages with no .ts, exit 2 on BOTH arms, so excluding them removes no evidence). Engines installed from npm into separate prefixes, fix = candor-ts 82da250. All three figures reproduced EXACTLY: v0.39.0 to fix ADDED 24 REMOVED 0 CHANGED 80 (inferred-only ~10); v0.38.3 to fix ADDED 131 REMOVED 0 CHANGED 1007 (inferred-only ~13); baseline v0.38.3 to v0.39.0 ADDED 110 REMOVED **3** CHANGED 991. REACH 223 hits across 11 entries, identical per-entry breakdown (ajv 103, apollo-server-core 47, mongodb 32, zod 11, hono 11). The three baseline removals named from the --out JSON: apollo-server-core#ApolloServerPluginLandingPageGraphQLPlayground, ajv-formats#src.limit.<module>, apollo-server-core#<structural>.requestDidStart. REMOVED 0 on the 0.38.3-to-fix arm is what proves all three RETURN: every unit present at 0.38.3 is present at the fix. ajv-formats#src.limit.<module> was ALSO reproduced by hand before the sweep ran — present at 0.38.3 with invisible:['ajv'], ABSENT from functions[] at 0.39.0, restored at HEAD now edging to its three minted units — so the finding has two independent routes. | **The question to settle is whether the effects are still REACHABLE from the caller or merely RELOCATED.** If `visit()` is opaque, the edge from `sortAST` to those sixteen methods is invisible and the effects are ORPHANED — a caller reads `['Env']` over code that performs `Net` and `Rand`, which is a silent under-report however sound the attribution is in isolation. **⟨0.39⟩'s own text is the argument for the new behaviour** — it records charging a closure by SYNTACTIC CONTAINMENT and *"a passed abstraction implementor nothing"*, which IS the post-fix rule — so this is a genuine consistency gain that may have bought a disclosure loss. Measure the 8 rows' callers before deciding; do not close it on the spec quotation alone **MEASURED, both halves.** Rows 1-4 (`apollo-server-core`) are REACHABLE and post-fix STRICTLY MORE so — a compiling consumer chained to each arm reads `[]` `unresolved:false` PRE over Net/Rand/Clock-performing code and gets the effects POST on `apollo-server-plugin-base#GraphQLServerListener.*`; PRE published no such entry at all. **Rows 5-8 are ORPHANED**: `sortAST` passes its visitor literal to `graphql`'s opaque `visit()`, and the sixteen minted methods land under the PRODUCING package's own prefix with NO `interfaceUnion` entry — `candor callers` returns 0 direct and 0 transitive, `candor path` stops, `candor impact` 0. Cross-package with everything else held constant: `deny Unknown normalise` **exit 1 PRE, exit 0 POST**. `invisible: ["graphql"]` survives so it is not a BARE purity claim — but `deny Unknown` reads `inferred`, which went `['Unknown']` → `[]` with `unresolved:false` **THE TRIGGER IS A UNION CONTEXTUAL TYPE WITH A MAPPED ARM**: `graphql`'s `ASTVisitor = EnterLeaveVisitor<ASTNode> \| KindVisitor` puts the effectful member names in the mapped-type arm, which the emitter cannot iterate. Falsified BOTH ways — a pure local mapped alias keeps its charge in both arms (nothing minted), and the identical LOCAL-union shape is byte-identical pre and post, so **the class is PRE-EXISTING on the local arm and [[R512]] extended its REACH to foreign abstractions, which is how it reached ecosystem code.** No fix was improvised, correctly: both directions here remove or restore an `Unknown` and that is where this register's own record says silences get introduced THREE MITIGATIONS I CLAIMED ARE FALSE, each measured rather than argued. (1) 'Disclosed via invisible' is SELF-COMFORT: the invisible entry is BYTE-IDENTICAL in the arm where the gate fires and the arm where it does not — it discloses the foreign CALL, the same string every foreign call carries under ⟨0.30⟩, not that sixteen bodies the engine READ and placed in the report as units are reachable from nothing. R133 rules it non-gating and PART 4s pins 'invisible OR Unknown', so the one instrument that looks at this key accepts the pre-to-post transition BY CONSTRUCTION. (2) 'Pre-existing on the local arm' AGGRAVATES rather than mitigates: the local arm still DISCLOSES (viaUnionLitL and viaIndexedL both read ['Unknown'] at HEAD) because it has an in-scan dispatch site; the foreign arm has none, so R512 moved the withdrawal to the one place nothing can disclose it. Same mechanism, different honesty. (3) 'Both candidate directions move an Unknown' is FALSE: the sound fix RESTORES charges and is monotone-up against both shipped arms, so the 4-defects-in-5-fabrication-fixes record — which is about REMOVING over-charges — runs the other way here. Also measured: consumer-side gates are silent too, not merely scoped producer ones, and viaMethod (R512's own success case) reads [] when only the unscanned dep dispatches. **CLOSED — candor-ts `82da250`, SHIPPED IN v0.39.1 2026-09-21 and verified live.** `mintPositionalStructuralUnit` now edges the lexically enclosing unit to every structural member it mints, so minting is an ADDITION (an addressable unit for dispatch) and never a MOVE. Unconditional; failure direction OVER-charge — the same over-approximation already applied to every closure the engine does not mint. Aliases (`go: other.method`) excluded, since referencing a fn is not calling it. R512's minting and foreign `interfaceUnion` publication are preserved and asserted. Corpus A/B RE-RUN INDEPENDENTLY by the coordinator on a separately-derived 47-entry list that came out identical to the agent's: v0.39.0-to-fix REMOVED 0, v0.38.3-to-fix REMOVED 0 — monotone-up against BOTH shipped arms — and the baseline's three removals all return. 36 fixture assertions written FIRST, 22 red pre-fix. Cut scoped via ENGINE_PIN_TS=0.39.1, WHICH MUST BE CLEARED AT THE NEXT FAMILY CUT. The disclosure went out the same day: the published v0.39.0 release notes on candor-ts and the umbrella carry a known-issue block naming the trigger, the affected versions and the pin workaround. See [[R524]] for the OLDER foreign-interface asymmetry this work surfaced and did NOT close. |
| R520 **a USAGE ERROR creates a directory tree in the user's CWD, named after the mistyped argument** — `candor-scan nonexistent-target /also-bogus` exits 2 correctly and leaves `./nonexistent-target/.candor/report.refused.json` behind | 2026-09-20 | rust + ts (java and swift are CLEAN) | **FOUND AS LITTER IN OUR OWN REPO, not by any gate.** An agent mistyped a scan invocation from `~/git/candor` and left `gate/.candor/report.refused.json` — a directory called `gate` that nothing in the umbrella expects. Reproduced in a clean temp dir on all four engines: **rust CREATES it, ts CREATES it, java does NOT, swift does NOT.** All four exit 2, so the REFUSAL is right in every engine and only the filesystem effect differs | **The ⟨0.28⟩ fail-closed sink exists so a STALE report cannot be read as current — it REPLACES a previous run's document. Here there was never a report to replace, and the target does not exist, so the write has nothing to fail closed OVER.** That is the distinction the two engines miss. It matters more than tidiness in this family: ⟨0.28⟩'s own review found the rung DESTROYED USER FILES four-way, so what a refusal writes is exactly the surface this project has already been burned on. **java and swift being clean means no clause has to change — this is two engines disagreeing with the other two, and the correct behaviour is already shipped in half the family** RUST HALF CLOSED (candor-rust 196f049); ts half still open. The fix is ONE guard on the default-prefix latch, and the boundary it draws is the point: --out <pfx> is a path the OPERATOR NAMED, independent of the target, so a previous run over a different existing target may have left reports there and today's typo must still shadow them — that arm is deliberately untouched. <target>/.candor/report is a CONVENTION computed from the target, and an absent target cannot hold reports, so the write has nothing to fail closed over. VERIFIED BY MUTATION IN BOTH DIRECTIONS: reverting the guard reddens arm A (the directory appears in the operator's CWD), and applying the OVER-BROAD fix — silencing the default latch entirely — reddens arm B, which asserts the CONSUMER resolves the marker rather than merely that a file exists, and seeds a real completing scan first so it cannot pass vacuously. Generated argv sweep over 1,575 three-token argvs: residue 459 to 150, and every one of the 150 survivors is an operator-declared sink. Zero-REACH stated precisely and more sharply than the A/B admits: under --out the changed branch is never evaluated at all, so reach was measured separately on --out-free copies (guard evaluated 6/6, suppression arm 0/6, all 82 default-prefix files byte-identical). Gates 31/31. TS HALF CLOSED TOO (candor-ts b1df57e) — R520 is now closed in all four engines. The ts fix is narrower than the obvious one and deliberately so: NOT a blanket existsSync on the dirname, because that spelling would mute the marker on a FRESH PROJECT, which is the case the ⟨0.32⟩ rung exists for, and would have reddened CLI-1b's own control. The marker may create its own .candor and never a tree above it. Revert test run rather than reasoned: exactly one check reddens and both controls stay green. A/B over 197 packages is ADDED 0 REMOVED 0 CHANGED 0 with REACH 0 hits across 0 entries — stated as zero-REACH rather than inert, because a corpus of SUCCESSFUL scans never reaches a refusal path at all, so the fixture is the whole of the evidence. |
| R521 ts: **neither structural-implementor walker crosses the local/foreign boundary** — `registerStructuralImpl` climbs only LOCAL super-interfaces and `registerForeignStructuralImpl` only foreign ones, so `interface LocalSub extends dep.MethodShaped` publishes NOTHING under `depiface#MethodShaped.run`, and `LocalSub.run` is not published either because the emitter iterates only the sub's OWN members | 2026-09-20 | ts | **PRE-EXISTING and byte-identical pre/post [[R512]] — measured on an isolated fixture, so this is not a regression.** BOTH spellings are silent: the structural literal typed `LocalSub` and the nominal `class … implements LocalSub`. **It is exactly the shape of `InternalApolloServerPlugin extends ApolloServerPlugin`**, which is real ecosystem code in the same package the [[R519]] measurement ran against | The extends-across-the-boundary case is the third spelling of [[R475]]'s class, after the named form (⟨0.39⟩) and the structural form ([[R512]]). Worth fixing with them rather than alone — and worth noting that PART 92 still has no arm for ANY of these spellings beyond c7's nesting, which is [[R518]]'s point recurring |
| R522 ts: **`candor-ts . /also-bogus` dies with an UNCAUGHT `EROFS` stack trace — no exit 2, no usage error, and no refusal marker** | 2026-09-20 | ts | The second positional is taken as the legacy `--out` prefix, and `contract.mjs:155 writeSinkAtomic` throws before `refuseEarly` can run, so the throw bypasses the refusal path entirely. Found in passing while fixing [[R520]]'s ts half and reported rather than folded into it | **CLOSED 2026-09-23 — candor-ts `a70e435`.** The final write of the primary report set (`scan.mjs:11067`) had no `try`/`catch`, so a valid target ran a FULL SUCCESSFUL SCAN and then died in `writeAtomic` → `writeSinkAtomic` → `fs.writeFileSync` with an uncaught `EROFS`: exit 1, no usage error, no refusal marker. **R520's `nonexistent-target /also-bogus` control never caught it because that arm refuses at ARGV-PARSE time, before any write — a different code path, which is why a neighbouring green control read as coverage.** Fixed by routing the write failure through the existing `refuseEarly(why)` + exit 2 path, naming the real OS error. Calibrated by reproducing the crash BEFORE the fix was written, then a PORTABLE regression (CLI-1d, `chmod 555` on a scratch dir rather than depending on macOS SIP for `EROFS`) asserting exit 2, a named cause, NOT a stack trace (`!/at Object\.writeFileSync/`), and nothing created in the unwritable directory. Gates: `npm test` green with the new lines named in the log; `self-gate` green. **A stack trace is not a verdict.** §3.3.1 makes a typo'd or not-applicable flag an exit-2 usage error, and every other refusal route in this engine produces a fail-closed document — this one produces a crash, which a CI consumer reads as neither pass nor refusal. Sibling of [[R520]] (both are the refusal path answering wrongly) but a different defect: R520 wrote too much, this writes nothing and exits wrongly |
| R523 java: **five jgit callers fork from PRIVATE methods behind a public API that is NOT charged** — `DirCacheCheckout.runExternalFilterCommand`, `PatchApplier.filterClean`, `WorkingTreeIterator.filterClean` (the gitattributes `filter.*.clean` driver), `diffmergetool.CommandExecutor.run`, and `TransportLocal.spawn` (`git-upload-pack` over `file://`) | 2026-09-20 | java | **SWEPT AND LEFT OPEN DELIBERATELY by [[R498]]'s agent, which is the right call and the reason this is a row rather than a silent gap.** Every class in the jar was enumerated for a body call to FS's fork surface; these five remain. Closing them means charging `CheckoutCommand.call`, `ApplyCommand.call` and a local `fetch` — **and none of those was RUN. An unrun widening is a guess**, and the absence is recorded in the rule and in the test so it cannot later be read as a purity finding | Same direction as [[R498]]: `org.eclipse` is not κ-covered, so an omission here keeps its `invisible` floor and is DISCLOSED. **Close it the way R498 was closed — with a shim on PATH and a consumer that actually performs the checkout/apply/fetch — not by reading `javap` and widening.** R498's own fix chose an enumeration over R480's whole-package shape for the same reason: whole-package `Exec` over `org.eclipse.jgit` would label `ObjectId`, `Ref` and `RevWalk`, and [[R499]] is what that costs |
| R524 ts: **a call on a member of a FOREIGN interface is answered by the external-call path, where its LOCAL twin hedges — so the foreign arm says nothing and the local arm says `Unknown`** — surfaced by the [[R519]] work and NOT closed by it. Byte-identical fixtures but for where the interface is DECLARED: with `interface Handlers { [k: string]: (n: number) => number }` in the project, `viaIndexed` reads `inferred:['Unknown'] unresolved:true unknownWhy:['callback:src.main.Handlers']` — a DISCLOSURE. Move the same declaration into `node_modules/dep/index.d.ts` and it reads `inferred:[] unresolved:false invisible:['dep']`. The effect is real and reachable in both; only the disclosure differs. | 2026-09-21 | ts |**NOT a regression and NOT R519 — it predates it by many releases.** Measured present at v0.34.0, v0.36.2, v0.38.3, v0.39.0 AND at the R519 fix (candor-ts `82da250`), which restores containment and leaves this untouched. The mechanism is that a member call on a foreign interface falls to the external-call/κ path and takes its `invisible` floor, while the in-project twin reaches the callback hedge. **Whether this is a SIN turns entirely on R133**, which ruled `invisible` NON-GATING: if a consumer may treat `invisible:['dep']` as a disclosure that the scan did not answer for this call, the foreign arm is disclosed-and-honest; if not, it is a silent under-report wearing a key that no gate reads. PART 4s pins `invisible OR Unknown`, so the one instrument that looks at this key accepts BOTH arms by construction and cannot separate them. |**OPEN — RE-SCOPED 2026-09-23, AND IT IS NOT A RULING. THE "FAMILY-LEVEL DECISION" DISSOLVED UNDER REVIEW BEFORE IT WAS ESCALATED.** **This row conflated TWO fixture shapes with different answers, and neither needs a ruling.** Measured four-way at HEAD, holding everything constant but WHERE the abstraction is declared. **TS HALF CLOSED 2026-09-23 — candor-ts `d46c098`** (rust's half is [[R556]], still open). **SHAPE (i) — the consumer's OWN effectful implementor is visible.** This is a PORT GAP against text that ALREADY BINDS: ⟨0.35⟩ (SPEC.md:4655) requires the caller's `inferred` to carry a visible structural implementor's effects or `Unknown`, *"where a synthesised or structural implementor is VISIBLE to the engine's own resolution"*, with no local-abstraction restriction; ⟨0.39⟩ obligation 3 says *"its own visible implementors"*. **Two engines of four, each on ONE spelling: ts (an index-signature member) and rust (a `let`-position `dyn`). java and swift are CLEAN** — they charge the effect on both arms. rust's is gate-visible: `deny Fs via_indexed` and `pure via_indexed` go **1→0** on the foreign twin while that same run publishes the union entry `dep#Handlers::roll` carrying `Fs`. Filed for rust as [[R556]]; ts's is R519's hedge ported to the local arm only. **SHAPE (ii) — nothing implements the abstraction anywhere.** NOT open and never was: it is the position PART 92 **PINS** (`gen_chained_dispatch.py`, arms c5/c10 assert `invisible=True, unknown=False` for exactly this), and its tightening is [[R533]], the ⟨0.39⟩ (the clause once drafted as 0.40) candidate already under a per-engine A/B. A ruling here would have DUPLICATED R533. **WHY R133 DOES NOT SETTLE IT, which this row asserted it did (*"turns entirely on R133"*): R524's foreign rows carry a FALSE `invisible`.** `dep` has no code (a types-only `.d.ts`; a bodyless trait), no function of `dep` is ever called, and the effect lives in the CONSUMER's own file — so this is not about `invisible`'s gating force at all. The remedy the disclosure names was tested and **cannot work**: chaining `dep`'s report leaves `via_indexed` at `inferred:[]` with gates still at exit 0. **AND THE CONTROL IS VACUOUS, as this row suspected — verified: PART 4s's `fn_ok` (`conformance/run.sh:1394-1398`) is true if ANY function carries `invisible` naming the dep OR ANY function anywhere carries `Unknown`, so both arms pass by construction.** It is also the wrong instrument: the separating arm belongs in PART 92 beside c8/c9/c10 — *consumer's own effectful implementor, unchained, direct dispatch*, `want=disclosed(effect)` per the ⟨0.35⟩ disjunction, in the index-signature and let-`dyn` spellings, with the declared-member and signature spellings as controls that must stay green. **THE PROHIBITION BELOW STANDS AND WAS CONFIRMED MECHANICALLY**: a blanket hedge would red PART 92 c5/c10 and re-buy the in-crate hedge ⟨0.39⟩ declined at 2.60% of functions. It does not bear on these narrow fixes, which fire only where a LOCAL implementor is VISIBLE. Do NOT close this by widening the foreign arm to hedge `Unknown`: that is a NON-ADDITIVE change to every consumer of every dependency-typed callback in the ecosystem, and the ⟨0.39⟩ toggle is the warning — a hedge added everywhere is a disclosure nobody reads. The three candidate directions, in the order they should be priced: (a) re-open R133 and make `invisible` GATING for a call whose target the scan READ but could not attribute, which is narrower than it sounds; (b) mint a distinct key so a consumer can tell 'foreign, unmodelled' from 'foreign, modelled-pure' — the [[candor-unanswerable-key]] three-row rule applies, since absence licenses a purity claim only if the key could have had a body; (c) rule it WORKING AS INTENDED and write the asymmetry into the SPEC so it stops being re-found. **Whichever wins, PART 4s must gain an arm that can distinguish the two spellings** — a control that accepts both is the vacuous-guard class. |
| R525 rust: **AN UNRELATED RULE USING A CONFIG-DEFINED `unknown-alias` ERASED THE PEEK'S DISCLOSURE AND TOOK A REAL GATE FROM EXIT 2 TO EXIT 0** — a cardinal sin, SHIPPED in the published candor-scan 0.39.0 crates. The ⟨0.29⟩/⟨0.30⟩ peek parsed the policy with the alias-less `parse_policy` (`scan.rs:4429`) while the gate used `parse_policy_with_aliases`, so an `Unknown[<config alias>]` was an unrecognised reason-class, `fatal_messages()` was non-empty, the peek's §3.1 refusal arm returned `None`, and the fail-closed verdict never armed. This is the rust instance of [[R220]]'s four-way question, which is therefore ANSWERED rather than open for this engine. | 2026-09-21 | rust |**Reproduced by the coordinator from a scratch fixture before any fix, then ground-truthed on real published code.** Fixture: crate with a pure `src/lib.rs`, a `build.rs` whose `grab()` does `TcpStream::connect`, `.candor/config` = `unknown-alias corp = reflect`. On candor-scan 0.39.0 — `deny Net` exit 2 with `outOfScope` naming `build::grab`, `scannedUnder` present, `peeked:true`; **`deny Net` + `deny Unknown[corp]` exit 0 \"policy ✓\" with all three ABSENT/false**; `deny Net` + `deny Unknown[reflect]` (BUILTIN class, the CONTROL that rules out 'a second rule' as the cause) exit 2 like the first. AT SCALE, 1,593 crates.io registry crates, published-0.39.0 binary vs fixed: peek-key census `outOfScope` ABSENT-to-present 1593 / lost 0, `scannedUnder` 1593 / 0, `peeked` false-to-true 1059 / 0; 108 packages gain a NON-EMPTY `outOfScope` and **56 FLIP EXIT 0 TO EXIT 2** — serde_json, rustls, h2, syn (11 versions), tokio-util, native-tls, quinn-proto, rdkafka, hickory-resolver. GROUND-TRUTHED FROM SOURCE, NOT FROM CANDOR: serde_json 1.0.151 `tests/test.rs:1644` really does `TcpListener::bind(\"localhost:20000\")` inside `test_deserialize_from_stream`, and the fixed binary names exactly that unit. Control: plain `deny Net` with no alias anywhere moved 0 of 1,593 packages, so the hoist is inert where no alias is involved. |**CLOSED — candor-rust `45f3730`.** The policy-anchored `.candor/config` is discovered ONCE before the peek and all three `parse_policy*` sites in `scan_one` read that binding, so the two cannot anchor differently — the defect ⟨0.24⟩ closed between the scan route and `gate --report`, reopened one level down. **FAILS CLOSED**: the peek is handed only the vocabulary the gate already had, so a genuinely unhonourable policy is still refused — `deny Unknown[nosuchtok]` stays exit 2 and publishes no `outOfScope`/`scannedUnder`, pinned as a FOURTH arm, because buying the first three by making the peek permissive would trade a silent under-report for a silent over-acceptance. **A HALF-CONVERTED CALL SITE, NOT A MISSED IDEA** — `parse_policy_silent(&text, &unknown_aliases)` had ALREADY landed at the zero-rule check (`scan.rs:5219`) while both siblings in the same function stayed bare; the audit was widened past the two lines in the brief to every `parse_policy*` call AND every `fatal_messages()` consumer, i.e. the PROPERTY rather than the name. The `corpus-ab.py` 0/0/0 on function rows is the OVER-CHARGE control, not a measurement of the intended direction — this fix moves top-level keys, which that instrument does not diff — and REACH was 1593/1593, so it is not a zero-reach result. Four-arm test at `crates/candor-scan/tests/cli.rs`, watched RED pre-fix. Cross-engine: candor-ts and candor-swift both CLEAN with calibrated fixtures; **candor-java UNEXAMINED at filing.** RESIDUAL, needs its own row: `src/lib.rs:371`, the nightly dylint driver, parses `CANDOR_POLICY` alias-less and discovers no config at all — a real divergence (it REFUSES a policy candor-scan honours) but FAIL-LOUD, not this sin. |
| R526 java: **the peek EXITS 2 WITH AN EMPTY `outOfScope` when the policy names a config-defined `unknown-alias`** — the same root cause as [[R525]] (the peek does not resolve `.candor/config` aliases the way the gate does) with the OPPOSITE failure direction. rust went to exit 0 over a real violation, a cardinal sin; java refuses, so it FAILS CLOSED and no gate goes green — what is lost is the ⟨0.30⟩ disclosure, the thing the rung was justified on. The operator is told the verdict is incomplete and NOT which unit was out of scope. | 2026-09-21 | java |**Found by the conformance matrix within minutes of the `deny-unknown-alias` shape being added to PART 55's SHAPES table, which is the point — the matrix had eleven `Unknown[…]` forms and every one named a BUILTIN reason class, so no arm had ever needed the project's `.candor/config` and this whole class was unreachable.** PART 55 cell: `java deny-unknown-alias EMPTY-FINDING exit 2 with an EMPTY outOfScope`. The matrix's own EMPTY-FINDING guard caught it — that guard exists because \"a peek that always answers 2 with an empty block passes every cell\", and it earned its place here. The new shape is CALIBRATED: run against the PRE-fix candor-scan binary (196f049, built in a throwaway worktree) it reports `rust deny-unknown-alias DISAGREE in-scope gate=1 -> peek should be 2, got 0`, so the arm demonstrably catches R525 rather than being a row that has never failed. ts and swift agree on this shape in both placements. | **CLOSED — candor-java `1d51e23` (filed as [[R530a]]).** The predicted mechanism — a half-converted call site — was WRONG: it is a THREAD boundary. `ctx().unknownAliases` is thread-local, the peek runs on its own thread with a fresh context, and `Policy.parsePolicy` reads that map as its only ctx input; the main thread resolved the vocabulary immediately before `peekExcluded` and nothing carried it across. Recall measured as an envelope diff over five real repos: **32 out-of-scope findings withheld pre-fix**. PART 55's XFAIL line went PASSING and has been RETIRED (candor-spec `3f87ba2`); that table is empty and all four engines agree, 44 cells / 0 disagreements. **The text below was true when written and is kept for the measurement:** **OPEN, DECLARED, AND TRACKED — entered in PART 55's new `XFAIL` table as `(java, deny-unknown-alias)`, modelled on PART 92's.** Two rules make that a ledger rather than an excuse list: a PASSING xfail is a FAILURE (when java is fixed the cell agrees and the part goes RED until the line is retired — calibrated by declaring an xfail on a cell that already agrees and watching `XFAIL-PASSING` fire), and every entry names a row. **The bar for a line here is that the divergence FAILS CLOSED**; R525's rust arm exited 0 over a real violation and was NOT given one — it was fixed before this shape landed, because a cardinal sin does not get an exception. Why it is xfailed rather than fixed now: blocking every family release on a fail-closed DISCLOSURE gap, while the unsafe arm is already closed, prices the cure above the disease — but leaving the suite green over a known divergence is the vacuous-guard shape this register keeps re-finding, so it is declared instead of hidden. **Fix direction (unverified, read from source not measured): `AnalysisContext.java:57-59` already holds `unknownAliases` and its comment says the vocabulary source is populated \"before parsePolicy\" — so this is likely the same half-converted-call-site shape as R525, where one route got the alias map and the peek did not. Confirm before believing it.** |
| R527 instrument: **`soundness-status.py` read `Not fixed.` as FIXED for its entire life, so the list it exists to print — \"THIS is the shipping-defect list\" — was missing a THIRD of itself.** `CLOSURE` matched the word `fixed` and nothing looked to its left, so a row whose outcome cell OPENS with `Not fixed.` was bucketed as resolved. The register's own cardinal sin, committed by the instrument that measures it: a silent under-report of what is open. | 2026-09-21 | instrument (candor-spec/scripts) |**43 rows reclassified on the live register, measured not estimated.** Buckets before -> after: closed-with-fix 376->365, resolved-no-fix 88->56, cites-a-sha-only 3->14, **open 23->55**. So 32 rows moved resolved-no-fix -> OPEN and 11 moved closed-with-fix -> cites-a-sha-only. The negated word was `fixed` 49 times and `closed` once; **ZERO rows moved on `resolved`**, which was the over-correction risk worth measuring, since `unresolved`/`could not be resolved` is candor's vocabulary for DISPATCH rather than for a row's status. Sampled four of the 32 and read them: R218 \"Not fixed. One producer, or a normalisation at the writer.\"; R289 \"Not fixed. **The operational instruction until it is:** …\"; R156 \"Not fixed. Converge on `rust_bin`\"; R137 \"Not fixed. **No effect is hidden TODAY** …\" — every one a live defect with an un-applied remedy. Found while fixing the SINGLE row I had noticed ([[R524]], \"NOT closed by it\"); the one-row fix exposed forty-two more. |**CLOSED — candor-spec, this commit.** `_NEGATED_CLOSURE` strips an explicit negation adjacent to a closure word before `CLOSURE` is asked, alongside the existing `_NOT_A_VERDICT` strip for the fail-closed idiom. Scoped tightly to `not\|never\|isn't\|wasn't\|aren't` + optional `yet\|been` + the closure verb, so the domain uses survive — asserted, not assumed. **NOW GATED AND CALIBRATED**: `--selftest` with 8 cases is wired into `scripts/doc-gates.sh`, and removing the strip fails exactly 3 of them. Three of the 8 are NEAR-MISSES that must NOT move (`CLOSED … the callee is not resolved through the alias`, `CLOSED … fails-closed on an unresolved import`, `the dispatch could not be resolved … \\`deadbee\\``), because a strip that ate those would empty the resolved bucket instead and be the same failure pointing the other way. The selftest also caught MY expectation: I wrote R524's shape as `open` and it is `cites-a-sha-only` — a row citing a sha while denying closure is honestly \"neither clearly, read it\", and what matters is that it is no longer invisible under closed-with-fix. **Every \"N open\" figure quoted from this tool before today is a LOWER BOUND**, including the 23 I reported hours earlier in this session. **INCOMPLETE AS FIRST FIXED — a review caught two more, and both were measured rather than argued.** (1) The tool matched `^\| R[0-9]+` across the WHOLE FILE, sweeping in the ACCEPTED-FLOOR table (R1-R12 and friends), whose schema is `\| id \| engine \| description \| SILENT \| low \| remedy \|` — its 5th cell is a SEVERITY, not an outcome, so all 60 of those rows carried no closure word and printed as OPEN with garbled text at the top of the shipping-defect list. That is also why the tool's row count (491) disagreed with `grep -c '^\| R'` (453). Now scoped to the table whose HEADER is `\| entry \| date \| engine \| class \| outcome \|`, by the same contiguity rule check_soundness_tables.py uses, and it REFUSES loudly if that header is absent rather than printing an empty list. 431 register rows. (2) An EXPLICIT declaration now beats inference: [[R524]]'s cell opens `**OPEN — needs a family-level ruling**` and cites R519's SHA to name the work that did NOT close it, so the tool filed it under cites-a-sha-only — out of closed-with-fix, but still off the list its author put it on. `_DECLARED_OPEN` is anchored to the START of the outcome cell so the word in ordinary prose (`this left an open question for R99`) cannot move a closed row; that near-miss is a selftest case, and removing the rule fails exactly one. FINAL: 431 rows, 47 open. |
| R528 rust: **the nightly dylint driver parses `CANDOR_POLICY` with NO alias vocabulary AND discovers no `.candor/config` at all, so it REFUSES a policy `candor-scan` honours** — [[R525]]'s mechanism on a third route, found by that row's own audit sweep and deliberately left unfixed there. `src/lib.rs:371` calls the alias-less `parse_policy`; an `unknown-alias` is an unrecognised reason-class, `fatal_messages()` is non-empty, and the lint exits 2. | 2026-09-21 | rust-deep (dylint driver) |**NOT this sin, and the direction is the whole point: it FAILS LOUD.** Where R525's peek went silently to exit 0 over a real violation, this refuses at exit 2 — an operator sees a divergence rather than a green gate, so no purity claim is fabricated. Confirmed by reading the call site during R525's sweep, which was widened past the two lines in the brief to every `parse_policy*` call AND every `fatal_messages()` consumer. The remaining divergence is real: the same policy file is honoured by `candor-scan` and refused by the lint, which is a ⟨0.24⟩ANCHORING disagreement between two routes over one document. |**OPEN — deliberately not fixed with R525, and the reason is worth keeping.** Closing it means putting `discover_config` (which can `exit(2)`, walks parent directories, and reads `CANDOR_CONFIG`) inside a lint hosted in rustc, where the agent could not write a falsifying test to the standard R525's own fix was held to — so it reported the finding instead of shipping an unfalsified change. That is the correct call and the reason this is a row rather than a silent gap. Fix direction: give the driver the same policy-anchored config discovery `scan_one` now uses, and pin it with a test that FAILS against the current driver before it passes. Do NOT close it by making the lint's parse permissive — that trades a loud refusal for a silent over-acceptance, which is the trade this register has recorded four times. |
| R529c **A BODY-LOCAL STRUCT'S FIELD TYPE IS IN NO INDEX, so a call through that field resolves to nothing and the caller reads PURE.** `fn body_field() { struct BodyHolder { c: Inner } … h.c.go(); }` is ABSENT while the byte-identical program with the struct at MODULE level reads `['Fs']`. | 2026-09-22 | rust | [[R529]]'s mechanism, a THIRD index (`fields`). **Identical pre and post the R529/R529b fixes — pre-existing, not a regression.** The boundary was MEASURED rather than assumed, and it is narrower than the mechanism suggests: the body-local blind spot bites ONLY where the effectful body lies OUTSIDE the enclosing function. Body-local `impl Drop`, `impl From` + `.into()` and `impl Deref` were each measured and are all CORRECTLY charged, by containment. The two reachable shapes are (a) the implementor escapes via `Box<dyn Trait>` — that is R529 — and (b) resolution points at a module-level body through a type index — this row. That characterisation stopped three false rows being filed. | **OPEN, and deliberately not fixed in the R529 wave.** `fields` is keyed by type LEAF crate-wide, so admitting body-local structs lets a body-local type collide with a module-level one of the same name and MIS-TYPE a receiver — the FABRICATION direction, unlike R529/R529b which are disclosure-only. Needs its own A/B and its own over-charge control before anything lands. Loose prevalence bound: indented `struct X {` in 104 of the first 400 registry crates (indentation heuristic, includes nested modules — quoted as an upper bound, not a count). Also unmeasured: the other ~19 indexes `collect_decls` produces. |
| R530b java: **a returned LAMBDA implementing a dependency-declared interface reaches no chained consumer — the ⟨0.39⟩ `interfaceUnion` omits the `lambda$…` implementor, and a single pure sibling then certifies the consumer PURE over a real socket.** CARDINAL SIN. `unionCandidates` ARM 2 enumerates project ClassNodes with foreign supertypes; a lambda has no class, and `lambda$…` is ACC_SYNTHETIC and skipped besides. | 2026-09-22 | java | Three packages, **one variable — `() -> {…}` vs `new Handler(){…}`** — with one pure local implementer in the dispatching library. Reproduced by the coordinator on the published 0.39.0 jar with the chain regenerated from source: **lambda arm `app.json` is `functions: []`, and `deny Net` AND `pure` BOTH exit 0**; the anon control reads `app.App.go ['Net']` and exits 1 on both. **Ground truth EXECUTED** — an in-process listener accepts the connection from `app.App.go` in both arms, 3/3 deterministic. Identical on 0.37.0, 0.38.3 and HEAD, so **pre-existing rather than introduced by ⟨0.39⟩**. **MY BRIEF'S PREMISE — that the single-project case is covered by the union or `Unknown[task-handoff]` — WAS MEASURED FALSE on both halves**, and the agent said so rather than working around it. Widened past the trigger: the same toggle fires for a JDK SAM in ONE scan — adding an unrelated `class PureTask implements Runnable` and nothing else takes `FS.dispatch` from `['Unknown']` + `unknownWhy: callback:java.lang.Runnable.run` to ABSENT (coordinator-verified on the published jar). | **CLOSED — candor-java `d17dc66`.** A new `samLambdaImpls` index files every LambdaMetafactory implementor under the SAM's own entry hash, kept deliberately OUT of `chaTargets` because every emptiness test there reads empty as *disclose*, so widening that chokepoint deletes disclosures — the fix direction I briefed was the one the repo had ALREADY measured broken, in a doc comment. It had to COMPLETE rather than hedge, and that was derivable from PART 87's `java-pure-argstore` control before any code: the control forbids `Unknown+unresolved:true` on the fixture whose only difference from the sin arm is what the lambda does. **The agent's own `add-only` claim was FALSIFIED by its first A/B — 49 rows LOST effects** because the fan-out bound counted the new lambdas; re-measured at 0. A/B 372 jars: ADDED 2765 REMOVED 0 CHANGED 50047, of which 4869 gained only Unknown, 4589 only effects, 285 both, **0 lost anything**. R183 measured NOT to transfer (zero probe hits over all five of its named controls). PART 87 `argstore:one` and PART 92 `c8` java both went PASSING and are RETIRED. Side effect: [[R179]] closed too. **The text below was true when written:** **OPEN — a rung question, not a patch, and deliberately unfixed.** One mechanism underneath all of it: **lambda bodies are never CHA implementors of their SAM, yet the empty-CHA test is what arms the `Unknown`.** Fix direction (PRICED, NOT MEASURED): register the indy's implementation `Handle` as an implementor of the SAM named in the indy descriptor, feeding both `chaTargets` and `unionCandidates`. **R183 already measured that widening `isJdkFunctionalSam` OVER-CHARGES** (`map(Iterable::iterator)`, `map(Principal::getName)`), so bounding this to non-JDK dependency-declared SAMs is the obvious denylist — and an obvious bound is exactly what this register keeps measuring wrong. `soundness/functional_sam_probe.sh` cannot see the class: its own header scopes it to *empty CHA*, which is §A.2's sibling nobody was handed. Also falsifies `HofSpellingParityTest`'s assertion that the escaping-reference case is *a relocation of the disclosure and not a loss of it* — true for its same-class `readIt`, false for its own `giveLam` shape once the downstream read is a dispatch with a pure sibling. PINNED FOUR-WAY 2026-09-22 as PART 92 arm c8_body_local_implementor, java's line owned by this row. The arm's consumer SUPPLIES the implementor and writes it where its own index does not hold it, with the dep declaring ONE PURE implementor so the toggle's right side is present. java reads ABSENT — a purity claim — while rust passes by DISCLOSING (R529's hedge) and ts by COMPLETING (R512). The want is the ⟨0.35⟩ DISJUNCTION (a new `disclosed` kind added to the generator), because demanding the effect would score rust's licensed hedge as a failure and measure the arm's preference rather than the contract. A PASSING xfail is a FAILURE, so this line retires loudly when java closes it. ALSO PINNED SINGLE-SCAN 2026-09-22 as PART 87's new `argstore` shape — a lambda handed straight to a project dispatcher, which is the most ordinary spelling Java has and which NO arm in that part could reach: every existing shape (instance, static, inherited, mrefstore, mrefhof) holds the callable in a FIELD. That part's own comment says its boundary had been drawn around its own trigger and that a row pinning ONE spelling of a property states the PROPERTY holds; the enumeration that followed covered field spellings and stopped. Measured: with ZERO implementors `Widget.dispatch` carries Unknown + unresolved:true ['callback:java.lang.Runnable.run']; add one unrelated pure `Runnable` and it is ABSENT from functions[]. The caller under test is the DISPATCHER, not the creator — `fire` stays pure by `lambdaEscapesUninvoked`, which is the deliberate clinit-smear guard. Declared in a new P87_XFAIL table with the same two rules as PART 92's and PART 55's (a passing xfail is a FAILURE; every entry names a row), calibrated by declaring a fake line on the passing zero arm. Its own over-charge control `java-pure-argstore` ships with it and is declared so PART 44 verifies it exists. |
| R532b swift: **⚠ ITEM (d) IS RECLASSIFIED 2026-09-23 — it is a LOST EFFECT AND A GATE FLIP, not a key nobody can answer; see [[R550]], fixed.** **on real code essentially EVERY foreign `dispatchesOn` key ⟨0.39⟩ publishes is a key nobody can answer — and the free-call form is a MISSED JOIN, not merely noise.** | 2026-09-22 | swift | Over 9 real Swift packages: **12,498 LOCAL keys, 2,071 FOREIGN keys in 69 distinct spellings, ZERO naming an abstraction the owning package has.** (b) a module-qualified free call published as a type member — `CNIOLinux#CNIOLinux.io_uring_submit` x117, `CNIOWindows#WinSDK.closesocket` x150, `CNIOLinux#WinSDK.inet_ntop` x50 keyed under the WRONG module, because the owner is the file's one declared dep import rather than the module the call names. (c) Swift STDLIB types — `Unmanaged.passRetained` x264, `MemoryLayout.size` x65, `Swift.type` x66. (d) a TYPE-level generic parameter, 36 rows — which is [[R532a]]'s own fix's stated boundary. | **OPEN.** Neither (b) nor (c) is covered by the rung's `STD_PURE_PROTOCOLS` / `RAW_VALUE_BASE_TYPES` carve-outs, **because neither name is a protocol at all** — the carve-outs guard a protocol-shaped fabrication and these are a MODULE and a stdlib TYPE. (b) is soundness-relevant beyond noise: a consumer resolves `dispatchesOn` by `deps.lookup(k)` and a producer files a free function under `M#name`, never `M#M.name`, so obligation 1's chain breaks exactly where it was added to hold. NOT FIXED: any carve-out moves 2,071 rows on one package and needs a full removal audit first. |
| R533 all engines: **A CONSUMER DISPATCHING ON A CHAINED DEPENDENCY'S ABSTRACTION WITH ZERO KNOWN IMPLEMENTORS READS SILENTLY PURE IN THREE ENGINES OF FOUR — INCLUDING THE REFERENCE ENGINE.** `int go(Handler h) { return h.handle(); }` where `Handler` is declared by a chained dep and NOTHING implements it anywhere: the engine records `dispatchesOn`, so it KNOWS this is a dispatch, and gates on nothing. | 2026-09-22 | **PARTLY CLOSED 2026-09-25 — rust FIXED**, candor-rust `e4808bf` (⟨0.39⟩ (the clause once drafted as 0.40) R608+R609), and its `c9_consumer_zero_union` xfail is RETIRED. Originally filed: java + rust + swift SILENT; ts discloses. **STILL OPEN in java and swift**, whose `c9` arms remain xfail | **Found by a review panel attacking a DIFFERENT claim of mine, and it is the purest form of the sin in this register — no sibling, no body-local trick, no lambda, just a dispatch nobody implements.** Coordinator-verified on the reference engine (candor-java 0.39.0 jar, chain rebuilt from source): `app.App.go` reads `inferred: [] unresolved: false unknownWhy: null dispatchesOn: ['iface/backend/Handler.handle()I']`, and **`pure` exits 0** — as do `deny Net` and `deny Net Unknown`. Panel's four-way measurement, same consumer body per language, one dep report, zero implementors in both packages, only the ENGINE varying: rust `go []` + `dispatchesOn` + ~~`invisible:['iface']`~~ **— CORRECTED 2026-09-23: at HEAD rust carries NO `invisible` on this arm. Re-measured first-hand via `conformance/part.sh 92`: java, rust and swift all read `eff=∅ unknown=False invisible=∅`, ts alone `unknown=True`. Chaining DELETES the `invisible` (SPEC §4), so the row's own evidence line described the UNCHAINED shape — which matters, because `invisible` was the one thread by which rust looked less silent than the other two, and it is not there**; java as above 0/0/0; swift `useIt []` + `dispatchesOn`, 0/0/0; **ts is the only engine that discloses** — `go ['Unknown']` with `dispatch:iface.Handler.handle`, and `deny Net Unknown` exits 1. **PART 92's `c2_zero_impl` does NOT catch it**: its zero-implementor dispatch sits at the DEP, where a local trait makes bounded-CHA fire and the consumer inherits the hedge. The consumer's OWN foreign site with an empty union is unpinned four-way. **SWIFT IS PRICED — 2026-09-24, and it was priced AROUND [[R565]] rather than behind it, so this decision is no longer blocked.** The prototype arm (`CANDOR_R533_MODMAP`, a module→package map read from each consumer's own `.product(name:package:)` lines) over 5 swift-nio-family pairs: **REMOVED 0, 1 row added + 10 changed = 11 of 6,942 rows = 0.158%**, reach `R533HIT` 18. **But the CEILING is not the price: all 18 hits are on CONCRETE types** — `EmbeddedChannel` (a final class, 12 of 18), `HTTPClientRequestPart` (an enum), `ClientBootstrap`, `MultiThreadedEventLoopGroup` — because swift's obligation-1 key is spelled from the receiver's external type name and this engine cannot tell a protocol from a class across a package boundary. **R533's actual shape — a dispatch on an ABSTRACTION whose implementor union is EMPTY — fired ZERO times on swift's five pairs.** So swift measures **0**, with 0.158% as an upper bound that reflects [[R565]]/[[R532b]]'s key-spelling defect rather than this rung's cost. **FOUR-WAY PRICE, all with zero effect losses: java 0.87%, rust 1.71% MEASURED (0.23% is the PROJECTED figure once [[R549]]'s malformed keys are removed, and R549 is still open — do not quote 0.23% as a measurement), swift 0 measured / ≤0.158% ceiling, ts 0 (already ships it).** Register precedents for a disclosure-only change: **SHIPPED at 1.44% ([[R452]]); DECLINED at 2.60% (the ⟨0.39⟩ in-crate hedge) and 7.02% ([[R190]](c))**. Three engines sit below the lowest SHIPPED precedent; rust's measured 1.71% is the only figure in the judgement band between shipped and declined. [[R547]] closed both halves today, so a DECLINATION is now recordable. **THE RUST 1.71% WAS A MIS-PORT, NOT A PRICE — MEASURED 2026-09-24/25, one binary (`0c0a0fd`), one base, six chained pairs, Σ analyzed 2,826.** Six arms, `REMOVED 0` and `−Unknown 0` in EVERY one: rule with no conjunct 4 → 110 functions (3.89%); **conjunct 4 MEMBER-keyed (the prototype) → 48 (1.70%)**; **conjunct 4 TRAIT-PREFIX-keyed → 6 (0.21%)**; plus the §4 exempt leaves → 6 (0.21%, byte-identical); minus the two verified-malformed keys → 3 (0.11%). **The member-keyed arm reproduces r557's 45 gained-`Unknown` and all 42 `R533HIT` lines BYTE-FOR-BYTE from a different base and different dep reports, so the 1.71% was real and is the same 45** — the denominator moved 2,637→2,826 because the ENGINE moved, not the corpus; on r557's denominator the prefix arm is 0.23%. **THE SPLIT THAT EXPLAINS IT: of the 42 hits, 1 DECLARED and 41 MALFORMED ([[R549]]).** `tower_service::Service` declares only `poll_ready` and `call`, so `map_err`, `map`, `map_ok`, `and_then`, `err_into`, `then`, `load`, `clone`, `make_service`, `is_pending` are all fabricated keys — and a fabricated member is BY CONSTRUCTION never a key in `foreign_impls`, so conjunct 4 could not fire. 39 of 42 name a trait the consumer has a local `impl` for (tower has 58 `impl Service for`). **The predicted hit is there and is the only one left at the tightest arm:** `discover::D::poll_discover` on `futures_core#TryStream::try_poll_next` — `tower-0.4.13/src/discover/mod.rs:96`, tower has ZERO local `impl TryStream`, the sole implementor is futures-core's blanket impl. **SO EVERY ENGINE IS NOW UNDER THE SHIPS-WITHOUT-ASKING BAND: java 0.87% (419/48,116), rust 0.21% (6/2,826), swift 0, ts 0 — with zero gate flips package-wide in both measured engines**, since every affected package already carried `Unknown` rows and the only verb this rule can move is entry-scoped `deny e Unknown`. **RESIDUAL, stated rather than buried: 2 of the 6 remaining functions still fire on FABRICATED keys and survive only because futures-util happens not to implement `TryStream` — so ~0.11% is the rung's own price and ~0.10% is [[R549]] leaking into it. The prefix rule narrows the QUESTION; it does not fix the key.** **SWIFT RE-PRICED AFTER [[R567]]+[[R610]], 2026-09-25 — AND THE VERDICT IS DO NOT PORT ON THE NUMBER.** One binary, one variable (the fix kill switches), end-to-end over 5 pairs: fixes OFF **36 rows of 6,426 (0.560%)**, fixes ON **26 of 6,444 (0.403%)**, `REMOVED 0` in both; R567+R610 removed **21 of 52 reach hits (40%)**, every one ground-truthed malformed. **BUT OF THE 30 HITS THAT REMAIN, ZERO ARE ON A GENUINE ABSTRACTION.** Ground-truthed from swift-nio source: `ChannelHandlerContext`, `EmbeddedChannel`, `MultiThreadedEventLoopGroup`, `ClientBootstrap` are all `public final class`; `TLSUserEvent` is an `enum`; `HTTPClientRequestPart` a `typealias` to one. **R533's actual shape still fires ZERO times in swift** — the 2026-09-24 conclusion survives on a much cleaner key population. **And the conjunct-4 narrowing that saved rust is INERT here (30 -> 30)**, because none of these types has a local conformer. **The 30 split two ways and NEITHER IS THE RUNG: 23 are well-formed but CONCRETE**, where the prototype's `deps.lookup(k) == nil` test **cannot distinguish "no implementor of an abstraction" from "the producer judged this concrete member effect-free"** — and for a COVERED package SPEC §2 rule 3 says that silence IS the answer. **7 are still malformed** ([[R611]]'s extension members, enum cases spelled as dispatches, a protocol-extension member keyed to the concrete type). **So the rule AS PROTOTYPED ASKS THE WRONG QUESTION IN SWIFT**, and 0.403% is a ceiling over a population where 7/30 should not exist and 23/30 are the producer's own silence re-read as ignorance. | **OPEN — and this is the only genuine ⟨0.39⟩ (the clause once drafted as 0.40) CANDIDATE to come out of the R529–R532 round; the class I proposed was NOT a rung.** The tightening would be that a chained abstraction is a LOCAL abstraction for bounded-CHA purposes — the consumer's implementor set is the union of its own and every chained report's, and an EMPTY union at a consumer's dispatch site MUST read `Unknown`, never silent purity. That touches ⟨0.39⟩ obligation 3 and the bounded-CHA scope, so it is a real MINOR, not a clarification. **DO NOT DRAFT IT YET.** The ⟨0.39⟩ cost paragraph already DECLINED the neighbouring hedge at 2.60% of functions across 435 libraries, and this one bills every middle library scanned alone (the tower / http-body shape). ts already pays it and ships, which is the existence proof that it is affordable somewhere; rust discloses only via non-gating `invisible` (R133); java and swift disclose nothing. **Needs a corpus A/B per engine BEFORE it is a clause**, plus a `c9_consumer_zero_union` PART arm filed first as a MEASURED DIVERGENCE rather than a MUST. **JAVA PRICED 2026-09-23 — THE FIRST OF THE FOUR A/Bs THE ROW REQUIRED, AND IT COMES IN AT ONE THIRD OF THE DECLINED PRECEDENT.** Nine chained JVM library pairs (jackson-databind←core+annotations, logback-classic←core+slf4j, HikariCP←slf4j, jul-to-slf4j←slf4j, okhttp←okio, httpclient5←httpcore5, httpclient4←httpcore4, error_prone_check_api←annotation, aws-s3←sdk-core), **48,116 analyzed functions**, chained via `CANDOR_DEPS`, differenced with `bin/corpus-ab.py` (wide key, multiset — never a fresh `ab.py`). Arms built from detached WORKTREES at HEAD so the engine tree stayed clean; PRE is HEAD's own release jar (`v0.39.2`, 0 commits since, tree clean). **BROAD form (java's `untypedDepReceiver` minus conjunct 5, `depDeclaresSigElsewhere`): 420 functions gain `Unknown` = 0.87% of analyzed, and ZERO functions lose an effect in any of the nine pairs.** Compare the two precedents the ⟨0.39⟩ cost paragraph sets: the in-crate hedge it DECLINED cost **2.60% of functions** across 435 libraries, and this same broad form measured **2.1%** when it was last priced in July at ⟨0.23⟩ (`candor-java e202379`). **The rung got cheaper by more than half while nobody was pricing it** — ⟨0.33⟩–⟨0.39⟩ resolved work this hedge used to have to cover, which is the argument for RE-pricing a declined item rather than treating a declination as permanent. **NARROW form is a STRICT SUBSET and cannot exceed 0.87%**: with pure-only unions published (arm B2, `ReportWriter.java:579`'s `continue` removed) the nine pairs publish **2,477** `interfaceUnion` entries of which **73.4% are pure-only** — so a rule gating only on a genuinely ABSENT union fires on strictly fewer sites. That 73.4% also confirms July's diagnosis verbatim at HEAD: without conjunct 5 the hedge *"lands overwhelmingly on interfaces whose every implementation in the dep is a pure accessor"*. **CORROBORATION FROM AN INDEPENDENT PASS: 166 of the 420 (39.5%) sit in classes whose §5 capability-reconciliation `declared` set ALREADY contained `Unknown`** — i.e. the class's injected dependency types can supply exactly the effect the disclosure names, computed by a different pass that knows nothing of this rule, and `overdeclared` shrinks accordingly. (Checked rather than assumed: `declared` here is NOT a human declaration — `ReportWriter.java:57` defines it as what the class's injected dependency types can supply.) **RUST CENSUSED 2026-09-23 — AND THE HEADLINE IS THAT THE CENSUS IS NOT THE COST, WHICH IS THE MISTAKE THIS ROW EXISTS TO AVOID REPEATING.** Six chained crate pairs in the exact vein ⟨0.39⟩'s cost paragraph singles out (tower←tower-service+tower-layer+futures-core, http-body-util←http-body+http+bytes, tower-http←tower+http+http-body+bytes, futures-util←futures-core, tracing←tracing-core, http-body←bytes+http), sources from crates.io, scanned with the STOCK HEAD engine (`candor-scan 0.39.2 (spec 0.39)`) — **no prototype was needed, because rust already emits `dispatchesOn` for the key it cannot answer**. 2,637 analyzed consumer functions; **102 rows (3.87%) dispatch on a key NO chained report answers and still read PURE**. **But the same census run on JAVA's nine pairs gives 5.56% (2,673 of 48,116) while java's ACTUAL measured A/B cost is 0.87% — the census overstates the rule by 6.4×**, because the rule's conjuncts exclude the conventionally-pure surface, provably-typed receivers and sites with a local implementor. So the census is an UPPER BOUND on either engine, and the only defensible cross-engine statement is the like-for-like one: **on the identical instrument rust (3.87%) is CHEAPER than java (5.56%)**, i.e. rust does NOT look like the expensive outlier the ⟨0.39⟩ paragraph's `tower`/`http-body` warning predicted — the opposite. Quoting rust's 3.87% against the 2.60% DECLINED figure would compare a census with an A/B and is exactly the mismatched-population error the ⟨0.39⟩ cost paragraph indicts itself for. **THE CONCRETE INSTANCE, worth naming because it is the abstraction that paragraph names first: `tower` carries 98 rows whose `dispatchesOn` is `tower_service#Service::call`, EVERY ONE `inferred: []`, while `tower-service` publishes ZERO `interfaceUnion` entries** — it is a trait-only crate, so the trait genuinely has no implementor in the dep. That is R533's NARROW case, not merely the broad one, which settles a real question: the narrow rule has live value in rust rather than being a theoretical subset. **Rust's narrow/broad SPLIT cannot be measured from reports alone** — a dep publishes no entry whether its implementors are absent or merely all-pure (`scan.rs:4298`, *"silence = purity"*), which is the wire limitation that makes the narrow rule inexpressible today. Splitting it needs the format half first. **RUST A/B'd 2026-09-23 — 3.68%, ABOVE the declined precedent, and the measurement exposed that CONJUNCT 5 AND THIS ROW'S RULE ARE COMPLEMENTARY RATHER THAN NESTED.** Prototype at `scan.rs:3742` (a chained foreign dispatch whose key NOTHING in `deps_idx.by_key` answers now discloses), **env-gated behind `CANDOR_R533_PROTO` so PRE and POST are the SAME BINARY** — no build difference can be mistaken for the rule. Control first: prototype-OFF is identical to HEAD on every report key for tower and futures-util. (Note `candor-rust/target/release/candor-scan` is STALE at `scan-0.39.1` and produced a different report — the debug binary is HEAD; a measurement taken with the release one would have been of the wrong engine.) **97 of 2,637 analyzed functions gain `Unknown`, ZERO lose an effect, 118 `R533HIT` reach probes — 3.68%.** Per pair: tower 67/483 (**13.9%**), http-body-util 18/91 (19.8%), futures-util 12/1181, tower-http 0/762, tracing 0, http-body 0. **The cost is NOT inflated by the exempt surface**: only 7 of 118 hits (5.9%) are on SPEC §4's permitted exclusions (all `clone`); the dominant targets are `poll_ready` (28), `call` (24), `map_err` (9), `is_terminated` (6) — i.e. exactly the abstractions the ⟨0.39⟩ cost paragraph names as the ones an effect hides behind. So rust genuinely exceeds the **2.60%** in-crate hedge that was DECLINED, where java came in at 0.87%. **THE CRUX, and it reframes the whole rung: java's conjunct 5 (`depDeclaresSigElsewhere`) fires only when the chained report HOLDS AN EFFECTFUL BODY for the signature. `tower-service` is a trait-only crate that publishes no functions and no union entries at all — so JAVA'S SHIPPED RULE WOULD NOT CATCH R533'S MOTIVATING CASE EITHER.** Conjunct 5 discloses *"something effectful implements this"*; R533's narrow rule discloses *"nothing implements this, so nothing is known"*. They are disjoint, not one inside the other, and the middle case — implementors exist and are all pure — is ⟨0.39⟩'s own toggle, which the producer's `silence = purity` drop (`scan.rs:4298`) makes invisible to a consumer. **So the go/no-go is not one number.** The cheap reading ("rust 3.68% > 2.60%, decline") is wrong in the same way the census reading was: it prices the UNFILTERED rule against a filtered precedent. What is actually owed before a clause is rust's equivalent of java's conjuncts 1-4 applied, and then a re-price — and a decision on whether the tower shape is worth a rule that conjunct 5 structurally cannot express. **RUST RE-PRICED WITH CONJUNCT 4, 2026-09-23 — 1.93%, BELOW the declined precedent — and the re-price found a shipped defect ([[R549]]) that makes even that an upper bound.** Same binary, second env flag (`CANDOR_R533_C4`) so all three arms are one build: **unfiltered 97/2,637 = 3.68%; with conjunct 4 ("this crate has no local impl of the foreign trait") 51/2,637 = 1.93%; zero effect losses in every arm.** The unfiltered arm reproduced 97 exactly on the rebuild, so no drift. Conjunct 4's rust equivalent is `merged.foreign_impls` with the 2-segment-tail fallback — the same lookup `scan.rs:3020` already performs. **It cuts tower 67→34 because tower carries 80 local `impl … Service<` sites**, so `Service::call`/`poll_ready` genuinely have a local answer and are NOT this row's case — **which CORRECTS the claim recorded above that tower's 98 `Service::call` rows are R533's narrow case. About half are not**; the narrow case is the subset with no local implementor, and the correction was produced by the next measurement rather than by re-reading the claim. **Then R549: of what SURVIVES conjunct 4, a large share is charged against keys that name members `Service` does not declare** (`map_err`, `load`, `make_service` — `ServiceExt` methods, a trait tower declares locally). Those are unanswerable because they are MALFORMED. So 1.93% is an upper bound, the true figure sits below it, and **rust must be re-priced AFTER R549 is fixed, not before.** Standing comparison: java 0.87% with the same conjunct set, the in-crate hedge ⟨0.39⟩ declined 2.60%. Rust now lands between them rather than above both. **RUST RE-PRICED A THIRD TIME 2026-09-23 at candor-rust `e82e00f`, AND THE COST COLLAPSES ONCE [[R549]]'s MALFORMED KEYS ARE DISCOUNTED — which is the whole reason the re-price was owed.** Same env-gated prototype, one binary, **control first: prototype-OFF byte-identical to HEAD on all six pairs.** 2,637 analyzed consumer functions over 6 packages, and every figure is an **A/B — NOT a census; the 3.87%/5.56% census numbers must never be compared with these**: **unfiltered 101/2,637 = 3.83% of functions (3/6 = 50% of packages), 0 effect losses; + conjunct 4 → 45/2,637 = 1.71%, 0 losses; + conjunct 4 with R549's 17 malformed keys removed → 6/2,637 = 0.23% (2/6 = 33% of packages), 0 losses.** The prior run reproduces within noise (3.68%/1.93%), per-pair shape matching. **THE THIRD ROW IS THE FINDING: after conjunct 4, 38 of 42 hits — 90% — sit on keys [[R549]] PROVED MALFORMED.** Discounting them leaves **0.23%: a QUARTER of java's 0.87% and a TENTH of the 2.60% ⟨0.39⟩ DECLINED.** Sound in the removal direction for all 17 — extension-trait keys re-key to a crate-OWNED trait and leave the foreign path, chain-drift keys correspond to no dispatch at all, and `Service::clone` is std. **AND THE PREDICTION VERIFIED EXACTLY:** crossing the new C4 arm against the prior C4 arm (same pairs, same corpus, both C4 — the lane first crossed unfiltered-vs-C4 by mistake and DISCARDED that rather than reporting it) shows **6 keys gone, every one [[R551]]'s class**, plus one new well-formed key from `186e854`'s fn-ref half. **So the two engine fixes shipped today are most of the reason this rung now looks affordable, and pricing it before them would have overstated it by roughly 7×** — which is the concrete answer to why this row said DO NOT DRAFT IT YET. **SWIFT CANNOT BE PRICED AT ALL YET, 2026-09-23 — and the reason is [[R565]], not a missing measurement.** The prototype was built (env-gated, one binary, prototype-OFF verified byte-identical to HEAD) and run over 5 real chained SPM pairs / 8,932 analyzed functions: **0/0/0 with ZERO REACH — safety-only, NOT a cost of zero.** ⟨0.39⟩ keys by MODULE while §2 chaining keys by PACKAGE, and for a conventional SPM package those namespaces never intersect, so **R533's rule cannot fire on any real swift chained package at HEAD**. **No swift figure may be placed beside java's 0.87% or the 2.60% declined precedent until R565 is closed** — an upper bound on an artefact is not a price, and quoting one would be this row's third pricing error after the census-vs-A/B and the unfiltered-vs-filtered ones. **Also established while trying: swift's producer carries rust's `silence = purity` drop VERBATIM (`main.swift:1930`), calibrated by adding a PURE conformer and watching the prototype still fire — so swift's narrow/broad split is inexpressible on the wire too, exactly as recorded for rust.** That makes the format half a TWO-ENGINE prerequisite, not a rust quirk. **STILL OWED: [[R565]] closed, then swift priced. rust and java are priced.** **STILL OWED before any clause: rust and swift.** java alone does not price a four-way rung, and rust is the engine whose ecosystem carries the `tower`/`http-body` shape the ⟨0.39⟩ paragraph singles out. Arms, jars and reports are reproducible from the driver recorded with this row. **The "DO NOT DRAFT IT YET" above still stands.** PINNED FOUR-WAY 2026-09-22 as PART 92 arm c9_consumer_zero_union (candor-spec, this commit) — a MEASURED DIVERGENCE with rust/java/swift declared in the arm-keyed XFAIL table and ts passing. The arm reuses the nesteddispatch consumer body, which is what puts the dispatch in the CONSUMER rather than the dep: that is precisely the difference from c2_zero_impl, whose zero-implementor dispatch sits at the dep where bounded CHA fires and the consumer inherits a hedge it did not derive. Declared as a divergence and NOT as a port lag, stated in the table: no engine has ever been asked for this because no clause requires it yet. A PASSING xfail is a FAILURE, so the line retires loudly the moment an engine closes it — calibrated by declaring a fake xfail on ts, which fires XFAIL ARM PASSED ... expectation is STALE and reds the part. Full four-way suite OK, 0 divergences, with the three lines declared. |
| R534 swift: **A PROTOCOL-TYPED *OPTIONAL PARAMETER* DISPATCHED BY OPTIONAL-CHAINING IS SILENTLY PURE — the function vanishes from `functions[]` entirely.** `func viaOptionalParam(_ h: Sink?) { h?.emit() }` is ABSENT and `deny Net` exits 0, while the byte-identical plain-parameter and `if let`-unwrapped spellings both charge `['Net']` and exit 1. | 2026-09-22 | swift (rust/java/ts UNASKED) | **Found by the [[R532]] fix agent as an UNRELATED pre-existing defect, and reported rather than folded in — which is the right call and the reason this is a row.** Coordinator-verified independently on the post-R532 binary with a fixture built from scratch: `viaPlainParam(_ h: Sink)` → `['Net']`, `deny Net` exit 1; `viaOptionalParam(_ h: Sink?)` → **ABSENT from `functions[]`**, `deny Net` **exit 0**; `viaIfLet(_ h: Sink?) { if let g = h { g.emit() } }` → `['Net']`, exit 1. Two controls, one variable — only the OPTIONAL-PARAMETER binder under optional chaining is silent. The agent's own controls add a third: an optional STORED PROPERTY chained the same way charges correctly, so it is the parameter binder specifically and not optional-chaining in general. Byte-identical on v0.39.0 and post-fix, so PRE-EXISTING and not an R532 regression. **ONE LINE OF THE SPELLING TABLE ABOVE IS STALE, corrected 2026-09-22: `[Sink].first?.` is NOT silent at HEAD — [[R537]] closed it — and `[Sink?]` charges in all three declaration orders. Everything else in the table reproduced against a 66-arm fixture (22 spellings x 3 declaration orders) in which every arm COMPILES AND EXECUTES, proven by a hit counter. | **CLOSED — candor-swift `4a1861c`. THE CORRECTED CLASS SURVIVED MY OWN MEASUREMENT, AND THE SECOND CONDITION IS SHARPER THAN "DECLARED ABOVE": it is WHICH OF TWO INDEXES the parameter landed in.** The READ lead was right and is now MEASURED: `DeclCollector.swift`'s `if` writes `info.protoParams` and its exclusive `else` writes `info.params`, and the test that chooses — `protocolMethods[resolved] != nil` over a **per-FILE** map filled as that file's walk descends — asks "was this protocol spelled earlier in THIS file?". The two indexes have DISJOINT consumer sets, which is the whole of the inversion: populate either and the other's consumers go blind. **66-arm fixture, 22 spellings × 3 declaration orders (above / below / another file), every arm COMPILED AND EXECUTED** — a hit counter proved all 66 reach the witness, because an absence assertion over a program that cannot run asserts nothing (§E3). **23 silent or incomplete at `5b6e806`, 3 after**, and the three columns now agree arm for arm. Scoped `deny Net h_opt` **exit 0 → exit 1**; blanket `deny Net` catches it only INCIDENTALLY (the conformer is independently reported) and `deny Unknown` is exit 0 both sides — the absence was total, not hedged. **ONE LINE OF THE ROW'S SPELLING TABLE IS STALE:** `[Sink].first?.` is NOT silent at HEAD — [[R537]] closed it — and `[Sink?]` charges in all three orders; everything else reproduced. **FIX = ORDER-INDEPENDENCE, not a new rule.** DeclCollector populates BOTH maps (and `opaqueParams`); the **Driver backfills `protoParams` from the MERGED, scan-global `protocolMethods`**, resolving each function's own `genericBounds`, at the first point that map is complete and before any CallCollector exists. Two arms silent in ALL THREE orders closed with it: `<T: P>(_ h: T?)` needed `Self.peel` at the **two of six** `protoTyped` consumers that did not peel — method dispatch and property read, §F1.3, the other four already did — and `h: P!`, because `ImplicitlyUnwrappedOptionalTypeSyntax` appeared **nowhere** in this engine's sources. **The `T!` peel is SCOPED TO PARAMETERS by measurement, not caution:** placed in `typeName` itself it turned FOUR disclosed Kingfisher rows into ABSENCES (`@IBOutlet weak var cellImageView: UIImageView!` became typed and `dispatch:UICollectionViewCell.cancelDownloadTask` stopped being disclosed) — one variable, same tree, same binary, only the peel's location. **`T!` in FIELD/BINDING position stays untyped and OPEN.** **THE FIX'S OWN FAILURE DIRECTION FIRED TWICE AND NEITHER WAS VISIBLE IN `inferred`.** (1) A REBIND kept the parameter's protocol: `visit(VariableDeclSyntax)` skipped clearing `protoTyped` whenever the initializer mentioned the name, so `func enc(_ r: Conv) { var r = r.asReq(); _ = r.headers }` read `r.headers` through `Conv`, which declares no such member, and `enc` read **PURE**. That exemption was correct when written and **EXPIRED when R98 moved the initializer walk to the top of the visitor** — a stale ordering fact, the third variant of [[feedback-documented-limitation-is-not-measured]]. At `5b6e806` the protocol-BELOW spelling charged `Fs` and the protocol-ABOVE spelling was ALREADY silent, so it is this row's order dependence again; **the 12-package corpus showed it only as three Alamofire `URLRequest.headers`/`.method` edges with `inferred` UNCHANGED**, because those accessors happen to be effect-free — movement in `inferred` is not the test for this shape, and it was found by widening the fixture past the corpus. (2) `vars` is function-wide and not in `ShadowSave`, so a nested func's parameter shadowing a protocol-typed one dispatched the NESTED type over the OUTER protocol's conformers. **`TypedRebindShadowProcessTests.nestedFuncShadow` caught it** — and that suite declares its protocol at the TOP of its fixture, so it had only ever exercised the ABOVE order where the leak was latent; in the other two orders the fabrication was LIVE at `5b6e806` and no fixture looked (§A.2). `vars` is now saved per FunctionDecl id and restored at `visitPost`, with an `outerAfter` mirror control. **A/B — `bin/corpus-ab.py`, 12 packages, 41,400 pre-rows → 41,712 post, `--key unit`, WIDE value: ADDED 314 REMOVED 2 CHANGED 1202** (narrow `inferred` key: CHANGED 210). **REACH 2534 marker hits across 11 of 12 entries — NOT safety-only.** Unknown counted apart from effects: of 314 added rows **306 are Unknown-only/empty and 8 carry a concrete `Env`**; CHANGED gained a concrete effect on 10 rows and Unknown on 11; `dispatchesOn` +892, `invisible` +258, `calls` +243, `unknownWhy` +74, `direct` +18. **Losses field by field: `direct` LOST 0**; `inferred` 189, `calls` 21, `dispatchesOn` 90, `invisible` 7, `unknownWhy` 2, `unresolved` 2. **BOTH REMOVALS AUDITED IN FULL FROM SOURCE, never from candor's own report.** (1) swift-nio `NIOClientTCPBootstrap.init(NIOClientTCPBootstrapProtocol, tlsEnabler:)`, `['Clock','Env','Unknown']` → absent: **the body is TWO FIELD ASSIGNMENTS**. Pre carried `calls:["bootstrap"]` — an untyped protocol-typed parameter's bare identifier resolved as a call to a same-named unit elsewhere in the scan, chaining `removeHandlers → channel → bootstrap → group → MultiThreadedEventLoopGroup.init → NIODeadline.now`. A fabrication removed. (2) swift-syntax `CompilerPluginMessageListener.init(connection:provider:)` — `inferred:[]` on BOTH sides, but the `PluginProviderMessageHandler.init` ctor edge and its `invisible:["_SwiftSyntaxCShims"]` hedge are gone: **the one unexplained residual**, recorded rather than explained away. **The 189 `inferred` losses are that same mechanism** — all swift-nio, 183 losing `Clock` alone — rooted at `ChannelCore.removeHandlers(channel: Channel)` whose whole body is `self.removeHandlers(pipeline: channel.pipeline)` and whose only diff is the fabricated `channel` edge, with `Channel.pipeline` and `removeHandlers(ChannelPipeline)` both surviving. **CONTROL: rows carrying `Clock` 453 → 265, every legitimate carrier intact** (`NIODeadline.now`, `SelectableEventLoop.run()`, `MultiThreadedEventLoopGroup.runTheLoop` unchanged). The 21 `calls` losses are TWO mechanisms, each read from source: that bare-name fabrication (`channel`, `bootstrap`, `query`×5 and `name`×2 in SQLite.swift, `Note.message`, `SyntaxProtocol.node`, `ResponseSerializer.dataPreprocessor` — in every one the source contains no such call), and the nested-func scoping working as intended (`ServerBootstrap.enableMPTCP`×4 from `func makeChannel(…, _ enableMPTCP: Bool)` inside a type that HAS that property; several swift-syntax `.init` overload edges reached through a nested `func addResult(_ node:)` shadowing the outer `node`). The 2 `unknownWhy` losses are from that second mechanism and **both keep `unresolved: true` with unchanged `inferred`** — no disclosure collapsed to silence. **GATES:** `swift test` 1245/0, **`smoke.sh` 160 passed 0 failed**, `ci/self-gate.sh` OK, `recall.sh` 5 recalled 0 under-reports, `fabrication_probe.py` OK, `fuzz.py` 25/0, `assert-audit.sh` OK. **CALIBRATION (§1b): the new `ProtocolParamDeclOrderProcessTests`, run against the pre-fix worktree at `5b6e806`, reports `Executed 3 tests, with 54 failures (0 unexpected)`; against HEAD, 5 tests 0 failures.** **STILL OPEN: the ternary-valued receiver — see [[R589]].** --- |
| R589 swift: **A TERNARY-VALUED RECEIVER WHOSE TWO ARMS RESOLVE DIFFERENTLY IS SILENTLY PURE, and `pure` CERTIFIES IT.** `(c ? CT() as PT : DT() as PT).emitT()` and `(h != nil ? h! : CT()).emitT()` are ABSENT from `functions[]`, while `(c ? a : b).emitT()` over two protocol-typed params, `(c ? CT() : CT())` over one type, and `let x: PT = if c { CT() } else { DT() }; x.emitT()` all charge `['Env','Net']` in the SAME scan. | 2026-09-22 | swift (rust = [[R535]], the same §F1.1 class by a different spelling; java/ts UNASKED) **RENUMBERED 2026-09-24 from R540**, which was a DUPLICATE: a rust row (the dispatch hot-path guard) already held that id nine lines below, and the rust one keeps it — it owns 13 in-code references, the runtime probe string R540REACH, a sub-row R540b and a shipped CHANGELOG entry. Nothing outside this register referenced the swift row. R534's forward link was repointed in the same edit. | candor-swift at `4a1861c` and byte-identically at `5b6e806`, so **PRE-EXISTING, not an [[R534]] regression** — found by widening R534's sweep past its trigger (§9) and NOT its class: the protocol is declared in its own file, so R534's declaration-order condition is excluded by construction, and the fix for R534 does not move these arms. **Every arm COMPILES (`swiftc -typecheck` exit 0) and EXECUTES** — a marker counter proved both silent arms really reach `CT.emitT()` → `URLSession.shared.dataTask` on both branches, 4/4 `ok`. Five controls charge in the same run, so the discriminator is exactly "the two arms resolve to different things". **`pure t_ternary_concrete` exit 0** — an affirmative purity certification, not merely an absence — and `deny Net`, `deny Unknown` scoped to it are exit 0 too. `(h ?? CT()).emitT()` is the honest neighbour: it discloses `['Unknown']`. | **OPEN.** §F1 question 1 — a receiver decided by syntactic adjacency where the question is a CONTROL-FLOW MERGE. Mechanism NOT measured, fix direction unpriced, over-charge control unrun. Do not close it by charging every ternary-valued receiver without an A/B: [[R534]]'s own A/B is the cautionary case — a widening that looks additive removed 189 rows' worth of a fabricated bare-name chain, and the reverse is equally possible here. java and ts have NOT been asked; [[R535]] says they charge for the rust spelling, which is a different expression shape. |
| R537 swift: **THE SAME ACCESSOR-FAMILY HOLE AS [[R536]], AND IT IS NOT [[R534]].** `hs.first?.emitN()`, `hs.last?.`, `v.popLast()?.`, `v.removeFirst().`, `hs.dropFirst().first?.`, `d.values.first?.`, `hs.first.map { … }` and `hs.compactMap { $0 }.first?.` over `[PN]` of a protocol type are ABSENT, while `hs[0].emitN()`, `for g in hs`, `d[\"k\"]?.` and `hs.map { … }` charge `['Net']` in the same scan. | 2026-09-22 | swift (rust SAME class — [[R536]]; java 8/8 and ts 8/8 CORRECT) | candor-swift 0.39.0 at `5c53e96`. **The protocol is declared in a DIFFERENT FILE from every call site, so R534's same-file condition is excluded BY CONSTRUCTION** — these are independent defects, which is why this is its own row rather than more text on R534. Four controls charge in the same run. **java 8/8 and ts 8/8 are CORRECT on the identical question** (`get(0)`, `iterator().next()`, `stream().findFirst().get()`, `Deque.pop()`, `Map.get`, `remove(0)`, for-each, array; and `[0]`, `pop`, `shift`, `find`, `Map.get`, `at`, for-of, `slice`). **WIDENED past the 8 handed spellings to 38 arms that COMPILE AND EXECUTE (each really deletes its own probe file): 30 ABSENT, not 8.** The list was COMPLETE and ORPHANED, not short — see the fix cell. | **CLOSED — candor-swift `5b6e806`. THE PREMISE WAS HALF WRONG AND THE CORRECTION IS THE REUSABLE PART: the hand list was COMPLETE, not short.** `ELEMENT_ACCESSORS` (`CallCollector.swift:3004`) had named all eight handed spellings since R192. It had ONE consumer — `callableValue`'s *is this a closure?* — and nothing wired it to `rootOf`'s *what TYPE is this receiver?*, which knew only the `[i]` subscript. So [[candor-handlist-vein]] holds for the MECHANISM and fails for the REMEDY: grepping the list for MISSING NAMES — the obvious follow-up, and what my brief pointed at — finds **1 of 33** silent arms; the other 30 come from wiring the existing list to the resolver. **Ask how many CONSUMERS a list has, not which names it lacks.** Fixed through ONE shared answer read by the resolver's member arm, its call arm, `elementTypeOf`'s nested arm and the closure-parameter arm, ordered after the field arms so a user type's own `first`/`last` still wins. Two siblings of the same allowlists closed with it — `drop(while:)`, silent even in the `for`-in form with `dropFirst` one character away charging, and the container conversions `Array(…)`/`Set(…)`/`joined()`. **THE FIX'S OWN FAILURE DIRECTION FIRED AND WAS A PRE-EXISTING CARDINAL SIN.** Typing a receiver hands `matchOverloads` a CONCRETE argument type where it had none, and `at != pt` with no `subtypesOf` entry was read as a PROVEN mismatch — impossible when `pt` is `Self`, an associated type or a bare generic. The candidate set went to ZERO, the edge was dropped, `Bag.add(_ other: Self)` went `Fs` -> ABSENT. **Found in the A/B's CHANGED column on a run whose HEADLINE read `ADDED 0 REMOVED 0`** — a filter-to-zero cannot appear in either headline number. Closed in `Driver.narrowByArgTypes`, now the single implementation shared with R266's twin: **the type filter may NARROW the candidate set, never EMPTY it**, falling back to the arity-compatible set — the over-approximation the same function already uses when argument types are unknown. Keyed on the OUTCOME rather than on detecting a type parameter, because the scan records only BOUNDED generic names (`struct Box<T>`'s `T` is invisible to any such test — measured). Coordinator-verified on shipped swift-nio: public `EventLoopPromise.succeed()`, body `succeed(Void())`, was **ABSENT — an affirmative purity claim** — and now carries `['Env','Unknown']`; 153 rows gained, **0 lost**. Also 38 public `TableBuilder.column(…)` overloads in SQLite.swift. A/B 12 packages, 41,249 pre-rows: R537 alone ADDED 0 REMOVED 0 CHANGED 24; R537 + never-zero ADDED 158 REMOVED 7 CHANGED 1577 (inferred +158 -7 ~74). **All 7 REMOVED audited in full** — every one `inferred: []`, one mechanism, a false `invisible:` hedge an emptied overload set had manufactured; 4 ground-truthed from source. REACH **186 hits across 10 of 12 entries**, not safety-only. Gate: 39 failures against the v0.39.0 binary, 0 against HEAD. smoke 160/0. **STILL OPEN BY STATED DESIGN:** `d.keys.first` (no key-type index — `dictElem` holds the VALUE) and `compactMap`/`map` chains (not element-preserving). |
| R539 rust: **AN OVER-CHARGE: `for y in o.unwrap()` FABRICATES an effect the bare receiver correctly does not carry** — and the comment asserting it is harmless is false in both halves. | 2026-09-22 | rust | `is_element_preserving_adapter`'s note says of `unwrap` that *both resolvers then find no element and return nothing — harmless in that direction*. **Measured false twice over:** `elem_type` has answered with the payload since R185; and the shape FABRICATES — over an `Option<Holder>` whose `IntoIterator::Item`'s `go` is PURE, `for y in o.unwrap() { y.go() }` is charged `['Exec']`, while `for y in h { y.go() }` over a bare `Holder` correctly reads ABSENT. One variable: the `Option` wrapper. Found by the [[R536]] agent testing the comment rather than reading it (§K). | **OPEN, and the entry was deliberately KEPT:** removing `unwrap` from the preserving list costs R347's guard chain, so this is recorded as a known over-charge with a repro rather than traded for a silence. That direction matters — this register's measured rate is 4 defects in 5 fabrication-fixes, two of them cardinal sins, so a fabrication removed carelessly is how the next silent under-report arrives. The comment has been scoped in place; **a false safety sentence corrected is worth as much as the fix**, and this one had stood unmeasured. |
| R538 rust: **A TUPLE-DESTRUCTURING LOOP OVER A CONTAINER OF TRAIT OBJECTS IS SILENTLY PURE — `resolve_elem_tuple` has a CONCRETE route and no DISPATCH counterpart.** `for (g, _) in v.iter().zip(n.iter())` charges `['Exec']` over `Vec<G>` and is ABSENT over `Vec<Box<dyn Sink>>`; same for `enumerate()` and for the `.for_each(\|(g,_)\| …)` spelling. | 2026-09-22 | rust (swift UNASKED — relayed; java/ts plausibly immune) | **Found by the [[R536]] agent sweeping the \"NOT swept\" list that R536's own OPEN row had named** — which is the register working as intended rather than a boundary being redrawn. One variable (the element type), same crate, same body: `Vec<G>` -> `['Exec']`, `Vec<Box<dyn Sink>>` -> ABSENT, across `zip`, `enumerate` and `for_each`. Mechanism LOCATED, not inferred: `collector.rs::resolve_elem_tuple` resolves both the `enumerate` and `zip` slots through `resolve_elem_type` only, and `tuple_pat_elem_binds` takes concrete type strings. **R349 built that resolver FOR this shape and built only the concrete half** — its own unstated residual, the same class as R536's second half and R347's. The located mechanism was RE-DERIVED from source rather than inherited, and it holds: both slots went through `resolve_elem_type` and the slot type was a `String` — which IS the defect, since a `Vec<Box<dyn Sink>>` element has no concrete type to put in one. | **CLOSED — candor-rust `7eb231d`.** Slots carry a `Bound`, so `scoped_binding` installs a dispatch slot exactly as the single-name `for` binder does. **R177 is REPRODUCED, not re-decided, and that is the direction that matters:** a synthetic `["Fn"]` hedge does NOT displace a concrete element type, because binding the hedge over a resolvable type would WITHDRAW that charge — a fix for a silent under-report introducing one. A/B 1,595 crates: **ADDED 16, REMOVED 0, CHANGED 86**, audited field-by-field — **LOSSES NONE, zero new concrete effects.** REACH 361 hits / 139 entries. Ground truth from SOURCE: proptest `VecValueTree::current`, zerovec `write_serializable_bytes_without_length`, arrow-row `fixed::encode_not_null`. `resolve_elem_tuple` has exactly TWO consumers and both now carry `Bound`; the third tuple route already had both halves, and `if let Some((g,_)) = it.next()` over a `let`-bound zip iterator is STILL SILENT because that iterator records no tuple shape — stated, not closed. The map arm remains absent, so [[R536]]'s `pin_map_pop_tuple` under-report pin is still red-on-change rather than quietly passing. **§F still relayed to swift — unasked.** |
| R542 rust: **A FACTORY RETURNING A COLLECTION OF CONCRETE ELEMENTS IS SILENTLY PURE — the mirror direction of [[R538]].** `for g in mk_conc() { g.go() }` over `fn mk_conc() -> Vec<G>` reads ABSENT while the `dyn` twin charges. | 2026-09-22 | rust (the MIRROR of [[R538]]; swift/java/ts unasked) | The fourth asymmetry from [[R541]]'s arm-set diff, measured and deliberately NOT folded into that fix. `resolve_elem_trait_leaves` decodes an `<elemdyn>` return sentinel; `resolve_elem_type` has **no concrete element sentinel to decode**. Same half-a-resolver shape as R538, opposite direction — which is why the sweep found it and meeting the bug would not have. | **OPEN, and PRICED rather than deferred silently.** The hole is a silent under-report on ANY factory-returning-a-collection. The fix is a new `ReturnIndex` encoding, roughly 40 lines across `decls.rs`/`model.rs`/`collector.rs`, **plus the fabrication control a leaf-keyed `returns` index demands** — a leaf-keyed index is what made [[R532]]'s first cut manufacture a purity claim, so that control is the deliverable rather than an extra. Not folded into R541 because it needs its own A/B; folding it in would make R541's 1,596-crate numbers stop describing the code that shipped. |
| R543 all engines: **THE SCHEDULED CORPUS ROUND REPORTS FOUR HONESTY FINDINGS THAT ARE STILL LIVE AT HEAD — a caller of a PROVIDED/body-less declaration reads pure.** rust serde `de::Visitor::visit_char` is *omitted from the report* while its body is `self.visit_str(v.encode_utf8(…))`; rust clap `builder::value_parser::OsStringValueParser::…`; swift Alamofire `Request.didCancelTask` *reads certain but calls* `EventMonitor.request(DataRequest…)`; swift `GenerateManual.generatePages`. | 2026-09-22 | rust + swift (ts/java UNASKED) | **Found by the `corpus` workflow (a SCHEDULED monitor, so its red does not sit on any HEAD and `ci-watch` had to surface it deliberately — *its NEWEST run, on an earlier commit; HEAD needs no run, so this red would otherwise vanish from the summary*).** Coordinator-verified still live at HEAD after the whole R529-R541 wave: `visit_char` is ABSENT from `functions[]` in a fresh scan of the corpus serde, and serde's source at `src/de/mod.rs:1469` confirms the body is a call to `visit_str` — another provided trait method, i.e. a dispatch over implementors. Absence is an affirmative purity claim under SPEC §2 rule 3. **NOT introduced by this wave and NOT closed by it**, which is the point: the family of defects closed today was the same shape, and these four survived all of it. | **THE TWO RUST FINDINGS RE-EXAMINED 2026-09-23 AT HEAD, AND THEY ARE NOT ONE THING — measured, not re-filed.** **clap: EXPLAINED and split out as [[R570]]** — `OsStringValueParser::parse_ref` is absent because it calls `TypedValueParser::parse(self, …)`, a fully-qualified trait call, which is a seventh receiver spelling in today's vein; reproduced minimally. **serde: NOT explained by the obvious hypothesis, and that is worth recording because the hypothesis was mine.** `visit_char`'s body is `self.visit_str(v.encode_utf8(…))`, so the natural guess is *a `self` receiver inside a trait DEFAULT method* — but a minimal fixture of exactly that shape **RESOLVES** (`via_self_default` → `['Net']`). So serde's cause is something else (the generic `<E: Error>` bound, or `encode_utf8`'s `&mut [u8;4]` retyping the argument) and remains UNDIAGNOSED rather than assumed. Both are still live at HEAD despite today's six rust fixes. **BOTH SWIFT FINDINGS NOW DIAGNOSED 2026-09-24 BY THE CODE REVIEW, AND THEY ARE THREE SEPARATE PRE-EXISTING CARDINAL SINS, NOT ONE.** **Alamofire → [[R572]]**: an overloaded conformer member plus a protocol-extension default makes `resolveQual` miss every conformer while `providedEdged` takes the union branch, so empty sets are unioned — no edge and no `Unknown`. Eight honesty violations live at HEAD, and the shape is Alamofire's ordinary idiom. **swift-argument-parser → [[R578]] and [[R579]]**: an implicit-`self` READ of a protocol-typed requirement loses the receiver's type ([[R134]] closed the CALL half), AND a `String` local whose type the engine cannot name (`let page = xs.map{…}.joined(separator:)`) loses `write(to:)` with no hedge at all. **R579 is the half the honesty checker CANNOT see — there is no `Unknown` anywhere for it to find a contradiction in.** All three are byte-identical at `eae95e9`, so none is from the 2026-09-23 wave. **Together with [[R570]] (clap) and [[R576]] (serde), all four of this row's findings now have a named mechanism.** **OPEN. The 0.39.2 family cut PROCEEDED over these deliberately, and the arithmetic is the reason: THREE of the four are ALREADY IN the published 0.39.0 artifacts byte-for-byte, and the FOURTH is in 0.39.0 in a STRICTLY MORE SILENT form, so holding the release protects nobody from them while withholding twelve cardinal-sin fixes that ARE closed.** **CORRECTED 2026-09-23 — this cell previously read "all four are ALREADY IN the published 0.39.0/0.39.1 artifacts", which was asserted, not measured, and was wrong in the direction that flattered the decision it justified.** Measured by running the PUBLISHED v0.39.0 / v0.39.1 / v0.39.2 macOS-arm64 assets over the same corpus checkouts with the same unchanged `check_honesty.py`, varying only the engine binary: clap, Alamofire and swift-argument-parser reproduce identically at 0.39.0; serde `de::Visitor::visit_char` reads *ok* at 0.39.0 — **because at 0.39.0 the callee `visit_str` is ABSENT FROM `functions[]` ENTIRELY, silently certified pure, so the checker had nothing to compare against.** At 0.39.2 the callee discloses `inferred:["Unknown"], unresolved:true, interfaceUnion` and only the caller's propagation is missing. The 0.39.0 checker PASS is the instrument being blind one level deeper, not the engine being right — the same shape as every vacuous guard in this register. The conclusion the row draws survives and is STRONGER: cutting strictly reduces silent under-reporting on every path this row names. Recorded here rather than left in a scheduled workflow's log, because a monitor's red that no HEAD carries is exactly the finding that evaporates. The corpus check's own caveat applies and is quoted rather than paraphrased: *a finding is a candidate cardinal sin; trace each to ground truth* and *a corpus finding has been wrong before: reduce every mechanism story to a FIXTURE before filing*. So the next step for each is a FIXTURE, not a fix — and the serde one is already half-reduced: the caller, the callee and the source line are named above. §F: the same check names TWO engines, so ask ts and java the provided-method question too. **RE-RUN 2026-09-25 AFTER THE NIGHT'S WAVE — 4 FINDINGS -> 2, and the two that closed were NOT attributed to any row by the work that closed them.** Dispatched deliberately because this monitor's red had been stale since 2026-09-15 while ten rows closed across three engines. **GONE: rust clap `builder::value_parser::OsStringValueParser::parse_ref` and swift Alamofire `Request.didCancelTask`.** **REMAIN: rust serde `de::Visitor::visit_char` ([[R576]](a), open) and swift `GenerateManual.generatePages` ([[R579]], open)** — both predicted to remain, and both did. **AND `visit_char` CHANGED ITS SHAPE: it read *"pure (omitted from report)"* and now reads *"reads certain"*.** So the row now EXISTS where it was ABSENT — a purity claim under ⟨0.21⟩ converted into a present-but-imprecise row, which is a real improvement on the cardinal-sin axis even though the finding stands. **ATTRIBUTION IS NOT ESTABLISHED and is not claimed:** the wave closed [[R595]], [[R598]], [[R569]], [[R567]], [[R592]], [[R593]], [[R603]], [[R608]], [[R609]], [[R610]] and any of several could be responsible; `parse_ref` is [[R570]]'s UFCS shape and R570 is STILL OPEN, so whatever closed it did so by another route. **That is the point of re-running an oracle that does not know what changed** — it measured a real-code improvement nobody was looking for, and it also says the register is under-crediting itself. Worth bisecting before anyone claims a row closed it. 56 reports checked, same as the stale run. **BISECT RESULT 2026-09-25 — one of the two closures ATTRIBUTED, the other NARROWED, and this row's own attribution was WRONG.** **(1) rust clap `OsStringValueParser::parse_ref`: closed by [[R576]](b), `05f3325`.** ABSENT at `845cb32`, PRESENT with `inferred:['Unknown']` immediately after; every later rust commit tested (`0c0a0fd`, `d449ee5`, `cf8782d`, HEAD) already shows it present, and `CANDOR_R598_PRE` changes nothing. Mechanism confirmed, not inferred: **`TypedValueParser` has 17 implementors against the bounded-CHA limit of 12, and `R576WIDE` fires twice on the crate** — the missing `else` emitted no edge and no reason, so `parse_ref` left `functions[]`; (b)'s disclosure gives it an inherited `Unknown`. **THIS ROW RECORDED THAT INSTANCE AS [[R570]]'s UFCS SHAPE AND THAT WAS WRONG** — it was a wide fan-out, not a fully-qualified trait call. **(2) swift Alamofire `Request.didCancelTask`: NOT attributed, but narrowed to the 2026-09-23/24 swift wave.** Measured against the corpus's PINNED **5.9.1** (a first attempt used current `main` and was discarded — the arms differed in more than the variable): it DISCLOSES at HEAD, with `CANDOR_R567A_OFF`+`R567B_OFF`+`R610_OFF` all set, at `96211f0` (before tonight's swift wave) and at `e539208` (before [[R565]]). So none of today's swift work closed it and the cause lies earlier. Not drilled further — three builds bought a narrowing, and a fourth would buy a name this row does not need. |
| R544 INSTRUMENT (umbrella `bin/release-preflight.sh`): **[10] CALLED A PUSHED COMMIT `NOT PUSHED` — THE SIBLING COPY OF R505, FIXED IN `ci-watch.sh` ON 2026-09-18 AND NEVER PORTED.** `if ! git merge-base --is-ancestor HEAD "@{u}"` at :1060. `@{u}` is UNSET on six of the seven family repos (`fatal: no upstream configured for branch 'main'`, exit 128), so the `!` read the ERROR as the answer and printed `✘ candor: HEAD (d462ab6) is NOT PUSHED — nothing could have run` for a commit whose SHA `origin/main` equalled and the GitHub API returned. | 2026-09-23 | umbrella instrument (not an engine) | **Found by disbelieving a release gate and checking the claim it made, mid-cut.** It is the family's most-repeated shape — *one copy of a question corrected, its sibling left* — and this time the corrected copy's own comment WARNED about the trap the sibling then hit. It stayed latent because this branch is only reached when a repo's HEAD has NO CI run at all, which is exactly what a CHANGELOG-only or pins-only commit produces: **the defect was waiting specifically for the last rung of a release ladder**, where the cost of believing it is a cut abandoned as unpublishable. Note the direction — it fails CLOSED (a false STOP, not a false GO), which is why it cost time rather than a bad release. | **FIXED 2026-09-23.** Ported ci-watch's fallback chain verbatim and in the same order: `@{u}` when it resolves, else `origin/<branch>` (which the push itself writes, and which resolves in all seven repos), else `origin/HEAD` for a detached HEAD. Calibrated BOTH directions per §1b in the same commit: the real umbrella HEAD now reads PUSHED, and a fresh throwaway repo with one unpushed commit still reads NOT PUSHED. The message now names the ref it tested (`not on $_up`) so the next false verdict is legible instead of flat. |
| R545 INSTRUMENT (umbrella `bin/release-preflight.sh` [10]): **A SCHEDULE-ONLY MONITOR'S VERDICT WAS READ AS EVIDENCE ABOUT HEAD, AND THE ONE RED IT PRODUCES CAN ONLY BE CLEARED BY THE STEP IT BLOCKS.** `release-audit.yml` derives its version from `ENGINE_PIN` and runs `release-verify` against it, so from the moment `release.sh` step 6 moves the pins to $VER until step 7 creates the umbrella's `v$VER` release, its `candor:v$VER` row cannot resolve. [10] read that red and refused step 7 — the step that creates the release whose absence was the complaint. | 2026-09-23 | umbrella instrument (not an engine) | **Found mid-cut on 0.39.2, and the first framing of it was WRONG in two ways a review caught.** I called it *"the NONE fallback is unsatisfiable"* — but the reds were on HEAD's MAIN path, because I had dispatched the two workflows onto HEAD myself while investigating; a fallback-only fix would have changed nothing. The intent/implementation gap is real and separate: the fallback's own comment at :1052 describes it as *"the last commit that DID run CI"*, while the code asks every workflow's newest run, cron monitors included. `release-audit.yml` contradicts itself in the same file — *"It is a MONITOR, not a gate … cannot block anything"* at :20, against *"a stuck runner blocks the release gate (release-preflight [10])"* at :37. No escape hatch existed: `PINS_ADVISORY` covers [3] only, `CI_NO_WAIT` only stops the wait loop, and `--only` cannot exclude the umbrella because step 7's own pin check requires every excluded engine to be pinned elsewhere. | **FIXED 2026-09-23 — and NOT by a name denylist, which was the first plan and was unsound.** `release-audit` also live-smokes the published artifacts, so denylisting its name would wave through a DRAFT engine release (the 0.33.0 case step 8 cites) or a 404'd jar at the exact moment it matters — the 0.24 class with a better error message. Instead `rs_cut_window_expected()` RE-RUNS THE AUDIT'S OWN INSTRUMENTS and demands they pass now: the red is advisory only if (1) release mode and the umbrella is in the cut set and **every** red is `release-audit` — a `corpus` red still blocks; (2) `rs_family_pin` == $VER, so step 6 is done; (3) `v$VER` does not exist, so step 7 is not; (4) `release-verify.sh $SPEC $VER --only <set minus candor>` passes NOW; (5) `pin-currency.sh` passes NOW. Fails closed at every condition. Calibrated in the same commit per §1b — 1 advisory, 4 blocking: only-release-audit → advisory; `corpus:failure` alongside → BLOCKS; injected draft engine release (cond 4) → BLOCKS; `pin-currency` red (cond 5) → BLOCKS; pins not yet moved (cond 2) → BLOCKS. The window logic is deliberately NOT in `release-audit.yml` or `release-verify.sh`: a monitor cannot know a cut is in flight, and one that tolerates a missing umbrella release is blind to the rot it exists to catch. **§9 SWEEP — all seven repos' workflows, not just the one in hand, and it found ONE TRUE SIBLING: `candor-spec/.github/workflows/conformance.yml`'s `released-floor` job** pins the suite to the latest RELEASED spec tag and installs the latest published java jar and rust crates, so at a FLOOR cut it is red between the spec tag landing (step 3) and the engines resolving. It does NOT block, and the reason is the MIRROR defect: it shares a workflow ID with the push jobs, so `_ci_verdict.py`'s newest-per-workflow rule hides its red behind the next push-green — **a monitor red [10] can never see**, which is the opposite error and left open here deliberately rather than fixed blind. Everything else is outside the class and was checked rather than assumed: the release's own `on: release` workflows ([10] already waits for those), the integration builds that consume published artifacts (kept satisfiable by the ladder publishing at steps 1-3, before the pins move at 6), and the tree-reading monitors (`corpus`, the confirmatory corpora, `soundness-weekly`) whose red is real signal about HEAD. |
| R546 SPEC (§2, the cross-package interface dispatch clause at SPEC.md:466): **THE DOCUMENT ASSERTED THE EXACT INVERSE OF FOUR-WAY BEHAVIOUR, IN THE ONE SENTENCE A READER WOULD USE TO DECIDE THIS NEEDED NO CLAUSE.** It read *"the silent-pure form of this miss was candor-ts-specific; the other engines already fall to a disclosed `Unknown` here."* Measured at HEAD: candor-ts is the ONLY engine that discloses. | 2026-09-23 | SPEC document (all four engines measured) | **Found while pricing the ⟨0.39⟩ (the clause once drafted as 0.40) candidate [[R533]] — a review attacked the premise that ts "already pays" the rule, and the SPEC sentence was the premise's source.** Re-derived first-hand rather than taken from the review's words, via `conformance/part.sh 92`, arm `c9_consumer_zero_union`, all four engines from clean working trees: java `eff=∅ unknown=False invisible=∅`; rust the same; swift the same; **ts `eff=∅ unknown=True`**. This is the [[feedback-documented-limitation-is-not-measured]] class in its most expensive form — not a stale comment in an engine, but a false statement of engine behaviour in the SPEC itself, sitting in the clause a rung would amend. A sentence that says *the other engines already handle this* is precisely the sentence that stops anyone measuring whether they do. | **FIXED 2026-09-23 — corrected in place, not deleted**, because the claim is what a reader relied on and a silent deletion leaves no trace of the error. The replacement states what was measured, names the arm it was measured on, and records the direction that matters for pricing: ts is the existence proof that disclosure SHIPS, not the outlier that needed fixing. The MUST ledger caught the edit (the statement's sha moved) and the entry was RE-ANCHORED by re-reading the clause against PART 92 rather than assuming the reword was cosmetic — the normative content is unchanged, only the factual NB. |
| R547 java + swift: **THE SILENCE AT A FOREIGN-ABSTRACTION DISPATCH IS PERMITTED ONLY IF DOCUMENTED, AND NEITHER ENGINE DOCUMENTS IT.** SPEC §4: *"Dispatch through an EXTERNAL abstraction an engine does not model MAY remain unflagged, but then MUST be documented as a named miss (item 7, §7)"*, and §7 item 7 is *"documents, plainly and in its own docs, what it cannot see"*. On the [[R533]] shape java and swift read silently pure and say nothing about it anywhere in their own docs. | 2026-09-23 | java + swift (rust COMPLIANT, ts flags so owes nothing) | **Re-derived first-hand, and the scope came out NARROWER than the review that suggested it — which is why it was re-derived.** A review reported no named-miss line in java, rust OR swift; searching each engine's own docs found **candor-rust/README.md:181 DOES carry one**: *"It **misses** — silently, by design — effects reached only through external-trait dispatch or an uninferrable receiver"*. That is a compliant §7 item 7 disclosure and rust is not in this row. ts discloses `Unknown` at the site, so it owes no documentation. That leaves java and swift. **java is the worse of the two, because its README does not merely omit the miss — it implies the opposite**: after documenting the pure-exempt set (the `Object` surface, Kotlin/Scala/Groovy closures, `Runnable`/`Callable`) it states *"All other dispatch — including over external interfaces with project impls (`java.util.Iterator`) — is still CHA-resolved"*, which a reader takes as coverage of exactly the case that is silent. swift documents nothing at all for this shape. | **CLOSED BOTH HALVES — candor-java `85cb65c`, candor-swift `4ef6f48`. The swift half was closed 2026-09-23 and this row was never updated; verified at HEAD 2026-09-24, claim by claim, before being closed here.** Originally filed: OPEN — the SWIFT half. (Written this way deliberately: the cell first read *"JAVA HALF CLOSED…"*, which the status tool correctly bucketed `closed-with-fix`, taking a row with a live open half OFF the shipping-defect list — **the exact class [[R553]] had just fixed, committed by me one hour later.** A partially-closed row must DECLARE the part that is still open FIRST; anything else is commentary. That is the rule `_DECLARED_OPEN` encodes, and it caught its author.) java's README now carries a named miss instead of the sentence that implied coverage, and it was written from a FIRST-HAND measurement rather than from this row's description — the whole point of the row being that a doc must state what the engine does. Three arms, one variable: `iface` alone → `Terminal.termSize` correctly reads `Unknown` (`unknownWhy:["dispatch:iface.Backend.size"]`), so bounded CHA works as documented; `app` UNCHAINED → discloses via `invisible:["iface"]`; **`app` CHAINED → `inferred:[]`, `unresolved:false`, no `unknownWhy`, omitted from the report, and `pure`/`deny` answer as if proven pure.** That third arm is an independent confirmation of [[R533]]'s shape in the reference engine, from a different fixture. Gates: 10 run, 10 ok, 0 failed (2 environmental skips, named). **OPEN for swift — and deliberately filed SEPARATELY from [[R533]], because it is true today whichever way R533's pricing lands.** If the ⟨0.39⟩ (the clause once drafted as 0.40) tightening ships, both engines flag and the documentation question dissolves. If it is DECLINED on cost, the silence becomes permanent-by-decision and this documentation is the only thing that makes it conformant — so the declination cannot be recorded without closing this row first. Either way the fix is cheap and owes nothing to the measurement: java must replace the "all other dispatch is still CHA-resolved" sentence with the truth (a foreign abstraction with no visible implementor resolves to nothing and is not disclosed), and swift must add a named miss at all. **The shape to avoid is a third engine quietly matching rust's wording without matching rust's behaviour** — the documentation must say what the engine actually does, which is the [[feedback-documented-limitation-is-not-measured]] trap in the direction that writes a comforting sentence rather than a true one. **SWIFT HALF VERIFIED AND CLOSED 2026-09-24 — candor-swift `4ef6f48` ("document the dependency-abstraction dispatch miss (R547, swift half)"), which landed the day this row was filed and was never recorded here.** README.md:68-95 carries `### The named miss: dispatch through a DEPENDENCY's abstraction`, and it does the thing this row warned against doing wrong: it states what THIS engine does, not rust's wording. **Every claim in it was re-verified first-hand rather than read approvingly** — four off today's four-way run and the fifth by executing it: (1) chained + zero implementors reads silently pure — the App row is literally `{inferred:[], unresolved:false, dispatchesOn:["Iface#Backend.size"]}`, no `unknownWhy`, no `invisible`, matching the doc verbatim; (2) "three of the four engines behave this way, TypeScript is the one that discloses" — c9 java/rust/swift all `unknown=False`, ts `unknown=True`; (3) unchained via a free function carries `invisible` — c5 swift `invisible=['EffImpl','Iface']`; (4) unchained direct dispatch is ABSENT from `functions[]` — c10 swift `ABSENT (a purity claim)`, which is [[R548]] and stays open; (5) **`pure` and `deny Net` both exit 0 over the chained consumer — EXECUTED, and the gate was CALIBRATED to prove it can fail: the same `deny Net` over the effectful package exits 1 with '1 policy violation(s)'.** **CONSEQUENCE FOR [[R533]]: this row is no longer a prerequisite for recording a declination** — a review had flagged it as one, correctly at the time it was filed and no longer true at HEAD. |
| R548 swift: **THE CONSUMER ROW GOES ABSENT — AN AFFIRMATIVE PURITY CLAIM — WHEN THE DISPATCH IS WRITTEN AS A DIRECT METHOD CALL RATHER THAN THROUGH A FREE FUNCTION.** Unchained dep, protocol with zero conformers, consumer calls `b.size()` on a protocol-typed value: swift emits no row at all and no `invisible`. java, rust and ts each disclose `invisible:['iface']` on the identical fixture. | 2026-09-23 | swift (java/rust/ts all disclose) | **The tell is that swift PASSES `c5_unchained` on the same dep, same protocol, same zero-conformer setup — `invisible:['EffImpl','Iface']`.** The ONLY variable is the consumer body: c5 calls a free function that takes the abstraction (`iface.termSize(b)`), this arm dispatches directly on the value. So the divergence is body-shape-dependent, in a PART whose entire subject is that those two shapes resolve differently — and c9 had been pinning the chained direct-dispatch case with **no unchained control for its own body shape**. "Unchained discloses via `invisible`" was measured for one shape and ASSUMED for the other. Suggested by a review as a side finding; re-derived first-hand before filing, which is how the ABSENT came out — the review reported `inferred: []`, the judge reports the row missing entirely, and those are different claims. **PRICED, NOT FIXED — 2026-09-24, candor-swift `e539208` (an env-gated diagnostic probe; no report changes).** **The defect, exact and one token wide:** on PART 92, `appSize(_ b: Backend) { termSize(b) }` (c5) keeps its row with `invisible:[EffImpl, Iface]`, and `appSize(_ b: Backend) { b.size() }` (c10) loses the row ENTIRELY. Driver.swift's blind attribution has two arms — `call.unqualified` (free call/ctor) and a module-qualified member call whose `extOwner` IS a blind module — and a bare `b.size()` is neither, so nothing attributes and a row carrying nothing is dropped. Its own comment (*a member-only blind receiver is covered by the scan-level κ-ledger*) is TRUE at scan level and FALSE per-function, which is §K. **THE PROBE SETTLED THE THING A PREDICATE TWEAK WOULD HAVE GOT WRONG:** for c10 `extOwner=Backend` — the receiver's spelled TYPE, not a module — and `foreignOwnerModule` returns **nil** because the file has TWO blind imports, which is also why c10 emits no `dispatchesOn` where chained c9 does. **AND THE OBVIOUS THIRD ARM FLOODS, MEASURED ON THREE REAL PACKAGES: candor-swift 1306 sites (Set 687, String 172, ExprSyntax 141, NSString 52, Array 15), pollen 869 (Text 164, Color 70, Image 59, Button 40, VStack 37, Calendar 34, NSApp 29), swift-syntax 106.** Stdlib and SwiftUI receivers — precisely the *rampant false uncertainty* the earlier widening was REVERTED for. **The revert was right and this row's stated remedy would have repeated it.** **THE FIX NEEDS TYPE PROVENANCE THE ENGINE DOES NOT HAVE:** telling `Backend` (blind-module type, attribute) from `Set` (stdlib, never a blind reach) requires knowing which module declares an EXTERNAL type; `declaredTypes`/`localTypes` are LOCAL-only, there is no stdlib type list, and `PLATFORM_MODULES`/`KAPPA_MODULES` are MODULE lists that cannot answer a question asked about a type. Tractable shape: a stdlib/platform TYPE denylist (the [[candor-denylist-over-allowlist]] direction, since the starting point — silence — is unsound), error mode over-disclosure. Residual after it: `ExprSyntax`/`Syntax`, genuine dependency types where attributing may be CORRECT. | **OPEN, and pinned as PART 92 arm `c10_unchained_direct` (candor-spec, this commit) with swift alone declared in the arm-keyed XFAIL table.** Calibrated in the same commit per §1b, both directions: the arm FIRES (swift red before the line was declared, `ABSENT (a purity claim) — invisible=∅ want True`) and it RETIRES LOUDLY (a fake xfail injected on ts, which passes, produced `XFAIL ARM PASSED … expectation is STALE: R548` and redded the part; removed by string match, never `git checkout`, which would have destroyed the other uncommitted edits in that file). **NOT gate-affecting** — `invisible` arms no policy form ([[R133]], CLOSED as a documentation gap on Tom's 2026-09-03 ruling) — which is precisely the argument for pinning it rather than trusting it: no gate, no verdict and no other arm in the suite would ever have noticed. Fix is swift's: emit the row and the `invisible` on the direct-dispatch path as it already does through the free-function path. |
| R549 rust: **A PUBLISHED `dispatchesOn` KEY NAMES A MEMBER THE NAMED TRAIT DOES NOT DECLARE, SO NO CONSUMER CAN EVER JOIN IT — AND ⟨0.39⟩ OBLIGATION 3 IS THE JOIN.** On `tower` chained onto `tower-service`, **11 of 19 distinct `dispatchesOn` keys** are `tower_service#Service::<member>` for members `Service` does not have: `map_err` (9 rows), `clone` (9), `map_future` (5), `load` (3), `make_service` (3), `err_into` (2), `is_pending`, `and_then`. `tower_service::Service` declares exactly **`call` and `poll_ready`**. | 2026-09-23 | rust (shipped 0.39.2 — not the prototype) | **Found while pricing [[R533]], by asking why certain keys survived java's conjunct 4 — and the answer was that they are not real.** The key is formed from the RECEIVER's trait qualifier plus the method LEAF, with nothing checking the leaf belongs to that trait. `map_err`/`load`/`make_service` are `ServiceExt` methods — an extension trait **tower declares LOCALLY** — so the receiver is `Service`-typed but the method resolves elsewhere. Measured on the STOCK HEAD binary (`scan-0.39.2`), not on the R533 prototype, and the prototype-OFF arm was first verified byte-identical to HEAD. **The engine already has the rule this breaks, one function over:** `scan.rs:3852` filters `dispatchesOn` to owners in `Cargo.toml`'s real dependency set precisely because *"a key no consumer could ever join is not harmless — it is a name collision waiting for a real crate called `io`"*. That guard checks the OWNER and never the MEMBER. | **OPEN — SWEPT AND ESCALATED 2026-09-23 — SYSTEMIC, TWO MECHANISMS, AND ONE OF THEM LOSES A REAL KEY.** §9 sweep across all six chained rust pairs rather than the instance in hand, using a ONE-DIRECTIONAL test (a member appearing nowhere as `fn <leaf>` in the dep's source CANNOT be declared by it; presence proves nothing, absence proves malformation): **26 of 75 checkable keys (35%) are malformed**, across four consumers and four dep crates — `tower_service`, `bytes`, `http_body`, `futures_core`. **MECHANISM A — an extension trait's method attributed to the base trait. ⚠ THE EXAMPLE THIS ROW CITES FOR A IS ACTUALLY B — CORRECTED 2026-09-23 BY INSTRUMENTING THE KEY-FORMATION SITE RATHER THAN RE-READING THE ROW.** All 9 `tower_service#Service::map_err` occurrences are CHAIN DRIFT (`self.inner.poll_ready(cx).map_err(..)`), not extension-trait misattribution. Classifying all 53 malformed occurrences by the RECEIVER'S SYNTACTIC SHAPE gives FIELD 22 malformed / 117 well-formed, CHAIN 22 / 3, PATH 9 / 17 — a **31 direct-receiver / 22 chain-drift** split, not the one-example-each this row implied. The genuine A instances in tower are `map_future` (ServiceExt), `make_service`, `load`, `clone`; the cleanest A corpus is futures-util/futures-lite, not tower.** `ServiceExt::map_err` is published as `tower_service#Service::map_err`; `ServiceExt` is declared LOCALLY by tower and `tower_service::Service` declares only `call` and `poll_ready`. **MECHANISM B — the receiver's trait drifts along a METHOD CHAIN onto the wrong link**, with a source witness: `http-body-util/src/util.rs:37`, `self.bufs.front().map(Buf::chunk).unwrap_or_default()`, publishes `bytes#Buf::map` and `bytes#Buf::unwrap_or_default` — neither a `Buf` member. Likewise `me.body.as_mut().poll_frame(cx)` publishes `http_body#Body::as_mut`, where `as_mut` is a `Pin` method. **SEVERITY IS SPLIT, AND THE SECOND HALF IS THE SERIOUS ONE.** Usually the bogus key is ADDITIVE — `BoxBody::poll_frame` carries both `Body::as_mut` (bogus) and `Body::poll_frame` (real), so nothing is lost and this is precision noise, NOT a cardinal sin. **But where the trait method is passed as a FUNCTION REFERENCE the real key is DROPPED: `util::BufList::chunk` publishes ONLY `bytes#Buf::map` and `bytes#Buf::unwrap_or_default`, and `bytes#Buf::chunk` appears NOWHERE in the entire report** — while the row reads `inferred: []`. A consumer chaining onto http-body-util cannot learn that `BufList::chunk` reaches `Buf::chunk`, because the key ⟨0.39⟩ obligation 3 tells it to join on was never published. That half IS a silent under-report of the dispatch surface.  **FIX ATTEMPTED 2026-09-23 AND DELIBERATELY NOT LANDED — the attempt found the REAL blocker, which is a gap one level down.** Minimal three-row reproduction built first (`scratchpad/r549-fix`, dep crate + consumer, ONE variable — how the dispatch is spelled): `self.bufs.front().map(Buffy::chunk).unwrap_or_default()` publishes `depcrate#Buffy::map` + `depcrate#Buffy::unwrap_or_default` and **NOT** `Buffy::chunk`; the control `match self.bufs.front() { Some(b) => b.chunk(), .. }` publishes `depcrate#Buffy::chunk` correctly. Same dispatch, same dep, same trait. (The first cut of that fixture was itself HOLLOW — a trait with only bodiless methods gives `analyzed.count: 0` — and the engine said so loudly, `chained dependency report(s) judged NOTHING`. Fixed by giving the trait a defaulted method and an impl before any number was read.) **The fix — record a trait method named as a FUNCTION REFERENCE, in the existing `visit_expr_path` hook, gated on provenance plus "the penultimate segment is a known trait" — BUILT CLEAN AND DID NOT FIRE. The `CANDOR_ALIAS_DEBUG` reach probe reported 0 `R549HIT` while showing 5 other hits on the same channel, so the channel was live and the code simply never ran.** Not attributed to the code before the fixture was proven to reach it, per [[feedback-prove-the-fixture-reaches-the-code]]. **ROOT CAUSE OF THE MISFIRE, and it is the finding: `trait_quals` is built by `sig_trait_quals(sig)` at `decls.rs:2007` — from a FUNCTION SIGNATURE'S generics ONLY. A bound written on the IMPL BLOCK (`impl<T: Buf> BufList<T>`) never reaches it.** That is the shape both this fixture and the real http-body-util code use, so the trait-name index cannot answer "is `Buf` a trait here?" at the very site that needs it. Only `sig` is in scope at that construction, so supplying impl-level bounds means threading the `ItemImpl`'s generics down the caller chain — a resolver change with real blast radius, not a one-liner, and not something to land unvalidated at the end of a long session. **So the correct order is: close the impl-generic-bound gap in the trait-name index FIRST, then this fix becomes small and gateable.** The `via_method` control resolves today only because it travels the R478 trait-FIELD route (`R478JOIN BufList.bufs ["Buffy"]`), which knows the field's element trait independently of `trait_quals` — i.e. two indices answer "what trait is this?" and only one of them knows about impl bounds.  **THE FN-REF HALF IS FIXED AND SHIPPED, candor-rust `186e854` — the half that LOSES a key.** The second attempt landed because the first one's misfire named the right predicate: not `trait_quals` (a QUALIFIER map, which drops bare-leaf bounds by design and reads signature generics only) but a new additive index **`bound_trait_leaves`** — bare leaves included, the enclosing impl block's generics merged in, read by NOTHING except this one predicate, so it cannot move an effect already inferred. Qualification still comes from `expand` + the file's `use` map, exactly as the method-call route does it. A/B over the six chained crate pairs: **0 effect rows changed, 12 dispatch keys ADDED, 0 lost**, and every new key is a real trait member — `futures_core#TryStream::try_poll_next`, `bytes#Buf::chunk`, `futures_01#Future::poll`, `futures_core#Stream::size_hint`, `tokio_io#AsyncWrite::poll_flush`/`shutdown`. Disclosure direction, never fabrication. Calibrated per §1b in the same commit: a three-row test whose CONTROL is the method-call spelling of the identical dispatch (one variable — how it is spelled), which **FAILS on exactly its own assertion with the gate disabled**, and which asserts the dep report is not HOLLOW first. Gates: clippy `-D warnings` clean, `cargo test --workspace` 892 passed / 0 failed.  **STILL OPEN: mechanism A (an extension trait's method attributed to the base trait) and the CHAIN-DRIFT half of B (`Buf::map`, `Buf::unwrap_or_default` are still published).** Those are precision, not loss — the bogus keys sit ALONGSIDE the real one now that the real one exists — so they are worth fixing after, not before. Note the drift fix runs in the UNDER-REPORT direction (returning "no trait" for a chain link turns a resolved dispatch into silence) and so needs its own A/B rather than a name added to a list. Two consequences remain, and the second is why this is filed separately rather than inside R533.** (1) DIRECT: a consumer joining obligation 3's union on such a key finds nothing, the union reads empty, and the dispatch reads silently pure — the exact toggle ⟨0.39⟩ exists to close, reached through a malformed key instead of a missing implementor. (2) MEASUREMENT: **it inflates every figure keyed on "a dispatch nothing answers", which is precisely how R533 is being priced** — those keys are unanswerable because they are WRONG, not because the abstraction has no implementor. So R533's rust numbers (3.68% unfiltered, 1.93% with conjunct 4) are UPPER BOUNDS pending this fix, and re-pricing rust should follow it rather than precede it. The fix is to check the member against the trait's declared items before forming the key, and to fall back to the local extension trait when the leaf resolves there — the same question `foreign_impls`' 2-segment-tail fallback already answers in the other direction. **⚠ AND THAT PROPOSED FIX IS NOW REFUTED FOR THE CHAIN-DRIFT HALF, 2026-09-23, WITH THREE INDEPENDENT PROOFS — a member check on TODAY'S WIRE would reintroduce the cardinal sin this row's other half just closed.** Premise check first: the LOSS half IS closed at HEAD — `util::BufList::chunk` now publishes `bytes#Buf::chunk` beside the two bogus keys — so what remains is additive precision noise and any further fix is purely SUBTRACTIVE. **Why the wire cannot answer "does trait T declare member m": (1) `functions[]` holds only members WITH BODIES — `bytes.json` carries 44 `Buf::` members, the DEFAULTED ones present, while the REQUIRED bodyless `chunk`/`advance`/`remaining` (`buf_impl.rs:119/150/224`) are ABSENT; (2) `interfaceUnion` drops any member whose implementors are all pure (`scan.rs:4299`, "silence = purity") — 2 entries in all of `bytes.json`, none for `Buf`; (3) `typeSurface.returns` names ZERO `Buf::` members.** **So that check would DELETE `bytes#Buf::chunk`, `::advance` and `::remaining` — real, joinable keys on 5 of the 6 `BufList` rows, three of them restored by `186e854` itself. Absent-from-report is exactly what a REQUIRED trait method looks like.** The dep report must first carry each trait's DECLARED member set: the same format gap [[R533]] names at `scan.rs:4298` — two open rows, one wire extension. **And a purely local rule cannot cover the class, measured not assumed:** a fresh HEAD census over the six pairs gives 326 checkable occurrences, **58 malformed (18%)**, 18 distinct keys — of which **15 are `tower_service#Service::clone` on a DIRECT receiver** (`self.inner.clone()`, `inner: S, S: Service<Request>`). No chain rule can see those; only a member check can. **STATUS HEAD CORRECTED 2026-09-24 — the row read as CLOSED to the status tool while its own body says STILL OPEN** (mechanism A, and the CHAIN-DRIFT half of B). The outcome cell opened `SWEPT AND ESCALATED`, which is a description of work done, not a status word, so `bucket()` fell through to the closure-word inference and filed it `closed-with-fix`. Re-headed `OPEN —`. This is the same class as [[R588]] and was found in the same pass: the remainder includes the function-reference spelling, where the real key is DROPPED rather than merely mis-named, which is a silent under-report and belongs on the shipping-defect list. |
| R553 INSTRUMENT (`scripts/soundness-status.py`): **NINE ROWS THAT SAID "NOT FIXED" IN PLAIN ENGLISH WERE KEPT OFF THE SHIPPING-DEFECT LIST BY AN INCIDENTALLY-CITED COMMIT HASH.** `bucket()` substituted the negated closure away BEFORE testing, so the cell lost its only status word, matched `FIXSHA` on a commit it merely cites, and landed in `cites-a-sha-only` — a bucket the report prints as *"read these, they are neither clearly"*. R126, R137, R138, R162, R202, R213, R225, R231, R369. | 2026-09-23 | register instrument (not an engine) | **Found by working the limbo bucket itself rather than by any row complaining.** The tool's own docstring calls `open` *"THE shipping-defect list"*, and it was under-reporting it by nine — **the third defect of this exact shape in this one file**: reading `Not fixed.` as FIXED once hid 32 open rows, and sweeping the accepted-floor table once added 38 phantom ones. An instrument that measures under-reporting, under-reporting. The count I had been quoting all day (54, then 57) was wrong in the flattering direction every time. | **FIXED 2026-09-23 — and the FIRST version of the fix was too WIDE, which the selftest caught before it shipped.** I first treated ANY negated closure as a declaration of open; that broke a deliberate existing case, `"surfaced by the R519 work and NOT closed by it"`, whose comment argues it belongs in limbo because the negation is SCOPED TO OTHER WORK and makes no claim about this row. That argument is right. The landed rule is ANCHORED to the head of the outcome cell (`_DECLARED_NOT_FIXED`, mirroring `_DECLARED_OPEN`): the first thing a cell says is the row's own verdict, anything later is commentary about something else. All nine moved rows open with the declaration; the near-miss does not. Row-by-row diff of old vs new bucketing over the whole register: **exactly 9 rows moved, all in one direction, nothing else changed.** Four selftest cases pin it — three for the declaration WITH a cited SHA, one for the scoped near-miss that must stay put. Register now reads **460 rows, 65 open**. |
| R551 rust: **AN EXTENSION TRAIT'S METHOD WAS KEYED TO THE BASE TRAIT, WHICH DOES NOT DECLARE IT — the direct-receiver half of [[R549]]'s malformed-key class.** `ServiceExt::map_future` published as `tower_service#Service::map_future`; `Service` declares only `call` and `poll_ready`. 31 of the 53 malformed occurrences are this shape. | 2026-09-23 | rust (the class is rust + swift; java/ts structurally immune) | **Found by instrumenting the key-formation site and classifying every occurrence by the RECEIVER'S SYNTACTIC SHAPE, which is also what corrected [[R549]]'s own mis-drawn A/B split.** | **FIXED 2026-09-23 — candor-rust `0cc0b4e`.** A method resolving to a locally-declared extension trait is keyed to THAT trait: `tower#util::ServiceExt::map_future`, `futures_util#stream::stream::StreamExt::poll_next_unpin`, `futures_lite#io::AsyncWriteExt::write`. Verified first-hand at HEAD. A/B with `bin/corpus-ab.py` over THREE corpora — 6 chained pairs (13 changed), 20 crates carrying the shape (30 changed), and **a 39-package CONTROL without it (clap/hyper/regex/ripgrep/serde/tokio) printing 0/0/0** — **ADDED 0, REMOVED 0, `inferred` unchanged in every arm**. All 43 changed rows audited individually, no sampling: one malformed key swapped for one real one, no other field moved. R549's census over the six pairs drops **26 of 78 malformed → 20 of 78**. **TWO NEAR-MISSES, BOTH CAUGHT BY THE A/B AND NEITHER BY REASONING, and they are the reusable part.** (1) The author's own soundness *proof* was FALSE — the claim that a supertrait and its extension cannot declare one name (`x.m()` would be E0034) fails on `futures-lite`, which declares `StreamExt::poll_next(&mut self)` beside `Stream::poll_next(self: Pin<&mut Self>)`: different receiver types, no ambiguity. The draft rewrote **11 rows' REAL, joinable `futures_core#stream::Stream::poll_next` onto the extension trait — the under-report direction.** Fixed by asking the crate's own ⟨0.39⟩ obligation-2 index (`foreign_impls`): a member this crate is seen IMPLEMENTING for that foreign trait is a member the trait really has. **The engine already held the answer — tower's `interfaceUnion` set contains exactly `Service::call` and `::poll_ready` and none of the bogus members. Obligation 1's key set and obligation 2's key set DISAGREED, and R549 is that disagreement.** (2) Without the `contains("::")` provenance half, rayon's marker supertrait `trait Producer: Send + Sized` made `OptionProducer::into_iter` claim a dispatch on `Producer::into_iter` where the call is `Option::into_iter` — fabrication, found by auditing the ADDED column against rayon's source. |
| R554 INSTRUMENT / EVIDENCE: **A 1,608-CRATE CORPUS CITED BY A MEASUREMENT IS NOW 1,608 EMPTY DIRECTORIES — 0 bytes, 0 files — so the measurement that rests on it can no longer be re-run.** `scratchpad/fable-r475-corpus/`. A hollow corpus is the R242 shape: a differential over it prints `ADDED 0 / REMOVED 0 / CHANGED 0`, character-for-character what a correct, safely-inert change prints. | 2026-09-23 | instrument / evidence durability (not an engine) | **Found by a dispatched agent that tried to USE the corpus and was refused, then verified first-hand: 1,608 directories, `du` reports 0B, `find -type f` returns nothing.** | **OPEN as a STANDING HAZARD, and deliberately NOT escalated into a claim that the ⟨0.39⟩ cost numbers are void — because they are not.** The decisive argument is arithmetic, not trust: **a hollow corpus prints 0/0/0, and SPEC.md's ⟨0.39⟩ cost paragraph reports 9,636 ADDED / 0 REMOVED / 34,211 changed keys over 'over 1,608 crates'.** Those figures cannot come from an empty tree, so that corpus WAS populated when it was measured; it emptied afterwards. Sibling scratch directories created 09-20 still hold files (one holds 13,208) and the box runs no daily cleanup script, so simple aging is not established either — the cause is undetermined and saying so is the honest answer. **What IS established and is the finding: evidence under a temp path is not durable, so a citation of the form 'measured over corpus X' silently becomes unverifiable while still reading as evidence.** `bin/corpus-ab.py` REFUSED rather than printing 0/0/0 (exit 3 on `analyzed.count == 0`) — the instrument working exactly as R242 built it to. The remedy is not to keep the bytes: it is that a measurement's write-up must carry the numbers that let it be CHECKED (counts, denominators, direction) rather than a path that will rot, which the ⟨0.39⟩ paragraph happens to do and is why it survives this. |
| R550 swift: **A GENERIC BOUND WRITTEN ON THE ENCLOSING TYPE IS INVISIBLE TO THE DISPATCH SPELLING, SO THE CONSUMER READS PURE AND THE GATE FLIPS — A CARDINAL SIN, AND THE IDENTICAL ROOT CAUSE AS RUST'S [[R549]]/[[R551]] IN A SECOND ENGINE.** `FnInfo.genericBounds` is built from a `FunctionDecl`/`InitializerDecl`'s OWN generic and `where` clauses, so a bound declared on the enclosing type never reaches `dispatchAbstraction` — the one site ⟨0.39⟩ spells a receiver onto the wire, and the site SPEC §2's chained join keys on. | 2026-09-23 | swift (rust had the same defect in `sig_trait_quals`) | **Found by handing the swift lane rust's R549 as a TEMPLATE and asking it to measure swift rather than port the description — and the root cause came back identical, which neither engine's own investigation would have shown alone.** In rust the blind index was `sig_trait_quals` missing `impl<T: Buf>`; in swift it is `FnInfo.genericBounds` missing a type-level bound. **[[R532a]] fixed the FUNCTION-level spelling and left the TYPE-level one open — §A.2 exactly: a fixture inheriting the blind spot of the report that prompted it.** [[R532b]] item (d) named this as "R532a's own fix's stated boundary" and classed it under *keys nobody can answer*; **that classification is WRONG and is corrected here — it is a LOST KEY AND A LOST EFFECT, and a gate flip.** Three-package fixture with BOTH spellings in ONE consumer file, so dependency, conformer, consumer text and binary are literally shared and the only variable is where `B: Backend` is written: `func appSize<B: Backend>(_ b: B)` → `inferred:[Net]`, `dispatchesOn:[Iface#Backend.size]`, **`deny Net` exit 1**; `struct Box<B: Backend> { func boxSize(_ b: B) }` → `inferred:[]`, `dispatchesOn:[Iface#B.size]`, **`deny Net` exit 0** — green over a call reaching a third package's `URLSession.dataTask`. | **FIXED 2026-09-23 — candor-swift `92496cb`.** `dispatchAbstraction` also consults `typeGenericBoundsAll`, the merged scan-global index the resolver ALREADY builds for `unresolvedGenericFields` — not a second copy, which is the §F1.3 trap. The function's own bound still shadows. A/B with `bin/corpus-ab.py` over 11 real packages including swift-nio-ssl and swift-nio-http2 CHAINED onto swift-nio, PRE from a detached worktree at `eae95e9`, 10,210 rows: **ADDED 0, REMOVED 0, CHANGED 29, `inferred` CHANGED 0.** **REACH IS STATED RATHER THAN LEFT TO BE FOUND (§E1): 15 probe hits, all swift-nio, all on bounds the scanned package declares itself — so ON THIS CORPUS THE CHANGE IS NAME-ONLY and the effect recovery rests on the fixture.** The recall hunt did find the shape in real chained code (`NIOSSLClientTLSProvider<Bootstrap: NIOClientTCPBootstrapProtocol>`) but neither site calls a member on that receiver. Calibrated in the same commit per §1b: three enclosing-type spellings (`<B: Backend>`, `where B: Backend`, `extension Box where B: Backend`) each asserted EQUAL to the existential spelling, the FUNCTION-level spelling as an in-test control, and a `struct Box<T: Encodable>` fabrication control; all four arms `swift build` cleanly (§E3); pre-fix output pasted (`Executed 2 tests, with 7 failures`). Gates: `swift test` 1247/0, `smoke.sh` 160/0, `fuzz.py` 25 seeds/0, `recall.sh` 5 recalled/0 under-reports, `self-gate` OK. |
| R555 swift: **A GUARD WRITTEN TO EXCLUDE LOCAL ABSTRACTIONS IS VACUOUS FOR NEARLY EVERY ABSTRACTION IT WAS WRITTEN FOR — it tests a set that protocols are DELIBERATELY kept out of.** `dispatchAbstraction`'s own doc says *"A bound that is LOCAL … returns nil rather than a key"*. The guard reads `localTypes`, which `pushType` fills from class/struct/enum/actor/extension and **not** from `ProtocolDecl` — deliberately, because a protocol in `conformers` would pollute the CHA. **A protocol bound is nearly every bound.** | 2026-09-23 | swift | **Found by the swift lane while fixing [[R550]] — i.e. by reading the guard that was supposed to make its own fix safe.** This is the session's dominant shape in its purest form: a check that cannot fire, whose comment asserts it does. Measured: swift-nio's 15 sites publish `CNIOAtomics#AtomicPrimitive.*` and `CNIOAtomics#NIOAtomicPrimitive.*` for protocols declared in `NIOConcurrencyHelpers` ITSELF — a local protocol keyed under a dependency module. | **CLOSED 2026-09-23 — candor-swift `0c16614`.** Ownership is now decided where the key is SPELLED, via `localProtocolWirePath` — **the same function the in-scan protocol-CHA publish site already used, so the two sites that disagreed now share ONE implementation**, which is [[R549]]'s shape resolved rather than patched. Ground truth for the instance: `CNIOAtomics` is a C target with **zero Swift files**, and swift-nio published 15 keys / 33 occurrences / 29 rows under it. **THE REMOVAL AUDIT THIS ROW DEMANDED, DONE IN FULL AND NOT SAMPLED** — because closing it removes keys and a removed key can make a row vanish: 11 packages, 11,680 rows, **ADDED 0, REMOVED 0, CHANGED 29, `inferred` CHANGED 0**; `dispatchesOn` was the ONLY field that moved on any row; **no row lost its last key and no row left `functions[]`**; 15 distinct keys lost against 15 gained, each lost key having an exactly-matching replacement at the identical occurrence count. Reach 15 hits, all swift-nio; the ambiguous-leaf branch fired 0 times. A chained A/B is byte-identical **but its reach is ZERO, so it is safety-only and is NOT evidence a consumer join was exercised** — stated rather than left to be read as coverage. **BOUNDARY SWEPT, NOT ASSUMED:** only local PROTOCOLS slip through (local classes/structs/enums are already caught by `localTypes`), and across all 11 packages there are zero non-protocol local types under a foreign key. The NON-GENERIC local-protocol receiver never reaches this site at all — `protoParams` resolves it — **which is why [[R532]] and [[R550]] both missed it.** **AND ONE THING IMPLEMENTED, MEASURED, THEN BACKED OUT:** dropping the `foreignOwnerModule` trigger publishes 18 further sites and 10 new rows, but **4 of those keys name a member the protocol does not have** (`_CollectionsTestSupport#_SortedCollection.distance`, where `_SortedCollection` is an empty marker protocol) — that is [[R532b]]'s open class, so it was left out and recorded as needing a `protoOrSuperDeclares` gate rather than shipped as a bonus.  Pre-existing in both directions (the un-fixed key was equally unanswerable), so this is not a regression of R550's fix. The honest key is `<pkgName>#AtomicPrimitive.*`, matching what `abstractionOwnerPkg`'s "LOCAL WINS" already does for obligation 2 — two sites answering one ownership question and disagreeing, which is [[R549]]'s shape one level over. **Its boundary is WIDER than R550's: the same wrong-owner spelling affects the NON-GENERIC local-protocol receiver too.** |
| R556 rust: **`let h: &dyn dep::Trait = &LocalImpl; h.m()` BYPASSES THE IMPORTED-TRAIT LOCAL-IMPL ARM AND THE GATE FLIPS — because the `dyn` index is built from SIGNATURE PARAMETERS ONLY.** `dyn_sig_trait_leaves` (`lang.rs:83-93`) iterates `sig.inputs` and nothing else — its own doc says *"a signature's PARAMETERS"* — so the arm at `collector.rs:3676-3700`, gated on `dyn_sig_traits`, never fires for a `let`- or field-position `dyn`. Measured: `deny Fs via_indexed` and `pure via_indexed` both **1→0** on the foreign twin, while the SAME run publishes `dep#Handlers::roll` carrying `Fs`. | 2026-09-23 | rust (ts has the same class on a different spelling; java + swift clean) | **THE THIRD OCCURRENCE OF ONE ROOT CAUSE IN ONE DAY, and that is the finding rather than the row: an abstraction index built from a FUNCTION SIGNATURE, blind to every other spelling of the same bound.** [[R549]]/[[R551]] — rust's `sig_trait_quals`, blind to `impl<T: Buf>`. [[R550]] — swift's `FnInfo.genericBounds`, blind to a type-level bound. This — rust's `dyn_sig_trait_leaves`, blind to a `let`-position `dyn`. Three engines' worth of the same mistake, each found by a different route, none of which would have generalised alone. Split out of [[R524]], whose "family-level ruling" dissolved into two port gaps once the shapes were separated. | **CLOSED 2026-09-23 — candor-rust `e82e00f`, and the arm is what announced it.** Fixed by a NEW ADDITIVE index `dyn_local_traits`: written at exactly ONE site (the annotated-`let` arm of `visit_local`) through the same `collect_dyn_trait_leaves` the signature uses, and read by exactly ONE predicate (the R4 erasure carve-out) — not a widening of `dyn_sig_traits`, which every other resolver sees. **The diagnosis was confirmed by BREAKING it rather than by reading the source: adding an entirely UNUSED `_x: &dyn Handlers` parameter to the failing function flips it `inferred:[]` → `['Fs']` with the body otherwise unchanged — which names `dyn_sig_traits` and nothing else.** Coordinator-verified independently on a fresh fixture: `via_let` → `['Net']`, matching its `via_sig` control. Gate: `deny Fs` and `pure` both 0 → 1. A/B in the OVER-CHARGE direction: **1,598 crates / 321,606 rows → ADDED 0, REMOVED 0, CHANGED 0**; the 12-crate chained set and a 6-package control likewise 0. **The lane wrote down that the registry A/B is close to SAFETY-ONLY and why, which is itself the finding: a source census of those 1,598 crates found 150 carrying a `let`-position `dyn`, 102 also declaring a local impl, but only 18 naming a DEPENDENCY-declared trait — and 17 of the 18 are `std`, excluded by the provenance carve-out. A registry of self-contained libraries is not this defect's population; a CONSUMER of a library is.** Calibrated both directions in the commit: predicate reverted to signature-only ⇒ RED; index widened to include monomorphized bounds ⇒ the erasure carve-out goes RED. §1b. XFAIL retired in candor-spec in the same wave. **ORIGINALLY PINNED as PART 92 arm `c11_local_impl_via_binding` (candor-spec, this commit) with rust ALONE in the arm-keyed XFAIL table.** Independently reproduced rather than ported from the review's words, and the result is stronger than a two-engine divergence: **java, swift AND ts all carry `eff=['Net']` on the identical fixture; rust reads `eff=∅` — the judge's words, *"SILENT on Net — neither the effect nor a disclosed Unknown"*.** Three engines as the control. The arm's own control is `dispatch`, where the SAME implementor reached through a SIGNATURE PARAMETER resolves in all four — one variable, how the receiver is bound. `disclosed` rather than `has`, because ⟨0.35⟩ licenses completing OR disclosing and demanding the effect would score a licensed choice as a failure. Calibrated in the same commit per §1b, both directions: the arm FIRED for rust before the line was declared, and a fake xfail injected on java produced `XFAIL ARM PASSED … expectation is STALE: R556` and redded the part (removed by string match, never `git checkout` — TRAP 7). Full four-way suite green afterwards: **0 FAIL cells, 0 passing xfails, 0 left files.** **This arm replaces a control that could not fail** — PART 4s's `fn_ok` passed both spellings by construction, which is why the defect survived. **ts is CLEAN on this spelling and is a control here; its own R524 half needs an INDEX-SIGNATURE dep, a different dep shape, and was deliberately NOT approximated into this arm.** The engine arm already exists and is correct; only its GATE is too narrow. Fixture: `fable-r524/rust-{local,foreign}` against the `-sig` controls, where writing the identical receiver as a signature parameter charges `Fs` on both arms. **The fix widens an index, which is the direction that ADDS resolution — so it needs an A/B for over-charge (`bin/corpus-ab.py`, REMOVED must be 0 and every ADDED traced to a body), not for loss.** Do NOT widen it to a blanket hedge: that is [[R524]]'s confirmed prohibition and would red PART 92 c5/c10. |
| R558 ts: **AN INTERFACE MEMBER PASSED AS A FIRST-CLASS VALUE EVAPORATES — the function is ABSENT from `functions[]` and `pure` EXITS 0 over a body that writes to disk.** `[n].map(d.roll)` where `d: Handlers` → no row at all; `d.roll(n)`, the SAME member CALLED, resolves `['Fs']`. Local AND foreign, index-signature AND declared-member, so it is WIDER than [[R524]] and a different mechanism. | 2026-09-23 | ts — **and it is the ts spelling of a class already fixed in rust ([[R549]] mechanism B)** | **Three controls in ONE file, all resolving, which is what makes this a purity claim and not a gap: `refPlain` (a plain function reference) `['Fs']`; `refClass` (a CLASS member reference) `['Fs']`; `callDeclared` (the same interface member, CALLED rather than referenced) `['Fs']`. Only the interface-member-as-VALUE spellings vanish — `refDeclared` and `refIndexed` are ABSENT from `functions[]`, which SPEC §2 rule 3 makes an affirmative purity claim.** Coordinator-verified first-hand at HEAD, not taken from the lane's words: same fixture, `analyzed: 11` so not hollow, both "pure" functions really write. **`pure refDeclared` and `pure refIndexed` exit 0 while `pure refClass`/`refPlain` exit 1.** | **CLOSED 2026-09-23 — candor-ts `d534b62` — AND THIS ROW'S SCOPE IS WRONG IN ONE CELL OF FOUR, which the lane measured rather than inherited.** Crossing declaration site against spelling: LOCAL declared → ABSENT; LOCAL index-sig → ABSENT; FOREIGN index-sig → `[]`; **FOREIGN DECLARED → `['Fs']`, ALREADY RESOLVED** — `resolveFnRefUnit` chases the local structural implementor before the HOF-ref guards are reached. **That cell was moved to a CONTROL; asserting it as a defect row would have been a check that cannot fail (attack A).** Fix follows rust's `186e854` template — a NEW ADDITIVE predicate `chargeMemberRefDispatch`, NOT a widening of the HOF-ref opacity index (which answers *is this holder caller-controlled*; widening it would hand every resolved dep function the opaque-callback `Unknown`). `pure refDeclared` and `pure refIndexed` both 0 → 1. **A DEFECT THE A/B FOUND AND NO FIXTURE WOULD HAVE: the first cut mapped every `<…>` pseudo-module onto the scanned package's name, so `xs.some(Array.isArray)` published `<pkg>#ArrayConstructor.isArray` — 530 typeorm rows plus 2 nest rows, and nothing else in the entire diff. That is rust's [[R549]] malformed-key class reproduced in a SECOND ENGINE BY A FIX.** Cause: `declIsNodeTypes` tests `@types/node` and not the TypeScript ES lib — the caller-side `!mod.startsWith("<")` guard had always kept the lib out, and a helper deriving its own key had to restate it. Pinned by a control. **A/B over 25 real TS packages / 7,429 rows: 0/0/0 on the wide key — but R558's REACH IS 0, so that half is SAFETY-ONLY and is recorded as such, not as a measured cost of zero**; a recall hunt across all 25 finds no interface-member-as-reference outside the platform type surface. **ORIGINALLY OPEN — and the cross-engine link is the point: rust had this EXACT class today as [[R549]] mechanism B (`xs.front().map(Buf::chunk)`), fixed in `186e854` by recording a member named as a function reference as a dispatch.** The two engines differ in severity: rust LOST THE KEY while keeping the row; **ts loses THE WHOLE ROW.** That makes ts's the worse of the two and the rust fix a ready template. **JAVA IS SWEPT AND CLEAN, 2026-09-23, AND NOW PINNED** — `refDeclared` (`d::roll`), `refLambda` (`L::roll`) and `refUnbound` all read `['Fs']`, with a plain function ref, a class member ref and the same member CALLED as controls, and `pure` exiting 1 on all three. **Calibrated so the greens discriminate: with both implementors emptied the charge drops and the same policy exits 0 — absence is reachable.** Now a `test/smoke.sh` block, 547 → 559 passed. Clean because `invokedynamic`/`LambdaMetafactory` names the target in the constant pool — **which is also why a future indy change could silently un-clean it, with absence as the only symptom, and is the argument for the pin.** **SWIFT REMAINS UNSWEPT** (a protocol member as a value). **TWO VACUOUS-GUARD DEFECTS IN THAT SWEEP'S OWN CALIBRATION, both caught by gates and both worth carrying: (1) the pure twin was written to `MainPure.java` while the class stayed `public class Main`, so javac compiled NOTHING behind a `2>/dev/null` and all three `absent` rows passed over an EMPTY report — now also pinned with `analyzed.count > 0`; (2) the rows grepped the whole row object and matched `declared: ["Fs"]`, the ⟨0.24⟩ CONTRIBUTES field that stays populated when `inferred` empties — every row now reads `inferred` and nothing else.** — the question translates directly (a method reference / a protocol member as a value), and "filed against one engine" has been a poor guide to scope all day: three of today's rows turned out to be one root cause across two engines. Sits on the desugared arm the R524 fix deliberately did not touch, and is named in a comment there. |
| R560 ts: **A κ WHOLE-MODULE RULE FIRES ON A CALL WHOSE ONLY LINK TO THE PACKAGE IS ITS TYPE — dropping the real effect AND fabricating a different one. Both directions at once.** `ev.save(n)` where `ev` is typed by socket.io's `DefaultEventsMap` but the implementor is LOCAL and writes a file: `inferred:['Net']`. **`deny Fs` exits 0 while the body writes; `deny Net` exits 1 while nothing dials.** | 2026-09-23 | ts | **Held constant and measured: same file, same literal shape, same call spelling, same implementor body — the ONLY variable is whether the interface comes from a κ-classified package.** The local-interface twin `viaLocal` reads `inferred:['Fs']` and `deny Fs` exits 1, correctly. Also observed on the same probe: `pure emitNoop` exits 1 over an EMPTY body — the fabrication reaching a function that does nothing at all. | **THE UNDER-REPORT HALF IS CLOSED 2026-09-23 — candor-ts `d534b62`. The κ FABRICATION HALF IS DELIBERATELY NOT FIXED and is now PINNED BY A CONTROL so a later "fix" cannot quietly trade the disclosure away.** Mechanism, measured: `ev.save(n)` resolves to socket.io's INDEX SIGNATURE, which has no `.name`, so κ's member token is `""` and the whole-module rule (member regex `null`) matches unconditionally → `eff = "Net"`; **that truthy `eff` then SUPPRESSED the ⟨0.39⟩ obligation-3 join sitting behind `if (!eff && !mod.startsWith("<"))` at `scan.mjs:8306`.** The engine already held the answer — the same report published `TypedEventBroadcaster.emit -> ['Fs']` while the caller row read `['Net']`. Fix: drop the `!eff`; purely additive, since `joinLocalImpls` only edges to minted units or hedges `Unknown`. **§9 — THE BOUNDARY WAS NOT DRAWN AROUND THIS ROW'S TRIGGER, AND IT PAID: this row is filed on the INDEX-SIGNATURE spelling, but a `viaDecl` arm built on socket.io's real DECLARED member `TypedEventBroadcaster.emit` loses the same effect the same way.** Both now flip: `deny Fs src.main.viaIdx` and `deny Fs src.main.viaDecl` each 0 → 1. **⚠ CORRECTION TO THIS ROW AS I FILED IT: the `deny Fs` / `deny Net` exit pair only separates under a SCOPED rule. A blanket `deny Fs` exits 1 in BOTH arms, because the structural implementor is independently reported** — brief §F1's *"caught by blanket `deny` only incidentally"*, and I wrote the unscoped pair into this row as if it discriminated. **The κ `Net` still stands and `pure emitNoop` still exits 1 over an empty body. Withdrawing it needs a RULING, not a patch:** it means deciding *does THIS receiver hold a value the package produced*, which is unprovable here (F1 #5 — narrowing a sound over-approximation) and is non-additive across every consumer of every κ-classified package. **ORIGINALLY OPEN — rarer than a silent under-report and worse to debug, because BOTH gates lie in OPPOSITE directions at once: a user denying `Fs` is told they are clean over a file write, and a user denying `Net` is told they dial out when nothing does.** The type is the only tie to the package — no code of it is called — so the whole-module classification is being applied to a value's TYPE rather than to a call into the module, which is the same category error as [[R524]]'s false `invisible` (a disclosure naming a package that has no code and is never called). Unchanged by the R524 fix: that join sits behind the same `!eff` guard the existing `recordDispatch` call does, so it never runs when κ has already charged. **The `!eff` guard's interaction with this is the thing to measure first.** |
| R557 rust: **A `static` OR `const` ITEM USED AS A METHOD RECEIVER RESOLVES NOTHING AT ALL — the caller is ABSENT from `functions[]` and `pure` exits 0. NOT `dyn`-specific, and not about abstractions: it is about where the receiver VALUE is declared.** `static C1: Client = …; C1.fetch()` → ABSENT; `let c = Client{..}; c.fetch()` → `['Fs']`. Also `const`, `OnceLock::get_or_init(..).fetch()`, and `let s = &C1; s.fetch()`. | 2026-09-23 | rust | **One variable — where the receiver value is declared — with a `let` control that resolves. All fixtures `cargo build` exit 0, which matters here because of the near-miss below.** **REPORTED NEAR-MISS (§J/§E3): the lane's FIRST fixtures for this used `static S: &dyn Handlers = &H;`, which DOES NOT COMPILE (`Sync`), and it nearly filed an absence finding over an unbuildable program — the same failure §E3 records against swift. Both findings were re-derived on programs that build.** Pre-existing: reproduced on the PRE binary, so not a regression of [[R556]]'s fix. | **CLOSED 2026-09-23 — candor-rust `36a917d` — AND TWO OF THE FOUR ARMS THIS ROW LISTED ARE NOT STATIC-SPECIFIC, measured rather than assumed, so they are split out as [[R568]] and [[R569]].** A `static`/`const` is an ITEM, so it passes through no binding site at all; `C1.fetch()` fell to the unit-struct-literal fallback and formed an edge to a type no crate declares. Fixed by a new `static_types` index (NAME → declared type), read in exactly ONE place, gated on `locally_bound`, with **ambiguity REFUSED rather than resolved** in both the collect and the merge. **The two arms that are NOT this row: `C3.get_or_init(..).fetch()` over `OnceLock<Client>` and `let s = &C1; s.fetch()` — their `let` twins are equally silent on one fixture, so they were never about `static` at all.** Real instances traced to bodies from source: find-msvc-tools' `const LOCAL_MACHINE: RegistryKey` → `RegOpenKeyExW`; jiff's `static DEFAULT_DATETIME_PARSER`. **OPEN no longer, and it was the widest of today's rust rows because it is not about dispatch at all.** A `static`/`const` receiver is an ordinary idiom (a module-level client, a lazily-initialised handle), and the effect is simply dropped: `deny Fs via_static` exits 0 over a body that writes. It is NOT the [[R549]]/[[R551]]/[[R556]] index-blindness family — those concern which ABSTRACTION a receiver names; this one loses the receiver itself. Repro: `scratchpad/rustagent-r557/gap4/`, `gap5/`. |
| R561 rust: **A CLOSURE PARAMETER ANNOTATED `&dyn T` RESOLVES NO TRAIT — the enclosing function is ABSENT and `deny Fs` exits 0.** `\|h: &dyn Handlers\| h.roll()` → ABSENT, while `fn f(h: &dyn Handlers) { h.roll() }` and `let h: &dyn Handlers = …; h.roll()` both read `['Fs']`. | 2026-09-23 | rust | **Three controls, one variable, fixture compiles (`cargo build` exit 0): a concrete-typed closure param resolves; the SIGNATURE spelling resolves; the `let` spelling resolves since [[R556]]. Only the CLOSURE parameter does not.** | **OPEN.** **⚠ CORRECTED 2026-09-24 — the "LOCAL vs FOREIGN" framing below is WRONG and was mine. The variable is the trait's QUALIFICATION SPELLING at the declaration site, not where it is declared: with `use dep::Q;` present these arms RESOLVE, and written inline as `dep::Q` in a scope with no `use` they are ABSENT. My local fixture happened to use a bare leaf. See [[R577]]; PART 93's three XFAILs are retired and replaced by one that varies the real thing.** **⚠ REOPENED 2026-09-23 BY PART 93, WHICH FOUND IT WITHIN MINUTES OF EXISTING: CLOSED FOR A **LOCAL** ABSTRACTION, OPEN FOR A **FOREIGN** ONE.** One fixture pair differing ONLY in where the trait is declared, same six shapes, same binary: with the trait LOCAL, `via_param_field`, `via_return` and `via_closure` all read `[Net]`; with the trait FOREIGN — a chained dependency's, i.e. the case ⟨0.39⟩ and cross-package scanning exist for — **all three read ABSENT, a ⟨0.21⟩ purity claim**, while the signature-parameter control resolves in both. The `CANDOR_ALIAS_DEBUG` probe shows the fix's own code path taking **ZERO hits** on the foreign arm, so it is not firing rather than firing and deciding. **A LOCAL-ONLY FIXTURE WOULD HAVE REPORTED THIS CLASS FIXED, which is exactly why PART 93's fixture uses a FOREIGN dep** — and is the general lesson: a regression test written beside the fix inherits the fix's own blind spot (§A.2). Also measured and separate: `via_self_field` (a field reached through `self` inside an `impl`) is ABSENT in BOTH arms. **The LOCAL half is CLOSED — candor-rust `2078e08`/`36a917d`.** `visit_expr_closure` handled callable types and `type_path` and **never asked `trait_leaves` at all** — §F1.3 at the EIGHTH binder. Fixed at that one site with the same `trait_leaves` + `collect_dyn_trait_leaves` pair every other declaration site uses, scoped save/restore. Live instance: nom's `move \|i: Input\| i.slice_index(c)`, now disclosing rather than CHA-ing. **A self-reported VACUOUS calibration is recorded at the fixture: the first scope carve-out could not fail, because `vars` wins at the call site so a leaked `trait_vars` entry is invisible to a concrete control.** **ORIGINALLY OPEN — the FOURTH spelling in the [[R556]] family, which is the point: `dyn_sig_traits` covers signature parameters, `dyn_local_traits` (new) covers annotated `let`s, and a closure's own parameter list is a third declaration site nothing indexes.** The family is *an erasure fact recorded per declaration site, with one index per site and no shared question*. Cheapest next member of the class: the same additive-index treatment at the closure-parameter site. |
| R562 rust: **[[R556]]'s FIELD and RETURN positions are still erased — measured, NOT asserted safe, and deliberately left open by its fix.** `self.inner.roll()` where `inner: Box<dyn T>`, and `mk().roll()` where `mk()` returns `Box<dyn T>`, both read `eff=∅` at HEAD while the `let`, `let`-Box and annotated-collection-element spellings now resolve. | 2026-09-23 | rust | **Found by the §9 sweep that [[R556]]'s fix required rather than by meeting the bug: SEVEN spellings measured against a LOCAL-trait control that resolves all seven, where the foreign arm resolved only the signature parameter.** Three were closed by `e82e00f`; these two were not, and the lane stated them as open rather than reporting the row closed. | **OPEN.** **⚠ CORRECTED 2026-09-24 — the "LOCAL vs FOREIGN" framing below is WRONG and was mine. The variable is the trait's QUALIFICATION SPELLING at the declaration site, not where it is declared: with `use dep::Q;` present these arms RESOLVE, and written inline as `dep::Q` in a scope with no `use` they are ABSENT. My local fixture happened to use a bare leaf. See [[R577]]; PART 93's three XFAILs are retired and replaced by one that varies the real thing.** **⚠ REOPENED 2026-09-23 BY PART 93, WHICH FOUND IT WITHIN MINUTES OF EXISTING: CLOSED FOR A **LOCAL** ABSTRACTION, OPEN FOR A **FOREIGN** ONE.** One fixture pair differing ONLY in where the trait is declared, same six shapes, same binary: with the trait LOCAL, `via_param_field`, `via_return` and `via_closure` all read `[Net]`; with the trait FOREIGN — a chained dependency's, i.e. the case ⟨0.39⟩ and cross-package scanning exist for — **all three read ABSENT, a ⟨0.21⟩ purity claim**, while the signature-parameter control resolves in both. The `CANDOR_ALIAS_DEBUG` probe shows the fix's own code path taking **ZERO hits** on the foreign arm, so it is not firing rather than firing and deciding. **A LOCAL-ONLY FIXTURE WOULD HAVE REPORTED THIS CLASS FIXED, which is exactly why PART 93's fixture uses a FOREIGN dep** — and is the general lesson: a regression test written beside the fix inherits the fix's own blind spot (§A.2). Also measured and separate: `via_self_field` (a field reached through `self` inside an `impl`) is ABSENT in BOTH arms. **The LOCAL half is CLOSED — candor-rust `2078e08` — AND THE BLOCKER THIS ROW RECORDED DOES NOT APPLY. I wrote that blocker; it was wrong, and the refutation is a fixture rather than an argument.** The hazard is real for a CRATE-WIDE LEAF-KEYED UNION, but the erasure can be asked **of the receiver EXPRESSION**: `visit_expr_method_call` has `node.receiver` in hand, the field arm keys on (base type leaf, field name) and the return arm on the callee leaf — **the same keys the resolution already used.** The separating fixture carries a `dyn Handler` FIELD, a `dyn Handler` RETURN, **and** `T: Handler` / `impl Handler` / `Gen<T: Handler>` receivers **in one crate** — the only arrangement that distinguishes a per-receiver rule from a union — and the monomorphized three stay `eff=∅`, i.e. no fabrication. **THE LIVE INSTANCE IS WORTH THE ROW ON ITS OWN: aws-smithy-http-client's `struct Connector { adapter: Box<dyn HttpConnector> }` with `fn call(&self, r) { self.adapter.call(r) }` — a dispatch entry point of the AWS SDK's HTTP client — reported `inferred: []`.** Verified present in the registry at `aws-smithy-http-client-1.4.0/src/client.rs:91`. A/B over 1,598 registry crates: ADDED 0, REMOVED 0, 7 wide / 6 `inferred` changed. **ORIGINALLY OPEN, with a reason that did not survive measurement: field and return erasure live in CRATE-WIDE indexes (`trait_fields`, `rets`), so a leaf-keyed union there would let ONE struct field's `dyn Serializer` license CHA on EVERY `T: Serializer` receiver in the crate — R4's fabrication reached by a third door.** Closing them needs the erasure fact in the SAME KEY SPACE as the resolution, which is a larger change than the one-site additive index that closed the `let` position. |
| R559 swift: **THE PACKAGE'S OWN NAME WAS READ BY A REGEX THAT TOOK THE FIRST `name: "…"` ANYWHERE IN `Package.swift` — SO 2 OF 11 REAL PACKAGES REPORTED A NAME THAT IS NOT THEIRS, AND THE §2 COVERAGE PAIRING INVERTED.** swift-nio called itself `Atomics`; swift-collections called itself `_CollectionsTestSupport`. The package name is §2 rule 3's COVERAGE KEY. | 2026-09-23 | swift | **THE LIMITATION WAS DOCUMENTED IN TWO PLACES — the function's own doc AND `PackageTargets.swift`'s header — AND MEASURED IN NEITHER. That is [[feedback-documented-limitation-is-not-measured]] in its exact form: a comment saying "this is approximate" reads as CONSIDERED, which is what stops anyone measuring it.** Found only because the [[R555]] sweep needed real package identities. **Not cosmetic: on a three-package fixture with swift-nio's exact manifest shape, chaining INVERTED the disclosure pairing — `coverage.uncovered` went `["Dep","Ghost"]` → `["Dep"]`, withdrawing the disclosure for the package the report does NOT cover and keeping it for the one it does.** | **FIXED 2026-09-23 — candor-swift `52c1668`**, by asking the SwiftSyntax parser already in the tree (`CandorCore.parsePackageName`) instead of a regex. A/B on the unit key: **ADDED 7,734 / REMOVED 7,734, all 7,734 audited, residual ZERO**, confined to the two misread packages with the other nine byte-identical. **Two process notes worth more than the fix: (1) the existing `WorkspaceCacheProcessTests` sweep/writer row went RED and WAS RIGHT TO — it was mirrored rather than relaxed, and now also guards against regressing to the unanchored parse; (2) the first audit pass reported 306 mismatches that were ENTIRELY AN ARTEFACT OF THE AUDIT (`dispatchesOn` is sorted and the new prefix sorts differently) — recorded because a residual read as real is how a rename hides a genuine removal.** |
| R563 swift: **A GENERIC PARAMETER USED AS A *TYPE* RECEIVER OVER A LOCAL PROTOCOL LOSES THE EFFECT ENTIRELY — FIVE SPELLINGS SILENTLY ABSENT, `deny Net` EXIT 0, OVER A CONFORMER THAT REACHES `URLSession.dataTask` TWO LINES AWAY.** `P.make(v)`, `P(sink: v)`, `P.maker(v)`, a metatype parameter's `t.make(v)`, and the same inside `struct Box<P: Prim>` — all ABSENT from `functions[]`, a ⟨0.21⟩ positive purity claim. | 2026-09-23 | swift | **One package, one file, one protocol, one conformer, NO chaining — the only variable is the receiver spelling, and two spellings resolve correctly as controls: `instFnLevel<P: Prim>(_ p: P)` → `p.inst(v)` reads `[Net]` and `deny Net` exits 1, as does the existential `existentialInst(_ p: Prim)`.** Found by widening [[R555]]'s boundary rather than by meeting the bug. Mechanism: `DeclCollector` resolves a PARAMETER's bound into `protoParams`, so an INSTANCE receiver reaches the local-protocol CHA; when the type parameter is itself the receiver, `base.root` is `P`, which is in neither `localTypes` nor `localProtocols`, and every branch misses — §F1.3 again. Pre-existing; unchanged by either swift commit today. | **CLOSED 2026-09-23 — candor-swift `6d7fd06`, all FIVE spellings and all THREE syntactic paths in one commit**, because fixing the spelling in hand would have drawn the boundary around its own trigger. `protoBoundParams` (a function bound ∪ [[R550]]'s `typeGenericBoundsAll`) routes a type-parameter receiver into the SAME `ProtoDispatch` machinery a protocol-typed value already uses; `metatypeParams` handles `t: P.Type` in its own map rather than widening `typeName`; an `init` requirement is registered; a function-typed requirement hedges `Unknown` BESIDE the dispatch. After: `staticFnLevel`/`Box.staticTypeLevel`/`metatypeParam` `[Net]` with `deny Net` exit 1; `ctorFnLevel`/`staticVarFnLevel` `[Unknown]` with `deny Net Unknown` exit 1. **A/B over 11 packages / 16,428 analyzed functions: `inferred` ADDED 3, REMOVED 0, CHANGED 25 — every movement a GAIN of `Unknown`, zero effect losses, zero `dispatchesOn` losses — REACH 46 hits across 4 of 11.** Real instances ground-truthed from source: swift-nio's `Pool<Element: PoolElement>.get() { return Element() }` was ABSENT from `functions[]`; swift-argument-parser's `RawRepresentable where Self: ExpressibleByArgument` calling `RawValue(argument:)` likewise. **AND THE FIRST CUT REGRESSED THE ⟨0.39⟩ WIRE, CAUGHT BY THE A/B AND NOT BY REVIEW: emitting the hedge INSTEAD OF the dispatch removed 15 keys / 33 occurrences of `swift-nio#{NIO,}AtomicPrimitive.*` — the exact keys [[R555]] had fixed hours earlier.** The shipped form emits both and a test pins it. §1b: four degradations, each red in its own place (17/4/2/1 failures). `NameKeyedStateTests` went red for the two new indexes and was right to.  **ORIGINALLY FILED OPEN, and deliberately NOT shipped with the two fixes it was found beside — the reasons are the useful part.** It needs a NEW `CallCollector` dispatch branch fed by an index that collector does not currently receive; it **WIDENS** a protocol CHA, which is the OVER-CHARGE direction and so needs its own A/B and removal audit rather than riding on one already done; and it spans THREE syntactic paths (static member, initialiser, static var), so a fix for the spelling in hand would draw the boundary around its own trigger. Fixture that compiles AND runs: `scratchpad/swiftagent-r559/fx2`; write-up at `r559b-repro.md`. |
| R564 rust: **A PURITY CLAIM DECIDED BY THE SPELLING OF A PARAMETER NAME — an Upper-initial binding read as a unit-struct literal.** `fn p_upper(Z: &dyn Q) { Z.fetch() }` → ABSENT from `functions[]`, `deny Net` exit 0; `fn p_upper_us(Z_A: &dyn Q)` → `['Net']`, exit 1; `fn p_lower(z: &dyn Q)` → `['Net']`, exit 1. **The underscore arm passes only because the fallback happens to exclude an underscore.** | 2026-09-23 | rust | **Found ONLY because [[R557]]'s shadowing carve-out could not be built without hitting it** — not by looking for it. Pre-existing: reproduces on the pre-R557 binary. Three arms, one variable, and the variable is a NAME: nothing about the type, the trait, the call or the body differs. | **FIXED 2026-09-23 — candor-rust `36a917d`, calibrated with NINE falsifications, each applied and reverted BY STRING MATCH (never `git checkout`, TRAP 7) and each producing its named FAILED line.** Coordinator-verified post-fix on an independent fixture: all three spellings now read `['Net']`. Across the whole wave (R557+R561+R564) over 1,598 registry crates / 321,807 rows: **ADDED 201, REMOVED 0, 49 `inferred` changed — and NOT ONE added row carries a concrete effect; all 185 added keys are `inferred: []` or a disclosed `Unknown`, and 48 of the 49 `inferred` moves ADD `Unknown`.** Gate impact 124 rows (0.04%), every one silence→disclosure. **THE SINGLE `Unknown` WITHDRAWAL IS A FABRICATION RETIRED, ground-truthed: termwiz declares `pub const CSI: &str` AND `pub enum CSI` with `impl Display`, and the fallback typed the `&str` as the enum, so `write!(buf, "{}", CSI)` charged the enum's `fmt`.** |
| R566 ts: **A SECOND IMPLEMENTOR OF AN INDEX-SIGNATURE INTERFACE TAKES A SCOPED `deny` FROM EXIT 1 TO EXIT 0 — so [[R524]]'s resolution effectively only lands with a SINGLE implementor.** One implementor → `viaA` `['Fs']`, `deny Fs src.index.viaA` exit 1. Two implementors, where the second supplies a DIFFERENT key → `viaA` `['Unknown']`, same scoped gate exit **0**. | 2026-09-23 | ts | **Pre-existing at `d46c098` — [[R524]]'s own `push`/`allResolved` path, verified in a worktree at that commit, so NOT introduced by the R558/R560 work.** The second implementor supplies `other` rather than `save`, so `allResolved` goes false for `save` and the whole call hedges. One variable, everything else byte-identical. Repro: `scratchpad/jtsagent-r566/idx-one` and `idx-two`. | **OPEN — and explicitly NOT a cardinal sin: it is DISCLOSED, and both `deny Fs Unknown` and `deny Unknown src.index.viaA` exit 1.** It is filed because **a handler-map interface with several implementors supplying different keys is the NORMAL case, not the exception** — so R524's resolution lands only in the single-implementor case, and a bare scoped `deny Fs` relaxes silently as soon as a second handler appears. The fix direction is per-KEY resolution rather than all-or-nothing `allResolved`, which is the [[candor-unanswerable-key]] three-row rule applied to an index signature: absence licenses a purity claim only where the key could have had a body. |
| R568 rust: **A GENERIC CELL IN A CHAIN LOSES THE RECEIVER — `OnceLock<Client>::get_or_init(..).fetch()` reads ABSENT, and its `let` twin does too, so it is NOT [[R557]]'s static case.** | 2026-09-23 | rust | **Split out of [[R557]] by MEASUREMENT: that row listed four arms as static-specific, and this one's `let` twin (`let c: OnceLock<Client> = …; c.get_or_init(..).fetch()`) is equally silent on the same fixture. A row that lists arms it has not crossed against their twins is a row that will send the fix to the wrong place.** | **OPEN.** The chain passes through a generic cell whose element type the resolver does not carry through `get_or_init`'s return — the same *element-yielding accessor* question `is_element_yielding_accessor` already answers for other containers, which is where to look first. |
| R569 rust: **AN UNANNOTATED `let` BOUND TO A REFERENCE LOSES THE RECEIVER — `let s = &C1; s.fetch()` reads ABSENT, and so does `let c = Client{..}; let r = &c; r.fetch()`.** | 2026-09-23 | **CLOSED — candor-rust `cf8782d`. The row's premise verified: the answer already existed and this path simply did not ask it.** rust | **Also split out of [[R557]] by measuring its twin rather than trusting the arm list.** The fallback on this path is `ctor_type`, **not** `resolve_recv_type` — two routes answering *what type is this binding* and only one of them walking a reference, which is [[R347]]'s shape at another site. | **OPEN.** Cheapest member of the family to close next: the answer already exists in `resolve_recv_type`; this path simply does not ask it. **CLOSED 2026-09-25, candor-rust `cf8782d`, and this row's premise was VERIFIED before it was acted on.** `resolve_recv_type_for` has had an `Expr::Reference` arm since long before the row (`collector.rs:895`); the unannotated-`let` site asked only `ctor_type`, which types a CONSTRUCTION. The fix asks the existing function — the answer already existed and this path simply did not ask it. **A/B: ADDED 91, REMOVED 0, CHANGED 406 (narrow 33), rows +91, REACH 3,829 hits / 490 entries, and ZERO effects lost on any row anywhere.** **Every concrete gain traced to source:** cc `KillOnDrop::drop` **Exec** — a `Drop` body that kills a child process and read PURE — sqlx-core `{Read,Write}::poll` **Net**, postgres `Client::is_valid` **Db**, jobserver `acquire*` **Fs**, h2/tokio **Log**, mongodb **Clock/Env/Rand**, zip **Clock**. **Over-charge controls executed rather than asserted:** a `&*q` to a pure same-named twin charges nothing, and `&String::new()` types to nothing. R557's now-false "still silent" comment was corrected in the same commit. |
| R570 rust: **A FULLY-QUALIFIED TRAIT CALL PASSING `self` LOSES THE DISPATCH — `T::method(self)` reads ABSENT while `self.method()` and a parameter receiver both resolve.** The SEVENTH spelling in the receiver family, and it is live in a real crate: `clap_builder`'s `OsStringValueParser::parse_ref` calls `TypedValueParser::parse(self, cmd, arg, value.to_owned())` and is ABSENT from the report, **while its 14 sibling `parse_ref` rows that dispatch ordinarily all read `inferred:['Unknown'] unresolved:true`**. | 2026-09-23 | rust | **Found by taking [[R543]]'s corpus finding and asking WHY rather than re-filing it — and the answer put it in today's vein.** Reproduced on a minimal fixture that `cargo build`s, three arms one variable: `via_param(v: &dyn V) { v.go() }` → `['Net']`; `via_self_default` (a `self.go()` inside a trait DEFAULT method) → `['Net']`; **`via_ufcs` (`T2::parse(self)` inside a default method) → ABSENT.** Ground-truthed from source: `clap_builder-4.6.7/src/builder/value_parser.rs`, and the scan is not hollow (`analyzed=1217`). **NOTE THE NEAR-MISS: the `clap` FACADE crate scans to `analyzed=1`, so reading ABSENT off it would have proved nothing — R543 names `clap_builder` and that distinction is load-bearing (R242).** | **OPEN.** Same family as [[R556]]/[[R561]]/[[R562]]/[[R557]]/[[R564]] — an abstraction index consulted at a site whose receiver spelling it does not cover — but a different SITE: the receiver arrives as the first ARGUMENT of a path call rather than as a receiver expression at all, so `visit_expr_method_call` never sees it. **NOT added to PART 93: that part's six arms are portable four-way, and UFCS has no natural java/ts analogue; forcing it in would need a `skip` mechanism the generator does not have, and a rust-only arm in a four-way part is the shape that reads as coverage.** **ADDENDUM 2026-09-25, from the [[R598]] work — this row has a CONSEQUENCE it does not record.** Confirmed at HEAD on a minimal fixture: `pub fn caller(s: &S) -> u8 { Dsl::limit(s) }` is ABSENT and `pure caller` exits **0** over a body reaching a `warn!`. **The consequence: R570 is what makes [[R598]]'s fabrication ACCIDENTALLY LOAD-BEARING.** Because the delegating default body publishes nothing, the union's wrong guess was the ONLY thing carrying the effect on 9 corpus keys (snapbox, diesel) — which is why R598's first fix, which simply dropped the bad candidate, traded a fabrication for the cardinal sin on those 9. **So R570 must close BEFORE [[R606]] can be narrowed safely**, and that ordering is now measured rather than argued. **REAL-CODE INSTANCE CLOSED 2026-09-25 WHILE THIS ROW REMAINED OPEN — worth a bisect before anyone claims it.** The `corpus` oracle's clap finding, `builder::value_parser::OsStringValueParser::parse_ref`, is GONE after the night's wave. [[R543]] records that instance as THIS row's UFCS shape (`TypedValueParser::parse(self, …)`), so either the attribution in R543 was wrong, or something in the wave ([[R569]], [[R598]], [[R609]] are the rust candidates) resolves that receiver by another route. **The row stays OPEN: a disappeared corpus finding is not a closed row, and the minimal fixture in this row's own evidence must be re-checked at HEAD before anything is claimed.** **ATTRIBUTION CORRECTED 2026-09-25 by bisect — this row did NOT own the clap corpus finding.** [[R543]] recorded `OsStringValueParser::parse_ref` as this row's UFCS shape (`TypedValueParser::parse(self, …)`). Bisecting the finding's disappearance over `clap_builder-4.6.7` lands on **[[R576]](b)** (`05f3325`): ABSENT at `845cb32`, PRESENT immediately after, and `TypedValueParser` has **17 implementors** against the bounded-CHA limit of 12, with `R576WIDE` firing twice on the crate. So the real mechanism was a WIDE FAN-OUT falling silent, not a fully-qualified trait call. **This row remains OPEN on its own fixture** — `Dsl::limit(s)` still reads ABSENT with `pure caller` exiting 0 — but it is no longer credited with clap, and [[R606]]'s dependency on it is unaffected. **RE-CONFIRMED AT HEAD 2026-09-26 by [[R689]]/[[R690]]'s matrix, and the boundary widened: `Tr::m(r)` and `let f = Tr::m; f(r)` are ABSENT with a LOCAL trait and PURE-with-a-row once the trait is CHAINED — two spellings of one open class, both exit 0 under `deny Net <q>::` and under `deny Net Unknown <q>::`.** The same matrix shows the charged controls (free fn through a binding, inherent method through a binding, the implementing type's own path) so the silence is specific to resolving through the TRAIT. |
| R571 rust: **THE R556/R561 WAVE INTRODUCED A FABRICATION — a `dyn` binding ANYWHERE IN A BODY licenses CHA on every OTHER receiver in that body, including caller-monomorphized ones the R4 carve-out exists to protect.** `mono_ctl(t: &impl Handler) { t.roll(n) }` reads `[]` — correct, the caller picks the type. Add a `let b: &dyn Handler = &H; b.ping();` (a call to a PURE method) earlier in the body and the SAME `t.roll(n)` reads `['Fs']` with `calls=['H::ping','H::roll']`, and **scoped `deny Fs mono_after_let` exits 1**. | 2026-09-24 | rust — INTRODUCED 2026-09-23 by `e82e00f` (R556) and `36a917d` (R561) | **Found by the code review of that wave, and coordinator-verified independently on the reviewer's own fixture after my first attempt to reproduce it FAILED — my version had the monomorphized receiver calling the PURE method, which resolves concretely and hides it. The fixture `cargo build`s clean; `analyzed=6`.** `dyn_local_traits` is a BODY-WIDE ADDITIVE SET — *this body erased that trait somewhere* — and the read site licenses CHA on any receiver carrying that leaf, so an `impl Trait`/`T: Trait` receiver matches it. `2078e08`'s own commit message argues the opposite (*"No union, no crate-wide set, so a `T: Serializer` parameter receiver matches neither"*), and that is TRUE of its `via_recv` path and FALSE of `via_local`. The class pre-exists on `dyn_sig_traits` (the `mono_unused_sig` arm reads `['Fs']` at both revisions); **R556 and R561 extended it to the `let` and closure-parameter positions.** The registry A/B's `ADDED 0` is honest and simply never contained the shape — the commit's own source census says it is rare in libraries, which is why a consumer-side corpus was the wrong thing to skip. | **FIXED 2026-09-24 — candor-rust `4752644` (re-tagged `736852b`), and the WHOLE class rather than only what we added**, because fixing two of the three body-wide reads would have left a denylist that knows the answer and declines to apply it at one call site — §9's boundary drawn around its own trigger. **The obvious fix was declined for the reason [[R575]] names: the body-wide read is SUBTRACTED by a per-name denylist (`mono_recv_traits`, derived as `trait_leaves \ dyn_trait_leaves_of` so it cannot drift from either existing authority) rather than replaced by a per-receiver route that has fewer arms.** The safety property is structural: **an entry can only REMOVE a receiver from the CHA, so a binding shape the table does not know keeps today's over-charge and NO GAP IN IT CAN PRODUCE A SILENT UNDER-REPORT** — it fails toward the over-report, never toward the sin. R575 is untouched and still open. `dispatchesOn` is deliberately unaffected (obligation 1 is not gated on erasure), so the consumer still learns the dispatch happened; only the fabricated edge and its effect go. Coordinator-verified at HEAD: all four arms now `inferred=[]` with `dispatchesOn` intact. **Narrowing-direction evidence, which is a different property from an additive fix's `REMOVED 0`:** registry 1,626 crates / 327,581 rows → 0/0/0 with **REACH 1**, and that hit is named rather than waved at — tower-0.5.3 `BoxService::new`, `fn new<S: Service>(inner: S)` calling `inner.map_future(…)` inside `let inner: Box<dyn Service<…>>`. **The row did not move because tower declares 39 local `impl Service` types and the bounded fan-out stops at 12, so it is REACHED BUT NOT MOVED — a different claim from a 0/0/0 with no reach, and recorded as such.** A consumer corpus carrying the shape gives ADDED 0 / REMOVED 0 / CHANGED 5, all five traced to source with no sampling: four drop a fabricated edge, the fifth drops the fabricated half and KEEPS the real `Fs` from a shadowing for-loop variable. Six falsifications, each red on its own assertion. **INDEPENDENT CORROBORATION ON REAL CODE, 2026-09-24, from an instrument not designed for it — which is the evidence this row's own A/B could not supply (registry 0/0/0 with REACH 1).** The `coverage-gate-refresh` job compares a fresh self-scan against checked-in manifests; the manifests were regenerated at `b26df9a` (09-23 22:34) and this fix landed at `4752644` (09-24 03:48), so the delta isolates it. Result: **REGRESSED 0, GROWN 0, and ORACLE-DROPPED 2 — `reqwest::async_impl::request::RequestBuilder::send` and `reqwest::blocking::client::Client::execute`, each `classified_as='Net'` with `self_scan_effects` now `'Log,Unknown'`.** **No coverage is lost — `classify()` still supplies the real `Net` rule; what changed is that the ORACLE stopped independently nominating them, because the fabricated concrete effect it used to see is gone and honest `Unknown` is what remains.** The job treats oracle-dropped as non-failing by design (its own selftest pins that), and it exited **success**. So two real reqwest entries were carrying a fabricated `Net` in the self-scan before this fix. **Addendum worth keeping for any future hunt: the `let`-position fabrication is ORDER-DEPENDENT within a body** (`dyn_local_traits` grows as lets are visited) while the signature position is not — the lane's first tower-shaped fixture put the call BEFORE the erasing `let` and never reached the branch. **ORIGINALLY OPEN, an OVER-CHARGE rather than a silent under-report — so it is not a cardinal sin, and it must not be traded for one.** The tempting fix (scope the index per-receiver) is right in direction, but the same commit's `receiver_is_erased` walker is where [[R575]] shows arms are already missing, so narrowing must not be done by deleting the body-wide read until the per-receiver route covers what it covered. Related and separate, same wave: `2078e08`'s Field arm licenses CHA for leaves the trait does not declare (`dep#Handler::as_ref`, `::unwrap` — chain drift), turning a body that performs nothing into `['Net']` with `deny Net` exit 1; `[]` at `36a917d`, `['Net']` at HEAD. |
| R573 ts: **FIVE CALL SPELLINGS THAT PROVABLY WRITE A FILE REPORT `inferred: []` AND `pure` EXITS 0 — the ⟨0.39⟩ index-signature join never runs at the `chargeExternalDecl` funnel or at the element-access reference spelling.** `i.roll.call(null,n)` → `[]`; `i.roll.apply(null,[n])` → `[]`; `[n].map(i["roll"])` → ABSENT; and the LOCAL twins `li.roll.call(…)` / `li.roll.apply(…)` → ABSENT. | 2026-09-24 | ts — PRE-EXISTING, not a regression | **Ground-truthed by EXECUTION, not by reading the report: `node` ran the same bodies and `.call`, `.apply` and `[5].map(li["roll"])` each wrote the file.** Fixture `tsc -p` exit 0 with the compile CHECKED rather than silenced; `analyzed` 10 and 15, not hollow. **Attribution attacked rather than assumed — measured in a worktree at `a70e435`, where `fCall`/`fApply`/`fBind`/`fAlias`/`fElemCall` were ALL `[]`: [[R524]] and [[R558]] closed four of them and left these five. A residual of both fixes' audit boundaries, not a reintroduction.** `scan.mjs:6320` (`chargeExternalDecl`) calls `joinLocalImpls(recordDispatch(...))` and never `joinIndexSignatureImpls`; R524 added that join only at the CallExpression arm and R558 only at the HOF-ref arm, while the `.call`/`.apply`/`Reflect.apply` funnel routes through `chargeExternalDecl` and gets neither. **The LOCAL half is strictly WORSE than its own baseline: `li.roll(n)` is a disclosed `Unknown` (`deny Unknown` exit 1), and the identical body through `.call` is ABSENT (`deny Unknown` exit 0).** | **FIXED 2026-09-24 — candor-ts `6b55ed6` — AND THIS ROW'S SCOPE AND REMEDY WERE BOTH WRONG: see [[R587]]. The real set is FIFTEEN spellings, not five, and the remedy recorded below closes THREE of them and structurally cannot close the rest.** **ORIGINALLY OPEN — and it survived because only the SCOPED policy form separates it.** Blanket `deny Fs` exits 1 on every arm, because the structural implementor is independently reported; the four-form discipline (blanket / `deny Unknown` / `deny <E> Unknown` / scoped) is what exposed it, which is the §F1 requirement working. Distinct from [[R566]] (disclosed `Unknown` there, SILENCE here). The review also printed the full enumeration of the five sites that answer *does this call dispatch, and to what* — the remedy is that `chargeExternalDecl` join the index signature as the other two arms do, not a fourth site. |
| R577 rust: **WHETHER A DISPATCH IS SEEN DEPENDS ON WHETHER THE FILE HAPPENS TO `use` THE TRAIT — `trait_fields`, `rets` and R561's closure arm record only the BARE LEAF, and the foreign branch is gated on `full.contains("::")` after expanding that leaf through the FILE's `use` map.** Same field, same dep, same body: with `use dep::Q;` it reads `['Net']`; written inline as `Box<dyn dep::Q>` in a module with no `use`, it is **ABSENT**. | 2026-09-24 | rust | **Found by the code review OF PART 93 ITSELF, and it corrects that part AND two of my rows.** `collector.rs:3689` derives `written = trait_quals_by_param[recv][tr] ?? trait_quals[tr] ?? tr` then `full = expand(written, uses)`; `sig_trait_quals` and `visit_local` record the QUALIFICATION, while `trait_fields`, `rets` and the new closure arm record only the leaf. So `full == "Q"`, `contains("::")` is false, and obligation-1 disclosure, the crate-qualified call AND the R4 CHA are all skipped — which is the *"ZERO hits"* PART 93 observed and drew the wrong conclusion from. **Gate named by measurement: adding an entirely UNUSED `_x: &dyn dep::Q` parameter — nothing in the body references it — flips the silent arms to charging.** Four policy forms: scoped `deny Net via_field` and `pure via_field` both exit 0 while `via_param` exits 1 on both; blanket `deny Net` exits 1 only INCIDENTALLY because the implementor is independently reported. | **FIXED 2026-09-24 — candor-rust `845cb32`**, by SHARING the derivation rather than adding a fourth copy: `lang::collect_written_trait_quals` is a `syn::visit` over every `syn::Type` with **no arm set of its own, so it cannot drift from the declaration sites it serves**, consulted as the LAST fallback only when `expand` yields no `::`. Four safety properties each pinned by a red-on-disable assertion; the closure binder additionally records its own per-parameter qualification, **which fixes a FALSE KEY HEAD was emitting** — a closure param shadowing a signature param inherited the signature's crate and published `beta#S::ping` for a body dispatching only on `alpha::S`. [[R571]] not regressed. **ORIGINALLY OPEN, and it RETIRED three XFAILs and corrected two rows of mine.** [[R561]] and [[R562]] were recorded as *"closed for a LOCAL abstraction, open for a FOREIGN one"*. **That was wrong. The local comparison happened to write a bare leaf and the foreign one an inline `dep::Q`, so the variable I isolated was not the variable.** With the trait imported, all six PART 93 arms resolve in rust. The remedy is to record the qualification at the field / return / closure-parameter declaration sites the way `visit_local` already does — one question, four sites, and three of them answer it with less information than the fourth (§F1 Q3). |
| R572 swift: **AN OVERLOADED CONFORMER MEMBER PLUS A PROTOCOL-EXTENSION DEFAULT DROPS EVERY CONFORMER'S EFFECTS SILENTLY — a CARDINAL SIN, and it is [[R543]]'s ALAMOFIRE HALF, 8 honesty violations live at HEAD.** `resolveQual` is an EXACT-NAME lookup and an overloaded member's qual carries a signature suffix (`Impl.two(Int)`), so `resolveQual("Impl.two")` returns empty for EVERY conformer; `providedEdged` is true because the extension default WAS matched through `matchOverloads`, so `\|\| providedEdged` takes the union branch and **unions empty sets — no edge, and no `Unknown`**. | 2026-09-24 | swift — PRE-EXISTING (byte-identical at `eae95e9`) | **Five arms, executed, and the 2×2 that isolates it: `callTwoDefaulted` (conformer member overloaded AND an extension default) reads `inferred:[]`, `unresolved:false` — while `callOneDefaulted` (not overloaded), `callOneBare`, `callTwoBare` (overloaded, NO default → discloses `Unknown`) and `Req2.callX` (the PROTOCOL member overloaded but the conformer's not) all answer correctly.** `pure Req.callTwoDefaulted` and `deny Net Req.callTwoDefaulted` both exit 0 over a function that executes `URLSession.dataTask` and prints. **The comment twelve lines above the defect says *"OVERLOADS RESOLVE HERE EXACTLY AS THEY DO ON THE TYPED-CALL PATH"* and cites the corpus A/B for it — TRUE of the `provided` half (`overloadedBases` → `matchOverloads`) and FALSE of the conformer half beneath it. §F1.3 and §K in one place.** | **FIXED 2026-09-24 — candor-swift `5b2dc0e`, and THE OBVIOUS FIX WOULD HAVE INTRODUCED A FRESH CARDINAL SIN INSIDE THE FIX FOR THIS ONE.** Substituting `matchOverloads` for `resolveQual` — what the two older copies do — is a REMOVAL smuggled into an additive change: the signature-suffixing pass skips ACCESSOR units, so an overloaded base can still have real units under the bare name, and a default-argument-expression body is the common case (**45 of them in swift-argument-parser**). Measured rather than reasoned: that form **lost call edges on 10 corpus rows** and took `CallD.call` from `[Net]` to `[]` with `deny Net` exit 0. **So `memberTargets` UNIONS rather than substitutes**, and both halves of the protocol-dispatch loop now go through it; the stale comment names the failure instead of asserting the property. Coordinator-verified at HEAD: `callTwoDefaulted` reads `['Net']`, matching its `callOneDefaulted` control. A/B over 7 real Swift packages, 8,091 rows: **ADDED 0, REMOVED 0, CHANGED 184 (84 inferred-only), REACH 145 across 4 entries.** Audited in FULL, not sampled: **0 rows lost an effect, 0 lost a call edge, 0 lost a `dispatchesOn` key**; 817 call edges and 93 distinct wire keys GAINED; the 16 rows that lost a disclosure VALUE each either kept `Unknown` or gained the concrete effect it stood for. **The over-charge is NAMED rather than claimed away: 49 of the 84 effect-gaining rows carry an undiscriminated overload spread** because `narrowByArgTypes` falls back to the whole arity-compatible set when argument types are unrecoverable ([[R537]]'s never-zero rule — pre-existing, and already the typed-call path's behaviour). The clearest is Alamofire's `SessionDelegate.request(for:as:)`, which picks up 3 unreachable `Session.request` overloads and reads `[Net, Rand, Unknown]` over a dictionary lookup, against a PRE value of `[]`. **A self-caught instrument defect worth keeping: the lane's reach probe was wrong on its first build and it found that by a CONTRADICTION, not by luck** — 0 hits in swift-argument-parser while the diff showed 2 changed rows there. `resolveQual(base).isEmpty` missed the bare-name class; corrected, reach went 129 → 145, so the first number was a lower bound. **And one existing test changed and is STRONGER, not relaxed**: R563's `testAnUnnameableWitnessDisclosesRatherThanVanishing` went red because its own doc asserted a property of THIS DEFECT rather than of Swift — the witness is now pinned by argument shape and the arm asserts the effect and the named callee.  **ORIGINALLY OPEN. In Alamofire: `EventMonitor` is a requirement with an empty extension default, and `CompositeEventMonitor`/`ClosureEventMonitor` OVERLOAD `request` — so the shape is the library's ordinary idiom, not a corner. The discriminating control is Alamofire's OWN `didGatherMetrics`, which lists the same 17 `EventMonitor.request` callees and reads `Unknown` only because of an unrelated `Protected.write` callee — i.e. the row that looks healthy is healthy by accident.** Distinct from [[R567]]: no chaining and no wire key involved, the effect is lost IN-SCAN. |
| R578 swift: **AN IMPLICIT-`self` READ OF A PROTOCOL-TYPED REQUIREMENT INSIDE `extension P` LOSES THE RECEIVER'S TYPE — [[R134]] closed the CALL half and the property-READ half one hop over is live.** `readImplicit { body.go() }` gives `calls: []`; writing `self.body.go()` gives 4 edges and `Net`. | 2026-09-24 | swift — PRE-EXISTING (byte-identical at `eae95e9`) | **Crossed 2×2 so the confound is ruled out — the variable is implicit-vs-explicit `self`, NOT requirement-ness: both the requirement and the non-requirement rows read `calls: []` implicitly and resolve explicitly.** Scoped further: `callImplicit() { go() }`, a direct implicit-self requirement CALL (R134's shape), resolves correctly; only the property READ does not. Consequence measured end-to-end: `consume() { let d = Plain(); return d.ast }` is ABSENT from `functions[]` with `pure`, `deny Net` and `deny Unknown` all exit 0, over a body that executes and prints. | **OPEN — and this is [[R543]]'s swift-argument-parser half, DIAGNOSED.** `MDocComponent.ast`'s real unit carries `calls: []` in the callgraph sidecar, while the `MDocComponent.ast` row in `functions[]` is the SYNTHETIC `interfaceUnion` entry — which is exactly why the honesty checker sees a contradiction it cannot localise. |
| R579 swift: **A `String` LOCAL WHOSE TYPE THE ENGINE CANNOT NAME LOSES `write(to:)` ENTIRELY — no effect, no `Unknown`, no row.** `let page = xs.map { … }.joined(separator: "\n"); try page.write(to: outputPath, …)` → ABSENT from `functions[]`; `pure` exit 0, `deny Fs` exit 0, **and `deny Unknown` exit 0 too, because there is no hedge to catch.** | 2026-09-24 | swift — PRE-EXISTING (byte-identical at `eae95e9`) | **One variable, how the local's type is known, with four resolving controls: `viaParam` ✓, `viaLocalAnnotated` (`let page: String = …`) ✓, `viaLocalCall` (a local fn with a declared return) ✓, `viaLocalData` ✓ — against `viaLocalLiteral` (`let page = "hello"`) ABSENT and `viaLocalJoined` (`.joined(separator:)`) ABSENT.** Verified by execution: the file on disk provably holds what was written. | **OPEN — and this is the half of [[R543]]'s `GenerateManual.generatePages` that the HONESTY CHECKER CANNOT SEE**, because there is no `Unknown` anywhere to contradict. A checker keyed on *uncertainty that failed to propagate* is blind to a row that never appears; only a differential against a spelling that DOES resolve finds it, which is what PART 93 does for receivers and nothing yet does for locals. |
| R580 swift: **R563's local-protocol filter runs BEFORE the shadowing precedence, so an inner generic bound that is NOT a local protocol fails to displace the enclosing type's — and the receiver is charged the OUTER protocol's effects. A FABRICATION.** `Box<P: Pr>.shadowed<P: Base>(_ t: P.Type) { P.make() }` reads `inferred:['Net']`, `dispatchesOn:['Sh#Pr.make']`, `calls:['EffPr.make']` — while execution prints `SUB`, never `SINK`. | 2026-09-24 | swift — INTRODUCED 2026-09-23 by `6d7fd06` (R563) | **One variable — whether the inner shadowing bound is a local protocol. The `shadowed2<P: Pr2>` twin, where it IS one, reads correctly.** `Driver.swift:2074` builds `protoBoundParams` by filtering each bound map with `where localProtocolNames.contains(b)` BEFORE letting the function's bound overwrite the enclosing type's, whereas `dispatchAbstraction` applies the precedence unconditionally and refuses on `localTypes` only afterwards. **R563's own comment claims *"the same precedence `dispatchAbstraction` applies to the wire key (R550)"* — it is not the same, and that is §F1.3 twelve lines apart.** | **FIXED 2026-09-24 — candor-swift `42bb6c1`.** Precedence first, filter second, with three controls green in BOTH directions — including one that pins the precedence POSITIVELY, so an erasure could not pass by accident. **The A/B is SAFETY-ONLY and that is in the commit rather than discovered later: ADDED 0 / REMOVED 0 / CHANGED 0 with ZERO reach — and an independent SOURCE census (1,027 `.swift` files, 46 generic functions inside generic types) found 0 shadowing a type parameter's name, so the zero is a property of THE SHAPE, not of the corpus.** That is the right way to retire a 0/0/0: the executing fixture is the evidence, and the census says why the corpus is silent. **Near-miss recorded: the lane's first gate assertion `deny Net Box.shadowed` could never have gone green, because a policy scope matches by PREFIX and `Box.shadowedEff` satisfied it** — renamed so no arm is a prefix of another. **ORIGINALLY OPEN, with its severity BOUNDED by the reviewer rather than asserted: generic-parameter shadowing is a warning in Swift 5 and an ERROR in Swift 6 language mode, so the trigger is disappearing; and the direction is OVER-CHARGE, not silence. Under both PRE binaries BOTH arms were absent — R563 is a large net gain that over-reaches on one arm.** Same wave-introduced over-charge shape as [[R571]] (rust) and [[R574]] (ts): three engines, one day, each fix trading a little precision for the disclosure it added. |
| R581 INSTRUMENT (swift): **R563 TOOK R550'S AND R555'S ENTIRE CORPUS REACH TO ZERO — FOUR HOURS AFTER THEY WERE FILED — so anyone re-running those probes at HEAD reads 0 and would call both changes INERT.** Same checkouts, same invocation, only the binary varying: at `0c16614`, swift-nio gives `R550HIT` 15 and `R555HIT` 15; at HEAD `6d7fd06`, both are **0**. | 2026-09-24 | swift — instrument, not an engine defect | **Found by the code review re-deriving the reach numbers rather than citing them.** Nothing was lost: the prefix-stripped `dispatchesOn` diff `0c16614` → HEAD on swift-nio is **LOST 0, GAINED 216**. R563's `typeReceiverProto` branch now CONSUMES those 15 `T.atomic_*` sites and the obligation-1 key is published by the protoDispatches site instead — so the keys survive and only the probe's branch stopped being taken. | **OPEN as a standing hazard about EVIDENCE, not about behaviour. A reach probe measures a BRANCH, and a later fix that re-routes the same work through a different branch silently zeroes it — leaving a number in the register that now reads as "this change was inert".** R550's `typeBound` arm and R555's `localProtocolWirePath` arm now rest on their fixtures alone. This is the [[R554]] class one level in: evidence that decays while the citation still reads as evidence. The remedy is the same — a write-up must carry the counts and direction that let it be CHECKED, and a probe cited as reach must say WHICH branch it counts. **Note the ts twin found the same day and independently: R560's probe fires BEFORE the filter it is cited as evidence for ([[R574]]).** |
| R582 rust: **AN UNANNOTATED `let` COLLECTION BUILT FROM A `dyn` PARAMETER LOSES THE ELEMENT'S DISPATCH ENTIRELY — no row, no `Unknown`, no `unresolved`, no `invisible`, over a body that reads a file.** `let v = vec![s];` then `for x in v { x.emit(n) }` → **ABSENT from `functions[]`**, while the identical body with `let v: Vec<Box<dyn Sink>> = vec![s];` reads `['Fs']` and the direct `s.emit(n)` control reads `['Fs']`. | 2026-09-24 | rust — PRE-EXISTING (present at `b26df9a` and at HEAD) | **Found by the R571 fix lane while building its own controls, and coordinator-verified independently: `cargo build` exit 0, `analyzed=4`, `ctl` and `via_annotated_vec` both `['Fs']` against `via_unannotated_vec` ABSENT.** One variable — the type annotation on the `let`. Scoped `deny Fs via_unannotated_vec` exits **0** and `pure via_unannotated_vec` exits **0**; blanket `deny Fs` exits 1 only INCIDENTALLY because `W::emit` is independently reported, which is why the four-form discipline is what exposes it. Repro: `scratchpad/rustfix-r582/hcase/`. | **FIXED 2026-09-24 — candor-rust `845cb32`, AND THE MECHANISM I RECORDED HERE WAS WRONG: see [[R586]]. It is not the R569 family and not the `let` binder — the INLINE `for x in vec![s]`, with no binder at all, is equally ABSENT, as is the array spelling and the CONCRETE twin. Neither element resolver had an arm for a collection LITERAL.** Registry A/B over 1,626 crates: **ADDED 18, REMOVED 0, CHANGED 133 — and on the narrow `inferred` key, ADDED 18 / CHANGED 0 with all 18 added rows carrying `inferred: []`, so no effect set anywhere gained or lost a member.** All 18 traced to source with no sampling. **20 malformed key occurrences among the new disclosures are REPORTED rather than hidden** — anstream's multi-bound receiver and reqwest's R549 chain drift, both PRE-DATING this change, which merely makes them spellable where the branch previously emitted nothing. **ORIGINALLY OPEN — I recorded it as the [[R569]] family (an unannotated `let` typed by `ctor_type` rather than `resolve_recv_type`) reached through a COLLECTION and a for-loop, so the element type is lost before `resolve_elem_trait_leaves` is ever asked.** Related to [[R575]]'s missing element arms but distinct: there the walker has the fact and does not read it; here the fact is never recorded. |
| R583 INSTRUMENT (ts): **THREE REACH PROBES COUNTED BRANCH ENTRIES, NOT JOINS — and the one cited as evidence reported 182 hits where the real number of joins was ZERO.** `R560-REACH` fired on branch entry before `recordDispatch`; grepping the MECHANISM rather than the row found `R524-REACH` (fires whenever a foreign index signature resolves) and `R558-REACH` (fires whenever `recordDispatch` mints a key) doing the same. | 2026-09-24 | ts — instrument, and the ts twin of [[R581]] (swift) found the same day | **Found by taking [[R574]]'s probe defect and asking WHERE ELSE, rather than fixing the instance in hand — §9.** Re-derived over the same 16-package corpus with ONE variable (probe placement), reports byte-identical: **77 branch entries across 6 entries → 0 JOINS.** So R560's published *"182 hits across 12 entries — the over-charge control WITH real reach"* did not measure reach at all, and **on that corpus R560's entire change contributes nothing** — as does its fix. Recall hunt over real axios 1.7.2: 3 branch entries, 0 joins (`axios.create()` mints a key, but `c.get` resolves to `class Axios`'s own declaration and a class member mints no dispatch key). **Neither of the other two published figures was WRONG** — R524's 6 hits matched its 6 changed rows and R558's was already 0 — so this was latent rather than a wrong number, which is exactly the kind that survives. | **FIXED 2026-09-24 — candor-ts `6a639e6`.** All three marks now go through ONE `probeJoinReach` keyed on the join's RETURNED OUTCOME rather than on entering its branch. Inert to reports and MEASURED to be so: ADDED 0 / REMOVED 0 / CHANGED 0 over 1,730 rows, six fixtures byte-identical. **The general rule this earns: a reach probe cited as evidence must count the thing it is cited for, and the cheapest way to check is to place it on the OUTCOME, not the branch.** |
| R584 swift: **THE CLASS HALF OF TYPE-RECEIVER DISPATCH IS SILENT — every PROTOCOL spelling resolves and every CLASS spelling is ABSENT from `functions[]`, over a body that executes `URLSession.dataTask`.** `f<P: EffBase>(_ t: P.Type) { P.make() }`, its `t.make()` twin, the same inside `struct Box<P: EffBase>`, a concrete class metatype `f(_ t: CBase.Type) { t.validate() }`, and `type(of: c).validate()` — **all five ABSENT, with `deny Net <fn>` AND `pure <fn>` both exit 0.** | 2026-09-24 | swift — PRE-EXISTING at `6d7fd06`, not from this wave | **The discriminating control is the PROTOCOL twin of each row, which resolves `[Net]`: [[R563]] closed the protocol half and the class half was never asked** — §9, an audit scoped to the shape in hand. `EffBase.make()` with the class named literally also resolves, so the variable is the metatype/type-parameter receiver, not the class. Both fixtures `swift build` AND run. | **FIXED 2026-09-24 — candor-swift `1edf1ee`+`c40ffc5`, and the RULING is the interesting part: a class hierarchy is NOT a protocol conformer set, so the lane did not unify them.** The generic/metatype spelling now resolves to **exactly what the LITERAL spelling of the same call resolves to** — the statically-named class's own implementation ∪ every local subclass override, with no ≤12 cap and no `Unknown` hedge, **because the literal spelling has neither, and introducing one here would make one program answer two ways depending on how its receiver was written.** Resolution lands in `rootOfUnaliased` rather than in a dispatch branch of its own, deliberately: a separate branch would have answered MORE CONFIDENTLY than the literal does. `final` needs no case (no subtypes ⇒ no-op fan-out); `static` vs `class` needs none either. A/B over 15 packages / 33,434 rows: **`inferred` 0/0/0, wide CHANGED 117, REACH 469 across 10 of 15 — all 117 audited with zero values lost on any field**, and the 8 distinct added values ground-truthed FROM SOURCE (GRDB's `type(of: self).databaseTableName`, swift-crypto's `Self(bigEndianBytes:)`). **Auditing its own diff found a SECOND defect fixed in the same commit**: `type(of:)` on a value whose declared type is a generic PARAMETER dispatched on the parameter's NAME, and GRDB has that exact collision (`DAO<Record: …>` beside `open class Record`) — §F1.7. **Reach stated as what it is: no new effect row in the corpus, which is expected; a source census over 3,194 files found 1,119 metatype parameters of which only 48 name a local class — the shape is RARE, not absent, and where it occurs it was completely silent.** **ORIGINALLY OPEN, with corpus reach stated as what it was rather than estimated: none could be established.** 199 metatype parameters are declared across 5 of 7 packages, but only 15 call sites dispatch on one and all 15 are protocol-typed (`ParsableCommand.Type`), which resolves correctly; `type(of: x).member(` has **ZERO** occurrences. **So this is a latent silent under-report with an executing repro, not a measured corpus hit** — filed on the fixture's evidence and labelled accordingly. It is a THIRD direction (a widening) and wants its own A/B and fabrication guards. |
| R585 swift: **A METATYPE RECEIVER RESOLVES ONLY WHEN IT IS A FUNCTION OR INITIALIZER PARAMETER — every other binder of a metatype is dropped, in the CLASS half AND in [[R563]]'s PROTOCOL half.** `let t: RBase.Type = RSub.self; t.go()`, the un-annotated `let`, a stored property, a subscript parameter, a `for` element, and a factory return are ALL ABSENT; `init(_ t: RBase.Type)` is the one shape that works. | 2026-09-24 | **CLOSED — candor-swift `828f2b5`: ALL NINE binders, PLUS A TENTH THE ENUMERATION DID NOT CONTAIN. Fixture arms 4 of 22 -> 22 of 22.** swift | **Found by the [[R584]] fix lane widening its own boundary past its trigger (§9), on a fixture that builds and RUNS — it prints `SINK` for all eight arms while six are absent from `functions[]`, with `deny Net` and `pure` both rc 0.** The eighth arm, `let t: RP.Type = RPC.self`, is R563's OWN residual: the protocol half has the same binder gap. **ENUMERATED AND MEASURED 2026-09-24 — NINE BINDER KINDS, BOTH HALVES, EIGHTEEN ARMS, wider than this row's "six".** An executable fixture that `swift build`s clean (protocol half and class half for each): controls `ctlP`/`ctlC` (literal spelling) and `b1` (function parameter) carry `['Net']`; **b2 local `let` with `X.Type` annotation, b3 local `var` likewise, b4 stored PROPERTY of metatype type, b5 closure PARAMETER, b6 for-in over `[X.Type]`, b7 module-level GLOBAL, b8 optional metatype via `if let`, b9 function RETURN into a local, b10 enum payload via `case let` — ALL ABSENT, in BOTH halves.** **Mechanism confirmed by experiment, not asserted:** `CandorCore.typeName` has no `MetatypeTypeSyntax` case, so `.Type` survives in exactly one place — `FnInfo.metatypeParams`, written from the parameter clause alone. A 3-line throwaway experiment (record the base at the annotated-local binder, read it in `rootOfUnaliased` beside `typeBoundParams`) **flipped b2 and b3 in both halves to `['Net']` and moved nothing else**; reverted. **DELIBERATELY NOT FIXED:** each binder has its own name->type map (`vars`, `fields`, `globalTypes`, `arrayElem`, closure params, catch/case bindings, the `returns` index), so the fix is one new map per scope kind plus ~6-9 write sites, each carrying its own fabrication risk — and R584 is the precedent, where ONE binder needed a full commit plus an 11-package A/B. **Landing 2 of 9 and calling this closed would be the audit-boundary-drawn-around-its-own-trigger failure (§9) this family keeps paying for.** The next agent starts from a measured list and a confirmed mechanism. | **OPEN, and deliberately NOT ridden on R584's fix — closing it means teaching SEVEN more binder kinds what a metatype is, and R563's own note refused to widen `typeName` for exactly that reason.** It wants its own row, A/B and decision. **The documentation half IS closed (`c40ffc5`): AGENTS.md's bounded-CHA paragraph asserted the contract holds *"however the receiver was bound — parameter, stored property, local `let`"*, which is true of a VALUE receiver and FALSE of a TYPE one. A documented guarantee closes the question and licenses narrowing, so it converts straight into a silent under-report** — it now states the type-receiver contract and names this row instead of appearing to cover it. **CLOSED 2026-09-25, candor-swift `828f2b5` — all nine binders, PLUS A TENTH, and the tenth is the finding.** Fixture arms **4 of 22 -> 22 of 22**; the brief's table was re-verified at HEAD before anything was touched and held exactly. The read side is ONE authority (`metatypeBinder`), consulted from `typeReceiverProto`, `typeReceiverType` and `rootOfUnaliased`, so nothing downstream can tell which binder produced the name. **A/B, 15 packages / 3,194 Swift files / 32,764 rows: ADDED 19, REMOVED 0, CHANGED 156 — and ZERO REMOVALS IN EVERY FIELD, not merely at row level** (`dispatchesOn` +283/−0, `invisible` +125/−0, `calls` +18/−0, `inferred` +2/−0, `incomplete` +2/−0, `fs` +2/−0, `direct` +1/−0, `unknownWhy` +1/−0). The ⟨0.39⟩ wire gained keys and lost none, which is the regression [[R584]]'s own A/B caught, re-asked. **REACH 81 hits across 3 of 15 packages.** **The ONLY new concrete effect in the whole A/B is `Fs read` on `ParsableArguments.parse`/`parseOrExit`, traced one hop to source and adding NO approximation:** `static var asCommand: ParsableCommand.Type` + `self.asCommand.parseAsRoot(arguments)`, where `parseAsRoot` was ALREADY `['Fs','Unknown']` pre-fix. Everything else added is `Unknown` or `[]`. **Three over-charge controls, all COMPILING AND RUNNING with execution ground truth** (a pure hierarchy through all nine binders reading `[]` with the gate proven able to fail on the same bytes; an effectful neighbour bound through every binder but called only on its inert member — not charged, so the fix charges the CALL not the TYPE; and a rebind control). §1b kill switch `CANDOR_R585_OFF` restores the pre-fix answer EXACTLY, measured against a real PRE binary built at `8067f4a` — both say 4 of 22, the same four. **Near-miss reported per §E1:** the agent first read `Syntax.isProtocol` as `{ return true }` and nearly filed 5 rows as a fabricated call list; it was reading the wrong lines, and said so. Residual filed as [[R615]]; the tenth binder as [[R614]]. |
| R586 rust: **THE CONCRETE HALF OF [[R582]] — a collection LITERAL bound without an annotation loses a NOMINAL element's type too, not just a `dyn` one.** `let v = vec![c]; for x in v { x.go() }` over a concrete type that reads a file → ABSENT, while the annotated spelling charges. | 2026-09-24 | rust | **Found while fixing R582, on the same fixture, and it shows R582's stated mechanism was WRONG — mine.** R582 attributes it to the [[R569]] family (an unannotated `let` typed by `ctor_type`). It is neither that family nor the `let` binder at all: **the INLINE `for x in vec![s]` — no binder whatsoever — is equally ABSENT, so is the array spelling `[s]`, and so is the concrete twin.** Neither `resolve_elem_type` nor `resolve_elem_trait_leaves` had an arm for a collection LITERAL; there was no element to lose. | **FIXED 2026-09-24 — candor-rust `845cb32`**, as ONE `collection_literal_elems` helper feeding both resolvers, each asking the one authority its own call site already uses, with agreement-or-nothing across elements. **Residual stated rather than pinned: the disagreement branch is NOT exercised, because Rust makes a literal homogeneous and a program that would reach it does not compile — an assumption, not an asserted control (§E3).** |
| R587 ts: **[[R573]]'s SCOPE WAS FIVE SPELLINGS AND THE REAL SET IS FIFTEEN — and its stated remedy closes THREE of them and structurally cannot close the rest.** Two the row never mentions: `ld.roll.call(…)` / `.apply(…)` on a LOCAL DECLARED interface member, ABSENT while `ld.roll(n)` reads `['Fs']`; plus the element-access spelling in ALL FOUR cells, not just the one arm R573 quotes. | 2026-09-24 | ts | **25 arms in one file, `tsc` exit 0 with the compile CHECKED (poison → exit 2, remove → exit 0), `analyzed=31`, and ALL 25 ground-truthed by RUNNING them under node and watching the file appear.** THREE causes at TWO sites, not one: (a) `chargeExternalDecl` never calls `joinIndexSignatureImpls` — R573's stated cause; (b) `declIsLocal` keeps local declarations out of that funnel entirely, so five local reflective arms are ABSENT; (c) both the HOF-ref arm AND the reflective arm gate on `isIdentifier \|\| isPropertyAccess`, dropping every element-access spelling before either join is reached. **R573's remedy — *"have `chargeExternalDecl` join the index signature as the other two arms do"* — was mine, and it fixes only (a): that funnel receives NO call-site expression, so it cannot supply the member name an index signature has no declaration to give.** A fourth sub-finding found by measurement: **`checker.getSymbolAtLocation` returns `undefined` for an element access** on all four arms, so widening the gate alone silently does nothing. | **FIXED 2026-09-24 — candor-ts `6b55ed6`**, routing all three sites through R558's own `chargeMemberRefDispatch` predicate (§G: share, do not copy) with a new `refSlotDecl` doing the `getPropertyOfType` / `getIndexInfosOfType` lookup. **Only the SCOPED form moves** — blanket `deny Fs`, `deny Unknown` and `deny Fs Unknown` exit 1 on every arm before AND after, and were kept as LABELLED cannot-discriminate controls — while scoped `deny Fs` and `pure` each go **0 → 1 on all fifteen**. A/B over 18 packages / 6,506 rows: **0/0/0 with REACH 0, SAFETY-ONLY and recorded as such**, and bounded honestly by a throwaway probe: **42 branch entries across 7 packages → 0 joins**, against **71 real `.call`/`.apply`/`Reflect.apply` sites** in non-test source. **The syntax is in real library code; the JOIN is not, because it needs a visible implementor IN THE SAME SCAN — a consumer-side property an 18-library corpus structurally cannot have**, which is the same reason R558's reach was 0. Recall hunt against **socket.io 4.7.5's own published `DefaultEventsMap`** moves five arms and `deny Fs` 0→1 on each. **[[R574]] carried across rather than re-decided: without it this fix reintroduces R574's fabricated `Exec` at a new site**, so CONTROL 6 holds the call spelling constant against the engine's own CallExpression answer. Live residual named, not asserted away: a COMPUTED key (`i[k].call(…)`) still resolves nothing, and socket.io's own `this.sockets[fn].apply(…)` is a real instance. |
| R588 instrument (candor-spec `scripts/soundness-status.py`): **THE SHIPPING-DEFECT LIST UNDER-REPORTED ITSELF BY 23 ROWS, AND THE REGISTER HELD TWO ROWS NUMBERED R540.** `bucket()` read a declared `OPEN` only in the OUTCOME cell, but the register overloads the THIRD column as CURRENT STATUS for every row filed before ~R430 — so 23 rows whose status cell opens `**OPEN —` or `**REOPENED` were bucketed `closed-with-fix`/`resolved-no-fix`. Among them R372 (a declared CARDINAL SIN), R198/R209/R227/R305, all five saying `Not fixed` in the outcome cell. Separately R540 named BOTH a swift ternary-receiver sin and a rust dispatch-guard sin; the status tool keys its report by id, so one of the two was absent from every count it printed. | 2026-09-24 | instrument (candor-spec) — FOURTH defect of this shape in this file | **MEASURED.** open 77 -> 100, and the change is PURELY ADDITIVE: no row left the open list (checked by running the pre-change `bucket()` and the post-change one over the same 497 rows and diffing the id sets). The 23: R94 R127 R129 R131 R140 R141 R145 R153 R198 R200 R209 R212 R227 R235 R256 R300 R305 R349 R364 R372 R376 R387 R429. A further 10 rows head their status cell `PARTLY CLOSED`/`HALF CLOSED`/`FIRST HALF FIXED` and NAME what is still owed (R101 R107 R190 R207 R208 R214 R242 R243 R249 R414); they get their own `partly-closed` bucket rather than being called open, because a closure with a declared remainder is neither. The duplicate id was found because a bucket count taken two ways disagreed by one — 101 rows against 100 distinct ids. **The guard is calibrated, not assumed:** injecting a collision into a COPY of the register makes `check_soundness_tables.py` exit 1 naming both line numbers; the unmodified register exits 0. **Arithmetic, stated exactly:** the bucket fix alone takes the open list 77 -> 100 (the 23 above, purely additive). Unmasking the duplicate id adds the 24th — the swift row, now [[R589]], had been sharing R540's slot in a report keyed by id — so HEAD reads **101 open of 498 rows**. Both halves are under-reports of the same list and they were found in the same pass, but they are independent defects and the numbers should not be conflated. | Not a spec change; no engine behaviour moves. Three things landed: (1) `_DECLARED_OPEN` now also reads the STATUS cell and knows `REOPENED`/`STILL OPEN`, still case-sensitive and still anchored to the head of the cell so prose cannot trigger it; (2) an explicit `RESOLVED`/`CLOSED` at the head of the OUTCOME cell outranks a stale status head (R230's shape) and counts as a closure word, which `CLOSURE` deliberately does not because `resolved`/`unresolved` is candor's DISPATCH vocabulary; (3) `check_soundness_tables.py` gained a duplicate-id property, reading the FULL id — the letter suffix is part of it, and collapsing R529/R529b/R529c reported four phantom duplicates on the guard's first run. The swift R540 became [[R589]]. **The open count Tom steers by was wrong in the cardinal-sin direction, in the instrument built to find that direction** — the previous three instances are enumerated in `bucket()`'s own comments, which is the argument for the guard rather than more care. |
| R590 rust: **A REAL FUNCTION ROW DISPLACES THE SYNTHETIC `interfaceUnion` ROW ON THE SAME KEY, SO THE UNION'S EFFECTS ARE DROPPED THE MOMENT THE BODY IS ANALYSED.** Found while tracing the four rows that LOST `Log` in [[R576]](b)'s A/B — an additive disclosure fix cannot remove an effect, so the loss had to be explained before shipping. | 2026-09-24 | **CLOSED as [[R597]] — candor-rust `ea1d9b5` + `1f0c894`.** Originally: rust — OPEN, PRE-EXISTING; surfaced by [[R576]](b), not caused by it | **MEASURED on hickory-proto 0.26.1/0.26.2/0.26.3 and tokio 1.53.1.** Before the R576(b) fix, `serialize::binary::BinEncodable::to_bytes` was represented ONLY by a synthetic union row — `{inferred:[Log], interfaceUnion:true, invisible:[idna,tinyvec]}`, no `loc`, no `calls` — because the real row was the one R576(b) deleted. With the row restored the report carries the REAL row instead: `{inferred:[Unknown], unresolved:true, unknownWhy:[dispatch:BinEncodable.emit], loc:src/serialize/binary/mod.rs:32:5}`. Counted both arms: **384 functions before and after, interfaceUnion rows 5 -> 4, rows with a `loc` 379 -> 380** — so it is a REPLACEMENT on one key, not an addition. **NOT a silent under-report and NOT a gate flip, both checked:** the crate-wide effect set is identical (`Fs, Log, Rand, Unknown`), the `Log` remains visible on the implementor `op::message::Message::emit`, and `deny Log` and `deny Log Unknown` exit 1 on BOTH arms for both crates. **MEASURED 2026-09-24, candor-rust `e50a18e` — THE UNION DOES KNOW MORE, AND THE QUESTION IS NO LONGER OPEN IN THE 'UNMEASURED' SENSE.** An env-gated probe at both suppression sites, over the same 1626-crate cargo-registry corpus [[R576]](b) used: **244 suppressions reached, 233 where the union adds nothing, 11 WHERE IT KNEW MORE** (8 distinct keys). Two shapes. **(1) SEVEN where the body row is PURE and the union said `Unknown`** — `combine#parser::Parser::parse_mode_impl`, `derive_where#trait_::TraitImpl::build_body` and `::build_signature`, three `sea_query` builder members, `tracing_subscriber#fmt::writer::MakeWriter::make_writer_for`. The published key reads CLEAN to a chained consumer while this engine had computed an `Unknown` for it and discarded it — that is a purity claim over a key ⟨0.39⟩ obligation 3 exists to be joined on. **(2) ONE where a CONCRETE effect is dropped, and it was traced to ground truth rather than filed from the probe's word:** `portable_pty#Child::wait`, union `{Exec, Log}` vs real `[Exec]`. `Child` has three implementors; `std::process::Child::wait` supplies the Exec the body row carries, and `SerialChild::wait` at `src/serial.rs:153` contains `log::error!("Error reading carrier detect: {:#}", err)` — the Log the published key no longer carries. **THE MECHANISM CONFIRMS THIS IS [[R576]](a) FROM THE CONSUMER SIDE:** the real row is the DEFAULT BODY's analysis while the union loop reads `{ty}::{method}` per implementor, i.e. the OVERRIDES; neither set contains the other. One cause, two rows. **Not fixed: merging the union into the real row is a CHARGING-direction change needing its own A/B and over-charge controls, which is the direction that fabricates.** | **Not fixed, and R576(b) was shipped over it deliberately** — in that instance the displacement is an IMPROVEMENT: the union's `Log` was computed over an implementor set wider than the bounded-CHA limit, i.e. incomplete by construction, so `Unknown` is the more correct answer for that member. **The open question is the general case, which was NOT measured: when a body-analysed row displaces a union row, can the union have known MORE than the body does?** If it can, the displacement drops effects wherever both exist — and the direction of that loss is the cardinal sin. The population to measure is the 4 union rows remaining in hickory-proto and every other crate where `interfaceUnion` and a `loc` compete for one key; the instrument is the same registry A/B. Filed rather than folded into [[R576]] because it is a DIFFERENT site with a different owner, and §9 — an audit's boundary must not be drawn around its own trigger. **SUPERSEDED BY [[R597]], which fixed it — candor-rust `ea1d9b5` + `1f0c894` — AND CORRECTED THIS ROW'S MECHANISM.** This row said the suppressed union was the OVERRIDES against a real row that is the DEFAULT BODY. That is one route. **It is not the route the concrete-effect case takes:** `portable_pty#Child::wait` has NO default body (a bare requirement at `lib.rs:138`), and the real row is `impl Child for std::process::Child` at `:271` — the implementing TYPE's leaf equalling the TRAIT's leaf, a LEAF COLLISION. Also corrected: the silent-purity population is **5, not 7** — `derive_where`'s two keys carry `invisible:["proc_macro2"]`, which SPEC §2 makes not a purity claim. And the claim that this is [[R576]](a) from the consumer side is **wrong**: R576(a) is an ANALYSIS defect that survives R597 unchanged. **CORRECTED 2026-09-26 by [[R689]]: *NOT a gate flip* IS A PRODUCER-SCOPE VERDICT.** The four rows this row names — hickory-proto 0.26.1/2/3 to_bytes — take a CONSUMER's `deny Log <fn>::` from exit 1 to exit 0 once the crate is chained in via CANDOR_DEPS, measured on a hand-written consumer as well as a generated one. The reason this row could not have seen it: at the producer the PRE `Log` sat on an `interfaceUnion` row, and `deny Log …::to_bytes::` is exit 0 on BOTH arms there, so every gate available at producer scope was green-on-green. The open question this row states — can a displaced union have known MORE — is therefore a question about CONSUMER gates. |
| R591 instrument (candor-spec `scripts/soundness-status.py`): **THE STATUS RULE WAS ASYMMETRIC, AND MAKING IT SYMMETRIC IMMEDIATELY PRODUCED A FALSE CLOSURE.** [[R588]] taught `_DECLARED_OPEN` to read the STATUS cell as well as the outcome cell; it did not teach `_DECLARED_RESOLVED` the same. So a row whose status head says `RETRACTED` while its outcome head still carries the filing-time `Not yet fixed` stayed on the shipping-defect list (R126). The structural cause: **rows are updated by APPENDING, so the outcome cell's HEAD is the OLDEST text in the row** and every head-anchored rule reads the filing-time verdict. | 2026-09-24 | instrument (candor-spec) — CLOSED, candor-spec `df6a781`; the FIFTH defect of this shape in this file | **MEASURED.** open 94 -> 92, and NOTHING joined the open list (pre- and post-change `bucket()` run over the same rows, id sets diffed). Two rows left it — R126 (RETRACTED, not a defect) and R225 (does not reproduce; a decision) — and two more moved out of `cites-a-sha-only` into a true closure, R309 (candor `f135464`) and R467 (candor-swift `5fdf481`, recorded only in R387's sibling until now). **THE SYMMETRY FIX'S OWN FALSE CLOSURE IS THE FINDING:** R162 heads `RESOLVED as a PROCESS GAP ... two real generator bugs found beside it, OPEN` — resolved word FIRST — so the head test matched and the row left the list while its outcome cell still ends *What a correct instrument does, NOT IMPLEMENTED* over four items. A cell that says BOTH is now not resolved. **A review named R162 as hygiene; that same review had earlier said R162 was NOT hygiene. The two contradicted each other and reading the row settled it** — which is why closures are verified against their commit rather than applied from a list. | Three rules landed. (1) `_DECLARED_RESOLVED` reads the status cell too, guarded so a cell declaring BOTH stays open — the cheap direction, at the cost of a few finished rows sitting on the list until a sentence is rewritten. (2) **The bucket follows what the closure HEAD says**: `closed-with-fix` asserts *a defect, and here is the commit*, and R225 landed there on shas it merely CITES as evidence, including the commit it was measured at — choosing the label from a sha anywhere in the row is the [[R553]] defect one bucket over. A fix word with its commit beside it is a fix; a decision word with no commit beside it is a decision. (3) `_BODY_CLOSURE` no longer fires inside a PARTIAL closure — `\b CLOSED \b` sits inside `PARTIALLY CLOSED` and `(b) CLOSED`, so the SAYS BOTH advisory named R198, R209 and R576, all three of which say PARTLY, correctly, and are correctly open. **The check was right to fire and wrong about what it found, which is the worse failure: a reader who checks three and finds them fine stops reading the fourth.** Four selftest cases added, one of them an expectation I had written an HOUR earlier and got wrong — the selftest catching its own author's stale expectation is the only reason the fix-vs-decision distinction exists. |
| R595 java: **A FIELD-BOUND CALLBACK THAT A CHAINED CONSUMER REASSIGNS IS SILENT — ⟨0.39⟩'s TOGGLE, REACHED THROUGH A FIELD.** `Cha#collectFieldLambdaBindings` binds a `public static` NON-FINAL field on exactly the evidence it binds a `private final` one — no visibility or finality check. Against an unscanned downstream caller that is SPEC §4.0's accepted open-world trade. Against a CHAINED consumer it is not, and SPEC says so in as many words. | 2026-09-24 | **CLOSED — candor-java `04eb782`, AND THE DEFECT WAS NOT AT THE SITE THIS ROW NAMED: the binding is fine, the CONSULT site (`Candor#virtualDispatch`) returned ~55 lines before obligation 1 ever recorded the member, so the recording was ABSENT rather than wrong.** java — OPEN, CARDINAL SIN, PUBLISHED (shipped in 0.39.2) | **MEASURED on two arms differing in ONE LINE of the library, consumer byte-identical.** Library with `public static Hook afterStage = () -> { };` — `app.Main.main` reads `inferred: []`, `unresolved: false`, NO `unknownWhy`, and scoped `deny Fs Unknown app.Main.main` exits **0**. Delete only that initialiser and the same `main` reads `["Fs","Unknown"]`, `unknownWhy: ["dispatch:lib.Hooks$Hook.run"]`, `paths: ["/tmp/candor-r595"]`, exit **1**. **So adding a PURE DEFAULT to a library deletes a disclosure AND a real `Fs` from every consumer.** Blanket `deny Fs Unknown` stays exit 1 only INCIDENTALLY — the consumer's lambda is its own row and the caller is never judged, so the blanket form catches it for a reason unrelated to the defect. **Ground truth is the app's own bytecode: the PUTSTATIC and the effectful lambda are both in the scanned classes — a missing JOIN, not missing information.** **COORDINATOR-VERIFIED:** SPEC.md's ⟨0.39⟩ clause states this exactly — *"The paragraph above calls the invisible downstream implementor 'the accepted trade everywhere else in this contract'. Across a CHAIN that trade stops being acceptable, because the implementor is no longer downstream: the consumer supplies it, and the engine can see it"* — and names the shape as a TOGGLE running in the wrong direction. This row is that toggle reached through a FIELD rather than through an implementor set. | **Found while answering [[R94]], and it SUPERSEDES R94's framing.** R94 asked *state the limit or enforce it*; both answers were wrong because the open-world premise holds only while the consumer is UNSCANNED. **Fix direction — an added disclosure, not a refusal to bind:** keep publishing `dispatchesOn` when a binding came from an externally reassignable field (non-private, non-final). The unbound arm already works precisely BECAUSE the library publishes that key and the consumer resolves its own implementor against it, so the machinery exists. Enforcement (bind only private/final, `Unknown` otherwise) belongs behind `CANDOR_CLOSED_WORLD`, which already carries the *the flag moved a verdict* hazard warning. Documented as a named miss in candor-java `README.md:142`; pinned by **13 rows in `test/smoke.sh`, calibrated — 4 go red if the unbound arm reads the bound report** — and carrying the ledger rule in the file: *IF THE "today: SILENT" ROW GOES RED, R595 IS FIXED — invert it, do not delete it.* **NOT ASKED OF rust/swift/ts and the question translates directly** (§F): a field-bound callback reassigned by a chained consumer is expressible in all four. Documented in candor-java `d6419ae`. **FIXED 2026-09-25, candor-java `04eb782`.** Fixed by extracting `recordDispatchedMember` (one function, two callers, so owner qualification / Object-protocol exemption / member spelling cannot drift — §F1 q7) and gating the field-bound call on a new `Cha#externallyReassignableField` (non-private AND non-final) which **FAILS OPEN — discloses** — on an unparseable key, an off-classpath declarer, or a field the declaring node does not list. **A/B over 150 real jars, 145 compared, 293,482 rows: ADDED 0, REMOVED 0, CHANGED 0 on all four keys.** **That zero is NOT the flattering kind and was PROVEN so rather than asserted: the field-bound branch fires 26 times over 23 distinct fields, and the new disclosure fires 0 times because ALL 23 fields are `private`** — ground-truthed with `javap`, never from candor's own report (22 `private final`, 1 `private` non-final). The corpus reaches the branch and the gate correctly declines every instance. Cost 0 rows of 293,482 (0.00%) — ships under the standing rule. **CALIBRATED IN BOTH DIRECTIONS:** reverting the fix gives 5 failed; making the gate a rubber stamp (always 'reassignable') gives 1 failed; as committed 600 passed / 0 failed. The two smoke rows that went red were INVERTED IN PLACE per this row's own ledger instruction, not deleted, and an over-charge twin was added (`private static final` against `public static`, one class, one line apart, both reaching a real `FileWriter` so both rows exist — §E3). Also measured: the fix covers a NAMED-CLASS implementor as well as a lambda (`[]` pre-fix, `[Fs]` post-fix). Residual filed as [[R601]]. |
| R597 rust: **A REAL ROW AT AN ABSTRACTION MEMBER'S KEY DISCARDED THE IMPLEMENTOR UNION, SO THE KEY PUBLISHED THE WRONG ANSWER TO ⟨0.39⟩ OBLIGATION 3.** `scan.rs` emitted the `interfaceUnion` entry only where no real row claimed its hash. Two rows answer DIFFERENT questions at that key and neither contains the other: the real row is the member's DEFAULT BODY, the union is over the IMPLEMENTORS' OVERRIDES. | 2026-09-24 | rust — CLOSED, candor-rust `ea1d9b5` + `1f0c894` | **MEASURED over 1,626 cargo-registry crates: 244 suppressions, 233 benign, 11 WHERE THE UNION KNEW MORE.** Five published `inferred: []` with no disclosure — a §2 purity claim — over an `Unknown` this engine had COMPUTED AND DISCARDED (`combine#parser::Parser::parse_mode_impl`, three `sea_query` builder members, `tracing_subscriber#fmt::writer::MakeWriter::make_writer_for`). One dropped a concrete `Log`. **EXECUTED ground truth, one tree, one consumer binary, one variable: `deny Net` over `app::run(&dyn Sink)` goes exit 0 -> exit 1.** **AND THE MECHANISM IN THE FILING BRIEF WAS WRONG FOR THE CASE THAT MATTERED — corrected by the fixing agent and verified by the coordinator:** `portable_pty#Child::wait` has NO default body at all (`fn wait(&mut self) -> IoResult<ExitStatus>;`, `lib.rs:138`, a bare requirement). The real row there is `impl Child for std::process::Child` (`lib.rs:271`), whose implementing TYPE's leaf equals the TRAIT's leaf — **a LEAF COLLISION, a second route entirely.** Also corrected: `derive_where`'s two keys are NOT silent, they carry `invisible:["proc_macro2"]` which SPEC §2 makes not a purity claim, so the silent-purity population is **5, not 7**. | **FIXED by emitting the union BESIDE the real row, never merging into it.** Merging is the CHARGING direction and fabricates — the real row has a `loc` and is a concrete body. Two entries under one key are unioned BY THE CONSUMER, a family-wide ruling already made four-way (`ENTRY-COLLISION-DECISION.md`, 2026-08-02, PART 26) and implemented in `deps.rs` (`by_key … union_with`), whose own item 2 names the within-report collision as this same defect. **So no new resolution rule — a contributor to an existing one (§G).** Emitted only where the union knows something the real row does not. **A/B, `bin/corpus-ab.py`, 1,626 crates: ADDED 0 keys, REMOVED 0 keys, CHANGED 11, rows 327,762 -> 327,773. Audited IN FULL, not sampled: all 11 have PRE ⊆ POST, each gained exactly one member, every one an `interfaceUnion` row, and NO PUBLISHED ROW CHANGED A BYTE.** REACH 11 hits / 7 entries; **the foreign leg has ZERO reach over this corpus — safety-only, recorded now rather than discovered later.** **THE COVERAGE (`invisible`) LEG IS DELIBERATELY NOT EMITTED, blocked on [[R598]]:** including it took the change to 67 rows and bought 56 genuine coverage disclosures at the cost of THREE NEW FABRICATED `Log` claims from R598's pre-existing bug. `invisible` carries ⟨0.30⟩'s NON-GATING ruling and cannot flip a verdict; a fabricated `Log` makes `deny Log` fire. Coordinator's call: non-gating disclosure is not worth gate-flippable fabrication, and R598 being pre-existing does not license three new instances of it. **Five controls, each falsified in the direction that can catch it** — revert turns the three fix tests red; always-emit turns the silence control AND the new gate red; re-enabling the coverage leg alone turns ONLY the new gate red, which is what makes it a gate on this decision rather than a general over-emit alarm; and the absence assertion proves its fixture reaches the branch BOTH required ways before asserting anything is missing. `cargo test --workspace` 908 passed; full rust suite green. |
| R598 rust: **THE INTERFACE-UNION CHARGES AN INHERENT METHOD'S EFFECTS TO THE TRAIT MEMBER, because `{ty}::{method}` cannot distinguish `impl Trait for Ty` from `impl Ty`.** A FABRICATION, live in every union row ⟨0.39⟩ already ships. | 2026-09-24 | **CLOSED — candor-rust `d449ee5`, and the FIRST design of the fix traded the fabrication for the CARDINAL SIN on 9 keys before it was caught.** rust — OPEN, PRE-EXISTING, and it BLOCKS [[R597]]'s coverage leg | **Live on `hickory_proto` 0.26.1/0.26.2/0.26.3:** `serialize::binary::BinEncodable::to_bytes` publishes `Log`, sourced from `RData::to_bytes` at `record_data.rs:839` (a `warn!`) — an INHERENT method that `impl BinEncodable for RData` (`:1133`) does not override, so **no dispatch through that member can reach it.** **PROVEN PRE-EXISTING against the `e50a18e` binary on a compiling fixture where no row is suppressed**, so it is present in every union row the shipped rung publishes and was not introduced by [[R597]]. The same fixture also shows the two same-named units merged under one qual — the [[candor-global-unit-identity]] vein. | **NOT FIXED.** `trait_impls` records only (trait leaf -> implementing type) and never the impl block's MEMBER names, so narrowing the lookup needs a NEW INDEX — and narrowing a sound over-approximation on an incomplete index is exactly the denylist/allowlist hazard [[candor-denylist-over-allowlist]] describes. **This row is what [[R597]]'s coverage (`invisible`) leg is waiting on:** enabling that leg today buys 56 genuine non-gating coverage disclosures at the cost of 3 NEW fabricated `Log` claims from this bug. Once this closes, that leg lands free — re-enable it and the A/B should go from CHANGED 11 to CHANGED 67 with zero fabricated members. `r597_the_coverage_leg_is_not_emitted_while_r598_is_open` is the gate that will fail when someone re-enables it prematurely. **CLOSED 2026-09-25, candor-rust `d449ee5` — AND MY FIRST DESIGN OF THE FIX TRADED THE FABRICATION FOR THE CARDINAL SIN, caught by tracing rather than by reasoning.** Reproduced first on a `rustc`-compiled fixture and live on the crate; PRE-EXISTING confirmed by `git log -S` — the `{ty}::{method}` lookup dates from `e96f4d2`, the commit that INTRODUCED `interfaceUnion`. Gate direction executed, one tree / one consumer / one variable: `deny Log` over a `&dyn Enc` dispatch goes **exit 1 -> 0, and 0 is the true answer**. **THE TRAP:** the obvious form (drop the candidate) removed 14 keys over 1,626 crates, and tracing **ALL FOURTEEN** to a body found **NINE WHERE THE EFFECT REALLY IS REACHABLE** — the trait's default body delegates to a same-named method the resolver drops (`snapbox#IntoData::is` -> inherent `Data::is`; `diesel#QueryDsl::{limit,offset,having,find}` x2 -> `CombinationClause::limit`, every `impl QueryDsl for X {}` being empty). Only 5 of the 14 were true fabrications. So the narrowing carries THREE guards, each falsified individually: **evidence** (a macro item in the impl block narrows nothing), **delegation hedge**, and a **channel rule** (a narrowed candidate contributes its `Unknown`/`invisible` unchanged and its concrete effects not at all). **The index is the LOCAL twin of an index that was already member-precise:** `collect_foreign_trait_impls` never had this defect, so **the ASYMMETRY was the bug** — the same shape as R513. Cache schema to rev41, where a rev40 entry reads EMPTY = no evidence = the pre-fix over-approximation. **A/B over 1,626 cargo-registry crates: R598 isolated gives ADDED 0, REMOVED 0, CHANGED 3 with REACH 42,995 hits / 397 entries** — so REMOVED 0 is a property of a branch that demonstrably ran. **Shipped: ADDED 0, REMOVED 0, CHANGED 56, and 11 + 56 = 67 — [[R597]]'s own written prediction, confirmed.** All 56 audited in full: every one a gained synthetic union row, 0 published rows changed a byte, 0 rows lost, 0 bare purity claims; 50 carry only `Unknown`/`invisible`, and the 6 with a concrete effect trace to DECLARED overrides whose effect set the key already published. **Zero fabricated members, so R597's coverage leg is re-enabled and its holding test DELETED exactly as that test's own message instructed.** Residuals filed as [[R605]] and [[R606]]. |
| R599 rust (⟨0.39⟩ (the clause once drafted as 0.40) port design): **RUST CAN MINT DISPATCH-MEMBER KEYS JAVA STRUCTURALLY CANNOT, SO A CONJUNCT COPIED ACROSS ENGINES ASKS A NARROWER QUESTION AND [[R549]] FILLS THE GAP.** java's `untypedDepReceiver` conjunct 4 is `chaTargets(owner,name,desc).isEmpty()` — and the correction that matters: **it is ALSO a member query.** What makes it behave as an abstraction query is that its `name+desc` comes off an `INVOKEINTERFACE`, VERIFIER-GUARANTEED to be a member the interface declares. rust mints its member key from SOURCE, where nothing guarantees that. | 2026-09-25 | rust — OPEN as a PORT-DESIGN row; it priced [[R533]] and it will bite swift's port next | **MEASURED over six chained pairs, Σ analyzed 2,826, one binary.** Asking conjunct 4 as `foreign_impls.contains_key(member)` admits **45 functions (1.70%)**; asking it as *any `foreign_impls` key with prefix `owner#Trait::`* admits **6 (0.21%)**. `REMOVED 0` in every arm. The gap is entirely [[R549]]: **41 of the 42 dispatch members named are members the owning trait does not declare**, and a fabricated member is by construction never a key, so the conjunct could not fire. `tower_service::Service` declares only `poll_ready` and `call`, yet the keys include `map_err`, `map_ok`, `and_then`, `err_into`, `load`, `clone`, `make_service`, `is_pending`. **MECHANISM VERIFIED AT THE INDEX, not inferred:** `foreign_impls` is built in exactly one place (`lang.rs::collect_foreign_trait_impls`, keyed `"{root}#{qual}::{method}"`, one entry per method of each local `impl Trait for T`, sharing its single key authority with `collect_block_nested_trait_impls`), so a trait-prefix key exists for every local impl that overrides at least one method — the one boundary being an impl whose members are ALL defaults, which mints no key at all. | **NOT a defect in either engine's shipped behaviour — a defect in how a cross-engine conjunct is SPECIFIED.** The lesson is the family's own §F: translate the QUESTION across engines, not the code. Two consequences for ⟨0.39⟩ (the clause once drafted as 0.40) if it is drafted: **(1) write the conjuncts INTO the clause at java's granularity** (a CHA question about the dispatch, plus the §4 conventionally-pure surface) rather than the prose *an empty union MUST read Unknown*, or swift re-makes this mis-port — and swift's own post-[[R565]] measurement already shows the same trap, with 0 of 18 hits being the real shape and [[R567]] explaining 13 as malformed keys. **(2) The §4 exempt-leaf skip is INERT on this corpus and must not be credited with any of the reduction** — the trait-prefix arm is byte-identical with and without it. That was CALIBRATED rather than assumed: against the MEMBER-keyed arm the same flag takes tower 30 -> 23 hits, removing exactly the seven `tower_service#Service::clone` lines, so the guard can fire. Set: `clone, fmt, eq, ne, hash, cmp, partial_cmp, to_string` — java's four Object-protocol members in rust's spelling, plus `clone`, which this engine already treats as conventionally pure at the resolution site (`collector.rs`, `leaf != "clone"`), so omitting it would be two spellings of one fact. **`call` is DELIBERATELY EXCLUDED**: java exempts the function-object arm (`FunctionN.invoke`), but in rust `call` is a real effectful trait method (`tower_service#Service::call`) and exempting it would suppress genuine dispatches. |
| R600 instrument (the register itself): **A PATH CITATION IS A PROMISE A READER CAN FOLLOW, AND 25 OF 32 CANNOT BE.** Rows cite scratchpad directories as evidence (*fixture at `scratchpad/agsw/f5`*). Those live in a SESSION-SCOPED temp dir — a new session gets a new path and the old one is gone. | 2026-09-25 | instrument (candor-spec) — OPEN as a CONVENTION defect; ADVISORY, deliberately not fatal | **MEASURED 2026-09-25: 32 distinct scratchpad citations across `SOUNDNESS.md`, `SOUNDNESS-LOG.md` and `BACKLOG.md`; 25 point at nothing.** Dead examples: `agsw`, `dropglue-fixture`, `dupqual`, `panel-fixes-2`, `fix-visible-out`, `carveout`. **Same class as the 531 dead sha citations repaired today ([[R591]] era, candor-spec `2fd94aa`), with ONE difference that decides the remedy: THOSE WERE RECOVERABLE and these are not.** `git filter-repo` leaves a commit-map; a deleted temp directory leaves nothing. So there is no `--apply` for this and there never will be. The cost is not hypothetical — it is the same cost the sha citations imposed, where a review agent read a sha off [[R372]] that the redaction rewrite had killed, could not resolve it, and reported a CLOSED row as open — the live spelling is `94dc8b5`, and writing the dead one here as an illustration is what made this very row fail the citation gate. | **Reported, not repaired, and ADVISORY rather than fatal — on purpose.** A checker permanently red on something unfixable gets disabled, and then it is not a checker; that is the same reasoning that made the SAYS BOTH check in `soundness-status.py` advisory. Printed by `scripts/sha-citations.py`, which is already a doc-gate. **THE REAL REMEDY IS A CONVENTION AND IT BELONGS IN THE ROW THAT CITES: put the decisive artefact IN the row — the numbers, the fixture source, the exact command — and treat the path as a courtesy, never as the evidence.** A row whose claim can only be checked by opening a directory that no longer exists is, to a reader, indistinguishable from a row with no evidence at all. The good rows already do this (today's [[R595]] quotes both arms' JSON inline, [[R599]] quotes the index's key construction), which is why they survive their scratchpad being swept. **This row is also the argument for NOT sweeping the session scratchpad to reclaim disk** — measured the same day, deleting the 11 GB of agent directories would have converted 7 live citations into dead ones to buy space that `target/debug` (25 GB, zero evidence) supplied instead. |
| R601 java: **A REASSIGNABLE FIELD-BOUND CALLBACK WHOSE SAM OWNER IS κ-COVERED STILL LOSES THE CONSUMER'S EFFECT.** [[R595]]'s fix publishes `dispatchesOn` for an externally reassignable field-bound dispatch, but `recordDispatchedMember`'s owner qualification excludes the κ frontier — so `public static Consumer<String> hook = s -> {…}; hook.accept(x);` fires the reassignability gate and publishes NO key. A chained consumer that reassigns it reads silent. | 2026-09-25 | java — OPEN; a RESIDUAL of [[R595]]'s fix, needing a NEW obligation rather than a wider one | **Measured 2026-09-25 on a compiling fixture:** the reassignability gate fires (`R595FIELD`, observed) and `Fs` is charged IN-SCAN, while a chained consumer sees nothing. **PREVALENCE, from a class-file census of the same 150-jar corpus parsed DIRECTLY rather than through candor: 49,771 classes, 170,960 fields, 7,387 SAM-TYPED fields, of which 207 are NON-PRIVATE NON-FINAL** — many of them `java.util.function.*`, i.e. exactly the κ-covered owners this row is about. The precondition is common; what is rare is a write-set the taint contract accepts. | **NOT closeable by relaxing the κ exclusion, and that exclusion is CORRECT at the CHA site:** publishing under `java/lang/Runnable.run()V` would charge one library's effectful `Runnable` onto every consumer's `r.run()`. Closing it needs **the FIELD's own identity as the join key** (`lib3/Jdk#hook`) — a new obligation, not a wider spelling of an existing one. **Worth asking of the other three engines before it is designed (§F):** the mechanism is java-specific (an early `return` in `virtualDispatch`), but the QUESTION — *does a locally-resolved dispatch skip the obligation-1 recording?* — translates directly and nobody has asked it. |
| R603 swift: **⟨0.39⟩ OBLIGATION 2's OWNER VOTE COUNTED MODULES WHERE THE THING IT ASSIGNS IS A PACKAGE.** `abstractionOwnerPkg` took `Set(files.compactMap { foreignOwnerModule($0) })` under a `count == 1` never-guess gate. **The gate is right; the UNIT was wrong** — a consumer conforming to one dependency's protocol across two files importing two modules OF THAT SAME PACKAGE (`import NIOTLS` here, `import NIOFoundationCompat` there, both `swift-nio`) read as two owners and the entry was DROPPED. [[R565]] at the one site R565 did not reach. | 2026-09-25 | swift — CLOSED, candor-swift `c31fef8` | **FOUND BY FIXING [[R592]], which created it:** un-suppressing `NIOSSL/NIOSSLHandler.swift` gave the vote two modules of one package, and **36 correct, joinable `swift-nio#ChannelInboundHandler.*` union entries carrying `Env`/`Net`/`Fs`/`Unknown` VANISHED** from the chained report. All 36 restored by counting the vote in packages; nothing lost. **MEASURED on nio-ssl + its 6 resolved checkouts, dep index held constant, HEAD `96211f0` -> R592+R603: ADDED 213, REMOVED 278, CHANGED 135**, REACH 1 hit. **Removal set audited IN FULL: all 278 are `CNIOBoringSSL#` union entries** naming nio-ssl's own C shim as the owner of `Equatable`/`Hashable`/`CustomStringConvertible` — unjoinable in both directions. Union rows 334 -> 267 with **0 `swift-nio#` entries lost**. **INERT where `modulePkgs` is empty** (`pkgOfModule` falls back to the module name), so the 11-package unresolved A/B is 0/0/0 — which is exactly why the reach probe exists: it separates inert from unreached. | `Deps.chainedPkgs` already carried the identical rationale for the JOIN half, so this is two implementations of one question that drifted (§F1.3). Fixture `UnionOwnerPackageVoteProcessTests`, two arms differing only in whether the modules share a package: the defect arm goes RED on revert, and **the two-different-packages never-guess CONTROL stays green in both arms** — which is what proves the fix did not collapse the rule it sits under. |
| R604 swift: **⟨0.39⟩ OBLIGATION 2 PUBLISHES STDLIB MARKER PROTOCOLS AS FOREIGN ABSTRACTIONS — `swift-nio#Sendable.*` — WHILE THE SIBLING DISPATCH SITE ALREADY CARVES THEM OUT.** `Driver.dispatchAbstraction` refuses a bound in `STD_PURE_PROTOCOLS` (`Sendable`, `Hashable`, `Equatable`, `CustomStringConvertible`, `Error`, `RawRepresentable`); the `abstractionOwnerPkg` loop that keys `interfaceUnion` entries, twelve hundred lines away, does not consult it. | 2026-09-25 | swift — OPEN, PRE-EXISTING; REDUCED but not closed by [[R592]]/[[R603]] | **MEASURED over 11 real packages: 983 foreign-keyed `interfaceUnion` entries at `96211f0`, of which 931 (94.7%) NAME A STDLIB PROTOCOL** — `CNIOLinux#CustomStringConvertible` 453, `CNIOBoringSSL#Sendable` 137, `NIOTLS#Sendable` 106, `CNIOBoringSSL#Hashable` 54, `CNIOLinux#RawRepresentable` 51, `CNIOBoringSSL#Equatable` 46, `CNIOLinux#Comparable` 29. **After R592+R603: 323 entries, of which 243 (75.2%) remain, all `NIOTLS#Sendable.*`.** So the own-C-target fix removed 688 of them INCIDENTALLY and left the class open one carve-out away. Chained arm (nio-ssl): 281 of 334 union rows stdlib-keyed before, 144 of 267 after — and the remainder moved from `CNIOBoringSSL` to `swift-nio`, **a differently-wrong owner, not a fixed one.** | **NOT FIXED, and the reason is the direction.** The fix is a `STD_PURE_PROTOCOLS` consult at the union-owner site, but that is **REMOVAL-ONLY** — 243 published rows in one corpus package — and a removal here means a chained consumer stops seeing an entry. It needs its own full removal audit plus an over-charge control proving a genuine foreign protocol that happens to share a stdlib NAME is not dropped. **Worth settling BEFORE the ⟨0.39⟩ (the clause once drafted as 0.40) port:** [[R599]] records that swift's post-[[R565]] measurement has 13 of 18 hits explained by [[R567]]'s malformed keys, and this is the same population — so swift's ⟨0.39⟩ (the clause once drafted as 0.40) price is still being taken over keys that name abstractions the named package does not have. |
| R605 rust: **THE SAME `{ty}::{method}` CONFUSION AT THE DISPATCH SITE, WHERE IT LANDS ON A REAL GATING ROW RATHER THAN A SYNTHETIC ONE.** Two sites, one spelling: `collector.rs:2370` (`dispatch_calls_for_trait_method`, `path: format!("{ty}::{leaf}")` for every entry in `trait_impls[tr]`) and its Pass B twin `scan.rs:~2788` (`by_tail2[format!("{imp_leaf}::{}", c.leaf)]`). **This is [[R598]]'s class one level worse: R598 put the fabrication on a synthetic `interfaceUnion` row; this puts it on a concrete caller's row WITH A `loc`.** | 2026-09-25 | rust — OPEN, PRE-EXISTING | **Measured on a compiling fixture, and the gate direction executed: `deny Log` exits 1 where the truth is UNREACHABLE.** `pub fn via_dyn(e: &dyn Enc) -> u8 { e.to_bytes() }` reports `inferred:["Log"], calls:["RData::to_bytes"]` — where `to_bytes` is a PROVIDED member, `impl Enc for RData` declares `emit` alone, and `RData::to_bytes` is a private INHERENT method that no dispatch through that member can reach. | **NOT FIXED, and the index it needs ALREADY EXISTS** — `impl_members` plus `model::impl_does_not_declare`, both added for [[R598]] in `d449ee5`. It was deliberately not applied here because **dropping a dispatch edge is the REMOVAL direction on real gating rows** and needs its own A/B and over-charge controls. And [[R570]] is what makes the delegation hedge necessary: without it the same 9-of-14 trap R598 hit would apply here. |
| R606 rust: **[[R598]]'s FIX DELIBERATELY RETAINS A NARROWED CANDIDATE'S `Unknown`, AND 5 OF THOSE ARE KNOWN-FABRICATED.** The channel rule passes a non-declaring implementor's `Unknown` and `invisible` through unchanged while withholding its concrete effects. | 2026-09-25 | rust — OPEN BY DECISION, measured and priced | **Measured over 1,626 crates in the same A/B that closed [[R598]]:** `axum#handler::Handler::{layer,with_state}` (two versions) and `sea_orm#entity::base_entity::EntityName::table_ref` (two versions) still publish an `Unknown` sourced from a method no dispatch through that member reaches — axum's inherent `MethodRouter::layer` (`routing/method_routing.rs:967`), and sea-orm's `dynamic::Entity::table_ref`, **a DIFFERENT TYPE under the merged leaf `Entity`**. `deny Unknown` does fire on these 5, so it is not cost-free. | **Open by decision, and the decision is priced: an over-report is not the cardinal sin and a silence is.** Withholding the `Unknown` as well removed **9 REACHABLE disclosures** on snapbox/diesel in the same corpus — the trait's default body delegates to a same-named method the resolver drops. Narrowing this safely needs **[[R570]] closed first**: a UFCS call to a trait member reads ABSENT, which is exactly why those default bodies publish nothing and why the hedge has to exist. |
| R607 rust: **A CONSUMER-SUPPLIED IMPLEMENTOR REACHED THROUGH A MUTABLE GLOBAL DOES NOT REACH THE CONSUMER'S ROW — DISCLOSED, NOT SILENT.** [[R595]]'s class asked of all four engines as PART 92's `c13_reassigned_field_hook`: the library declares a hook with a PURE default and invokes it; the consumer supplies an EFFECTFUL callable through the global and calls the library entry. | 2026-09-25 | rust — OPEN; a ⟨0.39⟩ COMPLETENESS gap, NOT a cardinal sin | **MEASURED FOUR-WAY IN ONE PART RUN — java `eff=['Net']`, swift `eff=['Net'] unknown=True`, ts `eff=['Net'] unknown=True`, rust `eff=∅ unknown=True`.** So three engines carry the consumer's own effect and rust does not. **NOTE WHAT THIS IS NOT: `unknown=True` means rust DISCLOSES.** Under ⟨0.35⟩'s disjunction — complete the dispatch OR disclose it, only silence is forbidden — rust is CONFORMANT here. What it fails is ⟨0.39⟩'s COMPLETENESS obligation: *a chained consumer's inherited signature must carry the effects of every implementor visible to the consumer*, and the consumer supplied this one itself. **Recorded at its real weight: materially weaker than [[R595]], which was silent and shipped.** Also visible on the wire in the same run: **java alone publishes `dispatchesOn=1` for this shape** (R595's fix), while rust, swift and ts all publish `dispatchesOn=0` — so swift and ts reach the effect by a route that does not involve naming the member. | **Each language spells the mutable global differently and the SHAPE is what the arm holds constant, not the syntax** — java a `public static` non-final field, swift a `public static var`, ts a property on an exported object (ES module bindings are read-only to importers), rust a `OnceLock` (no safe mutable static exists, so set-once by a consumer is the closest faithful spelling). rust's fixture is `pub static AFTER_STAGE: OnceLock<fn()>` with a `noop` default, set by the consumer to an effectful `fn`. **The arm is the deliverable as much as the finding:** this question was going to be three agent tasks asking rust, swift and ts whether they had java's sin; as a generated arm it answers all four in one `conformance/part.sh 92` and keeps answering on every future run. That is the family's own rule — the row before the port — applied to a cross-engine question rather than to a rung. |
| R608 rust: **A CONSUMER DISPATCHING ON A CHAINED DEPENDENCY'S ABSTRACTION WITH AN EMPTY IMPLEMENTOR UNION READ SILENTLY PURE** — rust's half of [[R533]]/⟨0.39⟩ (the clause once drafted as 0.40). `pub fn run(h: &dyn dep::Handler) { h.handle(); }` with nothing implementing `Handler` anywhere gave `inferred: []`, `unresolved: false`, no `unknownWhy`, `dispatchesOn` present, and `deny Net Unknown` **exit 0**. | 2026-09-25 | rust — CLOSED, candor-rust `e4808bf`. **THE FIRST ENGINE TO CLOSE ⟨0.39⟩ (the clause once drafted as 0.40)'s consumer half.** | **Ported at TRAIT-PREFIX conjunct-4 granularity, and that choice is the whole row.** java's `chaTargets(owner,name,desc)` is ALSO a member query — what makes it behave as an abstraction query is that its `name+desc` comes off an `INVOKEINTERFACE`, **verifier-guaranteed to name a member the interface declares**. rust mints its key from SOURCE, where nothing guarantees that, so a member-keyed port cannot fire on an [[R549]]-malformed key and charges `Unknown` for a dispatch that does not exist ([[R599]]). **Six chained pairs, Σ 2,826 analyzed consumer functions, `REMOVED 0` in EVERY arm: member-keyed 39 rows (1.38%) of which 34 of 35 hits are MALFORMED; trait-prefix 6 rows (0.21%); with [[R609]]'s producer half, 2 rows (0.07%), and BOTH of those are malformed too.** **So on this corpus the rung's OWN price is 0 and what remains is R549 leaking in.** The predicted hit landed and was then correctly retired: `tower#discover::D::poll_discover` on `futures_core#TryStream::try_poll_next` (`src/discover/mod.rs:92`) is one of the 6 — and the producer half REMOVES it, because futures-core's blanket `impl TryStream for S` then publishes its pure union and the hedge is no longer needed. Ships under the standing rule at 0.21%/0.07% with REMOVED 0. | **THE TWO HALVES ARE NOT SEPARABLE TO SHIP, and this was not in the brief — it was measured.** On a consumer-only binary PART 92's `c3_pure_only` shape (`&dyn dep::Handler` where the dep's ONLY implementor is pure) **exits 1 with `inferred: ['Unknown']`** — a FABRICATION, which is exactly what that control exists to catch. With [[R609]]'s publication it is pure again. That is the strongest argument for ⟨0.39⟩ (the clause once drafted as 0.40) being ONE clause with two obligations. **Also corrected: the member-keyed figure does not reproduce at this base** — 39 rows / 35 hits, not the 45 measured at `0c0a0fd`, because [[R569]] landed in between; the trait-prefix figure did not move. **The §4 exempt-leaf skip is measured INERT at prefix granularity** (six pairs byte-identical, compared file by file) and is credited with none of the reduction — calibrated where it CAN fire, removing exactly 7 `tower_service#Service::clone` hits on the member-keyed arm. Retires PART 92's `c9_consumer_zero_union` xfail for rust, the first of the three. |
| R609 rust: **THE PRODUCER DROPPED A UNION ENTRY WHOSE IMPLEMENTORS ARE ALL PURE, so "every implementor is pure" and "nothing implements this" are THE SAME BYTES ON THE WIRE.** Two sites in `scan.rs`, both commented `silence = purity`. | 2026-09-25 | rust — CLOSED, candor-rust `e4808bf`; ⟨0.39⟩ (the clause once drafted as 0.40)'s PRODUCER half | **That ambiguity is WHY [[R608]] must hedge on wire absence — and on its own R608 FABRICATES:** `c3_pure_only` exits 1 with a spurious `Unknown`. **1,626 registry crates, 327,920 pre rows: ADDED 12,704, REMOVED 0, CHANGED 0**, reach 12,704 hits across **850 of 1,626 entries** — and `ADDED == REACH` exactly. **All 12,704 added rows checked: every one a pure-only `interfaceUnion` row, ZERO concrete effects added anywhere.** Pure-only share of published union entries **332/394 (84.3%)** — the same direction as java's 73.4%, which is why publishing them makes the consumer rule fire on strictly FEWER sites rather than more. | **A stated claim in the brief was FALSE and the agent said so rather than inheriting it: "REMOVED 0 by construction" is wrong.** A new key in a consumer's `deps_idx.by_key` can set `dep_join_hit`, which `already_handled` reads — so a call that previously fell through to the R452/R190(e)/R128 disclosures could stop disclosing. It measured 0 in both corpora; **that is a MEASUREMENT, not a construction**, and the code now says so. A zero-implementor abstraction still publishes nothing, which is what keeps [[R608]]'s conjunct meaningful. **Two pre-existing [[R598]] tests went red and were CHANGED deliberately** — they asserted *the key publishes nothing*, which is precisely what this rung changes; they now assert *the key publishes no CONCRETE effect*, and the discrimination was measured identical (under `CANDOR_R598_PRE=1` both rows read `["Fs"]`). |
| R610 swift: **A BINDING LAUNDERS THE OUTER-BASE GUESS THAT [[R567]](a) REFUSES — `vars` records the TYPE and not the fact that it was a CONVENTION.** `rootOf` keeps the outer base's type through an unexplained `.member` hop (the κ static-chain idiom rides on that); R567(a) made the §2 key site refuse it via `opaqueHop`; and the six binder arms that type a name from a `rootOf` answer DROPPED THE FLAG. | 2026-09-25 | swift — CLOSED, candor-swift `8067f4a`. FOUND ON REAL CODE while re-pricing ⟨0.39⟩ (the clause once drafted as 0.40). | **One variable — the receiver's SPELLING, with dep source, consumer body, sink and binary all held:** `c.loop.spin()` gives `['Unknown']`; `if let l = c.maybeLoop { l.spin() }` gives **`['Fs']`** — the decoy `Channel.spin` — under key `RatesDep#Channel.spin`, and **`deny Fs` exits 1 over code that opens no file.** Real site: nio-ssl `NIOSSLHandler.swift:631` published `swift-nio#ChannelHandlerContext.getOption`, a member that class does not declare. A/B in isolation: ADDED 0, REMOVED 0, CHANGED 62, **0 effects moved**, 1 key / 62 occurrences removed, reach 52 -> 96. | **The first patch MISSED one of the six arms (`visitPost(OptionalBindingConditionSyntax)`) and the FIXTURE caught it** — the arm still read `['Fs']`. `opaqueVars` is dropped with `vars` in `clearBindingTypeOnly`, and `NameKeyedStateTests` caught the new property unclassified on its first run; it now records the disposition AND the direction it fails in — a stale entry OVER-hedges, where a stale `vars` entry FABRICATES. Carries a §1b kill switch (`CANDOR_R610_OFF`) shown RED under degradation with controls green in both arms. |
| R611 swift: **A MEMBER THE CONSUMER DECLARES IN ITS OWN `extension` OF A FOREIGN TYPE IS PUBLISHED UNDER THE DEPENDENCY'S PACKAGE — [[R555]]'s class for an EXTENSION MEMBER rather than a PROTOCOL.** Doubly wrong: the owner is the receiver's CONCRETE type rather than the protocol, and the package is the DEPENDENCY rather than the declarer. | 2026-09-25 | swift — OPEN; 3 of the 30 surviving ⟨0.39⟩ (the clause once drafted as 0.40) prototype hits, and part of a family of 7 | nio-http2 declares `func configureHTTP2Pipeline(...)` in `extension Channel` (`Sources/NIOHTTP2/HTTP2PipelineHelpers.swift:91/107`); swift-nio declares no such member. The consumer publishes **`swift-nio#EmbeddedChannel.configureHTTP2Pipeline`** — a key no consumer can join, naming a member the package does not have (the `DepLib#String.lowercased` class). **Measured: 3 occurrences on nio-http2, and they are 3 of the 30 hits surviving R567+R610.** **Same family, also unfixed and also in the surviving 30:** `HTTPClientRequestPart.head`/`.end` and `TLSUserEvent.handshakeCompleted` are enum CASES spelled as dispatches (3 hits), and `MultiThreadedEventLoopGroup.syncShutdownGracefully` is declared on the `EventLoopGroup` PROTOCOL EXTENSION while the consumer keys the concrete type (1 hit) — **7 of 30 malformed in total.** | [[R555]] closed this for a LOCALLY-DECLARED PROTOCOL via `localProtocolWirePath`; the extension-member case is unfixed. **R555's own comment already priced the naive widening** — dropping the `foreignOwnerModule` trigger costs 18 further sites, 10 new rows and 4 keys naming members the protocol lacks — and deferred it pending a requirement gate and a removal audit. That pricing still stands. |
| R612 all engines: **THE `dispatchesOn` WIRE VALUE IS MUTUALLY UNJOINABLE ACROSS ENGINES, AND NO CONFORMANCE PART COMPARES IT.** java writes `iface/Backend.size()I` (JVM internal name + descriptor), rust `iface#backend::Backend::size`, ts `iface#Backend.size`, swift `Pkg#Owner.leaf`. Each is locally consistent — which is all ⟨0.39⟩ asks — so nothing is violated and nothing catches it. | 2026-09-25 | all engines — OPEN as a CROSS-ENGINE question; reported by the swift lane, not yet measured end to end | **Live for any cross-engine chain**: a java producer's key cannot be joined by a rust consumer, and vice versa. `gen_chained_dispatch.py:65-70` **declines to assert the wire shape BY DESIGN** — PART 92 asserts at the consumer VERDICT, not on key spellings — so the suite is green over it and always has been. ⟨0.39⟩ obligation 2 pins only PACKAGE QUALIFICATION, not the member spelling, and SPEC §2 rule 1 leaves key spelling per-language (*derivable from both sides*). So this is not a violation of any clause; it is a clause nobody wrote. | **Not yet measured, and the measurement is the first step, not the fix.** The question to answer before any clause: does a REAL cross-engine chain occur in practice — a java library consumed by a ts application, a rust crate consumed from swift — or is the family's chaining de facto single-engine? If it is single-engine today, this is a latent design gap and the cheap remedy is a PART that asserts the spellings AGREE for one shape, so the divergence cannot widen silently. If cross-engine chains are real, it is a live silent under-report: the consumer's join misses and ⟨0.21⟩ then reads the row as pure. **Note the shape — this is the [[R565]]/[[R599]] family one level up.** R565 was module-vs-package WITHIN swift; R599 was a conjunct copied across engines that asked a narrower question in rust; this is the KEY ITSELF differing across engines with each engine internally consistent. The recurring lesson is that per-engine consistency is not cross-engine agreement, and only a PART can tell them apart. |
| R613 ts: **THE CONSUMER-SITE PURE-ONLY UNION IS HEDGED WHERE THE ANSWER IS KNOWABLE.** A consumer dispatching on a chained dependency whose ONLY implementor anywhere is PURE reads `Unknown` in ts, where java, rust and swift all read pure. An OVER-disclosure, not a silence — recorded at that weight. | 2026-09-25 | ts — OPEN; a PRECISION defect, NOT a soundness one | **Found by PART 92's new `c12_consumer_pure_only_union` arm on its FIRST run, which also corrected MY expectation:** I declared the arm xfail four-way believing every engine would hedge on wire absence, and the suite reported `XFAIL ARM PASSED` for java, rust and swift. ts reads `eff=∅ unknown=True` where the arm wants `unknown=False`. **java and swift pass with `interfaceUnion=absent`** — they resolve the dependency's single pure implementor through the dep's own row rather than through a union entry. **rust passes with `interfaceUnion=present`, which is [[R609]]'s producer half**, shipped the same night, doing exactly what it was built for. | **Direction matters and this is the safe one:** ts hedges where it could resolve, so it over-reports rather than certifying something it cannot see. That is the opposite of the cardinal sin and it is why this row is filed as a precision defect rather than a sin. It does cost something real — an entry-scoped `deny E Unknown` fires where the truth is pure — so it is not free, just not dangerous. **The arm is the finding as much as the defect:** `c3_pure_only` puts the dispatch in the DEPENDENCY, where the dep's own CHA resolves it; c12 moves it to the CONSUMER's body, the site ⟨0.39⟩ (the clause once drafted as 0.40) actually governs, and that one knob separates three engines from one. |
| R614 swift: **THE METATYPE ITERATOR ELEMENT WAS THE SPELLING REAL CODE USES, AND THE NINE-BINDER ENUMERATION DID NOT CONTAIN IT.** [[R585]] enumerated nine binders and closed all nine — including b6, a `for t in ts` over `[X.Type]`. **On 15 real packages that arm got ZERO hits while the corpus declared 34 `[X.Type]` types.** The idiom is `let validators: [ParsableArgumentsValidator.Type] = […]` followed by `validators.map { v in v.validate() }`. | 2026-09-25 | swift — CLOSED, candor-swift `828f2b5`; filed because the LESSON outlives the fix | swift-argument-parser's own validator list, plus `commandStack.map { $0._commandName }`. **Both the named and the `$0` spellings were ABSENT over a body that dials.** Fixed by asking `metatypeElementOf` from `typeClosureParams` for the same eight iterator methods the `for` binder already uses: **reach went 57 -> 81 hits and the A/B ADDED 16 -> 19.** **Found by a SOURCE CENSUS contradicting the probe** — the enumeration said b6 covered containers, the probe said b6 never fired, and the census said the corpus is full of `[X.Type]`. Two of those three had to be reconciled. | **AN ENUMERATION IS AN AUDIT BOUNDARY TOO, and §9 applies to the list of shapes you WROTE DOWN, not only to the instance you were handed.** [[R585]]'s nine were drawn around PLAUSIBLE spellings rather than MEASURED ones — on 15 real packages b2/b3/b5/b6/b7/b8 got zero hits, the dominant real instance was b4 in its **computed**-property form (`static var asCommand: ParsableCommand.Type`), and the idiomatic container form was this one, which no row of the table covered. **So "close all nine and R585 is closed" would ITSELF have been the audit-boundary failure the brief warned against** — the brief applied §9 to landing 2 of 9, one level below where it actually bit. The generalisation: when a fix is driven by an enumeration, the enumeration needs its own reach measurement before the fix is called complete. |
| R615 swift: **A METATYPE READ OUT OF A CONTAINER IS STILL SILENT, ONE HOP PAST [[R585]].** R585 closed nine binders plus the iterator element ([[R614]]); a metatype reached through a CONTAINER READ is not followed. | 2026-09-25 | swift — OPEN; the stated residual of [[R585]] | **Executable repro that RUNS and prints `SINK` ×5.** With `let all: [RB.Type] = [RS.self]` where `RS.go` dials: `for t in all { t.go() }` charges `['Net']` (the control), while **`if let t = all.first { t.go() }`, `for t in all.filter({_ in true}) { t.go() }`, `all[0].go()` and `if let t = byName["a"] { t.go() }` (a `[String: RB.Type]`) are ALL ABSENT.** Gate direction executed: `deny Net viaFirst` **exit 0** and `pure viaFirst` **exit 0**, against `deny Net direct` exit 1. | **NOT FIXED, and the reason is §F1.3.** `elementTypeOf` already answers these for ordinary element types by recursing through an index the metatype side does not have. **A second partial copy of that walk would be the two-implementations-of-one-question shape** — which is precisely how [[R592]]/[[R603]] and [[R599]] happened. So the fix is to give the metatype indexes the same accessor/transform/subscript/dict-value coverage in ONE resolver, not to special-case `.first`. Recorded as R585's residual in `AGENTS.md`, `CHANGELOG.md` and at `metatypeElementOf`'s doc comment, so it is not discoverable only from this register. |
| R616 swift: **~~[[R567]](b)'s~~ THE BARE-LEAF KEY FOR A METHOD MAKES `depShadows` TRUE FOR A METHOD NAME, SUPPRESSING THE kappa FREE-CALL TABLE.** **ATTRIBUTION CORRECTED 2026-09-25, and the correction is the finding: this is NOT [[R567]](b)'s doing.** `Deps.swift:721` mints `pkg#<leaf>` for EVERY entry, a method's leaf included, and has since the three-shape key set existed; `8931087` added only the DE-SUFFIXED spelling, widening the same hole to OVERLOADED members. **The remedy this row first proposed — restrict the widening to `segs.count == 1` — closes the OVERLOADED arm and leaves the single-signature arm, the common case, exit 0. A half-fix that reads as a fix.** Measured under `CANDOR_R567B_OFF=1`: the single-signature arm is exit 0 there too. **Superseded by [[R649]], which closed it.** Before R567(b) the bare-leaf key `pkg#<leaf>` was minted ONLY for a free function (`segs.count == 1`); after it, every overloaded METHOD leaf files under one too, so `swift-nio#ClientBootstrap.connect(String,Int)` also files as `swift-nio#connect`. | 2026-09-25 | swift — **CLOSED 2026-09-25 by [[R649]]**, candor-swift `483d6ad`. **The attribution in this row was WRONG and the correction cost nothing only because the fix lane was told to attack it:** this was NOT introduced by `8931087`, it is as old as the three-shape key set, and `8931087` widened it to overloaded members. Found by adversarial code review (analysis-only), re-dated by measurement | `depShadows` (`CallCollector.swift:1910-1916`) uses **key EXISTENCE** as the predicate *this name is declared by a chained dependency, so the platform kappa table must not fire*. Two consumers: `CallCollector.swift:5100`, where a true answer **suppresses the kappa free-call effect entirely**, and `:4298`, gating `zipElementSlots`. kappa free names that are also ordinary Swift method leaves include **`connect`, `write`, `File`, `Folder`, `Date`, `UUID`, `Process`, `Pipe`, `FileHandle`, `fopen`, `sendmsg`** — so a file importing a chained `swift-nio` and calling the POSIX free `connect(fd, &addr, len)` has its `Net` suppressed by the mere existence of `ClientBootstrap.connect`'s bare key. **Worse than a plain silence: the unqualified-call join at `Driver.swift:3099-3101` finds that same key and attaches the METHOD's effects instead of kappa's answer — a fabrication masking an under-report.** | **Sec K in its purest form.** The commit's safety sentence — *Purely ADDITIVE, `insert` UNIONS and never withdraws, so no key that worked can stop working* — **is true ABOUT THE INDEX, and is the exact sentence that stopped the consumer side being asked.** `insert` never withdraws; `depShadows` withdraws a kappa classification on a MEMBERSHIP TEST, not on a value. **Remedy:** restrict the bare-leaf widening to `segs.count == 1` as before and give methods only the bare TAIL2 and FULL shapes — all R567(b)'s own measurement needs; no consumer wants the bare-leaf METHOD key. **Fixture:** a dep declaring two `Chan.connect` overloads; a consumer importing it plus `Glibc` calling 3-arg POSIX `connect`. One overload -> `['Net']` exit 1; two overloads -> `Net` gone; rename control restores it. Blast radius UNMEASURED — needs a probe at `CallCollector.swift:1914` and a build. |
| R617 swift: **[[R610]]'s BOUNDARY STOPS ONE BINDER SHORT — the SINGLETON-ACCESSOR binders launder the same convention, and the two spellings now give OPPOSITE VERDICTS.** R610 flagged seven `vars[name] = <rootOf answer>` sites; it did not touch the three that type a name from `Type.singleton` WITHOUT going through `rootOf` at all (`CallCollector.swift:7304-7331`, `DeclCollector.swift:648-655`, `:1201-1212`), which read `SINGLETON_ACCESSORS` directly. | 2026-09-25 | swift — OPEN; [[R610]]'s own defect statement, one binder over | One variable, the receiver's SPELLING. `DepType.shared.foo()` gives `rootOf -> opaqueHop TRUE`, R567(a) refuses, owner nil, **DISCLOSES `Unknown`**. `let s = DepType.shared; s.foo()` writes `vars["s"]` with **no `opaqueVars`**, so `rootOf` reports `opaqueHop FALSE` and it **publishes `Dep#DepType.foo` and joins its effects**. `private let session = URLSession.shared` is the idiom `DeclCollector:1201`'s own comment calls *the dominant way an Apple-platform type holds a system service*. **Both directions, and they cannot both be right:** if R567(a)'s rule holds, the bound spelling FABRICATES a key and SUPPRESSES the disclosure R567(a) added; if the singleton convention is trustworthy, R567(a) OVER-REFUSES the direct spelling and removed correct keys among its 37 of 99. | **Why the A/B could not see it:** R610's diff is `dispatchesOn keys REMOVED: 1 distinct` — the singleton-bound keys were never candidates for removal, so **a zero there is not evidence**. [[R610]]'s stated property — *the flag travels with the binding so the two spellings can never disagree* — does not cover this binder, because the type never came from `rootOf`. Fixture: `Chan.shared.spin()` vs `let c = Chan.shared; c.spin()`, same dep, same sink; the two arms must agree. |
| R618 swift: **`opaqueHop` IS HARD-CODED `false` ON TWO `rootOf` ARMS AND IS CARRIED BY NO CONTAINER INDEX — three spellings of one question, one answered and two not.** `CallCollector.swift:1156-1158` (call-shaped element accessor) and `:1162-1170` (subscript / dict subscript) both `return (…, false)`, while their MemberAccess twin at `:1047` correctly returns `inner.opaqueHop`; and `elementTypeOf`'s field arm (`:2917-2920`) reads `rootOf(base).root` and DISCARDS the flag. | 2026-09-25 | swift — OPEN; [[R567]](a)'s class in the spellings its own commit claimed to cover | `c.loop.items.first?.spin()` -> opaqueHop TRUE, refused. `c.loop.items[0].spin()`, `c.loop.items.popLast()!.spin()` and `let ws = c.loop.items; for w in ws { w.spin() }` -> **FALSE, key published.** The commit states the flag is *inherited forward by the field walk, the element accessor, `type(of:)` and (disjunctively) the ternary join* — the field walk, the `.value` arm, `type(of:)` and the ternary all check out, but **the element accessor is true of exactly ONE of its three arms.** `arrayElem`/`dictElem`/`arrayElemNested` have no guess-flag companion at all (`opaqueElem` tracks `mono`, a different fact). | Direction: **fabrication + silent under-report**, the same pair [[R567]](a) measured, reachable wherever the outer base is a local type declaring a same-named `[Foreign]` field. Sec F1.3 and Sec 9. **Reachability on real code NOT established** — the review proved the arms disagree, not that a corpus hits them; the experiment is a counter in `elementTypeOf`'s `fieldArrayElem` arm printing when `rootOf(base).opaqueHop` is true. |
| R619 swift: **[[R585]]'s b4 AND b9 ARE CLOSED FOR THE CLASS HALF IN EVERY SPELLING AND FOR THE PROTOCOL HALF ONLY IN THE BINDER SPELLING.** `CallCollector.swift:1026` (`fieldMetatypes[rt]?[member]`, the EXPLICIT-receiver reading of b4) and `:1104` (`metatypeReturns[n]`, the DIRECT reading of b9) both gate on `localTypes` with **no `localProtocols` twin**, while their binder-side counterparts route through `metatypeBinder` -> `typeReceiverProto`, which does answer protocols. | 2026-09-25 | swift — OPEN; a residual of [[R585]], and a second instance of [[R614]]'s lesson | Direction: **silent under-report** — no `ProtoDispatch` is appended, the bounded conformer CHA never runs, and a conformer performing `Net` is not charged; the receiver keeps the outer root (now `opaqueHop` true) so it does not even disclose locally. **R585's fixture pins only the BINDER spellings and says so in its own comments** (`// 4 — a stored PROPERTY of metatype type, read through implicit self`). So the commit's *NINE BINDERS x BOTH HALVES = EIGHTEEN ARMS* is an enumeration over BINDER KINDS, not over SPELLINGS — **which is exactly how [[R614]]'s tenth spelling was missed the first time.** | **The fix is cheap and lands on machinery that already exists:** `CallCollector.swift:5379` already turns a protocol root into a `ProtoDispatch`, so relaxing `:1026`/`:1104` to `localTypes.contains(mb) \|\| localProtocols.contains(mb)` reaches it. Fixture: add `b4pExplicit`, `b4pSelf`, `b9pDirect` beside the existing arms, with the CLASS twins as the discriminating control — those pass today. |
| R620 swift: **[[R585]]'s TWO CLOSURE-PARAMETER METATYPE ARMS SET `metatypeBinders` WITHOUT CLEARING `vars`, so the arm is inert precisely under SHADOWING.** `CallCollector.swift:4010-4013` and `:4536-4539` call `scopeBindingType`/`snapshotType` — which SNAPSHOT, they do not clear — then write `metatypeBinders[p.name]`; and `metatypeBinder`'s first guard is `vars[spelling] == nil`. | 2026-09-25 | swift — OPEN as a RESIDUAL, not a regression | The sibling ANNOTATED arm (`:3997-4000`) is safe only because `vars[p.name] = p.annotated` OVERWRITES; the metatype arm has no equivalent, and the catch-all `else { clearBindingTypeOnly(p.name) }` at `:4590` is in a branch these two never take. So `func f(_ t: Deleter) { xs.forEach { (t: CBase.Type) in t.validate() } }` resolves the closure's `t` against **`Deleter`**, not `CBase`. **NOT A REGRESSION** — pre-R585 the closure parameter was untyped and `vars` won the same way. Both arms are also recorded by the commit as ZERO-HIT / safety-only, so nothing on the corpus would show it. | **Sec K one round after [[R124]] was found in the same function for the same reason:** the comment at `:4007-4009` asserts the guard is safe having reasoned about exactly ONE source of `vars[t]` (the annotated loop) and not about an enclosing binding of the same name. Fixture is R124's own shape, metatyped, with a rename control. |
| R621 swift: **`pkgOfModule`'s FALLBACK PUTS MODULE NAMES AND PACKAGE NAMES IN ONE NAMESPACE, and [[R603]] counts `count == 1` over that MIXED namespace.** `Deps.swift:231` is `modulePkgs[m] ?? m`, feeding `Driver.swift:1887-1897`. | 2026-09-25 | swift — OPEN, LOW LIKELIHOOD, filed because the audit boundary was drawn around the empty case | **The review confirmed the fallback is inert for a fully-empty index and NOT wrong for two fully-unmapped modules** (they fall back to distinct names, `count == 2`, the never-guess rule fires); `dependencyModulePackages` also refuses a module two packages claim and drops the `p == m` identity case. **The hole is the PARTIALLY-POPULATED index, which neither commit prices:** module `M` unmapped -> `"M"`, while module `MCore` maps to a package whose `Package(name:)` is literally `"M"` -> `"M"`. Two genuinely different owners collapse to one, `count == 1` passes, and obligation 2's union entry is published into the wrong package's namespace, where a consumer of that package can join it. **Direction: fabrication.** `Deps.chainedPkgs` (`:242-252`) has the identical exposure on the join side. | **Sec 9:** R603's stated boundary — *NOTE WHERE THIS CANNOT FIRE, deliberately: in an UNCHAINED scan, `pkgs == mods`* — is drawn around the EMPTY index, and the mixed one is the untested middle. Needs a dependency package name equal to an unmapped module name in the same file, so likelihood is low; filed so the next audit does not re-derive it. |
| R622 java: **[[R131]]'s `Unknown` IS THE ONLY REASONLESS `dir.add(Effect.UNKNOWN)` IN THE ENGINE — all 21 sites enumerated, 20 record an `UnknownReason` on the adjacent line and R131's is the twenty-first.** `Candor.java:4415-4416` unions into `dir` and leaves `effect == null`; the only tagger of a classifier-derived Unknown, `effectMetadata` (`Candor.java:5263-5265`), is gated on `effect`. | 2026-09-25 | java — **FIXED 2026-09-25**, candor-java `448dad8` (11 non-comment lines), **BUT THE LANDING IS BLOCKED ON A RULING: it is a GATE RELAXATION — see [[R674]]** | `Policy.gateInputFromScan` (`Policy.java:478-489`) builds `reasonClassDirect` SOLELY from `ctx().unknownWhy`; `Policy.reasonClassesOf` (`:549-562`) returns `{UNRESOLVED}` on an empty token set and **only the parsed tokens otherwise**. So: **`deny Unknown[reflect]` fires on `FilterInputStream.read` and NOT on `DataInputStream.read`** — one syscall, two spellings, two verdicts, the exact subject of this repo's own `SecondSpellingRouteTest`. And **`deny Unknown[unresolved]` misses the R131 hole entirely whenever the unit carries any other tagged Unknown**, because the token set is then `{dispatch}` and non-empty — falsifying `Policy.java:769-772`'s stated invariant on the scan route. `candor blindspots` will not name **any** of the commit's own measured **1,784 rows that gained Unknown**. | Direction: **fail-OPEN for a reason-scoped gate**, plus a NEW blind area in the disclosure channel. Not a bare-`deny Unknown` sin — R131 genuinely closes that. Fix: record `UnknownReason.of(Kind.REFLECT, …)` (or a new kind naming the modelled supertype) inside the R131 loop when `se == Effect.UNKNOWN`. Fixture: `d.read()` on `DataInputStream` vs on `FilterInputStream` in one class; predicted `[reflect]` flags only the second and `[unresolved]` only the first. |
| R623 java: **[[R131]]'s `Fs` CHARGES BYPASS BOTH FAIL-CLOSED LOCATOR GUARDS, so a BENIGN SIBLING LITERAL CERTIFIES THEM.** Same root as [[R622]] (`effect == null`), different consumer: `extractLiteralSurfaces` is called with `effect == null` (`Candor.java:5529`) and both Fs masking guards are gated on `effect == Effect.FS` — the path-ctor capture (`:5431-5440`) and **[[R421]]'s stat-locator rung (`:5581-5584`)**. | 2026-09-25 | java — **FIXED 2026-09-25**, candor-java `448dad8` (11 non-comment lines), **BUT THE LANDING IS BLOCKED ON A RULING: it is a GATE RELAXATION — see [[R674]]** | An Fs the walk charges produces **no `paths` entry and no `incomplete: ["Fs"]` marker**. On the commit's own ground-truthed instance: under the direct spelling `ResourceBundle.getBundle` it reaches `:5581` and either captures the literal or discloses; under the R131 spelling it reaches **neither**. Direction: **fail-OPEN under a locator-scoped `allow`** — and it is also a NEW PARTIAL POSITIVE CLAIM, because pre-R131 the row said nothing and post-R131 it says *this function does Fs, and here are its paths* with an incomplete path set and no hedge. Fixture: `new File("allowed.txt").exists()` beside `PropertyResourceBundle.getBundle("secret.config")`; predicted `paths:["allowed.txt"]`, `incomplete` ABSENT, `allow Fs allowed.txt` exit **0**, with the `ResourceBundle` spelling as the exit-1 control. | **Sec K — the commit message's and the code comment's safety sentence is wrong in substance.** *Leaving `effect` null costs the literal surface … and buys a change that cannot subtract. Nearly everything it adds is `Unknown`, which has no literal surface anyway.* It names the one PRECISION cost and omits **three FAIL-CLOSED guards** ([[R622]], this row, [[R624]]). *Nearly everything it adds is Unknown* is load-bearing — and the commit itself measured **24 Fs rows** and ground-truthed them with `javap`. Those 24 are exactly the rows the sentence waves away. **Evidence gap (Sec E1):** the A/B reported `CHANGED 3648` wide against `~1502` on `inferred`; **2,146 rows changed on non-`inferred` fields and were never broken down** — `incomplete`, `fs` and `unknownWhy` all live in that number. |
| R624 java: **[[R131]]'s `Fs` NEVER POISONS `fsDirect`, so the report can claim WRITES BUT NEVER READS over a read it just charged.** `Candor.java:5266-5277` — whose own comment records paying for this exact defect once (conformance PART 31, `mixed()` reported `fs:["write"]`) — is gated on `effect == Effect.FS`, and `fsFixpoint()` (`:8655-8659`) seeds ONLY from `fsDirect` and `viaCross`. | 2026-09-25 | java — **FIXED 2026-09-25**, candor-java `448dad8` (11 non-comment lines), **BUT THE LANDING IS BLOCKED ON A RULING: it is a GATE RELAXATION — see [[R674]]** | R131's Fs lands in `s.dir` — neither seed — so it **abstains, which is precisely what that comment forbids**: *A verb that reveals no direction must POISON the fixpoint, not merely abstain.* Fixture: a method calling `new FileWriter("out.txt").write("x")` AND `PropertyResourceBundle.getBundle("msgs")` should report `fs:["write"]` while `inferred` carries the Fs of a **read**; the `ResourceBundle` spelling is the control that drops `fs` to `[]`. | Fix for [[R622]]/[[R623]]/this row is one change: **route R131's charge through the same three guards the classified path uses**, or give it its own `effect`-equivalent so the guards can see it. Filed as three rows because the three consumers fail in different directions and can be fixed and gated independently. |
| R625 java: **[[R595]]'s CLAIM *no site loses an effect or gains an Unknown* IS FALSE FOR A CONSUMER WITH A BROAD IMPLEMENTOR FAN-OUT, and that cost was not measured.** The javadoc is precise (`Candor.java:5977`: *nothing THIS ENGINE RESOLVES becomes an Unknown*); the commit sentence is not. | 2026-09-25 | java — OPEN as a NAMED RESIDUAL, fail-CLOSED direction (over-disclosure), not a sin | `Candor.java:6822-6829`, CONTRIBUTOR 2 of the join the new key feeds: a newly published `dispatchesOn` key reaching a consumer with **more than `CHA_FANOUT_LIMIT` (12) visible implementors** of that SAM produces `viaCross += Unknown` plus a `dispatch:` reason the consumer did not have before. **The A/B was 150 STANDALONE producer-side library scans**, so it could not see this; the commit names the standalone-vs-chained residual but names it as *the shape may not be rare in real chains*, not as this specific cost. | Direction is over-disclosure, so this is a residual rather than a defect — filed because it is the claim's counterexample and belongs in the row rather than being rediscovered. **Also recorded from the same review:** [[R601]]'s residual **cannot** be closed by relaxing the kappa exclusion, and the review found a SECOND independent mechanism the row does not name — even a one-sided relaxation fabricates through CONTRIBUTOR 2, because `chaTargets("java/lang/Runnable","run","()V")` plus `samLambdaImplementors` in the CONSUMER's own scan is every thread body and every Runnable lambda in the app. **The kappa bound is load-bearing on both sides independently.** |
| R626 java: **THE OWNER-BLANKET DENYLIST PROBE IS SHORT-CIRCUITED BY `isPureHandleAccessor`, WHICH `Classifier.classify` CHECKS FIRST** (`Classifier.java:164-168`), so the first `startsWith("get")`-style carve-out written into it would silently switch the [[R131]] denylist OFF for exactly the owners that have carve-outs — which are exactly the blanket-with-exemption owners it targets. | 2026-09-25 | java — OPEN as a LATENT structural hazard; **SAFE TODAY, verified** | Verified safe at HEAD: `isPureHandleAccessor` (`Candor.java:9293+`) and `isPureSocketHandleState`/`isPureSocketOptionAccessor`/`isPureTlsHandshakeConfig` (`:9207/9243/9278`) are all exact `switch`/`case` or `method.equals`; `Classifier.java` has **zero** `method.contains(` hits and one `charAt(0)`/`isLowerCase` hit, on an owner segment. The `\u0000`-bearing `BLANKET_PROBE` defeats every `equals`, `startsWith` and `endsWith`. **The hazard is that nothing goes red when that changes.** Cheap gate: pin the probe's two directions — `isOwnerBlanketRule("java.net.ServerSocket", "()I")` true and `isOwnerBlanketRule("java.io.FilterInputStream", "()I")` false. | **The probe's OTHER direction, which the commit does not state:** a DESC-ONLY rule (`Classifier.java:738-739`, `1315-1316`, `1345-1346`, `2202-2203` — `if (desc.startsWith("(Ljava/io/File;")) return Effect.FS;` with no name test) reads as BLANKET to the probe, so the walk declines it. Conservative (no fabrication) but it means **R131 is inert over that whole rule family**, and the commit states only the prefix/suffix residual, which errs the other way. |
| R627 spec: **`conformance/must-ledger.json:3326` ASSERTS *two engines can no longer satisfy PART 92 with incompatible keys*. THEY CAN, AND ALL FOUR DO.** That sentence retires the stated price of the [[R612]] hole, and it is false on the evidence. | 2026-09-25 | candor-spec — OPEN; a Sec K claim of correctness that SUPPRESSED the measurement | PART 92's `judge()` (`gen_chained_dispatch.py:953-975`) reads only the consumer row's `eff`/`unknown`/`invisible` and **never touches a key string**; `dispatchesOn` appears in the harness once, as a printed counter (`:923-938`, *printed, never asserted*); and each arm is rendered into a per-engine subtree (`:1001`), so **every chain is single-engine by construction.** **[[R612]]'s divergence is confirmed exactly as filed** — java `iface/Backend.size()I`, rust `iface#backend::Backend::size`, ts `iface#Backend.size`, swift `Pkg#Owner.leaf` — with the write sites verified at `Candor.java:9086-9087`, `scan.rs:3984`, `scan.mjs:5737`, `Driver.swift:3215`. | **Three corrections to [[R612]] itself.** (1) **No engine joins a foreign key today, but the masking is INCIDENTAL, not designed:** every engine reads `dispatchesOn` only inside the `!stale` branch of the Sec 2.1 version check, and the version string carries the ENGINE IDENTITY, so a foreign report is ALWAYS stale and the row is downgraded to `Unknown` (verified in java at `Loader.java:831-836`). **The day the family adopts a shared spec-version staleness test, all four joins go live simultaneously with four incompatible key spaces, and the failure mode is *silence IS purity*.** (2) **R612's own excuse is contradicted by the clause it cites:** `SPEC.md:4423-4429` says verbatim *`dispatchesOn`'s VALUE takes the same spelling rule as obligation 2's key … A consumer MUST be able to join a `dispatchesOn` value against the producing package's entry keys WITHOUT A PER-ENGINE RULE* — so *each is locally consistent, which is all ⟨0.39⟩ asks* must be struck. (3) **The one non-version-gated cross-engine input path is the `.hierarchy.json` sidecar** (`Loader.java:702-705` -> `:416-461`), routed BEFORE any staleness check; it carries supertype lists into CONTRIBUTOR 2 of the join, not member keys, so it does not transport a key today — but it is the place to watch. **Remedy: a conformance PART asserting the spellings agree for one shape, landed together with striking the retired-price sentence.** |
| R628 rust: **[[R609]] PUBLISHES A PURE UNION ROW FOR A TRAIT WITH ZERO REAL IMPLEMENTORS whenever a same-LEAF foreign or std trait is implemented in the crate — breaking the exact invariant [[R608]]'s conjunct 3 rests on.** `trait_impls` is keyed by trait LEAF and records impls of FOREIGN and std traits too (`decls.rs:3276`, no locality check), so a local trait `L` with zero implementors gets a non-empty `impls` vector the moment the crate writes `impl <anything else>::L for Y`. | 2026-09-25 | rust — **CLOSED 2026-09-25 by [[R652]]**, candor-rust `17c3f9e`. Was: INTRODUCED by `e4808bf`, CARDINAL SIN on the wire at the one key ⟨0.39⟩ obligation 3 tells a consumer to join. **Confirmed live in the wild before the fix: serde's `mod std_error { pub trait Error }` — zero implementors, leaf shared with `de::Error`/`ser::Error` — published `Error::source` as a bare purity claim in SEVEN corpus crates** | `lt.count > 1` does NOT guard it: `trait_decls` counts only local `Item::Trait` declarations and the foreign trait contributes none, so `count == 1` and the ambiguity refusal never fires. The loop unions over `Y::method` for the LOCAL trait's methods, which resolve to nothing: `inf_u = {}`, `blind_u = {}`, `impls = ["Y"]`. Pre-`e4808bf` that hit the old `if inf_u.is_empty() && blind_u.is_empty() { continue }`; **post-`e4808bf` (`scan.rs:4640`) it publishes `crate#L::method` with `inferred: []`, `unresolved: false`, no `invisible` — a Sec 2 purity claim about an abstraction that has no implementor at all.** Downstream, that row lands in the consumer's `deps_idx.by_key`, so **R608's conjunct 3 (`scan.rs:3872`) sees the key as ANSWERED and the `Unknown` is never charged.** R609 was meant to make *all implementors pure* distinguishable from *nothing implements this*; on this path it makes the second look like the first. | **Sec A.2 / Sec 9 in the review's own terms:** arm (b) of R609's new test is the exact shape the author had in mind, and the blind spot of that shape is the next instance. **Fixture needs no dependency, one crate:** a local `pub trait Write { fn emit(&self) -> String; }` with ZERO implementors, plus `impl std::io::Write for W`. Expected: no row at `<crate>#Write::emit`. Predicted: a row with `interfaceUnion: true`, `inferred: []`. The colliding-leaf shape is ordinary — `Write`, `Read`, `Error`, `Display`, `Iterator`, `Default`, `From`, `Service`, `Handler`; a crate declaring its own `Service`/`Handler` abstraction while implementing `tower::Service`/`axum::Handler` is the everyday instance. |
| R629 rust: **[[R576]](b)'s `else` ARM DOES NOT COVER THE FALL-THROUGH PASS A's `_` ARM EXISTS FOR, AND THE PROBE THAT WAS SUPPOSED TO PRICE IT COUNTS A DIFFERENT SET.** The `scan.rs:2824` site reaches its arms only INSIDE `if let Some(impls) = trait_impls.get(&t_type)`; the `None` branch — precisely Pass A's *no impl visible* — emits **no edge, no `Unknown`, no reason**. | 2026-09-25 | rust — OPEN; Sec K, and the probe is Sec 9 | Pass A's authority (`collector.rs:2382`) is `match … { Some(impls) if impls.len() <= 12 => …, _ => mark_unresolved("dispatch:{tr}.{leaf}") }`, and its own comment says the `_` arm covers **`None` AND `len > 12`**. The scan.rs site covers only the second. **And `R576EMPTY` is placed inside `Some(impls)`, so the claim *the `R576EMPTY` probe counts it: over 400 registry crates it fires 0 times* counts *implementors exist but none has a matching method* — it CANNOT count *no implementor exists at all*, which is the half Pass A names.** Reachability is not hypothetical: `self` inside a trait DEFAULT BODY is seeded into `vars` as the trait leaf (`decls.rs:1817` via `decls.rs:186`), not into `trait_vars`, so `dispatch_calls_for_trait_method` is never consulted and `resolvable` is satisfied by the default body's own `local_types`. | **Fixture is one line on a test that already exists:** `a_wide_implementor_set_discloses…` (`src/tests.rs:10551`) has an `fn src(n: usize)` generator; `src(0)` yields `pub trait Sink { fn sink(&self); fn drive(&self) { self.sink(); } }`. Predicted: `Sink::drive` **absent from `functions[]`** — the same Sec 2 rule-3 purity claim R576(b) closed at 15 implementors, still open at 0. Contrast `a_zero_implementor_dispatch_discloses_dispatch_not_callback` (`:22500`), which DOES disclose for the `&dyn Store` parameter spelling of the same trait and method. **Same trait, same member, two receiver spellings, two answers** (Sec F1 q4). Note the near-miss the other way: the DELIBERATE non-widening at `hits.is_empty()` looks right, and it is what made the gap look measured. |
| R630 rust: **`r533_exempt_leaf` IS A LEAF-ONLY ALLOWLIST NARROWING A SOUND OVER-APPROXIMATION, ON A PROPERTY RUST DOES NOT GUARANTEE.** `scan.rs:3952` matches bare leaves `clone\|fmt\|eq\|ne\|hash\|cmp\|partial_cmp\|to_string`, justified as *mirroring java, whose test is `(name, desc)` and never the owner*. | 2026-09-25 | rust — OPEN; Sec F1 q5, and the denylist-over-allowlist rule | **java's four names are Object-protocol members with FIXED DESCRIPTORS. In rust none of these eight names is reserved** — any trait may declare `hash`, `fmt`, `cmp`, `to_string` or `eq` as a genuinely effectful requirement. **The commit already found this exact hazard for one name** (`call`, excluded because `tower_service#Service::call` is real and effectful) **and then stopped at the one name it tripped over — Sec 9, an audit boundary drawn around its own trigger, in the same paragraph that names the risk.** The cost side is stated in the source: *MEASURED INERT ON THE PRICING CORPUS — it is not credited with any reduction*, so the skip currently buys nothing measured and pays with a silent channel. | **The abstraction key is right there:** `member` is `{owner}#{trait qual}::{method}`, so the sound spelling exempts on the **(trait qual, leaf) PAIR** (`core::fmt::Debug::fmt`, `core::hash::Hash::hash`, …) — a denylist of known-conventional abstractions rather than an allowlist of bare names. Fixture: dep `pub trait Digest { fn hash(&self) -> String; }` with no implementor; consumer `fn run(d: &dyn dep::Digest) { d.hash(); }` chained under `deny Net Unknown`. R608's other three conjuncts pass, so the exempt list alone decides; predicted `inferred: []` exit 0, against an exit-1 control spelled `fn digest(&self)`. |
| R631 rust: **[[R569]] CHANGED PASS B WITHOUT BUMPING THE CACHE REV, so a warm `--incremental` cache serves the pre-fix silence — the exact class R569 closes.** `cf8782d` adds an `Expr::Reference` arm (`collector.rs:5537`) that writes `self.vars` and therefore changes the `calls` list stored in `FnInfo`; `FnInfo` is cached (`cache.rs:1502`) and invalidated only by `content_hash`, `decl_index_hash` and `cache_schema()`, none of which R569 changes. | 2026-09-25 | rust — **CLOSED 2026-09-25**, candor-rust: **rev42** for R569's Pass B change, then **rev43** for [[R652]]. **The A/B-contamination worry was MEASURED AND REFUTED, which is why it was asked first:** `bin/corpus-ab.py` does not pass `--incremental` and cannot introduce it (it runs the argv templates it is given and sets no cache env), AND the flag is **opt-in for both READ and WRITE** — `scan.rs:964` *a full scan (no flag) never reads the cache*, and the write-back at `scan.rs:1745` sits inside `if incremental`. `cf8782d`'s arms were plain `--json` scans; the *one target dir* in its message is the BUILD cache, not the scan cache. **REACH 3,829 stands as measured; nothing needed re-deriving.** Found while fixing: the schema test's stale list was missing **rev34–rev42** entirely | `d449ee5` correctly bumped rev40->rev41 for `impl_members` one commit earlier, and the file's own precedent is explicit that this is required for exactly this shape — rev10 (*FnInfo gained `dispatch` … exactly the silent under-report this field exists to close*) and rev37/rev31 (*the FIELD is unchanged, its CONTENT is wider*). **R569 is the rev31/rev37 shape.** The window is a same-version binary swap with `--incremental`, which is the whole dev cycle and every A/B arm that reuses one tree. | **ACTION BEYOND THE FIX: check whether `bin/corpus-ab.py`'s arms pass `--incremental`.** If they do, `cf8782d`'s pre/post arms shared a cache directory and **the REACH 3,829 figure needs re-deriving.** Fixture: run `--incremental` at `d449ee5`, then the same command at `cf8782d` against the same tree; the second should differ by 91 added rows on the registry corpus and is predicted byte-identical. Remedy: `rev42`, with the rev-comment stating the stale direction is SILENCE, not fabrication. |
| R632 rust: **[[R608]]'s CONJUNCT 4 ASKS *declares no MEMBER*, NOT *supplies no IMPLEMENTOR* — and that is the Sec F1 q7 answer: `impl_members` and `collect_foreign_trait_impls` are TWO implementations of one question that have ALREADY DRIFTED in the dimension that matters.** `collect_foreign_trait_impls` (`lang.rs:3561`) mints a key only per `syn::ImplItem::Fn`, with no *block seen* key and no opaque marker. | 2026-09-25 | rust — OPEN; OVER-REPORT / precision direction, plus a Sec K statement mismatch | Three shapes produce **no key at all**, so conjunct 4 charges `Unknown` even though the crate really does implement the abstraction: (1) `impl dep::Tr for X {}` with every member taken from defaults — **which R608's own R598 audit names as the ordinary case** (*every `impl QueryDsl for X {}` block is EMPTY*); (2) `impl dep::Tr for X { forward_all!(); }`, a macro body, which `collect_local_impl_members` marks `impl_opaque_key` and the foreign walk does not; (3) a block-nested impl, recorded in `nested_impl_foreign`, which conjunct 4 does not consult (harmless in practice — the R529 loop charges `Unknown` separately). | **`impl_members` was built THREE COMMITS EARLIER precisely because *a narrowing over an incomplete index fails in the SILENT direction*, and therefore needs seen/member/opaque (`model.rs:558`). R608's conjunct 4 is a narrowing over `foreign_impls`, which has only the member shape — the design lesson was recorded and not carried across.** Here it fails in the OVER-CHARGE direction rather than the silent one, which is why it is a precision row and not a sin. **The stated semantics in the commit message and the source banner — *this crate supplies no implementor of that abstraction* — is not what the code tests.** Fixture: clone `r608_conjunct_4_is_asked_of_the_abstraction_not_of_the_member` with the consumer's impl block emptied to `impl r608two::Handler for Mine {}`; the existing test asserts exit 0 for the DECLARING spelling, predicted exit 1 here. Remedy: give `collect_foreign_trait_impls` a seen key and an opaque key, read through one `impl_does_not_declare`-style predicate. |
| R633 rust: **`r533_fi_prefix` SKIPS THE MANIFEST FILTER THAT `foreign_impls`' OWN EMISSION SITE APPLIES, so the safety comment asserting inertness is now false.** `collect_foreign_trait_impls`'s doc (`lang.rs:3529`) says a local module spelled like a crate *is filtered at emission against the manifest's real dependency set … a bogus key here is inert wire noise* — true when written (`scan.rs:4801`, `scan.rs:4056` both filter), but `r533_fi_prefix` and `r533_fi_prefix_t2` apply **no such filter**. | 2026-09-25 | rust — OPEN, VERY NARROW; filed for the stale safety comment (Sec K) more than the reach | A local `mod tokio { pub trait Handler { … } }` plus `impl tokio::Handler for X { fn handle(&self) {} }` mints the prefix `tokio#Handler::`, which would suppress [[R608]] for a genuine dispatch on the real `tokio`'s same-named trait. Direction: silent suppression. Contrived — but **the comment asserting inertness is the sentence that would stop anyone checking**, which is the whole of [[feedback-documented-limitation-is-not-measured]]'s third variant: a comment TRUE WHEN WRITTEN that expired because the thing it points at moved. | Remedy is one line: apply the same `deps.contains(owner)` filter at the prefix-set construction that the emission site already applies. |
| R634 rust: **`union_adds_over_row`'s `covered` SHORT-CIRCUIT WITHHOLDS CONCRETE UNION EFFECTS BEHIND A BARE `Unknown`, and the comment calling those cases *not losses* is stronger than what holds.** `scan.rs:4014` — when the real row at the hash carries `Unknown`, the union's concrete effects are withheld entirely. | 2026-09-25 | rust — OPEN as a PRECISION / gate-shape row; DISCLOSED, not silent, so not a sin | The comment says *`Unknown` on the real row COVERS any effect, which is why those cases are not losses* — **true for `deny Fs Unknown`, false for a bare `deny Fs`**, which reads `inferred` and does not treat `Unknown` as an `Fs`. So a policy denying `Fs` without denying `Unknown` passes where the union knows a real implementor performs `Fs`. | Noted because it is load-bearing for [[R597]]'s *exact set arithmetic* claim. The honest wording is *not a loss FOR ANY POLICY FORM THAT DENIES `Unknown`*. |
| R635 instrument (candor-spec): **`soundness-status.py` COULD NOT SEE A DECLARED `OPEN` THAT FOLLOWS AN ENGINE PREFIX — AND THAT BECAME THE FILING CONVENTION THE SAME DAY.** `_DECLARED_OPEN` was anchored to the head of the status cell; every row filed 2026-09-25 heads it `<engine> — OPEN …`. **THE FIFTH DEFECT OF THIS SHAPE IN `bucket()`.** | 2026-09-25 | instrument (candor-spec) — **CLOSED 2026-09-25**, candor-spec `PENDING`, with the widening MEASURED and seven selftest cases in the new convention | Five rows — **R600, R604, R606, R611, R615** — bucketed `closed-with-fix`/`resolved-no-fix` and were absent from `--ids` (88 printed, should be 93). The anchor missed, inference took over, and any closure word anywhere in the row won — **including ANOTHER ROW'S closure**: R611 on *[[R555]] closed this*, R615 on *R585 closed nine binders*. R604 lost on `fixed` inside *not **a** fixed one*, where the ARTICLE defeats `_NEGATED_CLOSURE`; R604 and R615 additionally open their OUTCOME cell `**NOT FIXED**` and were outranked by the inference. Direction: **UNDER-REPORT of the shipping-defect list — the register's own cardinal-sin shape, in the instrument that exists to print it.** | **Fix, and why it is bounded:** the anchor now admits a prefix of at most 70 characters ending in a dash, with the OPEN required IMMEDIATELY after it — so `swift — CLOSED, …` cannot match and a prose *open question* 80 characters in is still out of reach. **Measured before landing: flips exactly those five rows and no others** (88 -> 93, zero lost). The rejected alternative was hoisting `declared_not_fixed`, which [[R588]]/[[R591]] already priced at **143 false OPENs**. **Why `--selftest` could not have caught it: every case in the suite was written in the OLD convention, so the tool and its calibration agreed about a convention they both assumed.** Seven cases added in the new one, including the two near-misses that bound the widening. **The general lesson: the head anchor was never wrong about prose — it encoded a CONVENTION, and a convention is a habit of whoever is filing that day, not a property of the document. An instrument keyed to a habit goes quietly wrong the day the habit improves.** |
| R636 instrument (candor-spec): **PART 92's `judge()` ACCEPTS AN ABSENT CONSUMER ROW AS A PASS FOR `c3_pure_only` AND `c12_consumer_pure_only_union` — an UNCONDITIONAL PASS in the arm written to close the gap `c3` left open.** `facts()` renders a missing entry as `eff=∅, unknown=False, absent=True`, and its own docstring calls that *a POSITIVE purity claim … conflating the two is the shape of the sin itself*. | 2026-09-25 | instrument (candor-spec) — **OPEN ONLY ON THE FOUR-WAY RUN.** The instrument defect is FIXED and unit-calibrated (see the remedy cell and [[R677]]); what is outstanding is `conformance/run.sh`, which says which cells flip. **attack B, in a conformance arm** | The reviewer drove every arm's `want` against an absent row: **11 of 13 catch it** (c1, c2, c4, c5, c6, c7, c8, c9, c10, c11, c13); **`c3_pure_only` and `c12_consumer_pure_only_union` PASS.** `c12`'s `hasnt={Net}` is additionally **VACUOUS BY CONSTRUCTION** — there is no `SINK` anywhere in its fixture (iface `impl` has none, app `nesteddispatch` has none, `third=False`), so the arm's only discriminating assertion is `unknown=False`, which absence gives for free. **`c12`'s current green for java, rust and swift does not distinguish *reads the dep's one pure implementor correctly* from *produced no row for `appSize` at all*.** | **Sec 9 with a twist: `c12` was written BECAUSE *the middle case `c3_pure_only` does not pin, and the gap is one knob wide* — and it reproduced `c3`'s unfalsifiable half.** One-line remedy: have `judge()` reject `f["absent"]` unless an arm explicitly opts in. **Landing it needs a conformance run**, because it may flip `c3`/`c12` cells that are currently green for the wrong reason — which is the point. **CLOSED 2026-09-25 in `candor-spec`, and the boundary this row drew was too narrow — see [[R677]], which measures the same vacuity in FOUR arms and carries the fix, its three-way calibration and the flips to expect. The conformance run is still outstanding: it was deliberately NOT started, because three engine lanes had dirty trees and `conformance/run.sh` reads trees.** |
| R637 instrument (candor-spec): **THE FOUR `c13_reassigned_field_hook` TEMPLATES DO NOT HOLD THE SHAPE CONSTANT, AND [[R607]] IS MEASURED ACROSS THE DIFFERENCE.** The arm's comment claims *the SHAPE is what is held constant, not the syntax*. It is not. | 2026-09-25 | instrument (candor-spec) — OPEN; **Sec 4, two variables at once** | java's hook is a **nominal SAM interface with a MEMBER** (`Hook { void run(); }`, called as `afterStage.run()`); ts's is `type Hook = () => void` and swift's is `() -> Void` — **anonymous function types with no member**; rust's is a **function POINTER** in a `OnceLock<fn()>`. **⟨0.39⟩ obligation 1 is defined over a dispatched MEMBER, so only java's arm expresses it.** Separately, rust's consumer supplies a **named top-level `fn sink`** while the other three write the effectful callable **lexically inside the measured entry `appRun`** — so for ts and swift the arm cannot distinguish a hook join from ordinary containment. **R607's verdict — *rust is the only engine that does not COMPLETE* — is measured on a fixture where rust is being asked a value-provenance question through a global**, which is [[candor-value-provenance]] Sec 2 territory, not this rung's. | **The reviewer tried to break this and half of it broke, which is why the row is narrower than the first hypothesis.** The first claim was that java/ts/swift all pass by containment alone; **[[R595]]'s own row refutes that for java** — *the consumer's lambda is its own row and the caller is never judged* — so java's `appRun` genuinely needs the hook join and java's cell is real. For ts and swift it could not be established without running the engines, **and the arm as written cannot tell the two apart either.** **The file already knows the fix:** `c8` deliberately splits into `appSize` + `appSupply` *so containment charges the SUPPLIER* and measures the ENTRY; `c13` measures `appRun`, the function the effectful closure is written inside. Remedy: a single-method NOMINAL abstraction in all four (`trait Hook { fn run(&self); }` behind `OnceLock<Box<dyn Hook>>`, `protocol Hook`, `interface Hook`) plus the `c8` two-function split. **[[R607]] should not be treated as a measured four-way divergence until then.** |
| R638 instrument (candor-spec): **`sha-citations.py`'s `TARGETS` BOUNDARY IS DRAWN AROUND ITS OWN TRIGGER — 253 DEAD CITATIONS SIT OUTSIDE IT, INCLUDING 25 IN `SPEC.md`, THE NORMATIVE DOCUMENT.** The tool reports `795 citation(s) checked, 0 dead` and exits 0. | 2026-09-25 | instrument (candor-spec) — OPEN; **Sec 9, in the instrument built to answer Sec 9's question** | Running the tool's OWN `resolves()` over every backticked sha in candor-spec outside `TARGETS`, excluding `CHANGELOG.md` (stated reason) and its `NOT_SHAS`/`LOST`: **411 distinct tokens, 253 DEAD** — 189 in `SCAN-BOUNDARY-WORK-QUEUE.md`, **71 in `conformance/run.sh`** (the four-way suite's own evidence comments), **25 in `SPEC.md`**, 17 in the scan-boundary VEIN document, and 18 across four design docs. **`SPEC.md:1051` cites all four engines' commits for a clause — 7378f4f, 27f4beb, a034371, 8584572 (spelled WITHOUT backticks here on purpose — backticking them makes this row cite them, and `sha-citations --apply` would then rewrite them to live shas and invert the finding) — and NONE of them resolves.** (8 of the 253 are `soundness-status.py` selftest literals such as abc1234; subtract those, the rest are genuine.) | **And today's own work is in the uncovered set:** `conformance/gen_chained_dispatch.py` cites `e4808bf`, `d17dc66`, `5c53e96`, `e82e00f`, `ee844f0`, `df4cf3f`, `2078e08` as the evidence that each xfail retirement was EARNED — **and nothing checks any of them.** Remedy: widen `TARGETS` to every tracked `.md` and the instrument sources, keeping `CHANGELOG.md`'s exclusion and its stated reason. |
| R639 instrument (candor-spec): **THE [[R600]] EPHEMERAL-CITATION ADVISORY NEVER RUNS, ANYWHERE, AND DOES NOT SAY THAT IT CANNOT LOOK.** `grep -rn CANDOR_SCRATCH` across both repos returns **only the two lines inside the function itself** — nothing sets it, not `doc-gates.sh`, not CI, not any shell profile. | 2026-09-25 | instrument (candor-spec) — OPEN; **attack H, and the SECOND vacuous shipping of this same check in two days** | Measured: with `CANDOR_SCRATCH` set to any existing dir it reports `(32 total, 32 dead)`; **unset — which is every real run — it returns `(0, 0, [])` and the `if _d:` block never fires.** **Deleting `ephemeral_citations` entirely changes no output anywhere** (attack C). The fix for *reports 32 of 32 dead because it looked nowhere* produced *reports nothing at all, ever, and does not say it could not look* — **the worse of the two**, because the first was at least visible. | Two further problems even once it is wired: the answer is a function of **WHICH SESSION RUNS IT** (a fresh session reports everything dead by construction), so the docstring's *33 citations, 26 dead* is not reproducible; and `(0,0,[])` is **indistinguishable in the caller from *all live***. Remedy: return a three-state answer and print the UNKNOWN state, or delete the check and keep only the convention it was written to enforce — **put the decisive artefact IN the row; a path is a courtesy, never the evidence.** |
| R640 instrument (candor-spec): **`check_soundness_tables.py` IS BLIND TO A ROW WITH TWO OR MORE LEADING EMPTY CELLS, OR A BOLDED ID — ALL THREE PROPERTIES SKIP IT.** `row_id` (`check_soundness_tables.py:63-76`) reads only `cells[:2]`. | 2026-09-25 | instrument (candor-spec) — **CLOSED 2026-09-25**, with the tool's first selftest; **Sec 9, one constructor over from the instance the 2026-09-21 fix was written for** | A 7-cell row in a 5-column table **that is also a duplicate id** is reported GREEN: `\|\|\| R900 a duplicate hiding behind two empty leading cells \| … \|` -> `check_soundness_tables: OK`, rc=0. **Control: the single `\|\| R900` spelling the 2026-09-21 fix was written for correctly FAILS (rc=1).** A bolded id escapes the same way: `\| **R900** … \|` is never seen by the cell-count property. The comment at `:49-59` says *a row is therefore identified by its ID appearing as the first NON-EMPTY cell*; **it is actually identified by the id appearing in the first or second cell.** Direction: flattering, silent. | Remedy: scan for the id in the first non-empty cell whatever its index, and strip bold before matching. **Retro-rediscovery (attack I): the new duplicate-id property WOULD have caught the two rows numbered R540 on 2026-09-24** (verified on a constructed fixture); `\|\|\|` and `\| **R524**` still escape. **CLOSED 2026-09-25 by the [[R641]] shared recogniser, which implements exactly that remedy** — first NON-EMPTY cell whatever its index, `[\s*_~]*` stripped before the match, letter suffix kept. **CALIBRATED IN BOTH DIRECTIONS against the PRE-fix tool on the same two fixtures: `\|\|\| R900 …` (a duplicate id behind two empty cells) — pre `OK, rc=0`, post `FAILED — 1 row(s) do not carry their table's cell count, rc=1`; `\| **R900** …` — pre `OK, rc=0`, post `FAILED — 1 row id(s) used more than once, rc=1`.** The tool also gains its FIRST selftest (`--selftest`, six end-to-end fixtures plus the shared recogniser's 18 cases), wired into both `scripts/doc-gates.sh` and the conformance workflow — it had run in CI for its whole life with nothing calibrating it, which is how it was measured vacuous twice in four days. |
| R641 instrument (candor-spec): **THE TWO TOOLS THAT PARSE THE REGISTER DISAGREE ABOUT WHAT A REGISTER ROW IS, AND ONLY ONE OF THEM WAS HARDENED.** `check_soundness_tables.py` was taught the malformed spellings on 2026-09-21; `soundness-status.py:240` still uses `^\\| ~?~?R\d+`. | 2026-09-25 | instrument (candor-spec) — **CLOSED 2026-09-25**; it was FIVE implementations of one question, not two; **Sec F1 q3** | Measured: `'\| R524 x \\| … \\|'` is seen (True); **`'\\|\\| R524 x \\| … \\|'` and `'\\| \\| R524 x \\| … \\|'` are NOT (False).** A `\\|\\| R524` row **vanishes from the counts, from the OPEN list and from `--ids` entirely** — the shipping-defect list silently one row short, which is this instrument's cardinal sin. Mitigated today only because the OTHER tool would go red on cell count — **and only `check_soundness_tables.py` is in CI** ([[R643]]). | Remedy: one shared row-recogniser, imported by both, rather than two regexes that drift. Pairs with [[R640]] — fix them together or the shared recogniser inherits the `cells[:2]` bound. **CLOSED 2026-09-25 — `scripts/soundness_row.py`, imported by both, with ONE case table that both selftests run.** There were FIVE copies of the question, not two: `check_soundness_tables.row_id` (tolerant of leading empty cells, DROPS the letter suffix), its `full_id` (a second scan added because the first drops it), and three `^\| ~?~?R\d+` sites in `soundness-status.py`. **END-TO-END CALIBRATION, not a unit test:** injecting `\|\| R9001 …` into a copy of the real register, the PRE-fix `soundness-status.py --ids` prints it **0** times and the post-fix one **1**. On the unpoisoned register the two are BYTE-IDENTICAL (578 rows, 134 open), so the widening is inert on real content. A malformed row is now SEEN rather than dropped even though its columns are shifted and `bucket()` may read the wrong one — **wrong bucket is loud, vanished is the under-report this tool exists to prevent**, and `check_soundness_tables.py` fails on the same row, which is what stops it being committed. |
| R642 instrument (candor-spec): **TWO OF `soundness-status.py`'s GUARDS ARE RUBBER-STAMPABLE WITH THE SELFTEST GREEN — INCLUDING [[R591]]'s OWN FIX, WHICH HAS ZERO CALIBRATION.** Replacing each guard with a never-matching pattern: six of eight are caught by `--selftest`; **`_PARTIAL_BEFORE` and `_NOT_A_VERDICT` are NOT.** | 2026-09-25 | instrument (candor-spec) — OPEN; **attack C, and the check that shipped vacuous yesterday is still uncalibrated today** | `_PARTIAL_BEFORE` (`:101`) and the whole `_body_closure`/SAYS BOTH path (`:107-114`, `:288-301`) have **no selftest case at all**. And `_PARTIAL_BEFORE` is fragile in the NOISY direction — it fires only when the partial word touches `CLOSED` within 24 characters. Measured on six realistic partial spellings, **four produce a FALSE SAYS BOTH**: *THE SWIFT HALF IS CLOSED*, *HALF of this row is NOW CLOSED*, *PARTLY — only the rust arm is CLOSED* (over 24 chars of prose), *PARTLY: CLOSED* (a colon). **That is the failure [[R591]]'s own comment calls the worse one — *a reader who checks three and finds them all fine stops reading the fourth*.** On the live register it currently suppresses exactly 4 (R198, R209, R524, R576) and **all four are correct.** | Also uncalibrated: `_NOT_A_VERDICT` (the [[R520]] guard). Its discriminating case is one line and absent — a row whose evidence cell reads *The engine must fail closed here.* followed by a bare sha buckets `cites-a-sha-only` with the guard and `closed-with-fix` without it. **Separately measured and worth recording so nobody 'fixes' it: `_SAYS_OPEN_TOO` is INERT FOR THREE OF ITS SIX CLOSURE WORDS** — when `_closure_head` suppresses, control falls to `:197` where `CLOSURE.search` re-reads the WHOLE line, and `CLOSED`/`FIXED`/`WITHDRAWN` are in both regexes, so the guard survives only for `RESOLVED`/`RETRACTED`/`SUPERSEDED`. **Do NOT widen it naively: 54 rows carry a closure head plus the word OPEN, and 53 are legitimately closed** (*Originally filed: OPEN*, *STATUS CORRECTED … this cell said OPEN*). The discriminator must be the narrative framing, not the token. |
| R643 instrument (candor-spec): **EIGHT OF `doc-gates.sh`'s NINE GATES ARE IN NO CI WORKFLOW, so `bin/gates.sh candor-spec` — which reads CI `run:` steps — WILL NEVER PRINT THEM.** Only `check_soundness_tables.py` runs in CI (`conformance.yml:64`). | 2026-09-25 | instrument (candor-spec) — OPEN; **the mechanism `doc-gates.sh`'s own header was written to fix, one level out** | `sha-citations.py --check`, `soundness-status.py --selftest` and `doc-gates.sh` itself appear in **no workflow**. And **neither `check_soundness_tables.py` nor `sha-citations.py` has a selftest or a fixture** — their only input is the live, currently-clean documents, so **attack B on both is trivial: replace the body with `sys.exit(0)` and every gate stays green.** Only `soundness-status.py` can answer attack B, via `--selftest`, and that covers `bucket()` only: `main()`, the table detection, `_idkey`, `_body_closure` and the SAYS BOTH report are **untested and unrun by any gate**. | This is the same shape as the per-repo gate-list rule in CLAUDE.md: **a gate that is not in the printed list is a gate nobody runs**, and the coordinator's habit of running `doc-gates.sh` by hand is exactly the memory the list exists to replace. Remedy: add the documents-only gates to a `paths:`-filtered workflow so they enter `gates.sh`'s output. |
| R644 instrument (candor): **`fast-gates.sh`'s *THE DISK CHECK IS FIRST AND IT LATCHES* IS FALSE — it runs `disk-guard.sh` ONCE, as a SUBPROCESS, and never again.** There is nothing to latch: `CANDOR_DISK_BROKE` lives in the guard's own process and dies with it. | 2026-09-25 | instrument (candor umbrella) — OPEN; **Sec K, in a file whose own header is a correction of a false safety claim made the same day** | `gate-run.sh:85` **sources** `disk-guard.sh` and calls `disk_guard_check` after every gate; **`fast-gates.sh` borrowed the word without the mechanism.** CLAUDE.md names this exact blind spot: *The dangerous case is the MID-RUN crossing, not the start … a startup-only check is blind to precisely the case that bites.* ~50s and no engine build makes the residual risk small — **but the sentence is the comment-asserting-safety class, and it is the second one in this file in two days.** | **The guard itself is SOUND and was driven, not assumed:** `CANDOR_DISK_FAKE_FREE_MB=100 bash bin/fast-gates.sh` -> `REFUSING`, rc=2, before any gate; and the zero-gate guard is reachable — renaming `run()` yields `REFUSING — zero gates executed`, rc=2, against `13 gate(s) run` unmodified. **One of its three named triggers is impossible, though: *a `set -e` added at the top* could never reach line 101.** Remedy: either source the guard and check between gates, or correct the sentence to *checked once, before the first gate*. |
| R645 instrument (candor-spec): **`FIXSHA` MATCHES A PLAIN 7-DIGIT NUMBER, so a date or a unit count can move a row off the shipping-defect list — and the comment's *no false positives* is ALREADY FALSE.** `soundness-status.py:43` accepts bare unbackticked tokens. | 2026-09-25 | instrument (candor-spec) — OPEN, narrow but live | Measured: a row whose evidence reads *The remedy is known; measured 20260924 over 1179132 rows.* buckets **`cites-a-sha-only`** — off the shipping-defect list — while the same row with the numbers removed buckets **`open`**. **The register routinely quotes 7-digit unit counts.** And of the 15 rows currently decided by a bare token, 14 resolve to real commits and **one does not: [[R527]]'s deadbee, a fixture literal quoted in the row's own prose (spelled here without backticks for the same reason)** — so the comment's *accepting bare hex added exactly 2 matches and no false positives* is off by at least one. | Remedy: require the backticks, or require that a bare token RESOLVE in a family repo before it counts — the second is better, because it makes the claim the comment already makes actually true. Pairs with [[R638]]: `resolves()` is already written and already family-wide. |
| R646 instrument (candor-spec): **[[R533]]'s REGISTER ROW WAS NOT UPDATED WHEN RUST CLOSED IT — the row still reads `java + rust + swift SILENT; ts discloses`.** `gen_chained_dispatch.py:363` retires `("c9_consumer_zero_union","rust")` citing candor-rust `e4808bf`, confirmed as candor-rust HEAD. | 2026-09-25 | candor-spec — OPEN as a BOOKKEEPING defect; the retirement itself is CORRECT | Anyone reading R533 gets the wrong engine set. **Retiring rust's `c9` line is right — a passing xfail is a failure, so it had to come out.** The defect is that the xfail table and the register row are two records of one fact and only one was updated. Second instance in the same edit: **`c12`'s arm prose (`:204-206`) still says *it is declared xfail FOUR-WAY until both halves ship*, while the XFAIL note at `:348-357` records that three engines passed on first run** — and that **java and swift pass with `interfaceUnion=absent`**, i.e. by resolving through the dependency's own row, NOT through the union entry. So for java and swift the arm does not pin ⟨0.39⟩ (the clause once drafted as 0.40)'s producer half, and the `why=` string overstates what a green cell means. **rust's pass is real** (`interfaceUnion=present`, [[R609]]). | Remedy: update R533's engine set, and correct `c12`'s prose and `why=` to say what a green cell actually establishes per engine. Related to [[R636]] — `c12`'s green is weaker still than this row alone implies, because an absent row passes it. |
| R647 instrument (candor-spec): **THE XFAIL LEDGER AND THE REGISTER ARE TWO RECORDS OF ONE FACT WITH NO GATE BETWEEN THEM.** [[R646]] is the instance; this row is the class. | 2026-09-25 | candor-spec — OPEN as a CLASS row, filed so the next instance is not re-derived | [[R475]] already established that **every xfail entry names a ROW**, and `part_declarations.py` checks that the named row EXISTS. **Nothing checks that the row and the xfail AGREE about which engines are affected**, so retiring one engine's arm leaves the row asserting a stale engine set — and the register is the document a reader trusts for that. Direction: a reader is told a defect is live in an engine that fixed it, which is the mirror of the cardinal sin and costs re-derivation rather than a missed defect. | **SECOND INSTANCE THE SAME DAY, and it is worse than the first: SIXTEEN register mentions cited a rung ⟨0.40⟩ THAT DOES NOT EXIST.** `SPEC.md` says it verbatim — *drafted as a separate rung 0.40 and folded back before either shipped* — `SPEC_VERSION` is `"0.39"`, and the zero-implementor clause is inside the **0.39** ladder entry. **A version no engine will ever declare, in the rows a future `grep` will trust.** Relabelled 2026-09-25 to ⟨0.39⟩ (the clause once drafted as 0.40); the COMMIT MESSAGES keep the original wording, because rewriting shipped history to match a label is the opposite of the fix. Cheap remedy in the same family as [[R643]]: extend `part_declarations.py` to parse the engine names out of the cited row's status cell and compare them with the xfail table's engine set for that row, reporting a DISAGREEMENT rather than a failure — the two can legitimately differ mid-fix, so it wants to be an advisory that names both, not a gate. |
| R648 instrument (candor): **THE SCHEDULED CORPUS ROUND SAT RED FOR THREE DAYS ON TWO FINDINGS THAT WERE ALREADY FILED AND OPEN, AND NOBODY LOOKED — INCLUDING ME, WHILE READING `ci-watch` AFTER EVERY PUSH WAVE.** `corpus.yml` failed 2026-09-22, 09-23 and 09-25 on the honesty oracle's serde `de::Visitor::visit_char` ([[R576]]) and swift-argument-parser `GenerateManual.generatePages` ([[R578]]). | 2026-09-25 | instrument (candor umbrella) — **CLOSED 2026-09-25** by a known-findings ledger, candor `PENDING`, calibrated 14/14 | The round exits 1 on `$findings > 0` with no way to distinguish a NEW finding from a filed one, so **a scheduled job whose findings are all known is permanently red — and a permanently red job teaches exactly one lesson, which is the wrong one.** It only surfaced today because `ci-watch` deliberately reports a red run on an EARLIER commit when HEAD needs none: *its NEWEST run, on an earlier commit. HEAD needs no run, so this red would otherwise vanish from the summary.* **That line is the only reason this was found**, and it was written for a different failure ([[R382]]). | **THE LEDGER IS THE CONFORMANCE XFAIL LEDGER'S RULE, BORROWED WHOLE ([[R475]]): every entry names a ROW, and an entry that STOPS REPRODUCING IS A FAILURE** — either the row was closed and the ledger was not updated, or the ORACLE STOPPED ASKING, and the second is the one that costs because it is silent and looks like progress. Entries key on TWO substrings (scope AND symbol), so a different defect at a known symbol, or the known symbol in another project, is still a FINDING. **The calibration found two defects on its first run, which is the argument for Sec 1b:** the ledger's first entry cited **R543, a CLOSED row** — the finding is owned by R576, and nothing but a check against the register catches a plausible wrong id; and **two assertions were VACUOUS because `out=$(finding …)` runs in a COMMAND-SUBSTITUTION SUBSHELL**, so every counter increment was discarded and the *does not count* assertion passed for the wrong reason. **A ledger SUPPRESSES output, which makes it the most dangerous kind of check to add to a suite whose subject is silent under-reporting** — hence `bin/corpus-ledger.test.sh`, wired into `fast-gates.sh`, with both refusal paths driven: extraction-marker drift and an absent register each REFUSE rather than skip. |
| R649 swift: **`depShadows` READ KEY EXISTENCE AS A DECLARATION, AND `pkg#<leaf>` IS MINTED FOR EVERY ENTRY, A METHOD'S LEAF INCLUDED.** A dependency that merely HAS a method called `connect` silenced the POSIX free `connect(fd, &addr, len)` in every file importing it. | 2026-09-25 | swift — **CLOSED 2026-09-25**, candor-swift `483d6ad`, calibrated via `CANDOR_R616_OFF=1` | One variable (the dep member NAME), consumer text, binary and policy held constant: `Chan.connectX` -> `['Net']` exit 1; `Chan.connect` -> **`['Env']` exit 0**. **Not merely silent — the FABRICATION is confirmed on real code:** the unqualified join at `Driver.swift:3100` finds the same bare key and attaches the METHOD's effects, so against the **REAL swift-nio report** the fixture reads `['Clock','Env','Net','Unknown']` plus 40 `unknownWhy` tokens, against an unchained ground truth of `['Net']` — swift-nio's ~50-entry `connect` union fabricated onto `connect(2)`. **It kept `Net` only by ACCIDENT** (one `DatagramBootstrap.connect` overload carries it), which is why no gate caught it. **Exposure census over 29 real dependency checkouts, 40,199 published entries:** five kappa free names are published as a dependency METHOD leaf and therefore withdrawn from every importer — `connect` (swift-nio, swift-nio-extras, swift-nio-transport-services), `sendmsg`, `recvmsg`, `getenv` (swift-nio), `fopen` (swift-nio-ssl). | **Remedy shipped: `DepIndex.freeLeaves`** — the leaf of a ONE-SEGMENT qual only — asked by `depShadows` via `declaresFreeName`. `byKey` is untouched, so no JOIN moves and [[R567]](b) does not regress. **The coordinator's proposed remedy was REFUSED for a measured reason: *no consumer wants the bare-leaf METHOD key* is FALSE** — `Driver.swift:3100` serves implicit-self calls into a dependency type (`extension DepType { func h() { depMethod() } }`, and the subclass form), both verified live, so withdrawing the key would have been a silent-direction removal on idiomatic Swift. **A/B: ADDED 0 / REMOVED 0 / CHANGED 0 over 4,142 rows — and the zero is EXPLAINED rather than trusted.** REACH 161 hits across 5 of 6 packages; all 16 distinct disagreeing names are ones `kappaFree` returns nil for. Because **a corpus that reaches the branch but not the outcome is not evidence about the outcome**, the census above and a recall hunt against the real swift-nio checkout were run as well: unchained `['Net']`, pre-fix chained `['Clock','Env','Net','Unknown']`, post-fix `['Net']` — **1 row, 3 effects removed, all traced by hand, all fabrications.** **Sec K: `8931087`'s *purely ADDITIVE — `insert` UNIONS and never withdraws* is TRUE ABOUT THE INDEX and was read as true about the BEHAVIOUR. Before widening a key, list every consumer and ask which reads the VALUE and which reads the KEY'S EXISTENCE.** |
| R651 swift: **A CONSUMER THAT WRITES AN `extension` ON A DEPENDENCY TYPE SILENCES EVERY MEMBER CALL ON THAT TYPE.** Extending a dependency type is completely idiomatic Swift, and this is LIVE AT HEAD after [[R649]]. | 2026-09-25 | swift — **CLOSED 2026-09-25 by [[R656]]**, candor-swift `2f9f3fd`. Was: CARDINAL SIN, gate-level, on a very common idiom. Found by the [[R649]] fix lane while measuring something else. **The mechanism stated in this row was HALF WRONG and the wrong half was load-bearing — see R656** | One variable, both arms compile. File A: `public func viaReceiver(_ c: Chan) { c.poke() }` where `Chan.poke` is an effectful chained dependency method -> `['Env']`, `deny Env` **exit 1**. Add file B containing ONLY `extension Chan { public func unrelatedHelper() -> Int { 1 } }` -> **`viaReceiver` is ABSENT from `functions` — a ⟨0.21⟩ purity claim — and `deny Env` is exit 0.** **Mechanism:** the extension puts `Chan` into `localTypes`/`declaredTypes`, so `c.poke()` resolves LOCALLY, finds no `poke`, and never reaches the cross-package join. | **The failure is total rather than partial: the function leaves the report entirely**, so absence licenses the purity claim under SPEC Sec 2 rule 3. Note the shape — a consumer ADDING code to its own tree deletes a disclosure about a dependency, which is the same direction as [[R595]] (adding a pure default to a library deleted a real `Fs` from every consumer) with the roles reversed. **Needs its own lane**; the fix is not a one-liner, because `localTypes` legitimately contains extended types for every other purpose. |
| R650 swift: **THE UNQUALIFIED-CALL JOIN ASKS `pkg#<call.path>` AND A DEPENDENCY METHOD ENTRY ANSWERS IT**, so a free call is charged with an unrelated dependency method's effects. `Driver.swift:3100`. | 2026-09-25 | swift — OPEN; OVER-CHARGE / precision, not a sin | Compiling repro, one variable: consumer `import Darwin; public func removeTemp(_ p: UnsafePointer<CChar>) { _ = unlink(p) }`. Dep declares `Chan.unlinkX` -> `['Unknown']` with `native:unlink`; dep declares `Chan.unlink` -> **`['Env','Unknown']`**. Always an over-charge — `applyDepEntry` unions and the `native:` disclosure survives — so it is a precision defect, not the cardinal sin. | **The remedy is NOT obvious, which is why this is a row rather than a fix:** the same key legitimately serves implicit-self calls into a dependency type (the reader that made [[R649]]'s first proposed remedy unsound), so a narrowing needs a DERIVABLE ENCLOSING-TYPE key rather than a deletion. Related: kappa's TYPE-shadow gap, deliberately not touched on the way past — `depShadows("File")` was already false for a dependency type `File`, published as `File.init` at two segments, and that is the over-charge direction. |
| R655 swift: **CHAINING swift-nio DEGRADES A CONCRETE `Env` TO `Unknown` ON THE `guard let getenv` + `String(cString:)` PATH, flipping `deny Env` from exit 1 to exit 0.** Unaffected by [[R649]] — measured in BOTH arms. | 2026-09-25 | swift — OPEN, UNDIAGNOSED; observed while measuring [[R649]] | `public func readTuning() -> String? { guard let v = getenv("MY_TUNING") else { return nil }; return String(cString: v) }` reads `['Env']` unchained and `['Unknown']` chained. **A bare `_ = getenv("X")` keeps `Env` in both arms**, so it is the `guard let` + `String(cString:)` path and not the shadow — which is the control that makes this a separate finding rather than a residual of R649. | Direction: **a concrete effect degraded to a hedge, which flips a gate the permissive way** — `deny Env` exit 1 -> exit 0. Not silent (the `Unknown` is disclosed), so it is a gate-shape defect rather than the cardinal sin, but it is user-visible and it fires on an ordinary environment read. Not yet reduced to a minimal fixture; the two arms above are the whole evidence. |
| R656 swift: **A CONSUMER THAT WRITES AN `extension` ON A DEPENDENCY TYPE SILENCED EVERY MEMBER CALL ON THAT TYPE — INCLUDING AN EMPTY `extension Chan { }`.** Closes [[R651]]. | 2026-09-25 | swift — **CLOSED 2026-09-25**, candor-swift `2f9f3fd`, calibrated via `CANDOR_R651_OFF=1` | One variable, both arms compile: `public func viaReceiver(_ c: Chan) { c.poke() }` -> `['Env']`, `deny Env` exit 1; add a file containing ONLY `extension Chan { }` -> **`viaReceiver` ABSENT from `functions[]`**, exit 0. **ELEVEN spellings fire** — method, computed property, `static let`, nested type, `typealias`, `private`, `fileprivate`, **the EMPTY extension**, sibling file, same file before the call, same file after it — and the receiver spelling is irrelevant (parameter, `let` local, stored field, `Chan().poke()`), as is the type kind (`open class`, `struct`). **Controls that correctly do NOT fire:** an extension on a type the package really declares; on a DIFFERENT dependency type; on a kappa-platform type (`extension Data` + `d.write(to:)` still `Fs`); and a local SUBCLASS of the dep type, where ⟨0.39⟩ already mints a `Chan.poke` unit. | **THE MECHANISM IN [[R651]] WAS HALF WRONG AND THE WRONG HALF WAS LOAD-BEARING.** The row said the extension enters `localTypes`/`declaredTypes`. **It does NOT enter `declaredTypes`** — `pushType` explicitly excludes extensions, correctly and long-standing. The real path is one disjunct on: `CallCollector`'s typed-local-receiver branch admits an extension-only owner via `kappaMember(...) == nil && !isFileWrite(...)`, emits `typed: true`, **and `typed` is the exact conjunct the Driver's §2 join is gated on** (`if !resolved, !deps.isEmpty, !call.typed`). **Had the lane fixed the stated mechanism it would have broken the kappa shadow discipline for nothing.** Fix: mark the receiver root `extOwner` iff `!declaredTypes.contains(rt) && !base.opaqueHop`, and have the Driver re-admit those calls to the two arms that ASK a dependency; local resolution keeps first refusal. **DENYLIST (`declaredTypes`), not allowlist**; the `STD_PURE_PROTOCOLS`/`RAW_VALUE_BASE_TYPES` carve-outs were deliberately NOT relaxed. **Direction: fails toward OVER-CHARGE, and that is a change of DIRECTION not degree — what it replaces is SILENCE, a purity claim.** **The lane introduced a defect and caught it in its own added-row audit:** the first cut lacked `!base.opaqueHop`, and vapor's `Request.Authentication.logout` published `swift-http-types#Request.Authentication.withLock` — a member the owner does not declare, in a package that owns neither, which is [[R567]](a)'s shape at a NEW site. The guard costs 33 of 203 added rows and 2 of 723 joins, **and all 33 carried no effect.** |
| R657 swift: **A RETROACTIVE CONFORMANCE ON A DEPENDENCY TYPE TRADES THE DEPENDENCY'S REAL EFFECT FOR `Unknown`**, so a plain `deny Env` stays exit 0 over a real environment read. | 2026-09-25 | swift — OPEN; DISCLOSED, so not the cardinal sin, but it flips a gate the permissive way | One variable, post-[[R656]]: `extension Chan { }` -> `viaReceiver` reads `['Env']`, `deny Env` exit 1; **`extension Chan: Marker { }` -> `['Unknown']` with `unknownWhy: dispatch:Marker.poke`, exit 0.** Ground truth is `Env`. **Mechanism:** `supertypesOf["Chan"]` becomes non-empty, so the external-supertype fallback (`Driver.swift:2624`, the UNIQUE site minting that token) inserts `Unknown` and sets `resolved = true`, **preempting the §2 join roughly 500 lines further down.** Identical in both arms of `2f9f3fd`, so it is neither caused nor fixed by R656. | **Fix shape: the join should be tried BEFORE the external-super `Unknown`, not after** — a reorder, with its own removal audit, since reordering a `resolved` short-circuit can withdraw whatever the later arm would have answered. Worth pairing with [[R650]] and [[R655]] as one swift lane: all three are the join being preempted or mis-keyed rather than being wrong. |
| R658 swift: **THE ⟨0.39⟩ OBLIGATION-2 UNION LOOP PUBLISHES ENTRIES UNDER STDLIB MARKER PROTOCOLS, ATTRIBUTED TO A DEPENDENCY'S PACKAGE — WHILE EVERY CONSUMER-SIDE KEY FORMER IN THE SAME ENGINE REFUSES EXACTLY THOSE NAMES.** Sec F1.3: two sides of one question, and the UNGUARDED side is the producer's. | 2026-09-25 | swift — OPEN, PRE-EXISTING; surfaced by [[R656]]'s added-row audit | Of 47 union entries newly emitted in R656's post arm, **25 are keyed on a `STD_PURE_PROTOCOLS` name** — `swift-nio#Sendable.insert`, `swift-nio#Hashable.receiveInboundFrame`, `swift-asn1#Hashable.init(Attribute)`, `swift-certificates#RandomAccessCollection._sortElements` — **and two carry a CONCRETE EFFECT**: `swift-nio#Sendable.tryToAutoRead` and `swift-nio#Sendable.selectivelyPropagateUserInboundEvent`, both `['Env','Unknown']`. At least one wrong-package attribution too: `swift-log#ChannelDuplexHandler.state`, where **swift-nio** owns that protocol. **Mechanism:** `main.swift`'s union loop unions EVERY method of EVERY conformer under `<ownerPkg>#<proto>.<method>` with no requirement gate and no `STD_PURE_PROTOCOLS` filter, while `dispatchAbstraction` (`Driver.swift`) returns `nil` for exactly those bounds. | **What the lane could NOT establish, stated rather than assumed: whether this engine's own consumer can JOIN them.** A compilable consumer cannot call a member a bound protocol does not declare, which is the argument `main.swift`'s comment makes (*harmless data*) — and that argument SURVIVED the attack. **The two routes left open are (a) a multi-bound generic `<T: Hashable & Inserting>` where `dispatchAbstraction` picks the wrong bound, and (b) `DepIndex`'s `pkg#<leaf>` shape feeding [[R650]]'s unqualified-call join — which is [[R649]]'s exact lesson, that a key shape minted harmlessly for one join is not harmless for a DIFFERENT reader.** Needs its own lane. |
| R659 rust: **163 ROWS LOST A CONCRETE EFFECT ACROSS THE MONTH, AND 162 OF THEM FLIP A REAL SCOPED GATE FROM exit 1 TO exit 0 OR 2.** Found by the gate-flip census ([[R662]]), not by review. | 2026-09-25 | rust — **CLOSED 2026-09-25 by [[R679]], NO CODE CHANGE: the residual is not one.** All of it is correct, it is **12 rows not 7**, and both causes name those crates in their own commit messages | **All 163 were gated, not sampled**, and 162 flip a scoped `deny <lost effect> <fn>`: 131 to exit 0, 31 to exit 2. **58 of the 163 bisect to `5588330`** — *a `use` item under an INACTIVE feature still bound its name* ([[R140]]) — whose own commit message names der-0.7.10, curve25519-dalek, hickory-resolver, git2 and reqwest's `Part::file` as **deliberate PRECISION GAINS**: functions that do not exist in a default build. Of the 105 residual, **64 are `Log`** and most of the rest are `#[cfg(test)]` helpers (`test_helpers::test_client`, `TestEnvironment`, `test_stream`, `test_logging`). | **The genuine residual is SEVEN functions and it is named rather than buried:** `rdkafka-0.36.2`/`0.39.0` x 3 functions losing `Clock`, and `terminal-trx-0.2.6 unix::pty_utils::pty_pair` losing `Fs`. 0.001% of 706,416 analysed units, and the only part of the category the lane could not attribute to a documented deliberate change. **The clean 17-commit fix window has ZERO user-visible regressions** — every loss is in the wide six-rung window. The 64 `Log` losses were not bisected. |
| R660 spec/rust: **A PUBLISHED ROW THAT NO SCOPED POLICY CAN ADDRESS — AND IT MAKES THE ENTIRE ⟨0.39⟩ (the clause once drafted as 0.40) RUNG INVISIBLE TO EVERY GATE BY CONSTRUCTION.** `interfaceUnion` rows are dropped at gate ingress (`candor-query/src/gate.rs:195`, `load.rs:75`, [[R511]]; `candor-scan --policy` behaves the same). | 2026-09-25 | rust + spec — **LARGELY WITHDRAWN 2026-09-25, same day, by an adversarial re-read.** Was filed as *an addressability gap*. **The census measured the PRODUCER HALF OF A CROSS-PACKAGE JOIN WITH A SINGLE-PACKAGE GATE.** Those rows are ⟨0.39⟩ **obligation 2**, published so a CHAINED CONSUMER can tell *all implementors pure* from *nobody implements*; their observable is the consumer's ORDINARY row via `CANDOR_DEPS`, which a single-crate census cannot reach **by construction**. [[R608]] measured that observable: 6 chained pairs, 2,826 functions, 6 rows to 2, plus a fabrication removed (`c3_pure_only` exits 1 without [[R609]]). **So *0 gate users can observe it* is true of the CENSUS'S SHAPE, not of the clause.** What legitimately remains open is ONE MEASUREMENT | `builder::value_parser::AnyValueParser::parse_ref_` in clap_builder is published with `inferred: ["Unknown"]`, and **`deny Unknown <that exact qual>` exits 0** while every real sibling row in the same module exits 1. **The R511 rationale was CHECKED and HOLDS: 808 of 808 union rows carrying a concrete effect have that effect on a GATABLE same-leaf row in the same report**, so no effect is lost. **But the scale is the finding: ⟨0.39⟩ (the clause once drafted as 0.40) (`e4808bf`) priced alone on 1,626 crates ADDED 12,704 rows, REMOVED 0, CHANGED 0 — every one an `interfaceUnion` row, 0 carrying `Unknown`, and 0 able to change any verdict.** Across POST, 21,362 rows (6.3%) are `interfaceUnion`; 4,424 carry `Unknown`, 808 carry a concrete effect, **none addressable by a scoped gate at its own qual.** | **RE-OPENED IN PART 2026-09-26 BY [[R689]], AND THE PART THAT RE-OPENS IS THE ARGUMENT I WITHDREW IT ON.** I withdrew this row saying a union row *can never move a verdict at its own qual* — true — and then reasoned from it that a union-row CHANGE is verdict-neutral. **[[R689]] falsifies the second step: [[R590]]'s *NOT a gate flip* is a PRODUCER-SCOPE verdict, and the four hickory-proto rows it names take a CONSUMER's `deny Log <fn>::` from exit 1 to exit 0.** The mechanism is the control: the producer-side `deny Log …::to_bytes::` is exit 0 on BOTH arms, precisely because the PRE `Log` sat on an `interfaceUnion` row no gate reads — so the producer arm cannot see the change that the consumer arm gates on. **THE STANDING CONSEQUENCE: R660's invisibility argument MAY NOT BE USED TO PRICE A UNION-ROW CHANGE AS VERDICT-NEUTRAL.** ~~This is the row the roadmap turns on.~~ Still not that — but not inert either — that sentence was mine and it was wrong, and it is recorded rather than deleted because it is the shape this register keeps re-finding: an instrument's blind spot read as a product defect.** The remaining item is ⟨0.39⟩'s CONSUMER-SIDE flip count at corpus scale — registry crates whose dependencies are also in the registry, scanned with `CANDOR_DEPS`, classified as [[R662]] classified the single-crate arms. **Six chained pairs is not a corpus.** The rejected remedies, and why: A whole rung of correctness work that no user of a gate can observe is not a soundness improvement to them; it is a wire-format improvement for a consumer that must join it. Either (a) union rows become addressable at their own qual under some policy form, (b) the rung's value is re-stated as a CONSUMER-SIDE contract rather than a gate improvement, or (c) the disclosure is routed to a surface a user reads. Pairs with [[R443]] (the advisory that one of the two gate routes does not print) — **both are the same question: does disclosure reach the person holding the gate?** |
| R661 rust: **`scope_matches` PREFIX-MATCHES THE LAST SEGMENT**, so `deny Unknown A::B::parse_ref` fires on `A::B::parse_ref_`. `policy.rs`: `w_last.starts_with(last)` unless the scope is marked exact. | 2026-09-25 | rust — OPEN as a CAVEAT rather than a defect; `scope_is_exact` exists | Measured directly while validating [[R660]], where it **masked the finding on one qual** — the scoped gate appeared to fire until the trailing-underscore qual was distinguished. It would mask generally wherever a trait member's name is a prefix of a sibling's. | Filed because **it is a measurement hazard before it is a product defect**: any census that gates on quals must use the exact form, or it will over-count flips. The [[R662]] census was re-run with exact scopes after this was found. |
| R662 all engines: **THE GATE-FLIP CENSUS — what a month of soundness work changed for a user of a gate.** Answers the strategy review's charge that candor's soundness output is *largely invisible to a user of the default gate*. | 2026-09-25 | instrument/measurement — **ANSWERED 2026-09-25**, candor-rust only; the other three engines UNMEASURED | **Arm F (clean: 17 R-numbered fix commits, ZERO rung, byte-identical `candor-query`): 31 verdict changes (0.0044%) against 450 disclosure-only `Unknown` gains (0.064%) — a ratio of 1:14.5.** **Arm W (a month, six rungs, context only): 3,653 (0.517%) against 32,522 (4.604%) — 1:8.9.** N = **1,626 crates**, identical entry list on every arm, denominator **706,416 analysed units**, instrument `bin/corpus-ab.py`. **VALIDATED RATHER THAN MODELLED, and the validation caught two real classifier errors** (an 8-of-39 and a 1-of-25 failure on the first pass, both fixed and re-validated on fresh seeds): **category 1 — 71 of 71 samples moved as predicted** (68 exit 0->1, 3 exit 2->1); **category 2 — 50 of 50 did NOT move on a bare scoped `deny <Effect>` and ALL 50 moved on `deny Unknown`**, reproducing [[R529]]'s `term_size` ruling exactly. `candor-query gate --report` was unusable (1,083 of 1,626 entries carry a non-empty `excluded[]`, so it exits 2 INCOMPLETE on both arms); the real user path `candor-scan <entry> --policy` was used instead. | **TWO COORDINATOR PREMISES WERE WRONG, BOTH IN THE DIRECTION THAT WOULD HAVE INFLATED THE ANSWER.** (1) **SIX rungs are in the wide window, not four** — PRE-A's binary reports spec **0.34**, so ⟨0.35⟩ and ⟨0.36⟩ are in it too. (2) **Rungs and fixes are NOT SEPARABLE BY COMMIT in candor-rust** — ⟨0.37⟩ was ported *as* [[R409]]/[[R414]]/[[R416]]/[[R417]] — so *subtract the rung commits* is not achievable, and the wide arm is therefore NOT reportable as a fix measurement. The lane built a new arm rather than report the conflated one. **The verdict: the strategy review is DIRECTIONALLY RIGHT that most soundness output is advisory, but *largely invisible* overstates it — 31 real gate flips in three days is not nothing.** The sharpest single result is [[R660]]: the ⟨0.39⟩ (the clause once drafted as 0.40) rung's 12,704 added rows are provably invisible to every gate. **Left unmeasured: the other three engines** — and note candor-java, the engine the strategy review calls the near-term product, **has a STANDING corpus of five jars** ([[R663]]). |
| R663 instrument (candor): **THERE IS NO STANDING JAVA CORPUS — `corpus.sh`'s java roster is FIVE JARS**, while rust's is 1,626 crates wired into the same tool. | 2026-09-25 | instrument (candor umbrella) — OPEN, **AND THE CONTRAST AS FIRST FILED WAS WRONG IN THE DIRECTION THAT FLATTERED RUST.** `bin/corpus.sh`'s **RUST** roster is *four git checkouts* (ripgrep, clap, serde, regex — `corpus.sh:279-282`), the same shape as java's five jars. **The 1,626 crates are `~/.cargo/registry/src/index.crates.io-*/` — whatever cargo happened to download on this machine. Nothing owns, pins or rebuilds THAT either, and it does not exist on `anya`.** So the finding is not *java has the weak corpus*; it is **NO ENGINE HAS AN OWNED CORPUS, and [[R662]] is UNREPEATABLE ON ANY OTHER BOX** | `bin/corpus.sh` acquires exactly five: commons-lang3, gson, jackson-core, joda-time, sqlite-jdbc (`~/.candor/corpus/jars`, 15 MB). **The register DOES cite java corpus runs of 325, 371, 372 and 395 jars** — so the capability is real and has been exercised — **but those were AD-HOC acquisitions by individual measurement rounds that cleaned up after themselves. Nothing owns or rebuilds a java corpus.** Not a disk constraint: 58 GB free when this was found. **And a figure of *449 jars* was quoted in a strategy review and repeated by the coordinator; it does not exist** — `449` in the tree is a carve-out count in a candor-java CHANGELOG line (`FileWriter.write` 449) and the row id [[R449]]. | **Why this outranks its size: the engine with the weakest standing corpus is the one a strategy review names as the near-term commercial product** (the JVM architecture gate), and [[R662]]'s census cannot be repeated for it without first building the corpus. Any claim about java's real-code soundness currently rests on five jars plus whatever an ad-hoc round happened to fetch. Remedy is cheap — a roster in `corpus.sh` and the disk to hold it. |
| R664 all engines: **THE GATE-FLIP RATE COLLAPSES TO ZERO AS THE POLICY SCOPE WIDENS, and [[R662]] priced the MOST FAVOURABLE scope.** A month of fixes flips nothing at module or crate scope. | 2026-09-25 | measurement — ANSWERED; candor-rust only | Measured by re-scanning the affected crates with [[R662]]'s own PRE binaries; **all 31 category-1 rows re-verified as reaching the code** (effect gained on the named fn in POST, absent in PRE). Clean 17-commit arm, then the wide six-rung arm: **function-scoped `deny E <fn>` 31 / 3,653 rows; parent type or module 22 / 3,395; TOP-LEVEL MODULE 0 / 1,679; CRATE-WIDE `deny E` 0 / 204 rows in 18 crates (1.1%), 165 of them `Clock`; crate-wide `deny Unknown` 0 / 1,455.** And in every one of the 31, **the crate ALREADY carried the gained effect on at least 5 other non-union rows** (median 26, max 322) — so even the function-scoped flip rarely tells a crate-level reader something new. | **The `candor init` / `deny Net com.acme.domain` shape — what a real deployment looks like — sits between the module row and the crate row, i.e. NEAR ZERO for fixes and about 1 crate in 13 for a month of rungs.** Strategic consequence, stated by the reviewer and worth keeping in the register rather than in a chat log: **soundness work is TABLE STAKES FOR THE CLAIM and cannot be the growth mechanism — do not sell fix velocity, sell the guarantee and the disclosure channel.** Note the disclosure channel is only visible through [[R443]]'s advisory lines, which is why that row and this one are the same argument. **Caveat carried forward: the classifier behind [[R662]] is a NEW INSTRUMENT written in a scratchpad and first run today, with two errors caught on its own first pass — under the cost-ceiling ruling a new instrument escalates regardless of its number.** Remedy: promote it into `bin/corpus-ab.py` as a `--buckets` mode before it is run a second time, per [[R288]]. |
| R665 java: **THE GATE-FLIP CENSUS ON THE REFERENCE ENGINE — 18.5x rust's flip rate, AND 97.2% OF IT IS ONE COMMIT.** Repeats [[R662]]/[[R664]] on candor-java over the newly pinned 118-artifact corpus ([[R663]]). | 2026-09-25 | measurement — **ANSWERED 2026-09-25**; the engine a strategy review names as the near-term commercial product | **The rung/fix confound does NOT apply to java, in the useful direction: candor-java lands each rung as its own SPEC-FLOOR commit**, so `687157f..HEAD` is **13 commits, ZERO rung** — a genuine clean fix-only window, unlike candor-rust where ⟨0.37⟩ *was* [[R409]]/[[R414]]/[[R416]]/[[R417]]. **Fix-only buckets: (1) CONCRETE 567 = 0.0816% of 695,273 analysed units; (2) `Unknown`-only 1,363; (3) CONCRETE LOST 0; ratio 1:2.40** against rust's 1:14.5. **Denominators are within 1.6% of each other** (695,273 vs 706,416) so the function-scope comparison is like-for-like despite a 14x difference in entry count. **VALIDATED BY EXECUTION: bucket 1 24/25, bucket 2 15/15** — bare `deny Fs <fn>` 0->0 on both arms while `deny Fs Unknown <fn>` 0->1, the gate calibrated in BOTH directions before use. | **THE FINDING IS THE ATTRIBUTION, NOT THE RATE. 551 of 567 flips come from ONE commit — [[R530]](b), *a lambda was never an implementor*. The other seven R-numbered fixes produced SIXTEEN flips between them, and three ([[R547]], [[R94]], [[R595]]) moved NOTHING on 117 jars.** **And the scope collapse is identical to [[R664]]'s: 567 function -> 163 class -> 7 package -> 0 artifact** (23 of 118 jars moved at all). **So at the `deny Net com.acme.domain` shape a real deployment uses, a month of java fixes moves SEVEN package gates across 117 artifacts and ZERO artifact gates — R664's verdict, unchanged, on the engine that bears on the roadmap.** Two honest negatives from the same lane: **the full 97-commit window's 229 bucket-3 rows are NOT a regression** — 222 are `Net` on socket OPTION accessors from `isPureSocketOptionAccessor` (`Candor.java:9243`), a documented deliberate narrowing landed `2a6793c` whose own comment states its failure direction. And **[[R622]]/[[R623]]/[[R624]] were probed and NOT confirmed: the hypothesis did not survive its own control** — 267 of R131's new `Unknown` rows carry no `unknownWhy`, but **83% of ALL `Unknown` rows corpus-wide (47,321 of 275,435) carry none**, so 267 is unremarkable. This census can neither confirm nor refute those rows. |
| R666 instrument (candor): **A JAR THAT RESOLVES IS NOT A JAR THAT CONTAINS CLASSES — the census roster pinned a Kotlin MULTIPLATFORM METADATA artifact, and sha1 verification could not tell.** Found on the roster's FIRST use, hours after it was built. | 2026-09-25 | instrument (candor umbrella) — **the okio entry FIXED 2026-09-25**, candor `PENDING`; the SIZE half stays OPEN | `com.squareup.okio:okio:3.9.0` verified against Maven Central, downloaded, and sha1-matched **perfectly** — and holds **ZERO `.class` files**. Every engine build correctly refuses it at exit 2, so it contributed nothing to any denominator and the census compared **117 of 118**. The JVM artifact is `okio-jvm` (114 classes), now pinned instead. **The lesson generalises past this entry: sha1 verification proves INTEGRITY, not FITNESS. A Maven coordinate can be real, current, popular and still be a BOM, a `-sources`, or a `-metadata` artifact.** This is the [[R242]] hollow-corpus class one layer up — the check that was built specifically to stop a corpus entry contributing zero rows, defeated by an entry that is not corrupt at all. | **THE SIZE HALF IS OPEN AND THE LANE WAS RIGHT TO REFUSE TO QUOTE IT: 118 artifacts CANNOT support [[R665]]'s package-scope (7) or artifact-scope (0) figures.** Only 23 jars moved at all and the mass is concentrated — amqp-client 139, jedis 98, log4j-core 68, guava 60, testng 46 carry **72%** between them. The FUNCTION-scope comparison is sound (matched unit denominators); the other two rows are not quotable. **Extend the roster to ~400 before *zero at artifact scope* is published as a claim** — the TSV format and `corpus-census.sh` make that cheap, which is the point of having built them. |
| R667 java: **~~THREE OF EIGHT~~ ONE OF SIX ENGINE FIXES IS INVISIBLE ON 117 REAL JARS — CORRECTED 2026-09-25 by the reach lane, see [[R669]].** [[R547]], [[R94]] and [[R595]] moved ZERO rows; R595 rewrote 223 lines across `Candor.java` and `Cha.java` for a byte-identical corpus report. | 2026-09-25 | java + process — **ANSWERED 2026-09-25 by [[R668]]/[[R669]]/[[R670]], and the answer is NOT the one this row expected.** Two of the three named fixes CHANGED NO ENGINE CODE AT ALL, and the third is REACHED AND INERT with a reach of exactly 1 — but the census that could not see it is blind for a STRUCTURAL reason a bigger corpus cannot fix | **This is NOT evidence the fixes are worthless, and saying so would be the same error in the opposite direction.** [[R595]] closed a CARDINAL SIN that shipped in 0.39.2 — adding a pure default to a library deleted a real `Fs` from every consumer — and a corpus of 117 library jars, scanned STANDALONE, cannot contain the chained producer/consumer shape that defect requires. **REACH WAS NOT INSTRUMENTED, so the correct statement is *invisible to this census*, not *proven inert*** — which is the distinction [[candor-locator-spelling-vein]] already paid for once: 17,944 units said *inert* and the probe said the code never ran. | **Remedy, and it is cheap: instrument `--mark` REACH PER FIX in the census.** `corpus-ab.py` already takes `--mark`/`--mark-env`, and a fix whose corpus movement is zero AND whose reach is zero is a different animal from one whose movement is zero and whose reach is thousands. Without that separation a census cannot distinguish *this fix does nothing* from *this corpus cannot see this fix*, and a roadmap that prices fix value off the first reading would retire exactly the work that closes chained-dispatch sins. |
| R668 instrument (candor): **THE JAVA CENSUS IS 118 STANDALONE LIBRARY SCANS WITH NO `CANDOR_DEPS` ARM AT ALL, so it is STRUCTURALLY BLIND TO THE ENTIRE ⟨0.39⟩ CHAINED-DISPATCH RUNG — and a bigger roster does not fix it.** | 2026-09-25 | instrument (candor umbrella) — **CLOSED 2026-09-25 by [[R671]]**, candor `5a142e4`. **And the answer it produced is YES: the fixes DO flip consumer gates, at package scope and past it** ([[R671]]) — plus one live silent under-report the standalone census structurally could not see ([[R672]]) | [[R595]] closed a CARDINAL SIN THAT SHIPPED IN 0.39.2: adding a pure default to a library deleted a real `Fs` from every consumer. **That defect requires a producer report joined by a consumer. The census scans libraries STANDALONE.** So do every other ⟨0.39⟩ chained-dispatch fix — which is the rung the last month of work was mostly about. **The reach lane demonstrated the defect LIVE on a constructed two-jar chain, one variable (the library's one-line initialiser), consumer class files byte-identical, same scoped policy, only the ENGINE JAR differing between blocks:** with `public static Hook afterStage;` the consumer reads `['Fs','Unknown']` and exits 1 on both engines; with `… = () -> { };` the PRE engine reads **`[]`, `unresolved:false`, no paths, exit 0** — the shipped sin — and the POST engine reads `['Fs']` with paths preserved, **exit 1**. An independent reproduction, not a re-run of `smoke.sh`, which pins only POST. | **This is why [[R667]]'s *invisible* was the wrong frame and [[R665]]'s numbers need this caveat beside them: the corpus cannot see the rung, so a fix's zero movement there is not evidence about the fix.** **Remedy is a SECOND ARM, not a bigger roster** — a hand-written consumer scanned against each library's own report, which `bin/AGENT-CORPUS-BRIEF.md` §2 already prescribes for exactly this reason and which the java census does not implement. Note the roster extension to 468 entries landed the same day addresses a DIFFERENT gap ([[R666]]'s scope-claim concentration) and does nothing for this one; conflating the two would retire this row without closing it. |
| R669 java: **TWO OF THE THREE *INVISIBLE FIXES* CHANGED NO ENGINE CODE AT ALL — their zero movement is ARITHMETIC, not a measurement.** [[R547]] (`85cb65c`) touches `CHANGELOG.md` and `README.md`; [[R94]] (`d6419ae`) touches those plus `test/smoke.sh`. Neither touches `src/`. | 2026-09-25 | java — **CLOSED as a bookkeeping correction to [[R667]]** | `src/main/resources/` holds `AGENTS.md` and `META-INF` only — not README, not CHANGELOG — **so the shipped jar is BYTE-EQUIVALENT across both commits and there is no code path to instrument.** Both are NAMED MISSES under the corpus brief's §7 item 7, which is the only form those fixes could take: **R547's defect WAS a README sentence, and R94's answer WAS to state the scope limit rather than enforce it.** | **A fourth classification the reach brief did not offer and should have: NO ENGINE CODE CHANGED** — distinct from *reached and inert* and from *never reached*, and the one that applies here. **Corrected statement: of the 8 R-numbered commits in `687157f..HEAD`, SIX touch `src/`, and exactly ONE of those six ([[R595]]) is invisible to the census.** The original *three of eight* framing was mine and it made the fix pace look worse than the evidence supports. |
| R670 java: **[[R595]]'s *223 LINES FOR A BYTE-IDENTICAL REPORT* OVERSTATES THE DELTA BY AN ORDER OF MAGNITUDE — and the row's own corpus figures are STALE.** | 2026-09-25 | java — **CLOSED as a correction**; the fix itself stands and is verified live | The 223 lines are **~15 lines of new logic** (`externallyReassignableField`, `fieldReceiverKey`, three call-site lines) plus a behaviour-preserving §G extraction — `recordDispatchedMember`, **called 1,151,828 times across 117 artifacts**, which is the opposite of unreached. **Reach of the actual behaviour change, measured with the fix's OWN shipped instrument (`CANDOR_R595_DEBUG=1`, calibrated in both directions on the smoke over-charge twin before use): `R595FIELD` 1 hit / 1 artifact / 1 field; `R595BOUND` 58 hits / 15 artifacts / 36 fields.** Classification: **REACHED AND INERT, CORRECTLY** — the single firing (`SocketChannelFrameHandlerState.<init>` on `NioParams#DEFAULT_WRITE_QUEUE_FACTORY`) declines because `java` is in `KAPPA_COVERED_PREFIXES`, so `foreignAbstractionOwner` returns false by design. Not *should have moved*. | **STALE SNAPSHOT IN R595's OWN COMMIT MESSAGE, and it is the [[R445]]/[[R449]] class:** it reports *26 times over 23 distinct fields, and the new disclosure fires 0 times — because ALL 23 of those fields are `private`* over an ad-hoc 150-jar corpus. **On the pinned census it is 59 firings / 37 fields / 1 DISCLOSURE, and a non-private non-final bound field DOES exist in real code** (`NioParams#DEFAULT_WRITE_QUEUE_FACTORY`, `flags: (0x0008) ACC_STATIC`). *Discloses 0* was CORPUS-SPECIFIC, not a property of the shape. **Ground-truthed with `javap` over all 37 fields, no sampling, never candor's own report: the 36 declined fields are every one `private` or `final`, including the ALL-CAPS public constants that look like counterexamples and are `public static final`. Zero wrong verdicts in either direction.** |
| R671 instrument (candor): **THE CHAINED CENSUS ARM — and it answers the question the standalone censuses could not: the last month's fixes DO flip CONSUMER gates, at PACKAGE scope and past it.** Closes [[R668]]. | 2026-09-25 | instrument (candor umbrella) — **BUILT AND CALIBRATED 2026-09-25**, candor `5a142e4`; java measured, **rust NOT run** | `bin/corpus-chained.sh` + `ConsumerGen.java` + `corpus-chained-judge.py`. **The consumer is GENERATED from each jar's public API by reflection** — the JDK's own binary-to-source mapping and its own erasure, so §G is honoured rather than reimplemented — in the two shapes §2 prescribes: a DISPATCH probe per public interface/abstract class ([[R608]]/[[R530]]b) and a FIELD probe per public static non-final functional field ([[R595]]). **Consumer BYTECODE is built once and shared by both arms, so the only variable is the engine jar**; both halves move together ([[R608]]); ABSENT is its own column and never folded into pure ([[R636]]). **Fix-only window `687157f..04eb782`, 112 of 118 jars. CONSUMER vs STANDALONE ([[R665]]): function 139 (0.719%) vs 567 (0.0816%); package **54 vs 7**; artifact **3 vs 0**; concrete LOST 1 vs 0; entries moved 33 of 112 vs 23 of 118.** Gate-VALIDATED, not predicted: category 1 **12/12** moved 0->1, category 2 control **12/12** (bare must not move, `+Unknown` must), **0 mispredictions in 24**. | **CALIBRATION IS IN THE SAME COMMIT AND INCLUDES ITS OWN FALSIFICATION (§1b).** On the measurement's own pair: [[R595]] PRE `exit=0 inferred=[]` — the shipped sin — to POST `exit=1 ['Fs']`, with a one-variable control (delete only the initialiser) red on both; [[R530]]b PRE `exit=0` to POST `exit=1 ['Net']`, anon-class control red on both. **And identical arms make it FAIL in BOTH directions** — `post,post` gives *the arm cannot see R595*, `18752cb,18752cb` gives *the POST engine does not FAIL this consumer*, exit 1 each. **Attribution matches the standalone census exactly: 24 of 24 flips on four bisected entries land on `d17dc66` alone ([[R530]]b)** — the same one-commit concentration. **Two premise corrections the lane made:** the R595 SHAPE occurs only **5 times in 117 real libraries** (so [[R668]] is right that the census is blind to it, and the other half is that the shape is RARE); and **`jars/pre.jar` is candor-java 0.34.0 / spec 0.34 — the WIDE window's PRE, not the fix-only one** (`mid.jar` is 687157f), so calibrating against it shows R595 red on both arms and **would have read as *the arm cannot see it***. Wide arm reported separately: 2,863 function / 2,432 package / 52 artifact, **11 concrete LOST**. `corpus-ab.py` REFUSED (exit 3) rather than silently dropping six probe-less jars; they are named in `entries-noprobe.txt` and excluded from every denominator. |
| R672 java: **A CONSUMER CONTRADICTS THE REPORT IT WAS HANDED AND TURNS AN EXPLICIT UNCERTAINTY INTO A CERTAINTY — A SILENT UNDER-REPORT THAT SHIPPED IN 0.39.2, and the standalone census is STRUCTURALLY INCAPABLE of seeing it.** | 2026-09-25 | java — **FIXED 2026-09-26 by [[R685]]**, candor-java `a75cdbd`. Was: OPEN, SHIPPED, CARDINAL SIN. Found by [[R671]]'s chained arm on its first measurement. **The mechanism in this row's brief was WRONG — it is not a coverage grant consulted before the dep row; the dep row is never read at all** | `C_GitHook.dispatch` reads `['Clock','Env','Exec','Fs','Log','Net','Rand','Unknown']` with `unresolved:true` on PRE and **`['Exec']` with `unresolved:false` on POST — while the LIBRARY'S OWN PUBLISHED ROW for `GitHook.call` is BYTE-IDENTICAL in both arms.** So the consumer is not reading a changed dependency; it is overriding one. `deny Fs` **1->0**, `deny Fs Unknown` **1->0**, `deny Net` **1->0**; control `deny Exec` 1->1. **Bisected to `687157f..1d51e23`, whose only `src/` commits are [[R498]]'s two, which charge `org.eclipse.jgit.hooks` WHOLE-PACKAGE.** Widened past the trigger rather than stopping at it: **not dispatch-specific** — direct `PreCommitHook.call()` and `Hooks.preCommit(r,o).call()` collapse identically. | **THE COMMIT THAT INTRODUCED IT SAYS IN ITS OWN MESSAGE WHY NOBODY SAW IT:** `f2749b1` — *CORPUS REACH IS ZERO … the A/B is SAFETY-ONLY*. **A generated consumer references it.** That sentence is the whole argument for [[R671]]: an engine change whose corpus reach is zero is not a change with no consumers, it is a change this corpus has no consumer for. **Audit boundary stated and justified: all 9,672 probe quals across 112 jars were checked, and jgit hooks is the only such owner in this corpus** — so the finding is bounded by measurement rather than by where it was found. Needs its own lane; candor-java was owned by another lane when this landed. |
| R673 instrument (candor): **THE CHAINED ARM'S OWN LIMITS, NAMED RATHER THAN DISCOVERED LATER.** | 2026-09-25 | instrument (candor umbrella) — **(a) CLOSED 2026-09-26 by [[R688]]**, candor `aa61fbf`: the rust arm is built, calibrated and run. **(b), (c) and (d) STAND**, and (c) is now MEASURED rather than suspected — see the note. Remaining residual: swift and ts have no chained arm ; **(a) CLOSED 2026-09-26 by [[R688]]** — the rust arm is built, calibrated and measured | **(a) RUST WAS NOT RUN.** The arm is built and calibrated for java only; the rust half ([[R662]]/[[R664]]'s engine) is unmeasured, so *the fixes flip consumer gates* is currently a JAVA statement. **(b) CLASS SCOPE IS DEGENERATE BY CONSTRUCTION** — a generated consumer has one probe class per shape, so its 139 equals the function figure and is NOT comparable to [[R665]]'s standalone 163. Quote function, package and artifact; do not quote class. **(c) SIX JARS HAVE NO PROBE** and are excluded from every denominator by name. **(d) The R595 shape is rare** (5 occurrences in 117 libraries), so the arm's power against that specific defect class is low even though its power against dispatch is high. | **AND (c) TURNED OUT TO BE THE HEADLINE, not a caveat: the consumer arm is NOT A SUPERSET of the producer arm, it is a DIFFERENT POPULATION.** [[R688]] measured it — **only 6 of rust's 31 producer-scope flips are reachable from a public probe at all, while 13 of the 16 consumer-moved entries are entries the standalone arm reports as COMPLETELY UNMOVED** (tower, winnow, futures-lite, rand, tracing-core). So a standalone census and a chained census do not bound each other in either direction, and no figure from one may be quoted as an upper or lower bound on the other. Filed so the next reader does not re-derive them, and so no figure from this arm is quoted past its evidence — which is the failure [[R666]] recorded for the 118-jar roster and [[R665]]'s lane avoided by refusing to quote its own package-scope number. |
| R674 java: **ROUTING [[R131]]'s CHARGE THROUGH THE TWO REFINERS FIXES [[R622]]/[[R623]]/[[R624]] — AND FLIPS `deny Unknown[unresolved]` FROM FAIL TO PASS ON 20 OF 25 JARS. THAT IS A GATE RELAXATION AND NEEDS A RULING.** | 2026-09-25 | java — **RULED 2026-09-25: MODIFY, THEN SHIP.** Route both refiners for every supertype effect EXCEPT the `Unknown` LABEL, which is withheld for [[R675]]. That ships [[R623]] and [[R624]] and the injection-taint arm — every one FAIL-CLOSED — and changes **no `Unknown[…]` gate anywhere**. **[[R622]] stays OPEN, re-pointed at [[R675]]**, and its two tests become its acceptance tests | All three rows reproduced first, each with its classified-spelling control in the same class and the same scan, the one variable being the STATIC RECEIVER TYPE. **The fix is 11 non-comment lines:** `externalSupertypeEffects`' result is captured and the two refiners re-run per supertype effect — **the SAME code the classified path uses, not a second copy of a guard** (§G). `effect` is still never assigned; mechanically checked, the five `effect = …` sites are byte-identical pre and post. **A/B over 452 jars: ADDED 0 / REMOVED 0 / CHANGED 620** (0.0508% of 1,220,848 rows), `inferred`-only key **0 changed**, REACH 1,097 hits across 110 of 452 entries, buckets 1/2/3 all zero. **All 620 audited in full, ground-truthed from `javap` rather than from candor's own report: ZERO losses of any field** — 614 gained `unknownWhy`, 6 gained `incomplete:["Fs"]`. Idempotence measured rather than asserted: a probe build running both refiners TWICE at every call site produced byte-identical reports over 30 jars. | **THE RULING'S REASONING, which is the part the raw numbers hide: THE 918 HOLES DO NOT MOVE TO `dispatch` — THEY MOVE TO `reflect`, WHICH [[R675]] ALREADY FILES AS WRONG FOR THIS SHAPE.** The sweep shows it exactly: `deny Unknown[dispatch,unresolved]` — a policy **the engine's own parse-time warning steers users toward** — goes 146,375 -> 145,457, **a loss of precisely 918.** So the relaxation was buying consistency between two spellings ONTO THE WRONG CLASS, and R675's fix delivers that consistency correctly and for free. **Also settled: `[unresolved]` alone is, by SPEC §6.2 and by that same engine warning, the NARROWEST reason form and not the conservative one** — a hole leaving it because it gained a CORRECT class is the gate becoming correct; a user who cannot see the taxonomy wrote bare `deny Unknown`, which is unchanged at 158,243. **Two remedies were considered and REJECTED:** a new reason class for supertype-derived holes (it re-splits the spellings by class, WORSE than today, and changes normative cross-engine §6.2 vocabulary for a labelling bug); and tagging the supertype path `dispatch:` alone (correct label, but `[dispatch]` then fires on `DataInputStream.read` and not on `FilterInputStream.read` — **the second-spelling defect reintroduced with the classes swapped**). **THE ORIGINAL COST, recorded because the modified fix must reproduce ZERO of it: `deny Unknown[unresolved]` NAMED ALONE goes 918 violations -> 0, flipping 20 of 25 high-reach jars FAIL -> PASS** (2 more go 1->2). Every other reason-scoped form holds or tightens: `[reflect]` 134,831 -> 136,370 with one jar 0->1, `[reflect,unresolved]` 135,749 -> 136,370, bare `deny Unknown` unchanged at 158,243. **Nothing goes silent** — the row still carries `Unknown` and now NAMES its hole — and on the `gate --report` route the same policy exits **2 (refused)**, not 0. But naming a hole necessarily removes it from the `{unresolved}` floor, so on the scan route a real policy form relaxes. **Sec 9, and the lane widened past its brief: `effect` has SIX consumers in `handleMethodInsn`, and [[R131]]'s own comment names only three of the four fallbacks — `kappaLedger` is a FOURTH**, whose `effect == null` arms record the kappa blind spot and the Spring floor `Unknown`. **Assigning `effect` — the obvious fix — would have deleted those too.** All four fallbacks untouched; only the two refiners route. **Fixture correction to [[R624]]:** its briefed fixture does not show the defect, because `FileWriter.close()` already poisons `fsDirect` for an unrelated reason — drop the `close()` and it reproduces. |
| R675 java: **`effectMetadata` LABELS EVERY CLASSIFIER-RETURNED `Unknown` AS `reflect:` — ALL 38 RULES — including DELEGATION and NATIVE calls, so every reason-scoped gate is asking the wrong question.** | 2026-09-25 | java — OPEN, PRE-EXISTING; applies to BOTH spellings, so it is not a [[R674]] regression | The java.io filter/buffered delegation is labelled `reflect:` while **the classifier's own comment calls it a delegation *to a wrapped stream of unknown concrete type … which candor cannot resolve*** — that is a **dispatch**. `com.sun.jna.Function.invoke` is labelled `reflect:` and is **native**. **~700 of [[R674]]'s 704 new tokens land on this mislabel**, and it is why `deny Unknown[dispatch,unresolved]` LOSES 918 violations across 22 jars. | Relabelling moves BOTH spellings at once and needs its own A/B — filed rather than folded into R674, because a reason-class change is a different decision from a hole-naming change and mixing them would make neither measurable. |
| R682 java: **`scan --policy` AND `gate --report` DISAGREE ON VIOLATION COUNT FOR THE SAME JAR AND THE SAME POLICY, IN BOTH DIRECTIONS.** commons-compress, bare `deny Unknown`: **857 vs 866**, with `fn` keys spelled differently. | 2026-09-25 | java — OPEN, UNDIAGNOSED, at HEAD; **explicitly NOT established as a defect** | Observed while measuring [[R674]] and **not chased**, because settling it needs an owner who has the SPEC: §3.3.1 pins route equality, and the honest position is that this **has not been established as a break rather than a legitimate route difference.** The differing counts go BOTH ways, which is what makes it interesting — a pure scope difference would go one way. | Filed at the strength the evidence supports, per the BACKLOG rule that an entry saying what it MEASURED can go stale honestly while one stating a conclusion cannot. **[[R662]]'s census already recorded that `gate --report` is unusable on this corpus for a different reason** (1,083 of 1,626 entries carry a non-empty `excluded[]`, so it exits 2 INCOMPLETE on both arms) — worth checking whether these are one mechanism or two. |
| R683 java: **THE [[R674]] RULING APPLIED — WITHHOLD THE `Unknown` LABEL, SHIP THE TWO FAIL-CLOSED HALVES. `deny Unknown[unresolved]` HOLDS AT 918, NOT 0.** One guard, `if (se != Effect.UNKNOWN) effectMetadata(…)`, the only non-comment line differing from the first cut. | 2026-09-25 | java — **FIXED as [[R623]] + [[R624]]**, candor-java `ef7b210` (AMENDS `448dad8`); **[[R622]] STAYS OPEN, re-pointed at [[R675]]** | **Three predictions stated BEFORE the run, all held.** A/B over the 452-jar census, pre arm rebuilt from `04eb782` so it could have failed (jar sha1 prefixes aa08496 vs f955ecd — spelled WITHOUT backticks because they are JAR hashes, not commits, and backticking them makes this row cite two commits that never existed; see [[R645]]): **ADDED 0 / REMOVED 0 / CHANGED 6** (was 620), `inferred` key 0/0/0, buckets 1-2-3 zero, **REACH UNCHANGED at 1,097 hits / 110 of 452 entries — so the withhold is INSIDE the loop, not around it.** All 6 audited in full and ground-truthed from `javap`, never from candor's own report: every one `incomplete: absent -> ["Fs"]`, zero losses of any field. **Six policy forms x 25 jars x 2 arms, 300 runs, 0 errors: every `Unknown[…]` count BYTE-IDENTICAL, 0 jars differing, 0 exit transitions** — `[unresolved]` alone stays **918**, `[dispatch,unresolved]` stays 146,375, and the pre column reproduces the first cut's digits exactly, which is what makes it a comparison. R623 fixture alone: `allow Fs allowed.txt` exit 0/0 -> **exit 1/1** (AS-EFF-008), `fs:["read"]` -> no claim at all; **calibrated with `allow Fs nothing.txt`, which fires on BOTH arms, so pre's exit 0 is a real pass and not a dead check.** | **THE PREMISE WAS ATTACKED AND HOLDS, verified by ENUMERATING the writes rather than reading around them.** `effectMetadata` writes `tainted`/`unknownWhy`/`fsDirect`; `extractLiteralSurfaces` writes `cmdsDirect`/`pathsDirect`/`hostsDirect`/`tablesDirect`/`surfaceIncomplete` and **reads none of the first three**. All three `surfaceIncomplete.add("Fs")` sites — including [[R421]]'s stat-locator rung — are INSIDE `extractLiteralSurfaces`, so R623 needs nothing the withhold removes; R624's poison sits on the `effect == Effect.FS` arm the guard cannot reach. **The guard subtracts EXACTLY the label:** `effectMetadata`'s only `UNKNOWN`-firing arm is the `reflect:` tag, the other two being `INJECTION.contains(effect)` (`{Fs,Exec,Db,Net,Llm,Env,Ipc}`, never `UNKNOWN`) and `effect == FS`. **[[R622]]'s two tests are `@Disabled`, NOT deleted, and PROVED GENUINELY BLOCKED — with the `@Disabled` removed, 2/2 FAIL under the withhold.** **AND [[R675]] ALONE WILL NOT RE-ENABLE THEM**, written into both the code comment and the test javadoc rather than left to be rediscovered: R675 relabels at the SOURCE and moves the supertype spelling only, so `deny Unknown[dispatch]` would then fire on `FilterInputStream.read` and not `DataInputStream.read` — the second-spelling defect with the classes swapped. **Delete the guard and the two `@Disabled`s in ONE change.** **COMMIT AMENDED rather than added on top, deliberately:** `448dad8`'s message asserted `CHANGED 620`, `R622 FIXED`, `918 -> 0` and a 4/4 revert test, **none of which ships** — leaving it on `main` would put a message there asserting a gate relaxation that never happened, which is what the put-the-numbers-in-the-message rule exists to prevent. **REACH-ZERO SUB-BRANCH, recorded now rather than discovered later (§E1):** the ruling names the AS-EFF-007 injection-taint arm as shipping; it is live in source but **0 of 452 jars changed `tainted`**, in this A/B or the first cut's audit — SAFETY-ONLY over this census, and the measured payoff is entirely R623's `incomplete` and R624's poison. **Near-miss reported rather than buried: `--json /dev/null` makes candor REFUSE (exit 2), and the lane briefly read that as a difference between the measured and shipping builds.** It was the harness; with a real `--out` the shipping build's reports are byte-identical to the measured post arm on the 8 highest-reach jars. |
| R679 rust: **[[R659]]'s *GENUINE RESIDUAL* IS NOT ONE — all 12 lost-effect rows are CORRECT, and both causes name those crates in their own commit messages.** The census said 7; it is 12, because rdkafka-0.36.2's producer `drop` bodies carried no `Clock` at the baseline, so the same removal produced no lost-effect row there. One mechanism, two visibilities. | 2026-09-25 | rust — **CLOSED 2026-09-25, NO CODE CHANGE** | **rdkafka, 11 rows:** `94446f5` ([[R173]], *the `?` early-exit veto ignored evaluation order*), whose message already reads *rdkafka 12 (root `BaseConsumer::new`)*. Ground truth from SOURCE: every `?` precedes the construction and the value is RETURNED; `Deadline` — the only `Clock` source — appears in `base_producer.rs` at 363/515 only, never in a constructor. **EXECUTED drop counter on an isolating fixture, everything else held constant:** the rdkafka shape `let n = v.ok_or(())?; Ok(Guard{n})` drops **0** on both paths (baseline charged it, HEAD does not); the control `let g = Guard{..}; v.ok_or(())?; Ok(Wrap{g})` drops **1 on Err** and is charged by BOTH arms; the [[R187]] loop control drops **2** and is charged by both. **The veto still fires where the drop is real; only the ordering was corrected.** **terminal-trx, 1 row:** `b45a169` ([[R459]]) — `src/unix.rs:13-14` is `#[cfg(test)] mod pty_utils;`, so the module does not exist in a default build. `pty_pair` really does call `File::open`, so the baseline was right about the CODE and wrong about the BUILD. | **HEAD IS LOUDER, NOT QUIETER, IN BOTH — which is the test that matters, since a lost effect replaced by SILENCE is the cardinal sin and one replaced by a disclosure is precision.** terminal-trx: HEAD carries `excluded: [{class:"test-module", count:2}]`, and the gate NAMES the function and its file and exits **2 INCOMPLETE** — *policy NOT enforced — 1 function(s) OUTSIDE this scan's scope perform an effect this policy denies* — while `--include-tests` restores `['Fs','Unknown']` byte-for-byte. 32 of 200 sampled crates carry a `test-module` exclusion, so the disclosure is structural rather than lucky. rdkafka, checked AS A DEPENDENCY per brief §2 with `CANDOR_DEPS`: `make_consumer` `['Clock','Log']` -> `['Clock','Log','Unknown']` (**`Clock` KEPT, disclosure GAINED**); `make_producer` `['Clock']` -> `['Log']`, correct per source. **The lesson for the census: its bucket labelled *`#[cfg(test)]` helpers* was keyed on names that LOOK test-ish — the exact heuristic [[R459]] exists to replace — so that bucket should be re-derived against `excluded[]` rather than against names.** All gate flips re-measured with EXACT scopes per [[R661]]. |
| R680 rust: **THE *UNCONDITIONAL* ESCAPE ROUTES ARE EXEMPT FROM [[R173]]'s POSITIONAL `?` FILTER, so a `mem::forget` / `ManuallyDrop::new` or a FIELD/INDEX/DEREF STORE lying AFTER a `?` certifies a value that really dies AT that `?`.** `lang.rs` ~4368, `acc.names.extend(uncond.names)`, on a comment asserting *a forgotten value's destructor does not run whichever exit the function takes* — **false when the route follows an early exit.** | 2026-09-25 | rust — OPEN. **CARDINAL SIN. PRE-EXISTING (identical on the 2026-08-31 baseline and HEAD), so NOT an [[R659]] regression.** Found by widening [[R679]]'s audit past its own trigger | **EXECUTED, not inferred.** `let g=H{..}; let r=step(i)?; std::mem::forget(g); Ok(r)` and `let g=H{..}; let r=step(i)?; self.g=Some(g); Ok(r)` each drop **1 `H` inside the frame on the Err path** (0 on Ok); **both are ABSENT from `functions[]` on both arms** — a §2 rule-3 purity claim — and `deny Fs <qual>::` exits **0**. A blanket `deny Fs` catches it only INCIDENTALLY, because `H::drop` is independently in the report, which is the §F1 shape. Controls with no early exit (`forget_plain`, `fill_plain`) stay pure correctly on both arms AND on the upper-bound arm, so this is not a blanket over-charge. | **THIS IS [[R209]](b)'s CLASS ONE ROUTE OVER** — that commit fixed the CLOSURE member of `sites.escapes` and its own message says *This does NOT close the class*; the `mem::forget`/`ManuallyDrop::new` operand and the field/deref `assigns` half were left. **DO NOT FIX BY REMOVING THE EXEMPTION:** `collector.rs:43` already prices that at **551 FABRICATED hard-effect charges over 1,561 crates, 0 of 28 sampled a real in-frame drop.** The fix must make the exemption **POSITIONAL** like everything else after R173 — a route survives a `?` only if the route's OWN position precedes it — which needs route sequence numbers in `EscapeSites` and its own ADDED-direction corpus A/B. **The lane deliberately did NOT fix it here rather than land it unpriced mid-join, which is the right call. REACH IS UNPRICED AT FILING:** an instrumented HEAD counting leaves re-added after the filter fires in **809 of 1,626 crates** — a LOOSE UPPER BOUND, since it cannot separate a route before a `?` from one after; a source scan found **0** `mem::forget`-after-`?` in five RAII-heavy crates and **325 of 84,533 fn bodies (0.38%)** carrying the field-store-after-`?` shape; lapin-4.11.0 shows 0 arm-only rows. **ASKED OF SWIFT, ANALYSIS-ONLY AND UNVERIFIED:** `CallCollector.swift` ~6969 returns `true` unconditionally for a member/subscript assignment destination while its sibling route is position-sensitive (`guaranteedToEscape(after:)`, `containsEarlyExit`) — the same shape. Java and ts have no destructor glue, so the question does not translate there. |
| R681 spec: **SPEC.md HAS NO CLAUSE FOR THE TRAILING-SEPARATOR EXACT SCOPE FORM, while ALL FOUR ENGINES IMPLEMENT IT and PART 64 PINS IT FOUR-WAY.** A third-party engine written from the SPEC alone would reimplement the prefix-match silence the exact form exists to escape. | 2026-09-25 | candor-spec — OPEN as a DOCUMENTATION defect with a real consequence; the [[R661]] decision | §6.2 pins the PREFIX rule (*the last segment is a prefix*) and that rule is **fail-closed for `deny`/`pure`/`forbid`**, so [[R661]] is correctly a caveat and not a defect — `only … to` already uses the non-prefix matcher, and `scope_is_exact` gives the exact form. **What is missing is the exact form's CLAUSE.** Implemented at rust `policy.rs:854`, java `Policy.java:1868`, ts `policy.mjs:733`, swift `Policy.swift:789`, and pinned by `conformance/run.sh` PART 64 — **four engines and a conformance PART agreeing on behaviour the normative document does not describe.** Measured cost of not knowing it: over 200 roster crates / 23,983 rows, **10.27% of rows are a strict prefix of a sibling qual, and 0.125% (30 rows) are where the sibling carries an effect the target lacks** — i.e. where the prefix rule changes the answer. Concentrated in `regex-automata` (15) and `git2` (8). Three published examples where a scoped probe on a genuinely pure qual fires on a sibling: `rustls-native-certs-0.8.4 deny Fs load_pem_certs` (bare 1, exact 0 — `load_pem_certs_from_dir` does the `Fs`), `execute-0.2.15 deny Exec Execute::execute`, `async-global-executor-3.1.0 deny Env init::init`. | Remedy is **one SPEC §6.2 sentence plus a README line per engine** — documentation of already-shipped, already-pinned behaviour, **zero verdict movement**. Filed rather than written because the lane that found it owned candor-rust, not candor-spec. **Note the shape: this is the ⟨0.34⟩ ITEM 1 failure in reverse** — there, four engines shipped prose citing a clause that did not exist; here, four engines ship BEHAVIOUR that no clause describes, and the conformance PART pins it so nothing will ever go red. |
| R677 instrument (candor-spec): **[[R636]]'s VACUITY BOUNDARY WAS DRAWN AROUND ITS OWN TRIGGER — `hasnt={Net}` COULD NOT FAIL IN FOUR ARMS OF PART 92, NOT ONE.** R636 recorded the by-construction vacuity for `c12_consumer_pure_only_union`. Rendering all 13 arms four ways and counting each language's own sink shows `c2_zero_impl`, `c3_pure_only`, `c9_consumer_zero_union` and `c12` contain **zero `Net` statements anywhere in the fixture** — no third package, and neither the `impl` nor the `zero` dependency has a sink. | 2026-09-25 | instrument (candor-spec) — **OPEN ONLY ON THE FOUR-WAY RUN**, exactly as [[R636]]: the fix is written and calibrated without an engine, the run that prices it has not been started. Sec 9, in a row itself filed against a Sec 9 failure | Measured, not read: `render_rust`/`render_java`/`render_ts`/`render_swift` over every arm, `str.count(SINK[lang])` over every rendered file outside `node_modules`. **48 cells, and the four arms above are 0 occurrences in all four engines.** `c2` and `c9` were never at risk — `unknown=True` is their real assertion and an absent or pure row fails it — but `c3` and `c12` had nothing else, which is [[R636]]'s finding. The two halves are independent defects with one symptom: `judge()` did not read `facts()`' `absent` flag (R636), AND the `hasnt` cells named an effect no fixture performs (this row). Closing only the first would leave the fabrication guard `c3` exists to be still unable to go red. | **FIX WRITTEN AND CALIBRATED WITHOUT AN ENGINE; the four-way run that prices it is OUTSTANDING and is what remains.** Three changes. (1) `judge()` rejects `f['absent']` unless the arm carries a NAMED `absent_ok`; exactly one arm does — `c3_pure_only`, where an omitted pure non-dispatching row is what a conforming engine produces under SPEC §2 rule 3 — and its licence is PRINTED in the cell. (2) The `impl` and `zero` dependencies gain an effectful function the consumer never calls, taken from `gen_differential.EFFECTS` rather than re-spelled (attack G), so `c2`/`c3`/`c9` gain a reachable `hasnt`; it is added to BOTH variants so the `c2`-vs-`c3` cross still differs only in the implementor set. (3) `c12` gets its own consumer body that CALLS that function, so the row must exist and carry `Fs` for reasons unrelated to the dispatch, and `unknown=False` — its ⟨0.40⟩ question — is asked of a row rather than of silence; `hasnt` is dropped there rather than kept vacuous. New `--selftest` (13 checks, wired into `scripts/doc-gates.sh`) drives `judge()` against the absent value, ratchets the licensed set, refuses a licence that contradicts its own arm, and RE-RENDERS every fixture to refuse an unreachable `hasnt`. **Calibrated in three directions, each shown RED first:** restoring the pre-fix `judge()` reddens *R636 shape is REJECTED when absent*; disabling `app_body`'s new branch reddens *c12 renders its OWN body*; deleting the dependency's carrier reddens *hasnt guard is clean on the real table*. **NOT YET RUN FOUR-WAY** — three engine lanes were editing their trees, and `conformance/run.sh` reads trees. Every touched fixture COMPILES four ways (40 of 40 arm-engine builds, `cargo`/`javac`/`tsc --noEmit`/`swift build`), which is the E3 half that can be proven without an engine. |
| R678 instrument (candor-spec): **TWO COMMENTS IN `gen_chained_dispatch.py` SAY A CONSUMER BODY IS *never rendered* FOR java AND swift. IT IS RENDERED FOR BOTH, BY `c9_consumer_zero_union`.** `JAVA_APP['nesteddispatch']` carries *present only so the table is TOTAL — java declares c7 inexpressible (no re-export), so this is never rendered*, and `SW_APP['nesteddispatch']` the matching *as above … so this is never rendered*. | 2026-09-25 | instrument (candor-spec) — OPEN as a STALE-COMMENT defect; **no verdict is wrong today** | TRUE WHEN WRITTEN and expired when the thing it points at moved: `c7_nested_abstraction` is the only arm that skips java and swift, and it was the only `nesteddispatch` arm at the time. `c9_consumer_zero_union` (R533) was added later with the same `entry` and NO skip list, and `c12_consumer_pure_only_union` after it, so both engines have been rendering that body ever since. Verified by rendering: java and swift produce an `appSize` for `c9` in the census run that produced [[R677]]'s figures. | **Why it is worth a row rather than a silent edit: the comment is the reason a reader would not test that body on those engines**, and it sits on the exact shape `c9` exists to measure — a consumer dispatching directly on a foreign abstraction, which java and swift are both still xfailed on. It is the [[candor-documented-limitation-is-not-measured]] third variant (true when written, expired because a later arm moved under it). Remedy is one sentence per template naming `c9` as the arm that renders it, landed with a check that `app_body`'s consumers match the arms' declared `skip` sets — the same shape as `assert_arm_table()`, which this commit adds for a different invariant. |
| R688 instrument (candor): **THE RUST CHAINED CENSUS ARM — and it settles the question the standalone census could not: MODULE AND CRATE SCOPE ARE NOT ZERO AT A CONSUMER, and the whole ⟨0.39⟩ obligation-3 rung is invisible to every standalone scan BY CONSTRUCTION.** Closes [[R673]](a). | 2026-09-26 | instrument (candor umbrella) — **BUILT, CALIBRATED AND MEASURED 2026-09-26**, candor `aa61fbf` | `bin/corpus-chained-rs.sh` + `bin/corpus-chained/consumer-gen-rs` (a `syn` binary) + a rust scope ladder in `bin/corpus-chained-judge.py` (`--coverage-rs` / `--report-rs`). **The consumer is GENERATED from each crate's public API as `syn` sees it**, not from the engine's own report — a candor report omits pure functions by design (`tower-service`: 2 rows for 4 analysed units) and a consumer generated from it could only probe what the engine already sees. `candor-scan` reads SOURCE, so the consumer need not compile: no javac and no recovery loop, which is the one place rust is easier than java. Every probe body holds EXACTLY ONE CALL, the callee's arguments supplied as the probe's own parameters — a `Default::default()` there would be a second, synthetic call whose effects are indistinguishable from the one under test. **TWO VARIANTS, because ⟨0.39⟩ conjunct 4 is asked of the CRATE and one crate cannot both withhold and supply an implementor:** `noimpl` (the java arm's shape, nothing planted) and `impl` (an effectful implementor of every public trait, in ONE separate top-level module). **Window `f69ade4` (v0.39.2) .. `17c3f9e` (HEAD). Corpus `bin/corpus-census-rust.tsv`, 1625 of 1626 acquired** (candor-query 0.32.1 is no longer obtainable); **1294 crates yield a `noimpl` consumer and 1094 an `impl` one**, 110,224 + 46,449 probe quals; the 331 and 531 that yield no probe are named in `entries-noprobe.txt` and are in no denominator. **STANDALONE 31 fn / 22 parent / 0 module / 0 crate / 0 LOST (1294 crates, 474,305 units) — which reproduces [[R662]]/[[R664]]'s 31 / 22 / 0 / 0 EXACTLY with a different instrument, and that is what licenses reading the consumer rows against it. CHAINED `noimpl` 6 / 2 / **2** / **2** / 12 LOST (110,224 units). CHAINED `impl` 22 fn (55,037 units).** **GATE-VALIDATED, POPULATIONS TAKEN WHOLE RATHER THAN SAMPLED: `noimpl` category 1 6/6 and category 2 25/25; `impl` category 1 22/22 and category 2 25/25 — 0 mispredictions in 78.** A/B per §E1 (`bin/corpus-ab.py`, wide key, multiset): `noimpl` ADDED 133 REMOVED 0 CHANGED 4488, buckets 6 / 3888 / 12 / 715; `impl` ADDED 65 REMOVED 0 CHANGED 644, buckets 22 / 64 / 12 / 611. | **THE WINDOW DECOMPOSES, AND THE SPLIT IS THE FINDING.** `f69ade4..cf8782d` is 18 commits with ZERO rung and carries EVERY verdict flip in the table; `cf8782d..17c3f9e` (the ⟨0.39⟩ obligation-3 commit `e4808bf` plus [[R652]]) carries **0 / 0 / 0 / 0 standalone, 0 / 0 / 0 / 0 in the `impl` consumer, and 3,588 disclosure-only rows (6.03% of post rows) in the `noimpl` consumer.** So the rung's entire measurable effect exists only where a consumer chains a dependency AND supplies no implementor — `e4808bf`'s own conjunct 2 predicts exactly that, and it is why [[R668]]'s *structurally blind* is a statement about arithmetic rather than about corpus size. **Total disclosure at the consumer: 3,888 probes gained `Unknown` only = 3.53% of 110,224 analysed consumer units, against the standalone census's 450 of 706,416 (0.064%) — the channel [[R664]] calls advisory is ~55x more active one hop out.** **Which figures are QUOTABLE, stated so no reader has to guess ([[R666]]/[[R673]] are the record of what happens otherwise):** the standalone row and the `noimpl` scope row are quotable; the `impl` variant's MODULE and CRATE figures are NOT (it plants an effect in the crate, so the crate is already red on both arms — the judge prints that caveat itself); percentages are not comparable to [[R662]]'s because the denominator is a 1294-crate subset, which is why the standalone comparison is quoted as ABSOLUTE counts that match to the unit. **NAMED LIMITS, not discovered later: (a) the generator descends only `pub mod` chains, so a crate whose public API is entirely re-exported from private modules yields no probe — 329 of 1625, ~20%, and the excluded set is biased toward the facade-pattern crates (libc, mio, wasi); (b) 2 crates fail `syn` parsing; (c) the consumer sees FEWER function-scope flips than the producer (6 vs 31) because 25 of the 31 changed library functions are not reachable from any public probe — the consumer arm is not a superset, it is a DIFFERENT population that happens to reach wider scopes; (d) 13 of the 16 consumer-moved entries are entries the standalone arm reports as completely unmoved (tower, winnow, futures-lite, rand, tracing-core), and only 3 (jobserver x2, zip) are shared.** **Calibration is in the same commit and fails in three directions** — identical POST arms give *the arm cannot see R609*, identical PRE arms give *the POST engine does not FAIL this consumer*, and driving the [[R652]] anchor with the whole window's pair instead of its own gives *the one-variable control passes on PRE*, each exit 1. **Two instrument defects found while building it, both of the kind that prints a plausible zero: `${arr[@]}` on an empty array is a `set -u` error on the bash 3.2 macOS ships (every entry GEN-FAILed and the build reported *entries WITH both consumers: 0*), and a SHARED `CARGO_TARGET_DIR` across two worktrees checked out in the same second produced BYTE-IDENTICAL binaries for two different commits — `cargo build --release` exited 0 in 0.02s without rebuilding, caught by `shasum` and not by the exit code.** |
| R689 rust: **[[R590]]'s *NOT a gate flip* IS A PRODUCER-SCOPE VERDICT — AT A CHAINED CONSUMER THE SAME FOUR ROWS TAKE `deny Log` FROM exit 1 TO exit 0.** And the general rule behind it: [[R660]]'s *an `interfaceUnion` row is provably invisible to every gate* is also producer-scope, because a consumer's join copies the union's effect set onto a NON-union row of its own, where every gate reads it. | 2026-09-26 | rust — **OPEN as a BARE-FORM GATE RELAXATION, shipped in v0.39.2's successor window; NOT a silent under-report.** Found by [[R688]]'s chained arm, which reports it as the whole of its bucket 3 | The 12 lost-effect rows of both chained variants are one mechanism: hickory-proto 0.26.1/0.26.2/0.26.3, `serialize::binary::BinEncodable::to_bytes`, `['Log']` -> `['Unknown']`. **Bisected to `05f3325` ([[R576]](b)), whose own commit message names these three crates and asserts *`deny Log` / `deny Log Unknown` exit 1 on both arms*.** That assertion is TRUE and it was measured at the producer. **Measured here with the REAL gate, exact scopes per [[R661]] (trailing `::`), and with a HAND-WRITTEN consumer as well as the generated one so it is not a generator artefact:** `pub fn encode_it(x: &dyn hickory_proto::serialize::binary::BinEncodable) { let _ = x.to_bytes(); }` reads `['Log']` on `f69ade4` and `['Unknown']` + `unknownWhy:['dispatch:BinEncodable.emit']` on `17c3f9e`; **`deny Log encode_it::` exit 1 -> 0**, and the generated probe `deny Log serialize::binary::p76_dyn::` does the same. **CONTROLS, all four run: `deny Log Unknown encode_it::` is exit 1 on BOTH arms — so nothing went silent, the row discloses, and this is the [[R674]] shape rather than the cardinal sin; producer crate-wide `deny Log` is exit 1 on both arms, reproducing R590's own check byte for byte; and producer `deny Log serialize::binary::BinEncodable::to_bytes::` is exit 0 on BOTH arms — because the PRE `Log` sat on an `interfaceUnion` row that no gate reads.** | **THAT LAST CONTROL IS THE MECHANISM AND THE REASON R590 COULD NOT HAVE SEEN THIS: at the producer there is no scope in which the PRE `Log` was ever gate-visible, so every gate R590 could run was green-on-green. The consumer's ⟨0.39⟩ join is what converts a union row into a row with a `loc` and a policy scope.** The general consequence outranks the instance: **any change to what an `interfaceUnion` row carries is gate-visible one hop out, so [[R660]]'s invisibility argument may not be used to price a union-row change as verdict-neutral** — and [[R590]]'s own open question (*when a body-analysed row displaces a union row, can the union have known MORE?*) is therefore a question about consumer gates, not only about report content. **Direction, stated because it decides whether this is a defect: PRE was OVER-CONFIDENT (`Log`, no `unresolved`, over an implementor set wider than the bounded-CHA limit of 12 — incomplete by construction) and POST is honest but less precise. So the row is filed as a relaxation needing a ruling, exactly as [[R674]] was, and NOT as a regression to revert.** What this leaves unexamined, named rather than implied: the population is the 4 remaining `interfaceUnion` rows in hickory-proto and every other crate where a union row and a `loc` row compete for one key — R590 names that set and nobody has enumerated it; [[R688]]'s arm can now price it, and the 12 rows here are only the ones this window happened to move. |
| R690 rust: **A GENERIC-BOUND RECEIVER WHOSE TRAIT IS DECLARED IN A CHAINED DEPENDENCY SILENTLY DROPS THE EFFECT OF THE IMPLEMENTOR THIS CRATE SUPPLIES — `<T: dep::Tr>`, `impl dep::Tr` and `for_each(dep::Tr::m)` all read PURE with NO disclosure, while the byte-identical spelling over a LOCAL trait is charged.** CARDINAL SIN, PRE-EXISTING, published in candor-scan 0.39.2 and live at HEAD. | 2026-09-26 | rust — **OPEN. CARDINAL SIN.** Found by [[R688]]'s `impl` consumer variant, which exists to supply an implementor and is the only arm that can see it | **ONE VARIABLE: whether the trait is declared in this crate or in a `CANDOR_DEPS`-chained dependency.** Nine call spellings, the same single effectful implementor supplied by the calling crate (`std::net::TcpStream::connect`), the same engine (`17c3f9e`, and `f69ade4` is identical so this is not the window's doing). LOCAL column then CHAINED column: `&dyn Tr` param Net/Net; **`<T: Tr>` Net/PURE**; **`impl Tr` Net/PURE**; `let r = &*box` Net/Net; `let f = Tr::m; f(r)` ABSENT/PURE; `Tr::m(r)` ABSENT/PURE; concrete receiver Net/Net; **`v.iter().for_each(Tr::m)` Net/PURE**; `box.m()` Net/Net. **Real gate, exact scopes: every PURE cell is `deny Net <q>::` exit 0 AND `deny Net Unknown <q>::` exit 0 — no `Unknown`, no `unknownWhy`, no `unresolved`. A positive SPEC §2 purity claim over a call that reaches a real socket in the same crate.** The charged cells are the over-charge control and they are exit 1 on both arms, so the fixture is discriminating and not simply unreachable. **REACH ON REAL CODE: 5,708 generated probes across 502 of the 1094 `impl` consumers are silent in the `gen` spelling while their `dyn` sibling on the SAME trait member carries a concrete effect; 1,293 more disclose `Unknown` instead, so 81.5% of the population is silent rather than hedged.** Examples: `ahash::random_state::RandomSource::gen_hasher_seed`, `aho_corasick::automaton::Automaton::start_state`. | **AUDIT BOUNDARY STATED AND WIDENED PAST ITS TRIGGER (§9). The class is NOT *fn-references* and NOT *generics*: a FREE fn through a binding, a free fn in argument position, an INHERENT method through a binding, an inherent method in argument position and the implementing TYPE's own path (`Mine::emit(r)`) are ALL charged. It is specifically a receiver whose trait identity must be resolved through the TRAIT, across the crate boundary.** **Two of the nine cells are NOT new: `Tr::m(r)` and `let f = Tr::m; f(r)` are [[R570]]'s open UFCS class, re-confirmed at HEAD here (ABSENT locally, PURE chained) — they are reported as a re-find, not claimed by this row.** The new cells are the three that are charged LOCALLY and silent CHAINED, which puts this in §F1 q4 — a caller that vanishes when resolution crosses a boundary, where absence is the sin's signature — and makes it the sibling of [[R652]] one index over: [[R652]] was `trait_impls` keyed by LEAF with no locality test; this is the generic-bound path finding no implementor at all once the trait is foreign, while the `dyn` path finds one. **THE HONEST LIMIT ON THE 5,708: the traits and their members are real third-party code, but the EFFECT is planted by the generator's implementor, so the figure prices the SHAPE's prevalence in real APIs and is NOT a count of silent under-reports in shipped crates.** A real consumer that implements a dependency's trait and calls it through a generic bound is the ordinary rust idiom, which is why the shape matters; what is not measured is how often real consumers write that rather than `&dyn`. **Reproduction: `scratchpad/rustlane-r688/matrix` — three crates, `mxdep` (the trait), `mxlocal` (trait local) and `mxchain` (trait chained), the body generated from one template so the two arms differ in the trait's path and nothing else.** |
| R691 instrument (candor): **TWO A/B INSTRUMENT DEFECTS FOUND MID-BUILD BY THE RUST CHAINED ARM, BOTH OF THE KIND THAT PRINTS A PLAUSIBLE ZERO — and one of them made two DIFFERENT COMMITS produce BYTE-IDENTICAL BINARIES.** | 2026-09-26 | instrument (candor umbrella) — **both FIXED in-lane 2026-09-26**, candor `aa61fbf`; filed because the CLASS recurs | **(1) A SHARED `CARGO_TARGET_DIR` gave two arms the same binary.** `cargo build --release` returned **exit 0 in 0.02s without rebuilding**, so the PRE and POST arms of a differential were the SAME ENGINE and every figure would have been a self-comparison — i.e. ADDED 0 / REMOVED 0 / CHANGED 0, **indistinguishable from a change that is correctly inert.** **Caught by `shasum` on the two binaries, NOT by the exit code**, which was 0 and honest. (2) `${arr[@]}` on an EMPTY array under `set -u` on **bash 3.2** — macOS's shell — aborts the loop, so a stage silently processed nothing. | **This is [[R242]]'s class at the BINARY rather than the corpus: a differential whose two arms are the same thing reports exactly what a safe change reports.** The family has now paid for this shape three times in three places — a hollow corpus (`/tmp/candor-corpus`, 0 `.rs` files), a hollow jar (`okio:3.9.0`, correct sha1, zero `.class` files, [[R666]]), and now a hollow ARM. **The general rule the third instance earns: every differential must PROVE ITS ARMS DIFFER before it reports a number, and the proof must be on the artefact rather than on the command's exit code.** The java chained arm ([[R671]]) already does this by construction — it fails when handed identical arms, in both directions — and the rust arm now does too; `bin/corpus-ab.py` should refuse a run whose two `--pre-cmd`/`--post-cmd` binaries hash equal, which it does not currently check. |
| R685 java: **A `classify` ANSWER REPLACED THE CHAINED DEPENDENCY'S OWN ROW — the consumer read MORE CERTAINTY than the report it was handed, and it SHIPPED in 0.39.2.** `Candor.crossDepJoin` was gated `effect == null`, so wherever the classifier had a rule for a member, the dep's published row for that exact key — sitting in `crossDeps` under the hash the call site forms — was **NEVER READ**. | 2026-09-26 | java — **FIXED 2026-09-26**, candor-java `a75cdbd`; closes [[R672]] | **CARDINAL SIN, REPRODUCED ON THE PUBLISHED 0.39.2 JAR.** jgit 6.9.0 publishes `GitHook.call()` as `['Clock','Env','Exec','Fs','Log','Net','Rand','Unknown']`, `unresolved: true`, `incomplete ['Exec','Fs','Net']`, `netClass ['unknown-host']`; **the consumer answered `['Exec']`, `unresolved: false`, `incomplete ['Exec']`.** `deny Fs`, `deny Fs Unknown` and `deny Net` each **exit 1 -> exit 0** against that same dep report; control `deny Exec` stayed 1. Direct `PreCommitHook.call()` and `Hooks.preCommit(r,o).call()` collapse identically, so it is not dispatch-specific. **MECHANISM MEASURED, AND IT IS NOT THE ONE I BRIEFED:** not a kappa/coverage grant consulted before the dep row — the classifier's table **SHORT-CIRCUITS THE JOIN ENTIRELY**, so the row is skipped rather than read-then-overridden. An instrumented build printed each skip beside the row it skipped. **A/B: chained** 112 entries, 8,405 rows/arm, wide key and value — **ADDED 0 / REMOVED 0 / CHANGED 87** (61 narrow `inferred`), CONCRETE-LOST **0**, 47 quals gained a concrete effect = **0.2431% of 19,333 analysed consumer units**; **47 of 47 scoped `deny <E> <qual>` predictions GATE-VALIDATED 0 -> 1 on both real arms, 0 mispredictions.** **standalone** 452-jar census, 1,220,848 rows/arm: **BYTE-IDENTICAL** — the containment claim, with reach measured on the chained arm rather than assumed. No perf cost (2.9s pre / 2.8s post on spring-core). | **BOUNDARY WIDENED PAST THE TRIGGER, AND jgit IS WHERE IT WAS CAUGHT RATHER THAN WHERE IT LIVES: the trigger is a KEY COLLISION — any classify answer, prefix grant or exact owner. 1,835 distinct keys across 20 of 112 entries had BOTH a classify answer and a `crossDeps` entry; 1,807 where the dep row says strictly MORE, 1,671 carrying `Unknown`, and only TEN exactly `[Unknown]`** — so the existing all-Unknown narrowing one arm down would have suppressed 10 of 1,835 and is not the answer. Owners include log4j/slf4j `Logger`, `io.lettuce.core.api.*`, `scala.sys.process.*`, `com.rabbitmq.client.Channel`, `org.apache.http.client.HttpClient`, netty, groovy `Script`, `org.eclipse.jgit.util.FS`; the table holds 64 package-prefix grants and 84 owner-prefix rules. **THE STRONGEST EVIDENCE IS THAT THE ENGINE CONTRADICTED ITS OWN IN-SCAN ANSWER ON IDENTICAL SOURCE:** `Impl implements org.apache.commons.exec.Executor` opening a socket, called through the interface, reads `['Exec','Net']` in ONE TREE and read `['Exec']` the moment the two classes were split across a scan boundary. **Chained-only — which is exactly why 452 standalone scans never saw it.** Non-chained was asked deliberately and shows no suppression: in-scan, classify already UNIONS with local CHA. **SECOND ARM FIXED TOO** — the project-owner arm carried the identical guard, so `class Sub extends <a modelled dep base>` skipped the `nearestDepFn` walk. **FAILS LOUD BY CONSTRUCTION** (`inheritDepFn` only unions into `viaCross`/`surfaceIncomplete`/`unknownWhy`/`edges`), and **`untypedDepReceiver` STAYS gated on `effect == null`** so a classified call with no chained entry gains nothing — hedging there is the 8-25% false-uncertainty flood, not a disclosure. That containment line is the reason the standalone arm is byte-identical. **[[R131]]'s comment in that same method had ALREADY WRITTEN THE WARNING DOWN** — *`effect` is the gate on three downstream branches that only run when the classifier found NOTHING* — **and nobody had asked what happens when a rule and a dep row name the same member.** |
| R686 java: **THE §2 REPORT QUAL `fn` IS NOT UNIQUE PER ENTRY, and three memos are keyed on it — so one overload's reason tags are handed to its siblings, and which sibling wins depends on QUERY ORDER.** | 2026-09-26 | java — OPEN; **the javadoc's false sentence corrected in `a75cdbd`, the behaviour NOT changed**. Found BY [[R685]]'s A/B | Measured on log4j-api-2.23.1: **1,740 rows, 1,327 distinct `fn`, 29 shared — and `org.apache.logging.log4j.Logger.debug` alone names 48 ROWS.** `depWhyByFn`, `depCallsByFn` and `depTransWhyMemo` are all keyed on `fn`, so one overload's direct tags — including the per-hash `dep:<hash>` that `Loader.synthesizeReasonlessDepReasons` synthesises — reach its siblings. **This accounts for all 6 of R685's 87 changed rows whose `unknownWhy` changed overload SPELLING** (`…Logger.debug(Ljava/lang/String;Ljava/lang/Object;)V` -> `…(Ljava/lang/CharSequence;)V`, count 32 -> 33). Direction is **LOUD** — extra reason classes, the `Unknown` travels on `effects`, and `ReasonClass` floors both `dep:` and an empty token set at `unresolved`. | **§K: `depTransitiveWhy`'s javadoc asserted *it cannot over-attribute*, in the commit that needed that to be true.** Replaced with the measurement; documentation only, no behaviour change. **A candidate fix was BUILT AND MEASURED rather than argued about — memoise only the transitive part and union `d.unknownWhy` on top — and it came in at CHANGED 1,352 with its OWN 211-row `unknownWhy` REMOVAL column**, so it was not folded in. A properly closed fix needs `fn` to identify an entry while `calls` names quals, which is a report-FORMAT question for candor-spec rather than an engine fix. |
| R692 all engines: **THE CHAINED-DEPENDENCY OVERRIDE VEIN — a consumer answering a question from LOCAL knowledge while the dependency's own published row says more, in TWO engines within one hour, by two different mechanisms.** | 2026-09-26 | all engines — OPEN as a CLASS row; java instance FIXED ([[R685]]), rust instance OPEN ([[R690]]) | **java ([[R685]]):** a `classify` rule short-circuits `crossDepJoin`, so the dep's row for that exact key is never read — 1,835 colliding keys, 1,807 where the dep says strictly more. **rust ([[R690]]):** a receiver whose trait is declared in a chained dependency reads PURE with no disclosure in three spellings (`<T: dep::Tr>`, `impl dep::Tr`, `for_each(dep::Tr::m)`) that ARE charged when the identical body names a LOCAL trait — `deny Net` and `deny Net Unknown` both exit 0 over a real `TcpStream::connect`, across 5,708 probes in 502 of 1,094 crates. **Both shipped in 0.39.2. Both are invisible to a standalone census by construction, and both were found within one hour of the chained arms existing.** | **The unifying statement, and it is the one to test the other two engines against: a consumer must never read MORE CERTAINTY than the report it was handed, and a LOCAL answer must UNION with a chained row rather than replace or preempt it.** Note the two mechanisms are unrelated — one is a guard ordering, the other a receiver-typing gap — which is why this is a class row rather than a shared fix. **Unasked so far: ts and swift.** The java lane's decisive control transfers directly and cheaply: **take one program, measure it as ONE TREE, then split it across a scan boundary and measure again. If the two answers differ, the engine is contradicting itself on identical source.** That control is what turned R685 from a plausible story into a verdict, and neither ts nor swift has been asked it. |
| R652 rust: **A LOCAL TRAIT WITH ZERO IMPLEMENTORS PUBLISHED A §2 PURITY CLAIM, because `trait_impls` is LEAF-KEYED with no locality test** — `impl <other path>::<same leaf> for Y` fills the local trait's implementor vector, and `lt.count > 1` cannot see it (`trait_decls` counts only local `Item::Trait`). | 2026-09-25 | rust — **CLOSED 2026-09-25**, candor-rust `17c3f9e`; closes [[R628]] | Both halves reproduced at `e4808bf` BEFORE any edit, one variable (the `impl std::io::Write for W` line; trait, consumer, policy and chaining byte-identical): with it, `prod#Write::emit inferred:[] interfaceUnion:true` and the consumer reads `[]` with `deny Net Unknown` **exit 0**; without it, no row, consumer `["Unknown"]`/`dispatch:Write.emit`, **exit 1**. **Live in the wild:** serde's `mod std_error { pub trait Error }` published `Error::source` as a bare purity claim in **7 corpus crates**. **Fix:** a new ADDITIVE fourth key shape in `impl_members` (same walk, `<tr>/<ty>/!<expanded path>`); a pure-only row publishes only where some implementor's written path is a segment SUBSEQUENCE of the trait's declaration qual. | **THE PLACEMENT WAS THE LOAD-BEARING PART, AND THE FIRST FORM WAS WRONG IN THE WAY THIS FAMILY KEEPS PAYING FOR.** The first version skipped the whole trait; the A/B priced it at **29 rows carrying CONCRETE EFFECTS withheld** (`Log`, `Clock,Exec`, `Env,Exec,Fs,Ipc`) — **and a withheld row reads as `Unknown`, which a bare `deny Log` does NOT deny. That trades a fabrication for a LOOSENED GATE**, which is [[feedback-fabrication-fixes-cause-misses]] exactly, caught by measuring rather than by reading. The guard now fires only at `inf_u.is_empty() && blind_u.is_empty() && !any_local_impl`. **It is an ALLOWLIST, inverting this family's usual denylist rule, and the inversion is justified rather than assumed: `trait_impls` is NOT a sound over-approximation for this question — it MANUFACTURES a §2 claim — so absence of evidence must produce no claim.** Unconfirmed ⇒ no row ⇒ the consumer charges `Unknown`: over-disclosure, never silence. **A/B, 1,626 real registry crates: ADDED 0 / REMOVED 42 / CHANGED 0** (340,624 -> 340,582 rows, 0.012%), REACH 44 hits / 26 entries. **All 42 audited in full and ground-truthed against source; all 42 are `inferred: []` with no disclosure of any kind** — the only shape this can remove. 29 correct (7 serde; **22 whose only implementor is `#[cfg(test)]`, so the pre-fix row certified purity from an UNANALYSED implementor**), 6 unknowable-but-honest (cfg-feature alternatives), 7 over-disclosure ([[R653]]/[[R654]]). **And the answer to the review's question about the two DEFENSIVE conjuncts: `:4826` HOLDS, `:4640` DID NOT — its label was literally true about the VECTOR and false about the QUESTION.** *No leaf reaches here with an empty vec* is right; a non-empty vec was never evidence about THIS trait. Now `!any_local_impl`: reachable, covered, comment corrected in place rather than deleted. |
| R653 rust: **A FILE-LEVEL EXTERNAL GLOB REWRITES A `use crate::…` PATH, breaking `expand`'s OWN DOCUMENTED INVARIANT** — *a path rooted at crate/self/super is EXPLICITLY crate-local, it is NOT subject to the file's `use` aliases*. `collect_use`'s `rebound` does exactly that. | 2026-09-25 | rust — OPEN; **[[candor-dispatch-key-vein]]'s class, at 32 of 65 rows in one real crate** | rdkafka 0.39.0: `use rdkafka_sys::types::*;` sitting beside `use crate::consumer::{Consumer}` yields `uses["Consumer"] = "rdkafka_sys::types::consumer::Consumer"`. **Measured symptom: 32 of rdkafka's 65 interface-union rows are published under `rdkafka_sys#…` keys** — the crate's OWN trait members minted into a DEPENDENCY's namespace, which no consumer can join, and which claims to answer an abstraction `rdkafka_sys` does not declare. Also cost 3 of [[R652]]'s 7 over-disclosures (security-framework). | **Not fixed in the R652 lane, for a stated reason rather than by omission: the glob fallback is what closed the sqlx `PgStream::connect` CARDINAL SIN**, so narrowing it needs its own A/B and its own over-charge control. This is the shape [[candor-denylist-over-allowlist]]'s mirror warns about — a shorter path on a destructive route is often a deliberate narrowing, and the two error directions cost differently. |
| R654 rust: **THE `use` MAP IS ONE NAMESPACE AND RUST HAS SEVERAL, so a DERIVE MACRO named after its trait captures the trait's path.** `pub use diesel_derives::AsChangeset;` beside `pub trait AsChangeset` in the same file — the standard derive-macro-named-after-its-trait idiom — makes `expand` resolve the TRAIT path to `diesel_derives::AsChangeset`. | 2026-09-25 | rust — OPEN; measured consequence here is OVER-DISCLOSURE, but the direction is not generally safe | Same in zeroize (`Zeroize`, `ZeroizeOnDrop`). Four rows of over-disclosure in the [[R652]] A/B. **The reason this is worth pricing rather than filing as cosmetic: the misresolution is UPSTREAM of every provenance-gated decision** — the imported-trait CHA carve-out, obligation-2 key minting — so the benign direction observed here is a property of the paths that happened to be measured, not of the defect. | Left unexamined by the lane, and named rather than silent: **no sweep was done for other consumers of `trait_impls` that make the same leaf-is-an-implementor inference OUTSIDE the interface-union loop** (`collector.rs`'s CHA reads it via `TraitIndexes::impls`). R652's fixture would not catch one, **and the audit boundary there was drawn around the union** — which is the Sec 9 shape this register keeps re-finding. |
| R592 swift: **⟨0.39⟩ OBLIGATION-1 AND OBLIGATION-2 KEYS ARE ATTRIBUTED TO THE SCANNED PACKAGE'S OWN C TARGETS.** `Driver.foreignOwnerModule` names the owner of a foreign abstraction as the file's one import that its target DECLARES and this run did not ANALYZE. A C target has no `.swift` files, so `analyzedTargets` never contains it while `declaredByFile` does — leaving it the sole surviving candidate. | 2026-09-24 | **CLOSED — candor-swift `e41e963` (+ figure correction `5560ca3`).** swift — OPEN, PRE-EXISTING; [[R565]] does NOT fix it (verified PRE/POST identical on resolved swift-nio) | **MEASURED over 11 real packages at candor-swift `cb84836`: 21,807 `dispatchesOn` occurrences, 4,952 foreign-prefixed, and 4,880 of those (98.5%) prefixed with a C TARGET OF THE PACKAGE BEING SCANNED** — CNIOLinux 3,071, CNIOWindows 1,205, CNIOBoringSSL 587, CNIOAtomics 14, CNIOLLHTTP 3. The keys look like `CNIOLinux#Swift.type`, `CNIOLinux#UnsafeRawPointer.assumingMemoryBound`, `CNIOWindows#WinSDK.socket`. The same resolution feeds obligation 2: **983 of 4,276 `interfaceUnion` entries are foreign-prefixed and 825 name a C target.** On a swift-nio checkout with dependencies RESOLVED the rate is **8,342 of 8,344 (99.98%)**. **TWO ERROR DIRECTIONS, BOTH LIVE: (a) MISATTRIBUTION** — a key no package's hash prefix can equal, so no consumer can join it and obligation 1's chain is broken for those rows; **(b) SUPPRESSION** — a C target adds a SECOND candidate, `cands.count != 1` fires the never-guess rule, and the key is dropped outright. At file granularity across the 11 packages: 31 file-observations publish a foreign key (25 name a C target, 6 a genuine sibling module) and 16 are suppressed, **every one of the 16 by a C target**; in swift-nio's own scan 24 of 24 non-empty candidate sets contain only C targets and not one genuine dependency. | **NOT FIXED.** Fix shape: a target of the package the FILE BELONGS TO is not a foreign owner, analyzed or not — exclude `targetsIn(owningPackage)` from `cands`, which also un-suppresses the genuine foreign owner in direction (b). Measured with a probe calibrated by separating 31 publish from 16 suppress over 47 non-empty candidate sets, so the numbers are not read off an unreached branch. **[[R593]] must land AFTER this row, not before** — dedupping the candidate list first would publish MORE C-target keys. **CLOSED 2026-09-25, candor-swift `e41e963`, with a figure correction at `5560ca3` that the fixing agent found in its OWN work and recorded rather than quietly amending.** The headline census reproduced EXACTLY at HEAD (`96211f0`, not the `cb84836` the row cites): 21,807 `dispatchesOn` occurrences, 4,952 foreign-prefixed, **4,880 (98.5%) naming a C target of the package being scanned**, each C prefix verified to be a real `.target(name:)` in that package's own manifest. **AFTER: dispatchesOn 21,807 -> 18,267; foreign-prefixed 4,952 -> 1,412; own-C-target-prefixed 4,880 -> ZERO.** Plain 11-package arm: ADDED 203, REMOVED 862, CHANGED 617, REACH 409 hits. **All 862 removals audited IN FULL, not sampled: 825 own-C-target keys + 32 `NIOFoundationCompat#ChannelInboundHandler.*` union entries + 5 ordinary rows whose ONLY content was a C-target key** (`inferred` empty before AND after; `analyzed.count` unchanged at 16,428, so no purity claim moved). All 203 additions come from direction (b), the un-suppression. **The chained arm is what settles whether the removals are right** — nio-ssl + 6 resolved checkouts: ADDED 213, REMOVED 278, CHANGED 135, every one of the 278 a `CNIOBoringSSL#` entry naming nio-ssl's own C shim as owner of `Equatable`/`Hashable`/`CustomStringConvertible`, and **union rows 334 -> 267 with 0 `swift-nio#` entries lost.** **The agent's own error, recorded:** its manifest classifier used a `name: "X"` regex that also matches `.product(name:)`, so four genuinely foreign modules counted as local — 40/43 and 19/19 were overstated, re-measured to **36 of 43** and **18 of 19** (the nineteenth is a genuine two-package ambiguity R592 does not touch). Neither figure is load-bearing; the occurrence census reads report prefixes and never went through that classifier. Fixture `OwnPackageCTargetOwnerProcessTests`, one tree, both directions, RED on revert. |
| R593 swift: **`foreignOwnerModule` COUNTS A DUPLICATED IMPORT AS TWO CANDIDATES, so a file with exactly ONE distinct candidate publishes no ⟨0.39⟩ key at all.** `fileImports` is a `[String: [String]]` — a LIST — and `cands.count == 1 ? cands.first : nil` is applied to it without dedup. | 2026-09-24 | **CLOSED — candor-swift `6905cd5`, landed AFTER [[R592]] as the row required, and the ordering is now MEASURED rather than argued.** swift — OPEN; its cost is currently MASKED by [[R592]] | The ordinary cross-platform idiom duplicates an import across mutually-exclusive `#if` arms: swift-nio's `NIOFileSystem/FileInfo.swift` writes `import CNIOLinux` under BOTH `#elseif canImport(Glibc)` and `#elseif canImport(Musl)`, so `cands == ["CNIOLinux","CNIOLinux"]` and **the never-guess rule fires on an ambiguity that does not exist.** **MEASURED over 11 real packages: 4 files suppressed for this reason alone** — `FileInfo.swift`, `Mocking.swift`, `SystemFileHandle.swift`, `FileDescriptor+Syscalls.swift` — plus one 3-element set that dedups to 2. SILENT: no key means a chained consumer cannot union its own implementors, and under ⟨0.21⟩ its row can read pure. | **NOT FIXED — one line (`Set(...)` before the count), but it MUST land after [[R592]].** Today every such candidate in swift-nio is a C target, so the suppressed key would have been a dead one anyway and the class is masked; dedupping first would simply publish more misattributed keys. The class bites wherever the sole distinct candidate is a GENUINE dependency. **CLOSED 2026-09-25, candor-swift `6905cd5` — and the ORDERING this row insisted on is now MEASURED rather than argued.** Landed after [[R592]]: `R593HIT` fires **84 times across 5 files**, and the A/B is **0/0/0** — reached and inert, because after R592 every duplicated candidate on that corpus is an own C target. **Landing it FIRST would have turned 4 of those 5 files into 4 MORE misattributed keys** (`FileInfo.swift`, `FileDescriptor+Syscalls.swift`, `Mocking.swift`, `SystemFileHandle.swift`, all `["CNIOLinux","CNIOLinux"]`). So the corpus cannot show the gain and the FIXTURE is the evidence — `DuplicatedImportOwnerVoteProcessTests`, RED on reverting the `Set(...)`. That is the row's premise confirmed by the one experiment that could have refuted it. |
| R574 ts: **R560's `!eff` REMOVAL ADDED A FABRICATED CONCRETE EFFECT TO CALLS κ HAD ALREADY ANSWERED PRECISELY.** `viaLib()` — `const g = makeClient(); g.fetchIt(u)`, where the receiver is genuinely the LIBRARY's own object — read `['Net']` before and reads **`['Exec','Net']`** after. The `Exec` comes from a local `class MyClient implements Gettable` that the call never reaches. | 2026-09-24 | ts — INTRODUCED 2026-09-23 by `d534b62` (R560) | **Found by the code review; coordinator-verified at HEAD on the reviewer's fixture (`analyzed=6`, `tsc` clean): `src.index.viaLib inferred=['Exec','Net'] dispatchesOn=['got#Gettable.fetchIt']`.** Removing `!eff` means the ⟨0.39⟩ join now runs on EVERY foreign call κ classified — every whole-module rule (`axios\|got\|undici\|ws\|socket.io` → Net, `pg\|mysql2\|mongodb` → Db, `execa\|cross-spawn` → Exec, …) plus every `@types/node` core call. It is the same CHA over-approximation the guarded branch already made, new in SCOPE rather than in KIND, and it never removes κ's charge. **But the commit's safety sentence — *"PURELY ADDITIVE … κ's own charge is never removed by it"* — is a claim about REMOVAL that reads as a claim about COST. §K: it is the sentence in that diff most likely to stop this measurement being made, and I wrote it.** **THE SAME SHAPE AS [[R571]] IN A SECOND ENGINE: two of yesterday's fixes traded an under-report for an over-charge, and both A/Bs missed it.** | **FIXED 2026-09-24 — candor-ts `6be1c47` — AND THE PREMISE I FILED THIS ROW ON WAS WRONG IN A LOAD-BEARING WAY.** I attributed the `Exec` to R560. **It is not peculiar to κ-classified packages: the same consumer built against a NON-κ package reads `['Exec']` at `d46c098` too, through the same join. So R560 widened the SCOPE of the shipped ⟨0.35⟩/⟨0.39⟩ CHA over-approximation, not its KIND** — and that correction changed the fix, ruling out two candidates I would have accepted: narrowing only for κ packages would give byte-identical code two different answers depending on κ-table membership (the exact category error R560 fixed), and narrowing both branches would delete a real charge on the `!eff` path. **The landed fix takes ⟨0.35⟩'s OTHER sanctioned branch: where the receiver's value is demonstrably the package's own product — a `const` with a single declaration bound to a ZERO-ARGUMENT call or `new` resolving into that same package — the join HEDGES (`Unknown` + `dispatch:<pkg>.<Iface>.<member>` + `unresolved: true`) instead of charging the local implementors. It hedges, it does NOT delete: no row loses an effect without gaining the disclosure that replaces it, and `recordDispatch` still runs so R560's wire key survives.** `deny Unknown src.index.viaLib` goes 0 → **1**, so against the last shipped behaviour this is a net GAIN in teeth; coordinator-verified at HEAD (`['Net','Unknown']`, `unresolved: true`, gate exit 1). A DENYLIST by construction: a factory that RECEIVED an implementor, a `let`, a parameter and a property access all keep the full charge. **Ground-truthed by EXECUTION against a runnable `got`: every arm whose `Exec` is withdrawn executes the LIBRARY's body and never `MyClient.fetchIt`, while the two arms where the local implementor really runs are exactly the two that keep the charge. The residual is NAMED and DEMONSTRATED rather than hypothesised — `makeClient = () => registered ?? lib` makes that arm execute the local implementor, and it is hedged rather than silent.** Self-caught during calibration: **the first GATE row written could not discriminate** (`deny Exec Unknown` exits 1 with and without the fix), so it was re-spelled to `deny Unknown` and the non-discriminating pair kept as a LABELLED control.  **ORIGINALLY OPEN. Also recorded, because it invalidated a number I published: `R560-REACH` fires on BRANCH ENTRY, before `recordDispatch`** — demonstrated with a file containing only `fs.writeFileSync` + `cp.execSync`, which emits two hits that `recordDispatch` rejects one line later and that can change no row. So *"182 hits across 12 entries — the over-charge control WITH real reach"* cannot distinguish 182 genuine joins from 182 node-core no-ops. The review could not bound the real number without corpus clones and said so rather than estimating. A reach probe placed before the filter it is cited as evidence for is the [[candor-oracle-disclosure-recall]] class: calibrate the instrument, never a copy of it. |
| R575 rust: **A THIRD RECEIVER WALKER WITH A NARROWER ARM SET THAN THE RESOLVER IT GATES — `receiver_is_erased` (`collector.rs:2388`) is missing Index, element-yielding-accessor and plain-chain arms that `resolve_recv_traits_walk` has.** So `self.hs[0].roll(n)`, `r.hs[0].roll(n)`, `r.inner.as_ref().unwrap().roll()` and `mk_opt().unwrap().roll(n)` all read `[]` while `let v: Vec<Box<dyn H>>; v[0].roll(n)` reads `['Fs']`. | 2026-09-24 | rust — the walker was ADDED 2026-09-23 by `2078e08` (R562) | **Found by the review doing exactly what [[R541]]'s own comment says found IT — *"by DIFFING THE TWO RESOLVERS' ARM SETS"* — except there are now THREE.** The one-variable pair is `f_let_vec_index` vs `f_field_index`: the identical `v[0].roll(n)` expression, differing only in whether the `Vec<Box<dyn Handler>>` is a `let` or a FIELD. Gate named by measurement: adding an entirely unused `&dyn Handler` parameter flips all three silent arms to `['Fs']`. Scoped `deny Fs` and `pure` both exit 0 on them; blanket `deny Fs` exits 1 only INCIDENTALLY, because the callee is independently reported. | **OPEN — and this is §G's rule broken by a fix that cited §G.** The remedy is not a fourth walker: it is for `receiver_is_erased` to share `resolve_recv_traits_walk`'s arm set the way `is_recv_type_changing` is already shared between the two resolvers that answer the concrete-type question. Note the interaction with [[R571]]: narrowing the body-wide index must not be done by deleting the body-wide read until this walker covers what that read covered. |
| R576 rust: **[[R543]]'s serde `visit_char` DIAGNOSED — a `self` receiver inside a trait DEFAULT method, and it splits in TWO.** (a) where the callee HAS a default body, `self.m()` binds statically to that body, the implementors' overrides are invisible, and **no `dispatchesOn` key is emitted at all**, so ⟨0.39⟩ obligation 3 has nothing to join. (b) where the callee is a REQUIRED member with **more than 12 implementors**, `scan.rs:2788-2811` is bounded `hits.len() <= 12` and **has no `else`** — so it falls silent instead of disclosing. | 2026-09-24 | rust **(b) CLOSED — candor-rust `05f3325`. (a) REMAINS OPEN.** | **Found by the code review, and it CORRECTS [[R570]]'s dismissal of this hypothesis — which was mine.** R570 rules out *a `self` receiver in a trait default method* on the evidence `via_self_default → ['Net']`. **That control was measured on the EASY case — few implementors, callee not overridden — and does not generalise.** The review's fixtures separate it: at ≤12 impls it resolves (`calls=[V1::sink,V2::sink,V3::sink]`); at 15 it goes ABSENT with `pure`, `deny Net` and `deny Unknown` all exit 0. Both halves predate yesterday's wave (reproduced at `36a917d`). Also corrected: R543's stated mechanism (*"a caller of a provided/body-less declaration reads pure"*) is NOT it — `visit_char` IS analysed and its edge IS recorded (`de::Visitor::visit_char -> ['de::Visitor::visit_str']` in the callgraph sidecar). | **OPEN. The (b) half is the cheaper and sharper one: its two SIBLING sites both disclose on the wide arm** — `collector.rs:2280` and `collector.rs:1653` each `mark_unresolved(format!("dispatch:{tr}.{leaf}"))` — so this is §F1 Q3 drift, the detector working and the aggregator dropping it. **Its own comment is §K exactly**: *"a wider open-world fan-out is an honest miss, never a guess"* — but an honest miss is `Unknown`, and this is SILENCE, which is a purity claim. **(b) CLOSED 2026-09-24, candor-rust `05f3325`.** The `else` was added at the bounded-CHA arm, writing `Unknown` + `dispatch:<trait>.<member>` — Pass A's reason string verbatim (§G: two passes, one question, one answer). `resolved_local` deliberately NOT set, unlike Pass A's sibling which returns 'do nothing further': here that would suppress the auto-deref fallback below, and losing an edge to buy a disclosure is the wrong direction. **A/B, `bin/corpus-ab.py`, 1626 REAL crates from the cargo registry: ADDED 163, REMOVED 0, CHANGED 141; REACH 131 hits across 56 entries** (fallible-iterator, rayon, arrow-array, asn1-rs). REMOVED 0 is the safety property for an additive fix; all 162 ADDED blobs carry `Unknown` and nothing else, the only effect gained across the 140 CHANGED rows is `Unknown` (107), and NO row gains a concrete effect. **`/tmp/candor-corpus` was NOT used — it is hollow (0 .rs files), the [[R554]] decay class, and an A/B over it prints 0/0/0 exactly like an inert change.** Regression test CALIBRATED: fails on a detached worktree at `845cb32` carrying the same test file, passes at HEAD, and carries the ≤12 CONTROL so a fix that disclosed everywhere would fail it. The `hits.is_empty()` half of Pass A's `_` arm is deliberately NOT adopted, on a MEASUREMENT rather than the flood argument: the `R576EMPTY` probe fires **0 times over 400 registry crates** against the wide arm's 27. gate-run candor-rust 31/31 OK; four-way conformance OK, 0 FAIL cells, 0 passing xfails, 0 left files. **(a) — the callee WITH a default body, where `self.m()` binds statically and no key is emitted at all — is untouched and stays open.** **CORRECTION 2026-09-24 — (a) IS NOT [[R590]]/[[R597]] SEEN FROM THE CONSUMER SIDE, and I filed it that way this morning.** Measured on a two-arm fixture differing ONLY in whether the member has a default body: `self.emit()` INSIDE a default body binds statically, so `Sink::run` reads `["Fs"]` with `calls:["Sink::emit"]` and NO `dispatchesOn`, against `["Net"]`/`calls:["Loud::emit"]` when `emit` is a requirement. **The overrides' effects are never COMPUTED, so nothing can publish them** — a chained consumer of `Sink::run` still reads `["Fs"]` after R597's fix. **R597 is a PUBLISHING defect; (a) is an ANALYSIS one, and no key-level fix reaches it.** It needs its own charging-direction fix at the `self.m()` resolver (`decls.rs:167` / `scan.rs:1944`, the R160 ruling that predates ⟨0.39⟩'s bounded CHA). In-crate `&dyn Sink` dispatch was already correct (`["Fs","Net"]` + `dispatchesOn`), which is why this half hid. **REAL-CODE MOVEMENT 2026-09-25, measured by the `corpus` oracle rather than by an A/B:** serde's `de::Visitor::visit_char` — this row's motivating instance — moved from **"pure (omitted from report)"** to **"reads certain"**. The row now EXISTS where it was ABSENT, so the ⟨0.21⟩ purity claim is gone; what remains is imprecision (it reads certain while calling an `Unknown`), which is why (a) stays open. **Not attributed: the night's wave closed ten rows across three engines and no A/B named this instance** — the oracle does not know what changed, which is exactly what makes the observation worth having. **BISECTED 2026-09-25 — (b) CLOSED A REAL-CODE HONESTY FINDING, AND THE REGISTER HAD ATTRIBUTED IT TO THE WRONG ROW.** The `corpus` oracle's clap finding (`builder::value_parser::OsStringValueParser::parse_ref`: *pure, omitted from report, but calls `TypedValueParser::parse` which is Unknown*) disappeared between the 09-22 and 09-25 runs. Bisected with one binary per commit over `clap_builder-4.6.7`: **ABSENT at `845cb32`, PRESENT with `inferred:['Unknown']` at `05f3325`** — i.e. this row's own (b) fix, and nothing after it (`CANDOR_R598_PRE` changes nothing; pre-R569 `d449ee5` and pre-R598 `0c0a0fd` both already show it PRESENT). **Mechanism confirmed rather than inferred: `TypedValueParser` has 17 implementors, above the bound of 12, and `R576WIDE` fires twice on this crate** — so the missing `else` emitted no edge and no reason, `parse_ref` left `functions[]`, and (b)'s disclosure now gives it an inherited `Unknown`. **[[R543]] recorded that instance as [[R570]]'s UFCS shape; that attribution was WRONG.** The finding was a >12 fan-out, not a fully-qualified trait call. |
| R565 swift: **⟨0.39⟩ KEYS BY MODULE, §2 CHAINING KEYS BY PACKAGE, AND FOR A CONVENTIONAL SPM PACKAGE THOSE NAMESPACES NEVER INTERSECT — SO THE ENTIRE CHAIN IS A NO-OP AND NOTHING SAYS SO.** Obligation-1 keys use `foreignOwnerModule`; `deps.lookup` and §2 coverage use `Package(name:)`. **Measured: 0 of 1,051 foreign dispatch-key occurrences across 5 real chained pairs name a chained package** — consumers publish `CNIOBoringSSL#`, `NIOEmbedded#`, `CNIOLinux#` while the reports register `swift-nio`, `swift-collections`, `swift-numerics`. Chaining nio-ssl onto swift-nio produces a **byte-identical report**. | 2026-09-23 | **CLOSED — candor-swift `cb84836`, and it was TEN SITES, not the six filed.** swift | **Found because R533's swift pricing could not fire, and COORDINATOR-VERIFIED INDEPENDENTLY on a freshly built two-package fixture rather than inherited — one variable, the dep's `Package(name:)` string, everything else identical: `name: "Iface"` (matching the module) → `appSize` `['Net']`, `deny Net` **exit 1**, `coverage.uncovered []`; `name: "iface-pkg"` (ordinary SPM naming) → `appSize` `[]`, `deny Net` **exit 0**, `coverage.uncovered ['Iface']`.** **NOT a cardinal sin, and the distinction is worth keeping: the κ ledger DOES disclose `Iface` as uncovered, exactly as it would unchained. The defect is that the disclosure is IDENTICAL to never having chained — so a user who supplies a dep report to obtain its effects gets nothing, and the only signal is a non-gating coverage entry ([[R133]], non-gating by ruling).** **§K, closed by this row's existence: `main.swift:1391` already cited *"the open package-vs-module keying row"* and `grep 'package-vs-module' SOUNDNESS.md` returned ZERO — a citation that read as considered for as long as nobody grepped it.** | **OPEN, and it BLOCKS [[R533]]'s swift pricing — which is the operationally important part.** With the prototype ON and HEAD's keying, the A/B over 5 chained pairs / 8,932 analyzed functions is **0/0/0 with ZERO REACH: safety-only, not a cost of zero.** R533's rule cannot fire on any real swift chained package at HEAD, so **swift can neither pay nor refuse ⟨0.39⟩ (the clause once drafted as 0.40) until this is closed, and no swift number may be placed beside java's 0.87% A/B or the 2.60% declined precedent.** The authority is one file away — the consumer's own `.product(name:package:)` lines. `--workspace` uses the same `isChained` predicate so the gap should apply there too, **but that was NOT measured and is not asserted.** **CLOSED 2026-09-24, candor-swift `cb84836` — CONFIRMED BY A ONE-VARIABLE A/B AND WIDER THAN THIS ROW SAID.** Everything byte-identical but the dep manifest's `Package(name:)` string: with `Package(name: "RatesDep")` / module `RatesCore`, chained, `go -> []` + `invisible:[RatesCore]`, `deny Fs` **exit 0**; rename the package to `RatesCore` and the same tree gives `go -> ['Fs']`, `deny Fs` **exit 1**. **TEN SITES, NOT SIX:** the six joins, plus `CallCollector.depShadows`, plus the COVERAGE half (`blindModules`, the κ uncovered ledger) which had the same confusion one direction over, plus **TWO ⟨0.39⟩ PUBLISH sites** — `abstractionOwnerPkg` (obligation 2's `interfaceUnion` hash) and the foreign arm of the `dispatchesOn` key (obligation 1). Those two are ONE namespace, so **fixing only the ASK half would have made the join SILENTLY MISS**; caught after the first pass and amended. **The map is built from the DEPENDENCY'S OWN MANIFEST, never from `.product(name:package:)`** — that string is an SPM identity (case-insensitive; for a path dep, the directory name), so the case-folding trap the brief warned about never arises: PART 92's `Package(name: "Iface")` / `package: "iface"` needs no resolving. Built UNCONDITIONALLY — gating it on "something is chained" would be a determinism bug, since it also decides what the run PUBLISHES. **A/B end-to-end, 4 SwiftPM packages × 16 resolved checkouts: rows 1801 -> 1979; ADDED 362, REMOVED 184, CHANGED 781. REACH — `dep:` join-provenance tokens PRE 0 -> POST 1041: THE §2 CHAIN HAD NEVER ONCE BEEN CONSULTED ON ANY OF THESE FOUR PACKAGES.** Effects LOST across all 781 changed rows: **NONE**. Gained: Env 448, Unknown 105, Clock 15, ground-truthed from source (`dep:swift-nio#EventLoop.execute` -> `SelectableEventLoop.strictModeEnabled` -> `getenv("SWIFTNIO_STRICT")` at `NIOPosix/SelectableEventLoop.swift:100`). **All 184 removals audited and bucketed:** 170 `interfaceUnion` entries re-keyed module->package (each with a matching ADDED row), 14 rows whose pre-image was `inferred: []` + an `invisible` hedge over a package a trusted report now legitimately covers (§2 rule 3); plus 21 `dispatchesOn` re-keys. Calibrated by 7 tests red in each of three degradations. **PART 92's ten swift arms replayed by hand: byte-identical pre/post on every field.** Stated narrowing: a `--target`-scoped dep report registers under `Pkg/Target`, which no module resolves to, so it stays unchained — pre-existing safe direction, unchanged. |
| R567 swift: **THE DISPATCH KEY TAKES THE OUTER BASE'S TYPE FOR A MEMBER-CHAIN RECEIVER, AND A BARE-LEAF KEY CANNOT MATCH A PRODUCER'S ARITY-SUFFIXED OVERLOAD — swift's [[R549]], 13 of 18 measured sites.** `channel.embeddedEventLoop.run()` keys `EmbeddedChannel.run` (×5); `channel.allocator.buffer(…)` keys `EmbeddedChannel.buffer`; `channel.pipeline.{addHandler,fireChannelInactive}` likewise; `configureHTTP2Pipeline` (×3) is the CONSUMER'S OWN extension keyed to swift-nio; `HTTPClientRequestPart.{head,end}` are enum CASES. | 2026-09-23 | **CLOSED BOTH HALVES — candor-swift `dbe3f68` (a) + `8931087` (b). (a) reproduced WORSE than this row said: a fabrication AND a silent under-report at once.** swift (rust's twin is [[R549]]/[[R551]], fixed) | **Found by auditing all 18 hits of the R533 measurement IN FULL and ground-truthing each against source rather than against candor's own report — which is how the malformed ones were separated from the real ones. 0 of the 18 turned out to be R533's actual shape.** A second, distinct half: **5 of 18 are WELL-FORMED but name concrete final classes, and 4 of those 5 the dep answers under a DIFFERENT SPELLING** — it publishes `swift-nio#EmbeddedChannel.finish()` and `.finish(Bool)` while the consumer asks the bare `…finish`, and `EventLoopGroup.syncShutdownGracefully` where the consumer asks `MultiThreadedEventLoopGroup.…`. **An OVERLOADED dependency member is unreachable by the consumer's §2 key.** | **OPEN. Two mechanisms in one row because they were measured together and both make a key unjoinable: receiver-chain attribution (the key names the OUTER base, not the member's real owner) and arity-spelling mismatch (bare leaf vs `name(ArgTypes)`).** The first is rust's [[R549]] shape and rust's fixes are the template; the second is NOT present in rust and is swift's own — it needs the consumer and producer to agree one spelling, the same class of disagreement [[R555]] closed for ownership. Both inflate any measurement keyed on *a dispatch nothing answers*, so they must be discounted before swift's [[R533]] price means anything — the same correction rust needed, where discounting malformed keys took 1.71% to 0.23%. |
| R541 rust: **`(*b).emit()` OVER A `Box<dyn Sink>` IS SILENTLY PURE — the deref arm had no dispatch twin.** `b.emit()` charges, `(*b).go()` over `Box<G>` charges, `(*b).emit()` over `Box<dyn Sink>` is ABSENT. One variable: a `*`. | 2026-09-22 | rust | **FOUND BY THE SWEEP, NOT BY MEETING THE BUG.** [[R538]] is *one half of a paired resolver was built*, so the question to ask of the file is **where else does the concrete route have an arm the dispatch route lacks** — answerable MECHANICALLY by diffing the two resolvers' `syn::Expr::` arm sets. Four asymmetries fell out; this is the one where a COMPILING fixture splits `dyn` from concrete. `Expr::Struct` is concrete by construction; `Expr::Cast` is dispatch-only on purpose ([[R535]]); `Expr::Try`/`Await` in the element pair was EXECUTED and does not split. | **CLOSED — candor-rust `b677535`.** A/B 1,596 crates: **ADDED 13, REMOVED 0, CHANGED 23**; **LOSSES NONE, zero new concrete effects.** REACH 155 / 55 entries. Ground truth from SOURCE: aws-smithy-runtime-api 1.15/1.16 `SharedAuthSchemeOptionResolver::resolve_auth_scheme_options` is literally `(*self.0).resolve_auth_scheme_options(params)` — **a public AWS SDK forwarding API, ABSENT before** — plus `SharedHttpConnector::call` and anyhow `Chain::next`. **THE INSTRUMENT IS THE REUSABLE PART: diff the two resolvers' arm sets, and diff the TABLES their bodies read (`grep 'self\.'`) — the first produced this row, the second produced [[R540b]], and both were invisible to reading the code.** |
| R540 rust: **THE DISPATCH HOT-PATH GUARD NAMED THREE OF THE SIX TABLES ITS ARMS READ, so a collection-only dispatch source never got past it — AND THE ANSWER DEPENDED ON AN UNUSED PARAMETER.** `v[0].emit()` over a `Vec<Box<dyn Sink>>` is ABSENT; the byte-identical body with one UNUSED `_a: &dyn Sink` parameter added charges `['Exec']`. | 2026-09-22 | rust | Found while fixing [[R535]] — the cast arm was unreachable and this guard was why. **Coordinator-verified on a scratch fixture: PRE `via_index` ABSENT / `via_index_unused` `['Exec']`; POST both `['Exec']`.** ONE VARIABLE, a parameter nothing references. `resolve_recv_traits` guarded on `trait_vars`/`trait_fields`/`has_dyn_return` while its `Index` and element-accessor arms answer out of `elem_trait_of`, `field_elem_trait` and `callable_statics`. The accessor spelling (`v.get(0).unwrap().emit()`) and the FIELD spelling (`self.hs[0].emit()`) are silent identically, so the R446 and R88 arms written for exactly those shapes were UNREACHABLE in any function with no other dyn binding. **§H one level in: the detector worked and something upstream discarded the detection.** | **CLOSED — candor-rust `82a7732` + `1c4e2c4`.** The condition is now DERIVED FROM THE ARMS mechanically: `grep 'self\.'` the two resolver bodies, and every data table must be in the condition or answered above it. **[[R540b]] is that same grep catching the agent's OWN fix** — `has_dyn_return` asked `ret_dyn_leaves` where the arms decode `ret_dispatch_leaves`, which is FOUR sentinels, so a crate whose only dispatch source was a factory returning `Vec<Box<dyn T>>` still failed the guard. **R540's own audit drew its boundary around its trigger (§9) — in the fix for an aggregator bug.** Both A/Bs, 1,595/1,596 crates: ADDED 0 REMOVED 0 CHANGED 0 on every key, with REACH 169/21 then 171/22 entries — **\"fired and moved nothing\", not \"never fired\"**. The behaviour change is proven by the FIXTURE, not the corpus, and this row says so rather than claiming impact. |
| R536 rust: `is_element_yielding_accessor` WAS FIVE NAMES OF A FAMILY OF TWENTY-FIVE, and the element-preserving list was missing the same twenty. `if let Some(g) = v.pop()` read silent-pure while `if let Some(g) = v.first()` one line above it charged — the five names it shipped with were simply the ones R346 and R401 had already put on `is_element_preserving_adapter` for a different reason. | 2026-09-22 | rust (java/ts CORRECT; swift has the SAME class — [[R537]]) | **CARDINAL SIN, and the fifth link in R185 → R345 → R346 → R401 → R446 — each of those rows' own fixture was the next defect's boundary, so the WHOLE family is now one fixture.** Measured over `Vec<Box<dyn Sink>>` / `VecDeque` / `BinaryHeap` / `HashMap` / `BTreeSet` / `Option` / `Result` with `first()`/`last()`/`get(0)`/`v[0]`/`Option::take()` charging as the calibration: **38 of 52 arms ABSENT** — `pop`, `pop_front`, `pop_back`, `front`, `back`, `first_mut`, `last_mut`, `next`, `next_back`, `nth`, `find`, `min_by_key`, `max_by_key`, `remove`, `swap_remove`, `Result::ok`, `Option::replace`, `BinaryHeap::peek`, `BTreeSet::pop_first`, and the receiver-position `.unwrap()` spelling of each. **THE REAL INSTANCE, ground-truthed from SOURCE rather than from candor's own report:** `sea-orm-2.0.2`/`2.0.3` `SchemaBuilder::apply` is `for t in self.sorted_tables() { if let Some(entity) = self.entities.iter().find(..) { entity.apply(db, ..).await? } }`, and `EntitySchemaInfo::apply` runs `db.execute(stmt)` three times — it reported `[]`, a §4 purity claim over a function whose own doc says it *"creates all registered tables, columns, unique keys and foreign keys"*. **THE BINDING SITE IS NOT THE VARIABLE, swept as its own axis:** `if let` / `while let` / `match` / `let`-else / `?` / `for g in` / `.map(\|g\| ..)` and a FIELD receiver were all silent before and all resolve now. Gate level, four policy forms: scoped `deny Exec`, `deny Exec Unknown` and `pure` all **exit 0 → 1**, blanket `deny Exec` catches in both arms incidentally; on a real crate `deny Unknown hpack::decoder::Table::reserve` over h2-0.4.19 is **exit 0 → 1**. | **CLOSED — candor-rust `c248276`.** **TWO LISTS, NOT ONE, and the difference decides membership:** `is_element_preserving_adapter` asks *does the result CONTAIN the same element* (so `take(2)`/`rev()`/`filter(..)` belong there and nowhere else — `v.take(2)` is still a container and typing it as the element fabricates one level in); `is_element_yielding_accessor` asks *is the result the element itself*. Exclusions stated at the list: `keys`/`into_keys` (the KEY), `position`/`count` (a `usize`), `find_map` (CHANGES the element), `split_first`/`first_key_value` (a TUPLE — R346's `windows`/`chunks` residual one over); `pop_first`/`pop_last` are IN, by R454's argument (the map spelling does not COMPILE and the tuple route has no map arm). **SECOND HALF, found by this row's own fixture and a THIRD copy of the element question:** `visit_local`'s UNANNOTATED arm carried a concrete element through an element-preserving rebind since R100 and never asked the DISPATCH question, though the annotated arm and the parameter and field positions all do — `for g in v.iter() { g.emit() }` charges and `let it = v.iter(); for g in it { g.emit() }` read ABSENT. It is **INSERT-ONLY, and the clear was written first and MEASURED OUT**: mirroring `elem_of`'s remove-then-insert cost `image-0.25.10` TEN rows their `dispatchesOn: moxcms#TransformExecutor::transform` and one its `invisible: ["moxcms"]`, because `let trs = trs.map(Option::unwrap);` shadows a PARAMETER with an init the resolver cannot answer for — **an annotation that is not a dispatch container is a positive answer; an init that resolves to nothing is the ABSENCE of one.** It also CLOSES [[R88]]'s pinned residual (b), both arms re-measured, and the pin is what turned a gate run red and named the stale note. **BOTH COMMENT ASSERTIONS IN THE AREA TESTED (§K): one HOLDS, one is FALSE.** `is_iter_consumer`'s *"an explicit `.next()` already resolves as an ordinary method call on the receiver type"* is TRUE on its own subject and is now SCOPED, because it was read as a ruling on element typing it never addressed. `is_element_preserving_adapter`'s `unwrap` note — *"both resolvers then find no element and return nothing. Harmless in that direction"* — is **FALSE in both halves**: `elem_type` has answered with the payload since R185, and the shape FABRICATES (`for y in o.unwrap() { y.go() }` over an `Option<Holder>` whose real `Item`'s `go` is pure is charged `['Exec']`, while the identical loop over a bare `Holder` correctly reads ABSENT). Entry kept (removing it costs R347's guard chain) and **filed as a known over-charge with a repro, owed its own row.** **A/B via `bin/corpus-ab.py`, 1,595 registry crates, pre = `2571225`, 321,493 → 321,544 rows: ADDED 51 · REMOVED 0 · CHANGED 227 (12 on `inferred`)** — audited in FULL, and **every changed field is a strict GAIN: zero losses in `inferred`, `calls`, `dispatchesOn`, `invisible`, `incomplete`, `unresolved`, `unknownWhy` or `direct`.** Gains counted apart: of the 51 ADDED, 25 are `['Unknown']` and 22 are `[]` rows carrying new edges — **none gains a concrete effect**; of the 12 `inferred` moves, 6 are effect gains (sea-orm `Db` ×2, `Log` ×4) and 6 Unknown gains (image ×4, redis ×2). **REACH** 6,550 hits: accessor arm 760/207 entries concrete + 873/159 dispatch, preserving peel 1,065/267 + 454/178, `let` rebind 3,398/519. Four more ground-truthed from source (quinn-proto `can_send_1rtt`, h2 `Table::reserve`, reqwest `try_clone`, sea-orm). **RELAYED FROM [[R537]], both measured, one a NEGATIVE:** rust's list is **NOT orphaned** — its two consumers are BOTH type resolvers, which is the wiring swift lacked — but rust had the same defect one binder down, which is this row's second half; and rust's matching **cannot empty a candidate set**, because Rust has no overloading so nothing narrows by argument type (`resolve_target`/`arm_exact_target` refuse and leave the standing answer, bounded CHA builds a union and declines at `> 12`, R330's arity refinement can only remove a fabricated `Rand`) — pinned by `typing_a_receiver_better_never_empties_a_candidate_set` over `Self` / associated-type / bare-generic / `Option<Box<T>>` parameters. **STATED RESIDUALS:** `m.entry(k).or_insert_with(f)` (the `Entry` route is different machinery); `r.unwrap()`/`r.expect("")` over a `Result<Box<dyn Sink>, ()>` at a receiver position, because `trait_leaves` deliberately excludes `Result` (R380's measured decision) while the `Option` twin charges; and the `unwrap` over-charge above. |
| R535 rust: **A BLOCK-, `if`- OR `match`-VALUED RECEIVER EXPRESSION IS SILENTLY PURE — no Option involved.** `(if c { a } else { b }).emit()`, `(match c { true => a, false => b }).emit()` and even `{ a }.emit()` over `a: &'static dyn Sink` are ABSENT from `functions[]`, while `a.emit()` charges `['Net']` in the same scan. | 2026-09-22 | rust (java/ts/swift CORRECT) | Found by widening the [[R534]] sweep PAST ITS TRIGGER (§9) — it is not R534's class at all. candor-scan 0.39.1 at `2571225`. Three spellings, ONE VARIABLE (the receiver is a block-valued expression rather than a path), with `rs_min_ifexpr_control(a: &dyn Sink) { a.emit() }` charging `['Net']` and exiting 1 in the SAME run. `deny Net` **exit 0** and **`pure` exit 0** — an affirmative purity certification. Reproduced identically in a second module with the trait cross-module, so not file- or order-dependent. **Asked of the other three and all charge**: swift the ternary receiver cross-file, java 6/6 (ternary/cast/switch-expression/conditional-array), ts 6/6 (ternary/paren/IIFE/`??`/`\|\|`). Mechanism MEASURED, not inferred: `resolve_recv_type_for` and `resolve_recv_traits` both ended `_ => None` / `_ => Vec::new()` and neither had an arm for `Expr::If`/`Match`/`Block`/`Unsafe`. A COMPILING 42-arm sweep of the whole receiver position put numbers on the class: **17 spellings ABSENT before, charging after.** | **CLOSED — candor-rust `293aa01` + `bf66ffd`.** `lang::merge_value_exprs` is ONE authority for a merge's value positions, consumed by all four resolvers and absorbing R101's private copy — leaving a narrower second copy is how R347, R380 and R446 each happened. The combine rules differ DELIBERATELY: in well-typed Rust every branch has one type, so the CONCRETE side takes any branch that answers and DECLINES when two disagree (declining is the pre-fix behaviour, so disagreement cannot regress), while the DISPATCH side UNIONS leaves. Two refusals, both about shadowing, because the resolvers key on bare names. **`bf66ffd` was the second half and was INSIDE the fix: syn parses a bare `None` as `Pat::Ident`, so the match-arm shadow refusal ate `(match o { Some(x) => x, None => b }).go()` — the ordinary spelling of an optional receiver.** An unsizing cast resolves too, gated: the first draft called `trait_leaves` on ANY cast type and moxcms-0.8.1's `where u32: AsPrimitive<T>` turned `(… as u32).min(…)` into a `num_traits#AsPrimitive::min` that does not exist — **caught in the A/B's ADDED column**, and that gate shipped without a fixture until `1c4e2c4` falsified it. A/B 1,595 crates: **ADDED 3, REMOVED 0, CHANGED 111**, audited field-by-field over all 111 — **LOSSES NONE on any field, zero new concrete effects** (all four `inferred` moves are `[] -> ['Unknown']`). REACH 2,104 hits. Ground truth from SOURCE: tracing-subscriber `subscriber_has_plf` is literally `(subscriber as &dyn Subscriber).is::<_>()`; dispatch2 `once::invoke_closure` INVOKES an opaque `F: FnOnce()` and was ABSENT. |
| R532a swift: **⟨0.39⟩ published a GENERIC PARAMETER as a dispatch key, and the charge went with it** — `useIt<T: Handler>(_ h: T)` published `Iface#T.handle` and charged nothing, where four other spellings of the same existential published `Iface#Backend.size` and charged `Net`. | 2026-09-22 | swift | ONE VARIABLE, same chain / conformer / binary: `_ h: Handler`, `_ h: Iface.Handler`, `any Handler`, `[Handler]` all give `inferred ['Net']` and `deny Net` exit 1; `<T: Handler>` and `<T> … where T: Handler` give `inferred []`, `dispatchesOn ['Iface#T.handle']`, **exit 0**. `Iface#T` names a type the owning package does not have. The LOCAL-protocol path has resolved the bound since R26 (both spellings -> `solo#Handler.handle`, both charge) — so this is **two implementations of one question that drifted, with only the newer one on the wire**. | **CLOSED — candor-swift `9f74856`.** Resolves the spelling through the declaration's own generic clause (`<T: P>` and `where T: P` unioned at the site that already reads them), applied at the ⟨0.39⟩ key site AND the §2 dep-join key **through ONE function so the key ASKED on and the key PUBLISHED cannot drift**. A LOCAL / std-pure / raw-value bound publishes NOTHING rather than a key; the CHA edge loop keeps the RAW spelling deliberately. CALIBRATED per §1b: degrading to the pre-fix `return owner` takes the suite 10/0 -> **5 failures**. **A/B SAFETY-ONLY, stated at the time rather than discovered later:** 9 packages, 27,165 rows, ADDED 0 / REMOVED 0 / CHANGED 0 with **REACH 0** — the branch needs a function-level generic bound in a file whose single declared dep import decides the owner, and these nine contain none. |
| R532 swift: **a conformance declared inside a FUNCTION or INITIALIZER body is invisible to ⟨0.39⟩ obligation 2, and the consumer has no hedge to fall back on** — the producer publishes no `interfaceUnion` entry, obligation 3 sees no visible conformer, and a FOREIGN abstraction carries no zero-witness `Unknown`. | 2026-09-22 | swift | Three packages, ONE VARIABLE — where the dependency's conformer is declared — byte-identical consumer, same policy, same 0.39.0 binary. **EXECUTED ground truth: the built program delivered `GET /charges HTTP/1.1` to a local listener.** Coordinator-verified single-package, which is the cleaner statement: file-scope conformer gives `L.handle ['Net']`, `fire ['Net']`, `Handler.handle ['Net']`; body-local gives NO `L.handle` unit, `fire` = `[]` + `dispatchesOn` only, and no `Handler.handle`. **A blanket `deny Net` catches BOTH (exit 1) because `register` still carries the effect by containment — only a SCOPED rule sees it**: `deny Net fire` exit 1 -> **exit 0**. Cross-package is worse, because the consumer's own scan has no `register` to carry anything. A control with NO conformer anywhere gives the byte-identical consumer row, so for a foreign abstraction the consumer never hedges at all, while the LOCAL-protocol path discloses `Unknown` + `unknownWhy` and exits 1. | **CLOSED — candor-swift `5c53e96`. COMPLETED, not hedged, and the arm was chosen on measurement:** rust's reason for hedging the same defect (*a body-local method has no unit*) does not transfer, because swift's `fns` IS the unit list. `finishBodyLocalTypes()` walks each body-local supertype-declaring decl with a throwaway sub-collector. **THE ENUMERATION IS THE MEASUREMENT:** the first cut merged wholesale and the A/B returned REMOVED 2 — a purity claim MANUFACTURED by a fix for a cardinal sin, in swift-syntax, where a body-local `enum TokenChoice` collided with a file-scope one and `caseAssoc` is keyed on the bare case name scan-globally. A/B 12 packages: ADDED 16 REMOVED 0 CHANGED 3, REACH 17 hits across 3 entries. The re-run sweep found a FIFTH host the four `.skipChildren` sites do not name — a DEFAULT-ARGUMENT expression — and corrected this row's own class line (a type-member computed property was already correct but collects PATH-QUALIFIED). PART 92 `c8` swift went PASSING and is RETIRED. It also surfaced [[R534]], reported not folded in. **The text below was true when written:** **OPEN — NOT A REGRESSION, a residual of the rung.** v0.37.0 on the same fixtures: BOTH arms exit 0 with the row ABSENT, so ⟨0.39⟩ closed the file-scope arm and left this one with strictly MORE disclosure than before. CLASS swept rather than assumed: silent for a conformer inside a **func / static func / init / subscript / deinit** body (struct, class, enum alike) — DeclCollector's four `.skipChildren` sites; CORRECT for file scope, computed-property body, closure body, nested type, and `extension S: P`. The one comment nearby (*nested decls attribute lexically via the body walk*) is TRUE about effect ATTRIBUTION and SILENT about the conformance being lost — which is the half that crosses the package boundary. **Not fixed on purpose:** the sound fix decides whether a body-local conformer becomes a UNIT, which is the same four-engine question as [[R529]] and [[R530b]]. PINNED FOUR-WAY 2026-09-22 as PART 92 arm c8_body_local_implementor, swift's line owned by this row — see [[R530b]] for the arm's shape. swift reads ABSENT (a purity claim) where rust discloses and ts completes. Unlike [[R533]]'s c9 lines these are PORT LAGS against text that already binds — ⟨0.35⟩ names a closure coerced to an interface an implementor, and ⟨0.39⟩ obligation 2 requires a package implementing a FOREIGN abstraction to publish an entry — which is the panel's reason this class is NOT a rung. |
| R531b ts: **an arrow or function expression RETURNED FROM AN IIFE OR FACTORY and stored in a const is silent at its call site** — the same `isArrowFunction` waiver as [[R531]], on a route the R531 fix does not reach: it is not an object-literal property and its enclosing unit is the factory. | 2026-09-22 | ts | **FOUND BY THE R531 REACH MEASUREMENT, IN REAL PUBLISHED CODE RATHER THAN IN A FIXTURE** — all 14 pre-fix silent sites on the 47-entry corpus are THIS shape, not R531's: cheerio `src.api.traversing`; bson `src.parser.utils.isUint8Array`, `src.extended_json.serializeDocument`; hono `Hono.route` / `Hono.dispatch`. Coordinator-verified at HEAD AFTER the R531 fix: `const c1 = (() => (n) => Math.floor(Math.random()*n))(); export function C1(n){ return c1(n) }` -> `C1` ABSENT from `functions[]` and `deny Rand src.index.C1` **exit 0**, while the byte-identical plain-arrow twin `C4` carries `['Rand']` and exits 1. Byte-identical on v0.38.3 / v0.39.0 / v0.39.1. | **CLOSED — candor-ts `a1b4cb6`** (+ `a51b889`). A pre-pass resolves every call and mints a position-keyed `<callable>@N` unit for a target that is an un-unit'd arrow/fn-expression, with [[R519]]'s containment edge to its PRE-mint owner — **and the edge was CHECKED for a non-property parent rather than assumed**, `enclosing()` answering for any node being what makes it hold. Iterated to a FIXPOINT, since minting refines `enclosing()`. **REMEDY (b) WAS BUILT AND RUN, AND THE RESULT THAT KILLED IT IS NOT THE FLOOD: `deny Rand C1` STAYS AT EXIT 0 UNDER (b)** — `Unknown` is not `Rand`, so a scoped gate still passes over a real `Math.random()`. It converts a silent under-report into a disclosed one that still certifies. Over 53 entries (b) recovered ZERO concrete effects, charged `Unknown` to 63 previously-absent callers of which **24 have targets the engine can SEE are pure**, and added a spurious reason to 202 rows. **BOUNDARY DRAWN AT THE WAIVER, NOT THE TRIGGER:** a 30-spelling sweep found EIGHT silent — IIFE, factory, IIFE-to-fn-expression, a ternary between two arrows, `.bind()`, an indexed array-literal element, an arrow returned from a class method, and one round-tripped through a generic `id<T>(x:T):T` — and the CORPUS then produced a ninth nobody had listed, `X.prototype.m = function(…)`. **A/B 53 entries: ADDED 121 REMOVED 0 CHANGED 248** (narrow `inferred` 121/0/8); removals audited in full and EMPTY; of 248 changed, **8 gained a real EFFECT and 0 gained Unknown-only**. **UNLIKE [[R531]] THIS MOVES SHIPPED VERDICTS** — coordinator-verified on the corpus entry: shipped pino's `pino.pino` and `pino.default` go `['Unknown']` -> `['Fs','Unknown']`, with the newly minted `lib.tools.<callable>@9595` carrying the `Fs`; that is `createArgsNormalizer` returning `function normalizeArgs(…)`, read from pino's own source. Also light-my-request's `Chain` +`Net`,`Rand`, avvio's `Boot` +`Clock`,`Rand`, three axios bin functions +`Exec`. **REACH MEASURED IN BOTH DIRECTIONS BECAUSE THEY DISAGREE:** the PRE engine instrumented at the silent condition reads **539 call sites across 13 entries**, the POST mint probe 111 across the same 13 — sites versus distinct arrows, and the larger number is the one that describes the defect. **THE FIRST BUILD DOUBLE-MINTED over R531's property mint** — bson's `serializeDocument` moved from `<structural>@10049.Binary` to `<callable>@10057`, one body through two units, exactly the double count PART 82 pins — **invisible on the narrow key and caught only by auditing the `calls` field**; the lookup is now spelled ONCE as `callTargetUnit`, shared by both walks. Calibrated per §1b: 16 arms RED against the parent commit. PARTs 81, 82 (`count:1`) and 92 (`c8` ts) unchanged. **Corrects this round's brief:** published `scan.mjs@0.39.1` is no longer byte-identical to HEAD, since R531 landed after 0.39.1 was staged — so PRE had to be HEAD, or the A/B would have conflated R531 with R531b. |
| R531 ts: **a call to a function-valued member of an UNTYPED object literal was SILENT — the caller vanished from `functions[]` entirely.** `const u = { roll: (n) => Math.random()*n }` has no contextual type, so it never reached `mintStructuralMembers`; `realDecl` unwraps `u.roll` to the ArrowFunction and the call walk waves an arrow through as *already walked lexically* — true of the arrow's LEXICAL OWNER, false of the CALLER. | 2026-09-22 | ts | **PRE-EXISTING, NOT an R519 regression** — byte-identical on published v0.38.3, v0.39.0, v0.39.1 and HEAD. One file, one package, no chain: `deny Rand A1` exit 0 while `deny Rand A8` (same body, ordinary function) exits 1, with `analyzed:{count:15}` and NO `unanalyzed` — SPEC §2 rule 3's affirmative purity claim. **THE SPELLING ASYMMETRY IS WHY IT SURVIVED:** `{ roll(n){…} }` — method SHORTHAND on the SAME untyped literal — is a MethodDeclaration, misses that arm, and discloses an honest `Unknown[callback:roll]`. One literal, two spellings, one loud and one silent. **My brief's incidental premise ('untyped parameter') was a RED HERRING** — a typed parameter is equally silent; the agent widened the boundary and said so. | **CLOSED — candor-ts `bddbc0e`.** TWO HALVES, NEITHER SUFFICIENT ALONE: mint every object literal's members rather than only a contextually-typed one's, AND resolve an arrow/fn-expression at the call site to the `nodeName` minted on the PROPERTY (resolution arrives at the initializer; the mint is keyed one node up). **Safe to widen ONLY because [[R519]] made minting ADDITIVE** — without the containment edge this moves every `{ onClick: () => … }` body out of its enclosing function, R519's own defect multiplied. The ALIAS arm is deliberately NOT widened (a literal in destructuring-TARGET position moved a setter invocation off the assigning function; red on the first build). Direction is PRECISION-POSITIVE, not over-charge — it RESOLVES rather than hedges; the rejected alternative was measured to charge `Unknown` to a caller of `{ twice: (n) => n*2 }`. A/B 47 entries, PRE = published npm 0.39.1 proven byte-identical to HEAD: ADDED 92 REMOVED 0 CHANGED 39; removal set audited IN FULL by mechanism, with exactly ONE `inferred` change ground-truthed from bson's source as a genuine precision gain. **NOT IN THE R529/R530b/R532 CLASS — a review panel measured this and I had it wrong.** I synthesised four engine findings into one class (*an implementor the engine cannot NAME*); ts does not belong. Re-measured at HEAD on the R531b fixture: `C1`/`C2`/`C3` ABSENT, `C4` `['Rand']`, `analyzed.count 9`, no `unanalyzed` — **the fixture has NO interface, NO CHA and NO sibling, so there is no left side to toggle.** The silence is unconditional rather than sibling-induced. This is [[R452]]'s shape — a call the checker RESOLVED to a body the engine READ, dropped with no unit and no `Unknown` — not a dispatch-universe defect. ts's actual analogue of the real class is [[R512]], already closed. **Two further corrections to my framing from the same panel:** the class boundary is NOT *cannot name* (java CAN name `lambda$mk$0` and still misses it) but **index != walk**, which is exactly why [[R529b]] and [[R529c]] fell out of the same rust sweep; and the class is THREE engines, not four. |
| R530a java: **the ⟨0.30⟩ peek parsed the policy with an EMPTY alias vocabulary, so a config-defined `Unknown[<alias>]` withheld `outOfScope` and `scannedUnder` entirely** — the disclosure half of [[R526]]. | 2026-09-22 | java | **CONFIRMED TO BE A THREAD BOUNDARY, NOT the half-converted call site the row PREDICTED** — the row's own hypothesis was wrong and measuring is what caught it. `ctx().unknownAliases` is thread-local; the peek runs on its own `candor-peek` thread with a fresh context, and `Policy.parsePolicy` reads that map as its ONLY ctx input (enumerated mechanically, not eyeballed). The main thread resolves the vocabulary via `policyVocabularyFor` — anchored at the POLICY file — immediately before `peekExcluded`, and nothing carried it across; `primarySourceFileRecords`, captured two lines above for exactly this reason, is the precedent the alias map was omitted from. Fails CLOSED, so no gate went green; what was lost is the disclosure the rung was justified on. Recall measured as an envelope diff over five real repos: **32 out-of-scope findings withheld pre-fix** (0/1/3/0/28). | **CLOSED — candor-java `1d51e23`.** Copied from the RESOLVED gate-side context rather than re-derived from `peekConfig`: that is the TARGET's config, and a policy outside the scan root resolves its aliases from a different file. Calibrated per §1b — the fixture was written FIRST and run RED at `f2749b1`. **PART 55's XFAIL line `(java, deny-unknown-alias)` went XFAIL-PASSING and has been RETIRED (candor-spec `3f87ba2`)**; the table is empty again and all four engines agree, 44 cells / 0 disagreements. Total lifetime of that exception: under a day — which is the argument for a ledger over a relaxed invariant. A/B ADDED 0 REMOVED 0 CHANGED 0 with REACH 5/5 is an OVER-CHARGE CONTROL ONLY, since `corpus-ab` keys on `functions[]` and this fix moves envelope keys. |
| R529b **A BLOCK-NESTED `extern \"C\"` BLOCK LOST THE FFI DISCLOSURE.** `fn wrap() { extern \"C\" { fn ffi(); } unsafe { ffi(); } }` reads PURE and is ABSENT, while the module-level spelling of the identical program discloses `Unknown` + `native:extern fn`. [[R529]]'s mechanism, a different index. | 2026-09-22 | rust-scan | **FOUND BY WIDENING R529'S AUDIT PAST ITS OWN TRIGGER, AND IT FALSIFIES A SAFETY ASSERTION IN THIS FILE.** The ⟨0.29⟩ footnote said direct `extern \"C\"` was *re-verified genuinely sound THROUGHOUT* and that `ForeignMod` handling *records EVERY declared name*; both hold for the module-level spelling ONLY, and PART 79 / PART 46 pin exactly that spelling — which is why re-verification kept agreeing with itself. Corrected in place rather than quietly. Reproduced by the coordinator on the SHIPPED v0.39.1 binary: module `wrap` = `Unknown`/`native:extern fn`, body `wrap` = ABSENT. A/B 1,595 crates: ADDED 409, REMOVED 0, CHANGED 111 — the 409 were silently-pure functions. REACH 4,284 hits / 73 crates; over those 73, `deny Unknown` 69->73 and `deny Unknown[native]` 47->73. Ground truth: wasi-0.14.2 `src/bindings.rs:184`, `pub fn get_stdin()` declaring `extern \"C\" { fn wit_import0() }` in its own body — the wit-bindgen idiom. | **CLOSED — candor-rust `34ea374`.** Names ride R529's block-depth walk into `extern_fns` — one question, one answer, not a parallel index. Residual STATED: the leaf set is crate-wide, so `extern \"C\" { fn drop(..) }` in a body hedges every bare `drop(..)` in that crate — over-disclosure, and the same residual the item-level arm has always carried. **PART 46's rust arm should gain the body-local spelling four-way** (java: a method-local class; ts: a function-local `declare`; swift: a nested type conforming inside a func). |
| R529 **AN IMPLEMENTOR WRITTEN INSIDE A BLOCK IS IN NO PASS A INDEX, SO THE DISPATCH CERTIFIED PURITY.** `impl Backend for NetBackend` inside `fn register`'s body is a `Stmt::Item`; `collect_decls` / `collect_foreign_trait_impls` / `collect_trait_decl_quals` recurse through `Item::Mod` and nothing else, while Pass B's collector DOES walk bodies. So the `Net` is charged to `register` by containment and `NetBackend::size` is never minted — the CHA universe holds every implementor except the one it cannot NAME. | 2026-09-22 | rust | [[R475]]'s TOGGLE ONE SPELLING OVER, and a cardinal sin. The zero-implementor case already reads `Unknown`; what makes this SILENT is adding a module-level PURE implementor. BOTH ⟨0.39⟩ obligations miss it: obligation 2 publishes no `interfaceUnion` entry (`foreign_impls` has no key for a body-local impl) and obligation 3 finds no contributor, so a chained consumer SUPPLYING THE IMPLEMENTOR ITSELF is told nothing by any report in the chain. Two-package fixture, both arms compiling, dep report byte-identical: module-level gives `app_size ['Net']` + the union entry; moved into a fn body `app_size` is ABSENT, no `Unknown`, no `invisible`. Gate-level with a SCOPED policy (so `register`'s containment charge cannot answer for it): `deny Net Unknown handler` exit 0 -> exit 1. Reproduces SINGLE-CRATE, no chain needed. | **CLOSED — candor-rust `5f00463`** (+ `c9a0989`, one authority for the wire key rather than two copies of the derivation). A HEDGE, not an implementor set: the body-local method has no unit, so `trait_impls` would edge to nothing (R452's deliberately-unhedged case) and would move the bound and ambiguity count. A/B 1,595 registry crates, pre = `5e6843e`, 320,292 -> 321,084 rows: **ADDED 792, REMOVED 0, CHANGED 1,046**; 5 of the 792 were functions previously ABSENT. REACH 1,901 hits / 174 entries. Ground-truthed from SOURCE: serde-1.0.228 `src/core/de/impls.rs:136`, compact_str-0.9.1 `src/lib.rs:2207`. **PART 92 HAS NO ARM FOR THIS SPELLING** — c7 is MODULE-nested (R513) and `gen_chained_dispatch.py` contains zero indented `impl … for`, so the hedge provably cannot fire there. Needs `c8_body_local_implementor`, four-way. **WHAT THE HEDGE DOES NOT BUY, measured by a review panel on HEAD and recorded here so the row is not read as more than it is:** on the `local_both` fixture, `term_size` reads `['Unknown']` + `dispatch:Backend.size` — the silence is closed — but **`deny Net term_size` still exits 0 and `pure term_size` still exits 0** over a real socket. Only `deny Net Unknown` and `deny Unknown[dispatch]` exit 1. So the hedge converts a SILENT under-report into a DISCLOSED one and stops there, which is ⟨0.35⟩'s disjunction arm (b) and is legitimate — but the two most common scoped policies stay green. Arm (a), completing the dispatch, is what would redden them, and for rust that means minting a unit for a body-local method, which is where [[R529c]]'s leaf-keyed `fields` collision lives. **The panel's ruling: the hedge is the right stopping point for rust until someone prices unit-minting against R529c — it is not the wrong answer, it is the cheaper arm of a disjunction the SPEC explicitly licenses.** |

## ⚠ OWED TO TOM — a gate-semantics question raised by R105's fix (2026-09-02)

R105 replaced "pick the `#[cfg]` arm that happens to be last in the file" with an honest
`Unknown` + `ambiguous:` when the arms classify differently. That is unambiguously more truthful. **But it
changes what a bare `deny <E>` does, and the change deserves a ruling rather than a default.**

Measured on two crates identical but for arm order, one arm `Fs` and one `Env`:

| policy | pre-fix (arm A) | pre-fix (arm B) | post-fix (both) |
|---|---|---|---|
| `deny Fs` | 1 | 0 | **0** |
| `deny Unknown` | 0 | 0 | **1** |
| `deny Fs Unknown` | 0 | 0 | **1** |

Post-fix the two orders agree under every form — that is the point, and it is right. The question is the
first row. **Pre-fix, `deny Fs` fired for roughly half the affected crates by coin-flip. Post-fix it never
fires**, because the honest answer is `Unknown` and a bare `deny <E>` does not act on `Unknown`.

**The engine now KNOWS `Fs` is one of exactly two possibilities and does not fail closed on it.** That is
defensible — `deny Unknown` is the documented upgrade and it does fire — but it is a different posture
from the rest of the family, where an effect the engine can name is denied. The alternative is: **a
`deny <E>` fires when `<E>` appears among the arms of an `ambiguous:` disclosure.**

This is a **cross-engine SPEC question**, not a rust one — java/ts/swift have not been measured for the
same shape. No engine or spec text was changed. **Tom's call.**

### UPDATED 2026-09-10 — THERE ARE NOW THREE ANSWERS IN THE FAMILY AND TWO OF THEM ARE LIVE IN ONE ENGINE

R140/R287 shipped a THIRD posture for the identical question, and it did not replace the second:

| posture | what a cfg-duplicated alias reports | where it is live |
|---|---|---|
| PICK an arm | whichever `use` was written last | pre-R105 — the defect both rows were filed against |
| HEDGE | `Unknown` + `unknownWhy:["ambiguous:cfg-duplicated alias"]`, **no surface captured** | R105, `scan.rs:2009`, the re-export route |
| UNION | every arm's effects charged, **surface captured** | R140/R287, `collector.rs`, the direct route |

**They are both reachable, and which one you get is decided by something with no bearing on the question.**
Measured (R375): two programs identical except that two `use` items are `pub use` rather than `use` —
the private spelling unions to `["Exec"]`, the `pub` spelling hedges to `["Exec","Unknown"]` with an
`ambiguous:` reason. So a bare `deny Exec` fires on one and not the other, and `allow Exec "<lit>"`
behaves differently again, because only the union captures a surface. That is the R347 shape — two
mechanisms answering one question differently — inside the rung that was meant to settle it.

The postures also disagree about DISCLOSURE, which is the part worth ruling on. `scan.rs:2006` says the
hedge captures no surface deliberately, *"so an empty surface cannot read as a complete one and let
`allow <E> <lit>` certify a claim the engine never made"*. The union captures one. Both arguments are
good and they cannot both be the rule.

**What the ruling has to decide, in one sentence each:**

1. **Is the answer the UNION or the HEDGE?** The union's case: the engine already unions a `#[cfg]` arm
   set written INSIDE one body (`fn go() { #[cfg(unix)] {fs::write} #[cfg(windows)] {env::set_var} }`
   measures `['Env','Fs']`), and PART 10's expected answer depends on it — so the hedge condemns
   behaviour the suite already pins. The hedge's case: an effect from an arm that is not compiled on
   this target is a fabrication, and `deny Unknown` is the documented way to fail closed on it.
2. **If the union wins, does it capture a SURFACE?** A union of arms where only one carries a literal
   currently yields a positive effect claim with a partial command set, and `allow Exec "/bin/echo"`
   fails closed on the lossy arm (`[AS-EFF-008]`) — measured, so this is a precision question, not a
   soundness one. But it is the question `scan.rs:2006` answered the other way.
3. **Does it change what a bare `deny <E>` does?** — the original question above, unchanged, and the
   union answers it YES by construction where the hedge answers NO.

### ⇢ THE OWED FIXTURE, BUILT AND MEASURED 2026-09-13 (candor-rust `8539a63`) — ARGUMENT IS NOW RESULT

The question above has been blocked on one thing: *"whether a union surface can CERTIFY where a hedge
failed closed"* is **argument, not result, and two attempts never reached the shape.** It is built now.
No engine or spec text was changed to do it.

**TWO THINGS IN THE RECORD ABOVE ARE STALE, AND SOMEONE REPRODUCING WOULD BE MISLED BY BOTH.**

1. **The route discriminator is a CROSS-MODULE RE-EXPORT, not `pub use`.** The 2026-09-10 update says
   two programs differing only in `pub use` vs `use` take different routes. On the current binary they do
   not — a same-file `pub use` UNIONS, identically to `use`. The hedge needs the alias to be re-exported
   from ANOTHER module and consumed across it (`mod shim; use crate::shim::act;`). Anyone re-testing with
   the documented `pub use` fixture measures no divergence and concludes the hedge is gone. It is not.
2. **`deny Fs` on the union route is now 1, not 0.** The 2026-09-02 table's headline row — the question's
   original subject — no longer reproduces.

**THE MATRIX.** One program, two routes, all four policy forms. Both controls included, and every column
is proven able to answer 0 so no cell is a stuck red:

| fixture | `allow Fs /tmp/benign` | `deny Fs` | `deny Env` | `deny Unknown` | `--strict` |
|---|---|---|---|---|---|
| CONTROL: no effects at all | 0 | 0 | 0 | 0 | — |
| CONTROL: arms AGREE (`fs::write` both) | **0** | 1 | 0 | 0 | — |
| arms DISAGREE, same file → **UNION** | **1** | **1** | **1** | 0 | 2 |
| arms DISAGREE, cross-module → **HEDGE** | **0** | **0** | **0** | 1 | 2 |

**ANSWER TO QUESTION 2, AND IT INVERTS THE CONCERN THAT MOTIVATED IT.** `scan.rs:2006` withholds the
hedge's surface *"so an empty surface cannot read as a complete one and let `allow <E> <lit>` certify a
claim the engine never made"*. Measured: **the UNION does not certify — it exits 1** (and the agreeing
control exits 0, so that 1 is a real fail-closed, not an always-red fixture). The union charges
`['Env','Fs']`, withholds `paths`, and sets `incomplete:['Fs']`. **It is the HEDGE that exits 0 under
`allow Fs`** — not by capturing a surface, but by renaming the effect to `Unknown`, which a bare
`allow Fs` does not constrain. The posture defended on the grounds of not letting a policy certify is the
one that lets this policy certify.

**So the live union posture is a FOURTH option none of the three rows above names:** union the EFFECTS,
WITHHOLD the surface, DISCLOSE `incomplete`. It has the union's `deny` behaviour and the hedge's surface
caution, and it is already shipped on the same-file route.

**IN FAIRNESS TO THE HEDGE, STATED BECAUSE IT WEAKENS THE CASE ABOVE:** neither posture is silent under
every form — each is silent under a different one (`deny Unknown` fires on the hedge and not the union),
and **`--strict` returns 2 for BOTH**, so a strict gate is blind to the difference. The asymmetry bites
only the BARE, ordinary forms — `deny Fs`, `allow Fs <lit>` — which is what most policies are.

**THIS DECIDES NOTHING — IT DISCHARGES A DEBT AGAINST A RULING ALREADY MADE.** Tom RULED UNION on
2026-09-12 (below). That ruling named two things it did NOT do, and the first was: *"the owed FIXTURE
stays owed … the ⟨0.37⟩ PART should not claim that property until a fixture demonstrates it."* This is
that fixture. The property is demonstrated, so the PART may now claim it. **Nothing here reopens the
ruling and nothing here needs a second decision** — writing "Tom's call" over a settled question is the
stale-snapshot failure this register exists to prevent, and the first draft of this section did exactly
that.

**BUT ONE PREMISE OF THE RULING IS FALSE AS MEASURED, which is the part worth acting on.** The ruling
reads *"UNION reverts nothing — the engines already do it — so this ruling CONFIRMS `main` rather than
changing it."* **`main` only unions on the SAME-FILE route.** The cross-module re-export route still
HEDGES, and the table above shows the hedge is the posture under which `deny Fs`, `deny Env` and
`allow Fs <lit>` are all silent. So implementing UNION on the alias route is real work, not a no-op — and
until it is done the two routes answer one program two ways, which is the R347 shape the family already
treats as a defect rather than a choice.

**WHAT IS STILL NOT MEASURED, so the boundary is not left to be found:** this is rust only. java/ts/swift
have not been put on the same fixture. Per the ruling's own scoping note that is at most a TWO-engine
question — Rust `#[cfg]` and Swift `#if` have conditional imports; java and ts have no such construct and
are excluded with a stated reason.

### RECOMMENDATION ON THE TABLE (2026-09-11) — reviewed adversarially, two of three reasons withdrawn

**Rule UNION for an UNDECIDABLE `#[cfg]`-duplicated binding: "decide what is decidable, union what is
not."** This CONFIRMS OR AMENDS RELEASED TEXT rather than deciding a fresh question — ⟨0.36⟩'s scoping
paragraph (`SPEC.md:4345-4353`, in tag `v0.36`) already says of bindings *"the answer is the same
union; but no conformance row pins it"*.

**THE ARGUMENT I FIRST GAVE WAS WRONG AND IS WITHDRAWN.** I claimed conformance PART 10 pins the
in-body `#[cfg]` arm-set union and therefore binds this ruling. It does not: PART 10's fixtures are
cfg-gated **definitions** (`run.sh:2668-2677`), the only `cfg(` in the whole runner, and there is none
in `conformance/rust/`. **Ruling HEDGE would turn no part red and contradict no conformance
obligation.** The correct argument is the opposite shape: a HEDGE ruling REVERSES released spec text
and R287's recorded ruling, while UNION confirms both.

**Union is not fabrication under SPEC's own definition** — §4 defines it as *"asserting something the
engine did not read from the code"*, and both arms were read. The clause that decides it is §4.0
(`SPEC.md:4333-4342`): `{Unknown}` alone is not `⊤`, so replacing a determined effect with a hedge
**WITHDRAWS** it and `deny Fs` stops firing on a call that reaches `Fs` in one configuration — *"the
noisy-direction unsoundness, not over-disclosure."*

**TWO AMENDMENTS to the question as originally put:**
1. **Scope to UNDECIDABLE predicates.** A declared-but-inactive `feature = "x"` arm is DECIDABLE and is
   already dropped (`use_item_applies`, R140's decidable half) — charging it WOULD be fabrication,
   because the engine read enough to know it binds nothing. Unscoped, the ruling covers a case where
   union is wrong.
2. **"Captures a surface" should read "carries the CALL SITE's own literal onto every arm edge."** The
   pushed edge currently carries `str_arg: None, argc: 0`, which says "no literal here" when the call
   site has one — that is R375's residual, and fixing it is part of the ruling rather than after it.

**COST, measured against what is tagged vs on `main`:** UNION reverts nothing. HEDGE un-commits
`20e4ac7`/`4c8420c`/`5780ab9` (unreleased, so un-commit not un-release), amends released `v0.36` text,
reverses R287, and permanently diverges the `use` shape from the definition shape on identical
undecidability.

**⟨0.36⟩ CONTAINS TWO SENTENCES THAT CANNOT BOTH STAND** and the ruling must strike one either way: the
scoping paragraph's *"a hedge to `Unknown` on that shape is over-disclosure and permitted"* against the
§4.0 paragraph's *"`{Unknown}` ALONE is not"* conformant.

**STILL OWED, AND LABELLED AS REASONING NOT MEASUREMENT:** whether a union surface can ever CERTIFY
where a hedge would have failed closed. For `Exec` the publish and mask predicates coincide
(`is_cmd_naming_method`) so it cannot; for `Fs` they do NOT coincide — publish is any Fs leaf, mask is
`is_fs_path_arg`. The argument that it still cannot leak is that the pushed edge shares the WRITTEN
call's argument expression, so any literal published from one arm is the same literal handed to the
other. **Two fixtures were built to measure this and NEITHER reached the shape:** the first landed on
R372's type route (the caller vanished entirely), the second produced no caller unit for a bare
associated-fn call. The engine's union is live (the R140 fixture still charges). **The fixture is owed;
until it exists this sub-question is an argument, not a result.**

**This is now blocking the ⟨0.37⟩ conformance PART**, which cannot pin an answer the family gives two
ways. Conditional compilation is unpinned cross-engine today: there is no `cfg(` anywhere in
`conformance/rust/src/lib.rs` and no `#if` in `candor-swift`'s `Cases.swift`. **And the PART can only
cover TWO engines** — Rust `#[cfg]` and Swift `#if` have conditional imports; Java and TypeScript have
no such construct, so they are exclusions with a stated reason under `part_declarations.py`, not a gap.
~~**Tom's call, and the engines should not be changed further until it is made.**~~

**RULED 2026-09-12 — UNION. Tom's call, and he noted the principle was already settled:** the `by_leaf` half of R222 took UNION on 2026-09-05 after a Fable review, on the ground that a genuinely differing `#[cfg]` twin does not go quiet but goes to the union of both arms — *a hedge replaced by a COMPLETE answer, not a withdrawal*. What stood open here was applying that same answer to the SIBLING route (the `use`-alias shape rather than the definition shape), and this section's own cost analysis argued for it: HEDGE would *"permanently diverge the `use` shape from the definition shape on identical undecidability"*. **UNION reverts nothing — the engines already do it — so this ruling CONFIRMS `main` rather than changing it, and the ⟨0.37⟩ conformance PART is unblocked.**

**TWO THINGS THE RULING DOES NOT DO, stated so neither is assumed.** (1) The owed FIXTURE stays owed: whether a union surface can ever CERTIFY where a hedge would have failed closed is still an argument, not a result — two attempts never reached the shape. It does not block, because UNION is what ships today either way, but the ⟨0.37⟩ PART should not claim that property until a fixture demonstrates it. (2) **The SPEC amendment is DEFERRED PAST 0.36.2 on purpose.** ⟨0.36⟩'s two contradictory sentences live in SPEC.md, and the versioning ladder decides a patch mechanically by `git diff v<prev>..HEAD -- SPEC.md` being empty. Striking one before the cut would make 0.36.2 not-a-patch and change what the release means. The cut ships the gate-bypass fixes; the amendment follows it.

## THE REUSABLE QUESTION FROM THE R109–R115 VEIN (2026-09-02)

Seven rows came out of one week-old-looking gap, and the question that produced five of them is not about
web globals at all:

> **Where does the mechanism that answers a question actually FIRE, and does the syntax that ASKS the
> question ever reach it?**

**"Not charged" and "not reachable by this mechanism" are different facts, and only the second one tells
you where to look next.** R111 wrote that distinction down as an aside — that `timeOrigin` and
`nodeTiming` are property accesses while the classify site fires only on calls — and that aside found
R113, R114 and R115.

Three corollaries, each measured rather than reasoned:

1. **A curated table is bounded by its call site, not by its contents.** Both of candor-ts's web-global
   tables are exhaustive-looking and sit under `isCallExpression || isNewExpression`. No amount of adding
   members reaches a property.
2. **Agreement between two resolution paths is worthless as a signal** (R111). R113 is the fourth instance
   of both-arms-agreeing-while-both-are-wrong, and it was sitting inside a comment that used the agreement
   as the reason to dismiss it.
3. **The same site can fail in both directions at once.** R113/R114 are silent under-reports; R115 is a
   fabrication that fails `deny Fs` on a function that performs nothing — same property machinery, opposite
   errors. Auditing only the miss direction would have found three of four.

**And the class turned out BOUNDED, which is the actionable part.** A table keyed on resolved-declaration
identity fires on **30 of 28,730 property accesses (0.104%)** across six real repos — as narrow as the
call-arm branches it would sit beside. The naive fear ("charging property reads would be catastrophic") is
true only of the shape nobody proposed: 52% of those accesses fall in the no-symbol bucket. **Treat 0.104%
as a floor** — that corpus is TS libraries, and `document.cookie` appears once in it, `localStorage.x = v`
zero times; a browser-application corpus would price it differently and was not built.

