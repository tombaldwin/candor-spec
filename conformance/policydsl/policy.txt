# Battery for the cross-impl policy-grammar differential (SPEC §6.2).
# Exercises all four rule kinds AND the edge cases the engines used to disagree on.
deny Net Db domain
deny Exec
deny Unknown api
pure parse
deny Net foo Db
deny notaneffect
allow Net in billing api.stripe.com hooks.stripe.com
allow Exec in ci git
allow Fs in config /etc/app
allow Net github.com
allow Clock whatever                  # no literal surface for Clock — dropped (the battery's negative case)
allow Db in billing ledger.* customers
forbid domain -> infra
forbid app::web -> app::db
forbid bad
forbid glued->arrow                   # the arrow must be its own token — dropped
forbid web -> db trailing-tokens-ignored   # tokens past <B> are ignored, rule kept
nonsense line
deny Exec  # inline comment: must be stripped, scope stays empty (not "#")
allow Net in stripe api.stripe.com # trailing comment must not become a host
allow Net in
allow Fs # comment-only after the keyword
deny Net Db                       # NBSP is NOT a separator (ASCII-only); token unrecognized -> dropped by all engines
deny Net Net                          # duplicate effect tokens dedup to a set (all engines)
allow Net dup.example.com dup.example.com   # duplicate values dedup
deny Net Unknown[reflect,dispatch] api      # reason-scoped Unknown: narrow to those classes (all engines)
deny Exec Unknown[dynamic]                   # the `dynamic` alias expands to every genuine class
deny Db Unknown[*] svc                        # `*` ⇒ all classes ⇒ empty filter (like bare Unknown)
deny Fs Unknown[bogus,reflect] io            # an unknown reason-class token is dropped with a warning; reflect kept
deny Net Unknown[risky] api2                  # config `unknown-alias risky` → reflect,native,dispatch (discovered .candor/config)
deny Exec Unknown[telemetry] jobs             # config `unknown-alias telemetry` → indirect
deny Db Unknown[reflect] audit                # `reflect` stays the built-in class (the config alias `reflect` is REJECTED as reserved)
deny Net[unknown-host] domain2                # Net destination-class: narrow to unknown-host (the security gate; all engines)
deny Net[known-telemetry,unknown-host] obs    # multiple destination classes on one rule
deny Net[*] anywhere                          # `*` ⇒ all destinations ⇒ empty filter (like bare Net)
deny Net[bogus,unknown-host] mixed           # an unknown destination-class is dropped with a warning; unknown-host kept
