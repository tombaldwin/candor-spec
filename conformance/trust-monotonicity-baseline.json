{
 "_": [
  "P3 TRUST MONOTONICITY — the ratchet (conformance PART 26, gen_trust_monotonicity.py).",
  "",
  "THIS FILE RECORDS DEFECTS, NOT EXPECTED ANSWERS. Nothing here tells the property what an effect should",
  "be: the engine's own `trusted` and `unchained` arms remain the only oracles. Each entry says only",
  "'this (engine, degraded-arm, shape) is known-broken today, and here is how to reproduce it without the",
  "generator'.",
  "",
  "IT RATCHETS BOTH WAYS. An unwaived failure fails the suite; a waiver whose cells now all pass ALSO",
  "fails it, so deleting the entry is part of the fix. `split: \"*\"` waives every shape for an",
  "(engine, arm) pair — a trust defect is a property of the JOIN, not of the call shape — and the failing",
  "shape list prints on every run so a shrinking defect stays visible.",
  "",
  "Every entry was found by the generator and then RE-DERIVED from a hand-written two-package fixture",
  "with HAND-WRITTEN dep reports, so no waiver rests on the generator being right."
 ],
 "known": [
  {
   "engine": "swift",
   "arm": "empty_zero",
   "split": "*",
   "why": "§2 ⟨0.24⟩: `analyzed.count == 0` IS \"I JUDGED NOTHING\", AND A CONSUMER MUST NOT READ IT AS FULL COVERAGE — the row-1 obligation is to treat the package as NOT COVERED. ⟨0.24⟩ FIXED in candor-swift `b41b1df` — the count-0 rule lands in `Deps.swift`'s `loadDepReports`, consumer-side coverage bookkeeping. NARROWED, not retired: 40 residual cells over [field, implicit_conv, lazy_init, loop_elem, method_recv], every one reading `unchained=(ABSENT)` — the same pre-existing per-function `invisible` attribution gap java has two of. CONTROL SEPARATION now SEPARATED on 24/80. CORPUS STUDY 2026-08-03 REFUTES THE PROPOSED FIX, so this waiver now rests on a measurement rather than a plan. The idea was to key the per-fn attribution on `Call.extOwner` (the resolved receiver type) instead of the call FORM. Instrumented over swift-syntax: 451 unresolved member calls, 71 distinct receiver names, and `localTypes` calls 100% of them NONLOCAL — so the gate would fire on every one, which is the sweep-[33]/[36] flood entire. Breakdown: 42.8% typealias/module/project-ish (the largest single name is swift-syntax's OWN typealias `RawSyntaxBuffer`, 108 records — `localTypes` misses typealiases, so the denominator is wrong before any denylist question), 25.1% stdlib, 14.0% markers (`Self` and candor's own `<super>`), 11.1% GENERIC PARAMETERS (T/S/Target — bound per declaration, so no static list can classify them), 7.1% macro-ish. A syntactic engine cannot attribute a member call's receiver type to a SPECIFIC blind module without scanning that module; the existing guard selects the two cases where the target IS identifiable. rust manages it because it has type resolution.",
   "repro": [
    "swift-syntax, CANDOR_EXTOWNER_DUMP instrumentation on Driver.swift's unresolved member-call arm: 451 records, 0 classified LOCAL.",
    "The five shapes remain ABSENT from `functions` in the unchained arm; rust discloses invisible:['deplib'] on every one, which is the in-family precedent and needs types."
   ]
  }
 ]
}